/** ATR scope token required for `network.tailcat_connect` tool calls. */
export const TAILCAT_EPHEMERAL_ATR_SCOPE = "network:tailcat_ephemeral" as const;

export const TAILCAT_CONNECT_TOOL_PATTERN = "network\\.tailcat_connect";
