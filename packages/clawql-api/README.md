# clawql-api

Composition root for ClawQL ([#308](https://github.com/danielsmithdevelopment/ClawQL/issues/308)): plugin registry, `search` / `execute` Effect entrypoints.

**Phase 1:** `createClawQLApi()` + `ClawQLApi` service; MCP in `clawql-mcp` will delegate here in a follow-up PR.
