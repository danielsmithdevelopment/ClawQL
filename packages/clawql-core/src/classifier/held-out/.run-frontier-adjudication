# Marker to trigger Fast Decision frontier adjudication (Harvey LAB style).
# Present on a PR/push that also touches this path → GHA runs live judge + GLiNER.
# First non-comment, non-empty line is the suite name (v0.1 | v0.2-harvey).
# Remove or empty this file after the run lands (do not leave permanently on main
# unless you intend schedule-like PR noise).
#
# Example PR: open a branch that only adds/updates this marker to v0.2-harvey.
v0.2-harvey
