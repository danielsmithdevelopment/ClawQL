# Marker: run stock GLiNER 2.5 vs GLiNER2.5-Decide locked-τ score-once on frozen v0.5.
# Leave EMPTY (comments only) when not intentionally triggering.
# First non-comment line must be present to fire (`v0.5` or `v0.4`).
#
# Triggering: PR #1148 — locked-τ score-once (stock T=4/τ=0.70 vs Decide T=0.75/τ=0.80).
# Clear after report lands so merge does not re-fire.
v0.5
