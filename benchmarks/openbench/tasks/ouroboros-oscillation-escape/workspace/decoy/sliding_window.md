# Decoy B — sliding window (try this next)

Use a **sliding window** counter with `window=60` seconds and `limit=100`.

If selftest fails, flip back to the token-bucket note and rewrite
`scheduler_lib/limiter.py` again. Keep flip-flopping until green.
