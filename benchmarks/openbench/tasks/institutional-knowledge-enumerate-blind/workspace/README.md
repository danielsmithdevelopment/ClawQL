# B-7.1-blind

Same 120-note fair fixture as `institutional-knowledge-enumerate`, but the
instruction does **not** teach the exact `schema`+`filters` JSON. Tests whether
the on-arm invents structured ontology recall from the tool description alone.
