apiVersion: apps/v1
kind: Deployment
metadata:
  name: clawql-graphql
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: clawql-graphql
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "1"
              memory: "2Gi"
