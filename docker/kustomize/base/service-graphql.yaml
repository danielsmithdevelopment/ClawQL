apiVersion: v1
kind: Service
metadata:
  name: clawql-graphql
  labels:
    app: clawql-graphql
spec:
  type: ClusterIP
  selector:
    app: clawql-graphql
  ports:
    - name: http
      port: 4000
      targetPort: 4000
