apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clawql

resources:
  - namespace.yaml
  - deployment-mcp-http.yaml
  - service-mcp-http.yaml

images:
  - name: clawql-mcp
    newName: clawql-mcp
    newTag: latest
