name: clawql-provision
description: Provision ClawQL hosted infra — Cloudflare edge, IDP K3s, EKS/Karpenter, golden hosts (AWS/GCP)
runtime:
  name: nodejs
  options:
    typescript: true
main: src/index.ts
