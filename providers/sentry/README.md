# Sentry public API

Source: [getsentry/sentry-api-schema](https://github.com/getsentry/sentry-api-schema) — use the **dereferenced** bundle `openapi-derefed.json` (single-file JSON suitable for tooling).

Upstream also publishes the TypeScript client as `@sentry/api`.

Refresh:

```bash
npm run fetch-provider-specs
```
