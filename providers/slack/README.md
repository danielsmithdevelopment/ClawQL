# Slack Web API

Source: [Slack API specs](https://api.slack.com/specs) — official OpenAPI 2.0 description (`slack_web.json`). ClawQL’s loader converts Swagger 2 to OpenAPI 3.

Refresh:

```bash
npm run fetch-provider-specs
```
