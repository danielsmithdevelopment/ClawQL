directive @length(subgraph: String, min: Int, max: Int) on SCALAR

directive @enum(subgraph: String, value: String) on ENUM_VALUE

directive @resolveRoot(subgraph: String) on FIELD_DEFINITION

directive @typescript(subgraph: String, type: String) on SCALAR | ENUM

directive @example(subgraph: String, value: ObjMap) repeatable on FIELD_DEFINITION | OBJECT | INPUT_OBJECT | ENUM | SCALAR

directive @resolveRootField(subgraph: String, field: String) on FIELD_DEFINITION | ARGUMENT_DEFINITION | INPUT_FIELD_DEFINITION

directive @dictionary(subgraph: String) on FIELD_DEFINITION

directive @statusCodeTypeName(subgraph: String, typeName: String, statusCode: String) repeatable on UNION

directive @regexp(subgraph: String, pattern: String) on SCALAR

directive @discriminator(subgraph: String, field: String, mapping: [[String]]) on INTERFACE | UNION

directive @httpOperation(subgraph: String, path: String, operationSpecificHeaders: [[String]], httpMethod: HTTPMethod, isBinary: Boolean, requestBaseBody: ObjMap, queryParamArgMap: ObjMap, queryStringOptionsByParam: ObjMap, jsonApiFields: Boolean, queryStringOptions: ObjMap) on FIELD_DEFINITION

directive @transport(subgraph: String, kind: String, location: String, headers: [[String]], queryStringOptions: ObjMap, queryParams: [[String]]) repeatable on SCHEMA

type Query {
  """Get Hypermedia links to resources accessible in GitHub's REST API"""
  meta_root: root

  """
  Lists all global security advisories that match the specified parameters. If no other parameters are defined, the request will return only GitHub-reviewed advisories that are not malware.
  
  By default, all responses will exclude advisories for malware, because malware are not standard vulnerabilities. To list advisories for malware, you must include the `type` parameter in your request, with the value `malware`. For more information about the different types of security advisories, see "[About the GitHub Advisory database](https://docs.github.com/code-security/security-advisories/global-security-advisories/about-the-github-advisory-database#about-types-of-security-advisories)."
  """
  security_advisories_list_global_advisories(
    """
    If specified, only advisories with this GHSA (GitHub Security Advisory) identifier will be returned.
    """
    ghsa_id: String
    type: queryInput_security_advisories_list_global_advisories_type = reviewed

    """
    If specified, only advisories with this CVE (Common Vulnerabilities and Exposures) identifier will be returned.
    """
    cve_id: String
    ecosystem: security_advisory_ecosystems
    severity: queryInput_security_advisories_list_global_advisories_severity
    cwes: [String]

    """Whether to only return advisories that have been withdrawn."""
    is_withdrawn: Boolean
    affects: [String]

    """
    If specified, only return advisories that were published on a date or date range.
    
    For more information on the syntax of the date range, see "[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates)."
    """
    published: String

    """
    If specified, only return advisories that were updated on a date or date range.
    
    For more information on the syntax of the date range, see "[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates)."
    """
    updated: String

    """
    If specified, only show advisories that were updated or published on a date or date range.
    
    For more information on the syntax of the date range, see "[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates)."
    """
    modified: String

    """
    If specified, only return advisories that have an EPSS percentage score that matches the provided value.
    The EPSS percentage represents the likelihood of a CVE being exploited.
    """
    epss_percentage: String

    """
    If specified, only return advisories that have an EPSS percentile score that matches the provided value.
    The EPSS percentile represents the relative rank of the CVE's likelihood of being exploited compared to other CVEs.
    """
    epss_percentile: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
    direction: direction_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: PositiveInt = 30
    sort: queryInput_security_advisories_list_global_advisories_sort = published
  ): [global_advisory]

  """
  Gets a global security advisory using its GitHub Security Advisory (GHSA) identifier.
  """
  security_advisories_get_global_advisory(
    """The GHSA (GitHub Security Advisory) identifier of the advisory."""
    ghsa_id: String!
  ): global_advisory

  """
  Returns the GitHub App associated with the authentication credentials used. To see how many app installations are associated with this GitHub App, see the `installations_count` in the response. For more details about your app's installations, see the "[List installations for the authenticated app](https://docs.github.com/rest/apps/apps#list-installations-for-the-authenticated-app)" endpoint.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_get_authenticated: integration

  """
  Returns the webhook configuration for a GitHub App. For more information about configuring a webhook for your app, see "[Creating a GitHub App](/developers/apps/creating-a-github-app)."
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_get_webhook_config_for_app: Webhook_Configuration

  """
  Returns a list of webhook deliveries for the webhook configured for a GitHub App.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_list_webhook_deliveries(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    Used for pagination: the starting delivery from which the page of deliveries is fetched. Refer to the `link` header for the next and previous page cursors.
    """
    cursor: String
  ): [Simple_webhook_delivery]

  """
  Returns a delivery for the webhook configured for a GitHub App.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_get_webhook_delivery(delivery_id: Int!): Webhook_delivery

  """
  Lists all the pending installation requests for the authenticated GitHub App.
  """
  apps_list_installation_requests_for_authenticated_app(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Integration_Installation_Request]

  """
  The permissions the installation has are included under the `permissions` key.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_list_installations(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime
    outdated: String
  ): [Installation]

  """
  Enables an authenticated GitHub App to find an installation's information using the installation id.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_get_installation(
    """The unique identifier of the installation."""
    installation_id: Int!
  ): Installation

  """
  > [!NOTE]
  > The `:app_slug` is just the URL-friendly name of your GitHub App. You can find this on the settings page for your GitHub App (e.g., `https://github.com/settings/apps/:app_slug`).
  """
  apps_get_by_slug(app_slug: String!): integration

  """
  Gets a GitHub Classroom assignment. Assignment will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.
  """
  classroom_get_an_assignment(
    """The unique identifier of the classroom assignment."""
    assignment_id: Int!
  ): Classroom_Assignment

  """
  Lists any assignment repositories that have been created by students accepting a GitHub Classroom assignment. Accepted assignments will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.
  """
  classroom_list_accepted_assignments_for_an_assignment(
    """The unique identifier of the classroom assignment."""
    assignment_id: Int!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Classroom_Accepted_Assignment]

  """
  Gets grades for a GitHub Classroom assignment. Grades will only be returned if the current user is an administrator of the GitHub Classroom for the assignment.
  """
  classroom_get_assignment_grades(
    """The unique identifier of the classroom assignment."""
    assignment_id: Int!
  ): [Classroom_Assignment_Grade]

  """
  Lists GitHub Classroom classrooms for the current user. Classrooms will only be returned if the current user is an administrator of one or more GitHub Classrooms.
  """
  classroom_list_classrooms(
    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Simple_Classroom]

  """
  Gets a GitHub Classroom classroom for the current user. Classroom will only be returned if the current user is an administrator of the GitHub Classroom.
  """
  classroom_get_a_classroom(
    """The unique identifier of the classroom."""
    classroom_id: Int!
  ): Classroom

  """
  Lists GitHub Classroom assignments for a classroom. Assignments will only be returned if the current user is an administrator of the GitHub Classroom.
  """
  classroom_list_assignments_for_a_classroom(
    """The unique identifier of the classroom."""
    classroom_id: Int!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Simple_Classroom_Assignment]

  """Returns array of all GitHub's codes of conduct."""
  codes_of_conduct_get_all_codes_of_conduct: [Code_Of_Conduct]

  """Returns information about the specified GitHub code of conduct."""
  codes_of_conduct_get_conduct_code(key: String!): Code_Of_Conduct

  """Lists all the emojis available to use on GitHub."""
  emojis_get: JSON

  """
  Gets GitHub Actions cache retention limit for an enterprise. All organizations and repositories under this
  enterprise may not set a higher cache retention limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  actions_get_actions_cache_retention_limit_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
  ): Actions_cache_retention_limit_for_an_enterprise

  """
  Gets GitHub Actions cache storage limit for an enterprise. All organizations and repositories under this
  enterprise may not set a higher cache storage limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  actions_get_actions_cache_storage_limit_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
  ): Actions_cache_storage_limit_for_an_enterprise

  """
  Lists the repository custom properties that are included in the OIDC token for repository actions in an enterprise.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  oidc_list_oidc_custom_property_inclusions_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
  ): [Actions_OIDC_Custom_Property_Inclusion]

  """
  Lists all code security configurations available in an enterprise.
  
  The authenticated user must be an administrator of the enterprise in order to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
  """
  code_security_get_configurations_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
  ): [code_security_configuration]

  """
  Lists the default code security configurations for an enterprise.
  
  The authenticated user must be an administrator of the enterprise in order to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
  """
  code_security_get_default_configurations_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
  ): [query_code_security_get_default_configurations_for_enterprise_items]

  """
  Gets a code security configuration available in an enterprise.
  
  The authenticated user must be an administrator of the enterprise in order to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
  """
  code_security_get_single_configuration_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
  ): code_security_configuration

  """
  Lists the repositories associated with an enterprise code security configuration in an organization.
  
  The authenticated user must be an administrator of the enterprise in order to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
  """
  code_security_get_repositories_for_enterprise_configuration(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    A comma-separated list of statuses. If specified, only repositories with these attachment statuses will be returned.
    
    Can be: `all`, `attached`, `attaching`, `removed`, `enforced`, `failed`, `updating`, `removed_by_enterprise`
    """
    status: String = "all"
  ): [code_security_configuration_repositories]

  """
  Lists Dependabot alerts for repositories that are owned by the specified enterprise.
  
  The authenticated user must be a member of the enterprise to use this endpoint.
  
  Alerts are only returned for organizations in the enterprise for which you are an organization owner or a security manager. For more information about security managers, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint.
  """
  dependabot_list_alerts_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    A comma-separated list of states. If specified, only alerts with these states will be returned.
    
    Can be: `auto_dismissed`, `dismissed`, `fixed`, `open`
    """
    state: String

    """
    A comma-separated list of severities. If specified, only alerts with these severities will be returned.
    
    Can be: `low`, `medium`, `high`, `critical`
    """
    severity: String

    """
    A comma-separated list of ecosystems. If specified, only alerts for these ecosystems will be returned.
    
    Can be: `composer`, `go`, `maven`, `npm`, `nuget`, `pip`, `pub`, `rubygems`, `rust`
    """
    ecosystem: String

    """
    A comma-separated list of package names. If specified, only alerts for these packages will be returned.
    """
    package: String

    """
    CVE Exploit Prediction Scoring System (EPSS) percentage. Can be specified as:
    - An exact number (`n`)
    - Comparators such as `>n`, `<n`, `>=n`, `<=n`
    - A range like `n..n`, where `n` is a number from 0.0 to 1.0
    
    Filters the list of alerts based on EPSS percentages. If specified, only alerts with the provided EPSS percentages will be returned.
    """
    epss_percentage: String
    has: [dependabot_alert_comma_separated_has_parameter_Input]

    """
    Filter alerts by assignees.
    Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`) to return alerts assigned to any of the specified users.
    Use `*` to list alerts with at least one assignee or `none` to list alerts with no assignees.
    """
    assignee: String
    scope: dependabot_alert_scope_parameter
    sort: dependabot_alert_sort_parameter = created
    direction: direction_parameter = desc

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [dependabot_alert_with_repository]

  """List all teams in the enterprise for the authenticated user"""
  enterprise_teams_list(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Enterprise_Team]

  """Lists all team members in an enterprise team."""
  enterprise_team_memberships_list(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """Returns whether the user is a member of the enterprise team."""
  enterprise_team_memberships_get(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """The handle for the GitHub user account."""
    username: String!
  ): simple_user

  """Get all organizations assigned to an enterprise team"""
  enterprise_team_organizations_get_assignments(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [organization_simple]

  """Check if an enterprise team is assigned to an organization"""
  enterprise_team_organizations_get_assignment(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """The organization name. The name is not case sensitive."""
    org: String!
  ): organization_simple

  """
  Gets a team using the team's slug. To create the slug, GitHub replaces special characters in the name string, changes all words to lowercase, and replaces spaces with a `-` separator and adds the "ent:" prefix. For example, "My TEam Näme" would become `ent:my-team-name`.
  """
  enterprise_teams_get(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The slug of the team name."""
    team_slug: String!
  ): Enterprise_Team

  """
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_public_events(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 15

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """
  Lists the feeds available to the authenticated user. The response provides a URL for each feed. You can then get a specific feed by sending a request to one of the feed URLs.
  
  *   **Timeline**: The GitHub global public timeline
  *   **User**: The public timeline for any user, using `uri_template`. For more information, see "[Hypermedia](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#hypermedia)."
  *   **Current user public**: The public timeline for the authenticated user
  *   **Current user**: The private timeline for the authenticated user
  *   **Current user actor**: The private timeline for activity created by the authenticated user
  *   **Current user organizations**: The private timeline for the organizations the authenticated user is a member of.
  *   **Security advisories**: A collection of public announcements that provide information about security-related vulnerabilities in software on GitHub.
  
  By default, timeline resources are returned in JSON. You can specify the `application/atom+xml` type in the `Accept` header to return timeline resources in Atom format. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  > [!NOTE]
  > Private feeds are only returned when [authenticating via Basic Auth](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication) since current feed URIs use the older, non revocable auth tokens.
  """
  activity_get_feeds: Feed

  """
  Lists the authenticated user's gists or if called anonymously, this endpoint returns all public gists:
  """
  gists_list(
    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Base_Gist]

  """
  List public gists sorted by most recently updated to least recently updated.
  
  Note: With [pagination](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api), you can fetch up to 3000 gists. For example, you can fetch 100 pages with 30 gists per page or 30 pages with 100 gists per page.
  """
  gists_list_public(
    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Base_Gist]

  """List the authenticated user's starred gists:"""
  gists_list_starred(
    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Base_Gist]

  """
  Gets a specified gist.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  """
  gists_get(
    """The unique identifier of the gist."""
    gist_id: String!
  ): Gist_Simple

  """
  Lists the comments on a gist.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  """
  gists_list_comments(
    """The unique identifier of the gist."""
    gist_id: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Gist_Comment]

  """
  Gets a comment on a gist.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  """
  gists_get_comment(
    """The unique identifier of the gist."""
    gist_id: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): Gist_Comment

  """List gist commits"""
  gists_list_commits(
    """The unique identifier of the gist."""
    gist_id: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Gist_Commit]

  """List gist forks"""
  gists_list_forks(
    """The unique identifier of the gist."""
    gist_id: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Gist_Simple]

  """Check if a gist is starred"""
  gists_check_is_starred(
    """The unique identifier of the gist."""
    gist_id: String!
  ): Void

  """
  Gets a specified gist revision.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  """
  gists_get_revision(
    """The unique identifier of the gist."""
    gist_id: String!
    sha: String!
  ): Gist_Simple

  """
  List all templates available to pass as an option when [creating a repository](https://docs.github.com/rest/repos/repos#create-a-repository-for-the-authenticated-user).
  """
  gitignore_get_all_templates: [String]

  """
  Get the content of a gitignore template.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw .gitignore contents.
  """
  gitignore_get_template(name: String!): Gitignore_Template

  """List repositories that an app installation can access."""
  apps_list_repos_accessible_to_installation(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): apps_list_repos_accessible_to_installation_200_response

  """
  List issues assigned to the authenticated user across all visible repositories including owned repositories, member
  repositories, and organization repositories. You can use the `filter` query parameter to fetch issues that are not
  necessarily assigned to you.
  
  > [!NOTE]
  > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list(
    filter: queryInput_issues_list_filter = assigned
    state: queryInput_issues_list_state = open

    """A list of comma separated label names. Example: `bug,ui,@high`"""
    labels: String
    sort: queryInput_issues_list_sort = created
    direction: direction_parameter = desc

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime
    collab: Boolean
    orgs: Boolean
    owned: Boolean
    pulls: Boolean

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue]

  """
  Lists the most commonly used licenses on GitHub. For more information, see "[Licensing a repository ](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/licensing-a-repository)."
  """
  licenses_get_all_commonly_used(
    featured: Boolean

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [license_simple]

  """
  Gets information about a specific license. For more information, see "[Licensing a repository ](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/licensing-a-repository)."
  """
  licenses_get(license: String!): License

  """
  Shows whether the user or organization account actively subscribes to a plan listed by the authenticated GitHub App. When someone submits a plan change that won't be processed until the end of their billing cycle, you will also see the upcoming pending change.
  
  GitHub Apps must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint. OAuth apps must use [basic authentication](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication) with their client ID and client secret to access this endpoint.
  """
  apps_get_subscription_plan_for_account(
    """account_id parameter"""
    account_id: Int!
  ): marketplace_purchase

  """
  Lists all plans that are part of your GitHub Marketplace listing.
  
  GitHub Apps must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint. OAuth apps must use [basic authentication](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication) with their client ID and client secret to access this endpoint.
  """
  apps_list_plans(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Marketplace_Listing_Plan]

  """
  Returns user and organization accounts associated with the specified plan, including free plans. For per-seat pricing, you see the list of accounts that have purchased the plan, including the number of seats purchased. When someone submits a plan change that won't be processed until the end of their billing cycle, you will also see the upcoming pending change.
  
  GitHub Apps must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint. OAuth apps must use [basic authentication](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication) with their client ID and client secret to access this endpoint.
  """
  apps_list_accounts_for_plan(
    """The unique identifier of the plan."""
    plan_id: Int!
    sort: sort_parameter = created
    direction: queryInput_apps_list_accounts_for_plan_direction

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [marketplace_purchase]

  """
  Shows whether the user or organization account actively subscribes to a plan listed by the authenticated GitHub App. When someone submits a plan change that won't be processed until the end of their billing cycle, you will also see the upcoming pending change.
  
  GitHub Apps must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint. OAuth apps must use [basic authentication](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication) with their client ID and client secret to access this endpoint.
  """
  apps_get_subscription_plan_for_account_stubbed(
    """account_id parameter"""
    account_id: Int!
  ): marketplace_purchase

  """
  Lists all plans that are part of your GitHub Marketplace listing.
  
  GitHub Apps must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint. OAuth apps must use [basic authentication](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication) with their client ID and client secret to access this endpoint.
  """
  apps_list_plans_stubbed(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Marketplace_Listing_Plan]

  """
  Returns repository and organization accounts associated with the specified plan, including free plans. For per-seat pricing, you see the list of accounts that have purchased the plan, including the number of seats purchased. When someone submits a plan change that won't be processed until the end of their billing cycle, you will also see the upcoming pending change.
  
  GitHub Apps must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint. OAuth apps must use [basic authentication](https://docs.github.com/rest/authentication/authenticating-to-the-rest-api#using-basic-authentication) with their client ID and client secret to access this endpoint.
  """
  apps_list_accounts_for_plan_stubbed(
    """The unique identifier of the plan."""
    plan_id: Int!
    sort: sort_parameter = created
    direction: queryInput_apps_list_accounts_for_plan_stubbed_direction

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [marketplace_purchase]

  """
  Returns meta information about GitHub, including a list of GitHub's IP addresses. For more information, see "[About GitHub's IP addresses](https://docs.github.com/articles/about-github-s-ip-addresses/)."
  
  The API's response also includes a list of GitHub's domain names.
  
  The values shown in the documentation's response are example values. You must always query the API directly to get the latest values.
  
  > [!NOTE]
  > This endpoint returns both IPv4 and IPv6 addresses. However, not all features support IPv6. You should refer to the specific documentation for each feature to determine if IPv6 is supported.
  """
  meta_get: Api_Overview

  """
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_public_events_for_repo_network(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """
  List all notifications for the current user, sorted by most recently updated.
  """
  activity_list_notifications_for_authenticated_user(
    """If `true`, show notifications marked as read."""
    all: Boolean

    """
    If `true`, only shows notifications in which the user is directly participating or mentioned.
    """
    participating: Boolean

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    Only show notifications updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    before: DateTime

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 50). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 50
  ): [Thread]

  """Gets information about a notification thread."""
  activity_get_thread(
    """
    The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)).
    """
    thread_id: Int!
  ): Thread

  """
  This checks to see if the current user is subscribed to a thread. You can also [get a repository subscription](https://docs.github.com/rest/activity/watching#get-a-repository-subscription).
  
  Note that subscriptions are only generated if a user is participating in a conversation--for example, they've replied to the thread, were **@mentioned**, or manually subscribe to a thread.
  """
  activity_get_thread_subscription_for_authenticated_user(
    """
    The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)).
    """
    thread_id: Int!
  ): Thread_Subscription

  """Get the octocat as ASCII art"""
  meta_get_octocat(
    """The words to show in Octocat's speech bubble"""
    s: String
  ): String

  """
  Lists all organizations, in the order that they were created.
  
  > [!NOTE]
  > Pagination is powered exclusively by the `since` parameter. Use the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers) to get the URL for the next page of organizations.
  """
  orgs_list(
    """
    An organization ID. Only return organizations with an ID greater than this ID.
    """
    since: Int

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [organization_simple]

  """
  Gets GitHub Actions cache retention limit for an organization. All repositories under this
  organization may not set a higher cache retention limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:organization` scope to use this endpoint.
  """
  actions_get_actions_cache_retention_limit_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Actions_cache_retention_limit_for_an_organization

  """
  Gets GitHub Actions cache storage limit for an organization. All repositories under this
  organization may not set a higher cache storage limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:organization` scope to use this endpoint.
  """
  actions_get_actions_cache_storage_limit_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Actions_cache_storage_limit_for_an_organization

  """
  Lists repositories that organization admins have allowed Dependabot to access when updating dependencies.
  > [!NOTE]
  >    This operation supports both server-to-server and user-to-server access.
  Unauthorized users will not see the existence of this endpoint.
  """
  dependabot_repository_access_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The page number of results to fetch."""
    page: PositiveInt = 1

    """Number of results per page."""
    per_page: PositiveInt = 30
  ): Dependabot_Repository_Access_Details

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets all budgets for an organization. The authenticated user must be an organization admin or billing manager.
  Each page returns up to 10 budgets.
  """
  billing_get_all_budgets_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The page number of the results to fetch."""
    page: Int = 1

    """The number of results per page (max 10)."""
    per_page: Int = 10
    scope: queryInput_billing_get_all_budgets_org_scope
  ): get_all_budgets

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets a budget by ID. The authenticated user must be an organization admin or billing manager.
  """
  billing_get_budget_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID corresponding to the budget."""
    budget_id: String!
  ): get_budget

  """
  Gets a report of premium request usage for an organization. To use this endpoint, you must be an administrator of an organization within an enterprise or an organization account.
  
  **Note:** Only data from the past 24 months is accessible via this endpoint.
  """
  billing_get_github_billing_premium_request_usage_report_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.
    """
    year: Int

    """
    If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used.
    """
    month: Int

    """
    If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.
    """
    day: Int

    """The user name to query usage for. The name is not case sensitive."""
    user: String

    """The model name to query usage for. The name is not case sensitive."""
    model: String

    """The product name to query usage for. The name is not case sensitive."""
    product: String
  ): billing_premium_request_usage_report_org

  """
  Gets a report of the total usage for an organization. To use this endpoint, you must be an administrator of an organization within an enterprise or an organization account.
  
  **Note:** This endpoint is only available to organizations with access to the enhanced billing platform. For more information, see "[About the enhanced billing platform](https://docs.github.com/billing/using-the-new-billing-platform)."
  """
  billing_get_github_billing_usage_report_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.
    """
    year: Int

    """
    If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. If no year is specified the default `year` is used.
    """
    month: Int

    """
    If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.
    """
    day: Int
  ): billing_usage_report

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets a summary report of usage for an organization. To use this endpoint, you must be an administrator of an organization within an enterprise or an organization account.
  
  **Note:** Only data from the past 24 months is accessible via this endpoint.
  """
  billing_get_github_billing_usage_summary_report_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.
    """
    year: Int

    """
    If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used.
    """
    month: Int

    """
    If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.
    """
    day: Int

    """The repository name to query for usage in the format owner/repository."""
    repository: String

    """The product name to query usage for. The name is not case sensitive."""
    product: String

    """The SKU to query for usage."""
    sku: String
  ): billing_usage_summary_report_org

  """
  Gets information about an organization.
  
  When the value of `two_factor_requirement_enabled` is `true`, the organization requires all members, billing managers, outside collaborators, guest collaborators, repository collaborators, or everyone with access to any repository within the organization to enable [two-factor authentication](https://docs.github.com/articles/securing-your-account-with-two-factor-authentication-2fa/).
  
  To see the full details about an organization, the authenticated user must be an organization owner.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to see the full details about an organization.
  
  To see information about an organization's GitHub plan, GitHub Apps need the `Organization plan` permission.
  """
  orgs_get(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Organization_Full

  """
  Gets the total GitHub Actions cache usage for an organization.
  The data fetched using this API is refreshed approximately every 5 minutes, so values returned from this endpoint may take at least 5 minutes to get updated.
  
  OAuth tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  actions_get_actions_cache_usage_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_cache_usage_org_enterprise

  """
  Lists repositories and their GitHub Actions cache usage for an organization.
  The data fetched using this API is refreshed approximately every 5 minutes, so values returned from this endpoint may take at least 5 minutes to get updated.
  
  OAuth tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  actions_get_actions_cache_usage_by_repo_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_get_actions_cache_usage_by_repo_for_org_200_response

  """
  Lists all GitHub-hosted runners configured in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `manage_runner:org` scope to use this endpoint.
  """
  actions_list_hosted_runners_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_hosted_runners_for_org_200_response

  """
  List custom images for an organization.
  
  OAuth tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_list_custom_images_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_list_custom_images_for_org_200_response

  """
  Get a custom image definition for GitHub Actions Hosted Runners.
  
  OAuth tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_get_custom_image_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Image definition ID of custom image"""
    image_definition_id: Int!
  ): GitHub_hosted_runner_custom_image_details

  """
  List image versions of a custom image for an organization.
  
  OAuth tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_list_custom_image_versions_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Image definition ID of custom image"""
    image_definition_id: Int!
  ): actions_list_custom_image_versions_for_org_200_response

  """
  Get an image version of a custom image for GitHub Actions Hosted Runners.
  
  OAuth tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_get_custom_image_version_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Image definition ID of custom image"""
    image_definition_id: Int!
    version: actions_custom_image_version_parameter!
  ): GitHub_hosted_runner_custom_image_version_details_

  """
  Get the list of GitHub-owned images available for GitHub-hosted runners for an organization.
  """
  actions_get_hosted_runners_github_owned_images_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_get_hosted_runners_github_owned_images_for_org_200_response

  """
  Get the list of partner images available for GitHub-hosted runners for an organization.
  """
  actions_get_hosted_runners_partner_images_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_get_hosted_runners_partner_images_for_org_200_response

  """Get the GitHub-hosted runners limits for an organization."""
  actions_get_hosted_runners_limits_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_hosted_runner_limits

  """
  Get the list of machine specs available for GitHub-hosted runners for an organization.
  """
  actions_get_hosted_runners_machine_specs_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_get_hosted_runners_machine_specs_for_org_200_response

  """
  Get the list of platforms available for GitHub-hosted runners for an organization.
  """
  actions_get_hosted_runners_platforms_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_get_hosted_runners_platforms_for_org_200_response

  """
  Gets a GitHub-hosted runner configured in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_get_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the GitHub-hosted runner."""
    hosted_runner_id: Int!
  ): GitHub_hosted_hosted_runner

  """
  Lists the repository custom properties that are included in the OIDC token for repository actions in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  oidc_list_oidc_custom_property_inclusions_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [Actions_OIDC_Custom_Property_Inclusion]

  """
  Gets the customization template for an OpenID Connect (OIDC) subject claim.
  
  OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  oidc_get_oidc_custom_sub_template_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Actions_OIDC_Subject_customization

  """
  Gets the GitHub Actions permissions policy for repositories and allowed actions and reusable workflows in an organization.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_get_github_actions_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_organization_permissions

  """
  Gets artifact and log retention settings for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_get_artifact_and_log_retention_settings_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_artifact_and_log_retention_response

  """
  Gets the fork PR contributor approval policy for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_get_fork_pr_contributor_approval_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_fork_pr_contributor_approval

  """
  Gets the settings for whether workflows from fork pull requests can run on private repositories in an organization.
  """
  actions_get_private_repo_fork_pr_workflows_settings_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_fork_pr_workflows_private_repos

  """
  Lists the selected repositories that are enabled for GitHub Actions in an organization. To use this endpoint, the organization permission policy for `enabled_repositories` must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for an organization](#set-github-actions-permissions-for-an-organization)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_list_selected_repositories_enabled_github_actions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_selected_repositories_enabled_github_actions_organization_200_response

  """
  Gets the selected actions and reusable workflows that are allowed in an organization. To use this endpoint, the organization permission policy for `allowed_actions` must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for an organization](#set-github-actions-permissions-for-an-organization)."
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_get_allowed_actions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): selected_actions

  """
  Gets the settings for self-hosted runners for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_get_self_hosted_runners_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): self_hosted_runners_settings

  """
  Lists repositories that are allowed to use self-hosted runners in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_list_selected_repositories_self_hosted_runners_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_selected_repositories_self_hosted_runners_organization_200_response

  """
  Gets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in an organization,
  as well as whether GitHub Actions can submit approving pull request reviews. For more information, see
  "[Setting the permissions of the GITHUB_TOKEN for your organization](https://docs.github.com/organizations/managing-organization-settings/disabling-or-limiting-github-actions-for-your-organization#setting-the-permissions-of-the-github_token-for-your-organization)."
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_get_github_actions_default_workflow_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): actions_get_default_workflow_permissions

  """
  Lists all self-hosted runner groups configured in an organization and inherited from an enterprise.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_list_self_hosted_runner_groups_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Only return runner groups that are allowed to be used by this repository.
    """
    visible_to_repository: String
  ): actions_list_self_hosted_runner_groups_for_org_200_response

  """
  Gets a specific self-hosted runner group for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_get_self_hosted_runner_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!
  ): runner_groups_org

  """
  Lists the GitHub-hosted runners in an organization group.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_list_github_hosted_runners_in_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_github_hosted_runners_in_group_for_org_200_response

  """
  Lists the repositories with access to a self-hosted runner group configured in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_list_repo_access_to_self_hosted_runner_group_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): actions_list_repo_access_to_self_hosted_runner_group_in_org_200_response

  """
  Lists self-hosted runners that are in a specific organization group.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_list_self_hosted_runners_in_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_self_hosted_runners_in_group_for_org_200_response

  """
  Lists all self-hosted runners configured in an organization.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_list_self_hosted_runners_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of a self-hosted runner."""
    name: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_self_hosted_runners_for_org_200_response

  """
  Lists binaries for the runner application that you can download and run.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.  If the repository is private, the `repo` scope is also required.
  """
  actions_list_runner_applications_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [Runner_Application]

  """
  Gets a specific self-hosted runner configured in an organization.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_get_self_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): Self_hosted_runners

  """
  Lists all labels for a self-hosted runner configured in an organization.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_list_labels_for_self_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): actions_runner_labels_response

  """
  Lists all secrets available in an organization without revealing their
  encrypted values.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_list_org_secrets(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_org_secrets_200_response

  """
  Gets your public key, which you need to encrypt secrets. You need to
  encrypt a secret before you can create or update secrets.
  
  The authenticated user must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_org_public_key(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): ActionsPublicKey

  """
  Gets a single organization secret without revealing its encrypted value.
  
  The authenticated user must have collaborator access to a repository to create, update, or read secrets
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): Actions_Secret_for_an_Organization

  """
  Lists all repositories that have been selected when the `visibility`
  for repository access to a secret is set to `selected`.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_list_selected_repos_for_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): actions_list_selected_repos_for_org_secret_200_response

  """
  Lists all organization variables.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_list_org_variables(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 30). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 10

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_org_variables_200_response

  """
  Gets a specific variable in an organization.
  
  The authenticated user must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the variable."""
    name: String!
  ): Actions_Variable_for_an_Organization

  """
  Lists all repositories that can access an organization variable
  that is available to selected repositories.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_list_selected_repos_for_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the variable."""
    name: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): actions_list_selected_repos_for_org_variable_200_response

  """
  List deployment records for an artifact metadata associated with an organization.
  """
  orgs_list_artifact_deployment_records(
    """The organization name. The name is not case sensitive."""
    org: String!
    subject_digest: subject_digest_parameter!
  ): orgs_list_artifact_deployment_records_200_response

  """
  List a collection of artifact storage records with a given subject digest that are associated with repositories owned by an organization.
  
  The collection of storage records returned by this endpoint is filtered according to the authenticated user's permissions; if the authenticated user cannot read a repository, the attestations associated with that repository will not be included in the response. In addition, when using a fine-grained access token the `content:read` permission is required.
  """
  orgs_list_artifact_storage_records(
    """The organization name. The name is not case sensitive."""
    org: String!
    subject_digest: queryInput_orgs_list_artifact_storage_records_subject_digest!
  ): orgs_list_artifact_storage_records_200_response

  """
  List repositories owned by the provided organization that have created at least one attested artifact
  Results will be sorted in ascending order by repository ID
  """
  orgs_list_attestation_repositories(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    Optional filter for fetching attestations with a given predicate type.
    This option accepts `provenance`, `sbom`, `release`, or freeform text
    for custom predicate types.
    """
    predicate_type: String
  ): [query_orgs_list_attestation_repositories_items]

  """
  List a collection of artifact attestations with a given subject digest that are associated with repositories owned by an organization.
  
  The collection of attestations returned by this endpoint is filtered according to the authenticated user's permissions; if the authenticated user cannot read a repository, the attestations associated with that repository will not be included in the response. In addition, when using a fine-grained access token the `attestations:read` permission is required.
  
  **Please note:** in order to offer meaningful security benefits, an attestation's signature and timestamps **must** be cryptographically verified, and the identity of the attestation signer **must** be validated. Attestations can be verified using the [GitHub CLI `attestation verify` command](https://cli.github.com/manual/gh_attestation_verify). For more information, see [our guide on how to use artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
  """
  orgs_list_attestations(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The parameter should be set to the attestation's subject's SHA256 digest, in the form `sha256:HEX_DIGEST`.
    """
    subject_digest: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    Optional filter for fetching attestations with a given predicate type.
    This option accepts `provenance`, `sbom`, `release`, or freeform text
    for custom predicate types.
    """
    predicate_type: String
  ): orgs_list_attestations_200_response

  """List the users blocked by an organization."""
  orgs_list_blocked_users(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """
  Returns a 204 if the given user is blocked by the given organization. Returns a 404 if the organization is not blocking the user, or if the user account has been identified as spam by GitHub.
  """
  orgs_check_blocked_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Lists campaigns in an organization.
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
  """
  campaigns_list_org_campaigns(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
    direction: direction_parameter = desc
    state: Campaign_state
    sort: queryInput_campaigns_list_org_campaigns_sort = created
  ): [Campaign_summary]

  """
  Gets a campaign for an organization.
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
  """
  campaigns_get_campaign_summary(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The campaign number."""
    campaign_number: Int!
  ): Campaign_summary

  """
  Lists code scanning alerts for the default branch for all eligible repositories in an organization. Eligible repositories are repositories that are owned by organizations that you own or for which you are a security manager. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` or `repo`s cope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_list_alerts_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the tool used to generate the code scanning analysis."""
    tool_name: String

    """
    The GUID of the tool used to generate the code scanning analysis, if provided in the uploaded SARIF data.
    """
    tool_guid: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
    direction: direction_parameter = desc
    state: code_scanning_alert_state_query
    sort: queryInput_code_scanning_list_alerts_for_org_sort = created
    severity: code_scanning_alert_severity

    """
    Filter alerts by assignees. Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`).
    Use `*` to list alerts with at least one assignee or `none` to list alerts with no assignees.
    """
    assignees: String
  ): [code_scanning_organization_alert_items]

  """
  Lists all code security configurations available in an organization.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  code_security_get_configurations_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    target_type: queryInput_code_security_get_configurations_for_org_target_type = all

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
  ): [code_security_configuration]

  """
  Lists the default code security configurations for an organization.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  code_security_get_default_configurations(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [query_code_security_get_default_configurations_for_enterprise_items]

  """
  Gets a code security configuration available in an organization.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  code_security_get_configuration(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
  ): code_security_configuration

  """
  Lists the repositories associated with a code security configuration in an organization.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  code_security_get_repositories_for_configuration(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    A comma-separated list of statuses. If specified, only repositories with these attachment statuses will be returned.
    
    Can be: `all`, `attached`, `attaching`, `detached`, `removed`, `enforced`, `failed`, `updating`, `removed_by_enterprise`
    """
    status: String = "all"
  ): [code_security_configuration_repositories]

  """
  Lists the codespaces associated to a specified organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_list_in_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): codespaces_list_in_organization_200_response

  """
  Lists all Codespaces development environment secrets available at the organization-level without revealing their encrypted
  values.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_list_org_secrets(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): codespaces_list_org_secrets_200_response

  """
  Gets a public key for an organization, which is required in order to encrypt secrets. You need to encrypt the value of a secret before you can create or update secrets.
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_get_org_public_key(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): CodespacesPublicKey

  """
  Gets an organization development environment secret without revealing its encrypted value.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_get_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): codespaces_org_secret

  """
  Lists all repositories that have been selected when the `visibility`
  for repository access to a secret is set to `selected`.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_list_selected_repos_for_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): codespaces_list_selected_repos_for_org_secret_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets information about an organization's Copilot subscription, including seat breakdown
  and feature policies. To configure these settings, go to your organization's settings on GitHub.com.
  For more information, see "[Managing policies for Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-policies-for-copilot-business-in-your-organization)."
  
  Only organization owners can view details about the organization's Copilot Business or Copilot Enterprise subscription.
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `read:org` scopes to use this endpoint.
  """
  copilot_get_copilot_organization_details(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Copilot_Organization_Details

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Lists all Copilot seats for which an organization with a Copilot Business or Copilot Enterprise subscription is currently being billed.
  Only organization owners can view assigned seats.
  
  Each seat object contains information about the assigned user's most recent Copilot activity. Users must have telemetry enabled in their IDE for Copilot in the IDE activity to be reflected in `last_activity_at`.
  For more information about activity data, see [Metrics data properties for GitHub Copilot](https://docs.github.com/copilot/reference/metrics-data).
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `read:org` scopes to use this endpoint.
  """
  copilot_list_copilot_seats(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 50
  ): copilot_list_copilot_seats_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets information about which repositories in an organization have been enabled
  or disabled for the Copilot coding agent.
  
  Organization owners can configure whether Copilot coding agent is enabled for
  all repositories, selected repositories, or no repositories owned by organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  copilot_get_copilot_coding_agent_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): copilot_get_copilot_coding_agent_permissions_organization_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Lists the selected repositories that are enabled for Copilot coding agent in an organization.
  
  Organization owners can use this endpoint when the coding agent repository policy
  is set to `selected` to see which repositories have been enabled.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  copilot_list_copilot_coding_agent_selected_repositories_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): copilot_list_copilot_coding_agent_selected_repositories_for_organization_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets information about an organization's Copilot content exclusion path rules.
  To configure these settings, go to the organization's settings on GitHub.
  For more information, see "[Excluding content from GitHub Copilot](https://docs.github.com/copilot/managing-copilot/configuring-and-auditing-content-exclusion/excluding-content-from-github-copilot#configuring-content-exclusions-for-your-organization)."
  
  Organization owners can view details about Copilot content exclusion rules for the organization.
  
  OAuth app tokens and personal access tokens (classic) need either the `copilot` or `read:org` scopes to use this endpoint.
  
  > [!CAUTION]
  > * At this time, the API does not support comments. This endpoint will not return any comments in the existing rules.
  > * At this time, the API does not support duplicate keys. If your content exclusion configuration contains duplicate keys, the API will return only the last occurrence of that key. For example, if duplicate entries are present, only the final value will be included in the response.
  """
  copilot_copilot_content_exclusion_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): JSON

  """
  Use this endpoint to see a breakdown of aggregated metrics for various GitHub Copilot features. See the response schema tab for detailed metrics definitions.
  
  > [!NOTE]
  > This endpoint will only return results for a given day if the organization contained **five or more members with active Copilot licenses** on that day, as evaluated at the end of that day.
  
  The response contains metrics for up to 100 days prior. Metrics are processed once per day for the previous day,
  and the response will only include data up until yesterday. In order for an end user to be counted towards these metrics,
  they must have telemetry enabled in their IDE.
  
  To access this endpoint, the Copilot Metrics API access policy must be enabled for the organization.
  Only organization owners and owners and billing managers of the parent enterprise can view Copilot metrics.
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot`, `read:org`, or `read:enterprise` scopes to use this endpoint.
  """
  copilot_copilot_metrics_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    Show usage metrics since this date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format (`YYYY-MM-DDTHH:MM:SSZ`). Maximum value is 100 days ago.
    """
    since: String

    """
    Show usage metrics until this date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format (`YYYY-MM-DDTHH:MM:SSZ`) and should not preceed the `since` date if it is passed.
    """
    until: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of days of metrics to display per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 100
  ): [Copilot_Usage_Metrics]

  """
  Lists Dependabot alerts for an organization.
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  dependabot_list_alerts_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    A comma-separated list of states. If specified, only alerts with these states will be returned.
    
    Can be: `auto_dismissed`, `dismissed`, `fixed`, `open`
    """
    state: String

    """
    A comma-separated list of severities. If specified, only alerts with these severities will be returned.
    
    Can be: `low`, `medium`, `high`, `critical`
    """
    severity: String

    """
    A comma-separated list of ecosystems. If specified, only alerts for these ecosystems will be returned.
    
    Can be: `composer`, `go`, `maven`, `npm`, `nuget`, `pip`, `pub`, `rubygems`, `rust`
    """
    ecosystem: String

    """
    A comma-separated list of package names. If specified, only alerts for these packages will be returned.
    """
    package: String

    """
    CVE Exploit Prediction Scoring System (EPSS) percentage. Can be specified as:
    - An exact number (`n`)
    - Comparators such as `>n`, `<n`, `>=n`, `<=n`
    - A range like `n..n`, where `n` is a number from 0.0 to 1.0
    
    Filters the list of alerts based on EPSS percentages. If specified, only alerts with the provided EPSS percentages will be returned.
    """
    epss_percentage: String

    """
    A comma-separated list of artifact registry URLs. If specified, only alerts for repositories with storage records matching these URLs will be returned.
    """
    artifact_registry_url: String

    """
    A comma-separated list of Artifact Registry name strings. If specified, only alerts for repositories with storage records matching these registries will be returned.
    
    Can be: `jfrog-artifactory`
    """
    artifact_registry: String
    has: [dependabot_alert_org_scope_comma_separated_has_parameter_Input]

    """
    Filter alerts by assignees.
    Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`) to return alerts assigned to any of the specified users.
    Use `*` to list alerts with at least one assignee or `none` to list alerts with no assignees.
    """
    assignee: String

    """
    A comma-separated list of runtime risk strings. If specified, only alerts for repositories with deployment records matching these risks will be returned.
    
    Can be: `critical-resource`, `internet-exposed`, `sensitive-data`, `lateral-movement`
    """
    runtime_risk: String
    scope: dependabot_alert_scope_parameter
    sort: dependabot_alert_sort_parameter = created
    direction: direction_parameter = desc

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [dependabot_alert_with_repository]

  """
  Lists all secrets available in an organization without revealing their
  encrypted values.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_list_org_secrets(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): dependabot_list_org_secrets_200_response

  """
  Gets your public key, which you need to encrypt secrets. You need to
  encrypt a secret before you can create or update secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_get_org_public_key(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): DependabotPublicKey

  """
  Gets a single organization secret without revealing its encrypted value.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_get_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): Dependabot_Secret_for_an_Organization

  """
  Lists all repositories that have been selected when the `visibility`
  for repository access to a secret is set to `selected`.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_list_selected_repos_for_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): dependabot_list_selected_repos_for_org_secret_200_response

  """
  Lists all packages that are in a specific organization, are readable by the requesting user, and that encountered a conflict during a Docker migration.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
  """
  packages_list_docker_migration_conflicting_packages_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [Package]

  """
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_public_org_events(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """
  The return hash contains `failed_at` and `failed_reason` fields which represent the time at which the invitation failed and the reason for the failure.
  """
  orgs_list_failed_invitations(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Organization_Invitation]

  """
  List webhooks for an organization.
  
  The authenticated user must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_list_webhooks(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Org_Hook]

  """
  Returns a webhook configured in an organization. To get only the webhook
  `config` properties, see "[Get a webhook configuration for an organization](/rest/orgs/webhooks#get-a-webhook-configuration-for-an-organization).
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_get_webhook(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Org_Hook

  """
  Returns the webhook configuration for an organization. To get more information about the webhook, including the `active` state and `events`, use "[Get an organization webhook ](/rest/orgs/webhooks#get-an-organization-webhook)."
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_get_webhook_config_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Webhook_Configuration

  """
  Returns a list of webhook deliveries for a webhook configured in an organization.
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_list_webhook_deliveries(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    Used for pagination: the starting delivery from which the page of deliveries is fetched. Refer to the `link` header for the next and previous page cursors.
    """
    cursor: String
  ): [Simple_webhook_delivery]

  """
  Returns a delivery for a webhook configured in an organization.
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_get_webhook_delivery(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    delivery_id: Int!
  ): Webhook_delivery

  """
  Get API request count statistics for an actor broken down by route within a specified time frame.
  """
  api_insights_get_route_stats_by_actor(
    """The organization name. The name is not case sensitive."""
    org: String!
    actor_type: api_insights_actor_type_parameter!

    """The ID of the actor"""
    actor_id: Int!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
    direction: direction_parameter = desc

    """The property to sort the results by."""
    sort: [queryInput_api_insights_get_route_stats_by_actor_sort_items]

    """
    Providing a substring will filter results where the API route contains the substring. This is a case-insensitive search.
    """
    api_route_substring: String
  ): [query_api_insights_get_route_stats_by_actor_items]

  """
  Get API request statistics for all subjects within an organization within a specified time frame. Subjects can be users or GitHub Apps.
  """
  api_insights_get_subject_stats(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
    direction: direction_parameter = desc

    """The property to sort the results by."""
    sort: [queryInput_api_insights_get_subject_stats_sort_items]

    """
    Providing a substring will filter results where the subject name contains the substring. This is a case-insensitive search.
    """
    subject_name_substring: String
  ): [query_api_insights_get_subject_stats_items]

  """
  Get overall statistics of API requests made within an organization by all users and apps within a specified time frame.
  """
  api_insights_get_summary_stats(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String
  ): Summary_Stats

  """
  Get overall statistics of API requests within the organization for a user.
  """
  api_insights_get_summary_stats_by_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the user to query for stats"""
    user_id: String!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String
  ): Summary_Stats

  """
  Get overall statistics of API requests within the organization made by a specific actor. Actors can be GitHub App installations, OAuth apps or other tokens on behalf of a user.
  """
  api_insights_get_summary_stats_by_actor(
    """The organization name. The name is not case sensitive."""
    org: String!
    actor_type: api_insights_actor_type_parameter!

    """The ID of the actor"""
    actor_id: Int!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String
  ): Summary_Stats

  """
  Get the number of API requests and rate-limited requests made within an organization over a specified time period.
  """
  api_insights_get_time_stats(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String

    """
    The increment of time used to breakdown the query results (5m, 10m, 1h, etc.)
    """
    timestamp_increment: String!
  ): [query_api_insights_get_time_stats_items]

  """
  Get the number of API requests and rate-limited requests made within an organization by a specific user over a specified time period.
  """
  api_insights_get_time_stats_by_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the user to query for stats"""
    user_id: String!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String

    """
    The increment of time used to breakdown the query results (5m, 10m, 1h, etc.)
    """
    timestamp_increment: String!
  ): [query_api_insights_get_time_stats_items]

  """
  Get the number of API requests and rate-limited requests made within an organization by a specific actor within a specified time period.
  """
  api_insights_get_time_stats_by_actor(
    """The organization name. The name is not case sensitive."""
    org: String!
    actor_type: api_insights_actor_type_parameter!

    """The ID of the actor"""
    actor_id: Int!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String

    """
    The increment of time used to breakdown the query results (5m, 10m, 1h, etc.)
    """
    timestamp_increment: String!
  ): [query_api_insights_get_time_stats_items]

  """
  Get API usage statistics within an organization for a user broken down by the type of access.
  """
  api_insights_get_user_stats(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the user to query for stats"""
    user_id: String!

    """
    The minimum timestamp to query for stats. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    min_timestamp: String!

    """
    The maximum timestamp to query for stats. Defaults to the time 30 days ago. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    max_timestamp: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
    direction: direction_parameter = desc

    """The property to sort the results by."""
    sort: [queryInput_api_insights_get_subject_stats_sort_items]

    """
    Providing a substring will filter results where the actor name contains the substring. This is a case-insensitive search.
    """
    actor_name_substring: String
  ): [query_api_insights_get_user_stats_items]

  """
  Enables an authenticated GitHub App to find the organization's installation information.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_get_org_installation(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Installation

  """
  Lists all GitHub Apps in an organization. The installation count includes
  all GitHub Apps installed on repositories in the organization.
  
  The authenticated user must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:read` scope to use this endpoint.
  """
  orgs_list_app_installations(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): orgs_list_app_installations_200_response

  """
  Shows which type of GitHub user can interact with this organization and when the restriction expires. If there is no restrictions, you will see an empty response.
  """
  interactions_get_restrictions_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): interactions_get_restrictions_for_org_200_response

  """
  The return hash contains a `role` field which refers to the Organization
  Invitation role and will be one of the following values: `direct_member`, `admin`,
  `billing_manager`, or `hiring_manager`. If the invitee is not a GitHub
  member, the `login` field in the return hash will be `null`.
  """
  orgs_list_pending_invitations(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
    role: queryInput_orgs_list_pending_invitations_role = all
    invitation_source: queryInput_orgs_list_pending_invitations_invitation_source = all
  ): [Organization_Invitation]

  """
  List all teams associated with an invitation. In order to see invitations in an organization, the authenticated user must be an organization owner.
  """
  orgs_list_invitation_teams(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the invitation."""
    invitation_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [team]

  """
  Lists all issue fields for an organization. OAuth app tokens and personal access tokens (classic) need the read:org scope to use this endpoint.
  """
  orgs_list_issue_fields(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [Issue_Field]

  """
  Lists all issue types for an organization. OAuth app tokens and personal access tokens (classic) need the read:org scope to use this endpoint.
  """
  orgs_list_issue_types(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [Issue_Type]

  """
  List issues in an organization assigned to the authenticated user.
  
  > [!NOTE]
  > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    filter: queryInput_issues_list_for_org_filter = assigned
    state: queryInput_issues_list_for_org_state = open

    """A list of comma separated label names. Example: `bug,ui,@high`"""
    labels: String

    """Can be the name of an issue type."""
    type: String
    sort: queryInput_issues_list_for_org_sort = created
    direction: direction_parameter = desc

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue]

  """
  List all users who are members of an organization. If the authenticated user is also a member of this organization then both concealed and public members will be returned.
  """
  orgs_list_members(
    """The organization name. The name is not case sensitive."""
    org: String!
    filter: queryInput_orgs_list_members_filter = all
    role: queryInput_orgs_list_members_role = all

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """
  Check if a user is, publicly or privately, a member of the organization.
  """
  orgs_check_membership_for_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Lists the codespaces that a member of an organization has for repositories in that organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_get_codespaces_for_user_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): codespaces_get_codespaces_for_user_in_org_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets the GitHub Copilot seat details for a member of an organization who currently has access to GitHub Copilot.
  
  The seat object contains information about the user's most recent Copilot activity. Users must have telemetry enabled in their IDE for Copilot in the IDE activity to be reflected in `last_activity_at`.
  For more information about activity data, see [Metrics data properties for GitHub Copilot](https://docs.github.com/copilot/reference/metrics-data).
  
  Only organization owners can view Copilot seat assignment details for members of their organization.
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `read:org` scopes to use this endpoint.
  """
  copilot_get_copilot_seat_details_for_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Copilot_Business_Seat_Detail

  """
  In order to get a user's membership with an organization, the authenticated user must be an organization member. The `state` parameter in the response can be used to identify the user's membership status.
  """
  orgs_get_membership_for_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Org_Membership

  """
  Lists the most recent migrations, including both exports (which can be started through the REST API) and imports (which cannot be started using the REST API).
  
  A list of `repositories` is only returned for export migrations.
  """
  migrations_list_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """Exclude attributes from the API response to improve performance"""
    exclude: [repositories_const]
  ): [Migration]

  """
  Fetches the status of a migration.
  
  The `state` of a migration can be one of the following values:
  
  *   `pending`, which means the migration hasn't started yet.
  *   `exporting`, which means the migration is in progress.
  *   `exported`, which means the migration finished successfully.
  *   `failed`, which means the migration failed.
  """
  migrations_get_status_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the migration."""
    migration_id: Int!

    """Exclude attributes from the API response to improve performance"""
    exclude: [repositories_const]
  ): Migration

  """Fetches the URL to a migration archive."""
  migrations_download_archive_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the migration."""
    migration_id: Int!
  ): JSON

  """List all the repositories for this organization migration."""
  migrations_list_repos_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the migration."""
    migration_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """
  Lists the organization roles available in this organization. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  To use this endpoint, the authenticated user must be one of:
  
  - An administrator for the organization.
  - A user, or a user on a team, with the fine-grained permissions of `read_organization_custom_org_role` in the organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_list_org_roles(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): orgs_list_org_roles_200_response

  """
  Gets an organization role that is available to this organization. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  To use this endpoint, the authenticated user must be one of:
  
  - An administrator for the organization.
  - A user, or a user on a team, with the fine-grained permissions of `read_organization_custom_org_role` in the organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_get_org_role(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the role."""
    role_id: Int!
  ): Organization_Role

  """
  Lists the teams that are assigned to an organization role. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  To use this endpoint, you must be an administrator for the organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_list_org_role_teams(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the role."""
    role_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [A_Role_Assignment_for_a_Team]

  """
  Lists organization members that are assigned to an organization role. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  To use this endpoint, you must be an administrator for the organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_list_org_role_users(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the role."""
    role_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [A_Role_Assignment_for_a_User]

  """List all users who are outside collaborators of an organization."""
  orgs_list_outside_collaborators(
    """The organization name. The name is not case sensitive."""
    org: String!
    filter: queryInput_orgs_list_outside_collaborators_filter = all

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """
  Lists packages in an organization readable by the user.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_list_packages_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: queryInput_packages_list_packages_for_organization_package_type!
    visibility: package_visibility_parameter

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Package]

  """
  Gets a specific package in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_package_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!
  ): Package

  """
  Lists package versions for a package owned by an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_all_package_versions_for_package_owned_by_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
    state: queryInput_packages_get_all_package_versions_for_package_owned_by_org_state = active
  ): [Package_Version]

  """
  Gets a specific package version in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_package_version_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Package_Version

  """
  Lists requests from organization members to access organization resources with a fine-grained personal access token.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_list_pat_grant_requests(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
    sort: personal_access_token_sort_parameter
    direction: direction_parameter = desc

    """A list of owner usernames to use to filter the results."""
    owner: [String]

    """The name of the repository to use to filter the results."""
    repository: String

    """The permission to use to filter the results."""
    permission: String

    """
    Only show fine-grained personal access tokens used before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    last_used_before: DateTime

    """
    Only show fine-grained personal access tokens used after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    last_used_after: DateTime

    """The ID of the token"""
    token_id: [String]
  ): [Simple_Organization_Programmatic_Access_Grant_Request]

  """
  Lists the repositories a fine-grained personal access token request is requesting access to.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_list_pat_grant_request_repositories(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    Unique identifier of the request for access via fine-grained personal access token.
    """
    pat_request_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """
  Lists approved fine-grained personal access tokens owned by organization members that can access organization resources.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_list_pat_grants(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
    sort: personal_access_token_sort_parameter
    direction: direction_parameter = desc

    """A list of owner usernames to use to filter the results."""
    owner: [String]

    """The name of the repository to use to filter the results."""
    repository: String

    """The permission to use to filter the results."""
    permission: String

    """
    Only show fine-grained personal access tokens used before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    last_used_before: DateTime

    """
    Only show fine-grained personal access tokens used after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    last_used_after: DateTime

    """The ID of the token"""
    token_id: [String]
  ): [Organization_Programmatic_Access_Grant]

  """
  Lists the repositories a fine-grained personal access token has access to.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_list_pat_grant_repositories(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the fine-grained personal access token."""
    pat_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  "\nLists all private registry configurations available at the organization-level without revealing their encrypted\nvalues.\n\nOAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint."
  private_registries_list_org_private_registries(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): private_registries_list_org_private_registries_200_response

  "\nGets the org public key, which is needed to encrypt private registry secrets. You need to encrypt a secret before you can create or update secrets.\n\nOAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint."
  private_registries_get_org_public_key(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): private_registries_get_org_public_key_200_response

  "\nGet the configuration of a single private registry defined for an organization, omitting its encrypted value.\n\nOAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint."
  private_registries_get_org_private_registry(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): org_private_registry_configuration

  """
  List all projects owned by a specific organization accessible by the authenticated user.
  """
  projects_list_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Limit results to projects of the specified type."""
    q: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Projects_v2_Project]

  """Get a specific organization-owned project."""
  projects_get_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!
  ): Projects_v2_Project

  """List all fields for a specific organization-owned project."""
  projects_list_fields_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
  ): [Projects_v2_Field]

  """Get a specific field for an organization-owned project."""
  projects_get_field_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the field."""
    field_id: Int!
  ): Projects_v2_Field

  """
  List all items for a specific organization-owned project accessible by the authenticated user.
  """
  projects_list_items_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!

    """
    Search query to filter items, see [Filtering projects](https://docs.github.com/issues/planning-and-tracking-with-projects/customizing-views-in-your-project/filtering-projects) for more information.
    """
    q: String
    fields: [String]

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [projects_v2_item_with_content]

  """Get a specific item from an organization-owned project."""
  projects_get_org_item(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the project item."""
    item_id: Int!
    fields: [String]
  ): projects_v2_item_with_content

  """
  List items in an organization project with the saved view's filter applied.
  """
  projects_list_view_items_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!

    """The number that identifies the project view."""
    view_number: Int!
    fields: [String]

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [projects_v2_item_with_content]

  """
  Gets all custom properties defined for an organization.
  Organization members can read these properties.
  """
  orgs_custom_properties_for_repos_get_organization_definitions(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [Organization_Custom_Property]

  """
  Gets a custom property that is defined for an organization.
  Organization members can read these properties.
  """
  orgs_custom_properties_for_repos_get_organization_definition(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The custom property name"""
    custom_property_name: String!
  ): Organization_Custom_Property

  """
  Lists organization repositories with all of their custom property values.
  Organization members can read these properties.
  """
  orgs_custom_properties_for_repos_get_organization_values(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Finds repositories in the organization with a query containing one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See "[Searching for repositories](https://docs.github.com/articles/searching-for-repositories/)" for a detailed list of qualifiers.
    """
    repository_query: String
  ): [Organization_Repository_Custom_Property_Values]

  """
  Members of an organization can choose to have their membership publicized or not.
  """
  orgs_list_public_members(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """Check if the provided user is a public member of the organization."""
  orgs_check_public_membership_for_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Lists repositories for the specified organization.
  
  > [!NOTE]
  > In order to see the `security_and_analysis` block for a repository you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  """
  repos_list_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    type: queryInput_repos_list_for_org_type = all
    sort: queryInput_repos_list_for_org_sort = created
    direction: queryInput_repos_list_for_org_direction

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """Get all the repository rulesets for an organization."""
  repos_get_org_rulesets(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    A comma-separated list of rule targets to filter by.
    If provided, only rulesets that apply to the specified targets will be returned.
    For example, `branch,tag,push`.
    """
    targets: String
  ): [Repository_ruleset]

  """
  Lists suites of rule evaluations at the organization level.
  For more information, see "[Managing rulesets for repositories in your organization](https://docs.github.com/organizations/managing-organization-settings/managing-rulesets-for-repositories-in-your-organization#viewing-insights-for-rulesets)."
  """
  repos_get_org_rule_suites(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The name of the ref. Cannot contain wildcard characters. Optionally prefix with `refs/heads/` to limit to branches or `refs/tags/` to limit to tags. Omit the prefix to search across all refs. When specified, only rule evaluations triggered for this ref will be returned.
    """
    ref: String

    """The name of the repository to filter on."""
    repository_name: String
    time_period: time_period_parameter = day

    """
    The handle for the GitHub user account to filter on. When specified, only rule evaluations triggered by this actor will be returned.
    """
    actor_name: String
    rule_suite_result: rule_suite_result_parameter = all

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [query_repos_get_org_rule_suites_items]

  """
  Gets information about a suite of rule evaluations from within an organization.
  For more information, see "[Managing rulesets for repositories in your organization](https://docs.github.com/organizations/managing-organization-settings/managing-rulesets-for-repositories-in-your-organization#viewing-insights-for-rulesets)."
  """
  repos_get_org_rule_suite(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the rule suite result.
    To get this ID, you can use [GET /repos/{owner}/{repo}/rulesets/rule-suites](https://docs.github.com/rest/repos/rule-suites#list-repository-rule-suites)
    for repositories and [GET /orgs/{org}/rulesets/rule-suites](https://docs.github.com/rest/orgs/rule-suites#list-organization-rule-suites)
    for organizations.
    """
    rule_suite_id: Int!
  ): Rule_Suite

  """
  Get a repository ruleset for an organization.
  
  **Note:** To prevent leaking sensitive information, the `bypass_actors` property is only returned if the user
  making the API request has write access to the ruleset.
  """
  repos_get_org_ruleset(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the ruleset."""
    ruleset_id: Int!
  ): Repository_ruleset

  """Get the history of an organization ruleset."""
  orgs_get_org_ruleset_history(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the ruleset."""
    ruleset_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Ruleset_version]

  """Get a version of an organization ruleset."""
  orgs_get_org_ruleset_version(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the ruleset."""
    ruleset_id: Int!

    """The ID of the version"""
    version_id: Int!
  ): ruleset_version_with_state

  """
  Lists secret scanning alerts for eligible repositories in an organization, from newest to oldest.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  secret_scanning_list_alerts_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    state: secret_scanning_alert_state_parameter

    """
    A comma-separated list of secret types to return. All default secret patterns are returned. To return generic patterns, pass the token name(s) in the parameter. See "[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)" for a complete list of secret types.
    """
    secret_type: String

    """
    A comma-separated list of resolutions. Only secret scanning alerts with one of these resolutions are listed. Valid resolutions are `false_positive`, `wont_fix`, `revoked`, `pattern_edited`, `pattern_deleted` or `used_in_tests`.
    """
    resolution: String

    """
    Filters alerts by assignee. Use `*` to get all assigned alerts, `none` to get all unassigned alerts, or a GitHub username to get alerts assigned to a specific user.
    """
    assignee: String
    sort: secret_scanning_alert_sort_parameter = created
    direction: direction_parameter = desc

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events before this cursor. To receive an initial cursor on your first request, include an empty "before" query string.
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events after this cursor.  To receive an initial cursor on your first request, include an empty "after" query string.
    """
    after: String

    """
    A comma-separated list of validities that, when present, will return alerts that match the validities in this list. Valid options are `active`, `inactive`, and `unknown`.
    """
    validity: String

    """
    A boolean value representing whether or not to filter alerts by the publicly-leaked tag being present.
    """
    is_publicly_leaked: Boolean

    """
    A boolean value representing whether or not to filter alerts by the multi-repo tag being present.
    """
    is_multi_repo: Boolean

    """
    A boolean value representing whether or not to hide literal secrets in the results.
    """
    hide_secret: Boolean
  ): [organization_secret_scanning_alert]

  """
  Lists the secret scanning pattern configurations for an organization.
  
  Personal access tokens (classic) need the `read:org` scope to use this endpoint.
  """
  secret_scanning_list_org_pattern_configs(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Secret_scanning_pattern_configuration

  """
  Lists repository security advisories for an organization.
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
  """
  security_advisories_list_org_repository_advisories(
    """The organization name. The name is not case sensitive."""
    org: String!
    direction: direction_parameter = desc
    sort: queryInput_security_advisories_list_org_repository_advisories_sort = created

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of advisories to return per page. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: PositiveInt = 30
    state: queryInput_security_advisories_list_org_repository_advisories_state
  ): [repository_advisory]

  """
  > [!WARNING]
  > **Closing down notice:** This operation is closing down and will be removed starting January 1, 2026. Please use the "[Organization Roles](https://docs.github.com/rest/orgs/organization-roles)" endpoints instead.
  """
  orgs_list_security_manager_teams(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): [team_simple] @deprecated(reason: "deprecated")

  """
  Gets the immutable releases policy for repositories in an organization.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_get_immutable_releases_settings(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Check_immutable_releases_organization_settings

  """
  List all of the repositories that have been selected for immutable releases enforcement in an organization.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_get_immutable_releases_settings_repositories(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): orgs_get_immutable_releases_settings_repositories_200_response

  """
  Lists all hosted compute network configurations configured in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `read:network_configurations` scope to use this endpoint.
  """
  hosted_compute_list_network_configurations_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): hosted_compute_list_network_configurations_for_org_200_response

  """
  Gets a hosted compute network configuration configured in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `read:network_configurations` scope to use this endpoint.
  """
  hosted_compute_get_network_configuration_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the hosted compute network configuration."""
    network_configuration_id: String!
  ): Hosted_compute_network_configuration

  """
  Gets a hosted compute network settings resource configured for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `read:network_configurations` scope to use this endpoint.
  """
  hosted_compute_get_network_settings_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the hosted compute network settings."""
    network_settings_id: String!
  ): Hosted_compute_network_settings_resource

  """
  Use this endpoint to see a breakdown of aggregated metrics for various GitHub Copilot features. See the response schema tab for detailed metrics definitions.
  
  > [!NOTE]
  > This endpoint will only return results for a given day if the team had **five or more members with active Copilot licenses** on that day, as evaluated at the end of that day.
  
  The response contains metrics for up to 100 days prior. Metrics are processed once per day for the previous day,
  and the response will only include data up until yesterday. In order for an end user to be counted towards these metrics,
  they must have telemetry enabled in their IDE.
  
  To access this endpoint, the Copilot Metrics API access policy must be enabled for the organization containing the team within GitHub settings.
  Only organization owners for the organization that contains this team and owners and billing managers of the parent enterprise can view Copilot metrics for a team.
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot`, `read:org`, or `read:enterprise` scopes to use this endpoint.
  """
  copilot_copilot_metrics_for_team(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """
    Show usage metrics since this date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format (`YYYY-MM-DDTHH:MM:SSZ`). Maximum value is 100 days ago.
    """
    since: String

    """
    Show usage metrics until this date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format (`YYYY-MM-DDTHH:MM:SSZ`) and should not preceed the `since` date if it is passed.
    """
    until: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of days of metrics to display per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 100
  ): [Copilot_Usage_Metrics]

  """
  Lists all teams in an organization that are visible to the authenticated user.
  """
  teams_list(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
    team_type: team_type_parameter = all
  ): [team]

  """
  Gets a team using the team's `slug`. To create the `slug`, GitHub replaces special characters in the `name` string, changes all words to lowercase, and replaces spaces with a `-` separator. For example, `"My TEam Näme"` would become `my-team-name`.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `GET /organizations/{org_id}/team/{team_id}`.
  """
  teams_get_by_name(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!
  ): Full_Team @deprecated(reason: "deprecated")

  """
  The return hash contains a `role` field which refers to the Organization Invitation role and will be one of the following values: `direct_member`, `admin`, `billing_manager`, `hiring_manager`, or `reinstate`. If the invitee is not a GitHub member, the `login` field in the return hash will be `null`.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `GET /organizations/{org_id}/team/{team_id}/invitations`.
  """
  teams_list_pending_invitations_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Organization_Invitation]

  """
  Team members will include the members of child teams.
  
  To list members in a team, the team must be visible to the authenticated user.
  """
  teams_list_members_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!
    role: queryInput_teams_list_members_in_org_role = all

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """
  Team members will include the members of child teams.
  
  To get a user's membership with a team, the team must be visible to the authenticated user.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `GET /organizations/{org_id}/team/{team_id}/memberships/{username}`.
  
  > [!NOTE]
  > The response contains the `state` of the membership and the member's `role`.
  
  The `role` for organization owners is set to `maintainer`. For more information about `maintainer` roles, see [Create a team](https://docs.github.com/rest/teams/teams#create-a-team).
  """
  teams_get_membership_for_user_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Team_Membership @deprecated(reason: "deprecated")

  """
  Lists a team's repositories visible to the authenticated user.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `GET /organizations/{org_id}/team/{team_id}/repos`.
  """
  teams_list_repos_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """
  Checks whether a team has `admin`, `push`, `maintain`, `triage`, or `pull` permission for a repository. Repositories inherited through a parent team will also be checked.
  
  You can also get information about the specified repository, including what permissions the team grants on it, by passing the following custom [media type](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types/) via the `application/vnd.github.v3.repository+json` accept header.
  
  If a team doesn't have permission for the repository, you will receive a `404 Not Found` response status.
  
  If the repository is private, you must have at least `read` permission for that repository, and your token must have the `repo` or `admin:org` scope. Otherwise, you will receive a `404 Not Found` response status.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `GET /organizations/{org_id}/team/{team_id}/repos/{owner}/{repo}`.
  """
  teams_check_permissions_for_repo_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): teams_check_permissions_for_repo_in_org_response

  """
  Lists the child teams of the team specified by `{team_slug}`.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `GET /organizations/{org_id}/team/{team_id}/teams`.
  """
  teams_list_child_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [team]

  """
  > [!NOTE]
  > Accessing this endpoint does not count against your REST API rate limit.
  
  Some categories of endpoints have custom rate limits that are separate from the rate limit governing the other REST API endpoints. For this reason, the API response categorizes your rate limit. Under `resources`, you'll see objects relating to different categories:
  * The `core` object provides your rate limit status for all non-search-related resources in the REST API.
  * The `search` object provides your rate limit status for the REST API for searching (excluding code searches). For more information, see "[Search](https://docs.github.com/rest/search/search)."
  * The `code_search` object provides your rate limit status for the REST API for searching code. For more information, see "[Search code](https://docs.github.com/rest/search/search#search-code)."
  * The `graphql` object provides your rate limit status for the GraphQL API. For more information, see "[Resource limitations](https://docs.github.com/graphql/overview/resource-limitations#rate-limit)."
  * The `integration_manifest` object provides your rate limit status for the `POST /app-manifests/{code}/conversions` operation. For more information, see "[Creating a GitHub App from a manifest](https://docs.github.com/apps/creating-github-apps/setting-up-a-github-app/creating-a-github-app-from-a-manifest#3-you-exchange-the-temporary-code-to-retrieve-the-app-configuration)."
  * The `dependency_snapshots` object provides your rate limit status for submitting snapshots to the dependency graph. For more information, see "[Dependency graph](https://docs.github.com/rest/dependency-graph)."
  * The `dependency_sbom` object provides your rate limit status for requesting SBOMs from the dependency graph. For more information, see "[Dependency graph](https://docs.github.com/rest/dependency-graph)."
  * The `code_scanning_upload` object provides your rate limit status for uploading SARIF results to code scanning. For more information, see "[Uploading a SARIF file to GitHub](https://docs.github.com/code-security/code-scanning/integrating-with-code-scanning/uploading-a-sarif-file-to-github)."
  * The `actions_runner_registration` object provides your rate limit status for registering self-hosted runners in GitHub Actions. For more information, see "[Self-hosted runners](https://docs.github.com/rest/actions/self-hosted-runners)."
  * The `source_import` object is no longer in use for any API endpoints, and it will be removed in the next API version. For more information about API versions, see "[API Versions](https://docs.github.com/rest/about-the-rest-api/api-versions)."
  
  > [!NOTE]
  > The `rate` object is closing down. If you're writing new API client code or updating existing code, you should use the `core` object instead of the `rate` object. The `core` object contains the same information that is present in the `rate` object.
  """
  rate_limit_get: Rate_Limit_Overview

  """
  The `parent` and `source` objects are present when the repository is a fork. `parent` is the repository this repository was forked from, `source` is the ultimate source for the network.
  
  > [!NOTE]
  > - In order to see the `security_and_analysis` block for a repository you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  > - To view merge-related settings, you must have the `contents:read` and `contents:write` permissions.
  """
  repos_get(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Full_Repository

  """
  Lists all artifacts for a repository.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_list_artifacts_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The name field of an artifact. When specified, only artifacts with this name will be returned.
    """
    name: String
  ): actions_list_artifacts_for_repo_200_response

  """
  Gets a specific artifact for a workflow run.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_artifact(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the artifact."""
    artifact_id: Int!
  ): Artifact

  """
  Gets a redirect URL to download an archive for a repository. This URL expires after 1 minute. Look for `Location:` in
  the response header to find the URL for the download. The `:archive_format` must be `zip`.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_download_artifact(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the artifact."""
    artifact_id: Int!
    archive_format: String!
  ): JSON

  """
  Gets GitHub Actions cache retention limit for a repository. This determines how long caches will be retained for, if
  not manually removed or evicted due to size constraints.
  
  OAuth tokens and personal access tokens (classic) need the `admin:repository` scope to use this endpoint.
  """
  actions_get_actions_cache_retention_limit_for_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Actions_cache_retention_limit_for_a_repository

  """
  Gets GitHub Actions cache storage limit for a repository. This determines the maximum size of caches that can be
  stored before eviction occurs.
  
  OAuth tokens and personal access tokens (classic) need the `admin:repository` scope to use this endpoint.
  """
  actions_get_actions_cache_storage_limit_for_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Actions_cache_storage_limit_for_a_repository

  """
  Gets GitHub Actions cache usage for a repository.
  The data fetched using this API is refreshed approximately every 5 minutes, so values returned from this endpoint may take at least 5 minutes to get updated.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_actions_cache_usage(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Actions_Cache_Usage_by_repository

  """
  Lists the GitHub Actions caches for a repository.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_actions_cache_list(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The full Git reference for narrowing down the cache. The `ref` for a branch should be formatted as `refs/heads/<branch name>`. To reference a pull request use `refs/pull/<number>/merge`.
    """
    ref: String

    """An explicit key or prefix for identifying the cache"""
    key: String
    sort: actions_cache_list_sort_parameter = last_accessed_at
    direction: direction_parameter = desc
  ): Repository_actions_caches

  """
  Gets a specific job in a workflow run.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_job_for_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the job."""
    job_id: Int!
  ): Job

  """
  Gets a redirect URL to download a plain text file of logs for a workflow job. This link expires after 1 minute. Look
  for `Location:` in the response header to find the URL for the download.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_download_job_logs_for_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the job."""
    job_id: Int!
  ): JSON

  """
  Gets the customization template for an OpenID Connect (OIDC) subject claim.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_custom_oidc_sub_claim_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Actions_OIDC_subject_customization_for_a_repository

  """
  Lists all organization secrets shared with a repository without revealing their encrypted
  values.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_repo_organization_secrets(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_repo_organization_secrets_200_response

  """
  Lists all organization variables shared with a repository.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_repo_organization_variables(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 30). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 10

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_repo_organization_variables_200_response

  """
  Gets the GitHub Actions permissions policy for a repository, including whether GitHub Actions is enabled and the actions and reusable workflows allowed to run in the repository.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_github_actions_permissions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): actions_repository_permissions

  """
  Gets the level of access that workflows outside of the repository have to actions and reusable workflows in the repository.
  This endpoint only applies to private repositories.
  For more information, see "[Allowing access to components in a private repository](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository#allowing-access-to-components-in-a-private-repository)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_workflow_access_to_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): actions_workflow_access_to_repository

  """
  Gets artifact and log retention settings for a repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_artifact_and_log_retention_settings_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): actions_artifact_and_log_retention_response

  """
  Gets the fork PR contributor approval policy for a repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_fork_pr_contributor_approval_permissions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): actions_fork_pr_contributor_approval

  """
  Gets the settings for whether workflows from fork pull requests can run on a private repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_private_repo_fork_pr_workflows_settings_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): actions_fork_pr_workflows_private_repos

  """
  Gets the settings for selected actions and reusable workflows that are allowed in a repository. To use this endpoint, the repository policy for `allowed_actions` must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for a repository](#set-github-actions-permissions-for-a-repository)."
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_allowed_actions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): selected_actions

  """
  Gets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in a repository,
  as well as if GitHub Actions can submit approving pull request reviews.
  For more information, see "[Setting the permissions of the GITHUB_TOKEN for your repository](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository#setting-the-permissions-of-the-github_token-for-your-repository)."
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_github_actions_default_workflow_permissions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): actions_get_default_workflow_permissions

  """
  Lists all self-hosted runners configured in a repository.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_self_hosted_runners_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of a self-hosted runner."""
    name: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_self_hosted_runners_for_repo_200_response

  """
  Lists binaries for the runner application that you can download and run.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_runner_applications_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [Runner_Application]

  """
  Gets a specific self-hosted runner configured in a repository.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_self_hosted_runner_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): Self_hosted_runners

  """
  Lists all labels for a self-hosted runner configured in a repository.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_labels_for_self_hosted_runner_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): actions_runner_labels_response

  """
  Lists all workflow runs for a repository. You can use parameters to narrow the list of results. For more information about using parameters, see [Parameters](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#parameters).
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  
  This endpoint will return up to 1,000 results for each search when using the following parameters: `actor`, `branch`, `check_suite_id`, `created`, `event`, `head_sha`, `status`.
  """
  actions_list_workflow_runs_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    Returns someone's workflow runs. Use the login for the user who created the `push` associated with the check suite or workflow run.
    """
    actor: String

    """
    Returns workflow runs associated with a branch. Use the name of the branch of the `push`.
    """
    branch: String

    """
    Returns workflow run triggered by the event you specify. For example, `push`, `pull_request` or `issue`. For more information, see "[Events that trigger workflows](https://docs.github.com/actions/automating-your-workflow-with-github-actions/events-that-trigger-workflows)."
    """
    event: String
    status: workflow_run_status_parameter

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Returns workflow runs created within the given date-time range. For more information on the syntax, see "[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates)."
    """
    created: DateTime

    """If `true` pull requests are omitted from the response (empty array)."""
    exclude_pull_requests: Boolean

    """Returns workflow runs with the `check_suite_id` that you specify."""
    check_suite_id: Int

    """
    Only returns workflow runs that are associated with the specified `head_sha`.
    """
    head_sha: String
  ): actions_list_workflow_runs_for_repo_200_response

  """
  Gets a specific workflow run.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_get_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!

    """If `true` pull requests are omitted from the response (empty array)."""
    exclude_pull_requests: Boolean
  ): Workflow_Run

  """
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_get_reviews_for_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): [Environment_Approval]

  """
  Lists artifacts for a workflow run.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_list_workflow_run_artifacts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The name field of an artifact. When specified, only artifacts with this name will be returned.
    """
    name: String
    direction: direction_parameter = desc
  ): actions_list_workflow_run_artifacts_200_response

  """
  Gets a specific workflow run attempt.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_get_workflow_run_attempt(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!

    """The attempt number of the workflow run."""
    attempt_number: Int!

    """If `true` pull requests are omitted from the response (empty array)."""
    exclude_pull_requests: Boolean
  ): Workflow_Run

  """
  Lists jobs for a specific workflow run attempt. You can use parameters to narrow the list of results. For more information
  about using parameters, see [Parameters](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#parameters).
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint  with a private repository.
  """
  actions_list_jobs_for_workflow_run_attempt(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!

    """The attempt number of the workflow run."""
    attempt_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_jobs_for_workflow_run_attempt_200_response

  """
  Gets a redirect URL to download an archive of log files for a specific workflow run attempt. This link expires after
  1 minute. Look for `Location:` in the response header to find the URL for the download.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_download_workflow_run_attempt_logs(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!

    """The attempt number of the workflow run."""
    attempt_number: Int!
  ): JSON

  """
  Lists jobs for a workflow run. You can use parameters to narrow the list of results. For more information
  about using parameters, see [Parameters](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#parameters).
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_list_jobs_for_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
    filter: queryInput_actions_list_jobs_for_workflow_run_filter = latest

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_jobs_for_workflow_run_200_response

  """
  Gets a redirect URL to download an archive of log files for a workflow run. This link expires after 1 minute. Look for
  `Location:` in the response header to find the URL for the download.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_download_workflow_run_logs(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): JSON

  """
  Get all deployment environments for a workflow run that are waiting for protection rules to pass.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_pending_deployments_for_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): [Pending_Deployment]

  """
  > [!WARNING]  
  > This endpoint is in the process of closing down. Refer to "[Actions Get workflow usage and Get workflow run usage endpoints closing down](https://github.blog/changelog/2025-02-02-actions-get-workflow-usage-and-get-workflow-run-usage-endpoints-closing-down/)" for more information.
  
  Gets the number of billable minutes and total run time for a specific workflow run. Billable minutes only apply to workflows in private repositories that use GitHub-hosted runners. Usage is listed for each GitHub-hosted runner operating system in milliseconds. Any job re-runs are also included in the usage. The usage does not include the multiplier for macOS and Windows runners and is not rounded up to the nearest whole minute. For more information, see "[Managing billing for GitHub Actions](https://docs.github.com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-actions)".
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_get_workflow_run_usage(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): Workflow_Run_Usage

  """
  Lists all secrets available in a repository without revealing their encrypted
  values.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_repo_secrets(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_repo_secrets_200_response

  """
  Gets your public key, which you need to encrypt secrets. You need to
  encrypt a secret before you can create or update secrets.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_repo_public_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): ActionsPublicKey

  """
  Gets a single repository secret without revealing its encrypted value.
  
  The authenticated user must have collaborator access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
  ): Actions_Secret

  """
  Lists all repository variables.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_repo_variables(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 30). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 10

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_repo_variables_200_response

  """
  Gets a specific variable in a repository.
  
  The authenticated user must have collaborator access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_repo_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the variable."""
    name: String!
  ): Actions_Variable

  """
  Lists the workflows in a repository.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_list_repo_workflows(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_repo_workflows_200_response

  """
  Gets a specific workflow. You can replace `workflow_id` with the workflow
  file name. For example, you could use `main.yaml`.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_get_workflow(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    workflow_id: workflow_id_parameter_Input!
  ): workflow

  """
  List all workflow runs for a workflow. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`. You can use parameters to narrow the list of results. For more information about using parameters, see [Parameters](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#parameters).
  
  Anyone with read access to the repository can use this endpoint
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  
  This endpoint will return up to 1,000 results for each search when using the following parameters: `actor`, `branch`, `check_suite_id`, `created`, `event`, `head_sha`, `status`.
  """
  actions_list_workflow_runs(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    workflow_id: workflow_id_parameter_Input!

    """
    Returns someone's workflow runs. Use the login for the user who created the `push` associated with the check suite or workflow run.
    """
    actor: String

    """
    Returns workflow runs associated with a branch. Use the name of the branch of the `push`.
    """
    branch: String

    """
    Returns workflow run triggered by the event you specify. For example, `push`, `pull_request` or `issue`. For more information, see "[Events that trigger workflows](https://docs.github.com/actions/automating-your-workflow-with-github-actions/events-that-trigger-workflows)."
    """
    event: String
    status: workflow_run_status_parameter

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Returns workflow runs created within the given date-time range. For more information on the syntax, see "[Understanding the search syntax](https://docs.github.com/search-github/getting-started-with-searching-on-github/understanding-the-search-syntax#query-for-dates)."
    """
    created: DateTime

    """If `true` pull requests are omitted from the response (empty array)."""
    exclude_pull_requests: Boolean

    """Returns workflow runs with the `check_suite_id` that you specify."""
    check_suite_id: Int

    """
    Only returns workflow runs that are associated with the specified `head_sha`.
    """
    head_sha: String
  ): actions_list_workflow_runs_200_response

  """
  > [!WARNING]  
  > This endpoint is in the process of closing down. Refer to "[Actions Get workflow usage and Get workflow run usage endpoints closing down](https://github.blog/changelog/2025-02-02-actions-get-workflow-usage-and-get-workflow-run-usage-endpoints-closing-down/)" for more information.
  
  Gets the number of billable minutes used by a specific workflow during the current billing cycle. Billable minutes only apply to workflows in private repositories that use GitHub-hosted runners. Usage is listed for each GitHub-hosted runner operating system in milliseconds. Any job re-runs are also included in the usage. The usage does not include the multiplier for macOS and Windows runners and is not rounded up to the nearest whole minute. For more information, see "[Managing billing for GitHub Actions](https://docs.github.com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-actions)".
  
  You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_get_workflow_usage(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    workflow_id: workflow_id_parameter_Input!
  ): Workflow_Usage

  """
  Lists a detailed history of changes to a repository, such as pushes, merges, force pushes, and branch changes, and associates these changes with commits and users.
  
  For more information about viewing repository activity,
  see "[Viewing activity and data for your repository](https://docs.github.com/repositories/viewing-activity-and-data-for-your-repository)."
  """
  repos_list_activities(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    direction: direction_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The Git reference for the activities you want to list.
    
    The `ref` for a branch can be formatted either as `refs/heads/BRANCH_NAME` or `BRANCH_NAME`, where `BRANCH_NAME` is the name of your branch.
    """
    ref: String

    """
    The GitHub username to use to filter by the actor who performed the activity.
    """
    actor: String
    time_period: queryInput_repos_list_activities_time_period
    activity_type: queryInput_repos_list_activities_activity_type
  ): [Activity]

  """
  Lists the [available assignees](https://docs.github.com/articles/assigning-issues-and-pull-requests-to-other-github-users/) for issues in a repository.
  """
  issues_list_assignees(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """
  Checks if a user has permission to be assigned to an issue in this repository.
  
  If the `assignee` can be assigned to issues in the repository, a `204` header with no content is returned.
  
  Otherwise a `404` status code is returned.
  """
  issues_check_user_can_be_assigned(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    assignee: String!
  ): Void

  """
  List a collection of artifact attestations with a given subject digest that are associated with a repository.
  
  The authenticated user making the request must have read access to the repository. In addition, when using a fine-grained access token the `attestations:read` permission is required.
  
  **Please note:** in order to offer meaningful security benefits, an attestation's signature and timestamps **must** be cryptographically verified, and the identity of the attestation signer **must** be validated. Attestations can be verified using the [GitHub CLI `attestation verify` command](https://cli.github.com/manual/gh_attestation_verify). For more information, see [our guide on how to use artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
  """
  repos_list_attestations(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The parameter should be set to the attestation's subject's SHA256 digest, in the form `sha256:HEX_DIGEST`.
    """
    subject_digest: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    Optional filter for fetching attestations with a given predicate type.
    This option accepts `provenance`, `sbom`, `release`, or freeform text
    for custom predicate types.
    """
    predicate_type: String
  ): repos_list_attestations_200_response

  """
  Gets all autolinks that are configured for a repository.
  
  Information about autolinks are only available to repository administrators.
  """
  repos_list_autolinks(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [Autolink_reference]

  """
  This returns a single autolink reference by ID that was configured for the given repository.
  
  Information about autolinks are only available to repository administrators.
  """
  repos_get_autolink(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the autolink."""
    autolink_id: Int!
  ): Autolink_reference

  """
  Shows whether Dependabot security updates are enabled, disabled or paused for a repository. The authenticated user must have admin read access to the repository. For more information, see "[Configuring Dependabot security updates](https://docs.github.com/articles/configuring-automated-security-fixes)".
  """
  repos_check_automated_security_fixes(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Check_Dependabot_security_updates

  """List branches"""
  repos_list_branches(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    Setting to `true` returns only branches protected by branch protections or rulesets. When set to `false`, only unprotected branches are returned. Omitting this parameter returns all branches.
    """
    protected: Boolean

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Short_Branch]

  """Get a branch"""
  repos_get_branch(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Branch_With_Protection

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_get_branch_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Branch_Protection

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_get_admin_branch_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Protected_Branch_Admin_Enforced

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_get_pull_request_review_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Protected_Branch_Pull_Request_Review

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  When authenticated with admin or owner permissions to the repository, you can use this endpoint to check whether a branch requires signed commits. An enabled status of `true` indicates you must sign commits on this branch. For more information, see [Signing commits with GPG](https://docs.github.com/articles/signing-commits-with-gpg) in GitHub Help.
  
  > [!NOTE]
  > You must enable branch protection to require signed commits.
  """
  repos_get_commit_signature_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Protected_Branch_Admin_Enforced

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_get_status_checks_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Status_Check_Policy

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_get_all_status_check_contexts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): [String]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Lists who has access to this protected branch.
  
  > [!NOTE]
  > Users, apps, and teams `restrictions` are only available for organization-owned repositories.
  """
  repos_get_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Branch_Restriction_Policy

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Lists the GitHub Apps that have push access to this branch. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
  """
  repos_get_apps_with_access_to_protected_branch(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): [integration]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Lists the teams who have push access to this branch. The list includes child teams.
  """
  repos_get_teams_with_access_to_protected_branch(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): [team]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Lists the people who have push access to this branch.
  """
  repos_get_users_with_access_to_protected_branch(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): [simple_user]

  """
  Gets a single check run using its `id`.
  
  > [!NOTE]
  > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
  """
  checks_get(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the check run."""
    check_run_id: Int!
  ): check_run

  """
  Lists annotations for a check run using the annotation `id`.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
  """
  checks_list_annotations(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the check run."""
    check_run_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Check_Annotation]

  """
  Gets a single check suite using its `id`.
  
  > [!NOTE]
  > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
  """
  checks_get_suite(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the check suite."""
    check_suite_id: Int!
  ): CheckSuite

  """
  Lists check runs for a check suite using its `id`.
  
  > [!NOTE]
  > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
  """
  checks_list_for_suite(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the check suite."""
    check_suite_id: Int!

    """Returns check runs with the specified `name`."""
    check_name: String
    status: status_parameter
    filter: queryInput_checks_list_for_suite_filter = latest

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): checks_list_for_suite_200_response

  """
  Lists code scanning alerts.
  
  The response includes a `most_recent_instance` object.
  This provides details of the most recent instance of this alert
  for the default branch (or for the specified Git reference if you used `ref` in the request).
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_list_alerts_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the tool used to generate the code scanning analysis."""
    tool_name: String

    """
    The GUID of the tool used to generate the code scanning analysis, if provided in the uploaded SARIF data.
    """
    tool_guid: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`,
    `refs/heads/<branch name>` or simply `<branch name>`.
    """
    ref: String

    """The number of the pull request for the results you want to list."""
    pr: Int
    direction: direction_parameter = desc

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
    sort: queryInput_code_scanning_list_alerts_for_repo_sort = created
    state: code_scanning_alert_state_query
    severity: code_scanning_alert_severity

    """
    Filter alerts by assignees. Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`).
    Use `*` to list alerts with at least one assignee or `none` to list alerts with no assignees.
    """
    assignees: String
  ): [code_scanning_alert_items]

  """
  Gets a single code scanning alert.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_alert(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
  ): code_scanning_alert

  """
  Gets the status and description of an autofix for a code scanning alert.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_autofix(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
  ): code_scanning_autofix

  """
  Lists all instances of the specified code scanning alert.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_list_alert_instances(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`,
    `refs/heads/<branch name>` or simply `<branch name>`.
    """
    ref: String

    """The number of the pull request for the results you want to list."""
    pr: Int
  ): [code_scanning_alert_instance_list]

  """
  Lists the details of all code scanning analyses for a repository,
  starting with the most recent.
  The response is paginated and you can use the `page` and `per_page` parameters
  to list the analyses you're interested in.
  By default 30 analyses are listed per page.
  
  The `rules_count` field in the response give the number of rules
  that were run in the analysis.
  For very old analyses this data is not available,
  and `0` is returned in this field.
  
  > [!WARNING]
  > **Closing down notice:** The `tool_name` field is closing down and will, in future, not be included in the response for this endpoint. The example response reflects this change. The tool name can now be found inside the `tool` field.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_list_recent_analyses(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the tool used to generate the code scanning analysis."""
    tool_name: String

    """
    The GUID of the tool used to generate the code scanning analysis, if provided in the uploaded SARIF data.
    """
    tool_guid: String

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """The number of the pull request for the results you want to list."""
    pr: Int

    """
    The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`,
    `refs/heads/<branch name>` or simply `<branch name>`.
    """
    ref: String

    """An identifier for the upload."""
    sarif_id: String
    direction: direction_parameter = desc
    sort: created_const
  ): [code_scanning_analysis]

  """
  Gets a specified code scanning analysis for a repository.
  
  The default JSON response contains fields that describe the analysis.
  This includes the Git reference and commit SHA to which the analysis relates,
  the datetime of the analysis, the name of the code scanning tool,
  and the number of alerts.
  
  The `rules_count` field in the default response give the number of rules
  that were run in the analysis.
  For very old analyses this data is not available,
  and `0` is returned in this field.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/sarif+json`**: Instead of returning a summary of the analysis, this endpoint returns a subset of the analysis data that was uploaded. The data is formatted as [SARIF version 2.1.0](https://docs.oasis-open.org/sarif/sarif/v2.1.0/cs01/sarif-v2.1.0-cs01.html). It also returns additional data such as the `github/alertNumber` and `github/alertUrl` properties.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_analysis(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The ID of the analysis, as returned from the `GET /repos/{owner}/{repo}/code-scanning/analyses` operation.
    """
    analysis_id: Int!
  ): code_scanning_get_analysis_200_response

  """
  Lists the CodeQL databases that are available in a repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_list_codeql_databases(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [CodeQL_Database]

  """
  Gets a CodeQL database for a language in a repository.
  
  By default this endpoint returns JSON metadata about the CodeQL database. To
  download the CodeQL database binary content, set the `Accept` header of the request
  to [`application/zip`](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types), and make sure
  your HTTP client is configured to follow redirects or use the `Location` header
  to make a second request to get the redirect URL.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_codeql_database(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The language of the CodeQL database."""
    language: String!
  ): CodeQL_Database

  """
  Gets the summary of a CodeQL variant analysis.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_variant_analysis(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the variant analysis."""
    codeql_variant_analysis_id: Int!
  ): Variant_Analysis

  """
  Gets the analysis status of a repository in a CodeQL variant analysis.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_variant_analysis_repo_task(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """The name of the controller repository."""
    repo: String!

    """The ID of the variant analysis."""
    codeql_variant_analysis_id: Int!

    """
    The account owner of the variant analysis repository. The name is not case sensitive.
    """
    repo_owner: String!

    """The name of the variant analysis repository."""
    repo_name: String!
  ): code_scanning_variant_analysis_repo_task

  """
  Gets a code scanning default setup configuration.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_default_setup(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): code_scanning_default_setup

  """
  Gets information about a SARIF upload, including the status and the URL of the analysis that was uploaded so that you can retrieve details of the analysis. For more information, see "[Get a code scanning analysis for a repository](/rest/code-scanning/code-scanning#get-a-code-scanning-analysis-for-a-repository)."
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_get_sarif(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SARIF ID obtained after uploading."""
    sarif_id: String!
  ): code_scanning_sarifs_status

  """
  Get the code security configuration that manages a repository's code security settings.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  code_security_get_configuration_for_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): code_security_get_configuration_for_repository_response

  """
  List any syntax errors that are detected in the CODEOWNERS
  file.
  
  For more information about the correct CODEOWNERS syntax,
  see "[About code owners](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners)."
  """
  repos_codeowners_errors(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    A branch, tag or commit name used to determine which version of the CODEOWNERS file to use. Default: the repository's default branch (e.g. `main`)
    """
    ref: String
  ): CODEOWNERS_errors

  """
  Lists the codespaces associated to a specified repository and the authenticated user.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_list_in_repository_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): codespaces_list_in_repository_for_authenticated_user_200_response

  """
  Lists the devcontainer.json files associated with a specified repository and the authenticated user. These files
  specify launchpoint configurations for codespaces created within the repository.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_list_devcontainers_in_repository_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): codespaces_list_devcontainers_in_repository_for_authenticated_user_200_response

  """
  List the machine types available for a given repository based on its configuration.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_repo_machines_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The location to check for available machines. Assigned by IP if not provided.
    """
    location: String

    """IP for location auto-detection when proxying a request"""
    client_ip: String

    """
    The branch or commit to check for prebuild availability and devcontainer restrictions.
    """
    ref: String
  ): codespaces_repo_machines_for_authenticated_user_200_response

  """
  Gets the default attributes for codespaces created by the user with the repository.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_pre_flight_with_repo_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The branch or commit to check for a default devcontainer path. If not specified, the default branch will be checked.
    """
    ref: String

    """
    An alternative IP for default location auto-detection, such as when proxying a request.
    """
    client_ip: String
  ): codespaces_pre_flight_with_repo_for_authenticated_user_200_response

  """
  Checks whether the permissions defined by a given devcontainer configuration have been accepted by the authenticated user.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_check_permissions_for_devcontainer(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The git reference that points to the location of the devcontainer configuration to use for the permission check. The value of `ref` will typically be a branch name (`heads/BRANCH_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!

    """
    Path to the devcontainer.json configuration to use for the permission check.
    """
    devcontainer_path: String!
  ): Codespaces_Permissions_Check

  """
  Lists all development environment secrets available in a repository without revealing their encrypted
  values.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  codespaces_list_repo_secrets(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): codespaces_list_repo_secrets_200_response

  """
  Gets your public key, which you need to encrypt secrets. You need to
  encrypt a secret before you can create or update secrets.
  
  If the repository is private, OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  codespaces_get_repo_public_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): CodespacesPublicKey

  """
  Gets a single repository development environment secret without revealing its encrypted value.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  codespaces_get_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
  ): repo_codespaces_secret

  """
  For organization-owned repositories, the list of collaborators includes outside collaborators, organization members that are direct collaborators, organization members with access through team memberships, organization members with access through default organization permissions, and organization owners.
  The `permissions` hash returned in the response contains the base role permissions of the collaborator. The `role_name` is the highest role assigned to the collaborator after considering all sources of grants, including: repo, teams, organization, and enterprise.
  There is presently not a way to differentiate between an organization level grant and a repository level grant from this endpoint response.
  
  Team members will include the members of child teams.
  
  The authenticated user must have write, maintain, or admin privileges on the repository to use this endpoint. For organization-owned repositories, the authenticated user needs to be a member of the organization.
  OAuth app tokens and personal access tokens (classic) need the `read:org` and `repo` scopes to use this endpoint.
  """
  repos_list_collaborators(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    affiliation: queryInput_repos_list_collaborators_affiliation = all
    permission: queryInput_repos_list_collaborators_permission

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [collaborator]

  """
  For organization-owned repositories, the list of collaborators includes outside collaborators, organization members that are direct collaborators, organization members with access through team memberships, organization members with access through default organization permissions, and organization owners.
  
  Team members will include the members of child teams.
  
  The authenticated user must have push access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:org` and `repo` scopes to use this endpoint.
  """
  repos_check_collaborator(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Checks the repository permission and role of a collaborator.
  
  The `permission` attribute provides the legacy base roles of `admin`, `write`, `read`, and `none`, where the
  `maintain` role is mapped to `write` and the `triage` role is mapped to `read`.
  The `role_name` attribute provides the name of the assigned role, including custom roles. The
  `permission` can also be used to determine which base level of access the collaborator has to the repository.
  
  The calculated permissions are the highest role assigned to the collaborator after considering all sources of grants, including: repo, teams, organization, and enterprise.
  There is presently not a way to differentiate between an organization level grant and a repository level grant from this endpoint response.
  """
  repos_get_collaborator_permission_level(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Repository_Collaborator_Permission

  """
  Lists the commit comments for a specified repository. Comments are ordered by ascending ID.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  repos_list_commit_comments_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Commit_Comment]

  """
  Gets a specified commit comment.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  repos_get_commit_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): Commit_Comment

  """
  List the reactions to a [commit comment](https://docs.github.com/rest/commits/comments#get-a-commit-comment).
  """
  reactions_list_for_commit_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    content: queryInput_reactions_list_for_commit_comment_content

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Reaction]

  """
  **Signature verification object**
  
  The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
  
  | Name | Type | Description |
  | ---- | ---- | ----------- |
  | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
  | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
  | `signature` | `string` | The signature that was extracted from the commit. |
  | `payload` | `string` | The value that was signed. |
  | `verified_at` | `string` | The date the signature was verified by GitHub. |
  
  These are the possible values for `reason` in the `verification` object:
  
  | Value | Description |
  | ----- | ----------- |
  | `expired_key` | The key that made the signature is expired. |
  | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
  | `gpgverify_error` | There was an error communicating with the signature verification service. |
  | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
  | `unsigned` | The object does not include a signature. |
  | `unknown_signature_type` | A non-PGP signature was found in the commit. |
  | `no_user` | No user was associated with the `committer` email address in the commit. |
  | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
  | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
  | `unknown_key` | The key that made the signature has not been registered with any user's account. |
  | `malformed_signature` | There was an error parsing the signature. |
  | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
  | `valid` | None of the above errors applied, so the signature is considered to be verified. |
  """
  repos_list_commits(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    SHA or branch to start listing commits from. Default: the repository’s default branch (usually `main`).
    """
    sha: String

    """Only commits containing this file path will be returned."""
    path: String

    """GitHub username or email address to use to filter by commit author."""
    author: String

    """GitHub username or email address to use to filter by commit committer."""
    committer: String

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Due to limitations of Git, timestamps must be between 1970-01-01 and 2099-12-31 (inclusive) or unexpected results may be returned.
    """
    since: DateTime

    """
    Only commits before this date will be returned. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Due to limitations of Git, timestamps must be between 1970-01-01 and 2099-12-31 (inclusive) or unexpected results may be returned.
    """
    until: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Commit]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Returns all branches where the given commit SHA is the HEAD, or latest commit for the branch.
  """
  repos_list_branches_for_head_commit(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SHA of the commit."""
    commit_sha: String!
  ): [Branch_Short]

  """
  Lists the comments for a specified commit.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  repos_list_comments_for_commit(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SHA of the commit."""
    commit_sha: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Commit_Comment]

  """
  Lists the merged pull request that introduced the commit to the repository. If the commit is not present in the default branch, it will return merged and open pull requests associated with the commit.
  
  To list the open or merged pull requests associated with a branch, you can set the `commit_sha` parameter to the branch name.
  """
  repos_list_pull_requests_associated_with_commit(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SHA of the commit."""
    commit_sha: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Pull_Request_Simple]

  """
  Returns the contents of a single commit reference. You must have `read` access for the repository to use this endpoint.
  
  > [!NOTE]
  > If there are more than 300 files in the commit diff and the default JSON media type is requested, the response will include pagination link headers for the remaining files, up to a limit of 3000 files. Each page contains the static commit information, and the only changes are to the file listing.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." Pagination query parameters are not supported for these media types.
  
  - **`application/vnd.github.diff`**: Returns the diff of the commit. Larger diffs may time out and return a 5xx status code.
  - **`application/vnd.github.patch`**: Returns the patch of the commit. Diffs with binary data will have no `patch` property. Larger diffs may time out and return a 5xx status code.
  - **`application/vnd.github.sha`**: Returns the commit's SHA-1 hash. You can use this endpoint to check if a remote reference's SHA-1 hash is the same as your local reference's SHA-1 hash by providing the local SHA-1 reference as the ETag.
  
  **Signature verification object**
  
  The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
  
  | Name | Type | Description |
  | ---- | ---- | ----------- |
  | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
  | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
  | `signature` | `string` | The signature that was extracted from the commit. |
  | `payload` | `string` | The value that was signed. |
  | `verified_at` | `string` | The date the signature was verified by GitHub. |
  
  These are the possible values for `reason` in the `verification` object:
  
  | Value | Description |
  | ----- | ----------- |
  | `expired_key` | The key that made the signature is expired. |
  | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
  | `gpgverify_error` | There was an error communicating with the signature verification service. |
  | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
  | `unsigned` | The object does not include a signature. |
  | `unknown_signature_type` | A non-PGP signature was found in the commit. |
  | `no_user` | No user was associated with the `committer` email address in the commit. |
  | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
  | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
  | `unknown_key` | The key that made the signature has not been registered with any user's account. |
  | `malformed_signature` | There was an error parsing the signature. |
  | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
  | `valid` | None of the above errors applied, so the signature is considered to be verified. |
  """
  repos_get_commit(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): Commit

  """
  Lists check runs for a commit ref. The `ref` can be a SHA, branch name, or a tag name.
  
  > [!NOTE]
  > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array.
  
  If there are more than 1000 check suites on a single git reference, this endpoint will limit check runs to the 1000 most recent check suites. To iterate over all possible check runs, use the [List check suites for a Git reference](https://docs.github.com/rest/reference/checks#list-check-suites-for-a-git-reference) endpoint and provide the `check_suite_id` parameter to the [List check runs in a check suite](https://docs.github.com/rest/reference/checks#list-check-runs-in-a-check-suite) endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
  """
  checks_list_for_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!

    """Returns check runs with the specified `name`."""
    check_name: String
    status: status_parameter
    filter: queryInput_checks_list_for_ref_filter = latest

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
    app_id: Int
  ): checks_list_for_ref_200_response

  """
  Lists check suites for a commit `ref`. The `ref` can be a SHA, branch name, or a tag name.
  
  > [!NOTE]
  > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository.
  """
  checks_list_suites_for_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!

    """Filters check suites by GitHub App `id`."""
    app_id: Int

    """Returns check runs with the specified `name`."""
    check_name: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): checks_list_suites_for_ref_200_response

  """
  Users with pull access in a repository can access a combined view of commit statuses for a given ref. The ref can be a SHA, a branch name, or a tag name.
  
  
  Additionally, a combined `state` is returned. The `state` is one of:
  
  *   **failure** if any of the contexts report as `error` or `failure`
  *   **pending** if there are no statuses or a context is `pending`
  *   **success** if the latest status for all contexts is `success`
  """
  repos_get_combined_status_for_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): Combined_Commit_Status

  """
  Users with pull access in a repository can view commit statuses for a given ref. The ref can be a SHA, a branch name, or a tag name. Statuses are returned in reverse chronological order. The first status in the list will be the latest one.
  
  This resource is also available via a legacy route: `GET /repos/:owner/:repo/statuses/:ref`.
  """
  repos_list_commit_statuses_for_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Status]

  """
  Returns all community profile metrics for a repository. The repository cannot be a fork.
  
  The returned metrics include an overall health score, the repository description, the presence of documentation, the
  detected code of conduct, the detected license, and the presence of ISSUE\_TEMPLATE, PULL\_REQUEST\_TEMPLATE,
  README, and CONTRIBUTING files.
  
  The `health_percentage` score is defined as a percentage of how many of
  the recommended community health files are present. For more information, see
  "[About community profiles for public repositories](https://docs.github.com/communities/setting-up-your-project-for-healthy-contributions/about-community-profiles-for-public-repositories)."
  
  `content_reports_enabled` is only returned for organization-owned repositories.
  """
  repos_get_community_profile_metrics(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Community_Profile

  """
  Compares two commits against one another. You can compare refs (branches or tags) and commit SHAs in the same repository, or you can compare refs and commit SHAs that exist in different repositories within the same repository network, including fork branches. For more information about how to view a repository's network, see "[Understanding connections between repositories](https://docs.github.com/repositories/viewing-activity-and-data-for-your-repository/understanding-connections-between-repositories)."
  
  This endpoint is equivalent to running the `git log BASE..HEAD` command, but it returns commits in a different order. The `git log BASE..HEAD` command returns commits in reverse chronological order, whereas the API returns commits in chronological order.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.diff`**: Returns the diff of the commit.
  - **`application/vnd.github.patch`**: Returns the patch of the commit. Diffs with binary data will have no `patch` property.
  
  The API response includes details about the files that were changed between the two commits. This includes the status of the change (if a file was added, removed, modified, or renamed), and details of the change itself. For example, files with a `renamed` status have a `previous_filename` field showing the previous filename of the file, and files with a `modified` status have a `patch` field showing the changes made to the file.
  
  When calling this endpoint without any paging parameter (`per_page` or `page`), the returned list is limited to 250 commits, and the last commit in the list is the most recent of the entire comparison.
  
  **Working with large comparisons**
  
  To process a response with a large number of commits, use a query parameter (`per_page` or `page`) to paginate the results. When using pagination:
  
  - The list of changed files is only shown on the first page of results, and it includes up to 300 changed files for the entire comparison.
  - The results are returned in chronological order, but the last commit in the returned list may not be the most recent one in the entire set if there are more pages of results.
  
  For more information on working with pagination, see "[Using pagination in the REST API](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api)."
  
  **Signature verification object**
  
  The response will include a `verification` object that describes the result of verifying the commit's signature. The `verification` object includes the following fields:
  
  | Name | Type | Description |
  | ---- | ---- | ----------- |
  | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
  | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
  | `signature` | `string` | The signature that was extracted from the commit. |
  | `payload` | `string` | The value that was signed. |
  | `verified_at` | `string` | The date the signature was verified by GitHub. |
  
  These are the possible values for `reason` in the `verification` object:
  
  | Value | Description |
  | ----- | ----------- |
  | `expired_key` | The key that made the signature is expired. |
  | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
  | `gpgverify_error` | There was an error communicating with the signature verification service. |
  | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
  | `unsigned` | The object does not include a signature. |
  | `unknown_signature_type` | A non-PGP signature was found in the commit. |
  | `no_user` | No user was associated with the `committer` email address in the commit. |
  | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
  | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
  | `unknown_key` | The key that made the signature has not been registered with any user's account. |
  | `malformed_signature` | There was an error parsing the signature. |
  | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
  | `valid` | None of the above errors applied, so the signature is considered to be verified. |
  """
  repos_compare_commits(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The base branch and head branch to compare. This parameter expects the format `BASE...HEAD`. Both must be branch names in `repo`. To compare with a branch that exists in a different repository in the same network as `repo`, the `basehead` parameter expects the format `USERNAME:BASE...USERNAME:HEAD`.
    """
    basehead: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): Commit_Comparison

  """
  Gets the contents of a file or directory in a repository. Specify the file path or directory with the `path` parameter. If you omit the `path` parameter, you will receive the contents of the repository's root directory.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw file contents for files and symlinks.
  - **`application/vnd.github.html+json`**: Returns the file contents in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup).
  - **`application/vnd.github.object+json`**: Returns the contents in a consistent object format regardless of the content type. For example, instead of an array of objects for a directory, the response will be an object with an `entries` attribute containing the array of objects.
  
  If the content is a directory, the response will be an array of objects, one object for each item in the directory. When listing the contents of a directory, submodules have their "type" specified as "file". Logically, the value _should_ be "submodule". This behavior exists [for backwards compatibility purposes](https://git.io/v1YCW). In the next major version of the API, the type will be returned as "submodule".
  
  If the content is a symlink and the symlink's target is a normal file in the repository, then the API responds with the content of the file. Otherwise, the API responds with an object describing the symlink itself.
  
  If the content is a submodule, the `submodule_git_url` field identifies the location of the submodule repository, and the `sha` identifies a specific commit within the submodule repository. Git uses the given URL when cloning the submodule repository, and checks out the submodule at that specific commit. If the submodule repository is not hosted on github.com, the Git URLs (`git_url` and `_links["git"]`) and the github.com URLs (`html_url` and `_links["html"]`) will have null values.
  
  **Notes**:
  
  - To get a repository's contents recursively, you can [recursively get the tree](https://docs.github.com/rest/git/trees#get-a-tree).
  - This API has an upper limit of 1,000 files for a directory. If you need to retrieve
  more files, use the [Git Trees API](https://docs.github.com/rest/git/trees#get-a-tree).
  - Download URLs expire and are meant to be used just once. To ensure the download URL does not expire, please use the contents API to obtain a fresh download URL for each download.
  - If the requested file's size is:
    - 1 MB or smaller: All features of this endpoint are supported.
    - Between 1-100 MB: Only the `raw` or `object` custom media types are supported. Both will work as normal, except that when using the `object` media type, the `content` field will be an empty
  string and the `encoding` field will be `"none"`. To get the contents of these larger files, use the `raw` media type.
    - Greater than 100 MB: This endpoint is not supported.
  """
  repos_get_content(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """path parameter"""
    path: String!

    """
    The name of the commit/branch/tag. Default: the repository’s default branch.
    """
    ref: String
    input: queryInput_repos_get_content_input_Input
  ): [repos_get_content_200_response]

  """
  Lists contributors to the specified repository and sorts them by the number of commits per contributor in descending order. This endpoint may return information that is a few hours old because the GitHub REST API caches contributor data to improve performance.
  
  GitHub identifies contributors by author email address. This endpoint groups contribution counts by GitHub user, which includes all associated email addresses. To improve performance, only the first 500 author email addresses in the repository link to GitHub users. The rest will appear as anonymous contributors without associated GitHub user information.
  """
  repos_list_contributors(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Set to `1` or `true` to include anonymous contributors in results."""
    anon: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [repos_list_contributors_response]

  """
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  dependabot_list_alerts_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    A comma-separated list of states. If specified, only alerts with these states will be returned.
    
    Can be: `auto_dismissed`, `dismissed`, `fixed`, `open`
    """
    state: String

    """
    A comma-separated list of severities. If specified, only alerts with these severities will be returned.
    
    Can be: `low`, `medium`, `high`, `critical`
    """
    severity: String

    """
    A comma-separated list of ecosystems. If specified, only alerts for these ecosystems will be returned.
    
    Can be: `composer`, `go`, `maven`, `npm`, `nuget`, `pip`, `pub`, `rubygems`, `rust`
    """
    ecosystem: String

    """
    A comma-separated list of package names. If specified, only alerts for these packages will be returned.
    """
    package: String

    """
    A comma-separated list of full manifest paths. If specified, only alerts for these manifests will be returned.
    """
    manifest: String

    """
    CVE Exploit Prediction Scoring System (EPSS) percentage. Can be specified as:
    - An exact number (`n`)
    - Comparators such as `>n`, `<n`, `>=n`, `<=n`
    - A range like `n..n`, where `n` is a number from 0.0 to 1.0
    
    Filters the list of alerts based on EPSS percentages. If specified, only alerts with the provided EPSS percentages will be returned.
    """
    epss_percentage: String
    has: [dependabot_alert_comma_separated_has_parameter_Input]

    """
    Filter alerts by assignees.
    Provide a comma-separated list of user handles (e.g., `octocat` or `octocat,hubot`) to return alerts assigned to any of the specified users.
    Use `*` to list alerts with at least one assignee or `none` to list alerts with no assignees.
    """
    assignee: String
    scope: dependabot_alert_scope_parameter
    sort: dependabot_alert_sort_parameter = created
    direction: direction_parameter = desc

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [dependabot_alert]

  """
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  dependabot_get_alert(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
  ): dependabot_alert

  """
  Lists all secrets available in a repository without revealing their encrypted
  values.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  dependabot_list_repo_secrets(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): dependabot_list_repo_secrets_200_response

  """
  Gets your public key, which you need to encrypt secrets. You need to
  encrypt a secret before you can create or update secrets. Anyone with read access
  to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint if the repository is private.
  """
  dependabot_get_repo_public_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): DependabotPublicKey

  """
  Gets a single repository secret without revealing its encrypted value.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  dependabot_get_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
  ): Dependabot_Secret

  """
  Gets the diff of the dependency changes between two commits of a repository, based on the changes to the dependency manifests made in those commits.
  """
  dependency_graph_diff_range(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The base and head Git revisions to compare. The Git revisions will be resolved to commit SHAs. Named revisions will be resolved to their corresponding HEAD commits, and an appropriate merge base will be determined. This parameter expects the format `{base}...{head}`.
    """
    basehead: String!

    """
    The full path, relative to the repository root, of the dependency manifest file.
    """
    name: String
  ): [query_dependency_graph_diff_range_items]

  """
  Exports the software bill of materials (SBOM) for a repository in SPDX JSON format.
  """
  dependency_graph_export_sbom(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Dependency_Graph_SPDX_SBOM

  """Simple filtering of deployments is available via query parameters:"""
  repos_list_deployments(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SHA recorded at creation time."""
    sha: String = "none"

    """The name of the ref. This can be a branch, tag, or SHA."""
    ref: String = "none"

    """
    The name of the task for the deployment (e.g., `deploy` or `deploy:migrations`).
    """
    task: String = "none"

    """
    The name of the environment that was deployed to (e.g., `staging` or `production`).
    """
    environment: String = "none"

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [deployment]

  """Get a deployment"""
  repos_get_deployment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """deployment_id parameter"""
    deployment_id: Int!
  ): deployment

  """Users with pull access can view deployment statuses for a deployment:"""
  repos_list_deployment_statuses(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """deployment_id parameter"""
    deployment_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Deployment_Status]

  """Users with pull access can view a deployment status for a deployment:"""
  repos_get_deployment_status(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """deployment_id parameter"""
    deployment_id: Int!
    status_id: Int!
  ): Deployment_Status

  """
  Lists the environments for a repository.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  repos_get_all_environments(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): repos_get_all_environments_200_response

  """
  > [!NOTE]
  > To get information about name patterns that branches must match in order to deploy to this environment, see "[Get a deployment branch policy](/rest/deployments/branch-policies#get-a-deployment-branch-policy)."
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  repos_get_environment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
  ): Environment

  """
  Lists the deployment branch policies for an environment.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  repos_list_deployment_branch_policies(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): repos_list_deployment_branch_policies_200_response

  """
  Gets a deployment branch or tag policy for an environment.
  
  Anyone with read access to the repository can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  repos_get_deployment_branch_policy(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The unique identifier of the branch policy."""
    branch_policy_id: Int!
  ): Deployment_branch_policy

  """
  Gets all custom deployment protection rules that are enabled for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
  
  For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app).
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  repos_get_all_deployment_protection_rules(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
  ): repos_get_all_deployment_protection_rules_200_response

  """
  Gets all custom deployment protection rule integrations that are available for an environment.
  
  The authenticated user must have admin or owner permissions to the repository to use this endpoint.
  
  For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
  
  For more information about the app that is providing this custom deployment rule, see "[GET an app](https://docs.github.com/rest/apps/apps#get-an-app)".
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  repos_list_custom_deployment_rule_integrations(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): repos_list_custom_deployment_rule_integrations_200_response

  """
  Gets an enabled custom deployment protection rule for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
  
  For more information about the app that is providing this custom deployment rule, see [`GET /apps/{app_slug}`](https://docs.github.com/rest/apps/apps#get-an-app).
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  repos_get_custom_deployment_protection_rule(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The unique identifier of the protection rule."""
    protection_rule_id: Int!
  ): Deployment_protection_rule

  """
  Lists all secrets available in an environment without revealing their
  encrypted values.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_environment_secrets(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_environment_secrets_200_response

  """
  Get the public key for an environment, which you need to encrypt environment
  secrets. You need to encrypt a secret before you can create or update secrets.
  
  Anyone with read access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_environment_public_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
  ): ActionsPublicKey

  """
  Gets a single environment secret without revealing its encrypted value.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_environment_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The name of the secret."""
    secret_name: String!
  ): Actions_Secret

  """
  Lists all environment variables.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_list_environment_variables(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """
    The number of results per page (max 30). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 10

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): actions_list_environment_variables_200_response

  """
  Gets a specific variable in an environment.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_get_environment_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The name of the variable."""
    name: String!
  ): Actions_Variable

  """
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_repo_events(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """List forks"""
  repos_list_forks(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    sort: queryInput_repos_list_forks_sort = newest

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """
  The `content` in the response will always be Base64 encoded.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw blob data.
  - **`application/vnd.github+json`**: Returns a JSON representation of the blob with `content` as a base64 encoded string. This is the default if no media type is specified.
  
  **Note** This endpoint supports blobs up to 100 megabytes in size.
  """
  git_get_blob(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    file_sha: String!
  ): Blob

  """
  Gets a Git [commit object](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects).
  
  To get the contents of a commit, see "[Get a commit](/rest/commits/commits#get-a-commit)."
  
  **Signature verification object**
  
  The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
  
  | Name | Type | Description |
  | ---- | ---- | ----------- |
  | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
  | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in the table below. |
  | `signature` | `string` | The signature that was extracted from the commit. |
  | `payload` | `string` | The value that was signed. |
  | `verified_at` | `string` | The date the signature was verified by GitHub. |
  
  These are the possible values for `reason` in the `verification` object:
  
  | Value | Description |
  | ----- | ----------- |
  | `expired_key` | The key that made the signature is expired. |
  | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
  | `gpgverify_error` | There was an error communicating with the signature verification service. |
  | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
  | `unsigned` | The object does not include a signature. |
  | `unknown_signature_type` | A non-PGP signature was found in the commit. |
  | `no_user` | No user was associated with the `committer` email address in the commit. |
  | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
  | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
  | `unknown_key` | The key that made the signature has not been registered with any user's account. |
  | `malformed_signature` | There was an error parsing the signature. |
  | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
  | `valid` | None of the above errors applied, so the signature is considered to be verified. |
  """
  git_get_commit(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SHA of the commit."""
    commit_sha: String!
  ): Git_Commit

  """
  Returns an array of references from your Git database that match the supplied name. The `:ref` in the URL must be formatted as `heads/<branch name>` for branches and `tags/<tag name>` for tags. If the `:ref` doesn't exist in the repository, but existing refs start with `:ref`, they will be returned as an array.
  
  When you use this endpoint without providing a `:ref`, it will return an array of all the references from your Git database, including notes and stashes if they exist on the server. Anything in the namespace is returned, not just `heads` and `tags`.
  
  > [!NOTE]
  > You need to explicitly [request a pull request](https://docs.github.com/rest/pulls/pulls#get-a-pull-request) to trigger a test merge commit, which checks the mergeability of pull requests. For more information, see "[Checking mergeability of pull requests](https://docs.github.com/rest/guides/getting-started-with-the-git-database-api#checking-mergeability-of-pull-requests)".
  
  If you request matching references for a branch named `feature` but the branch `feature` doesn't exist, the response can still include other matching head refs that start with the word `feature`, such as `featureA` and `featureB`.
  """
  git_list_matching_refs(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!
  ): [Git_Reference]

  """
  Returns a single reference from your Git database. The `:ref` in the URL must be formatted as `heads/<branch name>` for branches and `tags/<tag name>` for tags. If the `:ref` doesn't match an existing ref, a `404` is returned.
  
  > [!NOTE]
  > You need to explicitly [request a pull request](https://docs.github.com/rest/pulls/pulls#get-a-pull-request) to trigger a test merge commit, which checks the mergeability of pull requests. For more information, see "[Checking mergeability of pull requests](https://docs.github.com/rest/guides/getting-started-with-the-git-database-api#checking-mergeability-of-pull-requests)".
  """
  git_get_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!
  ): Git_Reference

  """
  **Signature verification object**
  
  The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
  
  | Name | Type | Description |
  | ---- | ---- | ----------- |
  | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
  | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
  | `signature` | `string` | The signature that was extracted from the commit. |
  | `payload` | `string` | The value that was signed. |
  | `verified_at` | `string` | The date the signature was verified by GitHub. |
  
  These are the possible values for `reason` in the `verification` object:
  
  | Value | Description |
  | ----- | ----------- |
  | `expired_key` | The key that made the signature is expired. |
  | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
  | `gpgverify_error` | There was an error communicating with the signature verification service. |
  | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
  | `unsigned` | The object does not include a signature. |
  | `unknown_signature_type` | A non-PGP signature was found in the commit. |
  | `no_user` | No user was associated with the `committer` email address in the commit. |
  | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
  | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
  | `unknown_key` | The key that made the signature has not been registered with any user's account. |
  | `malformed_signature` | There was an error parsing the signature. |
  | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
  | `valid` | None of the above errors applied, so the signature is considered to be verified. |
  """
  git_get_tag(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    tag_sha: String!
  ): Git_Tag

  """
  Returns a single tree using the SHA1 value or ref name for that tree.
  
  If `truncated` is `true` in the response then the number of items in the `tree` array exceeded our maximum limit. If you need to fetch more items, use the non-recursive method of fetching trees, and fetch one sub-tree at a time.
  
  > [!NOTE]
  > The limit for the `tree` array is 100,000 entries with a maximum size of 7 MB when using the `recursive` parameter.
  """
  git_get_tree(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SHA1 value or ref (branch or tag) name of the tree."""
    tree_sha: String!

    """
    Setting this parameter to any value returns the objects or subtrees referenced by the tree specified in `:tree_sha`. For example, setting `recursive` to any of the following will enable returning objects or subtrees: `0`, `1`, `"true"`, and `"false"`. Omit this parameter to prevent recursively returning objects or subtrees.
    """
    recursive: String
  ): Git_Tree

  """
  Lists webhooks for a repository. `last response` may return null if there have not been any deliveries within 30 days.
  """
  repos_list_webhooks(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Webhook]

  """
  Returns a webhook configured in a repository. To get only the webhook `config` properties, see "[Get a webhook configuration for a repository](/rest/webhooks/repo-config#get-a-webhook-configuration-for-a-repository)."
  """
  repos_get_webhook(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Webhook

  """
  Returns the webhook configuration for a repository. To get more information about the webhook, including the `active` state and `events`, use "[Get a repository webhook](/rest/webhooks/repos#get-a-repository-webhook)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:repo_hook` or `repo` scope to use this endpoint.
  """
  repos_get_webhook_config_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Webhook_Configuration

  """
  Returns a list of webhook deliveries for a webhook configured in a repository.
  """
  repos_list_webhook_deliveries(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    Used for pagination: the starting delivery from which the page of deliveries is fetched. Refer to the `link` header for the next and previous page cursors.
    """
    cursor: String
  ): [Simple_webhook_delivery]

  """Returns a delivery for a webhook configured in a repository."""
  repos_get_webhook_delivery(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    delivery_id: Int!
  ): Webhook_delivery

  """
  Shows whether immutable releases are enabled or disabled. Also identifies whether immutability is being
  enforced by the repository owner.  The authenticated user must have admin read access to the repository.
  """
  repos_check_immutable_releases(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Check_immutable_releases

  """
  View the progress of an import.
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  
  **Import status**
  
  This section includes details about the possible values of the `status` field of the Import Progress response.
  
  An import that does not have errors will progress through these steps:
  
  *   `detecting` - the "detection" step of the import is in progress because the request did not include a `vcs` parameter. The import is identifying the type of source control present at the URL.
  *   `importing` - the "raw" step of the import is in progress. This is where commit data is fetched from the original repository. The import progress response will include `commit_count` (the total number of raw commits that will be imported) and `percent` (0 - 100, the current progress through the import).
  *   `mapping` - the "rewrite" step of the import is in progress. This is where SVN branches are converted to Git branches, and where author updates are applied. The import progress response does not include progress information.
  *   `pushing` - the "push" step of the import is in progress. This is where the importer updates the repository on GitHub. The import progress response will include `push_percent`, which is the percent value reported by `git push` when it is "Writing objects".
  *   `complete` - the import is complete, and the repository is ready on GitHub.
  
  If there are problems, you will see one of these in the `status` field:
  
  *   `auth_failed` - the import requires authentication in order to connect to the original repository. To update authentication for the import, please see the [Update an import](https://docs.github.com/rest/migrations/source-imports#update-an-import) section.
  *   `error` - the import encountered an error. The import progress response will include the `failed_step` and an error message. Contact [GitHub Support](https://support.github.com/contact?tags=dotcom-rest-api) for more information.
  *   `detection_needs_auth` - the importer requires authentication for the originating repository to continue detection. To update authentication for the import, please see the [Update an import](https://docs.github.com/rest/migrations/source-imports#update-an-import) section.
  *   `detection_found_nothing` - the importer didn't recognize any source control at the URL. To resolve, [Cancel the import](https://docs.github.com/rest/migrations/source-imports#cancel-an-import) and [retry](https://docs.github.com/rest/migrations/source-imports#start-an-import) with the correct URL.
  *   `detection_found_multiple` - the importer found several projects or repositories at the provided URL. When this is the case, the Import Progress response will also include a `project_choices` field with the possible project choices as values. To update project choice, please see the [Update an import](https://docs.github.com/rest/migrations/source-imports#update-an-import) section.
  
  **The project_choices field**
  
  When multiple projects are found at the provided URL, the response hash will include a `project_choices` field, the value of which is an array of hashes each representing a project choice. The exact key/value pairs of the project hashes will differ depending on the version control type.
  
  **Git LFS related fields**
  
  This section includes details about Git LFS related fields that may be present in the Import Progress response.
  
  *   `use_lfs` - describes whether the import has been opted in or out of using Git LFS. The value can be `opt_in`, `opt_out`, or `undecided` if no action has been taken.
  *   `has_large_files` - the boolean value describing whether files larger than 100MB were found during the `importing` step.
  *   `large_files_size` - the total size in gigabytes of files larger than 100MB found in the originating repository.
  *   `large_files_count` - the total number of files larger than 100MB found in the originating repository. To see a list of these files, make a "Get Large Files" request.
  """
  migrations_get_import_status(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Import @deprecated(reason: "deprecated")

  """
  Each type of source control system represents authors in a different way. For example, a Git commit author has a display name and an email address, but a Subversion commit author just has a username. The GitHub Importer will make the author information valid, but the author might not be correct. For example, it will change the bare Subversion username `hubot` into something like `hubot <hubot@12341234-abab-fefe-8787-fedcba987654>`.
  
  This endpoint and the [Map a commit author](https://docs.github.com/rest/migrations/source-imports#map-a-commit-author) endpoint allow you to provide correct Git author information.
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  """
  migrations_get_commit_authors(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """A user ID. Only return users with an ID greater than this ID."""
    since: Int
  ): [Porter_Author] @deprecated(reason: "deprecated")

  """
  List files larger than 100MB found during the import
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  """
  migrations_get_large_files(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [Porter_Large_File] @deprecated(reason: "deprecated")

  """
  Enables an authenticated GitHub App to find the repository's installation information. The installation's account type will be either an organization or a user account, depending which account the repository belongs to.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_get_repo_installation(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Installation

  """
  Shows which type of GitHub user can interact with this repository and when the restriction expires. If there are no restrictions, you will see an empty response.
  """
  interactions_get_restrictions_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): interactions_get_restrictions_for_repo_200_response

  """
  When authenticating as a user with admin rights to a repository, this endpoint will list all currently open repository invitations.
  """
  repos_list_invitations(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [repository_invitation]

  """
  List issues in a repository. Only open issues will be listed.
  
  > [!NOTE]
  > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    If an `integer` is passed, it should refer to a milestone by its `number` field. If the string `*` is passed, issues with any milestone are accepted. If the string `none` is passed, issues without milestones are returned.
    """
    milestone: String
    state: queryInput_issues_list_for_repo_state = open

    """
    Can be the name of a user. Pass in `none` for issues with no assigned user, and `*` for issues assigned to any user.
    """
    assignee: String

    """
    Can be the name of an issue type. If the string `*` is passed, issues with any type are accepted. If the string `none` is passed, issues without type are returned.
    """
    type: String

    """The user that created the issue."""
    creator: String

    """A user that's mentioned in the issue."""
    mentioned: String

    """A list of comma separated label names. Example: `bug,ui,@high`"""
    labels: String
    sort: queryInput_issues_list_for_repo_sort = created
    direction: direction_parameter = desc

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue]

  """
  You can use the REST API to list comments on issues and pull requests for a repository. Every pull request is an issue, but not every issue is a pull request.
  
  By default, issue comments are ordered by ascending ID.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_comments_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    sort: sort_parameter = created
    direction: queryInput_issues_list_comments_for_repo_direction

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue_comment]

  """
  You can use the REST API to get comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_get_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): issue_comment

  """
  List the reactions to an [issue comment](https://docs.github.com/rest/issues/comments#get-an-issue-comment).
  """
  reactions_list_for_issue_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    content: queryInput_reactions_list_for_issue_comment_content

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Reaction]

  """Lists events for a repository."""
  issues_list_events_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Issue_Event]

  """Gets a single event by the event id."""
  issues_get_event(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    event_id: Int!
  ): Issue_Event

  """
  The API returns a [`301 Moved Permanently` status](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api#follow-redirects) if the issue was
  [transferred](https://docs.github.com/articles/transferring-an-issue-to-another-repository/) to another repository. If
  the issue was transferred to or deleted from a repository where the authenticated user lacks read access, the API
  returns a `404 Not Found` status. If the issue was deleted from a repository where the authenticated user has read
  access, the API returns a `410 Gone` status. To receive webhook events for transferred and deleted issues, subscribe
  to the [`issues`](https://docs.github.com/webhooks/event-payloads/#issues) webhook.
  
  > [!NOTE]
  > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_get(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
  ): issue

  """
  Checks if a user has permission to be assigned to a specific issue.
  
  If the `assignee` can be assigned to this issue, a `204` status code with no content is returned.
  
  Otherwise a `404` status code is returned.
  """
  issues_check_user_can_be_assigned_to_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    assignee: String!
  ): Void

  """
  You can use the REST API to list comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
  
  Issue comments are ordered by ascending ID.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_comments(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue_comment]

  """
  You can use the REST API to list the dependencies an issue is blocked by.
  
  This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
  
  - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_dependencies_blocked_by(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue]

  """
  You can use the REST API to list the dependencies an issue is blocking.
  
  This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
  
  - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_dependencies_blocking(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue]

  """Lists all events for an issue."""
  issues_list_events(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Issue_Event_for_Issue]

  """Lists all issue field values for an issue."""
  issues_list_issue_field_values_for_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Issue_Field_Value]

  """Lists all labels for an issue."""
  issues_list_labels_on_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [label]

  """
  You can use the REST API to get the parent issue of a sub-issue.
  
  This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_get_parent(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
  ): issue

  """
  List the reactions to an [issue](https://docs.github.com/rest/issues/issues#get-an-issue).
  """
  reactions_list_for_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    content: queryInput_reactions_list_for_issue_content

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Reaction]

  """
  You can use the REST API to list the sub-issues on an issue.
  
  This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
  
  - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_sub_issues(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue]

  """List all timeline events for an issue."""
  issues_list_events_for_timeline(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Timeline_Event]

  """List deploy keys"""
  repos_list_deploy_keys(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Deploy_Key]

  """Get a deploy key"""
  repos_get_deploy_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the key."""
    key_id: Int!
  ): Deploy_Key

  """Lists all labels for a repository."""
  issues_list_labels_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [label]

  """Gets a label using the given name."""
  issues_get_label(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    name: String!
  ): label

  """
  Lists languages for the specified repository. The value shown for each language is the number of bytes of code written in that language.
  """
  repos_list_languages(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): JSON

  """
  This method returns the contents of the repository's license file, if one is detected.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw contents of the license.
  - **`application/vnd.github.html+json`**: Returns the license contents in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup).
  """
  licenses_get_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`,
    `refs/heads/<branch name>` or simply `<branch name>`.
    """
    ref: String
  ): License_Content

  """Lists milestones for a repository."""
  issues_list_milestones(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    state: queryInput_issues_list_milestones_state = open
    sort: queryInput_issues_list_milestones_sort = due_on
    direction: queryInput_issues_list_milestones_direction = asc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [milestone]

  """Gets a milestone using the given milestone number."""
  issues_get_milestone(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the milestone."""
    milestone_number: Int!
  ): milestone

  """Lists labels for issues in a milestone."""
  issues_list_labels_for_milestone(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the milestone."""
    milestone_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [label]

  """
  Lists all notifications for the current user in the specified repository.
  """
  activity_list_repo_notifications_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """If `true`, show notifications marked as read."""
    all: Boolean

    """
    If `true`, only shows notifications in which the user is directly participating or mentioned.
    """
    participating: Boolean

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    Only show notifications updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    before: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Thread]

  """
  Gets information about a GitHub Pages site.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_get_pages(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): page

  """
  Lists builts of a GitHub Pages site.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_list_pages_builds(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Page_Build]

  """
  Gets information about the single most recent build of a GitHub Pages site.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_get_latest_pages_build(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Page_Build

  """
  Gets information about a GitHub Pages build.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_get_pages_build(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    build_id: Int!
  ): Page_Build

  """
  Gets the current status of a GitHub Pages deployment.
  
  The authenticated user must have read permission for the GitHub Pages site.
  """
  repos_get_pages_deployment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    pages_deployment_id: pages_deployment_id_parameter_Input!
  ): GitHub_Pages_deployment_status

  """
  Gets a health check of the DNS settings for the `CNAME` record configured for a repository's GitHub Pages.
  
  The first request to this endpoint returns a `202 Accepted` status and starts an asynchronous background task to get the results for the domain. After the background task completes, subsequent requests to this endpoint return a `200 OK` status with the health check results in the response.
  
  The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_get_pages_health_check(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): repos_get_pages_health_check_response

  """
  Returns a boolean indicating whether or not private vulnerability reporting is enabled for the repository. For more information, see "[Evaluating the security settings of a repository](https://docs.github.com/code-security/security-advisories/working-with-repository-security-advisories/evaluating-the-security-settings-of-a-repository)".
  """
  repos_check_private_vulnerability_reporting(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): repos_check_private_vulnerability_reporting_200_response

  """
  Gets all custom property values that are set for a repository.
  Users with read access to the repository can use this endpoint.
  """
  repos_custom_properties_for_repos_get_repository_values(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [Custom_Property_Value]

  """
  Lists pull requests in a specified repository.
  
  Draft pull requests are available in public repositories with GitHub
  Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing
  plans, and in public and private repositories with GitHub Team and GitHub Enterprise
  Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products)
  in the GitHub Help documentation.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_list(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    state: queryInput_pulls_list_state = open

    """
    Filter pulls by head user or head organization and branch name in the format of `user:ref-name` or `organization:ref-name`. For example: `github:new-script-format` or `octocat:test-branch`.
    """
    head: String

    """Filter pulls by base branch name. Example: `gh-pages`."""
    base: String
    sort: queryInput_pulls_list_sort = created
    direction: queryInput_pulls_list_direction

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Pull_Request_Simple]

  """
  Lists review comments for all pull requests in a repository. By default,
  review comments are in ascending order by ID.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_list_review_comments_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    sort: queryInput_pulls_list_review_comments_for_repo_sort
    direction: queryInput_pulls_list_review_comments_for_repo_direction

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [pull_request_review_comment]

  """
  Provides details for a specified review comment.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_get_review_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): pull_request_review_comment

  """
  List the reactions to a [pull request review comment](https://docs.github.com/rest/pulls/comments#get-a-review-comment-for-a-pull-request).
  """
  reactions_list_for_pull_request_review_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    content: queryInput_reactions_list_for_pull_request_review_comment_content

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Reaction]

  """
  Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Lists details of a pull request by providing its number.
  
  When you get, [create](https://docs.github.com/rest/pulls/pulls/#create-a-pull-request), or [edit](https://docs.github.com/rest/pulls/pulls#update-a-pull-request) a pull request, GitHub creates a merge commit to test whether the pull request can be automatically merged into the base branch. This test commit is not added to the base branch or the head branch. You can review the status of the test commit using the `mergeable` key. For more information, see "[Checking mergeability of pull requests](https://docs.github.com/rest/guides/getting-started-with-the-git-database-api#checking-mergeability-of-pull-requests)".
  
  The value of the `mergeable` attribute can be `true`, `false`, or `null`. If the value is `null`, then GitHub has started a background job to compute the mergeability. After giving the job time to complete, resubmit the request. When the job finishes, you will see a non-`null` value for the `mergeable` attribute in the response. If `mergeable` is `true`, then `merge_commit_sha` will be the SHA of the _test_ merge commit.
  
  The value of the `merge_commit_sha` attribute changes depending on the state of the pull request. Before merging a pull request, the `merge_commit_sha` attribute holds the SHA of the _test_ merge commit. After merging a pull request, the `merge_commit_sha` attribute changes depending on how you merged the pull request:
  
  *   If merged as a [merge commit](https://docs.github.com/articles/about-merge-methods-on-github/), `merge_commit_sha` represents the SHA of the merge commit.
  *   If merged via a [squash](https://docs.github.com/articles/about-merge-methods-on-github/#squashing-your-merge-commits), `merge_commit_sha` represents the SHA of the squashed commit on the base branch.
  *   If [rebased](https://docs.github.com/articles/about-merge-methods-on-github/#rebasing-and-merging-your-commits), `merge_commit_sha` represents the commit that the base branch was updated to.
  
  Pass the appropriate [media type](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types) to fetch diff and patch formats.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  - **`application/vnd.github.diff`**: For more information, see "[git-diff](https://git-scm.com/docs/git-diff)" in the Git documentation. If a diff is corrupt, contact us through the [GitHub Support portal](https://support.github.com/). Include the repository name and pull request ID in your message.
  """
  pulls_get(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
  ): pull_request2

  """
  Lists all review comments for a specified pull request. By default, review comments
  are in ascending order by ID.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_list_review_comments(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    sort: sort_parameter = created
    direction: queryInput_pulls_list_review_comments_direction

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [pull_request_review_comment]

  """
  Lists a maximum of 250 commits for a pull request. To receive a complete
  commit list for pull requests with more than 250 commits, use the [List commits](https://docs.github.com/rest/commits/commits#list-commits)
  endpoint.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_list_commits(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Commit]

  """
  Lists the files in a specified pull request.
  
  > [!NOTE]
  > Responses include a maximum of 3000 files. The paginated response returns 30 files per page by default.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_list_files(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Diff_Entry]

  """
  Checks if a pull request has been merged into the base branch. The HTTP status of the response indicates whether or not the pull request has been merged; the response body is empty.
  """
  pulls_check_if_merged(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
  ): Void

  """
  Gets the users or teams whose review is requested for a pull request. Once a requested reviewer submits a review, they are no longer considered a requested reviewer. Their review will instead be returned by the [List reviews for a pull request](https://docs.github.com/rest/pulls/reviews#list-reviews-for-a-pull-request) operation.
  """
  pulls_list_requested_reviewers(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
  ): Pull_Request_Review_Request

  """
  Lists all reviews for a specified pull request. The list of reviews returns in chronological order.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_list_reviews(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Pull_Request_Review]

  """
  Retrieves a pull request review by its ID.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_get_review(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """The unique identifier of the review."""
    review_id: Int!
  ): Pull_Request_Review

  """
  Lists comments for a specific pull request review.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_list_comments_for_review(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """The unique identifier of the review."""
    review_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Legacy_Review_Comment]

  """
  Gets the preferred README for a repository.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw file contents. This is the default if you do not specify a media type.
  - **`application/vnd.github.html+json`**: Returns the README in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup).
  """
  repos_get_readme(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the commit/branch/tag. Default: the repository’s default branch.
    """
    ref: String
  ): Content_File

  """
  Gets the README from a repository directory.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw file contents. This is the default if you do not specify a media type.
  - **`application/vnd.github.html+json`**: Returns the README in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup).
  """
  repos_get_readme_in_directory(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The alternate path to look for a README file"""
    dir: String!

    """
    The name of the commit/branch/tag. Default: the repository’s default branch.
    """
    ref: String
  ): Content_File

  """
  This returns a list of releases, which does not include regular Git tags that have not been associated with a release. To get a list of Git tags, use the [Repository Tags API](https://docs.github.com/rest/repos/repos#list-repository-tags).
  
  Information about published releases are available to everyone. Only users with push access will receive listings for draft releases.
  """
  repos_list_releases(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [release]

  """
  To download the asset's binary content:
  
  - If within a browser, fetch the location specified in the `browser_download_url` key provided in the response.
  - Alternatively, set the `Accept` header of the request to 
    [`application/octet-stream`](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types). 
    The API will either redirect the client to the location, or stream it directly if possible.
    API clients should handle both a `200` or `302` response.
  """
  repos_get_release_asset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the asset."""
    asset_id: Int!
  ): Release_Asset

  """
  View the latest published full release for the repository.
  
  The latest release is the most recent non-prerelease, non-draft release, sorted by the `created_at` attribute. The `created_at` attribute is the date of the commit used for the release, and not the date when the release was drafted or published.
  """
  repos_get_latest_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): release

  """Get a published release with the specified tag."""
  repos_get_release_by_tag(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """tag parameter"""
    tag: String!
  ): release

  """
  Gets a public release with the specified release ID.
  
  > [!NOTE]
  > This returns an `upload_url` key corresponding to the endpoint for uploading release assets. This key is a hypermedia resource. For more information, see "[Getting started with the REST API](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#hypermedia)."
  """
  repos_get_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!
  ): release

  """List release assets"""
  repos_list_release_assets(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Release_Asset]

  """
  List the reactions to a [release](https://docs.github.com/rest/releases/releases#get-a-release).
  """
  reactions_list_for_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!
    content: queryInput_reactions_list_for_release_content

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Reaction]

  """
  Returns all active rules that apply to the specified branch. The branch does not need to exist; rules that would apply
  to a branch with that name will be returned. All active rules that apply will be returned, regardless of the level
  at which they are configured (e.g. repository or organization). Rules in rulesets with "evaluate" or "disabled"
  enforcement statuses are not returned.
  """
  repos_get_branch_rules(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [repository_rule_detailed]

  """Get all the rulesets for a repository."""
  repos_get_repo_rulesets(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Include rulesets configured at higher levels that apply to this repository
    """
    includes_parents: Boolean = true

    """
    A comma-separated list of rule targets to filter by.
    If provided, only rulesets that apply to the specified targets will be returned.
    For example, `branch,tag,push`.
    """
    targets: String
  ): [Repository_ruleset]

  """
  Lists suites of rule evaluations at the repository level.
  For more information, see "[Managing rulesets for a repository](https://docs.github.com/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/managing-rulesets-for-a-repository#viewing-insights-for-rulesets)."
  """
  repos_get_repo_rule_suites(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the ref. Cannot contain wildcard characters. Optionally prefix with `refs/heads/` to limit to branches or `refs/tags/` to limit to tags. Omit the prefix to search across all refs. When specified, only rule evaluations triggered for this ref will be returned.
    """
    ref: String
    time_period: time_period_parameter = day

    """
    The handle for the GitHub user account to filter on. When specified, only rule evaluations triggered by this actor will be returned.
    """
    actor_name: String
    rule_suite_result: rule_suite_result_parameter = all

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [query_repos_get_org_rule_suites_items]

  """
  Gets information about a suite of rule evaluations from within a repository.
  For more information, see "[Managing rulesets for a repository](https://docs.github.com/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/managing-rulesets-for-a-repository#viewing-insights-for-rulesets)."
  """
  repos_get_repo_rule_suite(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the rule suite result.
    To get this ID, you can use [GET /repos/{owner}/{repo}/rulesets/rule-suites](https://docs.github.com/rest/repos/rule-suites#list-repository-rule-suites)
    for repositories and [GET /orgs/{org}/rulesets/rule-suites](https://docs.github.com/rest/orgs/rule-suites#list-organization-rule-suites)
    for organizations.
    """
    rule_suite_id: Int!
  ): Rule_Suite

  """
  Get a ruleset for a repository.
  
  **Note:** To prevent leaking sensitive information, the `bypass_actors` property is only returned if the user
  making the API request has write access to the ruleset.
  """
  repos_get_repo_ruleset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The ID of the ruleset."""
    ruleset_id: Int!

    """
    Include rulesets configured at higher levels that apply to this repository
    """
    includes_parents: Boolean = true
  ): Repository_ruleset

  """Get the history of a repository ruleset."""
  repos_get_repo_ruleset_history(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The ID of the ruleset."""
    ruleset_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Ruleset_version]

  """Get a version of a repository ruleset."""
  repos_get_repo_ruleset_version(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The ID of the ruleset."""
    ruleset_id: Int!

    """The ID of the version"""
    version_id: Int!
  ): ruleset_version_with_state

  """
  Lists secret scanning alerts for an eligible repository, from newest to oldest.
  
  The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  secret_scanning_list_alerts_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    state: secret_scanning_alert_state_parameter

    """
    A comma-separated list of secret types to return. All default secret patterns are returned. To return generic patterns, pass the token name(s) in the parameter. See "[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)" for a complete list of secret types.
    """
    secret_type: String

    """
    A comma-separated list of resolutions. Only secret scanning alerts with one of these resolutions are listed. Valid resolutions are `false_positive`, `wont_fix`, `revoked`, `pattern_edited`, `pattern_deleted` or `used_in_tests`.
    """
    resolution: String

    """
    Filters alerts by assignee. Use `*` to get all assigned alerts, `none` to get all unassigned alerts, or a GitHub username to get alerts assigned to a specific user.
    """
    assignee: String
    sort: secret_scanning_alert_sort_parameter = created
    direction: direction_parameter = desc

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events before this cursor. To receive an initial cursor on your first request, include an empty "before" query string.
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for events after this cursor.  To receive an initial cursor on your first request, include an empty "after" query string.
    """
    after: String

    """
    A comma-separated list of validities that, when present, will return alerts that match the validities in this list. Valid options are `active`, `inactive`, and `unknown`.
    """
    validity: String

    """
    A boolean value representing whether or not to filter alerts by the publicly-leaked tag being present.
    """
    is_publicly_leaked: Boolean

    """
    A boolean value representing whether or not to filter alerts by the multi-repo tag being present.
    """
    is_multi_repo: Boolean

    """
    A boolean value representing whether or not to hide literal secrets in the results.
    """
    hide_secret: Boolean
  ): [secret_scanning_alert]

  """
  Gets a single secret scanning alert detected in an eligible repository.
  
  The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  secret_scanning_get_alert(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int

    """
    A boolean value representing whether or not to hide literal secrets in the results.
    """
    hide_secret: Boolean
  ): secret_scanning_alert

  """
  Lists all locations for a given secret scanning alert for an eligible repository.
  
  The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  secret_scanning_list_locations_for_alert(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [secret_scanning_location]

  """
  Lists the latest default incremental and backfill scans by type for a repository. Scans from Copilot Secret Scanning are not included.
  
  > [!NOTE]
  > This endpoint requires [GitHub Advanced Security](https://docs.github.com/get-started/learning-about-github/about-github-advanced-security)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  secret_scanning_get_scan_history(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): secret_scanning_scan_history

  """
  Lists security advisories in a repository.
  
  The authenticated user can access unpublished security advisories from a repository if they are a security manager or administrator of that repository, or if they are a collaborator on any security advisory.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:read` scope to to get a published security advisory in a private repository, or any unpublished security advisory that the authenticated user has access to.
  """
  security_advisories_list_repository_advisories(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    direction: direction_parameter = desc
    sort: queryInput_security_advisories_list_repository_advisories_sort = created

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of advisories to return per page. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: PositiveInt = 30
    state: queryInput_security_advisories_list_repository_advisories_state
  ): [repository_advisory]

  """
  Get a repository security advisory using its GitHub Security Advisory (GHSA) identifier.
  
  Anyone can access any published security advisory on a public repository.
  
  The authenticated user can access an unpublished security advisory from a repository if they are a security manager or administrator of that repository, or if they are a
  collaborator on the security advisory.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:read` scope to to get a published security advisory in a private repository, or any unpublished security advisory that the authenticated user has access to.
  """
  security_advisories_get_repository_advisory(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The GHSA (GitHub Security Advisory) identifier of the advisory."""
    ghsa_id: String!
  ): repository_advisory

  """
  Lists the people that have starred the repository.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.star+json`**: Includes a timestamp of when the star was created.
  """
  activity_list_stargazers_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): activity_list_stargazers_for_repo_200_response

  """
  Returns a weekly aggregate of the number of additions and deletions pushed to a repository.
  
  > [!NOTE]
  > This endpoint can only be used for repositories with fewer than 10,000 commits. If the repository contains 10,000 or more commits, a 422 status code will be returned.
  """
  repos_get_code_frequency_stats(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [repos_get_code_frequency_stats_response]

  """
  Returns the last year of commit activity grouped by week. The `days` array is a group of commits per day, starting on `Sunday`.
  """
  repos_get_commit_activity_stats(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [repos_get_commit_activity_stats_response]

  "\nReturns the `total` number of commits authored by the contributor. In addition, the response includes a Weekly Hash (`weeks` array) with the following information:\n\n*   `w` - Start of the week, given as a [Unix timestamp](https://en.wikipedia.org/wiki/Unix_time).\n*   `a` - Number of additions\n*   `d` - Number of deletions\n*   `c` - Number of commits\n\n> [!NOTE]\n> This endpoint will return `0` values for all addition and deletion counts in repositories with 10,000 or more commits."
  repos_get_contributors_stats(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [repos_get_contributors_stats_response]

  """
  Returns the total commit counts for the `owner` and total commit counts in `all`. `all` is everyone combined, including the `owner` in the last 52 weeks. If you'd like to get the commit counts for non-owners, you can subtract `owner` from `all`.
  
  The array order is oldest week (index 0) to most recent week.
  
  The most recent week is seven days ago at UTC midnight to today at UTC midnight.
  """
  repos_get_participation_stats(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Participation_Stats

  """
  Each array contains the day number, hour number, and number of commits:
  
  *   `0-6`: Sunday - Saturday
  *   `0-23`: Hour of day
  *   Number of commits
  
  For example, `[2, 14, 25]` indicates that there were 25 total commits, during the 2:00pm hour on Tuesdays. All times are based on the time zone of individual commits.
  """
  repos_get_punch_card_stats(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [repos_get_punch_card_stats_response]

  """Lists the people watching the specified repository."""
  activity_list_watchers_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """
  Gets information about whether the authenticated user is subscribed to the repository.
  """
  activity_get_repo_subscription(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): repository_subscription

  """List repository tags"""
  repos_list_tags(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Tag]

  """
  Gets a redirect URL to download a tar archive for a repository. If you omit `:ref`, the repository’s default branch (usually
  `main`) will be used. Please make sure your HTTP framework is configured to follow redirects or you will need to use
  the `Location` header to make a second `GET` request.
  
  > [!NOTE]
  > For private repositories, these links are temporary and expire after five minutes.
  """
  repos_download_tarball_archive(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    ref: String!
  ): JSON

  """
  Lists the teams that have access to the specified repository and that are also visible to the authenticated user.
  
  For a public repository, a team is listed only if that team added the public repository explicitly.
  
  OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to use this endpoint with a public repository, and `repo` scope to use this endpoint with a private repository.
  """
  repos_list_teams(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [team]

  """Get all repository topics"""
  repos_get_all_topics(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): Topic

  """
  Get the total number of clones and breakdown per day or week for the last 14 days. Timestamps are aligned to UTC midnight of the beginning of the day or week. Week begins on Monday.
  """
  repos_get_clones(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    per: per_parameter = day
  ): Clone_Traffic

  """Get the top 10 popular contents over the last 14 days."""
  repos_get_top_paths(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [Content_Traffic]

  """Get the top 10 referrers over the last 14 days."""
  repos_get_top_referrers(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): [Referrer_Traffic]

  """
  Get the total number of views and breakdown per day or week for the last 14 days. Timestamps are aligned to UTC midnight of the beginning of the day or week. Week begins on Monday.
  """
  repos_get_views(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    per: per_parameter = day
  ): View_Traffic

  """
  Shows whether dependency alerts are enabled or disabled for a repository. The authenticated user must have admin read access to the repository. For more information, see "[About security alerts for vulnerable dependencies](https://docs.github.com/articles/about-security-alerts-for-vulnerable-dependencies)".
  """
  repos_check_vulnerability_alerts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Gets a redirect URL to download a zip archive for a repository. If you omit `:ref`, the repository’s default branch (usually
  `main`) will be used. Please make sure your HTTP framework is configured to follow redirects or you will need to use
  the `Location` header to make a second `GET` request.
  
  > [!NOTE]
  > For private repositories, these links are temporary and expire after five minutes. If the repository is empty, you will receive a 404 when you follow the redirect.
  """
  repos_download_zipball_archive(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    ref: String!
  ): JSON

  """
  Lists all public repositories in the order that they were created.
  
  Note:
  - For GitHub Enterprise Server, this endpoint will only list repositories available to all users on the enterprise.
  - Pagination is powered exclusively by the `since` parameter. Use the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers) to get the URL for the next page of repositories.
  """
  repos_list_public(
    """
    A repository ID. Only return repositories with an ID greater than this ID.
    """
    since: Int
  ): [minimal_repository]

  """
  Searches for query terms inside of a file. This method returns up to 100 results [per page](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api).
  
  When searching for code, you can get text match metadata for the file **content** and file **path** fields when you pass the `text-match` media type. For more details about how to receive highlighted search results, see [Text match metadata](https://docs.github.com/rest/search/search#text-match-metadata).
  
  For example, if you want to find the definition of the `addClass` function inside [jQuery](https://github.com/jquery/jquery) repository, your query would look something like this:
  
  `q=addClass+in:file+language:js+repo:jquery/jquery`
  
  This query searches for the keyword `addClass` within a file's contents. The query limits the search to files where the language is JavaScript in the `jquery/jquery` repository.
  
  Considerations for code search:
  
  Due to the complexity of searching code, there are a few restrictions on how searches are performed:
  
  *   Only the _default branch_ is considered. In most cases, this will be the `master` branch.
  *   Only files smaller than 384 KB are searchable.
  *   You must always include at least one search term when searching source code. For example, searching for [`language:go`](https://github.com/search?utf8=%E2%9C%93&q=language%3Ago&type=Code) is not valid, while [`amazing
  language:go`](https://github.com/search?utf8=%E2%9C%93&q=amazing+language%3Ago&type=Code) is.
  
  This endpoint requires you to authenticate and limits you to 10 requests per minute.
  """
  search_code(
    """
    The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See "[Searching code](https://docs.github.com/search-github/searching-on-github/searching-code)" for a detailed list of qualifiers.
    """
    q: String!
    sort: indexed_const
    order: queryInput_search_code_order = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): search_code_200_response

  """
  Find commits via various criteria on the default branch (usually `main`). This method returns up to 100 results [per page](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api).
  
  When searching for commits, you can get text match metadata for the **message** field when you provide the `text-match` media type. For more details about how to receive highlighted search results, see [Text match
  metadata](https://docs.github.com/rest/search/search#text-match-metadata).
  
  For example, if you want to find commits related to CSS in the [octocat/Spoon-Knife](https://github.com/octocat/Spoon-Knife) repository. Your query would look something like this:
  
  `q=repo:octocat/Spoon-Knife+css`
  """
  search_commits(
    """
    The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See "[Searching commits](https://docs.github.com/search-github/searching-on-github/searching-commits)" for a detailed list of qualifiers.
    """
    q: String!
    sort: queryInput_search_commits_sort
    order: order_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): search_commits_200_response

  """
  Find issues by state and keyword. This method returns up to 100 results [per page](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api).
  
  When searching for issues, you can get text match metadata for the issue **title**, issue **body**, and issue **comment body** fields when you pass the `text-match` media type. For more details about how to receive highlighted
  search results, see [Text match metadata](https://docs.github.com/rest/search/search#text-match-metadata).
  
  For example, if you want to find the oldest unresolved Python bugs on Windows. Your query might look something like this.
  
  `q=windows+label:bug+language:python+state:open&sort=created&order=asc`
  
  This query searches for the keyword `windows`, within any open issue that is labeled as `bug`. The search runs across repositories whose primary language is Python. The results are sorted by creation date in ascending order, which means the oldest issues appear first in the search results.
  
  > [!NOTE]
  > For requests made by GitHub Apps with a user access token, you can't retrieve a combination of issues and pull requests in a single query. Requests that don't include the `is:issue` or `is:pull-request` qualifier will receive an HTTP `422 Unprocessable Entity` response. To get results for both issues and pull requests, you must send separate queries for issues and pull requests. For more information about the `is` qualifier, see "[Searching only issues or pull requests](https://docs.github.com/github/searching-for-information-on-github/searching-issues-and-pull-requests#search-only-issues-or-pull-requests)."
  """
  search_issues_and_pull_requests(
    """
    The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See "[Searching issues and pull requests](https://docs.github.com/search-github/searching-on-github/searching-issues-and-pull-requests)" for a detailed list of qualifiers.
    """
    q: String!
    sort: queryInput_search_issues_and_pull_requests_sort
    order: order_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Set to `true` to use advanced search.
    Example: `http://api.github.com/search/issues?q={query}&advanced_search=true`
    """
    advanced_search: String
  ): search_issues_and_pull_requests_200_response

  """
  Find labels in a repository with names or descriptions that match search keywords. Returns up to 100 results [per page](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api).
  
  When searching for labels, you can get text match metadata for the label **name** and **description** fields when you pass the `text-match` media type. For more details about how to receive highlighted search results, see [Text match metadata](https://docs.github.com/rest/search/search#text-match-metadata).
  
  For example, if you want to find labels in the `linguist` repository that match `bug`, `defect`, or `enhancement`. Your query might look like this:
  
  `q=bug+defect+enhancement&repository_id=64778136`
  
  The labels that best match the query appear first in the search results.
  """
  search_labels(
    """The id of the repository."""
    repository_id: Int!

    """
    The search keywords. This endpoint does not accept qualifiers in the query. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query).
    """
    q: String!
    sort: queryInput_search_labels_sort
    order: order_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): search_labels_200_response

  """
  Find repositories via various criteria. This method returns up to 100 results [per page](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api).
  
  When searching for repositories, you can get text match metadata for the **name** and **description** fields when you pass the `text-match` media type. For more details about how to receive highlighted search results, see [Text match metadata](https://docs.github.com/rest/search/search#text-match-metadata).
  
  For example, if you want to search for popular Tetris repositories written in assembly code, your query might look like this:
  
  `q=tetris+language:assembly&sort=stars&order=desc`
  
  This query searches for repositories with the word `tetris` in the name, the description, or the README. The results are limited to repositories where the primary language is assembly. The results are sorted by stars in descending order, so that the most popular repositories appear first in the search results.
  """
  search_repos(
    """
    The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See "[Searching for repositories](https://docs.github.com/articles/searching-for-repositories/)" for a detailed list of qualifiers.
    """
    q: String!
    sort: queryInput_search_repos_sort
    order: order_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): search_repos_200_response

  """
  Find topics via various criteria. Results are sorted by best match. This method returns up to 100 results [per page](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api). See "[Searching topics](https://docs.github.com/articles/searching-topics/)" for a detailed list of qualifiers.
  
  When searching for topics, you can get text match metadata for the topic's **short\_description**, **description**, **name**, or **display\_name** field when you pass the `text-match` media type. For more details about how to receive highlighted search results, see [Text match metadata](https://docs.github.com/rest/search/search#text-match-metadata).
  
  For example, if you want to search for topics related to Ruby that are featured on https://github.com/topics. Your query might look like this:
  
  `q=ruby+is:featured`
  
  This query searches for topics with the keyword `ruby` and limits the results to find only topics that are featured. The topics that are the best match for the query appear first in the search results.
  """
  search_topics(
    """
    The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query).
    """
    q: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): search_topics_200_response

  """
  Find users via various criteria. This method returns up to 100 results [per page](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api).
  
  When searching for users, you can get text match metadata for the issue **login**, public **email**, and **name** fields when you pass the `text-match` media type. For more details about highlighting search results, see [Text match metadata](https://docs.github.com/rest/search/search#text-match-metadata). For more details about how to receive highlighted search results, see [Text match metadata](https://docs.github.com/rest/search/search#text-match-metadata).
  
  For example, if you're looking for a list of popular users, you might try this query:
  
  `q=tom+repos:%3E42+followers:%3E1000`
  
  This query searches for users with the name `tom`. The results are restricted to users with more than 42 repositories and over 1,000 followers.
  
  This endpoint does not accept authentication and will only include publicly visible users. As an alternative, you can use the GraphQL API. The GraphQL API requires authentication and will return private users, including Enterprise Managed Users (EMUs), that you are authorized to view. For more information, see "[GraphQL Queries](https://docs.github.com/graphql/reference/queries#search)."
  """
  search_users(
    """
    The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as the web interface for GitHub. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/rest/search/search#constructing-a-search-query). See "[Searching users](https://docs.github.com/search-github/searching-on-github/searching-users)" for a detailed list of qualifiers.
    """
    q: String!
    sort: queryInput_search_users_sort
    order: order_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): search_users_200_response

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the [Get a team by name](https://docs.github.com/rest/teams/teams#get-a-team-by-name) endpoint.
  """
  teams_get_legacy(
    """The unique identifier of the team."""
    team_id: Int!
  ): Full_Team @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [`List pending team invitations`](https://docs.github.com/rest/teams/members#list-pending-team-invitations) endpoint.
  
  The return hash contains a `role` field which refers to the Organization Invitation role and will be one of the following values: `direct_member`, `admin`, `billing_manager`, `hiring_manager`, or `reinstate`. If the invitee is not a GitHub member, the `login` field in the return hash will be `null`.
  """
  teams_list_pending_invitations_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Organization_Invitation] @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [`List team members`](https://docs.github.com/rest/teams/members#list-team-members) endpoint.
  
  Team members will include the members of child teams.
  """
  teams_list_members_legacy(
    """The unique identifier of the team."""
    team_id: Int!
    role: queryInput_teams_list_members_legacy_role = all

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user] @deprecated(reason: "deprecated")

  """
  The "Get team member" endpoint (described below) is closing down.
  
  We recommend using the [Get team membership for a user](https://docs.github.com/rest/teams/members#get-team-membership-for-a-user) endpoint instead. It allows you to get both active and pending memberships.
  
  To list members in a team, the team must be visible to the authenticated user.
  """
  teams_get_member_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The handle for the GitHub user account."""
    username: String!
  ): Void @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Get team membership for a user](https://docs.github.com/rest/teams/members#get-team-membership-for-a-user) endpoint.
  
  Team members will include the members of child teams.
  
  To get a user's membership with a team, the team must be visible to the authenticated user.
  
  **Note:**
  The response contains the `state` of the membership and the member's `role`.
  
  The `role` for organization owners is set to `maintainer`. For more information about `maintainer` roles, see [Create a team](https://docs.github.com/rest/teams/teams#create-a-team).
  """
  teams_get_membership_for_user_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The handle for the GitHub user account."""
    username: String!
  ): Team_Membership @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [List team repositories](https://docs.github.com/rest/teams/teams#list-team-repositories) endpoint.
  """
  teams_list_repos_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository] @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Check team permissions for a repository](https://docs.github.com/rest/teams/teams#check-team-permissions-for-a-repository) endpoint.
  
  > [!NOTE]
  > Repositories inherited through a parent team will also be checked.
  
  You can also get information about the specified repository, including what permissions the team grants on it, by passing the following custom [media type](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types/) via the `Accept` header:
  """
  teams_check_permissions_for_repo_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): teams_check_permissions_for_repo_legacy_response

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [`List child teams`](https://docs.github.com/rest/teams/teams#list-child-teams) endpoint.
  """
  teams_list_child_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [team] @deprecated(reason: "deprecated")

  """
  OAuth app tokens and personal access tokens (classic) need the `user` scope in order for the response to include private profile information.
  """
  users_get_authenticated: users_get_authenticated_200_response

  """List the users you've blocked on your personal account."""
  users_list_blocked_by_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """
  Returns a 204 if the given user is blocked by the authenticated user. Returns a 404 if the given user is not blocked by the authenticated user, or if the given user account has been identified as spam by GitHub.
  """
  users_check_blocked(
    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Lists the authenticated user's codespaces.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_list_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """ID of the Repository to filter on"""
    repository_id: Int
  ): codespaces_list_for_authenticated_user_200_response

  """
  Lists all development environment secrets available for a user's codespaces without revealing their
  encrypted values.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_list_secrets_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): codespaces_list_secrets_for_authenticated_user_200_response

  """
  Gets your public key, which you need to encrypt secrets. You need to encrypt a secret before you can create or update secrets.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_get_public_key_for_authenticated_user: CodespacesUserPublicKey

  """
  Gets a development environment secret available to a user's codespaces without revealing its encrypted value.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_get_secret_for_authenticated_user(
    """The name of the secret."""
    secret_name: String!
  ): codespaces_secret

  """
  List the repositories that have been granted the ability to use a user's development environment secret.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_list_repositories_for_secret_for_authenticated_user(
    """The name of the secret."""
    secret_name: String!
  ): codespaces_list_repositories_for_secret_for_authenticated_user_200_response

  """
  Gets information about a user's codespace.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_get_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
  ): codespace

  """
  Gets information about an export of a codespace.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_get_export_details_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!

    """
    The ID of the export operation, or `latest`. Currently only `latest` is currently supported.
    """
    export_id: String!
  ): Fetches_information_about_an_export_of_a_codespace_

  """
  List the machine types a codespace can transition to use.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_codespace_machines_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
  ): codespaces_codespace_machines_for_authenticated_user_200_response

  """
  Lists all packages that are owned by the authenticated user within the user's namespace, and that encountered a conflict during a Docker migration.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
  """
  packages_list_docker_migration_conflicting_packages_for_authenticated_user: [Package]

  """
  Lists all of your email addresses, and specifies which one is visible
  to the public.
  
  OAuth app tokens and personal access tokens (classic) need the `user:email` scope to use this endpoint.
  """
  users_list_emails_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Email]

  """Lists the people following the authenticated user."""
  users_list_followers_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """Lists the people who the authenticated user follows."""
  users_list_followed_by_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """Check if a person is followed by the authenticated user"""
  users_check_person_is_followed_by_authenticated(
    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Lists the current user's GPG keys.
  
  OAuth app tokens and personal access tokens (classic) need the `read:gpg_key` scope to use this endpoint.
  """
  users_list_gpg_keys_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [GPG_Key]

  """
  View extended details for a single GPG key.
  
  OAuth app tokens and personal access tokens (classic) need the `read:gpg_key` scope to use this endpoint.
  """
  users_get_gpg_key_for_authenticated_user(
    """The unique identifier of the GPG key."""
    gpg_key_id: Int!
  ): GPG_Key

  """
  Lists installations of your GitHub App that the authenticated user has explicit permission (`:read`, `:write`, or `:admin`) to access.
  
  The authenticated user has explicit permission to access repositories they own, repositories where they are a collaborator, and repositories that they can access through an organization membership.
  
  You can find the permissions for the installation under the `permissions` key.
  """
  apps_list_installations_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): apps_list_installations_for_authenticated_user_200_response

  """
  List repositories that the authenticated user has explicit permission (`:read`, `:write`, or `:admin`) to access for an installation.
  
  The authenticated user has explicit permission to access repositories they own, repositories where they are a collaborator, and repositories that they can access through an organization membership.
  
  The access the user has to each repository is included in the hash under the `permissions` key.
  """
  apps_list_installation_repos_for_authenticated_user(
    """The unique identifier of the installation."""
    installation_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): apps_list_installation_repos_for_authenticated_user_200_response

  """
  Shows which type of GitHub user can interact with your public repositories and when the restriction expires.
  """
  interactions_get_restrictions_for_authenticated_user: interactions_get_restrictions_for_authenticated_user_response

  """
  List issues across owned and member repositories assigned to the authenticated user.
  
  > [!NOTE]
  > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_list_for_authenticated_user(
    filter: queryInput_issues_list_for_authenticated_user_filter = assigned
    state: queryInput_issues_list_for_authenticated_user_state = open

    """A list of comma separated label names. Example: `bug,ui,@high`"""
    labels: String
    sort: queryInput_issues_list_for_authenticated_user_sort = created
    direction: direction_parameter = desc

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [issue]

  """
  Lists the public SSH keys for the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `read:public_key` scope to use this endpoint.
  """
  users_list_public_ssh_keys_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Key]

  """
  View extended details for a single public SSH key.
  
  OAuth app tokens and personal access tokens (classic) need the `read:public_key` scope to use this endpoint.
  """
  users_get_public_ssh_key_for_authenticated_user(
    """The unique identifier of the key."""
    key_id: Int!
  ): Key

  """Lists the active subscriptions for the authenticated user."""
  apps_list_subscriptions_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [User_Marketplace_Purchase]

  """Lists the active subscriptions for the authenticated user."""
  apps_list_subscriptions_for_authenticated_user_stubbed(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [User_Marketplace_Purchase]

  """Lists all of the authenticated user's organization memberships."""
  orgs_list_memberships_for_authenticated_user(
    state: queryInput_orgs_list_memberships_for_authenticated_user_state

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Org_Membership]

  """
  If the authenticated user is an active or pending member of the organization, this endpoint will return the user's membership. If the authenticated user is not affiliated with the organization, a `404` is returned. This endpoint will return a `403` if the request is made by a GitHub App that is blocked by the organization.
  """
  orgs_get_membership_for_authenticated_user(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Org_Membership

  """Lists all migrations a user has started."""
  migrations_list_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Migration]

  """
  Fetches a single user migration. The response includes the `state` of the migration, which can be one of the following values:
  
  *   `pending` - the migration hasn't started yet.
  *   `exporting` - the migration is in progress.
  *   `exported` - the migration finished successfully.
  *   `failed` - the migration failed.
  
  Once the migration has been `exported` you can [download the migration archive](https://docs.github.com/rest/migrations/users#download-a-user-migration-archive).
  """
  migrations_get_status_for_authenticated_user(
    """The unique identifier of the migration."""
    migration_id: Int!
    exclude: [String]
  ): Migration

  """
  Fetches the URL to download the migration archive as a `tar.gz` file. Depending on the resources your repository uses, the migration archive can contain JSON files with data for these objects:
  
  *   attachments
  *   bases
  *   commit\_comments
  *   issue\_comments
  *   issue\_events
  *   issues
  *   milestones
  *   organizations
  *   projects
  *   protected\_branches
  *   pull\_request\_reviews
  *   pull\_requests
  *   releases
  *   repositories
  *   review\_comments
  *   schema
  *   users
  
  The archive will also contain an `attachments` directory that includes all attachment files uploaded to GitHub.com and a `repositories` directory that contains the repository's Git data.
  """
  migrations_get_archive_for_authenticated_user(
    """The unique identifier of the migration."""
    migration_id: Int!
  ): JSON

  """Lists all the repositories for this user migration."""
  migrations_list_repos_for_authenticated_user(
    """The unique identifier of the migration."""
    migration_id: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """
  List organizations for the authenticated user.
  
  For OAuth app tokens and personal access tokens (classic), this endpoint only lists organizations that your authorization allows you to operate on in some way (e.g., you can list teams with `read:org` scope, you can publicize your organization membership with `user` scope, etc.). Therefore, this API requires at least `user` or `read:org` scope for OAuth app tokens and personal access tokens (classic). Requests with insufficient scope will receive a `403 Forbidden` response.
  
  > [!NOTE]
  > Requests using a fine-grained access token will receive a `200 Success` response with an empty list.
  """
  orgs_list_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [organization_simple]

  """
  Lists packages owned by the authenticated user within the user's namespace.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_list_packages_for_authenticated_user(
    package_type: queryInput_packages_list_packages_for_authenticated_user_package_type!
    visibility: package_visibility_parameter

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Package]

  """
  Gets a specific package for a package owned by the authenticated user.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_package_for_authenticated_user(
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!
  ): Package

  """
  Lists package versions for a package owned by the authenticated user.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_all_package_versions_for_package_owned_by_authenticated_user(
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
    state: queryInput_packages_get_all_package_versions_for_package_owned_by_authenticated_user_state = active
  ): [Package_Version]

  """
  Gets a specific package version for a package owned by the authenticated user.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_package_version_for_authenticated_user(
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Package_Version

  """
  Lists your publicly visible email address, which you can set with the
  [Set primary email visibility for the authenticated user](https://docs.github.com/rest/users/emails#set-primary-email-visibility-for-the-authenticated-user)
  endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `user:email` scope to use this endpoint.
  """
  users_list_public_emails_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Email]

  """
  Lists repositories that the authenticated user has explicit permission (`:read`, `:write`, or `:admin`) to access.
  
  The authenticated user has explicit permission to access repositories they own, repositories where they are a collaborator, and repositories that they can access through an organization membership.
  """
  repos_list_for_authenticated_user(
    visibility: queryInput_repos_list_for_authenticated_user_visibility = all

    """
    Comma-separated list of values. Can include:  
     * `owner`: Repositories that are owned by the authenticated user.  
     * `collaborator`: Repositories that the user has been added to as a collaborator.  
     * `organization_member`: Repositories that the user has access to through being a member of an organization. This includes every repository on every team that the user is on.
    """
    affiliation: String = "owner,collaborator,organization_member"
    type: queryInput_repos_list_for_authenticated_user_type = all
    sort: queryInput_repos_list_for_authenticated_user_sort = full_name
    direction: queryInput_repos_list_for_authenticated_user_direction

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    Only show repositories updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    Only show repositories updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    before: DateTime
  ): [repository]

  """
  When authenticating as a user, this endpoint will list all currently open repository invitations for that user.
  """
  repos_list_invitations_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [repository_invitation]

  """Lists all of your social accounts."""
  users_list_social_accounts_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Social_account]

  """
  Lists the SSH signing keys for the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `read:ssh_signing_key` scope to use this endpoint.
  """
  users_list_ssh_signing_keys_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [SSH_Signing_Key]

  """
  Gets extended details for an SSH signing key.
  
  OAuth app tokens and personal access tokens (classic) need the `read:ssh_signing_key` scope to use this endpoint.
  """
  users_get_ssh_signing_key_for_authenticated_user(
    """The unique identifier of the SSH signing key."""
    ssh_signing_key_id: Int!
  ): SSH_Signing_Key

  """
  Lists repositories the authenticated user has starred.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.star+json`**: Includes a timestamp of when the star was created.
  """
  activity_list_repos_starred_by_authenticated_user(
    sort: sort_starred_parameter = created
    direction: direction_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [activity_list_repos_starred_by_authenticated_user_200_response]

  """Whether the authenticated user has starred the repository."""
  activity_check_repo_is_starred_by_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """Lists repositories the authenticated user is watching."""
  activity_list_watched_repos_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """
  List all of the teams across all of the organizations to which the authenticated
  user belongs.
  
  OAuth app tokens and personal access tokens (classic) need the `user`, `repo`, or `read:org` scope to use this endpoint.
  
  When using a fine-grained personal access token, the resource owner of the token must be a single organization, and the response will only include the teams from that organization.
  """
  teams_list_for_authenticated_user(
    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Full_Team]

  """
  Provides publicly available information about someone with a GitHub account. This method takes their durable user `ID` instead of their `login`, which can change over time.
  
  If you are requesting information about an [Enterprise Managed User](https://docs.github.com/enterprise-cloud@latest/admin/managing-iam/understanding-iam-for-enterprises/about-enterprise-managed-users), or a GitHub App bot that is installed in an organization that uses Enterprise Managed Users, your requests must be authenticated as a user or GitHub App that has access to the organization to view that account's information. If you are not authorized, the request will return a `404 Not Found` status.
  
  The `email` key in the following response is the publicly visible email address from your GitHub [profile page](https://github.com/settings/profile). When setting up your profile, you can select a primary email address to be public which provides an email entry for this endpoint. If you do not set a public email address for `email`, then it will have a value of `null`. You only see publicly visible email addresses when authenticated with GitHub. For more information, see [Authentication](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#authentication).
  
  The Emails API enables you to list all of your email addresses, and toggle a primary email to be visible publicly. For more information, see [Emails API](https://docs.github.com/rest/users/emails).
  """
  users_get_by_id(
    """account_id parameter"""
    account_id: Int!
  ): users_get_by_id_200_response

  """
  Lists all users, in the order that they signed up on GitHub. This list includes personal user accounts and organization accounts.
  
  Note: Pagination is powered exclusively by the `since` parameter. Use the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers) to get the URL for the next page of users.
  """
  users_list(
    """A user ID. Only return users with an ID greater than this ID."""
    since: Int

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [simple_user]

  """
  Provides publicly available information about someone with a GitHub account.
  
  If you are requesting information about an [Enterprise Managed User](https://docs.github.com/enterprise-cloud@latest/admin/managing-iam/understanding-iam-for-enterprises/about-enterprise-managed-users), or a GitHub App bot that is installed in an organization that uses Enterprise Managed Users, your requests must be authenticated as a user or GitHub App that has access to the organization to view that account's information. If you are not authorized, the request will return a `404 Not Found` status.
  
  The `email` key in the following response is the publicly visible email address from your GitHub [profile page](https://github.com/settings/profile). When setting up your profile, you can select a primary email address to be public which provides an email entry for this endpoint. If you do not set a public email address for `email`, then it will have a value of `null`. You only see publicly visible email addresses when authenticated with GitHub. For more information, see [Authentication](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#authentication).
  
  The Emails API enables you to list all of your email addresses, and toggle a primary email to be visible publicly. For more information, see [Emails API](https://docs.github.com/rest/users/emails).
  """
  users_get_by_username(
    """The handle for the GitHub user account."""
    username: String!
  ): users_get_by_username_200_response

  """
  List a collection of artifact attestations with a given subject digest that are associated with repositories owned by a user.
  
  The collection of attestations returned by this endpoint is filtered according to the authenticated user's permissions; if the authenticated user cannot read a repository, the attestations associated with that repository will not be included in the response. In addition, when using a fine-grained access token the `attestations:read` permission is required.
  
  **Please note:** in order to offer meaningful security benefits, an attestation's signature and timestamps **must** be cryptographically verified, and the identity of the attestation signer **must** be validated. Attestations can be verified using the [GitHub CLI `attestation verify` command](https://cli.github.com/manual/gh_attestation_verify). For more information, see [our guide on how to use artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
  """
  users_list_attestations(
    """The handle for the GitHub user account."""
    username: String!

    """Subject Digest"""
    subject_digest: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    Optional filter for fetching attestations with a given predicate type.
    This option accepts `provenance`, `sbom`, `release`, or freeform text
    for custom predicate types.
    """
    predicate_type: String
  ): users_list_attestations_response

  """
  Lists all packages that are in a specific user's namespace, that the requesting user has access to, and that encountered a conflict during Docker migration.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
  """
  packages_list_docker_migration_conflicting_packages_for_user(
    """The handle for the GitHub user account."""
    username: String!
  ): [Package]

  """
  If you are authenticated as the given user, you will see your private events. Otherwise, you'll only see public events. _Optional_: use the fine-grained token with following permission set to view private events: "Events" user permissions (read).
  
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_events_for_authenticated_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """
  This is the user's organization dashboard. You must be authenticated as the user to view this.
  
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_org_events_for_authenticated_user(
    """The handle for the GitHub user account."""
    username: String!

    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_public_events_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """Lists the people following the specified user."""
  users_list_followers_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """Lists the people who the specified user follows."""
  users_list_following_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [simple_user]

  """Check if a user follows another user"""
  users_check_following_for_user(
    """The handle for the GitHub user account."""
    username: String!
    target_user: String!
  ): Void

  """Lists public gists for the specified user:"""
  gists_list_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
    """
    since: DateTime

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Base_Gist]

  """
  Lists the GPG keys for a user. This information is accessible by anyone.
  """
  users_list_gpg_keys_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [GPG_Key]

  """
  Provides hovercard information. You can find out more about someone in relation to their pull requests, issues, repositories, and organizations.
  
    The `subject_type` and `subject_id` parameters provide context for the person's hovercard, which returns more information than without the parameters. For example, if you wanted to find out more about `octocat` who owns the `Spoon-Knife` repository, you would use a `subject_type` value of `repository` and a `subject_id` value of `1300192` (the ID of the `Spoon-Knife` repository).
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  users_get_context_for_user(
    """The handle for the GitHub user account."""
    username: String!
    subject_type: queryInput_users_get_context_for_user_subject_type

    """
    Uses the ID for the `subject_type` you specified. **Required** when using `subject_type`.
    """
    subject_id: String
  ): Hovercard

  """
  Enables an authenticated GitHub App to find the user’s installation information.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_get_user_installation(
    """The handle for the GitHub user account."""
    username: String!
  ): Installation

  """
  Lists the _verified_ public SSH keys for a user. This is accessible by anyone.
  """
  users_list_public_keys_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Key_Simple]

  """
  List [public organization memberships](https://docs.github.com/articles/publicizing-or-concealing-organization-membership) for the specified user.
  
  This method only lists _public_ memberships, regardless of authentication. If you need to fetch all of the organization memberships (public and private) for the authenticated user, use the [List organizations for the authenticated user](https://docs.github.com/rest/orgs/orgs#list-organizations-for-the-authenticated-user) API instead.
  """
  orgs_list_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [organization_simple]

  """
  Lists all packages in a user's namespace for which the requesting user has access.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_list_packages_for_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: queryInput_packages_list_packages_for_user_package_type!
    visibility: package_visibility_parameter

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Package]

  """
  Gets a specific package metadata for a public package owned by a user.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_package_for_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!
  ): Package

  """
  Lists package versions for a public package owned by a specified user.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_all_package_versions_for_package_owned_by_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!
  ): [Package_Version]

  """
  Gets a specific package version for a public package owned by a specified user.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_get_package_version_for_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Package_Version

  """
  List all projects owned by a specific user accessible by the authenticated user.
  """
  projects_list_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """Limit results to projects of the specified type."""
    q: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [Projects_v2_Project]

  """Get a specific user-owned project."""
  projects_get_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!
  ): Projects_v2_Project

  """List all fields for a specific user-owned project."""
  projects_list_fields_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
  ): [Projects_v2_Field]

  """Get a specific field for a user-owned project."""
  projects_get_field_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the field."""
    field_id: Int!
  ): Projects_v2_Field

  """
  List all items for a specific user-owned project accessible by the authenticated user.
  """
  projects_list_items_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    Search query to filter items, see [Filtering projects](https://docs.github.com/issues/planning-and-tracking-with-projects/customizing-views-in-your-project/filtering-projects) for more information.
    """
    q: String
    fields: [String]
  ): [projects_v2_item_with_content]

  """Get a specific item from a user-owned project."""
  projects_get_user_item(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the project item."""
    item_id: Int!
    fields: [String]
  ): projects_v2_item_with_content

  """List items in a user project with the saved view's filter applied."""
  projects_list_view_items_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!

    """The number that identifies the project view."""
    view_number: Int!
    fields: [String]

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30
  ): [projects_v2_item_with_content]

  """
  These are events that you've received by watching repositories and following users. If you are authenticated as the
  given user, you will see private events. Otherwise, you'll only see public events.
  
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_received_events_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """
  > [!NOTE]
  > This API is not built to serve real-time use cases. Depending on the time of day, event latency can be anywhere from 30s to 6h.
  """
  activity_list_received_public_events_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Event]

  """Lists public repositories for the specified user."""
  repos_list_for_user(
    """The handle for the GitHub user account."""
    username: String!
    type: queryInput_repos_list_for_user_type = owner
    sort: queryInput_repos_list_for_user_sort = full_name
    direction: queryInput_repos_list_for_user_direction

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """
  Gets a report of premium request usage for a user.
  
  **Note:** Only data from the past 24 months is accessible via this endpoint.
  """
  billing_get_github_billing_premium_request_usage_report_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.
    """
    year: Int

    """
    If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used.
    """
    month: Int

    """
    If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.
    """
    day: Int

    """The model name to query usage for. The name is not case sensitive."""
    model: String

    """The product name to query usage for. The name is not case sensitive."""
    product: String
  ): billing_premium_request_usage_report_user

  """
  Gets a report of the total usage for a user.
  
  **Note:** This endpoint is only available to users with access to the enhanced billing platform.
  """
  billing_get_github_billing_usage_report_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.
    """
    year: Int

    """
    If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. If no year is specified the default `year` is used.
    """
    month: Int

    """
    If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.
    """
    day: Int
  ): billing_usage_report_user

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Gets a summary report of usage for a user.
  
  **Note:** Only data from the past 24 months is accessible via this endpoint.
  """
  billing_get_github_billing_usage_summary_report_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.
    """
    year: Int

    """
    If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. Default value is the current month. If no year is specified the default `year` is used.
    """
    month: Int

    """
    If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.
    """
    day: Int

    """The repository name to query for usage in the format owner/repository."""
    repository: String

    """The product name to query usage for. The name is not case sensitive."""
    product: String

    """The SKU to query for usage."""
    sku: String
  ): billing_usage_summary_report_user

  """
  Lists social media accounts for a user. This endpoint is accessible by anyone.
  """
  users_list_social_accounts_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [Social_account]

  """
  Lists the SSH signing keys for a user. This operation is accessible by anyone.
  """
  users_list_ssh_signing_keys_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [SSH_Signing_Key]

  """
  Lists repositories a user has starred.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.star+json`**: Includes a timestamp of when the star was created.
  """
  activity_list_repos_starred_by_user(
    """The handle for the GitHub user account."""
    username: String!
    sort: sort_starred_parameter = created
    direction: direction_parameter = desc

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): activity_list_repos_starred_by_user_200_response

  """Lists repositories a user is watching."""
  activity_list_repos_watched_by_user(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    page: Int = 1
  ): [minimal_repository]

  """Get all supported GitHub API versions."""
  meta_get_all_versions: [Date]

  """Get a random sentence from the Zen of GitHub"""
  meta_get_zen: String
}

type root {
  current_user_url: UriTemplate!
  current_user_authorizations_html_url: UriTemplate!
  authorizations_url: UriTemplate!
  code_search_url: UriTemplate!
  commit_search_url: UriTemplate!
  emails_url: UriTemplate!
  emojis_url: UriTemplate!
  events_url: UriTemplate!
  feeds_url: UriTemplate!
  followers_url: UriTemplate!
  following_url: UriTemplate!
  gists_url: UriTemplate!
  hub_url: UriTemplate @deprecated(reason: "deprecated")
  issue_search_url: UriTemplate!
  issues_url: UriTemplate!
  keys_url: UriTemplate!
  label_search_url: UriTemplate!
  notifications_url: UriTemplate!
  organization_url: UriTemplate!
  organization_repositories_url: UriTemplate!
  organization_teams_url: UriTemplate!
  public_gists_url: UriTemplate!
  rate_limit_url: UriTemplate!
  repository_url: UriTemplate!
  repository_search_url: UriTemplate!
  current_user_repositories_url: UriTemplate!
  starred_url: UriTemplate!
  starred_gists_url: UriTemplate!
  topic_search_url: UriTemplate
  user_url: UriTemplate!
  user_organizations_url: UriTemplate!
  user_repositories_url: UriTemplate!
  user_search_url: UriTemplate!
}

"""Represents uri-template values"""
scalar UriTemplate

"""A GitHub Security Advisory."""
type global_advisory {
  """The GitHub Security Advisory ID."""
  ghsa_id: String!

  """The Common Vulnerabilities and Exposures (CVE) ID."""
  cve_id: String

  """The API URL for the advisory."""
  url: String!

  """The URL for the advisory."""
  html_url: URL!

  """The API URL for the repository advisory."""
  repository_advisory_url: URL

  """A short summary of the advisory."""
  summary: query_security_advisories_list_global_advisories_items_summary!

  """A detailed description of what the advisory entails."""
  description: query_security_advisories_list_global_advisories_items_description
  type: query_security_advisories_list_global_advisories_items_type!
  severity: query_security_advisories_list_global_advisories_items_severity!

  """The URL of the advisory's source code."""
  source_code_location: URL
  identifiers: [query_security_advisories_list_global_advisories_items_identifiers_items]
  references: [String]

  """
  The date and time of when the advisory was published, in ISO 8601 format.
  """
  published_at: DateTime!

  """
  The date and time of when the advisory was last updated, in ISO 8601 format.
  """
  updated_at: DateTime!

  """
  The date and time of when the advisory was reviewed by GitHub, in ISO 8601 format.
  """
  github_reviewed_at: DateTime

  """
  The date and time when the advisory was published in the National Vulnerability Database, in ISO 8601 format.
  This field is only populated when the advisory is imported from the National Vulnerability Database.
  """
  nvd_published_at: DateTime

  """
  The date and time of when the advisory was withdrawn, in ISO 8601 format.
  """
  withdrawn_at: DateTime

  """The products and respective version ranges affected by the advisory."""
  vulnerabilities: [vulnerability]
  cvss: query_security_advisories_list_global_advisories_items_cvss
  cvss_severities: cvss_severities
  epss: security_advisory_epss
  cwes: [query_security_advisories_list_global_advisories_items_cwes_items]

  """The users who contributed to the advisory."""
  credits: [query_security_advisories_list_global_advisories_items_credits_items]
}

"""
A field whose value conforms to the standard URL format as specified in RFC3986: https://www.ietf.org/rfc/rfc3986.txt.
"""
scalar URL

"""A short summary of the advisory."""
scalar query_security_advisories_list_global_advisories_items_summary

"""A detailed description of what the advisory entails."""
scalar query_security_advisories_list_global_advisories_items_description

"""The type of advisory."""
enum query_security_advisories_list_global_advisories_items_type {
  reviewed
  unreviewed
  malware
}

"""The severity of the advisory."""
enum query_security_advisories_list_global_advisories_items_severity {
  critical
  high
  medium
  low
  unknown
}

type query_security_advisories_list_global_advisories_items_identifiers_items {
  type: query_security_advisories_list_global_advisories_items_identifiers_items_type!

  """The identifier value."""
  value: String!
}

"""The type of identifier."""
enum query_security_advisories_list_global_advisories_items_identifiers_items_type {
  CVE
  GHSA
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

"""
A vulnerability describing the product and its affected versions within a GitHub Security Advisory.
"""
type vulnerability {
  package: query_security_advisories_list_global_advisories_items_vulnerabilities_items_package

  """The range of the package versions affected by the vulnerability."""
  vulnerable_version_range: String

  """The package version that resolves the vulnerability."""
  first_patched_version: String

  """The functions in the package that are affected by the vulnerability."""
  vulnerable_functions: [String]
}

"""The name of the package affected by the vulnerability."""
type query_security_advisories_list_global_advisories_items_vulnerabilities_items_package {
  ecosystem: security_advisory_ecosystems!

  """The unique package name within its ecosystem."""
  name: String
}

"""The package's language or package management ecosystem."""
enum security_advisory_ecosystems {
  rubygems
  npm
  pip
  maven
  nuget
  composer
  go
  rust
  erlang
  actions
  pub
  other
  swift
}

type query_security_advisories_list_global_advisories_items_cvss {
  """The CVSS vector."""
  vector_string: String

  """The CVSS score."""
  score: NonNegativeFloat
}

"""Floats that will have a value of 0 or more."""
scalar NonNegativeFloat

type cvss_severities {
  cvss_v3: query_security_advisories_list_global_advisories_items_cvss_severities_cvss_v3
  cvss_v4: query_security_advisories_list_global_advisories_items_cvss_severities_cvss_v4
}

type query_security_advisories_list_global_advisories_items_cvss_severities_cvss_v3 {
  """The CVSS 3 vector string."""
  vector_string: String

  """The CVSS 3 score."""
  score: NonNegativeFloat
}

type query_security_advisories_list_global_advisories_items_cvss_severities_cvss_v4 {
  """The CVSS 4 vector string."""
  vector_string: String

  """The CVSS 4 score."""
  score: NonNegativeFloat
}

"""
The EPSS scores as calculated by the [Exploit Prediction Scoring System](https://www.first.org/epss).
"""
type security_advisory_epss {
  percentage: NonNegativeFloat
  percentile: NonNegativeFloat
}

type query_security_advisories_list_global_advisories_items_cwes_items {
  """The Common Weakness Enumeration (CWE) identifier."""
  cwe_id: String!

  """The name of the CWE."""
  name: String!
}

type query_security_advisories_list_global_advisories_items_credits_items {
  user: simple_user!
  type: security_advisory_credit_types!
}

"""A GitHub user."""
type simple_user {
  name: String
  email: String
  login: String!
  id: BigInt!
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  starred_at: String
  user_view_type: String
  additionalProperties: JSON
}

"""
The `BigInt` scalar type represents non-fractional signed whole numeric values.
"""
scalar BigInt

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON @specifiedBy(url: "http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf")

"""The type of credit the user is receiving."""
enum security_advisory_credit_types {
  analyst
  finder
  reporter
  coordinator
  remediation_developer
  remediation_reviewer
  remediation_verifier
  tool
  sponsor
  other
}

"""
If specified, only advisories of this type will be returned. By default, a request with no other parameters defined will only return reviewed advisories that are not malware.
"""
enum queryInput_security_advisories_list_global_advisories_type {
  reviewed
  malware
  unreviewed
}

"""If specified, only advisories with these severities will be returned."""
enum queryInput_security_advisories_list_global_advisories_severity {
  unknown
  low
  medium
  high
  critical
}

"""The direction to sort the results by."""
enum direction_parameter {
  asc
  desc
}

"""Integers that will have a value greater than 0."""
scalar PositiveInt

"""The property to sort the results by."""
enum queryInput_security_advisories_list_global_advisories_sort {
  updated
  published
  epss_percentage
  epss_percentile
}

"""
GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user accounts and granted access to specific repositories. They come with granular permissions and built-in webhooks. GitHub apps are first class actors within GitHub.
"""
type integration {
  """Unique identifier of the GitHub app"""
  id: Int!

  """The slug name of the GitHub app"""
  slug: String
  node_id: String!
  client_id: String
  owner: query_apps_get_authenticated_owner!

  """The name of the GitHub app"""
  name: String!
  description: String
  external_url: URL!
  html_url: URL!
  created_at: DateTime!
  updated_at: DateTime!
  permissions: query_apps_get_authenticated_permissions!

  """
  The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation.
  """
  events: [String]!

  """
  The number of installations associated with the GitHub app. Only returned when the integration is requesting details about itself.
  """
  installations_count: Int
}

union query_apps_get_authenticated_owner = simple_user | enterprise

"""An enterprise on GitHub."""
type enterprise {
  """A short description of the enterprise."""
  description: String
  html_url: URL!

  """The enterprise's website URL."""
  website_url: URL

  """Unique identifier of the enterprise"""
  id: Int!
  node_id: String!

  """The name of the enterprise."""
  name: String!

  """The slug url identifier for the enterprise."""
  slug: String!
  created_at: DateTime
  updated_at: DateTime
  avatar_url: URL!
}

"""The set of permissions for the GitHub app"""
type query_apps_get_authenticated_permissions {
  issues: String
  checks: String
  metadata: String
  contents: String
  deployments: String
  additionalProperties: JSON
}

"""Configuration object of the webhook"""
type Webhook_Configuration {
  """The URL to which the payloads will be delivered."""
  url: URL

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl
}

union webhook_config_insecure_ssl = String_container | Float_container

type String_container {
  String: String
}

type Float_container {
  Float: Float
}

"""Delivery made by a webhook, without request and response information."""
type Simple_webhook_delivery {
  """Unique identifier of the webhook delivery."""
  id: BigInt!

  """
  Unique identifier for the event (shared with all deliveries for all webhooks that subscribe to this event).
  """
  guid: String!

  """Time when the webhook delivery occurred."""
  delivered_at: DateTime!

  """Whether the webhook delivery is a redelivery."""
  redelivery: Boolean!

  """Time spent delivering."""
  duration: Float!

  """Describes the response returned after attempting the delivery."""
  status: String!

  """Status code received when delivery was made."""
  status_code: Int!

  """The event that triggered the delivery."""
  event: String!

  """The type of activity for the event that triggered the delivery."""
  action: String

  """The id of the GitHub App installation associated with this event."""
  installation_id: BigInt

  """The id of the repository associated with this event."""
  repository_id: BigInt

  """Time when the webhook delivery was throttled."""
  throttled_at: DateTime
}

"""Delivery made by a webhook."""
type Webhook_delivery {
  """Unique identifier of the delivery."""
  id: Int!

  """
  Unique identifier for the event (shared with all deliveries for all webhooks that subscribe to this event).
  """
  guid: String!

  """Time when the delivery was delivered."""
  delivered_at: DateTime!

  """Whether the delivery is a redelivery."""
  redelivery: Boolean!

  """Time spent delivering."""
  duration: Float!

  """Description of the status of the attempted delivery"""
  status: String!

  """Status code received when delivery was made."""
  status_code: Int!

  """The event that triggered the delivery."""
  event: String!

  """The type of activity for the event that triggered the delivery."""
  action: String

  """The id of the GitHub App installation associated with this event."""
  installation_id: Int

  """The id of the repository associated with this event."""
  repository_id: Int

  """Time when the webhook delivery was throttled."""
  throttled_at: DateTime

  """The URL target of the delivery."""
  url: URL
  request: query_apps_get_webhook_delivery_request!
  response: query_apps_get_webhook_delivery_response!
}

type query_apps_get_webhook_delivery_request {
  """The request headers sent with the webhook delivery."""
  headers: JSON

  """The webhook payload."""
  payload: JSON
}

type query_apps_get_webhook_delivery_response {
  """The response headers received when the delivery was made."""
  headers: JSON

  """The response payload received."""
  payload: String
}

"""Request to install an integration on a target"""
type Integration_Installation_Request {
  """Unique identifier of the request installation."""
  id: Int!
  node_id: String
  account: query_apps_list_installation_requests_for_authenticated_app_items_account!
  requester: simple_user!
  created_at: DateTime!
}

type query_apps_list_installation_requests_for_authenticated_app_items_account {
  """The name of the enterprise."""
  name: String!
  email: String
  login: String!

  """Unique identifier of the enterprise"""
  id: JSON
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  starred_at: String
  user_view_type: String
  additionalProperties: JSON

  """A short description of the enterprise."""
  description: String

  """The enterprise's website URL."""
  website_url: URL

  """The slug url identifier for the enterprise."""
  slug: String!
  created_at: DateTime
  updated_at: DateTime
}

"""Installation"""
type Installation {
  """The ID of the installation."""
  id: Int!
  account: query_apps_list_installations_items_account
  repository_selection: query_apps_list_installations_items_repository_selection!
  access_tokens_url: URL!
  repositories_url: URL!
  html_url: URL!
  app_id: Int!
  client_id: String

  """The ID of the user or organization this token is being scoped to."""
  target_id: Int!
  target_type: String!
  permissions: App_Permissions!
  events: [String]!
  created_at: DateTime!
  updated_at: DateTime!
  single_file_name: String
  has_multiple_single_files: Boolean
  single_file_paths: [String]
  app_slug: String!
  suspended_by: nullable_simple_user
  suspended_at: DateTime
  contact_email: String
}

type query_apps_list_installations_items_account {
  """The name of the enterprise."""
  name: String!
  email: String
  login: String!

  """Unique identifier of the enterprise"""
  id: JSON
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  starred_at: String
  user_view_type: String
  additionalProperties: JSON

  """A short description of the enterprise."""
  description: String

  """The enterprise's website URL."""
  website_url: URL

  """The slug url identifier for the enterprise."""
  slug: String!
  created_at: DateTime
  updated_at: DateTime
}

"""
Describe whether all repositories have been selected or there's a selection involved
"""
enum query_apps_list_installations_items_repository_selection {
  all
  selected
}

"""The permissions granted to the user access token."""
type App_Permissions {
  actions: query_apps_list_installations_items_permissions_actions
  administration: query_apps_list_installations_items_permissions_administration
  artifact_metadata: query_apps_list_installations_items_permissions_artifact_metadata
  attestations: query_apps_list_installations_items_permissions_attestations
  checks: query_apps_list_installations_items_permissions_checks
  codespaces: query_apps_list_installations_items_permissions_codespaces
  contents: query_apps_list_installations_items_permissions_contents
  dependabot_secrets: query_apps_list_installations_items_permissions_dependabot_secrets
  deployments: query_apps_list_installations_items_permissions_deployments
  discussions: query_apps_list_installations_items_permissions_discussions
  environments: query_apps_list_installations_items_permissions_environments
  issues: query_apps_list_installations_items_permissions_issues
  merge_queues: query_apps_list_installations_items_permissions_merge_queues
  metadata: query_apps_list_installations_items_permissions_metadata
  packages: query_apps_list_installations_items_permissions_packages
  pages: query_apps_list_installations_items_permissions_pages
  pull_requests: query_apps_list_installations_items_permissions_pull_requests
  repository_custom_properties: query_apps_list_installations_items_permissions_repository_custom_properties
  repository_hooks: query_apps_list_installations_items_permissions_repository_hooks
  repository_projects: query_apps_list_installations_items_permissions_repository_projects
  secret_scanning_alerts: query_apps_list_installations_items_permissions_secret_scanning_alerts
  secrets: query_apps_list_installations_items_permissions_secrets
  security_events: query_apps_list_installations_items_permissions_security_events
  single_file: query_apps_list_installations_items_permissions_single_file
  statuses: query_apps_list_installations_items_permissions_statuses
  vulnerability_alerts: query_apps_list_installations_items_permissions_vulnerability_alerts
  workflows: write_const
  custom_properties_for_organizations: query_apps_list_installations_items_permissions_custom_properties_for_organizations
  members: query_apps_list_installations_items_permissions_members
  organization_administration: query_apps_list_installations_items_permissions_organization_administration
  organization_custom_roles: query_apps_list_installations_items_permissions_organization_custom_roles
  organization_custom_org_roles: query_apps_list_installations_items_permissions_organization_custom_org_roles
  organization_custom_properties: query_apps_list_installations_items_permissions_organization_custom_properties
  organization_copilot_seat_management: write_const
  organization_copilot_agent_settings: query_apps_list_installations_items_permissions_organization_copilot_agent_settings
  organization_announcement_banners: query_apps_list_installations_items_permissions_organization_announcement_banners
  organization_events: read_const
  organization_hooks: query_apps_list_installations_items_permissions_organization_hooks
  organization_personal_access_tokens: query_apps_list_installations_items_permissions_organization_personal_access_tokens
  organization_personal_access_token_requests: query_apps_list_installations_items_permissions_organization_personal_access_token_requests
  organization_plan: read_const
  organization_projects: query_apps_list_installations_items_permissions_organization_projects
  organization_packages: query_apps_list_installations_items_permissions_organization_packages
  organization_secrets: query_apps_list_installations_items_permissions_organization_secrets
  organization_self_hosted_runners: query_apps_list_installations_items_permissions_organization_self_hosted_runners
  organization_user_blocking: query_apps_list_installations_items_permissions_organization_user_blocking
  email_addresses: query_apps_list_installations_items_permissions_email_addresses
  followers: query_apps_list_installations_items_permissions_followers
  git_ssh_keys: query_apps_list_installations_items_permissions_git_ssh_keys
  gpg_keys: query_apps_list_installations_items_permissions_gpg_keys
  interaction_limits: query_apps_list_installations_items_permissions_interaction_limits
  profile: write_const
  starring: query_apps_list_installations_items_permissions_starring
  enterprise_custom_properties_for_organizations: query_apps_list_installations_items_permissions_enterprise_custom_properties_for_organizations
}

"""
The level of permission to grant the access token for GitHub Actions workflows, workflow runs, and artifacts.
"""
enum query_apps_list_installations_items_permissions_actions {
  read
  write
}

"""
The level of permission to grant the access token for repository creation, deletion, settings, teams, and collaborators creation.
"""
enum query_apps_list_installations_items_permissions_administration {
  read
  write
}

"""
The level of permission to grant the access token to create and retrieve build artifact metadata records.
"""
enum query_apps_list_installations_items_permissions_artifact_metadata {
  read
  write
}

"""
The level of permission to create and retrieve the access token for repository attestations.
"""
enum query_apps_list_installations_items_permissions_attestations {
  read
  write
}

"""The level of permission to grant the access token for checks on code."""
enum query_apps_list_installations_items_permissions_checks {
  read
  write
}

"""
The level of permission to grant the access token to create, edit, delete, and list Codespaces.
"""
enum query_apps_list_installations_items_permissions_codespaces {
  read
  write
}

"""
The level of permission to grant the access token for repository contents, commits, branches, downloads, releases, and merges.
"""
enum query_apps_list_installations_items_permissions_contents {
  read
  write
}

"""
The level of permission to grant the access token to manage Dependabot secrets.
"""
enum query_apps_list_installations_items_permissions_dependabot_secrets {
  read
  write
}

"""
The level of permission to grant the access token for deployments and deployment statuses.
"""
enum query_apps_list_installations_items_permissions_deployments {
  read
  write
}

"""
The level of permission to grant the access token for discussions and related comments and labels.
"""
enum query_apps_list_installations_items_permissions_discussions {
  read
  write
}

"""
The level of permission to grant the access token for managing repository environments.
"""
enum query_apps_list_installations_items_permissions_environments {
  read
  write
}

"""
The level of permission to grant the access token for issues and related comments, assignees, labels, and milestones.
"""
enum query_apps_list_installations_items_permissions_issues {
  read
  write
}

"""
The level of permission to grant the access token to manage the merge queues for a repository.
"""
enum query_apps_list_installations_items_permissions_merge_queues {
  read
  write
}

"""
The level of permission to grant the access token to search repositories, list collaborators, and access repository metadata.
"""
enum query_apps_list_installations_items_permissions_metadata {
  read
  write
}

"""
The level of permission to grant the access token for packages published to GitHub Packages.
"""
enum query_apps_list_installations_items_permissions_packages {
  read
  write
}

"""
The level of permission to grant the access token to retrieve Pages statuses, configuration, and builds, as well as create new builds.
"""
enum query_apps_list_installations_items_permissions_pages {
  read
  write
}

"""
The level of permission to grant the access token for pull requests and related comments, assignees, labels, milestones, and merges.
"""
enum query_apps_list_installations_items_permissions_pull_requests {
  read
  write
}

"""
The level of permission to grant the access token to view and edit custom properties for a repository, when allowed by the property.
"""
enum query_apps_list_installations_items_permissions_repository_custom_properties {
  read
  write
}

"""
The level of permission to grant the access token to manage the post-receive hooks for a repository.
"""
enum query_apps_list_installations_items_permissions_repository_hooks {
  read
  write
}

"""
The level of permission to grant the access token to manage repository projects, columns, and cards.
"""
enum query_apps_list_installations_items_permissions_repository_projects {
  read
  write
  admin
}

"""
The level of permission to grant the access token to view and manage secret scanning alerts.
"""
enum query_apps_list_installations_items_permissions_secret_scanning_alerts {
  read
  write
}

"""
The level of permission to grant the access token to manage repository secrets.
"""
enum query_apps_list_installations_items_permissions_secrets {
  read
  write
}

"""
The level of permission to grant the access token to view and manage security events like code scanning alerts.
"""
enum query_apps_list_installations_items_permissions_security_events {
  read
  write
}

"""
The level of permission to grant the access token to manage just a single file.
"""
enum query_apps_list_installations_items_permissions_single_file {
  read
  write
}

"""The level of permission to grant the access token for commit statuses."""
enum query_apps_list_installations_items_permissions_statuses {
  read
  write
}

"""
The level of permission to grant the access token to manage Dependabot alerts.
"""
enum query_apps_list_installations_items_permissions_vulnerability_alerts {
  read
  write
}

enum write_const {
  write
}

"""
The level of permission to grant the access token to view and edit custom properties for an organization, when allowed by the property.
"""
enum query_apps_list_installations_items_permissions_custom_properties_for_organizations {
  read
  write
}

"""
The level of permission to grant the access token for organization teams and members.
"""
enum query_apps_list_installations_items_permissions_members {
  read
  write
}

"""
The level of permission to grant the access token to manage access to an organization.
"""
enum query_apps_list_installations_items_permissions_organization_administration {
  read
  write
}

"""
The level of permission to grant the access token for custom repository roles management.
"""
enum query_apps_list_installations_items_permissions_organization_custom_roles {
  read
  write
}

"""
The level of permission to grant the access token for custom organization roles management.
"""
enum query_apps_list_installations_items_permissions_organization_custom_org_roles {
  read
  write
}

"""
The level of permission to grant the access token for repository custom properties management at the organization level.
"""
enum query_apps_list_installations_items_permissions_organization_custom_properties {
  read
  write
  admin
}

"""
The level of permission to grant the access token to view and manage Copilot coding agent settings for an organization.
"""
enum query_apps_list_installations_items_permissions_organization_copilot_agent_settings {
  read
  write
}

"""
The level of permission to grant the access token to view and manage announcement banners for an organization.
"""
enum query_apps_list_installations_items_permissions_organization_announcement_banners {
  read
  write
}

enum read_const {
  read
}

"""
The level of permission to grant the access token to manage the post-receive hooks for an organization.
"""
enum query_apps_list_installations_items_permissions_organization_hooks {
  read
  write
}

"""
The level of permission to grant the access token for viewing and managing fine-grained personal access token requests to an organization.
"""
enum query_apps_list_installations_items_permissions_organization_personal_access_tokens {
  read
  write
}

"""
The level of permission to grant the access token for viewing and managing fine-grained personal access tokens that have been approved by an organization.
"""
enum query_apps_list_installations_items_permissions_organization_personal_access_token_requests {
  read
  write
}

"""
The level of permission to grant the access token to manage organization projects and projects public preview (where available).
"""
enum query_apps_list_installations_items_permissions_organization_projects {
  read
  write
  admin
}

"""
The level of permission to grant the access token for organization packages published to GitHub Packages.
"""
enum query_apps_list_installations_items_permissions_organization_packages {
  read
  write
}

"""
The level of permission to grant the access token to manage organization secrets.
"""
enum query_apps_list_installations_items_permissions_organization_secrets {
  read
  write
}

"""
The level of permission to grant the access token to view and manage GitHub Actions self-hosted runners available to an organization.
"""
enum query_apps_list_installations_items_permissions_organization_self_hosted_runners {
  read
  write
}

"""
The level of permission to grant the access token to view and manage users blocked by the organization.
"""
enum query_apps_list_installations_items_permissions_organization_user_blocking {
  read
  write
}

"""
The level of permission to grant the access token to manage the email addresses belonging to a user.
"""
enum query_apps_list_installations_items_permissions_email_addresses {
  read
  write
}

"""
The level of permission to grant the access token to manage the followers belonging to a user.
"""
enum query_apps_list_installations_items_permissions_followers {
  read
  write
}

"""
The level of permission to grant the access token to manage git SSH keys.
"""
enum query_apps_list_installations_items_permissions_git_ssh_keys {
  read
  write
}

"""
The level of permission to grant the access token to view and manage GPG keys belonging to a user.
"""
enum query_apps_list_installations_items_permissions_gpg_keys {
  read
  write
}

"""
The level of permission to grant the access token to view and manage interaction limits on a repository.
"""
enum query_apps_list_installations_items_permissions_interaction_limits {
  read
  write
}

"""
The level of permission to grant the access token to list and manage repositories a user is starring.
"""
enum query_apps_list_installations_items_permissions_starring {
  read
  write
}

"""
The level of permission to grant the access token for organization custom properties management at the enterprise level.
"""
enum query_apps_list_installations_items_permissions_enterprise_custom_properties_for_organizations {
  read
  write
  admin
}

"""A GitHub user."""
type nullable_simple_user {
  name: String
  email: String
  login: String!
  id: BigInt!
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  starred_at: String
  user_view_type: String
}

"""A GitHub Classroom assignment"""
type Classroom_Assignment {
  """Unique identifier of the repository."""
  id: Int!

  """Whether an accepted assignment creates a public repository."""
  public_repo: Boolean!

  """Assignment title."""
  title: String!
  type: query_classroom_get_an_assignment_type!

  """The link that a student can use to accept the assignment."""
  invite_link: URL!

  """
  Whether the invitation link is enabled. Visiting an enabled invitation link will accept the assignment.
  """
  invitations_enabled: Boolean!

  """Sluggified name of the assignment."""
  slug: String!

  """
  Whether students are admins on created repository when a student accepts the assignment.
  """
  students_are_repo_admins: Boolean!

  """
  Whether feedback pull request will be created when a student accepts the assignment.
  """
  feedback_pull_requests_enabled: Boolean!

  """The maximum allowable teams for the assignment."""
  max_teams: Int

  """The maximum allowable members per team."""
  max_members: Int

  """The selected editor for the assignment."""
  editor: String!

  """The number of students that have accepted the assignment."""
  accepted: Int!

  """The number of students that have submitted the assignment."""
  submitted: Int!

  """The number of students that have passed the assignment."""
  passing: Int!

  """The programming language used in the assignment."""
  language: String!

  """The time at which the assignment is due."""
  deadline: DateTime
  starter_code_repository: Simple_Classroom_Repository!
  classroom: Classroom!
}

"""Whether it's a group assignment or individual assignment."""
enum query_classroom_get_an_assignment_type {
  individual
  group
}

"""A GitHub repository view for Classroom"""
type Simple_Classroom_Repository {
  """A unique identifier of the repository."""
  id: Int!

  """The full, globally unique name of the repository."""
  full_name: String!

  """The URL to view the repository on GitHub.com."""
  html_url: URL!

  """The GraphQL identifier of the repository."""
  node_id: String!

  """Whether the repository is private."""
  private: Boolean!

  """The default branch for the repository."""
  default_branch: String!
}

"""A GitHub Classroom classroom"""
type Classroom {
  """Unique identifier of the classroom."""
  id: Int!

  """The name of the classroom."""
  name: String!

  """Whether classroom is archived."""
  archived: Boolean!
  organization: Organization_Simple_for_Classroom!

  """The URL of the classroom on GitHub Classroom."""
  url: URL!
}

"""A GitHub organization."""
type Organization_Simple_for_Classroom {
  id: Int!
  login: String!
  node_id: String!
  html_url: URL!
  name: String
  avatar_url: URL!
}

"""A GitHub Classroom accepted assignment"""
type Classroom_Accepted_Assignment {
  """Unique identifier of the repository."""
  id: Int!

  """Whether an accepted assignment has been submitted."""
  submitted: Boolean!

  """Whether a submission passed."""
  passing: Boolean!

  """Count of student commits."""
  commit_count: Int!

  """Most recent grade."""
  grade: String!
  students: [Simple_Classroom_User]!
  repository: Simple_Classroom_Repository!
  assignment: Simple_Classroom_Assignment!
}

"""A GitHub user simplified for Classroom."""
type Simple_Classroom_User {
  id: Int!
  login: String!
  avatar_url: URL!
  html_url: URL!
}

"""A GitHub Classroom assignment"""
type Simple_Classroom_Assignment {
  """Unique identifier of the repository."""
  id: Int!

  """Whether an accepted assignment creates a public repository."""
  public_repo: Boolean!

  """Assignment title."""
  title: String!
  type: query_classroom_list_accepted_assignments_for_an_assignment_items_assignment_type!

  """The link that a student can use to accept the assignment."""
  invite_link: URL!

  """
  Whether the invitation link is enabled. Visiting an enabled invitation link will accept the assignment.
  """
  invitations_enabled: Boolean!

  """Sluggified name of the assignment."""
  slug: String!

  """
  Whether students are admins on created repository on accepted assignment.
  """
  students_are_repo_admins: Boolean!

  """
  Whether feedback pull request will be created on assignment acceptance.
  """
  feedback_pull_requests_enabled: Boolean!

  """The maximum allowable teams for the assignment."""
  max_teams: Int

  """The maximum allowable members per team."""
  max_members: Int

  """The selected editor for the assignment."""
  editor: String!

  """The number of students that have accepted the assignment."""
  accepted: Int!

  """The number of students that have submitted the assignment."""
  submitted: Int!

  """The number of students that have passed the assignment."""
  passing: Int!

  """The programming language used in the assignment."""
  language: String!

  """The time at which the assignment is due."""
  deadline: DateTime
  classroom: Simple_Classroom!
}

"""Whether it's a Group Assignment or Individual Assignment."""
enum query_classroom_list_accepted_assignments_for_an_assignment_items_assignment_type {
  individual
  group
}

"""A GitHub Classroom classroom"""
type Simple_Classroom {
  """Unique identifier of the classroom."""
  id: Int!

  """The name of the classroom."""
  name: String!

  """Returns whether classroom is archived or not."""
  archived: Boolean!

  """The url of the classroom on GitHub Classroom."""
  url: URL!
}

"""Grade for a student or groups GitHub Classroom assignment"""
type Classroom_Assignment_Grade {
  """Name of the assignment"""
  assignment_name: String!

  """URL of the assignment"""
  assignment_url: String!

  """URL of the starter code for the assignment"""
  starter_code_url: String!

  """GitHub username of the student"""
  github_username: String!

  """Roster identifier of the student"""
  roster_identifier: String!

  """Name of the student's assignment repository"""
  student_repository_name: String!

  """URL of the student's assignment repository"""
  student_repository_url: String!

  """Timestamp of the student's assignment submission"""
  submission_timestamp: String!

  """Number of points awarded to the student"""
  points_awarded: Int!

  """Number of points available for the assignment"""
  points_available: Int!

  """If a group assignment, name of the group the student is in"""
  group_name: String
}

"""Code Of Conduct"""
type Code_Of_Conduct {
  key: String!
  name: String!
  url: URL!
  body: String
  html_url: URL
}

"""GitHub Actions cache retention policy for an enterprise."""
type Actions_cache_retention_limit_for_an_enterprise {
  """
  For repositories & organizations in an enterprise, the maximum duration, in days, for which caches in a repository may be retained.
  """
  max_cache_retention_days: Int
}

"""GitHub Actions cache storage policy for an enterprise."""
type Actions_cache_storage_limit_for_an_enterprise {
  """
  For repositories & organizations in an enterprise, the maximum size limit for the sum of all caches in a repository, in gigabytes.
  """
  max_cache_size_gb: Int
}

"""An OIDC custom property inclusion for repository properties"""
type Actions_OIDC_Custom_Property_Inclusion {
  """The name of the custom property that is included in the OIDC token"""
  custom_property_name: String!
  inclusion_source: query_oidc_list_oidc_custom_property_inclusions_for_enterprise_items_inclusion_source!
}

"""
Whether the inclusion was defined at the organization or enterprise level
"""
enum query_oidc_list_oidc_custom_property_inclusions_for_enterprise_items_inclusion_source {
  organization
  enterprise
}

"""A code security configuration"""
type code_security_configuration {
  """The ID of the code security configuration"""
  id: Int

  """
  The name of the code security configuration. Must be unique within the organization.
  """
  name: String
  target_type: query_code_security_get_configurations_for_enterprise_items_target_type

  """A description of the code security configuration"""
  description: String
  advanced_security: query_code_security_get_configurations_for_enterprise_items_advanced_security
  dependency_graph: query_code_security_get_configurations_for_enterprise_items_dependency_graph
  dependency_graph_autosubmit_action: query_code_security_get_configurations_for_enterprise_items_dependency_graph_autosubmit_action
  dependency_graph_autosubmit_action_options: query_code_security_get_configurations_for_enterprise_items_dependency_graph_autosubmit_action_options
  dependabot_alerts: query_code_security_get_configurations_for_enterprise_items_dependabot_alerts
  dependabot_security_updates: query_code_security_get_configurations_for_enterprise_items_dependabot_security_updates
  dependabot_delegated_alert_dismissal: query_code_security_get_configurations_for_enterprise_items_dependabot_delegated_alert_dismissal
  code_scanning_options: query_code_security_get_configurations_for_enterprise_items_code_scanning_options
  code_scanning_default_setup: query_code_security_get_configurations_for_enterprise_items_code_scanning_default_setup
  code_scanning_default_setup_options: query_code_security_get_configurations_for_enterprise_items_code_scanning_default_setup_options
  code_scanning_delegated_alert_dismissal: query_code_security_get_configurations_for_enterprise_items_code_scanning_delegated_alert_dismissal
  secret_scanning: query_code_security_get_configurations_for_enterprise_items_secret_scanning
  secret_scanning_push_protection: query_code_security_get_configurations_for_enterprise_items_secret_scanning_push_protection
  secret_scanning_delegated_bypass: query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass
  secret_scanning_delegated_bypass_options: query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass_options
  secret_scanning_validity_checks: query_code_security_get_configurations_for_enterprise_items_secret_scanning_validity_checks
  secret_scanning_non_provider_patterns: query_code_security_get_configurations_for_enterprise_items_secret_scanning_non_provider_patterns
  secret_scanning_generic_secrets: query_code_security_get_configurations_for_enterprise_items_secret_scanning_generic_secrets
  secret_scanning_delegated_alert_dismissal: query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_alert_dismissal
  secret_scanning_extended_metadata: query_code_security_get_configurations_for_enterprise_items_secret_scanning_extended_metadata
  private_vulnerability_reporting: query_code_security_get_configurations_for_enterprise_items_private_vulnerability_reporting
  enforcement: query_code_security_get_configurations_for_enterprise_items_enforcement

  """The URL of the configuration"""
  url: URL

  """The URL of the configuration"""
  html_url: URL
  created_at: DateTime
  updated_at: DateTime
}

"""The type of the code security configuration."""
enum query_code_security_get_configurations_for_enterprise_items_target_type {
  global
  organization
  enterprise
}

"""The enablement status of GitHub Advanced Security"""
enum query_code_security_get_configurations_for_enterprise_items_advanced_security {
  enabled
  disabled
  code_security
  secret_protection
}

"""The enablement status of Dependency Graph"""
enum query_code_security_get_configurations_for_enterprise_items_dependency_graph {
  enabled
  disabled
  not_set
}

"""The enablement status of Automatic dependency submission"""
enum query_code_security_get_configurations_for_enterprise_items_dependency_graph_autosubmit_action {
  enabled
  disabled
  not_set
}

"""Feature options for Automatic dependency submission"""
type query_code_security_get_configurations_for_enterprise_items_dependency_graph_autosubmit_action_options {
  """
  Whether to use runners labeled with 'dependency-submission' or standard GitHub runners.
  """
  labeled_runners: Boolean
}

"""The enablement status of Dependabot alerts"""
enum query_code_security_get_configurations_for_enterprise_items_dependabot_alerts {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependabot security updates"""
enum query_code_security_get_configurations_for_enterprise_items_dependabot_security_updates {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependabot delegated alert dismissal"""
enum query_code_security_get_configurations_for_enterprise_items_dependabot_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""Feature options for code scanning"""
type query_code_security_get_configurations_for_enterprise_items_code_scanning_options {
  """Whether to allow repos which use advanced setup"""
  allow_advanced: Boolean
}

"""The enablement status of code scanning default setup"""
enum query_code_security_get_configurations_for_enterprise_items_code_scanning_default_setup {
  enabled
  disabled
  not_set
}

"""Feature options for code scanning default setup"""
type query_code_security_get_configurations_for_enterprise_items_code_scanning_default_setup_options {
  runner_type: query_code_security_get_configurations_for_enterprise_items_code_scanning_default_setup_options_runner_type

  """
  The label of the runner to use for code scanning when runner_type is 'labeled'.
  """
  runner_label: String
}

"""Whether to use labeled runners or standard GitHub runners."""
enum query_code_security_get_configurations_for_enterprise_items_code_scanning_default_setup_options_runner_type {
  standard
  labeled
  not_set
}

"""The enablement status of code scanning delegated alert dismissal"""
enum query_code_security_get_configurations_for_enterprise_items_code_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning push protection"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_push_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated bypass"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass {
  enabled
  disabled
  not_set
}

"""Feature options for secret scanning delegated bypass"""
type query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass_options {
  """The bypass reviewers for secret scanning delegated bypass"""
  reviewers: [query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass_options_reviewers_items]
}

type query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass_options_reviewers_items {
  """The ID of the team or role selected as a bypass reviewer"""
  reviewer_id: Int!
  reviewer_type: query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type!

  """
  The ID of the security configuration associated with this bypass reviewer
  """
  security_configuration_id: Int
}

"""The type of the bypass reviewer"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type {
  TEAM
  ROLE
}

"""The enablement status of secret scanning validity checks"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_validity_checks {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning non-provider patterns"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_non_provider_patterns {
  enabled
  disabled
  not_set
}

"""The enablement status of Copilot secret scanning"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_generic_secrets {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated alert dismissal"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning extended metadata"""
enum query_code_security_get_configurations_for_enterprise_items_secret_scanning_extended_metadata {
  enabled
  disabled
  not_set
}

"""The enablement status of private vulnerability reporting"""
enum query_code_security_get_configurations_for_enterprise_items_private_vulnerability_reporting {
  enabled
  disabled
  not_set
}

"""The enforcement status for a security configuration"""
enum query_code_security_get_configurations_for_enterprise_items_enforcement {
  enforced
  unenforced
}

type query_code_security_get_default_configurations_for_enterprise_items {
  default_for_new_repos: query_code_security_get_default_configurations_for_enterprise_items_default_for_new_repos
  configuration: code_security_configuration
}

"""
The visibility of newly created repositories for which the code security configuration will be applied to by default
"""
enum query_code_security_get_default_configurations_for_enterprise_items_default_for_new_repos {
  public
  private_and_internal
  all
}

"""
Repositories associated with a code security configuration and attachment status
"""
type code_security_configuration_repositories {
  status: query_code_security_get_repositories_for_enterprise_configuration_items_status
  repository: simple_repository
}

"""
The attachment status of the code security configuration on the repository.
"""
enum query_code_security_get_repositories_for_enterprise_configuration_items_status {
  attached
  attaching
  detached
  removed
  enforced
  failed
  updating
  removed_by_enterprise
}

"""A GitHub repository."""
type simple_repository {
  """A unique identifier of the repository."""
  id: BigInt!

  """The GraphQL identifier of the repository."""
  node_id: String!

  """The name of the repository."""
  name: String!

  """The full, globally unique, name of the repository."""
  full_name: String!
  owner: simple_user!

  """Whether the repository is private."""
  private: Boolean!

  """The URL to view the repository on GitHub.com."""
  html_url: URL!

  """The repository description."""
  description: String

  """Whether the repository is a fork."""
  fork: Boolean!

  """
  The URL to get more information about the repository from the GitHub API.
  """
  url: URL!

  """A template for the API URL to download the repository as an archive."""
  archive_url: URL!

  """
  A template for the API URL to list the available assignees for issues in the repository.
  """
  assignees_url: URL!

  """
  A template for the API URL to create or retrieve a raw Git blob in the repository.
  """
  blobs_url: URL!

  """
  A template for the API URL to get information about branches in the repository.
  """
  branches_url: URL!

  """
  A template for the API URL to get information about collaborators of the repository.
  """
  collaborators_url: URL!

  """
  A template for the API URL to get information about comments on the repository.
  """
  comments_url: URL!

  """
  A template for the API URL to get information about commits on the repository.
  """
  commits_url: URL!

  """A template for the API URL to compare two commits or refs."""
  compare_url: URL!

  """A template for the API URL to get the contents of the repository."""
  contents_url: URL!

  """A template for the API URL to list the contributors to the repository."""
  contributors_url: URL!

  """The API URL to list the deployments of the repository."""
  deployments_url: URL!

  """The API URL to list the downloads on the repository."""
  downloads_url: URL!

  """The API URL to list the events of the repository."""
  events_url: URL!

  """The API URL to list the forks of the repository."""
  forks_url: URL!

  """
  A template for the API URL to get information about Git commits of the repository.
  """
  git_commits_url: URL!

  """
  A template for the API URL to get information about Git refs of the repository.
  """
  git_refs_url: URL!

  """
  A template for the API URL to get information about Git tags of the repository.
  """
  git_tags_url: URL!

  """
  A template for the API URL to get information about issue comments on the repository.
  """
  issue_comment_url: URL!

  """
  A template for the API URL to get information about issue events on the repository.
  """
  issue_events_url: URL!

  """
  A template for the API URL to get information about issues on the repository.
  """
  issues_url: URL!

  """
  A template for the API URL to get information about deploy keys on the repository.
  """
  keys_url: URL!

  """
  A template for the API URL to get information about labels of the repository.
  """
  labels_url: URL!

  """The API URL to get information about the languages of the repository."""
  languages_url: URL!

  """The API URL to merge branches in the repository."""
  merges_url: URL!

  """
  A template for the API URL to get information about milestones of the repository.
  """
  milestones_url: URL!

  """
  A template for the API URL to get information about notifications on the repository.
  """
  notifications_url: URL!

  """
  A template for the API URL to get information about pull requests on the repository.
  """
  pulls_url: URL!

  """
  A template for the API URL to get information about releases on the repository.
  """
  releases_url: URL!

  """The API URL to list the stargazers on the repository."""
  stargazers_url: URL!

  """
  A template for the API URL to get information about statuses of a commit.
  """
  statuses_url: URL!

  """The API URL to list the subscribers on the repository."""
  subscribers_url: URL!

  """The API URL to subscribe to notifications for this repository."""
  subscription_url: URL!

  """The API URL to get information about tags on the repository."""
  tags_url: URL!

  """The API URL to list the teams on the repository."""
  teams_url: URL!

  """
  A template for the API URL to create or retrieve a raw Git tree of the repository.
  """
  trees_url: URL!

  """The API URL to list the hooks on the repository."""
  hooks_url: URL!
}

"""A Dependabot alert."""
type dependabot_alert_with_repository {
  """The security alert number."""
  number: Int!
  state: query_dependabot_list_alerts_for_enterprise_items_state!
  dependency: query_dependabot_list_alerts_for_enterprise_items_dependency!
  security_advisory: dependabot_alert_security_advisory!
  security_vulnerability: dependabot_alert_security_vulnerability!

  """The REST API URL of the alert resource."""
  url: URL!

  """The GitHub URL of the alert resource."""
  html_url: URL!

  """
  The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime!

  """
  The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime!

  """
  The time that the alert was dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  dismissed_at: DateTime
  dismissed_by: nullable_simple_user
  dismissed_reason: query_dependabot_list_alerts_for_enterprise_items_dismissed_reason

  """An optional comment associated with the alert's dismissal."""
  dismissed_comment: query_dependabot_list_alerts_for_enterprise_items_dismissed_comment

  """
  The time that the alert was no longer detected and was considered fixed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  fixed_at: DateTime

  """
  The time that the alert was auto-dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  auto_dismissed_at: DateTime
  dismissal_request: Dependabot_alert_dismissal_request

  """The users assigned to this alert."""
  assignees: [simple_user]
  repository: simple_repository!
}

"""The state of the Dependabot alert."""
enum query_dependabot_list_alerts_for_enterprise_items_state {
  auto_dismissed
  dismissed
  fixed
  open
}

"""Details for the vulnerable dependency."""
type query_dependabot_list_alerts_for_enterprise_items_dependency {
  package: dependabot_alert_package

  """
  The full path to the dependency manifest file, relative to the root of the repository.
  """
  manifest_path: String
  scope: query_dependabot_list_alerts_for_enterprise_items_dependency_scope
  relationship: query_dependabot_list_alerts_for_enterprise_items_dependency_relationship
}

"""Details for the vulnerable package."""
type dependabot_alert_package {
  """The package's language or package management ecosystem."""
  ecosystem: String!

  """The unique package name within its ecosystem."""
  name: String!
}

"""The execution scope of the vulnerable dependency."""
enum query_dependabot_list_alerts_for_enterprise_items_dependency_scope {
  development
  runtime
}

"""
The vulnerable dependency's relationship to your project.

> [!NOTE]
> We are rolling out support for dependency relationship across ecosystems. This value will be "unknown" for all dependencies in unsupported ecosystems.
"""
enum query_dependabot_list_alerts_for_enterprise_items_dependency_relationship {
  unknown
  direct
  transitive
}

"""Details for the GitHub Security Advisory."""
type dependabot_alert_security_advisory {
  """The unique GitHub Security Advisory ID assigned to the advisory."""
  ghsa_id: String!

  """The unique CVE ID assigned to the advisory."""
  cve_id: String

  """A short, plain text summary of the advisory."""
  summary: query_dependabot_list_alerts_for_enterprise_items_security_advisory_summary!

  """A long-form Markdown-supported description of the advisory."""
  description: String!

  """Vulnerable version range information for the advisory."""
  vulnerabilities: [dependabot_alert_security_vulnerability]!
  severity: query_dependabot_list_alerts_for_enterprise_items_security_advisory_severity!
  cvss: query_dependabot_list_alerts_for_enterprise_items_security_advisory_cvss!
  cvss_severities: cvss_severities
  epss: security_advisory_epss

  """Details for the advisory pertaining to Common Weakness Enumeration."""
  cwes: [query_dependabot_list_alerts_for_enterprise_items_security_advisory_cwes_items]!

  """Values that identify this advisory among security information sources."""
  identifiers: [query_dependabot_list_alerts_for_enterprise_items_security_advisory_identifiers_items]!

  """Links to additional advisory information."""
  references: [query_dependabot_list_alerts_for_enterprise_items_security_advisory_references_items]!

  """
  The time that the advisory was published in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  published_at: DateTime!

  """
  The time that the advisory was last modified in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime!

  """
  The time that the advisory was withdrawn in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  withdrawn_at: DateTime
}

"""A short, plain text summary of the advisory."""
scalar query_dependabot_list_alerts_for_enterprise_items_security_advisory_summary

"""Details pertaining to one vulnerable version range for the advisory."""
type dependabot_alert_security_vulnerability {
  package: dependabot_alert_package!
  severity: query_dependabot_list_alerts_for_enterprise_items_security_advisory_vulnerabilities_items_severity!

  """
  Conditions that identify vulnerable versions of this vulnerability's package.
  """
  vulnerable_version_range: String!
  first_patched_version: query_dependabot_list_alerts_for_enterprise_items_security_advisory_vulnerabilities_items_first_patched_version
}

"""The severity of the vulnerability."""
enum query_dependabot_list_alerts_for_enterprise_items_security_advisory_vulnerabilities_items_severity {
  low
  medium
  high
  critical
}

"""
Details pertaining to the package version that patches this vulnerability.
"""
type query_dependabot_list_alerts_for_enterprise_items_security_advisory_vulnerabilities_items_first_patched_version {
  """The package version that patches this vulnerability."""
  identifier: String!
}

"""The severity of the advisory."""
enum query_dependabot_list_alerts_for_enterprise_items_security_advisory_severity {
  low
  medium
  high
  critical
}

"""
Details for the advisory pertaining to the Common Vulnerability Scoring System.
"""
type query_dependabot_list_alerts_for_enterprise_items_security_advisory_cvss {
  """The overall CVSS score of the advisory."""
  score: NonNegativeFloat!

  """The full CVSS vector string for the advisory."""
  vector_string: String
}

"""A CWE weakness assigned to the advisory."""
type query_dependabot_list_alerts_for_enterprise_items_security_advisory_cwes_items {
  """The unique CWE ID."""
  cwe_id: String!

  """The short, plain text name of the CWE."""
  name: String!
}

"""An advisory identifier."""
type query_dependabot_list_alerts_for_enterprise_items_security_advisory_identifiers_items {
  type: query_dependabot_list_alerts_for_enterprise_items_security_advisory_identifiers_items_type!

  """The value of the advisory identifer."""
  value: String!
}

"""The type of advisory identifier."""
enum query_dependabot_list_alerts_for_enterprise_items_security_advisory_identifiers_items_type {
  CVE
  GHSA
}

"""A link to additional advisory information."""
type query_dependabot_list_alerts_for_enterprise_items_security_advisory_references_items {
  """The URL of the reference."""
  url: URL!
}

"""The reason that the alert was dismissed."""
enum query_dependabot_list_alerts_for_enterprise_items_dismissed_reason {
  fix_started
  inaccurate
  no_bandwidth
  not_used
  tolerable_risk
}

"""An optional comment associated with the alert's dismissal."""
scalar query_dependabot_list_alerts_for_enterprise_items_dismissed_comment

"""
Information about an active dismissal request for this Dependabot alert.
"""
type Dependabot_alert_dismissal_request {
  """The unique identifier of the dismissal request."""
  id: Int
  status: query_dependabot_list_alerts_for_enterprise_items_dismissal_request_status
  requester: query_dependabot_list_alerts_for_enterprise_items_dismissal_request_requester

  """The date and time when the dismissal request was created."""
  created_at: DateTime

  """The API URL to get more information about this dismissal request."""
  url: URL
}

"""The current status of the dismissal request."""
enum query_dependabot_list_alerts_for_enterprise_items_dismissal_request_status {
  pending
  approved
  rejected
  cancelled
}

"""The user who requested the dismissal."""
type query_dependabot_list_alerts_for_enterprise_items_dismissal_request_requester {
  """The unique identifier of the user."""
  id: Int

  """The login name of the user."""
  login: String
}

input dependabot_alert_comma_separated_has_parameter_Input {
  String: String
  patch_const: patch_const
}

enum patch_const {
  patch
}

"""
The scope of the vulnerable dependency. If specified, only alerts with this scope will be returned.
"""
enum dependabot_alert_scope_parameter {
  development
  runtime
}

"""
The property by which to sort the results.
`created` means when the alert was created.
`updated` means when the alert's state last changed.
`epss_percentage` sorts alerts by the Exploit Prediction Scoring System (EPSS) percentage.
"""
enum dependabot_alert_sort_parameter {
  created
  updated
  epss_percentage
}

"""Group of enterprise owners and/or members"""
type Enterprise_Team {
  id: BigInt!
  name: String!
  description: String
  slug: String!
  url: URL!

  """Retired: this field will not be returned with GHEC enterprise teams."""
  sync_to_organizations: String
  organization_selection_type: String
  group_id: String

  """Retired: this field will not be returned with GHEC enterprise teams."""
  group_name: String
  html_url: URL!
  members_url: String!
  created_at: DateTime!
  updated_at: DateTime!
}

"""A GitHub organization."""
type organization_simple {
  login: String!
  id: Int!
  node_id: String!
  url: URL!
  repos_url: URL!
  events_url: URL!
  hooks_url: URL!
  issues_url: URL!
  members_url: URL!
  public_members_url: URL!
  avatar_url: URL!
  description: String
}

"""Event"""
type Event {
  id: String!
  type: String
  actor: Actor!
  repo: query_activity_list_public_events_items_repo!
  org: Actor
  payload: query_activity_list_public_events_items_payload!
  public: Boolean!
  created_at: DateTime
}

"""Actor"""
type Actor {
  id: Int!
  login: String!
  display_login: String
  gravatar_id: String
  url: URL!
  avatar_url: URL!
}

type query_activity_list_public_events_items_repo {
  id: Int!
  name: String!
  url: URL!
}

union query_activity_list_public_events_items_payload = CreateEvent | DeleteEvent | DiscussionEvent | IssuesEvent | IssueCommentEvent | ForkEvent | GollumEvent | MemberEvent | JSON_container | PushEvent | PullRequestEvent | PullRequestReviewCommentEvent | PullRequestReviewEvent | CommitCommentEvent | ReleaseEvent | WatchEvent

type CreateEvent {
  ref: String!
  ref_type: String!
  full_ref: String!
  master_branch: String!
  description: String
  pusher_type: String!
}

type DeleteEvent {
  ref: String!
  ref_type: String!
  full_ref: String!
  pusher_type: String!
}

type DiscussionEvent {
  action: String!
  discussion: Discussion!
}

"""A Discussion in a repository."""
type Discussion {
  active_lock_reason: String
  answer_chosen_at: String
  answer_chosen_by: User
  answer_html_url: String
  author_association: AuthorAssociation
  body: String!
  category: query_activity_list_public_events_items_payload_oneOf_2_discussion_category!
  comments: Int!
  created_at: DateTime!
  html_url: String!
  id: Int!
  locked: Boolean!
  node_id: String!
  number: Int!
  reactions: Reactions
  repository_url: String!
  state: query_activity_list_public_events_items_payload_oneOf_2_discussion_state!
  state_reason: query_activity_list_public_events_items_payload_oneOf_2_discussion_state_reason
  timeline_url: String
  title: String!
  updated_at: DateTime!
  user: query_activity_list_public_events_items_payload_oneOf_2_discussion_user
  labels: [label]
}

type User {
  avatar_url: URL
  deleted: Boolean
  email: String
  events_url: UriTemplate
  followers_url: URL
  following_url: UriTemplate
  gists_url: UriTemplate
  gravatar_id: String
  html_url: URL
  id: Int!
  login: String!
  name: String
  node_id: String
  organizations_url: URL
  received_events_url: URL
  repos_url: URL
  site_admin: Boolean
  starred_url: UriTemplate
  subscriptions_url: URL
  type: query_activity_list_public_events_items_payload_oneOf_2_discussion_answer_chosen_by_type
  url: URL
  user_view_type: String
}

enum query_activity_list_public_events_items_payload_oneOf_2_discussion_answer_chosen_by_type {
  Bot
  User
  Organization
}

"""How the author is associated with the repository."""
enum AuthorAssociation {
  COLLABORATOR
  CONTRIBUTOR
  FIRST_TIMER
  FIRST_TIME_CONTRIBUTOR
  MANNEQUIN
  MEMBER
  NONE
  OWNER
}

type query_activity_list_public_events_items_payload_oneOf_2_discussion_category {
  created_at: DateTime!
  description: String!
  emoji: String!
  id: Int!
  is_answerable: Boolean!
  name: String!
  node_id: String
  repository_id: Int!
  slug: String!
  updated_at: String!
}

type Reactions {
  _0PLUS_1: Int!
  NEGATIVE_1: Int!
  confused: Int!
  eyes: Int!
  heart: Int!
  hooray: Int!
  laugh: Int!
  rocket: Int!
  total_count: Int!
  url: URL!
}

"""
The current state of the discussion.
`converting` means that the discussion is being converted from an issue.
`transferring` means that the discussion is being transferred from another repository.
"""
enum query_activity_list_public_events_items_payload_oneOf_2_discussion_state {
  open
  closed
  locked
  converting
  transferring
}

"""The reason for the current state"""
enum query_activity_list_public_events_items_payload_oneOf_2_discussion_state_reason {
  resolved
  outdated
  duplicate
  reopened
}

type query_activity_list_public_events_items_payload_oneOf_2_discussion_user {
  avatar_url: URL
  deleted: Boolean
  email: String
  events_url: UriTemplate
  followers_url: URL
  following_url: UriTemplate
  gists_url: UriTemplate
  gravatar_id: String
  html_url: URL
  id: BigInt!
  login: String!
  name: String
  node_id: String
  organizations_url: URL
  received_events_url: URL
  repos_url: URL
  site_admin: Boolean
  starred_url: UriTemplate
  subscriptions_url: URL
  type: query_activity_list_public_events_items_payload_oneOf_2_discussion_user_type
  url: URL
  user_view_type: String
}

enum query_activity_list_public_events_items_payload_oneOf_2_discussion_user_type {
  Bot
  User
  Organization
}

"""
Color-coded labels help you categorize and filter your issues (just like labels in Gmail).
"""
type label {
  """Unique identifier for the label."""
  id: BigInt!
  node_id: String!

  """URL for the label"""
  url: URL!

  """The name of the label."""
  name: String!

  """Optional description of the label, such as its purpose."""
  description: String

  """6-character hex code, without the leading #, identifying the color"""
  color: String!

  """Whether this label comes by default in a new repository."""
  default: Boolean!
}

type IssuesEvent {
  action: String!
  issue: issue!
  assignee: simple_user
  assignees: [simple_user]
  label: label
  labels: [label]
}

"""
Issues are a great way to keep track of tasks, enhancements, and bugs for your projects.
"""
type issue {
  id: BigInt!
  node_id: String!

  """URL for the issue"""
  url: URL!
  repository_url: URL!
  labels_url: String!
  comments_url: URL!
  events_url: URL!
  html_url: URL!

  """Number uniquely identifying the issue within its repository"""
  number: Int!

  """State of the issue; either 'open' or 'closed'"""
  state: String!
  state_reason: query_activity_list_public_events_items_payload_oneOf_3_issue_state_reason

  """Title of the issue"""
  title: String!

  """Contents of the issue"""
  body: String
  user: nullable_simple_user

  """
  Labels to associate with this issue; pass one or more label names to replace the set of labels on this issue; send an empty array to clear all labels from the issue; note that the labels are silently dropped for users without push access to the repository
  """
  labels: [query_activity_list_public_events_items_payload_oneOf_3_issue_labels_items]!
  assignee: nullable_simple_user
  assignees: [simple_user]
  milestone: nullable_milestone
  locked: Boolean!
  active_lock_reason: String
  comments: Int!
  pull_request: query_activity_list_public_events_items_payload_oneOf_3_issue_pull_request
  closed_at: DateTime
  created_at: DateTime!
  updated_at: DateTime!
  draft: Boolean
  closed_by: nullable_simple_user
  body_html: String
  body_text: String
  timeline_url: URL
  type: Issue_Type
  repository: repository
  performed_via_github_app: nullable_integration
  author_association: author_association
  reactions: Reaction_Rollup
  sub_issues_summary: Sub_issues_Summary

  """URL to get the parent issue of this issue, if it is a sub-issue"""
  parent_issue_url: URL
  pinned_comment: nullable_issue_comment
  issue_dependencies_summary: Issue_Dependencies_Summary
  issue_field_values: [Issue_Field_Value]
}

"""The reason for the current state"""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_state_reason {
  completed
  reopened
  not_planned
  duplicate
}

union query_activity_list_public_events_items_payload_oneOf_3_issue_labels_items = String_container | query_activity_list_public_events_items_payload_oneOf_3_issue_labels_items_oneOf_1

type query_activity_list_public_events_items_payload_oneOf_3_issue_labels_items_oneOf_1 {
  id: BigInt
  node_id: String
  url: URL
  name: String
  description: String
  color: String
  default: Boolean
}

"""A collection of related issues and pull requests."""
type nullable_milestone {
  url: URL!
  html_url: URL!
  labels_url: URL!
  id: Int!
  node_id: String!

  """The number of the milestone."""
  number: Int!
  state: query_activity_list_public_events_items_payload_oneOf_3_issue_milestone_state!

  """The title of the milestone."""
  title: String!
  description: String
  creator: nullable_simple_user
  open_issues: Int!
  closed_issues: Int!
  created_at: DateTime!
  updated_at: DateTime!
  closed_at: DateTime
  due_on: DateTime
}

"""The state of the milestone."""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_milestone_state {
  open
  closed
}

type query_activity_list_public_events_items_payload_oneOf_3_issue_pull_request {
  merged_at: DateTime
  diff_url: URL
  html_url: URL
  patch_url: URL
  url: URL
}

"""The type of issue."""
type Issue_Type {
  """The unique identifier of the issue type."""
  id: Int!

  """The node identifier of the issue type."""
  node_id: String!

  """The name of the issue type."""
  name: String!

  """The description of the issue type."""
  description: String
  color: query_activity_list_public_events_items_payload_oneOf_3_issue_type_color

  """The time the issue type created."""
  created_at: DateTime

  """The time the issue type last updated."""
  updated_at: DateTime

  """The enabled state of the issue type."""
  is_enabled: Boolean
}

"""The color of the issue type."""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_type_color {
  gray
  blue
  green
  yellow
  orange
  red
  pink
  purple
}

"""A repository on GitHub."""
type repository {
  """Unique identifier of the repository"""
  id: BigInt!
  node_id: String!

  """The name of the repository."""
  name: String!
  full_name: String!
  license: nullable_license_simple
  forks: Int!
  permissions: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_permissions
  owner: simple_user!

  """Whether the repository is private or public."""
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  archive_url: URL!
  assignees_url: URL!
  blobs_url: URL!
  branches_url: URL!
  collaborators_url: URL!
  comments_url: URL!
  commits_url: URL!
  compare_url: URL!
  contents_url: URL!
  contributors_url: URL!
  deployments_url: URL!
  downloads_url: URL!
  events_url: URL!
  forks_url: URL!
  git_commits_url: URL!
  git_refs_url: URL!
  git_tags_url: URL!
  git_url: URL!
  issue_comment_url: URL!
  issue_events_url: URL!
  issues_url: URL!
  keys_url: URL!
  labels_url: URL!
  languages_url: URL!
  merges_url: URL!
  milestones_url: URL!
  notifications_url: URL!
  pulls_url: URL!
  releases_url: URL!
  ssh_url: String!
  stargazers_url: URL!
  statuses_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  tags_url: URL!
  teams_url: URL!
  trees_url: URL!
  clone_url: URL!
  mirror_url: URL
  hooks_url: URL!
  svn_url: URL!
  homepage: URL
  language: String
  forks_count: Int!
  stargazers_count: Int!
  watchers_count: Int!

  """
  The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0.
  """
  size: Int!

  """The default branch of the repository."""
  default_branch: String!
  open_issues_count: Int!

  """
  Whether this repository acts as a template that can be used to generate new repositories.
  """
  is_template: Boolean
  topics: [String]

  """Whether issues are enabled."""
  has_issues: Boolean!

  """Whether projects are enabled."""
  has_projects: Boolean!

  """Whether the wiki is enabled."""
  has_wiki: Boolean!
  has_pages: Boolean!

  """Whether downloads are enabled."""
  has_downloads: Boolean! @deprecated(reason: "deprecated")

  """Whether discussions are enabled."""
  has_discussions: Boolean

  """Whether pull requests are enabled."""
  has_pull_requests: Boolean
  pull_request_creation_policy: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_pull_request_creation_policy

  """Whether commit comments are enabled."""
  has_commit_comments: Boolean

  """Whether the repository is archived."""
  archived: Boolean!

  """Returns whether or not this repository disabled."""
  disabled: Boolean!

  """The repository visibility: public, private, or internal."""
  visibility: String
  pushed_at: DateTime
  created_at: DateTime
  updated_at: DateTime

  """Whether to allow rebase merges for pull requests."""
  allow_rebase_merge: Boolean
  temp_clone_token: String

  """Whether to allow squash merges for pull requests."""
  allow_squash_merge: Boolean

  """Whether to allow Auto-merge to be used on pull requests."""
  allow_auto_merge: Boolean

  """Whether to delete head branches when pull requests are merged"""
  delete_branch_on_merge: Boolean

  """
  Whether or not a pull request head branch that is behind its base branch can always be updated even if it is not required to be up to date before merging.
  """
  allow_update_branch: Boolean

  """
  Whether a squash merge commit can use the pull request title as default. **This property is closing down. Please use `squash_merge_commit_title` instead.
  """
  use_squash_pr_title_as_default: Boolean @deprecated(reason: "deprecated")
  squash_merge_commit_title: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_squash_merge_commit_title
  squash_merge_commit_message: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_squash_merge_commit_message
  merge_commit_title: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_merge_commit_title
  merge_commit_message: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_merge_commit_message

  """Whether to allow merge commits for pull requests."""
  allow_merge_commit: Boolean

  """Whether to allow forking this repo"""
  allow_forking: Boolean

  """Whether to require contributors to sign off on web-based commits"""
  web_commit_signoff_required: Boolean
  open_issues: Int!
  watchers: Int!
  master_branch: String
  starred_at: String

  """Whether anonymous git access is enabled for this repository"""
  anonymous_access_enabled: Boolean
  code_search_index_status: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_code_search_index_status
  additionalProperties: JSON
}

"""License Simple"""
type nullable_license_simple {
  key: String!
  name: String!
  url: URL
  spdx_id: String
  node_id: String!
  html_url: URL
}

type query_activity_list_public_events_items_payload_oneOf_3_issue_repository_permissions {
  admin: Boolean!
  pull: Boolean!
  triage: Boolean
  push: Boolean!
  maintain: Boolean
}

"""
The policy controlling who can create pull requests: all or collaborators_only.
"""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_repository_pull_request_creation_policy {
  all
  collaborators_only
}

"""
The default value for a squash merge commit title:

- `PR_TITLE` - default to the pull request's title.
- `COMMIT_OR_PR_TITLE` - default to the commit's title (if only one commit) or the pull request's title (when more than one commit).
"""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_repository_squash_merge_commit_title {
  PR_TITLE
  COMMIT_OR_PR_TITLE
}

"""
The default value for a squash merge commit message:

- `PR_BODY` - default to the pull request's body.
- `COMMIT_MESSAGES` - default to the branch's commit messages.
- `BLANK` - default to a blank commit message.
"""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_repository_squash_merge_commit_message {
  PR_BODY
  COMMIT_MESSAGES
  BLANK
}

"""
The default value for a merge commit title.

- `PR_TITLE` - default to the pull request's title.
- `MERGE_MESSAGE` - default to the classic title for a merge message (e.g., Merge pull request #123 from branch-name).
"""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_repository_merge_commit_title {
  PR_TITLE
  MERGE_MESSAGE
}

"""
The default value for a merge commit message.

- `PR_TITLE` - default to the pull request's title.
- `PR_BODY` - default to the pull request's body.
- `BLANK` - default to a blank commit message.
"""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_repository_merge_commit_message {
  PR_BODY
  PR_TITLE
  BLANK
}

"""The status of the code search index for this repository"""
type query_activity_list_public_events_items_payload_oneOf_3_issue_repository_code_search_index_status {
  lexical_search_ok: Boolean
  lexical_commit_sha: String
}

"""
GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user accounts and granted access to specific repositories. They come with granular permissions and built-in webhooks. GitHub apps are first class actors within GitHub.
"""
type nullable_integration {
  """Unique identifier of the GitHub app"""
  id: Int!

  """The slug name of the GitHub app"""
  slug: String
  node_id: String!
  client_id: String
  owner: query_activity_list_public_events_items_payload_oneOf_3_issue_performed_via_github_app_owner!

  """The name of the GitHub app"""
  name: String!
  description: String
  external_url: URL!
  html_url: URL!
  created_at: DateTime!
  updated_at: DateTime!
  permissions: query_activity_list_public_events_items_payload_oneOf_3_issue_performed_via_github_app_permissions!

  """
  The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation.
  """
  events: [String]!

  """
  The number of installations associated with the GitHub app. Only returned when the integration is requesting details about itself.
  """
  installations_count: Int
}

union query_activity_list_public_events_items_payload_oneOf_3_issue_performed_via_github_app_owner = simple_user | enterprise

"""The set of permissions for the GitHub app"""
type query_activity_list_public_events_items_payload_oneOf_3_issue_performed_via_github_app_permissions {
  issues: String
  checks: String
  metadata: String
  contents: String
  deployments: String
  additionalProperties: JSON
}

"""How the author is associated with the repository."""
enum author_association {
  COLLABORATOR
  CONTRIBUTOR
  FIRST_TIMER
  FIRST_TIME_CONTRIBUTOR
  MANNEQUIN
  MEMBER
  NONE
  OWNER
}

type Reaction_Rollup {
  url: URL!
  total_count: Int!
  _0PLUS_1: Int!
  NEGATIVE_1: Int!
  laugh: Int!
  confused: Int!
  heart: Int!
  hooray: Int!
  eyes: Int!
  rocket: Int!
}

type Sub_issues_Summary {
  total: Int!
  completed: Int!
  percent_completed: Int!
}

"""Comments provide a way for people to collaborate on an issue."""
type nullable_issue_comment {
  """Unique identifier of the issue comment"""
  id: BigInt!
  node_id: String!

  """URL for the issue comment"""
  url: URL!

  """Contents of the issue comment"""
  body: String
  body_text: String
  body_html: String
  html_url: URL!
  user: nullable_simple_user
  created_at: DateTime!
  updated_at: DateTime!
  issue_url: URL!
  author_association: author_association
  performed_via_github_app: nullable_integration
  reactions: Reaction_Rollup
  pin: Pinned_Issue_Comment
}

"""Context around who pinned an issue comment and when it was pinned."""
type Pinned_Issue_Comment {
  pinned_at: DateTime!
  pinned_by: nullable_simple_user
}

type Issue_Dependencies_Summary {
  blocked_by: Int!
  blocking: Int!
  total_blocked_by: Int!
  total_blocking: Int!
}

"""A value assigned to an issue field"""
type Issue_Field_Value {
  """Unique identifier for the issue field."""
  issue_field_id: BigInt!
  node_id: String!
  data_type: query_activity_list_public_events_items_payload_oneOf_3_issue_issue_field_values_items_data_type!

  """The value of the issue field"""
  value: query_activity_list_public_events_items_payload_oneOf_3_issue_issue_field_values_items_value
  single_select_option: query_activity_list_public_events_items_payload_oneOf_3_issue_issue_field_values_items_single_select_option
}

"""The data type of the issue field"""
enum query_activity_list_public_events_items_payload_oneOf_3_issue_issue_field_values_items_data_type {
  text
  single_select
  number
  date
}

"""The value of the issue field"""
type query_activity_list_public_events_items_payload_oneOf_3_issue_issue_field_values_items_value {
  String: String
  Float: Float
  Int: Int
}

"""
Details about the selected option (only present for single_select fields)
"""
type query_activity_list_public_events_items_payload_oneOf_3_issue_issue_field_values_items_single_select_option {
  """Unique identifier for the option."""
  id: BigInt!

  """The name of the option"""
  name: String!

  """The color of the option"""
  color: String!
}

type IssueCommentEvent {
  action: String!
  issue: issue!
  comment: issue_comment!
}

"""Comments provide a way for people to collaborate on an issue."""
type issue_comment {
  """Unique identifier of the issue comment"""
  id: BigInt!
  node_id: String!

  """URL for the issue comment"""
  url: URL!

  """Contents of the issue comment"""
  body: String
  body_text: String
  body_html: String
  html_url: URL!
  user: nullable_simple_user
  created_at: DateTime!
  updated_at: DateTime!
  issue_url: URL!
  author_association: author_association
  performed_via_github_app: nullable_integration
  reactions: Reaction_Rollup
  pin: Pinned_Issue_Comment
}

type ForkEvent {
  action: String!
  forkee: query_activity_list_public_events_items_payload_oneOf_5_forkee!
}

type query_activity_list_public_events_items_payload_oneOf_5_forkee {
  id: Int
  node_id: String
  name: String
  full_name: String
  private: Boolean
  owner: simple_user
  html_url: String
  description: String
  fork: Boolean
  url: String
  forks_url: String
  keys_url: String
  collaborators_url: String
  teams_url: String
  hooks_url: String
  issue_events_url: String
  events_url: String
  assignees_url: String
  branches_url: String
  tags_url: String
  blobs_url: String
  git_tags_url: String
  git_refs_url: String
  trees_url: String
  statuses_url: String
  languages_url: String
  stargazers_url: String
  contributors_url: String
  subscribers_url: String
  subscription_url: String
  commits_url: String
  git_commits_url: String
  comments_url: String
  issue_comment_url: String
  contents_url: String
  compare_url: String
  merges_url: String
  archive_url: String
  downloads_url: String
  issues_url: String
  pulls_url: String
  milestones_url: String
  notifications_url: String
  labels_url: String
  releases_url: String
  deployments_url: String
  created_at: DateTime
  updated_at: DateTime
  pushed_at: DateTime
  git_url: String
  ssh_url: String
  clone_url: String
  svn_url: String
  homepage: String
  size: Int
  stargazers_count: Int
  watchers_count: Int
  language: String
  has_issues: Boolean
  has_projects: Boolean
  has_downloads: Boolean
  has_wiki: Boolean
  has_pages: Boolean
  has_discussions: Boolean
  has_pull_requests: Boolean
  pull_request_creation_policy: query_activity_list_public_events_items_payload_oneOf_5_forkee_pull_request_creation_policy
  has_commit_comments: Boolean
  forks_count: Int
  mirror_url: String
  archived: Boolean
  disabled: Boolean
  open_issues_count: Int
  license: nullable_license_simple
  allow_forking: Boolean
  is_template: Boolean
  web_commit_signoff_required: Boolean
  topics: [String]
  visibility: String
  forks: Int
  open_issues: Int
  watchers: Int
  default_branch: String
  public: Boolean
}

"""
The policy controlling who can create pull requests: all or collaborators_only.
"""
enum query_activity_list_public_events_items_payload_oneOf_5_forkee_pull_request_creation_policy {
  all
  collaborators_only
}

type GollumEvent {
  pages: [query_activity_list_public_events_items_payload_oneOf_6_pages_items]!
}

type query_activity_list_public_events_items_payload_oneOf_6_pages_items {
  page_name: String
  title: String
  summary: String
  action: String
  sha: String
  html_url: String
}

type MemberEvent {
  action: String!
  member: simple_user!
}

type JSON_container {
  JSON: JSON
}

type PushEvent {
  repository_id: Int!
  push_id: Int!
  ref: String!
  head: String!
  before: String!
}

type PullRequestEvent {
  action: String!
  number: Int!
  pull_request: Pull_Request_Minimal!
  assignee: simple_user
  assignees: [simple_user]
  label: label
  labels: [label]
}

type Pull_Request_Minimal {
  id: BigInt!
  number: Int!
  url: String!
  head: query_activity_list_public_events_items_payload_oneOf_10_pull_request_head!
  base: query_activity_list_public_events_items_payload_oneOf_10_pull_request_base!
}

type query_activity_list_public_events_items_payload_oneOf_10_pull_request_head {
  ref: String!
  sha: String!
  repo: query_activity_list_public_events_items_payload_oneOf_10_pull_request_head_repo!
}

type query_activity_list_public_events_items_payload_oneOf_10_pull_request_head_repo {
  id: BigInt!
  url: String!
  name: String!
}

type query_activity_list_public_events_items_payload_oneOf_10_pull_request_base {
  ref: String!
  sha: String!
  repo: query_activity_list_public_events_items_payload_oneOf_10_pull_request_base_repo!
}

type query_activity_list_public_events_items_payload_oneOf_10_pull_request_base_repo {
  id: BigInt!
  url: String!
  name: String!
}

type PullRequestReviewCommentEvent {
  action: String!
  pull_request: Pull_Request_Minimal!
  comment: query_activity_list_public_events_items_payload_oneOf_11_comment!
}

type query_activity_list_public_events_items_payload_oneOf_11_comment {
  id: Int!
  node_id: String!
  url: URL!
  pull_request_review_id: Int
  diff_hunk: String!
  path: String!
  position: Int
  original_position: Int!
  subject_type: String
  commit_id: String!
  user: query_activity_list_public_events_items_payload_oneOf_11_comment_user
  body: String!
  created_at: DateTime!
  updated_at: DateTime!
  html_url: URL!
  pull_request_url: URL!
  _links: query_activity_list_public_events_items_payload_oneOf_11_comment__links!
  original_commit_id: String!
  reactions: query_activity_list_public_events_items_payload_oneOf_11_comment_reactions!
  in_reply_to_id: Int
}

type query_activity_list_public_events_items_payload_oneOf_11_comment_user {
  avatar_url: URL
  deleted: Boolean
  email: String
  events_url: UriTemplate
  followers_url: URL
  following_url: UriTemplate
  gists_url: UriTemplate
  gravatar_id: String
  html_url: URL
  id: BigInt
  login: String
  name: String
  node_id: String
  organizations_url: URL
  received_events_url: URL
  repos_url: URL
  site_admin: Boolean
  starred_url: UriTemplate
  subscriptions_url: URL
  type: query_activity_list_public_events_items_payload_oneOf_11_comment_user_type
  url: URL
  user_view_type: String
}

enum query_activity_list_public_events_items_payload_oneOf_11_comment_user_type {
  Bot
  User
  Organization
}

type query_activity_list_public_events_items_payload_oneOf_11_comment__links {
  html: Link!
  pull_request: Link!
  self: Link!
}

type Link {
  href: UriTemplate!
}

type query_activity_list_public_events_items_payload_oneOf_11_comment_reactions {
  _0PLUS_1: Int
  NEGATIVE_1: Int
  confused: Int
  eyes: Int
  heart: Int
  hooray: Int
  laugh: Int
  rocket: Int
  total_count: Int
  url: URL
}

type PullRequestReviewEvent {
  action: String!
  review: query_activity_list_public_events_items_payload_oneOf_12_review!
  pull_request: Pull_Request_Minimal!
}

type query_activity_list_public_events_items_payload_oneOf_12_review {
  id: Int
  node_id: String
  user: nullable_simple_user
  body: String
  commit_id: String
  submitted_at: String
  state: String
  html_url: URL
  pull_request_url: URL
  _links: query_activity_list_public_events_items_payload_oneOf_12_review__links
  updated_at: String
}

type query_activity_list_public_events_items_payload_oneOf_12_review__links {
  html: query_activity_list_public_events_items_payload_oneOf_12_review__links_html!
  pull_request: query_activity_list_public_events_items_payload_oneOf_12_review__links_pull_request!
}

type query_activity_list_public_events_items_payload_oneOf_12_review__links_html {
  href: String!
}

type query_activity_list_public_events_items_payload_oneOf_12_review__links_pull_request {
  href: String!
}

type CommitCommentEvent {
  action: String!
  comment: query_activity_list_public_events_items_payload_oneOf_13_comment!
}

type query_activity_list_public_events_items_payload_oneOf_13_comment {
  html_url: URL
  url: URL
  id: Int
  node_id: String
  body: String
  path: String
  position: Int
  line: Int
  commit_id: String
  user: nullable_simple_user
  created_at: DateTime
  updated_at: DateTime
  reactions: Reaction_Rollup
}

type ReleaseEvent {
  action: String!
  release: query_activity_list_public_events_items_payload_oneOf_14_release!
}

type query_activity_list_public_events_items_payload_oneOf_14_release {
  url: URL!
  html_url: URL!
  assets_url: URL!
  upload_url: String!
  tarball_url: URL
  zipball_url: URL
  id: Int!
  node_id: String!

  """The name of the tag."""
  tag_name: String!

  """
  Specifies the commitish value that determines where the Git tag is created from.
  """
  target_commitish: String!
  name: String
  body: String

  """
  true to create a draft (unpublished) release, false to create a published one.
  """
  draft: Boolean!

  """Whether to identify the release as a prerelease or a full release."""
  prerelease: Boolean!

  """Whether or not the release is immutable."""
  immutable: Boolean
  created_at: DateTime!
  published_at: DateTime
  updated_at: DateTime
  author: simple_user!
  assets: [Release_Asset]!
  body_html: String
  body_text: String
  mentions_count: Int

  """The URL of the release discussion."""
  discussion_url: URL
  reactions: Reaction_Rollup
  is_short_description_html_truncated: Boolean
  short_description_html: String
}

"""Data related to a release."""
type Release_Asset {
  url: URL!
  browser_download_url: URL!
  id: Int!
  node_id: String!

  """The file name of the asset."""
  name: String!
  label: String
  state: query_activity_list_public_events_items_payload_oneOf_14_release_allOf_0_assets_items_state!
  content_type: String!
  size: Int!
  digest: String
  download_count: Int!
  created_at: DateTime!
  updated_at: DateTime!
  uploader: nullable_simple_user
}

"""State of the release asset."""
enum query_activity_list_public_events_items_payload_oneOf_14_release_allOf_0_assets_items_state {
  uploaded
  open
}

type WatchEvent {
  action: String!
}

"""Feed"""
type Feed {
  timeline_url: URL!
  user_url: URL!
  current_user_public_url: URL
  current_user_url: URL
  current_user_actor_url: URL
  current_user_organization_url: URL
  current_user_organization_urls: [URL]
  security_advisories_url: URL

  """A feed of discussions for a given repository."""
  repository_discussions_url: URL

  """A feed of discussions for a given repository and category."""
  repository_discussions_category_url: URL
  _links: query_activity_get_feeds__links!
}

type query_activity_get_feeds__links {
  timeline: Link_With_Type!
  user: Link_With_Type!
  security_advisories: Link_With_Type
  current_user: Link_With_Type
  current_user_public: Link_With_Type
  current_user_actor: Link_With_Type
  current_user_organization: Link_With_Type
  current_user_organizations: [Link_With_Type]
  repository_discussions: Link_With_Type
  repository_discussions_category: Link_With_Type
}

"""Hypermedia Link with Type"""
type Link_With_Type {
  href: String!
  type: String!
}

"""Base Gist"""
type Base_Gist {
  url: URL!
  forks_url: URL!
  commits_url: URL!
  id: String!
  node_id: String!
  git_pull_url: URL!
  git_push_url: URL!
  html_url: URL!
  files: query_gists_list_items_files!
  public: Boolean!
  created_at: DateTime!
  updated_at: DateTime!
  description: String
  comments: Int!
  comments_enabled: Boolean
  user: nullable_simple_user
  comments_url: URL!
  owner: simple_user
  truncated: Boolean
  forks: [JSON]
  history: [JSON]
}

type query_gists_list_items_files {
  additionalProperties: [query_gists_list_items_files_additionalProperties_entry]
}

type query_gists_list_items_files_additionalProperties_entry {
  key: ID!
  value: query_gists_list_items_files_additionalProperties
}

type query_gists_list_items_files_additionalProperties {
  filename: String
  type: String
  language: String
  raw_url: String
  size: Int

  """
  The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported.
  """
  encoding: String
}

"""Gist Simple"""
type Gist_Simple {
  forks: [query_gists_get_forks_items] @deprecated(reason: "deprecated")
  history: [Gist_History] @deprecated(reason: "deprecated")
  fork_of: Gist
  url: String
  forks_url: String
  commits_url: String
  id: String
  node_id: String
  git_pull_url: String
  git_push_url: String
  html_url: String
  files: query_gists_get_files
  public: Boolean
  created_at: String
  updated_at: String
  description: String
  comments: Int
  comments_enabled: Boolean
  user: String
  comments_url: String
  owner: simple_user
  truncated: Boolean
}

type query_gists_get_forks_items {
  id: String
  url: URL
  user: Public_User
  created_at: DateTime
  updated_at: DateTime
}

"""Public User"""
type Public_User {
  login: String!
  id: BigInt!
  user_view_type: String
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: String!
  gists_url: String!
  starred_url: String!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: String!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  name: String
  company: String
  blog: String
  location: String
  email: EmailAddress
  notification_email: EmailAddress
  hireable: Boolean
  bio: String
  twitter_username: String
  public_repos: Int!
  public_gists: Int!
  followers: Int!
  following: Int!
  created_at: DateTime!
  updated_at: DateTime!
  plan: query_gists_get_forks_items_user_plan
  private_gists: Int
  total_private_repos: Int
  owned_private_repos: Int
  disk_usage: Int
  collaborators: Int
}

"""
A field whose value conforms to the standard internet email address format as specified in HTML Spec: https://html.spec.whatwg.org/multipage/input.html#valid-e-mail-address.
"""
scalar EmailAddress @specifiedBy(url: "https://html.spec.whatwg.org/multipage/input.html#valid-e-mail-address")

type query_gists_get_forks_items_user_plan {
  collaborators: Int!
  name: String!
  space: Int!
  private_repos: Int!
}

"""Gist History"""
type Gist_History {
  user: nullable_simple_user
  version: String
  committed_at: DateTime
  change_status: query_gists_get_history_items_change_status
  url: URL
}

type query_gists_get_history_items_change_status {
  total: Int
  additions: Int
  deletions: Int
}

"""Gist"""
type Gist {
  url: URL!
  forks_url: URL!
  commits_url: URL!
  id: String!
  node_id: String!
  git_pull_url: URL!
  git_push_url: URL!
  html_url: URL!
  files: query_gists_get_fork_of_files!
  public: Boolean!
  created_at: DateTime!
  updated_at: DateTime!
  description: String
  comments: Int!
  comments_enabled: Boolean
  user: nullable_simple_user
  comments_url: URL!
  owner: nullable_simple_user
  truncated: Boolean
  forks: [JSON]
  history: [JSON]
}

type query_gists_get_fork_of_files {
  additionalProperties: [query_gists_get_fork_of_files_additionalProperties_entry]
}

type query_gists_get_fork_of_files_additionalProperties_entry {
  key: ID!
  value: query_gists_get_fork_of_files_additionalProperties
}

type query_gists_get_fork_of_files_additionalProperties {
  filename: String
  type: String
  language: String
  raw_url: String
  size: Int
}

type query_gists_get_files {
  additionalProperties: [query_gists_get_files_additionalProperties_entry]
}

type query_gists_get_files_additionalProperties_entry {
  key: ID!
  value: query_gists_get_files_additionalProperties
}

type query_gists_get_files_additionalProperties {
  filename: String
  type: String
  language: String
  raw_url: String
  size: Int
  truncated: Boolean
  content: String

  """
  The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported.
  """
  encoding: String
}

"""A comment made to a gist."""
type Gist_Comment {
  id: Int!
  node_id: String!
  url: URL!

  """The comment text."""
  body: query_gists_list_comments_items_body!
  user: nullable_simple_user
  created_at: DateTime!
  updated_at: DateTime!
  author_association: author_association!
}

"""The comment text."""
scalar query_gists_list_comments_items_body

"""Gist Commit"""
type Gist_Commit {
  url: URL!
  version: String!
  user: nullable_simple_user
  change_status: query_gists_list_commits_items_change_status!
  committed_at: DateTime!
}

type query_gists_list_commits_items_change_status {
  total: Int
  additions: Int
  deletions: Int
}

"""Represents empty values"""
scalar Void

"""Gitignore Template"""
type Gitignore_Template {
  name: String!
  source: String!
}

type apps_list_repos_accessible_to_installation_200_response {
  total_count: Int!
  repositories: [repository]!
  repository_selection: String
}

"""
Indicates which sorts of issues to return. `assigned` means issues assigned to you. `created` means issues created by you. `mentioned` means issues mentioning you. `subscribed` means issues you're subscribed to updates for. `all` or `repos` means all issues you can see, regardless of participation or creation.
"""
enum queryInput_issues_list_filter {
  assigned
  created
  mentioned
  subscribed
  repos
  all
}

"""Indicates the state of the issues to return."""
enum queryInput_issues_list_state {
  open
  closed
  all
}

"""What to sort results by."""
enum queryInput_issues_list_sort {
  created
  updated
  comments
}

"""License Simple"""
type license_simple {
  key: String!
  name: String!
  url: URL
  spdx_id: String
  node_id: String!
  html_url: URL
}

"""License"""
type License {
  key: String!
  name: String!
  spdx_id: String
  url: URL
  node_id: String!
  html_url: URL!
  description: String!
  implementation: String!
  permissions: [String]!
  conditions: [String]!
  limitations: [String]!
  body: String!
  featured: Boolean!
}

"""Marketplace Purchase"""
type marketplace_purchase {
  url: String!
  type: String!
  id: Int!
  login: String!
  organization_billing_email: String
  email: String
  marketplace_pending_change: query_apps_get_subscription_plan_for_account_marketplace_pending_change
  marketplace_purchase: query_apps_get_subscription_plan_for_account_marketplace_purchase!
}

type query_apps_get_subscription_plan_for_account_marketplace_pending_change {
  is_installed: Boolean
  effective_date: String
  unit_count: Int
  id: Int
  plan: Marketplace_Listing_Plan
}

"""Marketplace Listing Plan"""
type Marketplace_Listing_Plan {
  url: URL!
  accounts_url: URL!
  id: Int!
  number: Int!
  name: String!
  description: String!
  monthly_price_in_cents: Int!
  yearly_price_in_cents: Int!
  price_model: query_apps_get_subscription_plan_for_account_marketplace_pending_change_plan_price_model!
  has_free_trial: Boolean!
  unit_name: String
  state: String!
  bullets: [String]!
}

enum query_apps_get_subscription_plan_for_account_marketplace_pending_change_plan_price_model {
  FREE
  FLAT_RATE
  PER_UNIT
}

type query_apps_get_subscription_plan_for_account_marketplace_purchase {
  billing_cycle: String
  next_billing_date: String
  is_installed: Boolean
  unit_count: Int
  on_free_trial: Boolean
  free_trial_ends_on: String
  updated_at: String
  plan: Marketplace_Listing_Plan
}

"""The property to sort the results by."""
enum sort_parameter {
  created
  updated
}

"""
To return the oldest accounts first, set to `asc`. Ignored without the `sort` parameter.
"""
enum queryInput_apps_list_accounts_for_plan_direction {
  asc
  desc
}

"""
To return the oldest accounts first, set to `asc`. Ignored without the `sort` parameter.
"""
enum queryInput_apps_list_accounts_for_plan_stubbed_direction {
  asc
  desc
}

"""Api Overview"""
type Api_Overview {
  verifiable_password_authentication: Boolean!
  ssh_key_fingerprints: query_meta_get_ssh_key_fingerprints
  ssh_keys: [String]
  hooks: [String]
  github_enterprise_importer: [String]
  web: [String]
  api: [String]
  git: [String]
  packages: [String]
  pages: [String]
  importer: [String]
  actions: [String]
  actions_macos: [String]
  codespaces: [String]
  dependabot: [String]
  copilot: [String]
  domains: query_meta_get_domains
}

type query_meta_get_ssh_key_fingerprints {
  SHA256_RSA: String
  SHA256_DSA: String
  SHA256_ECDSA: String
  SHA256_ED25519: String
}

type query_meta_get_domains {
  website: [String]
  codespaces: [String]
  copilot: [String]
  packages: [String]
  actions: [String]
  actions_inbound: query_meta_get_domains_actions_inbound
  artifact_attestations: query_meta_get_domains_artifact_attestations
}

type query_meta_get_domains_actions_inbound {
  full_domains: [String]
  wildcard_domains: [String]
}

type query_meta_get_domains_artifact_attestations {
  trust_domain: String
  services: [String]
}

"""Thread"""
type Thread {
  id: String!
  repository: minimal_repository!
  subject: query_activity_list_notifications_for_authenticated_user_items_subject!
  reason: String!
  unread: Boolean!
  updated_at: String!
  last_read_at: String
  url: String!
  subscription_url: URL!
}

"""Minimal Repository"""
type minimal_repository {
  id: BigInt!
  node_id: String!
  name: String!
  full_name: String!
  owner: simple_user!
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  archive_url: URL!
  assignees_url: URL!
  blobs_url: URL!
  branches_url: URL!
  collaborators_url: URL!
  comments_url: URL!
  commits_url: URL!
  compare_url: URL!
  contents_url: URL!
  contributors_url: URL!
  deployments_url: URL!
  downloads_url: URL!
  events_url: URL!
  forks_url: URL!
  git_commits_url: URL!
  git_refs_url: URL!
  git_tags_url: URL!
  git_url: String
  issue_comment_url: URL!
  issue_events_url: URL!
  issues_url: URL!
  keys_url: URL!
  labels_url: URL!
  languages_url: URL!
  merges_url: URL!
  milestones_url: URL!
  notifications_url: URL!
  pulls_url: URL!
  releases_url: URL!
  ssh_url: String
  stargazers_url: URL!
  statuses_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  tags_url: URL!
  teams_url: URL!
  trees_url: URL!
  clone_url: String
  mirror_url: String
  hooks_url: URL!
  svn_url: String
  homepage: String
  language: String
  forks_count: Int
  stargazers_count: Int
  watchers_count: Int

  """
  The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0.
  """
  size: Int
  default_branch: String
  open_issues_count: Int
  is_template: Boolean
  topics: [String]
  has_issues: Boolean
  has_projects: Boolean
  has_wiki: Boolean
  has_pages: Boolean
  has_downloads: Boolean
  has_discussions: Boolean
  has_pull_requests: Boolean
  pull_request_creation_policy: query_activity_list_notifications_for_authenticated_user_items_repository_pull_request_creation_policy
  has_commit_comments: Boolean
  archived: Boolean
  disabled: Boolean
  visibility: String
  pushed_at: DateTime
  created_at: DateTime
  updated_at: DateTime
  permissions: query_activity_list_notifications_for_authenticated_user_items_repository_permissions
  role_name: String
  temp_clone_token: String
  delete_branch_on_merge: Boolean
  subscribers_count: Int
  network_count: Int
  code_of_conduct: Code_Of_Conduct
  license: query_activity_list_notifications_for_authenticated_user_items_repository_license
  forks: Int
  open_issues: Int
  watchers: Int
  allow_forking: Boolean
  web_commit_signoff_required: Boolean
  security_and_analysis: security_and_analysis

  """
  The custom properties that were defined for the repository. The keys are the custom property names, and the values are the corresponding custom property values.
  """
  custom_properties: JSON
}

"""
The policy controlling who can create pull requests: all or collaborators_only.
"""
enum query_activity_list_notifications_for_authenticated_user_items_repository_pull_request_creation_policy {
  all
  collaborators_only
}

type query_activity_list_notifications_for_authenticated_user_items_repository_permissions {
  admin: Boolean
  maintain: Boolean
  push: Boolean
  triage: Boolean
  pull: Boolean
}

type query_activity_list_notifications_for_authenticated_user_items_repository_license {
  key: String
  name: String
  spdx_id: String
  url: String
  node_id: String
}

type security_and_analysis {
  advanced_security: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_advanced_security
  code_security: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_code_security
  dependabot_security_updates: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_dependabot_security_updates
  secret_scanning: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning
  secret_scanning_push_protection: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_push_protection
  secret_scanning_non_provider_patterns: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_non_provider_patterns
  secret_scanning_ai_detection: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_ai_detection
  secret_scanning_delegated_alert_dismissal: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_alert_dismissal
  secret_scanning_delegated_bypass: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass
  secret_scanning_delegated_bypass_options: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_options
}

"""
Enable or disable GitHub Advanced Security for the repository.

For standalone Code Scanning or Secret Protection products, this parameter cannot be used.
"""
type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_advanced_security {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_advanced_security_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_advanced_security_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_code_security {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_code_security_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_code_security_status {
  enabled
  disabled
}

"""Enable or disable Dependabot security updates for the repository."""
type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_dependabot_security_updates {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_dependabot_security_updates_status
}

"""
The enablement status of Dependabot security updates for the repository.
"""
enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_dependabot_security_updates_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_push_protection {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_push_protection_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_push_protection_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_non_provider_patterns {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_non_provider_patterns_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_non_provider_patterns_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_ai_detection {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_ai_detection_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_ai_detection_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_alert_dismissal {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_alert_dismissal_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_alert_dismissal_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass {
  status: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_status
}

enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_status {
  enabled
  disabled
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_options {
  """The bypass reviewers for secret scanning delegated bypass"""
  reviewers: [query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items]
}

type query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items {
  """The ID of the team or role selected as a bypass reviewer"""
  reviewer_id: Int!
  reviewer_type: query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type!
}

"""The type of the bypass reviewer"""
enum query_activity_list_notifications_for_authenticated_user_items_repository_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type {
  TEAM
  ROLE
}

type query_activity_list_notifications_for_authenticated_user_items_subject {
  title: String!
  url: String!
  latest_comment_url: String!
  type: String!
}

"""Thread Subscription"""
type Thread_Subscription {
  subscribed: Boolean!
  ignored: Boolean!
  reason: String
  created_at: DateTime
  url: URL!
  thread_url: URL
  repository_url: URL
}

"""GitHub Actions cache retention policy for an organization."""
type Actions_cache_retention_limit_for_an_organization {
  """
  For repositories in this organization, the maximum duration, in days, for which caches in a repository may be retained.
  """
  max_cache_retention_days: Int
}

"""GitHub Actions cache storage policy for an organization."""
type Actions_cache_storage_limit_for_an_organization {
  """
  For repositories in the organization, the maximum size limit for the sum of all caches in a repository, in gigabytes.
  """
  max_cache_size_gb: Int
}

"""
Information about repositories that Dependabot is able to access in an organization
"""
type Dependabot_Repository_Access_Details {
  default_level: query_dependabot_repository_access_for_org_default_level
  accessible_repositories: [nullable_simple_repository]
}

"""The default repository access level for Dependabot updates."""
enum query_dependabot_repository_access_for_org_default_level {
  public
  internal
}

"""A GitHub repository."""
type nullable_simple_repository {
  """A unique identifier of the repository."""
  id: BigInt!

  """The GraphQL identifier of the repository."""
  node_id: String!

  """The name of the repository."""
  name: String!

  """The full, globally unique, name of the repository."""
  full_name: String!
  owner: simple_user!

  """Whether the repository is private."""
  private: Boolean!

  """The URL to view the repository on GitHub.com."""
  html_url: URL!

  """The repository description."""
  description: String

  """Whether the repository is a fork."""
  fork: Boolean!

  """
  The URL to get more information about the repository from the GitHub API.
  """
  url: URL!

  """A template for the API URL to download the repository as an archive."""
  archive_url: URL!

  """
  A template for the API URL to list the available assignees for issues in the repository.
  """
  assignees_url: URL!

  """
  A template for the API URL to create or retrieve a raw Git blob in the repository.
  """
  blobs_url: URL!

  """
  A template for the API URL to get information about branches in the repository.
  """
  branches_url: URL!

  """
  A template for the API URL to get information about collaborators of the repository.
  """
  collaborators_url: URL!

  """
  A template for the API URL to get information about comments on the repository.
  """
  comments_url: URL!

  """
  A template for the API URL to get information about commits on the repository.
  """
  commits_url: URL!

  """A template for the API URL to compare two commits or refs."""
  compare_url: URL!

  """A template for the API URL to get the contents of the repository."""
  contents_url: URL!

  """A template for the API URL to list the contributors to the repository."""
  contributors_url: URL!

  """The API URL to list the deployments of the repository."""
  deployments_url: URL!

  """The API URL to list the downloads on the repository."""
  downloads_url: URL!

  """The API URL to list the events of the repository."""
  events_url: URL!

  """The API URL to list the forks of the repository."""
  forks_url: URL!

  """
  A template for the API URL to get information about Git commits of the repository.
  """
  git_commits_url: URL!

  """
  A template for the API URL to get information about Git refs of the repository.
  """
  git_refs_url: URL!

  """
  A template for the API URL to get information about Git tags of the repository.
  """
  git_tags_url: URL!

  """
  A template for the API URL to get information about issue comments on the repository.
  """
  issue_comment_url: URL!

  """
  A template for the API URL to get information about issue events on the repository.
  """
  issue_events_url: URL!

  """
  A template for the API URL to get information about issues on the repository.
  """
  issues_url: URL!

  """
  A template for the API URL to get information about deploy keys on the repository.
  """
  keys_url: URL!

  """
  A template for the API URL to get information about labels of the repository.
  """
  labels_url: URL!

  """The API URL to get information about the languages of the repository."""
  languages_url: URL!

  """The API URL to merge branches in the repository."""
  merges_url: URL!

  """
  A template for the API URL to get information about milestones of the repository.
  """
  milestones_url: URL!

  """
  A template for the API URL to get information about notifications on the repository.
  """
  notifications_url: URL!

  """
  A template for the API URL to get information about pull requests on the repository.
  """
  pulls_url: URL!

  """
  A template for the API URL to get information about releases on the repository.
  """
  releases_url: URL!

  """The API URL to list the stargazers on the repository."""
  stargazers_url: URL!

  """
  A template for the API URL to get information about statuses of a commit.
  """
  statuses_url: URL!

  """The API URL to list the subscribers on the repository."""
  subscribers_url: URL!

  """The API URL to subscribe to notifications for this repository."""
  subscription_url: URL!

  """The API URL to get information about tags on the repository."""
  tags_url: URL!

  """The API URL to list the teams on the repository."""
  teams_url: URL!

  """
  A template for the API URL to create or retrieve a raw Git tree of the repository.
  """
  trees_url: URL!

  """The API URL to list the hooks on the repository."""
  hooks_url: URL!
}

"""Response when getting all budgets"""
type get_all_budgets {
  """Array of budget objects for the enterprise"""
  budgets: [budget]!

  """
  Indicates if there are more pages of results available (maps to hasNextPage from billing platform)
  """
  has_next_page: Boolean

  """Total number of budgets matching the query"""
  total_count: Int
}

type budget {
  """The unique identifier for the budget"""
  id: String!

  """The type of pricing for the budget"""
  budget_type: String!

  """
  The budget amount limit in whole dollars. For license-based products, this represents the number of licenses.
  """
  budget_amount: Int!

  """The type of limit enforcement for the budget"""
  prevent_further_usage: Boolean!

  """
  The scope of the budget (enterprise, organization, repository, cost center)
  """
  budget_scope: String!

  """
  The name of the entity for the budget (enterprise does not require a name).
  """
  budget_entity_name: String

  """A single product or sku to apply the budget to."""
  budget_product_sku: String!
  budget_alerting: query_billing_get_all_budgets_org_budgets_items_budget_alerting!
}

type query_billing_get_all_budgets_org_budgets_items_budget_alerting {
  """Whether alerts are enabled for this budget"""
  will_alert: Boolean!

  """Array of user login names who will receive alerts"""
  alert_recipients: [String]!
}

"""Filter budgets by scope type."""
enum queryInput_billing_get_all_budgets_org_scope {
  enterprise
  organization
  repository
  cost_center
}

"""Response when updating a budget"""
type get_budget {
  """ID of the budget."""
  id: String!
  budget_scope: query_billing_get_budget_org_budget_scope!

  """The name of the entity to apply the budget to"""
  budget_entity_name: String!

  """
  The budget amount in whole dollars. For license-based products, this represents the number of licenses.
  """
  budget_amount: Int!

  """Whether to prevent additional spending once the budget is exceeded"""
  prevent_further_usage: Boolean!

  """A single product or sku to apply the budget to."""
  budget_product_sku: String!

  """The type of pricing for the budget"""
  budget_type: String!
  budget_alerting: query_billing_get_budget_org_budget_alerting!
}

"""The type of scope for the budget"""
enum query_billing_get_budget_org_budget_scope {
  enterprise
  organization
  repository
  cost_center
}

type query_billing_get_budget_org_budget_alerting {
  """Whether alerts are enabled for this budget"""
  will_alert: Boolean

  """Array of user login names who will receive alerts"""
  alert_recipients: [String]
}

"""Response when getting a billing premium request usage report"""
type billing_premium_request_usage_report_org {
  timePeriod: query_billing_get_github_billing_premium_request_usage_report_org_timePeriod!

  """The unique identifier of the organization."""
  organization: String!

  """The name of the user for the usage report."""
  user: String

  """The product for the usage report."""
  product: String

  """The model for the usage report."""
  model: String
  usageItems: [query_billing_get_github_billing_premium_request_usage_report_org_usageItems_items]!
}

type query_billing_get_github_billing_premium_request_usage_report_org_timePeriod {
  """The year for the usage report."""
  year: Int!

  """The month for the usage report."""
  month: Int

  """The day for the usage report."""
  day: Int
}

type query_billing_get_github_billing_premium_request_usage_report_org_usageItems_items {
  """Product name."""
  product: String!

  """SKU name."""
  sku: String!

  """Model name."""
  model: String!

  """Unit type of the usage line item."""
  unitType: String!

  """Price per unit of the usage line item."""
  pricePerUnit: Float!

  """Gross quantity of the usage line item."""
  grossQuantity: Float!

  """Gross amount of the usage line item."""
  grossAmount: Float!

  """Discount quantity of the usage line item."""
  discountQuantity: Float!

  """Discount amount of the usage line item."""
  discountAmount: Float!

  """Net quantity of the usage line item."""
  netQuantity: Float!

  """Net amount of the usage line item."""
  netAmount: Float!
}

"""Billing usage report response for an organization"""
type billing_usage_report {
  usageItems: [query_billing_get_github_billing_usage_report_org_usageItems_items]
}

type query_billing_get_github_billing_usage_report_org_usageItems_items {
  """Date of the usage line item."""
  date: String!

  """Product name."""
  product: String!

  """SKU name."""
  sku: String!

  """Quantity of the usage line item."""
  quantity: Int!

  """Unit type of the usage line item."""
  unitType: String!

  """Price per unit of the usage line item."""
  pricePerUnit: Float!

  """Gross amount of the usage line item."""
  grossAmount: Float!

  """Discount amount of the usage line item."""
  discountAmount: Float!

  """Net amount of the usage line item."""
  netAmount: Float!

  """Name of the organization."""
  organizationName: String!

  """Name of the repository."""
  repositoryName: String
}

"""Response when getting a billing usage summary"""
type billing_usage_summary_report_org {
  timePeriod: query_billing_get_github_billing_usage_summary_report_org_timePeriod!

  """The unique identifier of the organization."""
  organization: String!

  """The name of the repository for the usage report."""
  repository: String

  """The product for the usage report."""
  product: String

  """The SKU for the usage report."""
  sku: String
  usageItems: [query_billing_get_github_billing_usage_summary_report_org_usageItems_items]!
}

type query_billing_get_github_billing_usage_summary_report_org_timePeriod {
  """The year for the usage report."""
  year: Int!

  """The month for the usage report."""
  month: Int

  """The day for the usage report."""
  day: Int
}

type query_billing_get_github_billing_usage_summary_report_org_usageItems_items {
  """Product name."""
  product: String!

  """SKU name."""
  sku: String!

  """Unit type of the usage line item."""
  unitType: String!

  """Price per unit of the usage line item."""
  pricePerUnit: Float!

  """Gross quantity of the usage line item."""
  grossQuantity: Float!

  """Gross amount of the usage line item."""
  grossAmount: Float!

  """Discount quantity of the usage line item."""
  discountQuantity: Float!

  """Discount amount of the usage line item."""
  discountAmount: Float!

  """Net quantity of the usage line item."""
  netQuantity: Float!

  """Net amount of the usage line item."""
  netAmount: Float!
}

"""Organization Full"""
type Organization_Full {
  login: String!
  id: Int!
  node_id: String!
  url: URL!
  repos_url: URL!
  events_url: URL!
  hooks_url: URL!
  issues_url: URL!
  members_url: URL!
  public_members_url: URL!
  avatar_url: URL!
  description: String
  name: String
  company: String
  blog: URL
  location: String
  email: EmailAddress
  twitter_username: String
  is_verified: Boolean
  has_organization_projects: Boolean!
  has_repository_projects: Boolean!
  public_repos: Int!
  public_gists: Int!
  followers: Int!
  following: Int!
  html_url: URL!
  type: String!
  total_private_repos: Int
  owned_private_repos: Int
  private_gists: Int
  disk_usage: Int

  """
  The number of collaborators on private repositories.
  
  This field may be null if the number of private repositories is over 50,000.
  """
  collaborators: Int
  billing_email: EmailAddress
  plan: query_orgs_get_plan
  default_repository_permission: String

  """The default branch for repositories created in this organization."""
  default_repository_branch: String
  members_can_create_repositories: Boolean
  two_factor_requirement_enabled: Boolean
  members_allowed_repository_creation_type: String
  members_can_create_public_repositories: Boolean
  members_can_create_private_repositories: Boolean
  members_can_create_internal_repositories: Boolean
  members_can_create_pages: Boolean
  members_can_create_public_pages: Boolean
  members_can_create_private_pages: Boolean
  members_can_delete_repositories: Boolean
  members_can_change_repo_visibility: Boolean
  members_can_invite_outside_collaborators: Boolean
  members_can_delete_issues: Boolean
  display_commenter_full_name_setting_enabled: Boolean
  readers_can_create_discussions: Boolean
  members_can_create_teams: Boolean
  members_can_view_dependency_insights: Boolean
  members_can_fork_private_repositories: Boolean
  web_commit_signoff_required: Boolean

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether GitHub Advanced Security is enabled for new repositories and repositories transferred to this organization.
  
  This field is only visible to organization owners or members of a team with the security manager role.
  """
  advanced_security_enabled_for_new_repositories: Boolean @deprecated(reason: "deprecated")

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether Dependabot alerts are automatically enabled for new repositories and repositories transferred to this organization.
  
  This field is only visible to organization owners or members of a team with the security manager role.
  """
  dependabot_alerts_enabled_for_new_repositories: Boolean @deprecated(reason: "deprecated")

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether Dependabot security updates are automatically enabled for new repositories and repositories transferred to this organization.
  
  This field is only visible to organization owners or members of a team with the security manager role.
  """
  dependabot_security_updates_enabled_for_new_repositories: Boolean @deprecated(reason: "deprecated")

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether dependency graph is automatically enabled for new repositories and repositories transferred to this organization.
  
  This field is only visible to organization owners or members of a team with the security manager role.
  """
  dependency_graph_enabled_for_new_repositories: Boolean @deprecated(reason: "deprecated")

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether secret scanning is automatically enabled for new repositories and repositories transferred to this organization.
  
  This field is only visible to organization owners or members of a team with the security manager role.
  """
  secret_scanning_enabled_for_new_repositories: Boolean @deprecated(reason: "deprecated")

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether secret scanning push protection is automatically enabled for new repositories and repositories transferred to this organization.
  
  This field is only visible to organization owners or members of a team with the security manager role.
  """
  secret_scanning_push_protection_enabled_for_new_repositories: Boolean @deprecated(reason: "deprecated")

  """
  Whether a custom link is shown to contributors who are blocked from pushing a secret by push protection.
  """
  secret_scanning_push_protection_custom_link_enabled: Boolean

  """
  An optional URL string to display to contributors who are blocked from pushing a secret.
  """
  secret_scanning_push_protection_custom_link: URL
  created_at: DateTime!
  updated_at: DateTime!
  archived_at: DateTime

  """
  Controls whether or not deploy keys may be added and used for repositories in the organization.
  """
  deploy_keys_enabled_for_repositories: Boolean
}

type query_orgs_get_plan {
  name: String!
  space: Int!
  private_repos: Int!
  filled_seats: Int
  seats: Int
}

type actions_cache_usage_org_enterprise {
  """
  The count of active caches across all repositories of an enterprise or an organization.
  """
  total_active_caches_count: Int!

  """
  The total size in bytes of all active cache items across all repositories of an enterprise or an organization.
  """
  total_active_caches_size_in_bytes: Int!
}

type actions_get_actions_cache_usage_by_repo_for_org_200_response {
  total_count: Int!
  repository_cache_usages: [Actions_Cache_Usage_by_repository]!
}

"""GitHub Actions Cache Usage by repository."""
type Actions_Cache_Usage_by_repository {
  """The repository owner and name for the cache usage being shown."""
  full_name: String!

  """
  The sum of the size in bytes of all the active cache items in the repository.
  """
  active_caches_size_in_bytes: Int!

  """The number of active caches in the repository."""
  active_caches_count: Int!
}

type actions_list_hosted_runners_for_org_200_response {
  total_count: Int!
  runners: [GitHub_hosted_hosted_runner]!
}

"""A Github-hosted hosted runner."""
type GitHub_hosted_hosted_runner {
  """The unique identifier of the hosted runner."""
  id: Int!

  """The name of the hosted runner."""
  name: String!

  """The unique identifier of the group that the hosted runner belongs to."""
  runner_group_id: Int
  image_details: nullable_actions_hosted_runner_pool_image
  machine_size_details: Github_owned_VM_details_!
  status: query_actions_list_hosted_runners_for_org_runners_items_status!

  """The operating system of the image."""
  platform: String!

  """
  The maximum amount of hosted runners. Runners will not scale automatically above this number. Use this setting to limit your cost.
  """
  maximum_runners: Int

  """Whether public IP is enabled for the hosted runners."""
  public_ip_enabled: Boolean!

  """The public IP ranges when public IP is enabled for the hosted runners."""
  public_ips: [Public_IP_for_a_GitHub_hosted_larger_runners_]

  """The time at which the runner was last used, in ISO 8601 format."""
  last_active_on: DateTime

  """Whether custom image generation is enabled for the hosted runners."""
  image_gen: Boolean
}

"""Provides details of a hosted runner image"""
type nullable_actions_hosted_runner_pool_image {
  """
  The ID of the image. Use this ID for the `image` parameter when creating a new larger runner.
  """
  id: String!

  """Image size in GB."""
  size_gb: Int!

  """Display name for this image."""
  display_name: String!
  source: query_actions_list_hosted_runners_for_org_runners_items_image_details_source!

  """The image version of the hosted runner pool."""
  version: String
}

"""The image provider."""
enum query_actions_list_hosted_runners_for_org_runners_items_image_details_source {
  github
  partner
  custom
}

"""Provides details of a particular machine spec."""
type Github_owned_VM_details_ {
  """The ID used for the `size` parameter when creating a new runner."""
  id: String!

  """The number of cores."""
  cpu_cores: Int!

  """The available RAM for the machine spec."""
  memory_gb: Int!

  """The available SSD storage for the machine spec."""
  storage_gb: Int!
}

"""The status of the runner."""
enum query_actions_list_hosted_runners_for_org_runners_items_status {
  Ready
  Provisioning
  Shutdown
  Deleting
  Stuck
}

"""Provides details of Public IP for a GitHub-hosted larger runners"""
type Public_IP_for_a_GitHub_hosted_larger_runners_ {
  """Whether public IP is enabled."""
  enabled: Boolean

  """The prefix for the public IP."""
  prefix: String

  """The length of the IP prefix."""
  length: Int
}

type actions_list_custom_images_for_org_200_response {
  total_count: Int!
  images: [GitHub_hosted_runner_custom_image_details]!
}

"""Provides details of a custom runner image"""
type GitHub_hosted_runner_custom_image_details {
  """
  The ID of the image. Use this ID for the `image` parameter when creating a new larger runner.
  """
  id: Int!

  """The operating system of the image."""
  platform: String!

  """Total size of all the image versions in GB."""
  total_versions_size: Int!

  """Display name for this image."""
  name: String!

  """The image provider."""
  source: String!

  """The number of image versions associated with the image."""
  versions_count: Int!

  """The latest image version associated with the image."""
  latest_version: String!

  """The number of image versions associated with the image."""
  state: String!
}

type actions_list_custom_image_versions_for_org_200_response {
  total_count: Int!
  image_versions: [GitHub_hosted_runner_custom_image_version_details_]!
}

"""Provides details of a hosted runner custom image version"""
type GitHub_hosted_runner_custom_image_version_details_ {
  """The version of image."""
  version: String!

  """The state of image version."""
  state: String!

  """Image version size in GB."""
  size_gb: Int!

  """The creation date time of the image version."""
  created_on: DateTime!

  """The image version status details."""
  state_details: String!
}

scalar actions_custom_image_version_parameter

type actions_get_hosted_runners_github_owned_images_for_org_200_response {
  total_count: Int!
  images: [actions_hosted_runner_curated_image]!
}

"""Provides details of a hosted runner image"""
type actions_hosted_runner_curated_image {
  """
  The ID of the image. Use this ID for the `image` parameter when creating a new larger runner.
  """
  id: String!

  """The operating system of the image."""
  platform: String!

  """Image size in GB."""
  size_gb: Int!

  """Display name for this image."""
  display_name: String!
  source: query_actions_get_hosted_runners_github_owned_images_for_org_images_items_source!
}

"""The image provider."""
enum query_actions_get_hosted_runners_github_owned_images_for_org_images_items_source {
  github
  partner
  custom
}

type actions_get_hosted_runners_partner_images_for_org_200_response {
  total_count: Int!
  images: [actions_hosted_runner_curated_image]!
}

type actions_hosted_runner_limits {
  public_ips: Static_public_IP_Limits_for_GitHub_hosted_Hosted_Runners_!
}

"""
Provides details of static public IP limits for GitHub-hosted Hosted Runners
"""
type Static_public_IP_Limits_for_GitHub_hosted_Hosted_Runners_ {
  """
  The maximum number of static public IP addresses that can be used for Hosted Runners.
  """
  maximum: Int!

  """
  The current number of static public IP addresses in use by Hosted Runners.
  """
  current_usage: Int!
}

type actions_get_hosted_runners_machine_specs_for_org_200_response {
  total_count: Int!
  machine_specs: [Github_owned_VM_details_]!
}

type actions_get_hosted_runners_platforms_for_org_200_response {
  total_count: Int!
  platforms: [String]!
}

"""Actions OIDC Subject customization"""
type Actions_OIDC_Subject_customization {
  """
  Array of unique strings. Each claim key can only contain alphanumeric characters and underscores.
  """
  include_claim_keys: [String]!
}

type actions_organization_permissions {
  enabled_repositories: enabled_repositories!

  """
  The API URL to use to get or set the selected repositories that are allowed to run GitHub Actions, when `enabled_repositories` is set to `selected`.
  """
  selected_repositories_url: String
  allowed_actions: allowed_actions

  """
  The API URL to use to get or set the actions and reusable workflows that are allowed to run, when `allowed_actions` is set to `selected`.
  """
  selected_actions_url: String

  """Whether actions must be pinned to a full-length commit SHA."""
  sha_pinning_required: Boolean
}

"""
The policy that controls the repositories in the organization that are allowed to run GitHub Actions.
"""
enum enabled_repositories {
  all
  none
  selected
}

"""
The permissions policy that controls the actions and reusable workflows that are allowed to run.
"""
enum allowed_actions {
  all
  local_only
  selected
}

type actions_artifact_and_log_retention_response {
  """The number of days artifacts and logs are retained"""
  days: Int!

  """The maximum number of days that can be configured"""
  maximum_allowed_days: Int!
}

type actions_fork_pr_contributor_approval {
  approval_policy: query_actions_get_fork_pr_contributor_approval_permissions_organization_approval_policy!
}

"""
The policy that controls when fork PR workflows require approval from a maintainer.
"""
enum query_actions_get_fork_pr_contributor_approval_permissions_organization_approval_policy {
  first_time_contributors_new_to_github
  first_time_contributors
  all_external_contributors
}

type actions_fork_pr_workflows_private_repos {
  """
  Whether workflows triggered by pull requests from forks are allowed to run on private repositories.
  """
  run_workflows_from_fork_pull_requests: Boolean!

  """
  Whether GitHub Actions can create pull requests or submit approving pull request reviews from a workflow triggered by a fork pull request.
  """
  send_write_tokens_to_workflows: Boolean!

  """
  Whether to make secrets and variables available to workflows triggered by pull requests from forks.
  """
  send_secrets_and_variables: Boolean!

  """
  Whether workflows triggered by pull requests from forks require approval from a repository administrator to run.
  """
  require_approval_for_fork_pr_workflows: Boolean!
}

type actions_list_selected_repositories_enabled_github_actions_organization_200_response {
  total_count: Float!
  repositories: [repository]!
}

type selected_actions {
  """
  Whether GitHub-owned actions are allowed. For example, this includes the actions in the `actions` organization.
  """
  github_owned_allowed: Boolean

  """
  Whether actions from GitHub Marketplace verified creators are allowed. Set to `true` to allow all actions by GitHub Marketplace verified creators.
  """
  verified_allowed: Boolean

  """
  Specifies a list of string-matching patterns to allow specific action(s) and reusable workflow(s). Wildcards, tags, and SHAs are allowed. For example, `monalisa/octocat@*`, `monalisa/octocat@v2`, `monalisa/*`.
  
  > [!NOTE]
  > The `patterns_allowed` setting only applies to public repositories.
  """
  patterns_allowed: [String]
}

type self_hosted_runners_settings {
  enabled_repositories: query_actions_get_self_hosted_runners_permissions_organization_enabled_repositories!

  """
  The URL to the endpoint for managing selected repositories for self-hosted runners in the organization
  """
  selected_repositories_url: String
}

"""
The policy that controls whether self-hosted runners can be used by repositories in the organization
"""
enum query_actions_get_self_hosted_runners_permissions_organization_enabled_repositories {
  all
  selected
  none
}

type actions_list_selected_repositories_self_hosted_runners_organization_200_response {
  total_count: Int
  repositories: [repository]
}

type actions_get_default_workflow_permissions {
  default_workflow_permissions: actions_default_workflow_permissions!

  """
  Whether GitHub Actions can approve pull requests. Enabling this can be a security risk.
  """
  can_approve_pull_request_reviews: Boolean!
}

"""
The default workflow permissions granted to the GITHUB_TOKEN when running workflows.
"""
enum actions_default_workflow_permissions {
  read
  write
}

type actions_list_self_hosted_runner_groups_for_org_200_response {
  total_count: Float!
  runner_groups: [runner_groups_org]!
}

type runner_groups_org {
  id: Float!
  name: String!
  visibility: String!
  default: Boolean!

  """
  Link to the selected repositories resource for this runner group. Not present unless visibility was set to `selected`
  """
  selected_repositories_url: String
  runners_url: String!
  hosted_runners_url: String

  """The identifier of a hosted compute network configuration."""
  network_configuration_id: String
  inherited: Boolean!
  inherited_allows_public_repositories: Boolean
  allows_public_repositories: Boolean!

  """
  If `true`, the `restricted_to_workflows` and `selected_workflows` fields cannot be modified.
  """
  workflow_restrictions_read_only: Boolean

  """
  If `true`, the runner group will be restricted to running only the workflows specified in the `selected_workflows` array.
  """
  restricted_to_workflows: Boolean

  """
  List of workflows the runner group should be allowed to run. This setting will be ignored unless `restricted_to_workflows` is set to `true`.
  """
  selected_workflows: [String]
}

type actions_list_github_hosted_runners_in_group_for_org_200_response {
  total_count: Float!
  runners: [GitHub_hosted_hosted_runner]!
}

type actions_list_repo_access_to_self_hosted_runner_group_in_org_200_response {
  total_count: Float!
  repositories: [minimal_repository]!
}

type actions_list_self_hosted_runners_in_group_for_org_200_response {
  total_count: Float!
  runners: [Self_hosted_runners]!
}

"""A self hosted runner"""
type Self_hosted_runners {
  """The ID of the runner."""
  id: Int!

  """The ID of the runner group."""
  runner_group_id: Int

  """The name of the runner."""
  name: String!

  """The Operating System of the runner."""
  os: String!

  """The status of the runner."""
  status: String!
  busy: Boolean!
  labels: [Self_hosted_runner_label]!
  ephemeral: Boolean
}

"""A label for a self hosted runner"""
type Self_hosted_runner_label {
  """Unique identifier of the label."""
  id: Int

  """Name of the label."""
  name: String!
  type: query_actions_list_self_hosted_runners_in_group_for_org_runners_items_labels_items_type
}

"""
The type of label. Read-only labels are applied automatically when the runner is configured.
"""
enum query_actions_list_self_hosted_runners_in_group_for_org_runners_items_labels_items_type {
  read_only
  custom
}

type actions_list_self_hosted_runners_for_org_200_response {
  total_count: Int!
  runners: [Self_hosted_runners]!
}

"""Runner Application"""
type Runner_Application {
  os: String!
  architecture: String!
  download_url: String!
  filename: String!

  """A short lived bearer token used to download the runner, if needed."""
  temp_download_token: String
  sha256_checksum: String
}

"""Response"""
type actions_runner_labels_response {
  total_count: Int!
  labels: [Self_hosted_runner_label]!
}

type actions_list_org_secrets_200_response {
  total_count: Int!
  secrets: [Actions_Secret_for_an_Organization]!
}

"""Secrets for GitHub Actions for an organization."""
type Actions_Secret_for_an_Organization {
  """The name of the secret."""
  name: String!
  created_at: DateTime!
  updated_at: DateTime!
  visibility: query_actions_list_org_secrets_secrets_items_visibility!
  selected_repositories_url: URL
}

"""Visibility of a secret"""
enum query_actions_list_org_secrets_secrets_items_visibility {
  all
  private
  selected
}

"""The public key used for setting Actions Secrets."""
type ActionsPublicKey {
  """The identifier for the key."""
  key_id: String!

  """The Base64 encoded public key."""
  key: String!
  id: Int
  url: URL
  title: String
  created_at: DateTime
}

type actions_list_selected_repos_for_org_secret_200_response {
  total_count: Int!
  repositories: [minimal_repository]!
}

type actions_list_org_variables_200_response {
  total_count: Int!
  variables: [Actions_Variable_for_an_Organization]!
}

"""Organization variable for GitHub Actions."""
type Actions_Variable_for_an_Organization {
  """The name of the variable."""
  name: String!

  """The value of the variable."""
  value: String!

  """
  The date and time at which the variable was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  created_at: DateTime!

  """
  The date and time at which the variable was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  updated_at: DateTime!
  visibility: query_actions_list_org_variables_variables_items_visibility!
  selected_repositories_url: URL
}

"""Visibility of a variable"""
enum query_actions_list_org_variables_variables_items_visibility {
  all
  private
  selected
}

type actions_list_selected_repos_for_org_variable_200_response {
  total_count: Int!
  repositories: [minimal_repository]!
}

type orgs_list_artifact_deployment_records_200_response {
  """The number of deployment records for this digest and organization"""
  total_count: Int
  deployment_records: [Artifact_Deployment_Record]
}

"""Artifact Metadata Deployment Record"""
type Artifact_Deployment_Record {
  id: Int
  digest: String
  logical_environment: String
  physical_environment: String
  cluster: String
  deployment_name: String
  tags: JSON

  """A list of runtime risks associated with the deployment."""
  runtime_risks: [query_orgs_list_artifact_deployment_records_deployment_records_items_runtime_risks_items]
  created_at: String
  updated_at: String

  """
  The ID of the provenance attestation associated with the deployment record.
  """
  attestation_id: Int
}

enum query_orgs_list_artifact_deployment_records_deployment_records_items_runtime_risks_items {
  critical_resource
  internet_exposed
  lateral_movement
  sensitive_data
}

scalar subject_digest_parameter

type orgs_list_artifact_storage_records_200_response {
  """The number of storage records for this digest and organization"""
  total_count: Int
  storage_records: [query_orgs_list_artifact_storage_records_storage_records_items]
}

type query_orgs_list_artifact_storage_records_storage_records_items {
  id: Int
  name: String
  digest: String
  artifact_url: String
  registry_url: String
  repository: String
  status: String
  created_at: String
  updated_at: String
}

scalar queryInput_orgs_list_artifact_storage_records_subject_digest

type query_orgs_list_attestation_repositories_items {
  id: Int
  name: String
}

type orgs_list_attestations_200_response {
  attestations: [query_orgs_list_attestations_attestations_items]
}

type query_orgs_list_attestations_attestations_items {
  bundle: query_orgs_list_attestations_attestations_items_bundle
  repository_id: Int
  bundle_url: String
  initiator: String
}

"""
The attestation's Sigstore Bundle.
Refer to the [Sigstore Bundle Specification](https://github.com/sigstore/protobuf-specs/blob/main/protos/sigstore_bundle.proto) for more information.
"""
type query_orgs_list_attestations_attestations_items_bundle {
  mediaType: String
  verificationMaterial: JSON
  dsseEnvelope: JSON
}

"""The campaign metadata and alert stats."""
type Campaign_summary {
  """The number of the newly created campaign"""
  number: Int!

  """
  The date and time the campaign was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  created_at: DateTime!

  """
  The date and time the campaign was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  updated_at: DateTime!

  """The campaign name"""
  name: String

  """The campaign description"""
  description: String!

  """The campaign managers"""
  managers: [simple_user]!

  """The campaign team managers"""
  team_managers: [team]

  """
  The date and time the campaign was published, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  published_at: DateTime

  """
  The date and time the campaign has ended, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  ends_at: DateTime!

  """
  The date and time the campaign was closed, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Will be null if the campaign is still open.
  """
  closed_at: DateTime
  state: Campaign_state!

  """The contact link of the campaign."""
  contact_link: URL
  alert_stats: query_campaigns_list_org_campaigns_items_alert_stats
}

"""
Groups of organization members that gives permissions on specified repositories.
"""
type team {
  id: Int!
  node_id: String!
  name: String!
  slug: String!
  description: String
  privacy: String
  notification_setting: String
  permission: String!
  permissions: query_campaigns_list_org_campaigns_items_team_managers_items_permissions
  url: URL!
  html_url: URL!
  members_url: String!
  repositories_url: URL!
  type: query_campaigns_list_org_campaigns_items_team_managers_items_type!

  """Unique identifier of the organization to which this team belongs"""
  organization_id: Int

  """Unique identifier of the enterprise to which this team belongs"""
  enterprise_id: Int
  parent: nullable_team_simple
}

type query_campaigns_list_org_campaigns_items_team_managers_items_permissions {
  pull: Boolean!
  triage: Boolean!
  push: Boolean!
  maintain: Boolean!
  admin: Boolean!
}

"""The ownership type of the team"""
enum query_campaigns_list_org_campaigns_items_team_managers_items_type {
  enterprise
  organization
}

"""
Groups of organization members that gives permissions on specified repositories.
"""
type nullable_team_simple {
  """Unique identifier of the team"""
  id: Int!
  node_id: String!

  """URL for the team"""
  url: URL!
  members_url: URL!

  """Name of the team"""
  name: String!

  """Description of the team"""
  description: String

  """Permission that the team will have for its repositories"""
  permission: String!

  """The level of privacy this team should have"""
  privacy: String

  """The notification setting the team has set"""
  notification_setting: String
  html_url: URL!
  repositories_url: URL!
  slug: String!

  """Distinguished Name (DN) that team maps to within LDAP environment"""
  ldap_dn: String
  type: query_campaigns_list_org_campaigns_items_team_managers_items_parent_type!

  """Unique identifier of the organization to which this team belongs"""
  organization_id: Int

  """Unique identifier of the enterprise to which this team belongs"""
  enterprise_id: Int
}

"""The ownership type of the team"""
enum query_campaigns_list_org_campaigns_items_team_managers_items_parent_type {
  enterprise
  organization
}

"""Indicates whether a campaign is open or closed"""
enum Campaign_state {
  open
  closed
}

type query_campaigns_list_org_campaigns_items_alert_stats {
  """The number of open alerts"""
  open_count: Int!

  """The number of closed alerts"""
  closed_count: Int!

  """The number of in-progress alerts"""
  in_progress_count: Int!
}

"""The property by which to sort the results."""
enum queryInput_campaigns_list_org_campaigns_sort {
  created
  updated
  ends_at
  published
}

type code_scanning_organization_alert_items {
  """The security alert number."""
  number: Int!

  """
  The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime!

  """
  The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime

  """The REST API URL of the alert resource."""
  url: URL!

  """The GitHub URL of the alert resource."""
  html_url: URL!

  """The REST API URL for fetching the list of instances for an alert."""
  instances_url: URL!
  state: code_scanning_alert_state

  """
  The time that the alert was no longer detected and was considered fixed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  fixed_at: DateTime
  dismissed_by: nullable_simple_user

  """
  The time that the alert was dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  dismissed_at: DateTime
  dismissed_reason: code_scanning_alert_dismissed_reason

  """The dismissal comment associated with the dismissal of the alert."""
  dismissed_comment: code_scanning_alert_dismissed_comment
  rule: code_scanning_alert_rule_summary!
  tool: code_scanning_analysis_tool!
  most_recent_instance: code_scanning_alert_instance!
  repository: simple_repository!
  dismissal_approved_by: nullable_simple_user
  assignees: [simple_user]
}

"""State of a code scanning alert."""
enum code_scanning_alert_state {
  open
  dismissed
  fixed
}

"""
**Required when the state is dismissed.** The reason for dismissing or closing the alert.
"""
enum code_scanning_alert_dismissed_reason {
  false_positive
  won_SINGLE_QUOTE_t_fix
  used_in_tests
}

"""The dismissal comment associated with the dismissal of the alert."""
scalar code_scanning_alert_dismissed_comment

type code_scanning_alert_rule_summary {
  """A unique identifier for the rule used to detect the alert."""
  id: String

  """The name of the rule used to detect the alert."""
  name: String
  severity: query_code_scanning_list_alerts_for_org_items_rule_severity
  security_severity_level: query_code_scanning_list_alerts_for_org_items_rule_security_severity_level

  """A short description of the rule used to detect the alert."""
  description: String

  """A description of the rule used to detect the alert."""
  full_description: String

  """A set of tags applicable for the rule."""
  tags: [String]

  """Detailed documentation for the rule as GitHub Flavored Markdown."""
  help: String

  """A link to the documentation for the rule used to detect the alert."""
  help_uri: String
}

"""The severity of the alert."""
enum query_code_scanning_list_alerts_for_org_items_rule_severity {
  none
  note
  warning
  error
}

"""The security severity of the alert."""
enum query_code_scanning_list_alerts_for_org_items_rule_security_severity_level {
  low
  medium
  high
  critical
}

type code_scanning_analysis_tool {
  """The name of the tool used to generate the code scanning analysis."""
  name: String

  """The version of the tool used to generate the code scanning analysis."""
  version: String

  """
  The GUID of the tool used to generate the code scanning analysis, if provided in the uploaded SARIF data.
  """
  guid: String
}

type code_scanning_alert_instance {
  """
  The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`,
  `refs/heads/<branch name>` or simply `<branch name>`.
  """
  ref: String

  """
  Identifies the configuration under which the analysis was executed. For example, in GitHub Actions this includes the workflow filename and job name.
  """
  analysis_key: String

  """
  Identifies the variable values associated with the environment in which the analysis that generated this alert instance was performed, such as the language that was analyzed.
  """
  environment: String

  """
  Identifies the configuration under which the analysis was executed. Used to distinguish between multiple analyses for the same tool and commit, but performed on different languages or different parts of the code.
  """
  category: String
  state: code_scanning_alert_state
  commit_sha: String
  message: query_code_scanning_list_alerts_for_org_items_most_recent_instance_message
  location: code_scanning_alert_location
  html_url: String

  """
  Classifications that have been applied to the file that triggered the alert.
  For example identifying it as documentation, or a generated file.
  """
  classifications: [code_scanning_alert_classification]
}

type query_code_scanning_list_alerts_for_org_items_most_recent_instance_message {
  text: String
}

"""Describe a region within a file for the alert."""
type code_scanning_alert_location {
  path: String
  start_line: Int
  end_line: Int
  start_column: Int
  end_column: Int
}

"""A classification of the file. For example to identify it as generated."""
enum code_scanning_alert_classification {
  source
  generated
  test
  library
}

"""State of a code scanning alert."""
enum code_scanning_alert_state_query {
  open
  closed
  dismissed
  fixed
}

"""The property by which to sort the results."""
enum queryInput_code_scanning_list_alerts_for_org_sort {
  created
  updated
}

"""Severity of a code scanning alert."""
enum code_scanning_alert_severity {
  critical
  high
  medium
  low
  warning
  note
  error
}

"""The target type of the code security configuration"""
enum queryInput_code_security_get_configurations_for_org_target_type {
  global
  all
}

type codespaces_list_in_organization_200_response {
  total_count: Int!
  codespaces: [codespace]!
}

"""A codespace."""
type codespace {
  id: BigInt!

  """Automatically generated name of this codespace."""
  name: String!

  """Display name for this codespace."""
  display_name: String

  """UUID identifying this codespace's environment."""
  environment_id: String
  owner: simple_user!
  billable_owner: simple_user!
  repository: minimal_repository!
  machine: nullable_codespace_machine

  """Path to devcontainer.json from repo root used to create Codespace."""
  devcontainer_path: String

  """Whether the codespace was created from a prebuild."""
  prebuild: Boolean
  created_at: DateTime!
  updated_at: DateTime!

  """Last known time this codespace was started."""
  last_used_at: DateTime!
  state: query_codespaces_list_in_organization_codespaces_items_state!

  """API URL for this codespace."""
  url: URL!
  git_status: query_codespaces_list_in_organization_codespaces_items_git_status!
  location: query_codespaces_list_in_organization_codespaces_items_location!

  """
  The number of minutes of inactivity after which this codespace will be automatically stopped.
  """
  idle_timeout_minutes: Int

  """URL to access this codespace on the web."""
  web_url: URL!

  """
  API URL to access available alternate machine types for this codespace.
  """
  machines_url: URL!

  """API URL to start this codespace."""
  start_url: URL!

  """API URL to stop this codespace."""
  stop_url: URL!

  """API URL to publish this codespace to a new repository."""
  publish_url: URL

  """API URL for the Pull Request associated with this codespace, if any."""
  pulls_url: URL
  recent_folders: [String]!
  runtime_constraints: query_codespaces_list_in_organization_codespaces_items_runtime_constraints

  """
  Whether or not a codespace has a pending async operation. This would mean that the codespace is temporarily unavailable. The only thing that you can do with a codespace in this state is delete it.
  """
  pending_operation: Boolean

  """Text to show user when codespace is disabled by a pending operation"""
  pending_operation_disabled_reason: String

  """
  Text to show user when codespace idle timeout minutes has been overriden by an organization policy
  """
  idle_timeout_notice: String

  """
  Duration in minutes after codespace has gone idle in which it will be deleted. Must be integer minutes between 0 and 43200 (30 days).
  """
  retention_period_minutes: Int

  """
  When a codespace will be auto-deleted based on the "retention_period_minutes" and "last_used_at"
  """
  retention_expires_at: DateTime

  """
  The text to display to a user when a codespace has been stopped for a potentially actionable reason.
  """
  last_known_stop_notice: String
}

"""A description of the machine powering a codespace."""
type nullable_codespace_machine {
  """The name of the machine."""
  name: String!

  """The display name of the machine includes cores, memory, and storage."""
  display_name: String!

  """The operating system of the machine."""
  operating_system: String!

  """How much storage is available to the codespace."""
  storage_in_bytes: Int!

  """How much memory is available to the codespace."""
  memory_in_bytes: Int!

  """How many cores are available to the codespace."""
  cpus: Int!
  prebuild_availability: query_codespaces_list_in_organization_codespaces_items_machine_prebuild_availability
}

"""
Whether a prebuild is currently available when creating a codespace for this machine and repository. If a branch was not specified as a ref, the default branch will be assumed. Value will be "null" if prebuilds are not supported or prebuild availability could not be determined. Value will be "none" if no prebuild is available. Latest values "ready" and "in_progress" indicate the prebuild availability status.
"""
enum query_codespaces_list_in_organization_codespaces_items_machine_prebuild_availability {
  none
  ready
  in_progress
}

"""State of this codespace."""
enum query_codespaces_list_in_organization_codespaces_items_state {
  Unknown
  Created
  Queued
  Provisioning
  Available
  Awaiting
  Unavailable
  Deleted
  Moved
  Shutdown
  Archived
  Starting
  ShuttingDown
  Failed
  Exporting
  Updating
  Rebuilding
}

"""Details about the codespace's git repository."""
type query_codespaces_list_in_organization_codespaces_items_git_status {
  """The number of commits the local repository is ahead of the remote."""
  ahead: Int

  """The number of commits the local repository is behind the remote."""
  behind: Int

  """Whether the local repository has unpushed changes."""
  has_unpushed_changes: Boolean

  """Whether the local repository has uncommitted changes."""
  has_uncommitted_changes: Boolean

  """
  The current branch (or SHA if in detached HEAD state) of the local repository.
  """
  ref: String
}

"""The initally assigned location of a new codespace."""
enum query_codespaces_list_in_organization_codespaces_items_location {
  EastUs
  SouthEastAsia
  WestEurope
  WestUs2
}

type query_codespaces_list_in_organization_codespaces_items_runtime_constraints {
  """The privacy settings a user can select from when forwarding a port."""
  allowed_port_privacy_settings: [String]
}

type codespaces_list_org_secrets_200_response {
  total_count: Int!
  secrets: [codespaces_org_secret]!
}

"""Secrets for a GitHub Codespace."""
type codespaces_org_secret {
  """The name of the secret"""
  name: String!

  """
  The date and time at which the secret was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  created_at: DateTime!

  """
  The date and time at which the secret was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  updated_at: DateTime!
  visibility: query_codespaces_list_org_secrets_secrets_items_visibility!

  """
  The API URL at which the list of repositories this secret is visible to can be retrieved
  """
  selected_repositories_url: URL
}

"""
The type of repositories in the organization that the secret is visible to
"""
enum query_codespaces_list_org_secrets_secrets_items_visibility {
  all
  private
  selected
}

"""The public key used for setting Codespaces secrets."""
type CodespacesPublicKey {
  """The identifier for the key."""
  key_id: String!

  """The Base64 encoded public key."""
  key: String!
  id: Int
  url: URL
  title: String
  created_at: DateTime
}

type codespaces_list_selected_repos_for_org_secret_200_response {
  total_count: Int!
  repositories: [minimal_repository]!
}

"""
Information about the seat breakdown and policies set for an organization with a Copilot Business or Copilot Enterprise subscription.
"""
type Copilot_Organization_Details {
  seat_breakdown: Copilot_Seat_Breakdown!
  public_code_suggestions: query_copilot_get_copilot_organization_details_public_code_suggestions!
  ide_chat: query_copilot_get_copilot_organization_details_ide_chat
  platform_chat: query_copilot_get_copilot_organization_details_platform_chat
  cli: query_copilot_get_copilot_organization_details_cli
  seat_management_setting: query_copilot_get_copilot_organization_details_seat_management_setting!
  plan_type: query_copilot_get_copilot_organization_details_plan_type
  additionalProperties: JSON
}

"""The breakdown of Copilot Business seats for the organization."""
type Copilot_Seat_Breakdown {
  """
  The total number of seats being billed for the organization as of the current billing cycle.
  """
  total: Int

  """Seats added during the current billing cycle."""
  added_this_cycle: Int

  """
  The number of seats that are pending cancellation at the end of the current billing cycle.
  """
  pending_cancellation: Int

  """
  The number of users who have been invited to receive a Copilot seat through this organization.
  """
  pending_invitation: Int

  """
  The number of seats that have used Copilot during the current billing cycle.
  """
  active_this_cycle: Int

  """
  The number of seats that have not used Copilot during the current billing cycle.
  """
  inactive_this_cycle: Int
}

"""
The organization policy for allowing or blocking suggestions matching public code (duplication detection filter).
"""
enum query_copilot_get_copilot_organization_details_public_code_suggestions {
  allow
  block
  unconfigured
}

"""
The organization policy for allowing or disallowing Copilot Chat in the IDE.
"""
enum query_copilot_get_copilot_organization_details_ide_chat {
  enabled
  disabled
  unconfigured
}

"""
The organization policy for allowing or disallowing Copilot features on GitHub.com.
"""
enum query_copilot_get_copilot_organization_details_platform_chat {
  enabled
  disabled
  unconfigured
}

"""The organization policy for allowing or disallowing Copilot CLI."""
enum query_copilot_get_copilot_organization_details_cli {
  enabled
  disabled
  unconfigured
}

"""The mode of assigning new seats."""
enum query_copilot_get_copilot_organization_details_seat_management_setting {
  assign_all
  assign_selected
  disabled
  unconfigured
}

"""
The Copilot plan of the organization, or the parent enterprise, when applicable.
"""
enum query_copilot_get_copilot_organization_details_plan_type {
  business
  enterprise
}

type copilot_list_copilot_seats_200_response {
  """
  Total number of Copilot seats for the organization currently being billed.
  """
  total_seats: Int
  seats: [Copilot_Business_Seat_Detail]
}

"""
Information about a Copilot Business seat assignment for a user, team, or organization.
"""
type Copilot_Business_Seat_Detail {
  assignee: nullable_simple_user
  organization: nullable_organization_simple
  assigning_team: query_copilot_list_copilot_seats_seats_items_assigning_team

  """
  The pending cancellation date for the seat, in `YYYY-MM-DD` format. This will be null unless the assignee's Copilot access has been canceled during the current billing cycle. If the seat has been cancelled, this corresponds to the start of the organization's next billing cycle.
  """
  pending_cancellation_date: Date

  """Timestamp of user's last GitHub Copilot activity, in ISO 8601 format."""
  last_activity_at: DateTime

  """Last editor that was used by the user for a GitHub Copilot completion."""
  last_activity_editor: String

  """
  Timestamp of the last time the user authenticated with GitHub Copilot, in ISO 8601 format.
  """
  last_authenticated_at: DateTime

  """
  Timestamp of when the assignee was last granted access to GitHub Copilot, in ISO 8601 format.
  """
  created_at: DateTime!

  """
  **Closing down notice:** This field is no longer relevant and is closing down. Use the `created_at` field to determine when the assignee was last granted access to GitHub Copilot. Timestamp of when the assignee's GitHub Copilot access was last updated, in ISO 8601 format.
  """
  updated_at: DateTime @deprecated(reason: "deprecated")
  plan_type: query_copilot_list_copilot_seats_seats_items_plan_type
}

"""A GitHub organization."""
type nullable_organization_simple {
  login: String!
  id: Int!
  node_id: String!
  url: URL!
  repos_url: URL!
  events_url: URL!
  hooks_url: URL!
  issues_url: URL!
  members_url: URL!
  public_members_url: URL!
  avatar_url: URL!
  description: String
}

"""
The team through which the assignee is granted access to GitHub Copilot, if applicable.
"""
union query_copilot_list_copilot_seats_seats_items_assigning_team = team | Enterprise_Team

"""Represents date values"""
scalar Date

"""
The Copilot plan of the organization, or the parent enterprise, when applicable.
"""
enum query_copilot_list_copilot_seats_seats_items_plan_type {
  business
  enterprise
  unknown
}

type copilot_get_copilot_coding_agent_permissions_organization_200_response {
  enabled_repositories: query_copilot_get_copilot_coding_agent_permissions_organization_enabled_repositories!

  """
  The URL for the selected repositories endpoint. Only present when `enabled_repositories` is `selected`.
  """
  selected_repositories_url: String
}

"""
The policy for which repositories can use Copilot coding agent. Can be one of `all`, `selected`, or `none`.
"""
enum query_copilot_get_copilot_coding_agent_permissions_organization_enabled_repositories {
  all
  selected
  none
}

type copilot_list_copilot_coding_agent_selected_repositories_for_organization_200_response {
  total_count: Int!
  repositories: [minimal_repository]!
}

"""Copilot usage metrics for a given day."""
type Copilot_Usage_Metrics {
  """
  The date for which the usage metrics are aggregated, in `YYYY-MM-DD` format.
  """
  date: Date!

  """
  The total number of Copilot users with activity belonging to any Copilot feature, globally, for the given day. Includes passive activity such as receiving a code suggestion, as well as engagement activity such as accepting a code suggestion or prompting chat. Does not include authentication events. Is not limited to the individual features detailed on the endpoint.
  """
  total_active_users: Int

  """
  The total number of Copilot users who engaged with any Copilot feature, for the given day. Examples include but are not limited to accepting a code suggestion, prompting Copilot chat, or triggering a PR Summary. Does not include authentication events. Is not limited to the individual features detailed on the endpoint.
  """
  total_engaged_users: Int
  copilot_ide_code_completions: copilot_ide_code_completions
  copilot_ide_chat: copilot_ide_chat
  copilot_dotcom_chat: copilot_dotcom_chat
  copilot_dotcom_pull_requests: copilot_dotcom_pull_requests
  additionalProperties: JSON
}

"""Usage metrics for Copilot editor code completions in the IDE."""
type copilot_ide_code_completions {
  """
  Number of users who accepted at least one Copilot code suggestion, across all active editors. Includes both full and partial acceptances.
  """
  total_engaged_users: Int

  """Code completion metrics for active languages."""
  languages: [query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_languages_items]
  editors: [query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_editors_items]
  additionalProperties: JSON
}

"""
Usage metrics for a given language for the given editor for Copilot code completions.
"""
type query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_languages_items {
  """Name of the language used for Copilot code completion suggestions."""
  name: String

  """
  Number of users who accepted at least one Copilot code completion suggestion for the given language. Includes both full and partial acceptances.
  """
  total_engaged_users: Int
}

"""Copilot code completion metrics for active editors."""
type query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_editors_items {
  """Name of the given editor."""
  name: String

  """
  Number of users who accepted at least one Copilot code completion suggestion for the given editor. Includes both full and partial acceptances.
  """
  total_engaged_users: Int

  """List of model metrics for custom models and the default model."""
  models: [query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_editors_items_models_items]
  additionalProperties: JSON
}

type query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_editors_items_models_items {
  """
  Name of the model used for Copilot code completion suggestions. If the default model is used will appear as 'default'.
  """
  name: String

  """Indicates whether a model is custom or default."""
  is_custom_model: Boolean

  """The training date for the custom model."""
  custom_model_training_date: String

  """
  Number of users who accepted at least one Copilot code completion suggestion for the given editor, for the given language and model. Includes both full and partial acceptances.
  """
  total_engaged_users: Int

  """Code completion metrics for active languages, for the given editor."""
  languages: [query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_editors_items_models_items_languages_items]
}

"""
Usage metrics for a given language for the given editor for Copilot code completions.
"""
type query_copilot_copilot_metrics_for_organization_items_copilot_ide_code_completions_editors_items_models_items_languages_items {
  """
  Name of the language used for Copilot code completion suggestions, for the given editor.
  """
  name: String

  """
  Number of users who accepted at least one Copilot code completion suggestion for the given editor, for the given language. Includes both full and partial acceptances.
  """
  total_engaged_users: Int

  """
  The number of Copilot code suggestions generated for the given editor, for the given language.
  """
  total_code_suggestions: Int

  """
  The number of Copilot code suggestions accepted for the given editor, for the given language. Includes both full and partial acceptances.
  """
  total_code_acceptances: Int

  """
  The number of lines of code suggested by Copilot code completions for the given editor, for the given language.
  """
  total_code_lines_suggested: Int

  """
  The number of lines of code accepted from Copilot code suggestions for the given editor, for the given language.
  """
  total_code_lines_accepted: Int
}

"""Usage metrics for Copilot Chat in the IDE."""
type copilot_ide_chat {
  """Total number of users who prompted Copilot Chat in the IDE."""
  total_engaged_users: Int
  editors: [query_copilot_copilot_metrics_for_organization_items_copilot_ide_chat_editors_items]
  additionalProperties: JSON
}

"""Copilot Chat metrics, for active editors."""
type query_copilot_copilot_metrics_for_organization_items_copilot_ide_chat_editors_items {
  """Name of the given editor."""
  name: String

  """The number of users who prompted Copilot Chat in the specified editor."""
  total_engaged_users: Int

  """List of model metrics for custom models and the default model."""
  models: [query_copilot_copilot_metrics_for_organization_items_copilot_ide_chat_editors_items_models_items]
}

type query_copilot_copilot_metrics_for_organization_items_copilot_ide_chat_editors_items_models_items {
  """
  Name of the model used for Copilot Chat. If the default model is used will appear as 'default'.
  """
  name: String

  """Indicates whether a model is custom or default."""
  is_custom_model: Boolean

  """The training date for the custom model."""
  custom_model_training_date: String

  """
  The number of users who prompted Copilot Chat in the given editor and model.
  """
  total_engaged_users: Int

  """
  The total number of chats initiated by users in the given editor and model.
  """
  total_chats: Int

  """
  The number of times users accepted a code suggestion from Copilot Chat using the 'Insert Code' UI element, for the given editor.
  """
  total_chat_insertion_events: Int

  """
  The number of times users copied a code suggestion from Copilot Chat using the keyboard, or the 'Copy' UI element, for the given editor.
  """
  total_chat_copy_events: Int
}

"""Usage metrics for Copilot Chat in GitHub.com"""
type copilot_dotcom_chat {
  """
  Total number of users who prompted Copilot Chat on github.com at least once.
  """
  total_engaged_users: Int

  """List of model metrics for a custom models and the default model."""
  models: [query_copilot_copilot_metrics_for_organization_items_copilot_dotcom_chat_models_items]
  additionalProperties: JSON
}

type query_copilot_copilot_metrics_for_organization_items_copilot_dotcom_chat_models_items {
  """
  Name of the model used for Copilot Chat. If the default model is used will appear as 'default'.
  """
  name: String

  """Indicates whether a model is custom or default."""
  is_custom_model: Boolean

  """The training date for the custom model (if applicable)."""
  custom_model_training_date: String

  """
  Total number of users who prompted Copilot Chat on github.com at least once for each model.
  """
  total_engaged_users: Int

  """Total number of chats initiated by users on github.com."""
  total_chats: Int
}

"""Usage metrics for Copilot for pull requests."""
type copilot_dotcom_pull_requests {
  """
  The number of users who used Copilot for Pull Requests on github.com to generate a pull request summary at least once.
  """
  total_engaged_users: Int

  """
  Repositories in which users used Copilot for Pull Requests to generate pull request summaries
  """
  repositories: [query_copilot_copilot_metrics_for_organization_items_copilot_dotcom_pull_requests_repositories_items]
  additionalProperties: JSON
}

type query_copilot_copilot_metrics_for_organization_items_copilot_dotcom_pull_requests_repositories_items {
  """Repository name"""
  name: String

  """
  The number of users who generated pull request summaries using Copilot for Pull Requests in the given repository.
  """
  total_engaged_users: Int

  """List of model metrics for custom models and the default model."""
  models: [query_copilot_copilot_metrics_for_organization_items_copilot_dotcom_pull_requests_repositories_items_models_items]
}

type query_copilot_copilot_metrics_for_organization_items_copilot_dotcom_pull_requests_repositories_items_models_items {
  """
  Name of the model used for Copilot pull request summaries. If the default model is used will appear as 'default'.
  """
  name: String

  """Indicates whether a model is custom or default."""
  is_custom_model: Boolean

  """The training date for the custom model."""
  custom_model_training_date: String

  """
  The number of pull request summaries generated using Copilot for Pull Requests in the given repository.
  """
  total_pr_summaries_created: Int

  """
  The number of users who generated pull request summaries using Copilot for Pull Requests in the given repository and model.
  """
  total_engaged_users: Int
}

input dependabot_alert_org_scope_comma_separated_has_parameter_Input {
  String: String
  queryInput_dependabot_list_alerts_for_org_has_oneOf_1_items: queryInput_dependabot_list_alerts_for_org_has_oneOf_1_items
}

enum queryInput_dependabot_list_alerts_for_org_has_oneOf_1_items {
  patch
  deployment
}

type dependabot_list_org_secrets_200_response {
  total_count: Int!
  secrets: [Dependabot_Secret_for_an_Organization]!
}

"""Secrets for GitHub Dependabot for an organization."""
type Dependabot_Secret_for_an_Organization {
  """The name of the secret."""
  name: String!
  created_at: DateTime!
  updated_at: DateTime!
  visibility: query_dependabot_list_org_secrets_secrets_items_visibility!
  selected_repositories_url: URL
}

"""Visibility of a secret"""
enum query_dependabot_list_org_secrets_secrets_items_visibility {
  all
  private
  selected
}

"""The public key used for setting Dependabot Secrets."""
type DependabotPublicKey {
  """The identifier for the key."""
  key_id: String!

  """The Base64 encoded public key."""
  key: String!
}

type dependabot_list_selected_repos_for_org_secret_200_response {
  total_count: Int!
  repositories: [minimal_repository]!
}

"""A software package"""
type Package {
  """Unique identifier of the package."""
  id: Int!

  """The name of the package."""
  name: String!
  package_type: query_packages_list_docker_migration_conflicting_packages_for_organization_items_package_type!
  url: URL!
  html_url: URL!

  """The number of versions of the package."""
  version_count: Int!
  visibility: query_packages_list_docker_migration_conflicting_packages_for_organization_items_visibility!
  owner: nullable_simple_user
  repository: nullable_minimal_repository
  created_at: DateTime!
  updated_at: DateTime!
}

enum query_packages_list_docker_migration_conflicting_packages_for_organization_items_package_type {
  npm
  maven
  rubygems
  docker
  nuget
  container
}

enum query_packages_list_docker_migration_conflicting_packages_for_organization_items_visibility {
  private
  public
}

"""Minimal Repository"""
type nullable_minimal_repository {
  id: BigInt!
  node_id: String!
  name: String!
  full_name: String!
  owner: simple_user!
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  archive_url: URL!
  assignees_url: URL!
  blobs_url: URL!
  branches_url: URL!
  collaborators_url: URL!
  comments_url: URL!
  commits_url: URL!
  compare_url: URL!
  contents_url: URL!
  contributors_url: URL!
  deployments_url: URL!
  downloads_url: URL!
  events_url: URL!
  forks_url: URL!
  git_commits_url: URL!
  git_refs_url: URL!
  git_tags_url: URL!
  git_url: String
  issue_comment_url: URL!
  issue_events_url: URL!
  issues_url: URL!
  keys_url: URL!
  labels_url: URL!
  languages_url: URL!
  merges_url: URL!
  milestones_url: URL!
  notifications_url: URL!
  pulls_url: URL!
  releases_url: URL!
  ssh_url: String
  stargazers_url: URL!
  statuses_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  tags_url: URL!
  teams_url: URL!
  trees_url: URL!
  clone_url: String
  mirror_url: String
  hooks_url: URL!
  svn_url: String
  homepage: String
  language: String
  forks_count: Int
  stargazers_count: Int
  watchers_count: Int

  """
  The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0.
  """
  size: Int
  default_branch: String
  open_issues_count: Int
  is_template: Boolean
  topics: [String]
  has_issues: Boolean
  has_projects: Boolean
  has_wiki: Boolean
  has_pages: Boolean
  has_downloads: Boolean
  has_discussions: Boolean
  has_pull_requests: Boolean
  pull_request_creation_policy: query_packages_list_docker_migration_conflicting_packages_for_organization_items_repository_pull_request_creation_policy
  has_commit_comments: Boolean
  archived: Boolean
  disabled: Boolean
  visibility: String
  pushed_at: DateTime
  created_at: DateTime
  updated_at: DateTime
  permissions: query_packages_list_docker_migration_conflicting_packages_for_organization_items_repository_permissions
  role_name: String
  temp_clone_token: String
  delete_branch_on_merge: Boolean
  subscribers_count: Int
  network_count: Int
  code_of_conduct: Code_Of_Conduct
  license: query_packages_list_docker_migration_conflicting_packages_for_organization_items_repository_license
  forks: Int
  open_issues: Int
  watchers: Int
  allow_forking: Boolean
  web_commit_signoff_required: Boolean
  security_and_analysis: security_and_analysis

  """
  The custom properties that were defined for the repository. The keys are the custom property names, and the values are the corresponding custom property values.
  """
  custom_properties: JSON
}

"""
The policy controlling who can create pull requests: all or collaborators_only.
"""
enum query_packages_list_docker_migration_conflicting_packages_for_organization_items_repository_pull_request_creation_policy {
  all
  collaborators_only
}

type query_packages_list_docker_migration_conflicting_packages_for_organization_items_repository_permissions {
  admin: Boolean
  maintain: Boolean
  push: Boolean
  triage: Boolean
  pull: Boolean
}

type query_packages_list_docker_migration_conflicting_packages_for_organization_items_repository_license {
  key: String
  name: String
  spdx_id: String
  url: String
  node_id: String
}

"""Organization Invitation"""
type Organization_Invitation {
  id: BigInt!
  login: String
  email: String
  role: String!
  created_at: String!
  failed_at: String
  failed_reason: String
  inviter: simple_user!
  team_count: Int!
  node_id: String!
  invitation_teams_url: String!
  invitation_source: String
}

"""Org Hook"""
type Org_Hook {
  id: Int!
  url: URL!
  ping_url: URL!
  deliveries_url: URL
  name: String!
  events: [String]!
  active: Boolean!
  config: query_orgs_list_webhooks_items_config!
  updated_at: DateTime!
  created_at: DateTime!
  type: String!
}

type query_orgs_list_webhooks_items_config {
  url: String
  insecure_ssl: String
  content_type: String
  secret: String
}

type query_api_insights_get_route_stats_by_actor_items {
  """The HTTP method"""
  http_method: String

  """The API path's route template"""
  api_route: String

  """The total number of requests within the queried time period"""
  total_request_count: BigInt

  """
  The total number of requests that were rate limited within the queried time period
  """
  rate_limited_request_count: BigInt
  last_rate_limited_timestamp: String
  last_request_timestamp: String
}

"""The type of the actor"""
enum api_insights_actor_type_parameter {
  installation
  classic_pat
  fine_grained_pat
  oauth_app
  github_app_user_to_server
}

enum queryInput_api_insights_get_route_stats_by_actor_sort_items {
  last_rate_limited_timestamp
  last_request_timestamp
  rate_limited_request_count
  http_method
  api_route
  total_request_count
}

type query_api_insights_get_subject_stats_items {
  subject_type: String
  subject_name: String
  subject_id: BigInt
  total_request_count: Int
  rate_limited_request_count: Int
  last_rate_limited_timestamp: String
  last_request_timestamp: String
}

enum queryInput_api_insights_get_subject_stats_sort_items {
  last_rate_limited_timestamp
  last_request_timestamp
  rate_limited_request_count
  subject_name
  total_request_count
}

"""API Insights usage summary stats for an organization"""
type Summary_Stats {
  """The total number of requests within the queried time period"""
  total_request_count: BigInt

  """
  The total number of requests that were rate limited within the queried time period
  """
  rate_limited_request_count: BigInt
}

type query_api_insights_get_time_stats_items {
  timestamp: String
  total_request_count: BigInt
  rate_limited_request_count: BigInt
}

type query_api_insights_get_user_stats_items {
  actor_type: String
  actor_name: String
  actor_id: BigInt
  integration_id: BigInt
  oauth_application_id: BigInt
  total_request_count: Int
  rate_limited_request_count: Int
  last_rate_limited_timestamp: String
  last_request_timestamp: String
}

type orgs_list_app_installations_200_response {
  total_count: Int!
  installations: [Installation]!
}

type interactions_get_restrictions_for_org_200_response {
  limit: interaction_group!
  origin: String!
  expires_at: DateTime!
  JSON: JSON
}

"""
The type of GitHub user that can comment, open issues, or create pull requests while the interaction limit is in effect.
"""
enum interaction_group {
  existing_users
  contributors_only
  collaborators_only
}

"""Filter invitations by their member role."""
enum queryInput_orgs_list_pending_invitations_role {
  all
  admin
  direct_member
  billing_manager
  hiring_manager
}

"""Filter invitations by their invitation source."""
enum queryInput_orgs_list_pending_invitations_invitation_source {
  all
  member
  scim
}

"""
A custom attribute defined at the organization level for attaching structured data to issues.
"""
type Issue_Field {
  """The unique identifier of the issue field."""
  id: Int!

  """The node identifier of the issue field."""
  node_id: String!

  """The name of the issue field."""
  name: String!

  """The description of the issue field."""
  description: String
  data_type: query_orgs_list_issue_fields_items_data_type!
  visibility: query_orgs_list_issue_fields_items_visibility

  """Available options for single select fields."""
  options: [query_orgs_list_issue_fields_items_options_items]

  """The time the issue field was created."""
  created_at: DateTime

  """The time the issue field was last updated."""
  updated_at: DateTime
}

"""The data type of the issue field."""
enum query_orgs_list_issue_fields_items_data_type {
  text
  date
  single_select
  number
}

"""
The visibility of the issue field. Can be `organization_members_only` (visible only within the organization) or `all` (visible to all users who can see issues).
"""
enum query_orgs_list_issue_fields_items_visibility {
  organization_members_only
  all
}

type query_orgs_list_issue_fields_items_options_items {
  """The unique identifier of the option."""
  id: Int!

  """The name of the option."""
  name: String!

  """The description of the option."""
  description: String
  color: query_orgs_list_issue_fields_items_options_items_color

  """The priority of the option for ordering."""
  priority: Int

  """The time the option was created."""
  created_at: DateTime

  """The time the option was last updated."""
  updated_at: DateTime
}

"""The color of the option."""
enum query_orgs_list_issue_fields_items_options_items_color {
  gray
  blue
  green
  yellow
  orange
  red
  pink
  purple
}

"""
Indicates which sorts of issues to return. `assigned` means issues assigned to you. `created` means issues created by you. `mentioned` means issues mentioning you. `subscribed` means issues you're subscribed to updates for. `all` or `repos` means all issues you can see, regardless of participation or creation.
"""
enum queryInput_issues_list_for_org_filter {
  assigned
  created
  mentioned
  subscribed
  repos
  all
}

"""Indicates the state of the issues to return."""
enum queryInput_issues_list_for_org_state {
  open
  closed
  all
}

"""What to sort results by."""
enum queryInput_issues_list_for_org_sort {
  created
  updated
  comments
}

"""
Filter members returned in the list. `2fa_disabled` means that only members without [two-factor authentication](https://github.com/blog/1614-two-factor-authentication) enabled will be returned. `2fa_insecure` means that only members with [insecure 2FA methods](https://docs.github.com/organizations/keeping-your-organization-secure/managing-two-factor-authentication-for-your-organization/requiring-two-factor-authentication-in-your-organization#requiring-secure-methods-of-two-factor-authentication-in-your-organization) will be returned. These options are only available for organization owners.
"""
enum queryInput_orgs_list_members_filter {
  _2fa_disabled
  _2fa_insecure
  all
}

"""Filter members returned by their role."""
enum queryInput_orgs_list_members_role {
  all
  admin
  member
}

type codespaces_get_codespaces_for_user_in_org_200_response {
  total_count: Int!
  codespaces: [codespace]!
}

"""Org Membership"""
type Org_Membership {
  url: URL!
  state: query_orgs_get_membership_for_user_state!
  role: query_orgs_get_membership_for_user_role!

  """Whether the user has direct membership in the organization."""
  direct_membership: Boolean

  """
  The slugs of the enterprise teams providing the user with indirect membership in the organization.
  A limit of 100 enterprise team slugs is returned.
  """
  enterprise_teams_providing_indirect_membership: [String]
  organization_url: URL!
  organization: organization_simple!
  user: nullable_simple_user
  permissions: query_orgs_get_membership_for_user_permissions
}

"""
The state of the member in the organization. The `pending` state indicates the user has not yet accepted an invitation.
"""
enum query_orgs_get_membership_for_user_state {
  active
  pending
}

"""The user's membership type in the organization."""
enum query_orgs_get_membership_for_user_role {
  admin
  member
  billing_manager
}

type query_orgs_get_membership_for_user_permissions {
  can_create_repository: Boolean!
}

"""A migration."""
type Migration {
  id: BigInt!
  owner: nullable_simple_user
  guid: String!
  state: String!
  lock_repositories: Boolean!
  exclude_metadata: Boolean!
  exclude_git_data: Boolean!
  exclude_attachments: Boolean!
  exclude_releases: Boolean!
  exclude_owner_projects: Boolean!
  org_metadata_only: Boolean!

  """
  The repositories included in the migration. Only returned for export migrations.
  """
  repositories: [repository]!
  url: URL!
  created_at: DateTime!
  updated_at: DateTime!
  node_id: String!
  archive_url: URL

  """
  Exclude related items from being returned in the response in order to improve performance of the request. The array can include any of: `"repositories"`.
  """
  exclude: [String]
}

enum repositories_const {
  repositories
}

type orgs_list_org_roles_200_response {
  """The total number of organization roles available to the organization."""
  total_count: Int

  """The list of organization roles available to the organization."""
  roles: [Organization_Role]
}

"""Organization roles"""
type Organization_Role {
  """The unique identifier of the role."""
  id: BigInt!

  """The name of the role."""
  name: String!

  """
  A short description about who this role is for or what permissions it grants.
  """
  description: String
  base_role: query_orgs_list_org_roles_roles_items_base_role
  source: query_orgs_list_org_roles_roles_items_source

  """A list of permissions included in this role."""
  permissions: [String]!
  organization: nullable_simple_user

  """The date and time the role was created."""
  created_at: DateTime!

  """The date and time the role was last updated."""
  updated_at: DateTime!
}

"""The system role from which this role inherits permissions."""
enum query_orgs_list_org_roles_roles_items_base_role {
  read
  triage
  write
  maintain
  admin
}

"""
Source answers the question, "where did this role come from?"
"""
enum query_orgs_list_org_roles_roles_items_source {
  Organization
  Enterprise
  Predefined
}

"""The Relationship a Team has with a role."""
type A_Role_Assignment_for_a_Team {
  assignment: query_orgs_list_org_role_teams_items_assignment
  id: Int!
  node_id: String!
  name: String!
  slug: String!
  description: String
  privacy: String
  notification_setting: String
  permission: String!
  permissions: query_orgs_list_org_role_teams_items_permissions
  url: URL!
  html_url: URL!
  members_url: String!
  repositories_url: URL!
  parent: nullable_team_simple
  type: query_orgs_list_org_role_teams_items_type!

  """Unique identifier of the organization to which this team belongs"""
  organization_id: Int

  """Unique identifier of the enterprise to which this team belongs"""
  enterprise_id: Int
}

"""
Determines if the team has a direct, indirect, or mixed relationship to a role
"""
enum query_orgs_list_org_role_teams_items_assignment {
  direct
  indirect
  mixed
}

type query_orgs_list_org_role_teams_items_permissions {
  pull: Boolean!
  triage: Boolean!
  push: Boolean!
  maintain: Boolean!
  admin: Boolean!
}

"""The ownership type of the team"""
enum query_orgs_list_org_role_teams_items_type {
  enterprise
  organization
}

"""The Relationship a User has with a role."""
type A_Role_Assignment_for_a_User {
  assignment: query_orgs_list_org_role_users_items_assignment

  """Team the user has gotten the role through"""
  inherited_from: [team_simple]
  name: String
  email: String
  login: String!
  id: Int!
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  starred_at: String
  user_view_type: String
}

"""
Determines if the user has a direct, indirect, or mixed relationship to a role
"""
enum query_orgs_list_org_role_users_items_assignment {
  direct
  indirect
  mixed
}

"""
Groups of organization members that gives permissions on specified repositories.
"""
type team_simple {
  """Unique identifier of the team"""
  id: Int!
  node_id: String!

  """URL for the team"""
  url: URL!
  members_url: URL!

  """Name of the team"""
  name: String!

  """Description of the team"""
  description: String

  """Permission that the team will have for its repositories"""
  permission: String!

  """The level of privacy this team should have"""
  privacy: String

  """The notification setting the team has set"""
  notification_setting: String
  html_url: URL!
  repositories_url: URL!
  slug: String!

  """Distinguished Name (DN) that team maps to within LDAP environment"""
  ldap_dn: String
  type: query_orgs_list_org_role_users_items_inherited_from_items_type!

  """Unique identifier of the organization to which this team belongs"""
  organization_id: Int

  """Unique identifier of the enterprise to which this team belongs"""
  enterprise_id: Int
}

"""The ownership type of the team"""
enum query_orgs_list_org_role_users_items_inherited_from_items_type {
  enterprise
  organization
}

"""
Filter the list of outside collaborators. `2fa_disabled` means that only outside collaborators without [two-factor authentication](https://github.com/blog/1614-two-factor-authentication) enabled will be returned. `2fa_insecure` means that only outside collaborators with [insecure 2FA methods](https://docs.github.com/organizations/keeping-your-organization-secure/managing-two-factor-authentication-for-your-organization/requiring-two-factor-authentication-in-your-organization#requiring-secure-methods-of-two-factor-authentication-in-your-organization) will be returned.
"""
enum queryInput_orgs_list_outside_collaborators_filter {
  _2fa_disabled
  _2fa_insecure
  all
}

"""
The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry.
"""
enum queryInput_packages_list_packages_for_organization_package_type {
  npm
  maven
  rubygems
  docker
  nuget
  container
}

"""
The selected visibility of the packages.  This parameter is optional and only filters an existing result set.

The `internal` visibility is only supported for GitHub Packages registries that allow for granular permissions. For other ecosystems `internal` is synonymous with `private`.
For the list of GitHub Packages registries that support granular permissions, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
"""
enum package_visibility_parameter {
  public
  private
  internal
}

"""
The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry.
"""
enum package_type_parameter {
  npm
  maven
  rubygems
  docker
  nuget
  container
}

"""A version of a software package"""
type Package_Version {
  """Unique identifier of the package version."""
  id: Int!

  """The name of the package version."""
  name: String!
  url: URL!
  package_html_url: URL!
  html_url: URL
  license: String
  description: String
  created_at: DateTime!
  updated_at: DateTime!
  deleted_at: DateTime
  metadata: Package_Version_Metadata
}

type Package_Version_Metadata {
  package_type: query_packages_get_all_package_versions_for_package_owned_by_org_items_metadata_package_type!
  container: Container_Metadata
  docker: Docker_Metadata
}

enum query_packages_get_all_package_versions_for_package_owned_by_org_items_metadata_package_type {
  npm
  maven
  rubygems
  docker
  nuget
  container
}

type Container_Metadata {
  tags: [String]!
}

type Docker_Metadata {
  tag: [String]
  tags: JSON!
}

"""The state of the package, either active or deleted."""
enum queryInput_packages_get_all_package_versions_for_package_owned_by_org_state {
  active
  deleted
}

"""
Minimal representation of an organization programmatic access grant request for enumerations
"""
type Simple_Organization_Programmatic_Access_Grant_Request {
  """
  Unique identifier of the request for access via fine-grained personal access token. The `pat_request_id` used to review PAT requests.
  """
  id: Int!

  """Reason for requesting access."""
  reason: String
  owner: simple_user!
  repository_selection: query_orgs_list_pat_grant_requests_items_repository_selection!

  """
  URL to the list of repositories requested to be accessed via fine-grained personal access token. Should only be followed when `repository_selection` is `subset`.
  """
  repositories_url: String!
  permissions: query_orgs_list_pat_grant_requests_items_permissions!

  """Date and time when the request for access was created."""
  created_at: String!

  """
  Unique identifier of the user's token. This field can also be found in audit log events and the organization's settings for their PAT grants.
  """
  token_id: Int!

  """
  The name given to the user's token. This field can also be found in an organization's settings page for Active Tokens.
  """
  token_name: String!

  """Whether the associated fine-grained personal access token has expired."""
  token_expired: Boolean!

  """
  Date and time when the associated fine-grained personal access token expires.
  """
  token_expires_at: String

  """
  Date and time when the associated fine-grained personal access token was last used for authentication.
  """
  token_last_used_at: String
}

"""Type of repository selection requested."""
enum query_orgs_list_pat_grant_requests_items_repository_selection {
  none
  all
  subset
}

"""Permissions requested, categorized by type of permission."""
type query_orgs_list_pat_grant_requests_items_permissions {
  organization: JSON
  repository: JSON
  other: JSON
}

enum personal_access_token_sort_parameter {
  created_at
}

"""
Minimal representation of an organization programmatic access grant for enumerations
"""
type Organization_Programmatic_Access_Grant {
  """
  Unique identifier of the fine-grained personal access token grant. The `pat_id` used to get details about an approved fine-grained personal access token.
  """
  id: Int!
  owner: simple_user!
  repository_selection: query_orgs_list_pat_grants_items_repository_selection!

  """
  URL to the list of repositories the fine-grained personal access token can access. Only follow when `repository_selection` is `subset`.
  """
  repositories_url: String!
  permissions: query_orgs_list_pat_grants_items_permissions!

  """
  Date and time when the fine-grained personal access token was approved to access the organization.
  """
  access_granted_at: String!

  """
  Unique identifier of the user's token. This field can also be found in audit log events and the organization's settings for their PAT grants.
  """
  token_id: Int!

  """
  The name given to the user's token. This field can also be found in an organization's settings page for Active Tokens.
  """
  token_name: String!

  """Whether the associated fine-grained personal access token has expired."""
  token_expired: Boolean!

  """
  Date and time when the associated fine-grained personal access token expires.
  """
  token_expires_at: String

  """
  Date and time when the associated fine-grained personal access token was last used for authentication.
  """
  token_last_used_at: String
}

"""Type of repository selection requested."""
enum query_orgs_list_pat_grants_items_repository_selection {
  none
  all
  subset
}

"""Permissions requested, categorized by type of permission."""
type query_orgs_list_pat_grants_items_permissions {
  organization: JSON
  repository: JSON
  other: JSON
}

type private_registries_list_org_private_registries_200_response {
  total_count: Int!
  configurations: [org_private_registry_configuration]!
}

"""Private registry configuration for an organization"""
type org_private_registry_configuration {
  """The name of the private registry configuration."""
  name: String!
  registry_type: query_private_registries_list_org_private_registries_configurations_items_registry_type!

  """The URL of the private registry."""
  url: URL

  """The username to use when authenticating with the private registry."""
  username: String

  """
  Whether this private registry replaces the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When `true`, Dependabot will only use this registry and will not fall back to the public registry. When `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages.
  """
  replaces_base: Boolean
  visibility: query_private_registries_list_org_private_registries_configurations_items_visibility!
  created_at: DateTime!
  updated_at: DateTime!
}

"""The registry type."""
enum query_private_registries_list_org_private_registries_configurations_items_registry_type {
  maven_repository
  nuget_feed
  goproxy_server
  npm_registry
  rubygems_server
  cargo_registry
  composer_repository
  docker_registry
  git_source
  helm_registry
  hex_organization
  hex_repository
  pub_repository
  python_index
  terraform_registry
}

"""
Which type of organization repositories have access to the private registry.
"""
enum query_private_registries_list_org_private_registries_configurations_items_visibility {
  all
  private
  selected
}

type private_registries_get_org_public_key_200_response {
  """The identifier for the key."""
  key_id: String!

  """The Base64 encoded public key."""
  key: String!
}

"""A projects v2 project"""
type Projects_v2_Project {
  """The unique identifier of the project."""
  id: Float!

  """The node ID of the project."""
  node_id: String!
  owner: simple_user!
  creator: simple_user!

  """The project title."""
  title: String!

  """A short description of the project."""
  description: String

  """Whether the project is visible to anyone with access to the owner."""
  public: Boolean!

  """The time when the project was closed."""
  closed_at: DateTime

  """The time when the project was created."""
  created_at: DateTime!

  """The time when the project was last updated."""
  updated_at: DateTime!

  """The project number."""
  number: Int!

  """A concise summary of the project."""
  short_description: String

  """The time when the project was deleted."""
  deleted_at: DateTime
  deleted_by: nullable_simple_user
  state: query_projects_list_for_org_items_state
  latest_status_update: nullable_projects_v2_status_update

  """Whether this project is a template"""
  is_template: Boolean
}

"""The current state of the project."""
enum query_projects_list_for_org_items_state {
  open
  closed
}

"""An status update belonging to a project"""
type nullable_projects_v2_status_update {
  """The unique identifier of the status update."""
  id: Float!

  """The node ID of the status update."""
  node_id: String!

  """The node ID of the project that this status update belongs to."""
  project_node_id: String
  creator: simple_user

  """The time when the status update was created."""
  created_at: DateTime!

  """The time when the status update was last updated."""
  updated_at: DateTime!
  status: query_projects_list_for_org_items_latest_status_update_status

  """The start date of the period covered by the update."""
  start_date: Date

  """The target date associated with the update."""
  target_date: Date

  """Body of the status update"""
  body: String
}

"""The current status."""
enum query_projects_list_for_org_items_latest_status_update_status {
  INACTIVE
  ON_TRACK
  AT_RISK
  OFF_TRACK
  COMPLETE
}

"""A field inside a projects v2 project"""
type Projects_v2_Field {
  """The unique identifier of the field."""
  id: Int!

  """The ID of the issue field."""
  issue_field_id: Int

  """The node ID of the field."""
  node_id: String

  """The API URL of the project that contains the field."""
  project_url: URL!

  """The name of the field."""
  name: String!
  data_type: query_projects_list_fields_for_org_items_data_type!

  """The options available for single select fields."""
  options: [projects_v2_single_select_options]
  configuration: query_projects_list_fields_for_org_items_configuration

  """The time when the field was created."""
  created_at: DateTime!

  """The time when the field was last updated."""
  updated_at: DateTime!
}

"""The field's data type."""
enum query_projects_list_fields_for_org_items_data_type {
  assignees
  linked_pull_requests
  reviewers
  labels
  milestone
  repository
  title
  text
  single_select
  number
  date
  iteration
  issue_type
  parent_issue
  sub_issues_progress
}

"""An option for a single select field"""
type projects_v2_single_select_options {
  """The unique identifier of the option."""
  id: String!
  name: query_projects_list_fields_for_org_items_options_items_name!
  description: query_projects_list_fields_for_org_items_options_items_description!

  """The color associated with the option."""
  color: String!
}

"""The display name of the option, in raw text and HTML formats."""
type query_projects_list_fields_for_org_items_options_items_name {
  raw: String!
  html: String!
}

"""The description of the option, in raw text and HTML formats."""
type query_projects_list_fields_for_org_items_options_items_description {
  raw: String!
  html: String!
}

"""Configuration for iteration fields."""
type query_projects_list_fields_for_org_items_configuration {
  """The day of the week when the iteration starts."""
  start_day: Int

  """The duration of the iteration in days."""
  duration: Int
  iterations: [projects_v2_iteration_settings]
}

"""An iteration setting for an iteration field"""
type projects_v2_iteration_settings {
  """The unique identifier of the iteration setting."""
  id: String!

  """The start date of the iteration."""
  start_date: Date!

  """The duration of the iteration in days."""
  duration: Int!
  title: query_projects_list_fields_for_org_items_configuration_iterations_items_title!

  """Whether the iteration has been completed."""
  completed: Boolean!
}

"""The iteration title, in raw text and HTML formats."""
type query_projects_list_fields_for_org_items_configuration_iterations_items_title {
  raw: String!
  html: String!
}

"""An item belonging to a project"""
type projects_v2_item_with_content {
  """The unique identifier of the project item."""
  id: Float!

  """The node ID of the project item."""
  node_id: String

  """The API URL of the project that contains this item."""
  project_url: URL
  content_type: Projects_v2_Item_Content_Type!

  """The content of the item, which varies by content type."""
  content: JSON
  creator: simple_user

  """The time when the item was created."""
  created_at: DateTime!

  """The time when the item was last updated."""
  updated_at: DateTime!

  """The time when the item was archived."""
  archived_at: DateTime

  """The API URL of this item."""
  item_url: URL

  """The fields and values associated with this item."""
  fields: [JSON]
}

"""The type of content tracked in a project item"""
enum Projects_v2_Item_Content_Type {
  Issue
  PullRequest
  DraftIssue
}

"""Custom property defined on an organization"""
type Organization_Custom_Property {
  """The name of the property"""
  property_name: String!

  """
  The URL that can be used to fetch, update, or delete info about this property via the API.
  """
  url: URL
  source_type: query_orgs_custom_properties_for_repos_get_organization_definitions_items_source_type
  value_type: query_orgs_custom_properties_for_repos_get_organization_definitions_items_value_type!

  """Whether the property is required."""
  required: Boolean
  default_value: [String]

  """Short description of the property"""
  description: String

  """
  An ordered list of the allowed values of the property.
  The property can have up to 200 allowed values.
  """
  allowed_values: [String]
  values_editable_by: query_orgs_custom_properties_for_repos_get_organization_definitions_items_values_editable_by

  """Whether setting properties values is mandatory"""
  require_explicit_values: Boolean
}

"""The source type of the property"""
enum query_orgs_custom_properties_for_repos_get_organization_definitions_items_source_type {
  organization
  enterprise
}

"""The type of the value for the property"""
enum query_orgs_custom_properties_for_repos_get_organization_definitions_items_value_type {
  string
  single_select
  multi_select
  true_false
  url
}

"""Who can edit the values of the property"""
enum query_orgs_custom_properties_for_repos_get_organization_definitions_items_values_editable_by {
  org_actors
  org_and_repo_actors
}

"""List of custom property values for a repository"""
type Organization_Repository_Custom_Property_Values {
  repository_id: Int!
  repository_name: String!
  repository_full_name: String!

  """List of custom property names and associated values"""
  properties: [Custom_Property_Value]!
}

"""Custom property name and associated value"""
type Custom_Property_Value {
  """The name of the property"""
  property_name: String!
  value: [String]
}

"""Specifies the types of repositories you want returned."""
enum queryInput_repos_list_for_org_type {
  all
  public
  private
  forks
  sources
  member
}

"""The property to sort the results by."""
enum queryInput_repos_list_for_org_sort {
  created
  updated
  pushed
  full_name
}

"""
The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`.
"""
enum queryInput_repos_list_for_org_direction {
  asc
  desc
}

"""A set of rules to apply when specified conditions are met."""
type Repository_ruleset {
  """The ID of the ruleset"""
  id: Int!

  """The name of the ruleset"""
  name: String!
  target: query_repos_get_org_rulesets_items_target
  source_type: query_repos_get_org_rulesets_items_source_type

  """The name of the source"""
  source: String!
  enforcement: repository_rule_enforcement!

  """The actors that can bypass the rules in this ruleset"""
  bypass_actors: [Repository_Ruleset_Bypass_Actor]
  current_user_can_bypass: query_repos_get_org_rulesets_items_current_user_can_bypass
  node_id: String
  _links: query_repos_get_org_rulesets_items__links
  conditions: query_repos_get_org_rulesets_items_conditions
  rules: [repository_rule]
  created_at: DateTime
  updated_at: DateTime
}

"""The target of the ruleset"""
enum query_repos_get_org_rulesets_items_target {
  branch
  tag
  push
  repository
}

"""The type of the source of the ruleset"""
enum query_repos_get_org_rulesets_items_source_type {
  Repository
  Organization
  Enterprise
}

"""
The enforcement level of the ruleset. `evaluate` allows admins to test rules before enforcing them. Admins can view insights on the Rule Insights page (`evaluate` is only available with GitHub Enterprise).
"""
enum repository_rule_enforcement {
  disabled
  active
  evaluate
}

"""An actor that can bypass rules in a ruleset"""
type Repository_Ruleset_Bypass_Actor {
  """
  The ID of the actor that can bypass a ruleset. Required for `Integration`, `RepositoryRole`, and `Team` actor types. If `actor_type` is `OrganizationAdmin`, `actor_id` is ignored. If `actor_type` is `DeployKey`, this should be null. `OrganizationAdmin` is not applicable for personal repositories.
  """
  actor_id: Int
  actor_type: query_repos_get_org_rulesets_items_bypass_actors_items_actor_type!
  bypass_mode: query_repos_get_org_rulesets_items_bypass_actors_items_bypass_mode
}

"""The type of actor that can bypass a ruleset."""
enum query_repos_get_org_rulesets_items_bypass_actors_items_actor_type {
  Integration
  OrganizationAdmin
  RepositoryRole
  Team
  DeployKey
}

"""
When the specified actor can bypass the ruleset. `pull_request` means that an actor can only bypass rules on pull requests. `pull_request` is not applicable for the `DeployKey` actor type. Also, `pull_request` is only applicable to branch rulesets. When `bypass_mode` is `exempt`, rules will not be run for that actor and a bypass audit entry will not be created.
"""
enum query_repos_get_org_rulesets_items_bypass_actors_items_bypass_mode {
  always
  pull_request
  exempt
}

"""
The bypass type of the user making the API request for this ruleset. This field is only returned when
querying the repository-level endpoint.
"""
enum query_repos_get_org_rulesets_items_current_user_can_bypass {
  always
  pull_requests_only
  never
  exempt
}

type query_repos_get_org_rulesets_items__links {
  self: query_repos_get_org_rulesets_items__links_self
  html: query_repos_get_org_rulesets_items__links_html
}

type query_repos_get_org_rulesets_items__links_self {
  """The URL of the ruleset"""
  href: String
}

type query_repos_get_org_rulesets_items__links_html {
  """The html URL of the ruleset"""
  href: String
}

type query_repos_get_org_rulesets_items_conditions {
  ref_name: query_repos_get_org_rulesets_items_conditions_anyOf_0_ref_name
}

type query_repos_get_org_rulesets_items_conditions_anyOf_0_ref_name {
  """
  Array of ref names or patterns to include. One of these patterns must match for the condition to pass. Also accepts `~DEFAULT_BRANCH` to include the default branch or `~ALL` to include all branches.
  """
  include: [String]

  """
  Array of ref names or patterns to exclude. The condition will not pass if any of these patterns match.
  """
  exclude: [String]
}

"""A repository rule."""
union repository_rule = creation | update | deletion | required_linear_history | merge_queue | required_deployments | required_signatures | pull_request | required_status_checks | non_fast_forward | commit_message_pattern | commit_author_email_pattern | committer_email_pattern | branch_name_pattern | tag_name_pattern | file_path_restriction | max_file_path_length | file_extension_restriction | max_file_size | workflows | code_scanning | copilot_code_review

"""Only allow users with bypass permission to create matching refs."""
type creation {
  type: creation_const!
}

enum creation_const {
  creation
}

"""Only allow users with bypass permission to update matching refs."""
type update {
  type: update_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_1_parameters
}

enum update_const {
  update
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_1_parameters {
  """Branch can pull changes from its upstream repository"""
  update_allows_fetch_and_merge: Boolean!
}

"""Only allow users with bypass permissions to delete matching refs."""
type deletion {
  type: deletion_const!
}

enum deletion_const {
  deletion
}

"""Prevent merge commits from being pushed to matching refs."""
type required_linear_history {
  type: required_linear_history_const!
}

enum required_linear_history_const {
  required_linear_history
}

"""Merges must be performed via a merge queue."""
type merge_queue {
  type: merge_queue_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters
}

enum merge_queue_const {
  merge_queue
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters {
  """
  Maximum time for a required status check to report a conclusion. After this much time has elapsed, checks that have not reported a conclusion will be assumed to have failed
  """
  check_response_timeout_minutes: PositiveInt!
  grouping_strategy: query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_grouping_strategy!

  """
  Limit the number of queued pull requests requesting checks and workflow runs at the same time.
  """
  max_entries_to_build: NonNegativeInt!

  """The maximum number of PRs that will be merged together in a group."""
  max_entries_to_merge: NonNegativeInt!
  merge_method: query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_merge_method!

  """The minimum number of PRs that will be merged together in a group."""
  min_entries_to_merge: NonNegativeInt!

  """
  The time merge queue should wait after the first PR is added to the queue for the minimum group size to be met. After this time has elapsed, the minimum group size will be ignored and a smaller group will be merged.
  """
  min_entries_to_merge_wait_minutes: NonNegativeInt!
}

"""
When set to ALLGREEN, the merge commit created by merge queue for each PR in the group must pass all required checks to merge. When set to HEADGREEN, only the commit at the head of the merge group, i.e. the commit containing changes from all of the PRs in the group, must pass its required checks to merge.
"""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_grouping_strategy {
  ALLGREEN
  HEADGREEN
}

"""Integers that will have a value of 0 or more."""
scalar NonNegativeInt

"""Method to use when merging changes from queued pull requests."""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_merge_method {
  MERGE
  SQUASH
  REBASE
}

"""
Choose which environments must be successfully deployed to before refs can be pushed into a ref that matches this rule.
"""
type required_deployments {
  type: required_deployments_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_5_parameters
}

enum required_deployments_const {
  required_deployments
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_5_parameters {
  """
  The environments that must be successfully deployed to before branches can be merged.
  """
  required_deployment_environments: [String]!
}

"""Commits pushed to matching refs must have verified signatures."""
type required_signatures {
  type: required_signatures_const!
}

enum required_signatures_const {
  required_signatures
}

"""
Require all commits be made to a non-target branch and submitted via a pull request before they can be merged.
"""
type pull_request {
  type: pull_request_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters
}

enum pull_request_const {
  pull_request
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters {
  """
  Array of allowed merge methods. Allowed values include `merge`, `squash`, and `rebase`. At least one option must be enabled.
  """
  allowed_merge_methods: [query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters_allowed_merge_methods_items]

  """
  New, reviewable commits pushed will dismiss previous pull request review approvals.
  """
  dismiss_stale_reviews_on_push: Boolean!

  """
  Require an approving review in pull requests that modify files that have a designated code owner.
  """
  require_code_owner_review: Boolean!

  """
  Whether the most recent reviewable push must be approved by someone other than the person who pushed it.
  """
  require_last_push_approval: Boolean!

  """
  The number of approving reviews that are required before a pull request can be merged.
  """
  required_approving_review_count: NonNegativeInt!

  """
  All conversations on code must be resolved before a pull request can be merged.
  """
  required_review_thread_resolution: Boolean!

  """
  > [!NOTE]
  > `required_reviewers` is in beta and subject to change.
  
  A collection of reviewers and associated file patterns. Each reviewer has a list of file patterns which determine the files that reviewer is required to review.
  """
  required_reviewers: [RequiredReviewerConfiguration]
}

enum query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters_allowed_merge_methods_items {
  merge
  squash
  rebase
}

"""
A reviewing team, and file patterns describing which files they must approve changes to.
"""
type RequiredReviewerConfiguration {
  """
  Array of file patterns. Pull requests which change matching files must be approved by the specified team. File patterns use fnmatch syntax.
  """
  file_patterns: [String]!

  """
  Minimum number of approvals required from the specified team. If set to zero, the team will be added to the pull request but approval is optional.
  """
  minimum_approvals: Int!
  reviewer: Reviewer!
}

"""A required reviewing team"""
type Reviewer {
  """ID of the reviewer which must review changes to matching files."""
  id: Int!
  type: Team_const!
}

enum Team_const {
  Team
}

"""
Choose which status checks must pass before the ref is updated. When enabled, commits must first be pushed to another ref where the checks pass.
"""
type required_status_checks {
  type: required_status_checks_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_8_parameters
}

enum required_status_checks_const {
  required_status_checks
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_8_parameters {
  """
  Allow repositories and branches to be created if a check would otherwise prohibit it.
  """
  do_not_enforce_on_create: Boolean

  """Status checks that are required."""
  required_status_checks: [StatusCheckConfiguration]!

  """
  Whether pull requests targeting a matching branch must be tested with the latest code. This setting will not take effect unless at least one status check is enabled.
  """
  strict_required_status_checks_policy: Boolean!
}

"""Required status check"""
type StatusCheckConfiguration {
  """The status check context name that must be present on the commit."""
  context: String!

  """
  The optional integration ID that this status check must originate from.
  """
  integration_id: Int
}

"""Prevent users with push access from force pushing to refs."""
type non_fast_forward {
  type: non_fast_forward_const!
}

enum non_fast_forward_const {
  non_fast_forward
}

"""Parameters to be used for the commit_message_pattern rule"""
type commit_message_pattern {
  type: commit_message_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters
}

enum commit_message_pattern_const {
  commit_message_pattern
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""The operator to use for matching."""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters_operator {
  starts_with
  ends_with
  contains
  regex
}

"""Parameters to be used for the commit_author_email_pattern rule"""
type commit_author_email_pattern {
  type: commit_author_email_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters
}

enum commit_author_email_pattern_const {
  commit_author_email_pattern
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""The operator to use for matching."""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters_operator {
  starts_with
  ends_with
  contains
  regex
}

"""Parameters to be used for the committer_email_pattern rule"""
type committer_email_pattern {
  type: committer_email_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters
}

enum committer_email_pattern_const {
  committer_email_pattern
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""The operator to use for matching."""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters_operator {
  starts_with
  ends_with
  contains
  regex
}

"""Parameters to be used for the branch_name_pattern rule"""
type branch_name_pattern {
  type: branch_name_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters
}

enum branch_name_pattern_const {
  branch_name_pattern
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""The operator to use for matching."""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters_operator {
  starts_with
  ends_with
  contains
  regex
}

"""Parameters to be used for the tag_name_pattern rule"""
type tag_name_pattern {
  type: tag_name_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters
}

enum tag_name_pattern_const {
  tag_name_pattern
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""The operator to use for matching."""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters_operator {
  starts_with
  ends_with
  contains
  regex
}

"""
Prevent commits that include changes in specified file and folder paths from being pushed to the commit graph. This includes absolute paths that contain file names.
"""
type file_path_restriction {
  type: file_path_restriction_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_15_parameters
}

enum file_path_restriction_const {
  file_path_restriction
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_15_parameters {
  """
  The file paths that are restricted from being pushed to the commit graph.
  """
  restricted_file_paths: [String]!
}

"""
Prevent commits that include file paths that exceed the specified character limit from being pushed to the commit graph.
"""
type max_file_path_length {
  type: max_file_path_length_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_16_parameters
}

enum max_file_path_length_const {
  max_file_path_length
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_16_parameters {
  """The maximum amount of characters allowed in file paths."""
  max_file_path_length: PositiveInt!
}

"""
Prevent commits that include files with specified file extensions from being pushed to the commit graph.
"""
type file_extension_restriction {
  type: file_extension_restriction_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_17_parameters
}

enum file_extension_restriction_const {
  file_extension_restriction
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_17_parameters {
  """
  The file extensions that are restricted from being pushed to the commit graph.
  """
  restricted_file_extensions: [String]!
}

"""
Prevent commits with individual files that exceed the specified limit from being pushed to the commit graph.
"""
type max_file_size {
  type: max_file_size_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_18_parameters
}

enum max_file_size_const {
  max_file_size
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_18_parameters {
  """
  The maximum file size allowed in megabytes. This limit does not apply to Git Large File Storage (Git LFS).
  """
  max_file_size: PositiveInt!
}

"""
Require all changes made to a targeted branch to pass the specified workflows before they can be merged.
"""
type workflows {
  type: workflows_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_19_parameters
}

enum workflows_const {
  workflows
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_19_parameters {
  """
  Allow repositories and branches to be created if a check would otherwise prohibit it.
  """
  do_not_enforce_on_create: Boolean

  """Workflows that must pass for this rule to pass."""
  workflows: [WorkflowFileReference]!
}

"""A workflow that must run for this rule to pass"""
type WorkflowFileReference {
  """The path to the workflow file"""
  path: String!

  """The ref (branch or tag) of the workflow file to use"""
  ref: String

  """The ID of the repository where the workflow is defined"""
  repository_id: Int!

  """The commit SHA of the workflow file to use"""
  sha: String
}

"""
Choose which tools must provide code scanning results before the reference is updated. When configured, code scanning must be enabled and have results for both the commit and the reference being updated.
"""
type code_scanning {
  type: code_scanning_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters
}

enum code_scanning_const {
  code_scanning
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters {
  """Tools that must provide code scanning results for this rule to pass."""
  code_scanning_tools: [CodeScanningTool]!
}

"""A tool that must provide code scanning results for this rule to pass."""
type CodeScanningTool {
  alerts_threshold: query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_code_scanning_tools_items_alerts_threshold!
  security_alerts_threshold: query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_code_scanning_tools_items_security_alerts_threshold!

  """The name of a code scanning tool"""
  tool: String!
}

"""
The severity level at which code scanning results that raise alerts block a reference update. For more information on alert severity levels, see "[About code scanning alerts](https://docs.github.com/code-security/code-scanning/managing-code-scanning-alerts/about-code-scanning-alerts#about-alert-severity-and-security-severity-levels)."
"""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_code_scanning_tools_items_alerts_threshold {
  none
  errors
  errors_and_warnings
  all
}

"""
The severity level at which code scanning results that raise security alerts block a reference update. For more information on security severity levels, see "[About code scanning alerts](https://docs.github.com/code-security/code-scanning/managing-code-scanning-alerts/about-code-scanning-alerts#about-alert-severity-and-security-severity-levels)."
"""
enum query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_code_scanning_tools_items_security_alerts_threshold {
  none
  critical
  high_or_higher
  medium_or_higher
  all
}

"""
Request Copilot code review for new pull requests automatically if the author has access to Copilot code review and their premium requests quota has not reached the limit.
"""
type copilot_code_review {
  type: copilot_code_review_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_21_parameters
}

enum copilot_code_review_const {
  copilot_code_review
}

type query_repos_get_org_rulesets_items_rules_items_oneOf_21_parameters {
  """
  Copilot automatically reviews draft pull requests before they are marked as ready for review.
  """
  review_draft_pull_requests: Boolean

  """Copilot automatically reviews each new push to the pull request."""
  review_on_push: Boolean
}

type query_repos_get_org_rule_suites_items {
  """The unique identifier of the rule insight."""
  id: Int

  """The number that identifies the user."""
  actor_id: Int

  """The handle for the GitHub user account."""
  actor_name: String

  """The first commit sha before the push evaluation."""
  before_sha: String

  """The last commit sha in the push evaluation."""
  after_sha: String

  """The ref name that the evaluation ran on."""
  ref: String

  """The ID of the repository associated with the rule evaluation."""
  repository_id: Int

  """The name of the repository without the `.git` extension."""
  repository_name: String
  pushed_at: DateTime
  result: query_repos_get_org_rule_suites_items_result
  evaluation_result: query_repos_get_org_rule_suites_items_evaluation_result
}

"""
The result of the rule evaluations for rules with the `active` enforcement status.
"""
enum query_repos_get_org_rule_suites_items_result {
  pass
  fail
  bypass
}

"""
The result of the rule evaluations for rules with the `active` and `evaluate` enforcement statuses, demonstrating whether rules would pass or fail if all rules in the rule suite were `active`.
"""
enum query_repos_get_org_rule_suites_items_evaluation_result {
  pass
  fail
  bypass
}

"""
The time period to filter by.

For example, `day` will filter for rule suites that occurred in the past 24 hours, and `week` will filter for rule suites that occurred in the past 7 days (168 hours).
"""
enum time_period_parameter {
  hour
  day
  week
  month
}

"""
The rule suite results to filter on. When specified, only suites with this result will be returned.
"""
enum rule_suite_result_parameter {
  pass
  fail
  bypass
  all
}

"""Response"""
type Rule_Suite {
  """The unique identifier of the rule insight."""
  id: Int

  """The number that identifies the user."""
  actor_id: Int

  """The handle for the GitHub user account."""
  actor_name: String

  """The previous commit SHA of the ref."""
  before_sha: String

  """The new commit SHA of the ref."""
  after_sha: String

  """The ref name that the evaluation ran on."""
  ref: String

  """The ID of the repository associated with the rule evaluation."""
  repository_id: Int

  """The name of the repository without the `.git` extension."""
  repository_name: String
  pushed_at: DateTime
  result: query_repos_get_org_rule_suite_result
  evaluation_result: query_repos_get_org_rule_suite_evaluation_result

  """Details on the evaluated rules."""
  rule_evaluations: [query_repos_get_org_rule_suite_rule_evaluations_items]
}

"""
The result of the rule evaluations for rules with the `active` enforcement status.
"""
enum query_repos_get_org_rule_suite_result {
  pass
  fail
  bypass
}

"""
The result of the rule evaluations for rules with the `active` and `evaluate` enforcement statuses, demonstrating whether rules would pass or fail if all rules in the rule suite were `active`. Null if no rules with `evaluate` enforcement status were run.
"""
enum query_repos_get_org_rule_suite_evaluation_result {
  pass
  fail
  bypass
}

type query_repos_get_org_rule_suite_rule_evaluations_items {
  rule_source: query_repos_get_org_rule_suite_rule_evaluations_items_rule_source
  enforcement: query_repos_get_org_rule_suite_rule_evaluations_items_enforcement
  result: query_repos_get_org_rule_suite_rule_evaluations_items_result

  """The type of rule."""
  rule_type: String

  """The detailed failure message for the rule. Null if the rule passed."""
  details: String
}

type query_repos_get_org_rule_suite_rule_evaluations_items_rule_source {
  """The type of rule source."""
  type: String

  """The ID of the rule source."""
  id: Int

  """The name of the rule source."""
  name: String
}

"""The enforcement level of this rule source."""
enum query_repos_get_org_rule_suite_rule_evaluations_items_enforcement {
  active
  evaluate
  deleted_ruleset
}

"""The result of the evaluation of the individual rule."""
enum query_repos_get_org_rule_suite_rule_evaluations_items_result {
  pass
  fail
}

"""The historical version of a ruleset"""
type Ruleset_version {
  """The ID of the previous version of the ruleset"""
  version_id: Int!
  actor: query_orgs_get_org_ruleset_history_items_actor!
  updated_at: DateTime!
}

"""The actor who updated the ruleset"""
type query_orgs_get_org_ruleset_history_items_actor {
  id: Int
  type: String
}

type ruleset_version_with_state {
  """The ID of the previous version of the ruleset"""
  version_id: Int!
  actor: query_orgs_get_org_ruleset_history_items_actor!
  updated_at: DateTime!
  state: JSON!
}

type organization_secret_scanning_alert {
  """The security alert number."""
  number: Int!

  """
  The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime

  """
  The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime

  """The REST API URL of the alert resource."""
  url: URL

  """The GitHub URL of the alert resource."""
  html_url: URL

  """The REST API URL of the code locations for this alert."""
  locations_url: URL
  state: secret_scanning_alert_state
  resolution: secret_scanning_alert_resolution

  """
  The time that the alert was resolved in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  resolved_at: DateTime
  resolved_by: nullable_simple_user

  """The type of secret that secret scanning detected."""
  secret_type: String

  """
  User-friendly name for the detected secret, matching the `secret_type`.
  For a list of built-in patterns, see "[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)."
  """
  secret_type_display_name: String

  """The secret that was detected."""
  secret: String
  repository: simple_repository

  """Whether push protection was bypassed for the detected secret."""
  push_protection_bypassed: Boolean
  push_protection_bypassed_by: nullable_simple_user

  """
  The time that push protection was bypassed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  push_protection_bypassed_at: DateTime
  push_protection_bypass_request_reviewer: nullable_simple_user

  """An optional comment when reviewing a push protection bypass."""
  push_protection_bypass_request_reviewer_comment: String

  """An optional comment when requesting a push protection bypass."""
  push_protection_bypass_request_comment: String

  """The URL to a push protection bypass request."""
  push_protection_bypass_request_html_url: URL

  """The comment that was optionally added when this alert was closed"""
  resolution_comment: String
  validity: query_secret_scanning_list_alerts_for_org_items_validity

  """Whether the secret was publicly leaked."""
  publicly_leaked: Boolean

  """
  Whether the detected secret was found in multiple repositories in the same organization or enterprise.
  """
  multi_repo: Boolean

  """A boolean value representing whether or not alert is base64 encoded"""
  is_base64_encoded: Boolean
  first_location_detected: nullable_secret_scanning_first_detected_location

  """
  A boolean value representing whether or not the token in the alert was detected in more than one location.
  """
  has_more_locations: Boolean
  assigned_to: nullable_simple_user
}

"""
Sets the state of the secret scanning alert. You must provide `resolution` when you set the state to `resolved`.
"""
enum secret_scanning_alert_state {
  open
  resolved
}

"""
**Required when the `state` is `resolved`.** The reason for resolving the alert.
"""
enum secret_scanning_alert_resolution {
  false_positive
  wont_fix
  revoked
  used_in_tests
}

"""The token status as of the latest validity check."""
enum query_secret_scanning_list_alerts_for_org_items_validity {
  active
  inactive
  unknown
}

"""
Details on the location where the token was initially detected. This can be a commit, wiki commit, issue, discussion, pull request.
"""
union nullable_secret_scanning_first_detected_location = secret_scanning_location_commit | secret_scanning_location_wiki_commit | secret_scanning_location_issue_title | secret_scanning_location_issue_body | secret_scanning_location_issue_comment | secret_scanning_location_discussion_title | secret_scanning_location_discussion_body | secret_scanning_location_discussion_comment | secret_scanning_location_pull_request_title | secret_scanning_location_pull_request_body | secret_scanning_location_pull_request_comment | secret_scanning_location_pull_request_review | secret_scanning_location_pull_request_review_comment

"""
Represents a 'commit' secret scanning location type. This location type shows that a secret was detected inside a commit to a repository.
"""
type secret_scanning_location_commit {
  """The file path in the repository"""
  path: String!

  """Line number at which the secret starts in the file"""
  start_line: Float!

  """Line number at which the secret ends in the file"""
  end_line: Float!

  """
  The column at which the secret starts within the start line when the file is interpreted as 8BIT ASCII
  """
  start_column: Float!

  """
  The column at which the secret ends within the end line when the file is interpreted as 8BIT ASCII
  """
  end_column: Float!

  """SHA-1 hash ID of the associated blob"""
  blob_sha: String!

  """The API URL to get the associated blob resource"""
  blob_url: String!

  """SHA-1 hash ID of the associated commit"""
  commit_sha: String!

  """The API URL to get the associated commit resource"""
  commit_url: String!
}

"""
Represents a 'wiki_commit' secret scanning location type. This location type shows that a secret was detected inside a commit to a repository wiki.
"""
type secret_scanning_location_wiki_commit {
  """The file path of the wiki page"""
  path: String!

  """Line number at which the secret starts in the file"""
  start_line: Float!

  """Line number at which the secret ends in the file"""
  end_line: Float!

  """
  The column at which the secret starts within the start line when the file is interpreted as 8-bit ASCII.
  """
  start_column: Float!

  """
  The column at which the secret ends within the end line when the file is interpreted as 8-bit ASCII.
  """
  end_column: Float!

  """SHA-1 hash ID of the associated blob"""
  blob_sha: String!

  """The GitHub URL to get the associated wiki page"""
  page_url: URL!

  """SHA-1 hash ID of the associated commit"""
  commit_sha: String!

  """The GitHub URL to get the associated wiki commit"""
  commit_url: URL!
}

"""
Represents an 'issue_title' secret scanning location type. This location type shows that a secret was detected in the title of an issue.
"""
type secret_scanning_location_issue_title {
  """The API URL to get the issue where the secret was detected."""
  issue_title_url: URL!
}

"""
Represents an 'issue_body' secret scanning location type. This location type shows that a secret was detected in the body of an issue.
"""
type secret_scanning_location_issue_body {
  """The API URL to get the issue where the secret was detected."""
  issue_body_url: URL!
}

"""
Represents an 'issue_comment' secret scanning location type. This location type shows that a secret was detected in a comment on an issue.
"""
type secret_scanning_location_issue_comment {
  """The API URL to get the issue comment where the secret was detected."""
  issue_comment_url: URL!
}

"""
Represents a 'discussion_title' secret scanning location type. This location type shows that a secret was detected in the title of a discussion.
"""
type secret_scanning_location_discussion_title {
  """The URL to the discussion where the secret was detected."""
  discussion_title_url: URL!
}

"""
Represents a 'discussion_body' secret scanning location type. This location type shows that a secret was detected in the body of a discussion.
"""
type secret_scanning_location_discussion_body {
  """The URL to the discussion where the secret was detected."""
  discussion_body_url: URL!
}

"""
Represents a 'discussion_comment' secret scanning location type. This location type shows that a secret was detected in a comment on a discussion.
"""
type secret_scanning_location_discussion_comment {
  """
  The API URL to get the discussion comment where the secret was detected.
  """
  discussion_comment_url: URL!
}

"""
Represents a 'pull_request_title' secret scanning location type. This location type shows that a secret was detected in the title of a pull request.
"""
type secret_scanning_location_pull_request_title {
  """The API URL to get the pull request where the secret was detected."""
  pull_request_title_url: URL!
}

"""
Represents a 'pull_request_body' secret scanning location type. This location type shows that a secret was detected in the body of a pull request.
"""
type secret_scanning_location_pull_request_body {
  """The API URL to get the pull request where the secret was detected."""
  pull_request_body_url: URL!
}

"""
Represents a 'pull_request_comment' secret scanning location type. This location type shows that a secret was detected in a comment on a pull request.
"""
type secret_scanning_location_pull_request_comment {
  """
  The API URL to get the pull request comment where the secret was detected.
  """
  pull_request_comment_url: URL!
}

"""
Represents a 'pull_request_review' secret scanning location type. This location type shows that a secret was detected in a review on a pull request.
"""
type secret_scanning_location_pull_request_review {
  """
  The API URL to get the pull request review where the secret was detected.
  """
  pull_request_review_url: URL!
}

"""
Represents a 'pull_request_review_comment' secret scanning location type. This location type shows that a secret was detected in a review comment on a pull request.
"""
type secret_scanning_location_pull_request_review_comment {
  """
  The API URL to get the pull request review comment where the secret was detected.
  """
  pull_request_review_comment_url: URL!
}

"""
Set to `open` or `resolved` to only list secret scanning alerts in a specific state.
"""
enum secret_scanning_alert_state_parameter {
  open
  resolved
}

"""
The property to sort the results by. `created` means when the alert was created. `updated` means when the alert was updated or resolved.
"""
enum secret_scanning_alert_sort_parameter {
  created
  updated
}

"""
A collection of secret scanning patterns and their settings related to push protection.
"""
type Secret_scanning_pattern_configuration {
  """
  The version of the entity. This is used to confirm you're updating the current version of the entity and mitigate unintentionally overriding someone else's update.
  """
  pattern_config_version: String

  """Overrides for partner patterns."""
  provider_pattern_overrides: [secret_scanning_pattern_override]

  """Overrides for custom patterns defined by the organization."""
  custom_pattern_overrides: [secret_scanning_pattern_override]
}

type secret_scanning_pattern_override {
  """The ID of the pattern."""
  token_type: String

  """The version of this pattern if it's a custom pattern."""
  custom_pattern_version: String

  """The slug of the pattern."""
  slug: String

  """The user-friendly name for the pattern."""
  display_name: String

  """The total number of alerts generated by this pattern."""
  alert_total: Int

  """
  The percentage of all alerts that this pattern represents, rounded to the nearest integer.
  """
  alert_total_percentage: Int

  """The number of false positive alerts generated by this pattern."""
  false_positives: Int

  """
  The percentage of alerts from this pattern that are false positives, rounded to the nearest integer.
  """
  false_positive_rate: Int

  """
  The percentage of blocks for this pattern that were bypassed, rounded to the nearest integer.
  """
  bypass_rate: Int
  default_setting: query_secret_scanning_list_org_pattern_configs_provider_pattern_overrides_items_default_setting
  enterprise_setting: query_secret_scanning_list_org_pattern_configs_provider_pattern_overrides_items_enterprise_setting
  setting: query_secret_scanning_list_org_pattern_configs_provider_pattern_overrides_items_setting
}

"""The default push protection setting for this pattern."""
enum query_secret_scanning_list_org_pattern_configs_provider_pattern_overrides_items_default_setting {
  disabled
  enabled
}

"""
The push protection setting for this pattern set at the enterprise level. Only present for partner patterns when the organization has a parent enterprise.
"""
enum query_secret_scanning_list_org_pattern_configs_provider_pattern_overrides_items_enterprise_setting {
  not_set
  disabled
  enabled
}

"""
The current push protection setting for this pattern. If this is `not-set`, then it inherits either the enterprise setting if it exists or the default setting.
"""
enum query_secret_scanning_list_org_pattern_configs_provider_pattern_overrides_items_setting {
  not_set
  disabled
  enabled
}

"""A repository security advisory."""
type repository_advisory {
  """The GitHub Security Advisory ID."""
  ghsa_id: String!

  """The Common Vulnerabilities and Exposures (CVE) ID."""
  cve_id: String

  """The API URL for the advisory."""
  url: URL!

  """The URL for the advisory."""
  html_url: URL!

  """A short summary of the advisory."""
  summary: query_security_advisories_list_org_repository_advisories_items_summary!

  """A detailed description of what the advisory entails."""
  description: query_security_advisories_list_org_repository_advisories_items_description
  severity: query_security_advisories_list_org_repository_advisories_items_severity
  author: simple_user!
  publisher: simple_user!
  identifiers: [query_security_advisories_list_org_repository_advisories_items_identifiers_items]!
  state: query_security_advisories_list_org_repository_advisories_items_state!

  """
  The date and time of when the advisory was created, in ISO 8601 format.
  """
  created_at: DateTime

  """
  The date and time of when the advisory was last updated, in ISO 8601 format.
  """
  updated_at: DateTime

  """
  The date and time of when the advisory was published, in ISO 8601 format.
  """
  published_at: DateTime

  """The date and time of when the advisory was closed, in ISO 8601 format."""
  closed_at: DateTime

  """
  The date and time of when the advisory was withdrawn, in ISO 8601 format.
  """
  withdrawn_at: DateTime
  submission: query_security_advisories_list_org_repository_advisories_items_submission
  vulnerabilities: [repository_advisory_vulnerability]
  cvss: query_security_advisories_list_org_repository_advisories_items_cvss
  cvss_severities: cvss_severities
  cwes: [query_security_advisories_list_org_repository_advisories_items_cwes_items]

  """A list of only the CWE IDs."""
  cwe_ids: [String]
  credits: [query_security_advisories_list_org_repository_advisories_items_credits_items]
  credits_detailed: [repository_advisory_credit]

  """A list of users that collaborate on the advisory."""
  collaborating_users: [simple_user]

  """A list of teams that collaborate on the advisory."""
  collaborating_teams: [team]
  private_fork: simple_repository!
}

"""A short summary of the advisory."""
scalar query_security_advisories_list_org_repository_advisories_items_summary

"""A detailed description of what the advisory entails."""
scalar query_security_advisories_list_org_repository_advisories_items_description

"""The severity of the advisory."""
enum query_security_advisories_list_org_repository_advisories_items_severity {
  critical
  high
  medium
  low
}

type query_security_advisories_list_org_repository_advisories_items_identifiers_items {
  type: query_security_advisories_list_org_repository_advisories_items_identifiers_items_type!

  """The identifier value."""
  value: String!
}

"""The type of identifier."""
enum query_security_advisories_list_org_repository_advisories_items_identifiers_items_type {
  CVE
  GHSA
}

"""The state of the advisory."""
enum query_security_advisories_list_org_repository_advisories_items_state {
  published
  closed
  withdrawn
  draft
  triage
}

type query_security_advisories_list_org_repository_advisories_items_submission {
  """
  Whether a private vulnerability report was accepted by the repository's administrators.
  """
  accepted: Boolean!
}

"""
A product affected by the vulnerability detailed in a repository security advisory.
"""
type repository_advisory_vulnerability {
  package: query_security_advisories_list_org_repository_advisories_items_vulnerabilities_items_package

  """The range of the package versions affected by the vulnerability."""
  vulnerable_version_range: String

  """The package version(s) that resolve the vulnerability."""
  patched_versions: String

  """The functions in the package that are affected."""
  vulnerable_functions: [String]
}

"""The name of the package affected by the vulnerability."""
type query_security_advisories_list_org_repository_advisories_items_vulnerabilities_items_package {
  ecosystem: security_advisory_ecosystems!

  """The unique package name within its ecosystem."""
  name: String
}

type query_security_advisories_list_org_repository_advisories_items_cvss {
  """The CVSS vector."""
  vector_string: String

  """The CVSS score."""
  score: NonNegativeFloat
}

type query_security_advisories_list_org_repository_advisories_items_cwes_items {
  """The Common Weakness Enumeration (CWE) identifier."""
  cwe_id: String!

  """The name of the CWE."""
  name: String!
}

type query_security_advisories_list_org_repository_advisories_items_credits_items {
  """The username of the user credited."""
  login: String
  type: security_advisory_credit_types
}

"""A credit given to a user for a repository security advisory."""
type repository_advisory_credit {
  user: simple_user!
  type: security_advisory_credit_types!
  state: query_security_advisories_list_org_repository_advisories_items_credits_detailed_items_state!
}

"""The state of the user's acceptance of the credit."""
enum query_security_advisories_list_org_repository_advisories_items_credits_detailed_items_state {
  accepted
  declined
  pending
}

"""The property to sort the results by."""
enum queryInput_security_advisories_list_org_repository_advisories_sort {
  created
  updated
  published
}

"""
Filter by the state of the repository advisories. Only advisories of this state will be returned.
"""
enum queryInput_security_advisories_list_org_repository_advisories_state {
  triage
  draft
  published
  closed
}

"""Check immutable releases settings for an organization."""
type Check_immutable_releases_organization_settings {
  enforced_repositories: query_orgs_get_immutable_releases_settings_enforced_repositories!

  """
  The API URL to use to get or set the selected repositories for immutable releases enforcement, when `enforced_repositories` is set to `selected`.
  """
  selected_repositories_url: String
}

"""
The policy that controls how immutable releases are enforced in the organization.
"""
enum query_orgs_get_immutable_releases_settings_enforced_repositories {
  all
  none
  selected
}

type orgs_get_immutable_releases_settings_repositories_200_response {
  total_count: Int!
  repositories: [minimal_repository]!
}

type hosted_compute_list_network_configurations_for_org_200_response {
  total_count: Int!
  network_configurations: [Hosted_compute_network_configuration]!
}

"""A hosted compute network configuration."""
type Hosted_compute_network_configuration {
  """The unique identifier of the network configuration."""
  id: String!

  """The name of the network configuration."""
  name: String!
  compute_service: query_hosted_compute_list_network_configurations_for_org_network_configurations_items_compute_service

  """The unique identifier of each network settings in the configuration."""
  network_settings_ids: [String]

  """
  The unique identifier of each failover network settings in the configuration.
  """
  failover_network_settings_ids: [String]

  """Indicates whether the failover network resource is enabled."""
  failover_network_enabled: Boolean

  """
  The time at which the network configuration was created, in ISO 8601 format.
  """
  created_on: DateTime
}

"""The hosted compute service the network configuration supports."""
enum query_hosted_compute_list_network_configurations_for_org_network_configurations_items_compute_service {
  none
  actions
  codespaces
}

"""A hosted compute network settings resource."""
type Hosted_compute_network_settings_resource {
  """The unique identifier of the network settings resource."""
  id: String!

  """
  The identifier of the network configuration that is using this settings resource.
  """
  network_configuration_id: String

  """The name of the network settings resource."""
  name: String!

  """The subnet this network settings resource is configured for."""
  subnet_id: String!

  """
  The location of the subnet this network settings resource is configured for.
  """
  region: String!
}

"""
Filter team results by their type. For more information, see "[What kind of team should I use?](https://docs.github.com/enterprise-cloud@latest/admin/concepts/enterprise-fundamentals/teams-in-an-enterprise#what-kind-of-team-should-i-use)"
"""
enum team_type_parameter {
  all
  enterprise
  organization
}

"""
Groups of organization members that gives permissions on specified repositories.
"""
type Full_Team {
  """Unique identifier of the team"""
  id: Int!
  node_id: String!

  """URL for the team"""
  url: URL!
  html_url: URL!

  """Name of the team"""
  name: String!
  slug: String!
  description: String
  privacy: query_teams_get_by_name_privacy
  notification_setting: query_teams_get_by_name_notification_setting

  """Permission that the team will have for its repositories"""
  permission: String!
  members_url: URL!
  repositories_url: URL!
  parent: nullable_team_simple
  members_count: Int!
  repos_count: Int!
  created_at: DateTime!
  updated_at: DateTime!
  organization: Team_Organization!

  """
  The [distinguished name](https://www.ldap.com/ldap-dns-and-rdns) (DN) of the LDAP entry to map to a team.
  """
  ldap_dn: String
  type: query_teams_get_by_name_type!

  """Unique identifier of the organization to which this team belongs"""
  organization_id: Int

  """Unique identifier of the enterprise to which this team belongs"""
  enterprise_id: Int
}

"""The level of privacy this team should have"""
enum query_teams_get_by_name_privacy {
  closed
  secret
}

"""The notification setting the team has set"""
enum query_teams_get_by_name_notification_setting {
  notifications_enabled
  notifications_disabled
}

"""Team Organization"""
type Team_Organization {
  login: String!
  id: Int!
  node_id: String!
  url: URL!
  repos_url: URL!
  events_url: URL!
  hooks_url: URL!
  issues_url: URL!
  members_url: URL!
  public_members_url: URL!
  avatar_url: URL!
  description: String
  name: String
  company: String
  blog: URL
  location: String
  email: EmailAddress
  twitter_username: String
  is_verified: Boolean
  has_organization_projects: Boolean!
  has_repository_projects: Boolean!
  public_repos: Int!
  public_gists: Int!
  followers: Int!
  following: Int!
  html_url: URL!
  created_at: DateTime!
  type: String!
  total_private_repos: Int
  owned_private_repos: Int
  private_gists: Int
  disk_usage: Int
  collaborators: Int
  billing_email: EmailAddress
  plan: query_teams_get_by_name_organization_plan
  default_repository_permission: String
  members_can_create_repositories: Boolean
  two_factor_requirement_enabled: Boolean
  members_allowed_repository_creation_type: String
  members_can_create_public_repositories: Boolean
  members_can_create_private_repositories: Boolean
  members_can_create_internal_repositories: Boolean
  members_can_create_pages: Boolean
  members_can_create_public_pages: Boolean
  members_can_create_private_pages: Boolean
  members_can_fork_private_repositories: Boolean
  web_commit_signoff_required: Boolean
  updated_at: DateTime!
  archived_at: DateTime
}

type query_teams_get_by_name_organization_plan {
  name: String!
  space: Int!
  private_repos: Int!
  filled_seats: Int
  seats: Int
}

"""The ownership type of the team"""
enum query_teams_get_by_name_type {
  enterprise
  organization
}

"""Filters members returned by their role in the team."""
enum queryInput_teams_list_members_in_org_role {
  member
  maintainer
  all
}

"""Team Membership"""
type Team_Membership {
  url: URL!
  role: query_teams_get_membership_for_user_in_org_role!
  state: query_teams_get_membership_for_user_in_org_state!
}

"""The role of the user in the team."""
enum query_teams_get_membership_for_user_in_org_role {
  member
  maintainer
}

"""The state of the user's membership in the team."""
enum query_teams_get_membership_for_user_in_org_state {
  active
  pending
}

union teams_check_permissions_for_repo_in_org_response = Team_Repository | Void_container

"""A team's access to a repository."""
type Team_Repository {
  """Unique identifier of the repository"""
  id: Int!
  node_id: String!

  """The name of the repository."""
  name: String!
  full_name: String!
  license: nullable_license_simple
  forks: Int!
  permissions: query_teams_check_permissions_for_repo_in_org_oneOf_0_permissions
  role_name: String
  owner: nullable_simple_user

  """Whether the repository is private or public."""
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  archive_url: URL!
  assignees_url: URL!
  blobs_url: URL!
  branches_url: URL!
  collaborators_url: URL!
  comments_url: URL!
  commits_url: URL!
  compare_url: URL!
  contents_url: URL!
  contributors_url: URL!
  deployments_url: URL!
  downloads_url: URL!
  events_url: URL!
  forks_url: URL!
  git_commits_url: URL!
  git_refs_url: URL!
  git_tags_url: URL!
  git_url: URL!
  issue_comment_url: URL!
  issue_events_url: URL!
  issues_url: URL!
  keys_url: URL!
  labels_url: URL!
  languages_url: URL!
  merges_url: URL!
  milestones_url: URL!
  notifications_url: URL!
  pulls_url: URL!
  releases_url: URL!
  ssh_url: String!
  stargazers_url: URL!
  statuses_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  tags_url: URL!
  teams_url: URL!
  trees_url: URL!
  clone_url: URL!
  mirror_url: URL
  hooks_url: URL!
  svn_url: URL!
  homepage: URL
  language: String
  forks_count: Int!
  stargazers_count: Int!
  watchers_count: Int!
  size: Int!

  """The default branch of the repository."""
  default_branch: String!
  open_issues_count: Int!

  """
  Whether this repository acts as a template that can be used to generate new repositories.
  """
  is_template: Boolean
  topics: [String]

  """Whether issues are enabled."""
  has_issues: Boolean!

  """Whether projects are enabled."""
  has_projects: Boolean!

  """Whether the wiki is enabled."""
  has_wiki: Boolean!
  has_pages: Boolean!

  """Whether downloads are enabled."""
  has_downloads: Boolean!

  """Whether the repository is archived."""
  archived: Boolean!

  """Returns whether or not this repository disabled."""
  disabled: Boolean!

  """The repository visibility: public, private, or internal."""
  visibility: String
  pushed_at: DateTime
  created_at: DateTime
  updated_at: DateTime

  """Whether to allow rebase merges for pull requests."""
  allow_rebase_merge: Boolean
  temp_clone_token: String

  """Whether to allow squash merges for pull requests."""
  allow_squash_merge: Boolean

  """Whether to allow Auto-merge to be used on pull requests."""
  allow_auto_merge: Boolean

  """Whether to delete head branches when pull requests are merged"""
  delete_branch_on_merge: Boolean

  """Whether to allow merge commits for pull requests."""
  allow_merge_commit: Boolean

  """Whether to allow forking this repo"""
  allow_forking: Boolean

  """Whether to require contributors to sign off on web-based commits"""
  web_commit_signoff_required: Boolean
  subscribers_count: Int
  network_count: Int
  open_issues: Int!
  watchers: Int!
  master_branch: String
}

type query_teams_check_permissions_for_repo_in_org_oneOf_0_permissions {
  admin: Boolean!
  pull: Boolean!
  triage: Boolean
  push: Boolean!
  maintain: Boolean
}

type Void_container {
  Void: Void
}

"""Rate Limit Overview"""
type Rate_Limit_Overview {
  resources: query_rate_limit_get_resources!
  rate: Rate_Limit!
}

type query_rate_limit_get_resources {
  core: Rate_Limit!
  graphql: Rate_Limit
  search: Rate_Limit!
  code_search: Rate_Limit
  source_import: Rate_Limit
  integration_manifest: Rate_Limit
  code_scanning_upload: Rate_Limit
  actions_runner_registration: Rate_Limit
  scim: Rate_Limit
  dependency_snapshots: Rate_Limit
  dependency_sbom: Rate_Limit
  code_scanning_autofix: Rate_Limit
}

type Rate_Limit {
  limit: Int!
  remaining: Int!
  reset: Int!
  used: Int!
}

"""Full Repository"""
type Full_Repository {
  id: BigInt!
  node_id: String!
  name: String!
  full_name: String!
  owner: simple_user!
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  archive_url: URL!
  assignees_url: URL!
  blobs_url: URL!
  branches_url: URL!
  collaborators_url: URL!
  comments_url: URL!
  commits_url: URL!
  compare_url: URL!
  contents_url: URL!
  contributors_url: URL!
  deployments_url: URL!
  downloads_url: URL!
  events_url: URL!
  forks_url: URL!
  git_commits_url: URL!
  git_refs_url: URL!
  git_tags_url: URL!
  git_url: URL!
  issue_comment_url: URL!
  issue_events_url: URL!
  issues_url: URL!
  keys_url: URL!
  labels_url: URL!
  languages_url: URL!
  merges_url: URL!
  milestones_url: URL!
  notifications_url: URL!
  pulls_url: URL!
  releases_url: URL!
  ssh_url: String!
  stargazers_url: URL!
  statuses_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  tags_url: URL!
  teams_url: URL!
  trees_url: URL!
  clone_url: URL!
  mirror_url: URL
  hooks_url: URL!
  svn_url: URL!
  homepage: URL
  language: String
  forks_count: Int!
  stargazers_count: Int!
  watchers_count: Int!

  """
  The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0.
  """
  size: Int!
  default_branch: String!
  open_issues_count: Int!
  is_template: Boolean
  topics: [String]
  has_issues: Boolean!
  has_projects: Boolean!
  has_wiki: Boolean!
  has_pages: Boolean!
  has_downloads: Boolean
  has_discussions: Boolean!
  has_pull_requests: Boolean
  pull_request_creation_policy: query_repos_get_pull_request_creation_policy
  has_commit_comments: Boolean
  archived: Boolean!

  """Returns whether or not this repository disabled."""
  disabled: Boolean!

  """The repository visibility: public, private, or internal."""
  visibility: String
  pushed_at: DateTime!
  created_at: DateTime!
  updated_at: DateTime!
  permissions: query_repos_get_permissions
  allow_rebase_merge: Boolean
  template_repository: nullable_repository
  temp_clone_token: String
  allow_squash_merge: Boolean
  allow_auto_merge: Boolean
  delete_branch_on_merge: Boolean
  allow_merge_commit: Boolean
  allow_update_branch: Boolean
  use_squash_pr_title_as_default: Boolean
  squash_merge_commit_title: query_repos_get_squash_merge_commit_title
  squash_merge_commit_message: query_repos_get_squash_merge_commit_message
  merge_commit_title: query_repos_get_merge_commit_title
  merge_commit_message: query_repos_get_merge_commit_message
  allow_forking: Boolean
  web_commit_signoff_required: Boolean
  subscribers_count: Int!
  network_count: Int!
  license: nullable_license_simple
  organization: nullable_simple_user
  parent: repository
  source: repository
  forks: Int!
  master_branch: String
  open_issues: Int!
  watchers: Int!

  """Whether anonymous git access is allowed."""
  anonymous_access_enabled: Boolean
  code_of_conduct: code_of_conduct_simple
  security_and_analysis: security_and_analysis

  """
  The custom properties that were defined for the repository. The keys are the custom property names, and the values are the corresponding custom property values.
  """
  custom_properties: JSON
}

"""
The policy controlling who can create pull requests: all or collaborators_only.
"""
enum query_repos_get_pull_request_creation_policy {
  all
  collaborators_only
}

type query_repos_get_permissions {
  admin: Boolean!
  maintain: Boolean
  push: Boolean!
  triage: Boolean
  pull: Boolean!
}

"""A repository on GitHub."""
type nullable_repository {
  """Unique identifier of the repository"""
  id: BigInt!
  node_id: String!

  """The name of the repository."""
  name: String!
  full_name: String!
  license: nullable_license_simple
  forks: Int!
  permissions: query_repos_get_template_repository_permissions
  owner: simple_user!

  """Whether the repository is private or public."""
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  archive_url: URL!
  assignees_url: URL!
  blobs_url: URL!
  branches_url: URL!
  collaborators_url: URL!
  comments_url: URL!
  commits_url: URL!
  compare_url: URL!
  contents_url: URL!
  contributors_url: URL!
  deployments_url: URL!
  downloads_url: URL!
  events_url: URL!
  forks_url: URL!
  git_commits_url: URL!
  git_refs_url: URL!
  git_tags_url: URL!
  git_url: URL!
  issue_comment_url: URL!
  issue_events_url: URL!
  issues_url: URL!
  keys_url: URL!
  labels_url: URL!
  languages_url: URL!
  merges_url: URL!
  milestones_url: URL!
  notifications_url: URL!
  pulls_url: URL!
  releases_url: URL!
  ssh_url: String!
  stargazers_url: URL!
  statuses_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  tags_url: URL!
  teams_url: URL!
  trees_url: URL!
  clone_url: URL!
  mirror_url: URL
  hooks_url: URL!
  svn_url: URL!
  homepage: URL
  language: String
  forks_count: Int!
  stargazers_count: Int!
  watchers_count: Int!

  """
  The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0.
  """
  size: Int!

  """The default branch of the repository."""
  default_branch: String!
  open_issues_count: Int!

  """
  Whether this repository acts as a template that can be used to generate new repositories.
  """
  is_template: Boolean
  topics: [String]

  """Whether issues are enabled."""
  has_issues: Boolean!

  """Whether projects are enabled."""
  has_projects: Boolean!

  """Whether the wiki is enabled."""
  has_wiki: Boolean!
  has_pages: Boolean!

  """Whether downloads are enabled."""
  has_downloads: Boolean! @deprecated(reason: "deprecated")

  """Whether discussions are enabled."""
  has_discussions: Boolean

  """Whether pull requests are enabled."""
  has_pull_requests: Boolean
  pull_request_creation_policy: query_repos_get_template_repository_pull_request_creation_policy

  """Whether commit comments are enabled."""
  has_commit_comments: Boolean

  """Whether the repository is archived."""
  archived: Boolean!

  """Returns whether or not this repository disabled."""
  disabled: Boolean!

  """The repository visibility: public, private, or internal."""
  visibility: String
  pushed_at: DateTime
  created_at: DateTime
  updated_at: DateTime

  """Whether to allow rebase merges for pull requests."""
  allow_rebase_merge: Boolean
  temp_clone_token: String

  """Whether to allow squash merges for pull requests."""
  allow_squash_merge: Boolean

  """Whether to allow Auto-merge to be used on pull requests."""
  allow_auto_merge: Boolean

  """Whether to delete head branches when pull requests are merged"""
  delete_branch_on_merge: Boolean

  """
  Whether or not a pull request head branch that is behind its base branch can always be updated even if it is not required to be up to date before merging.
  """
  allow_update_branch: Boolean

  """
  Whether a squash merge commit can use the pull request title as default. **This property is closing down. Please use `squash_merge_commit_title` instead.
  """
  use_squash_pr_title_as_default: Boolean @deprecated(reason: "deprecated")
  squash_merge_commit_title: query_repos_get_template_repository_squash_merge_commit_title
  squash_merge_commit_message: query_repos_get_template_repository_squash_merge_commit_message
  merge_commit_title: query_repos_get_template_repository_merge_commit_title
  merge_commit_message: query_repos_get_template_repository_merge_commit_message

  """Whether to allow merge commits for pull requests."""
  allow_merge_commit: Boolean

  """Whether to allow forking this repo"""
  allow_forking: Boolean

  """Whether to require contributors to sign off on web-based commits"""
  web_commit_signoff_required: Boolean
  open_issues: Int!
  watchers: Int!
  master_branch: String
  starred_at: String

  """Whether anonymous git access is enabled for this repository"""
  anonymous_access_enabled: Boolean
  code_search_index_status: query_repos_get_template_repository_code_search_index_status
}

type query_repos_get_template_repository_permissions {
  admin: Boolean!
  pull: Boolean!
  triage: Boolean
  push: Boolean!
  maintain: Boolean
}

"""
The policy controlling who can create pull requests: all or collaborators_only.
"""
enum query_repos_get_template_repository_pull_request_creation_policy {
  all
  collaborators_only
}

"""
The default value for a squash merge commit title:

- `PR_TITLE` - default to the pull request's title.
- `COMMIT_OR_PR_TITLE` - default to the commit's title (if only one commit) or the pull request's title (when more than one commit).
"""
enum query_repos_get_template_repository_squash_merge_commit_title {
  PR_TITLE
  COMMIT_OR_PR_TITLE
}

"""
The default value for a squash merge commit message:

- `PR_BODY` - default to the pull request's body.
- `COMMIT_MESSAGES` - default to the branch's commit messages.
- `BLANK` - default to a blank commit message.
"""
enum query_repos_get_template_repository_squash_merge_commit_message {
  PR_BODY
  COMMIT_MESSAGES
  BLANK
}

"""
The default value for a merge commit title.

- `PR_TITLE` - default to the pull request's title.
- `MERGE_MESSAGE` - default to the classic title for a merge message (e.g., Merge pull request #123 from branch-name).
"""
enum query_repos_get_template_repository_merge_commit_title {
  PR_TITLE
  MERGE_MESSAGE
}

"""
The default value for a merge commit message.

- `PR_TITLE` - default to the pull request's title.
- `PR_BODY` - default to the pull request's body.
- `BLANK` - default to a blank commit message.
"""
enum query_repos_get_template_repository_merge_commit_message {
  PR_BODY
  PR_TITLE
  BLANK
}

"""The status of the code search index for this repository"""
type query_repos_get_template_repository_code_search_index_status {
  lexical_search_ok: Boolean
  lexical_commit_sha: String
}

"""
The default value for a squash merge commit title:

- `PR_TITLE` - default to the pull request's title.
- `COMMIT_OR_PR_TITLE` - default to the commit's title (if only one commit) or the pull request's title (when more than one commit).
"""
enum query_repos_get_squash_merge_commit_title {
  PR_TITLE
  COMMIT_OR_PR_TITLE
}

"""
The default value for a squash merge commit message:

- `PR_BODY` - default to the pull request's body.
- `COMMIT_MESSAGES` - default to the branch's commit messages.
- `BLANK` - default to a blank commit message.
"""
enum query_repos_get_squash_merge_commit_message {
  PR_BODY
  COMMIT_MESSAGES
  BLANK
}

"""
The default value for a merge commit title.

  - `PR_TITLE` - default to the pull request's title.
  - `MERGE_MESSAGE` - default to the classic title for a merge message (e.g., Merge pull request #123 from branch-name).
"""
enum query_repos_get_merge_commit_title {
  PR_TITLE
  MERGE_MESSAGE
}

"""
The default value for a merge commit message.

- `PR_TITLE` - default to the pull request's title.
- `PR_BODY` - default to the pull request's body.
- `BLANK` - default to a blank commit message.
"""
enum query_repos_get_merge_commit_message {
  PR_BODY
  PR_TITLE
  BLANK
}

"""Code of Conduct Simple"""
type code_of_conduct_simple {
  url: URL!
  key: String!
  name: String!
  html_url: URL
}

type actions_list_artifacts_for_repo_200_response {
  total_count: Int!
  artifacts: [Artifact]!
}

"""An artifact"""
type Artifact {
  id: Int!
  node_id: String!

  """The name of the artifact."""
  name: String!

  """The size in bytes of the artifact."""
  size_in_bytes: Int!
  url: URL!
  archive_download_url: URL!

  """Whether or not the artifact has expired."""
  expired: Boolean!
  created_at: DateTime
  expires_at: DateTime
  updated_at: DateTime

  """
  The SHA256 digest of the artifact. This field will only be populated on artifacts uploaded with upload-artifact v4 or newer. For older versions, this field will be null.
  """
  digest: URL
  workflow_run: query_actions_list_artifacts_for_repo_artifacts_items_workflow_run
}

type query_actions_list_artifacts_for_repo_artifacts_items_workflow_run {
  id: Int
  repository_id: Int
  head_repository_id: Int
  head_branch: String
  head_sha: String
}

"""GitHub Actions cache retention policy for a repository."""
type Actions_cache_retention_limit_for_a_repository {
  """The maximum number of days to keep caches in this repository."""
  max_cache_retention_days: Int
}

"""GitHub Actions cache storage policy for a repository."""
type Actions_cache_storage_limit_for_a_repository {
  """The maximum total cache size for this repository, in gigabytes."""
  max_cache_size_gb: Int
}

"""Repository actions caches"""
type Repository_actions_caches {
  """Total number of caches"""
  total_count: Int!

  """Array of caches"""
  actions_caches: [query_actions_get_actions_cache_list_actions_caches_items]!
}

type query_actions_get_actions_cache_list_actions_caches_items {
  id: Int
  ref: String
  key: String
  version: String
  last_accessed_at: DateTime
  created_at: DateTime
  size_in_bytes: Int
}

"""
The property to sort the results by. `created_at` means when the cache was created. `last_accessed_at` means when the cache was last accessed. `size_in_bytes` is the size of the cache in bytes.
"""
enum actions_cache_list_sort_parameter {
  created_at
  last_accessed_at
  size_in_bytes
}

"""Information of a job execution in a workflow run"""
type Job {
  """The id of the job."""
  id: Int!

  """The id of the associated workflow run."""
  run_id: Int!
  run_url: URL!

  """
  Attempt number of the associated workflow run, 1 for first attempt and higher if the workflow was re-run.
  """
  run_attempt: Int
  node_id: String!

  """The SHA of the commit that is being run."""
  head_sha: String!
  url: URL!
  html_url: URL
  status: query_actions_get_job_for_workflow_run_status!
  conclusion: query_actions_get_job_for_workflow_run_conclusion

  """The time that the job created, in ISO 8601 format."""
  created_at: DateTime!

  """The time that the job started, in ISO 8601 format."""
  started_at: DateTime!

  """The time that the job finished, in ISO 8601 format."""
  completed_at: DateTime

  """The name of the job."""
  name: String!

  """Steps in this job."""
  steps: [query_actions_get_job_for_workflow_run_steps_items]
  check_run_url: URL!

  """
  Labels for the workflow job. Specified by the "runs_on" attribute in the action's workflow file.
  """
  labels: [String]!

  """
  The ID of the runner to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
  """
  runner_id: Int

  """
  The name of the runner to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
  """
  runner_name: String

  """
  The ID of the runner group to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
  """
  runner_group_id: Int

  """
  The name of the runner group to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
  """
  runner_group_name: String

  """The name of the workflow."""
  workflow_name: String

  """The name of the current branch."""
  head_branch: String
}

"""The phase of the lifecycle that the job is currently in."""
enum query_actions_get_job_for_workflow_run_status {
  queued
  in_progress
  completed
  waiting
  requested
  pending
}

"""The outcome of the job."""
enum query_actions_get_job_for_workflow_run_conclusion {
  success
  failure
  neutral
  cancelled
  skipped
  timed_out
  action_required
}

type query_actions_get_job_for_workflow_run_steps_items {
  status: query_actions_get_job_for_workflow_run_steps_items_status!

  """The outcome of the job."""
  conclusion: String

  """The name of the job."""
  name: String!
  number: Int!

  """The time that the step started, in ISO 8601 format."""
  started_at: DateTime

  """The time that the job finished, in ISO 8601 format."""
  completed_at: DateTime
}

"""The phase of the lifecycle that the job is currently in."""
enum query_actions_get_job_for_workflow_run_steps_items_status {
  queued
  in_progress
  completed
}

"""Actions OIDC subject customization for a repository"""
type Actions_OIDC_subject_customization_for_a_repository {
  """
  Whether to use the default template or not. If `true`, the `include_claim_keys` field is ignored.
  """
  use_default: Boolean!

  """
  Array of unique strings. Each claim key can only contain alphanumeric characters and underscores.
  """
  include_claim_keys: [String]
}

type actions_list_repo_organization_secrets_200_response {
  total_count: Int!
  secrets: [Actions_Secret]!
}

"""Set secrets for GitHub Actions."""
type Actions_Secret {
  """The name of the secret."""
  name: String!
  created_at: DateTime!
  updated_at: DateTime!
}

type actions_list_repo_organization_variables_200_response {
  total_count: Int!
  variables: [Actions_Variable]!
}

type Actions_Variable {
  """The name of the variable."""
  name: String!

  """The value of the variable."""
  value: String!

  """
  The date and time at which the variable was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  created_at: DateTime!

  """
  The date and time at which the variable was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  updated_at: DateTime!
}

type actions_repository_permissions {
  """Whether GitHub Actions is enabled on the repository."""
  enabled: Boolean!
  allowed_actions: allowed_actions

  """
  The API URL to use to get or set the actions and reusable workflows that are allowed to run, when `allowed_actions` is set to `selected`.
  """
  selected_actions_url: String

  """Whether actions must be pinned to a full-length commit SHA."""
  sha_pinning_required: Boolean
}

type actions_workflow_access_to_repository {
  access_level: query_actions_get_workflow_access_to_repository_access_level!
}

"""
Defines the level of access that workflows outside of the repository have to actions and reusable workflows within the
repository.

`none` means the access is only possible from workflows in this repository. `user` level access allows sharing across user owned private repositories only. `organization` level access allows sharing across the organization.
"""
enum query_actions_get_workflow_access_to_repository_access_level {
  none
  user
  organization
}

type actions_list_self_hosted_runners_for_repo_200_response {
  total_count: Int!
  runners: [Self_hosted_runners]!
}

type actions_list_workflow_runs_for_repo_200_response {
  total_count: Int!
  workflow_runs: [Workflow_Run]!
}

"""An invocation of a workflow"""
type Workflow_Run {
  """The ID of the workflow run."""
  id: Int!

  """The name of the workflow run."""
  name: String
  node_id: String!

  """The ID of the associated check suite."""
  check_suite_id: Int

  """The node ID of the associated check suite."""
  check_suite_node_id: String
  head_branch: String

  """
  The SHA of the head commit that points to the version of the workflow being run.
  """
  head_sha: String!

  """The full path of the workflow"""
  path: String!

  """The auto incrementing run number for the workflow run."""
  run_number: Int!

  """
  Attempt number of the run, 1 for first attempt and higher if the workflow was re-run.
  """
  run_attempt: Int
  referenced_workflows: [Referenced_workflow]
  event: String!
  status: String
  conclusion: String

  """The ID of the parent workflow."""
  workflow_id: Int!

  """The URL to the workflow run."""
  url: URL!
  html_url: URL!

  """
  Pull requests that are open with a `head_sha` or `head_branch` that matches the workflow run. The returned pull requests do not necessarily indicate pull requests that triggered the run.
  """
  pull_requests: [Pull_Request_Minimal]
  created_at: DateTime!
  updated_at: DateTime!
  actor: simple_user
  triggering_actor: simple_user

  """The start time of the latest run. Resets on re-run."""
  run_started_at: DateTime

  """The URL to the jobs for the workflow run."""
  jobs_url: URL!

  """The URL to download the logs for the workflow run."""
  logs_url: URL!

  """The URL to the associated check suite."""
  check_suite_url: URL!

  """The URL to the artifacts for the workflow run."""
  artifacts_url: URL!

  """The URL to cancel the workflow run."""
  cancel_url: URL!

  """The URL to rerun the workflow run."""
  rerun_url: URL!

  """The URL to the previous attempted run of this workflow, if one exists."""
  previous_attempt_url: URL

  """The URL to the workflow."""
  workflow_url: URL!
  head_commit: nullable_simple_commit
  repository: minimal_repository!
  head_repository: minimal_repository!
  head_repository_id: Int

  """
  The event-specific title associated with the run or the run-name if set, or the value of `run-name` if it is set in the workflow.
  """
  display_title: String!
}

"""A workflow referenced/reused by the initial caller workflow"""
type Referenced_workflow {
  path: String!
  sha: String!
  ref: String
}

"""A commit."""
type nullable_simple_commit {
  """SHA for the commit"""
  id: String!

  """SHA for the commit's tree"""
  tree_id: String!

  """Message describing the purpose of the commit"""
  message: String!

  """Timestamp of the commit"""
  timestamp: DateTime!
  author: query_actions_list_workflow_runs_for_repo_workflow_runs_items_head_commit_author
  committer: query_actions_list_workflow_runs_for_repo_workflow_runs_items_head_commit_committer
}

"""Information about the Git author"""
type query_actions_list_workflow_runs_for_repo_workflow_runs_items_head_commit_author {
  """Name of the commit's author"""
  name: String!

  """Git email address of the commit's author"""
  email: EmailAddress!
}

"""Information about the Git committer"""
type query_actions_list_workflow_runs_for_repo_workflow_runs_items_head_commit_committer {
  """Name of the commit's committer"""
  name: String!

  """Git email address of the commit's committer"""
  email: EmailAddress!
}

"""
Returns workflow runs with the check run `status` or `conclusion` that you specify. For example, a conclusion can be `success` or a status can be `in_progress`. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`.
"""
enum workflow_run_status_parameter {
  completed
  action_required
  cancelled
  failure
  neutral
  skipped
  stale
  success
  timed_out
  in_progress
  queued
  requested
  waiting
  pending
}

"""An entry in the reviews log for environment deployments"""
type Environment_Approval {
  """The list of environments that were approved or rejected"""
  environments: [query_actions_get_reviews_for_run_items_environments_items]!
  state: query_actions_get_reviews_for_run_items_state!
  user: simple_user!

  """The comment submitted with the deployment review"""
  comment: String!
}

type query_actions_get_reviews_for_run_items_environments_items {
  """The id of the environment."""
  id: Int
  node_id: String

  """The name of the environment."""
  name: String
  url: URL
  html_url: URL

  """The time that the environment was created, in ISO 8601 format."""
  created_at: DateTime

  """The time that the environment was last updated, in ISO 8601 format."""
  updated_at: DateTime
}

"""
Whether deployment to the environment(s) was approved or rejected or pending (with comments)
"""
enum query_actions_get_reviews_for_run_items_state {
  approved
  rejected
  pending
}

type actions_list_workflow_run_artifacts_200_response {
  total_count: Int!
  artifacts: [Artifact]!
}

type actions_list_jobs_for_workflow_run_attempt_200_response {
  total_count: Int!
  jobs: [Job]!
}

type actions_list_jobs_for_workflow_run_200_response {
  total_count: Int!
  jobs: [Job]!
}

"""
Filters jobs by their `completed_at` timestamp. `latest` returns jobs from the most recent execution of the workflow run. `all` returns all jobs for a workflow run, including from old executions of the workflow run.
"""
enum queryInput_actions_list_jobs_for_workflow_run_filter {
  latest
  all
}

"""Details of a deployment that is waiting for protection rules to pass"""
type Pending_Deployment {
  environment: query_actions_get_pending_deployments_for_run_items_environment!

  """The set duration of the wait timer"""
  wait_timer: Int!

  """The time that the wait timer began."""
  wait_timer_started_at: DateTime

  """Whether the currently authenticated user can approve the deployment"""
  current_user_can_approve: Boolean!

  """
  The people or teams that may approve jobs that reference the environment. You can list up to six users or teams as reviewers. The reviewers must have at least read access to the repository. Only one of the required reviewers needs to approve the job for it to proceed.
  """
  reviewers: [query_actions_get_pending_deployments_for_run_items_reviewers_items]!
}

type query_actions_get_pending_deployments_for_run_items_environment {
  """The id of the environment."""
  id: BigInt
  node_id: String

  """The name of the environment."""
  name: String
  url: URL
  html_url: URL
}

type query_actions_get_pending_deployments_for_run_items_reviewers_items {
  type: deployment_reviewer_type
  reviewer: query_actions_get_pending_deployments_for_run_items_reviewers_items_reviewer
}

"""The type of reviewer."""
enum deployment_reviewer_type {
  User
  Team
}

type query_actions_get_pending_deployments_for_run_items_reviewers_items_reviewer {
  name: String!
  email: String
  login: String!
  id: JSON
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: JSON
  site_admin: Boolean!
  starred_at: String
  user_view_type: String
  additionalProperties: JSON
  slug: String!
  description: String
  privacy: String
  notification_setting: String
  permission: String!
  permissions: query_campaigns_list_org_campaigns_items_team_managers_items_permissions
  members_url: String!
  repositories_url: URL!

  """Unique identifier of the organization to which this team belongs"""
  organization_id: Int

  """Unique identifier of the enterprise to which this team belongs"""
  enterprise_id: Int
  parent: nullable_team_simple
}

"""Workflow Run Usage"""
type Workflow_Run_Usage {
  billable: query_actions_get_workflow_run_usage_billable!
  run_duration_ms: Int
}

type query_actions_get_workflow_run_usage_billable {
  UBUNTU: query_actions_get_workflow_run_usage_billable_UBUNTU
  MACOS: query_actions_get_workflow_run_usage_billable_MACOS
  WINDOWS: query_actions_get_workflow_run_usage_billable_WINDOWS
}

type query_actions_get_workflow_run_usage_billable_UBUNTU {
  total_ms: Int!
  jobs: Int!
  job_runs: [query_actions_get_workflow_run_usage_billable_UBUNTU_job_runs_items]
}

type query_actions_get_workflow_run_usage_billable_UBUNTU_job_runs_items {
  job_id: Int!
  duration_ms: Int!
}

type query_actions_get_workflow_run_usage_billable_MACOS {
  total_ms: Int!
  jobs: Int!
  job_runs: [query_actions_get_workflow_run_usage_billable_MACOS_job_runs_items]
}

type query_actions_get_workflow_run_usage_billable_MACOS_job_runs_items {
  job_id: Int!
  duration_ms: Int!
}

type query_actions_get_workflow_run_usage_billable_WINDOWS {
  total_ms: Int!
  jobs: Int!
  job_runs: [query_actions_get_workflow_run_usage_billable_WINDOWS_job_runs_items]
}

type query_actions_get_workflow_run_usage_billable_WINDOWS_job_runs_items {
  job_id: Int!
  duration_ms: Int!
}

type actions_list_repo_secrets_200_response {
  total_count: Int!
  secrets: [Actions_Secret]!
}

type actions_list_repo_variables_200_response {
  total_count: Int!
  variables: [Actions_Variable]!
}

type actions_list_repo_workflows_200_response {
  total_count: Int!
  workflows: [workflow]!
}

"""A GitHub Actions workflow"""
type workflow {
  id: Int!
  node_id: String!
  name: String!
  path: String!
  state: query_actions_list_repo_workflows_workflows_items_state!
  created_at: DateTime!
  updated_at: DateTime!
  url: URL!
  html_url: URL!
  badge_url: URL!
  deleted_at: DateTime
}

enum query_actions_list_repo_workflows_workflows_items_state {
  active
  deleted
  disabled_fork
  disabled_inactivity
  disabled_manually
}

input workflow_id_parameter_Input {
  Int: Int
  String: String
}

type actions_list_workflow_runs_200_response {
  total_count: Int!
  workflow_runs: [Workflow_Run]!
}

"""Workflow Usage"""
type Workflow_Usage {
  billable: query_actions_get_workflow_usage_billable!
}

type query_actions_get_workflow_usage_billable {
  UBUNTU: query_actions_get_workflow_usage_billable_UBUNTU
  MACOS: query_actions_get_workflow_usage_billable_MACOS
  WINDOWS: query_actions_get_workflow_usage_billable_WINDOWS
}

type query_actions_get_workflow_usage_billable_UBUNTU {
  total_ms: Int
}

type query_actions_get_workflow_usage_billable_MACOS {
  total_ms: Int
}

type query_actions_get_workflow_usage_billable_WINDOWS {
  total_ms: Int
}

"""Activity"""
type Activity {
  id: Int!
  node_id: String!

  """The SHA of the commit before the activity."""
  before: String!

  """The SHA of the commit after the activity."""
  after: String!

  """The full Git reference, formatted as `refs/heads/<branch name>`."""
  ref: String!

  """The time when the activity occurred."""
  timestamp: DateTime!
  activity_type: query_repos_list_activities_items_activity_type!
  actor: nullable_simple_user
}

"""The type of the activity that was performed."""
enum query_repos_list_activities_items_activity_type {
  push
  force_push
  branch_deletion
  branch_creation
  pr_merge
  merge_queue_merge
}

"""
The time period to filter by.

For example, `day` will filter for activity that occurred in the past 24 hours, and `week` will filter for activity that occurred in the past 7 days (168 hours).
"""
enum queryInput_repos_list_activities_time_period {
  day
  week
  month
  quarter
  year
}

"""
The activity type to filter by.

For example, you can choose to filter by "force_push", to see all force pushes to the repository.
"""
enum queryInput_repos_list_activities_activity_type {
  push
  force_push
  branch_creation
  branch_deletion
  pr_merge
  merge_queue_merge
}

type repos_list_attestations_200_response {
  attestations: [query_repos_list_attestations_attestations_items]
}

type query_repos_list_attestations_attestations_items {
  bundle: query_repos_list_attestations_attestations_items_bundle
  repository_id: Int
  bundle_url: String
  initiator: String
}

"""
The attestation's Sigstore Bundle.
Refer to the [Sigstore Bundle Specification](https://github.com/sigstore/protobuf-specs/blob/main/protos/sigstore_bundle.proto) for more information.
"""
type query_repos_list_attestations_attestations_items_bundle {
  mediaType: String
  verificationMaterial: JSON
  dsseEnvelope: JSON
}

"""An autolink reference."""
type Autolink_reference {
  id: Int!

  """The prefix of a key that is linkified."""
  key_prefix: String!

  """A template for the target URL that is generated if a key was found."""
  url_template: URL!

  """
  Whether this autolink reference matches alphanumeric characters. If false, this autolink reference only matches numeric characters.
  """
  is_alphanumeric: Boolean!
  updated_at: DateTime
}

"""Check Dependabot security updates"""
type Check_Dependabot_security_updates {
  """Whether Dependabot security updates are enabled for the repository."""
  enabled: Boolean!

  """Whether Dependabot security updates are paused for the repository."""
  paused: Boolean!
}

"""Short Branch"""
type Short_Branch {
  name: String!
  commit: query_repos_list_branches_items_commit!
  protected: Boolean!
  protection: Branch_Protection
  protection_url: URL
}

type query_repos_list_branches_items_commit {
  sha: String!
  url: URL!
}

"""Branch Protection"""
type Branch_Protection {
  url: String
  enabled: Boolean
  required_status_checks: Protected_Branch_Required_Status_Check
  enforce_admins: Protected_Branch_Admin_Enforced
  required_pull_request_reviews: Protected_Branch_Pull_Request_Review
  restrictions: Branch_Restriction_Policy
  required_linear_history: query_repos_list_branches_items_protection_required_linear_history
  allow_force_pushes: query_repos_list_branches_items_protection_allow_force_pushes
  allow_deletions: query_repos_list_branches_items_protection_allow_deletions
  block_creations: query_repos_list_branches_items_protection_block_creations
  required_conversation_resolution: query_repos_list_branches_items_protection_required_conversation_resolution
  name: String
  protection_url: String
  required_signatures: query_repos_list_branches_items_protection_required_signatures
  lock_branch: query_repos_list_branches_items_protection_lock_branch
  allow_fork_syncing: query_repos_list_branches_items_protection_allow_fork_syncing
}

"""Protected Branch Required Status Check"""
type Protected_Branch_Required_Status_Check {
  url: String
  enforcement_level: String
  contexts: [String]!
  checks: [query_repos_list_branches_items_protection_required_status_checks_checks_items]!
  contexts_url: String
  strict: Boolean
}

type query_repos_list_branches_items_protection_required_status_checks_checks_items {
  context: String!
  app_id: Int
}

"""Protected Branch Admin Enforced"""
type Protected_Branch_Admin_Enforced {
  url: URL!
  enabled: Boolean!
}

"""Protected Branch Pull Request Review"""
type Protected_Branch_Pull_Request_Review {
  url: URL
  dismissal_restrictions: query_repos_list_branches_items_protection_required_pull_request_reviews_dismissal_restrictions
  bypass_pull_request_allowances: query_repos_list_branches_items_protection_required_pull_request_reviews_bypass_pull_request_allowances
  dismiss_stale_reviews: Boolean!
  require_code_owner_reviews: Boolean!
  required_approving_review_count: NonNegativeInt

  """
  Whether the most recent push must be approved by someone other than the person who pushed it.
  """
  require_last_push_approval: Boolean
}

type query_repos_list_branches_items_protection_required_pull_request_reviews_dismissal_restrictions {
  """The list of users with review dismissal access."""
  users: [simple_user]

  """The list of teams with review dismissal access."""
  teams: [team]

  """The list of apps with review dismissal access."""
  apps: [integration]
  url: String
  users_url: String
  teams_url: String
}

"""
Allow specific users, teams, or apps to bypass pull request requirements.
"""
type query_repos_list_branches_items_protection_required_pull_request_reviews_bypass_pull_request_allowances {
  """The list of users allowed to bypass pull request requirements."""
  users: [simple_user]

  """The list of teams allowed to bypass pull request requirements."""
  teams: [team]

  """The list of apps allowed to bypass pull request requirements."""
  apps: [integration]
}

"""Branch Restriction Policy"""
type Branch_Restriction_Policy {
  url: URL!
  users_url: URL!
  teams_url: URL!
  apps_url: URL!
  users: [query_repos_list_branches_items_protection_restrictions_users_items]!
  teams: [team]!
  apps: [query_repos_list_branches_items_protection_restrictions_apps_items]!
}

type query_repos_list_branches_items_protection_restrictions_users_items {
  login: String
  id: BigInt
  node_id: String
  avatar_url: String
  gravatar_id: String
  url: String
  html_url: String
  followers_url: String
  following_url: String
  gists_url: String
  starred_url: String
  subscriptions_url: String
  organizations_url: String
  repos_url: String
  events_url: String
  received_events_url: String
  type: String
  site_admin: Boolean
  user_view_type: String
}

type query_repos_list_branches_items_protection_restrictions_apps_items {
  id: Int
  slug: String
  node_id: String
  owner: query_repos_list_branches_items_protection_restrictions_apps_items_owner
  name: String
  client_id: String
  description: String
  external_url: String
  html_url: String
  created_at: String
  updated_at: String
  permissions: query_repos_list_branches_items_protection_restrictions_apps_items_permissions
  events: [String]
}

type query_repos_list_branches_items_protection_restrictions_apps_items_owner {
  login: String
  id: Int
  node_id: String
  url: String
  repos_url: String
  events_url: String
  hooks_url: String
  issues_url: String
  members_url: String
  public_members_url: String
  avatar_url: String
  description: String
  gravatar_id: String
  html_url: String
  followers_url: String
  following_url: String
  gists_url: String
  starred_url: String
  subscriptions_url: String
  organizations_url: String
  received_events_url: String
  type: String
  site_admin: Boolean
  user_view_type: String
}

type query_repos_list_branches_items_protection_restrictions_apps_items_permissions {
  metadata: String
  contents: String
  issues: String
  single_file: String
}

type query_repos_list_branches_items_protection_required_linear_history {
  enabled: Boolean
}

type query_repos_list_branches_items_protection_allow_force_pushes {
  enabled: Boolean
}

type query_repos_list_branches_items_protection_allow_deletions {
  enabled: Boolean
}

type query_repos_list_branches_items_protection_block_creations {
  enabled: Boolean
}

type query_repos_list_branches_items_protection_required_conversation_resolution {
  enabled: Boolean
}

type query_repos_list_branches_items_protection_required_signatures {
  url: URL!
  enabled: Boolean!
}

"""
Whether to set the branch as read-only. If this is true, users will not be able to push to the branch.
"""
type query_repos_list_branches_items_protection_lock_branch {
  enabled: Boolean
}

"""
Whether users can pull changes from upstream when the branch is locked. Set to `true` to allow fork syncing. Set to `false` to prevent fork syncing.
"""
type query_repos_list_branches_items_protection_allow_fork_syncing {
  enabled: Boolean
}

"""Branch With Protection"""
type Branch_With_Protection {
  name: String!
  commit: Commit!
  _links: query_repos_get_branch__links!
  protected: Boolean!
  protection: Branch_Protection!
  protection_url: URL!
  pattern: String
  required_approving_review_count: Int
}

"""Commit"""
type Commit {
  url: URL!
  sha: String!
  node_id: String!
  html_url: URL!
  comments_url: URL!
  commit: query_repos_get_branch_commit_commit!
  author: query_repos_get_branch_commit_author
  committer: query_repos_get_branch_commit_committer
  parents: [query_repos_get_branch_commit_parents_items]!
  stats: query_repos_get_branch_commit_stats
  files: [Diff_Entry]
}

type query_repos_get_branch_commit_commit {
  url: URL!
  author: Git_User
  committer: Git_User
  message: String!
  comment_count: Int!
  tree: query_repos_get_branch_commit_commit_tree!
  verification: Verification
}

"""Metaproperties for Git author/committer information."""
type Git_User {
  name: String
  email: String
  date: DateTime
}

type query_repos_get_branch_commit_commit_tree {
  sha: String!
  url: URL!
}

type Verification {
  verified: Boolean!
  reason: String!
  payload: String
  signature: String
  verified_at: String
}

union query_repos_get_branch_commit_author = simple_user | JSON_container

union query_repos_get_branch_commit_committer = simple_user | JSON_container

type query_repos_get_branch_commit_parents_items {
  sha: String!
  url: URL!
  html_url: URL
}

type query_repos_get_branch_commit_stats {
  additions: Int
  deletions: Int
  total: Int
}

"""Diff Entry"""
type Diff_Entry {
  sha: String
  filename: String!
  status: query_repos_get_branch_commit_files_items_status!
  additions: Int!
  deletions: Int!
  changes: Int!
  blob_url: URL!
  raw_url: URL!
  contents_url: URL!
  patch: String
  previous_filename: String
}

enum query_repos_get_branch_commit_files_items_status {
  added
  removed
  modified
  renamed
  copied
  changed
  unchanged
}

type query_repos_get_branch__links {
  html: String!
  self: URL!
}

"""Status Check Policy"""
type Status_Check_Policy {
  url: URL!
  strict: Boolean!
  contexts: [String]!
  checks: [query_repos_get_status_checks_protection_checks_items]!
  contexts_url: URL!
}

type query_repos_get_status_checks_protection_checks_items {
  context: String!
  app_id: Int
}

"""A check performed on the code of a given code change"""
type check_run {
  """The id of the check."""
  id: BigInt!

  """The SHA of the commit that is being checked."""
  head_sha: String!
  node_id: String!
  external_id: String
  url: URL!
  html_url: URL
  details_url: URL
  status: query_checks_get_status!
  conclusion: query_checks_get_conclusion
  started_at: DateTime
  completed_at: DateTime
  output: query_checks_get_output!

  """The name of the check."""
  name: String!
  check_suite: query_checks_get_check_suite
  app: nullable_integration

  """
  Pull requests that are open with a `head_sha` or `head_branch` that matches the check. The returned pull requests do not necessarily indicate pull requests that triggered the check.
  """
  pull_requests: [Pull_Request_Minimal]!
  deployment: deployment_simple
}

"""
The phase of the lifecycle that the check is currently in. Statuses of waiting, requested, and pending are reserved for GitHub Actions check runs.
"""
enum query_checks_get_status {
  queued
  in_progress
  completed
  waiting
  requested
  pending
}

enum query_checks_get_conclusion {
  success
  failure
  neutral
  cancelled
  skipped
  timed_out
  action_required
}

type query_checks_get_output {
  title: String
  summary: String
  text: String
  annotations_count: Int!
  annotations_url: URL!
}

type query_checks_get_check_suite {
  id: Int!
}

"""
A deployment created as the result of an Actions check run from a workflow that references an environment
"""
type deployment_simple {
  url: URL!

  """Unique identifier of the deployment"""
  id: Int!
  node_id: String!

  """Parameter to specify a task to execute"""
  task: String!
  original_environment: String

  """Name for the target deployment environment."""
  environment: String!
  description: String
  created_at: DateTime!
  updated_at: DateTime!
  statuses_url: URL!
  repository_url: URL!

  """
  Specifies if the given environment is will no longer exist at some point in the future. Default: false.
  """
  transient_environment: Boolean

  """
  Specifies if the given environment is one that end-users directly interact with. Default: false.
  """
  production_environment: Boolean
  performed_via_github_app: nullable_integration
}

"""Check Annotation"""
type Check_Annotation {
  path: String!
  start_line: Int!
  end_line: Int!
  start_column: Int
  end_column: Int
  annotation_level: String
  title: String
  message: String
  raw_details: String
  blob_href: String!
}

"""A suite of checks performed on the code of a given code change"""
type CheckSuite {
  id: BigInt!
  node_id: String!
  head_branch: String

  """The SHA of the head commit that is being checked."""
  head_sha: String!
  status: query_checks_get_suite_status
  conclusion: query_checks_get_suite_conclusion
  url: URL
  before: String
  after: String
  pull_requests: [Pull_Request_Minimal]
  app: nullable_integration
  repository: minimal_repository!
  created_at: DateTime
  updated_at: DateTime
  head_commit: simple_commit!
  latest_check_runs_count: Int!
  check_runs_url: String!
  rerequestable: Boolean
  runs_rerequestable: Boolean
}

"""
The phase of the lifecycle that the check suite is currently in. Statuses of waiting, requested, and pending are reserved for GitHub Actions check suites.
"""
enum query_checks_get_suite_status {
  queued
  in_progress
  completed
  waiting
  requested
  pending
}

enum query_checks_get_suite_conclusion {
  success
  failure
  neutral
  cancelled
  skipped
  timed_out
  action_required
  startup_failure
  stale
}

"""A commit."""
type simple_commit {
  """SHA for the commit"""
  id: String!

  """SHA for the commit's tree"""
  tree_id: String!

  """Message describing the purpose of the commit"""
  message: String!

  """Timestamp of the commit"""
  timestamp: DateTime!
  author: query_checks_get_suite_head_commit_author
  committer: query_checks_get_suite_head_commit_committer
}

"""Information about the Git author"""
type query_checks_get_suite_head_commit_author {
  """Name of the commit's author"""
  name: String!

  """Git email address of the commit's author"""
  email: EmailAddress!
}

"""Information about the Git committer"""
type query_checks_get_suite_head_commit_committer {
  """Name of the commit's committer"""
  name: String!

  """Git email address of the commit's committer"""
  email: EmailAddress!
}

type checks_list_for_suite_200_response {
  total_count: Int!
  check_runs: [check_run]!
}

"""Returns check runs with the specified `status`."""
enum status_parameter {
  queued
  in_progress
  completed
}

"""
Filters check runs by their `completed_at` timestamp. `latest` returns the most recent check runs.
"""
enum queryInput_checks_list_for_suite_filter {
  latest
  all
}

type code_scanning_alert_items {
  """The security alert number."""
  number: Int!

  """
  The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime!

  """
  The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime

  """The REST API URL of the alert resource."""
  url: URL!

  """The GitHub URL of the alert resource."""
  html_url: URL!

  """The REST API URL for fetching the list of instances for an alert."""
  instances_url: URL!
  state: code_scanning_alert_state

  """
  The time that the alert was no longer detected and was considered fixed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  fixed_at: DateTime
  dismissed_by: nullable_simple_user

  """
  The time that the alert was dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  dismissed_at: DateTime
  dismissed_reason: code_scanning_alert_dismissed_reason

  """The dismissal comment associated with the dismissal of the alert."""
  dismissed_comment: code_scanning_alert_dismissed_comment
  rule: code_scanning_alert_rule_summary!
  tool: code_scanning_analysis_tool!
  most_recent_instance: code_scanning_alert_instance!
  dismissal_approved_by: nullable_simple_user
  assignees: [simple_user]
}

"""The property by which to sort the results."""
enum queryInput_code_scanning_list_alerts_for_repo_sort {
  created
  updated
}

type code_scanning_alert {
  """The security alert number."""
  number: Int!

  """
  The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime!

  """
  The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime

  """The REST API URL of the alert resource."""
  url: URL!

  """The GitHub URL of the alert resource."""
  html_url: URL!

  """The REST API URL for fetching the list of instances for an alert."""
  instances_url: URL!
  state: code_scanning_alert_state

  """
  The time that the alert was no longer detected and was considered fixed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  fixed_at: DateTime
  dismissed_by: nullable_simple_user

  """
  The time that the alert was dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  dismissed_at: DateTime
  dismissed_reason: code_scanning_alert_dismissed_reason

  """The dismissal comment associated with the dismissal of the alert."""
  dismissed_comment: code_scanning_alert_dismissed_comment
  rule: code_scanning_alert_rule!
  tool: code_scanning_analysis_tool!
  most_recent_instance: code_scanning_alert_instance!
  dismissal_approved_by: nullable_simple_user
  assignees: [simple_user]
}

type code_scanning_alert_rule {
  """A unique identifier for the rule used to detect the alert."""
  id: String

  """The name of the rule used to detect the alert."""
  name: String
  severity: query_code_scanning_get_alert_rule_severity
  security_severity_level: query_code_scanning_get_alert_rule_security_severity_level

  """A short description of the rule used to detect the alert."""
  description: String

  """A description of the rule used to detect the alert."""
  full_description: String

  """A set of tags applicable for the rule."""
  tags: [String]

  """Detailed documentation for the rule as GitHub Flavored Markdown."""
  help: String

  """A link to the documentation for the rule used to detect the alert."""
  help_uri: String
}

"""The severity of the alert."""
enum query_code_scanning_get_alert_rule_severity {
  none
  note
  warning
  error
}

"""The security severity of the alert."""
enum query_code_scanning_get_alert_rule_security_severity_level {
  low
  medium
  high
  critical
}

type code_scanning_autofix {
  status: code_scanning_autofix_status!

  """The description of an autofix."""
  description: String

  """
  The start time of an autofix in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  started_at: DateTime!
}

"""The status of an autofix."""
enum code_scanning_autofix_status {
  pending
  error
  success
  outdated
}

type code_scanning_alert_instance_list {
  """
  The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`,
  `refs/heads/<branch name>` or simply `<branch name>`.
  """
  ref: String

  """
  Identifies the configuration under which the analysis was executed. For example, in GitHub Actions this includes the workflow filename and job name.
  """
  analysis_key: String

  """
  Identifies the variable values associated with the environment in which the analysis that generated this alert instance was performed, such as the language that was analyzed.
  """
  environment: String

  """
  Identifies the configuration under which the analysis was executed. Used to distinguish between multiple analyses for the same tool and commit, but performed on different languages or different parts of the code.
  """
  category: String
  state: code_scanning_alert_instance_state
  commit_sha: String
  message: query_code_scanning_list_alert_instances_items_message
  location: code_scanning_alert_location
  html_url: String

  """
  Classifications that have been applied to the file that triggered the alert.
  For example identifying it as documentation, or a generated file.
  """
  classifications: [code_scanning_alert_classification]
}

"""State of a code scanning alert instance."""
enum code_scanning_alert_instance_state {
  open
  fixed
}

type query_code_scanning_list_alert_instances_items_message {
  text: String
}

type code_scanning_analysis {
  """
  The Git reference, formatted as `refs/pull/<number>/merge`, `refs/pull/<number>/head`,
  `refs/heads/<branch name>` or simply `<branch name>`.
  """
  ref: String!
  commit_sha: code_scanning_analysis_commit_sha!

  """
  Identifies the configuration under which the analysis was executed. For example, in GitHub Actions this includes the workflow filename and job name.
  """
  analysis_key: String!

  """
  Identifies the variable values associated with the environment in which this analysis was performed.
  """
  environment: String!

  """
  Identifies the configuration under which the analysis was executed. Used to distinguish between multiple analyses for the same tool and commit, but performed on different languages or different parts of the code.
  """
  category: String
  error: String!

  """
  The time that the analysis was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime!

  """The total number of results in the analysis."""
  results_count: Int!

  """The total number of rules used in the analysis."""
  rules_count: Int!

  """Unique identifier for this analysis."""
  id: Int!

  """The REST API URL of the analysis resource."""
  url: URL!

  """An identifier for the upload."""
  sarif_id: String!
  tool: code_scanning_analysis_tool!
  deletable: Boolean!

  """Warning generated when processing the analysis"""
  warning: String!
}

scalar code_scanning_analysis_commit_sha

enum created_const {
  created
}

union code_scanning_get_analysis_200_response = code_scanning_analysis | JSON_container

"""A CodeQL database."""
type CodeQL_Database {
  """The ID of the CodeQL database."""
  id: Int!

  """The name of the CodeQL database."""
  name: String!

  """The language of the CodeQL database."""
  language: String!
  uploader: simple_user!

  """The MIME type of the CodeQL database file."""
  content_type: String!

  """The size of the CodeQL database file in bytes."""
  size: Int!

  """
  The date and time at which the CodeQL database was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  created_at: DateTime!

  """
  The date and time at which the CodeQL database was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  updated_at: DateTime!

  """
  The URL at which to download the CodeQL database. The `Accept` header must be set to the value of the `content_type` property.
  """
  url: URL!

  """
  The commit SHA of the repository at the time the CodeQL database was created.
  """
  commit_oid: String
}

"""A run of a CodeQL query against one or more repositories."""
type Variant_Analysis {
  """The ID of the variant analysis."""
  id: Int!
  controller_repo: simple_repository!
  actor: simple_user!
  query_language: code_scanning_variant_analysis_language!

  """The download url for the query pack."""
  query_pack_url: String!

  """
  The date and time at which the variant analysis was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  created_at: DateTime

  """
  The date and time at which the variant analysis was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  updated_at: DateTime

  """
  The date and time at which the variant analysis was completed, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Will be null if the variant analysis has not yet completed or this information is not available.
  """
  completed_at: DateTime
  status: query_code_scanning_get_variant_analysis_status!

  """
  The GitHub Actions workflow run used to execute this variant analysis. This is only available if the workflow run has started.
  """
  actions_workflow_run_id: Int
  failure_reason: query_code_scanning_get_variant_analysis_failure_reason
  scanned_repositories: [query_code_scanning_get_variant_analysis_scanned_repositories_items]
  skipped_repositories: query_code_scanning_get_variant_analysis_skipped_repositories
}

"""The language targeted by the CodeQL query"""
enum code_scanning_variant_analysis_language {
  actions
  cpp
  csharp
  go
  java
  javascript
  python
  ruby
  rust
  swift
}

enum query_code_scanning_get_variant_analysis_status {
  in_progress
  succeeded
  failed
  cancelled
}

"""
The reason for a failure of the variant analysis. This is only available if the variant analysis has failed.
"""
enum query_code_scanning_get_variant_analysis_failure_reason {
  no_repos_queried
  actions_workflow_run_failed
  internal_error
}

type query_code_scanning_get_variant_analysis_scanned_repositories_items {
  repository: Repository_Identifier!
  analysis_status: code_scanning_variant_analysis_status!

  """
  The number of results in the case of a successful analysis. This is only available for successful analyses.
  """
  result_count: Int

  """
  The size of the artifact. This is only available for successful analyses.
  """
  artifact_size_in_bytes: Int

  """
  The reason of the failure of this repo task. This is only available if the repository task has failed.
  """
  failure_message: String
}

"""Repository Identifier"""
type Repository_Identifier {
  """A unique identifier of the repository."""
  id: Int!

  """The name of the repository."""
  name: String!

  """The full, globally unique, name of the repository."""
  full_name: String!

  """Whether the repository is private."""
  private: Boolean!
  stargazers_count: Int!
  updated_at: DateTime
}

"""The new status of the CodeQL variant analysis repository task."""
enum code_scanning_variant_analysis_status {
  pending
  in_progress
  succeeded
  failed
  canceled
  timed_out
}

"""
Information about repositories that were skipped from processing. This information is only available to the user that initiated the variant analysis.
"""
type query_code_scanning_get_variant_analysis_skipped_repositories {
  access_mismatch_repos: code_scanning_variant_analysis_skipped_repo_group!
  not_found_repos: query_code_scanning_get_variant_analysis_skipped_repositories_not_found_repos!
  no_codeql_db_repos: code_scanning_variant_analysis_skipped_repo_group!
  over_limit_repos: code_scanning_variant_analysis_skipped_repo_group!
}

type code_scanning_variant_analysis_skipped_repo_group {
  """The total number of repositories that were skipped for this reason."""
  repository_count: Int!

  """
  A list of repositories that were skipped. This list may not include all repositories that were skipped. This is only available when the repository was found and the user has access to it.
  """
  repositories: [Repository_Identifier]!
}

type query_code_scanning_get_variant_analysis_skipped_repositories_not_found_repos {
  """The total number of repositories that were skipped for this reason."""
  repository_count: Int!

  """
  A list of full repository names that were skipped. This list may not include all repositories that were skipped.
  """
  repository_full_names: [String]!
}

type code_scanning_variant_analysis_repo_task {
  repository: simple_repository!
  analysis_status: code_scanning_variant_analysis_status!

  """
  The size of the artifact. This is only available for successful analyses.
  """
  artifact_size_in_bytes: Int

  """
  The number of results in the case of a successful analysis. This is only available for successful analyses.
  """
  result_count: Int

  """
  The reason of the failure of this repo task. This is only available if the repository task has failed.
  """
  failure_message: String

  """
  The SHA of the commit the CodeQL database was built against. This is only available for successful analyses.
  """
  database_commit_sha: String

  """
  The source location prefix to use. This is only available for successful analyses.
  """
  source_location_prefix: String

  """
  The URL of the artifact. This is only available for successful analyses.
  """
  artifact_url: String
}

"""Configuration for code scanning default setup."""
type code_scanning_default_setup {
  state: query_code_scanning_get_default_setup_state

  """Languages to be analyzed."""
  languages: [query_code_scanning_get_default_setup_languages_items]
  runner_type: query_code_scanning_get_default_setup_runner_type

  """Runner label to be used if the runner type is labeled."""
  runner_label: String
  query_suite: query_code_scanning_get_default_setup_query_suite
  threat_model: query_code_scanning_get_default_setup_threat_model

  """Timestamp of latest configuration update."""
  updated_at: DateTime
  schedule: weekly_const
}

"""Code scanning default setup has been configured or not."""
enum query_code_scanning_get_default_setup_state {
  configured
  not_configured
}

enum query_code_scanning_get_default_setup_languages_items {
  actions
  c_cpp
  csharp
  go
  java_kotlin
  javascript_typescript
  javascript
  python
  ruby
  typescript
  swift
}

"""Runner type to be used."""
enum query_code_scanning_get_default_setup_runner_type {
  standard
  labeled
}

"""CodeQL query suite to be used."""
enum query_code_scanning_get_default_setup_query_suite {
  default
  extended
}

"""
Threat model to be used for code scanning analysis. Use `remote` to analyze only network sources and `remote_and_local` to include local sources like filesystem access, command-line arguments, database reads, environment variable and standard input.
"""
enum query_code_scanning_get_default_setup_threat_model {
  remote
  remote_and_local
}

enum weekly_const {
  weekly
}

type code_scanning_sarifs_status {
  processing_status: query_code_scanning_get_sarif_processing_status

  """The REST API URL for getting the analyses associated with the upload."""
  analyses_url: URL

  """Any errors that ocurred during processing of the delivery."""
  errors: [String]
}

"""
`pending` files have not yet been processed, while `complete` means results from the SARIF have been stored. `failed` files have either not been processed at all, or could only be partially processed.
"""
enum query_code_scanning_get_sarif_processing_status {
  pending
  complete
  failed
}

union code_security_get_configuration_for_repository_response = code_security_configuration_for_repository | Void_container

"""
Code security configuration associated with a repository and attachment status
"""
type code_security_configuration_for_repository {
  status: query_code_security_get_configuration_for_repository_oneOf_0_status
  configuration: code_security_configuration
}

"""
The attachment status of the code security configuration on the repository.
"""
enum query_code_security_get_configuration_for_repository_oneOf_0_status {
  attached
  attaching
  detached
  removed
  enforced
  failed
  updating
  removed_by_enterprise
}

"""A list of errors found in a repo's CODEOWNERS file"""
type CODEOWNERS_errors {
  errors: [query_repos_codeowners_errors_errors_items]!
}

type query_repos_codeowners_errors_errors_items {
  """The line number where this errors occurs."""
  line: Int!

  """The column number where this errors occurs."""
  column: Int!

  """The contents of the line where the error occurs."""
  source: String

  """The type of error."""
  kind: String!

  """
  Suggested action to fix the error. This will usually be `null`, but is provided for some common errors.
  """
  suggestion: String

  """
  A human-readable description of the error, combining information from multiple fields, laid out for display in a monospaced typeface (for example, a command-line setting).
  """
  message: String!

  """The path of the file where the error occured."""
  path: String!
}

type codespaces_list_in_repository_for_authenticated_user_200_response {
  total_count: Int!
  codespaces: [codespace]!
}

type codespaces_list_devcontainers_in_repository_for_authenticated_user_200_response {
  total_count: Int!
  devcontainers: [query_codespaces_list_devcontainers_in_repository_for_authenticated_user_devcontainers_items]!
}

type query_codespaces_list_devcontainers_in_repository_for_authenticated_user_devcontainers_items {
  path: String!
  name: String
  display_name: String
}

type codespaces_repo_machines_for_authenticated_user_200_response {
  total_count: Int!
  machines: [codespace_machine]!
}

"""A description of the machine powering a codespace."""
type codespace_machine {
  """The name of the machine."""
  name: String!

  """The display name of the machine includes cores, memory, and storage."""
  display_name: String!

  """The operating system of the machine."""
  operating_system: String!

  """How much storage is available to the codespace."""
  storage_in_bytes: Int!

  """How much memory is available to the codespace."""
  memory_in_bytes: Int!

  """How many cores are available to the codespace."""
  cpus: Int!
  prebuild_availability: query_codespaces_repo_machines_for_authenticated_user_machines_items_prebuild_availability
}

"""
Whether a prebuild is currently available when creating a codespace for this machine and repository. If a branch was not specified as a ref, the default branch will be assumed. Value will be "null" if prebuilds are not supported or prebuild availability could not be determined. Value will be "none" if no prebuild is available. Latest values "ready" and "in_progress" indicate the prebuild availability status.
"""
enum query_codespaces_repo_machines_for_authenticated_user_machines_items_prebuild_availability {
  none
  ready
  in_progress
}

type codespaces_pre_flight_with_repo_for_authenticated_user_200_response {
  billable_owner: simple_user
  defaults: query_codespaces_pre_flight_with_repo_for_authenticated_user_defaults
}

type query_codespaces_pre_flight_with_repo_for_authenticated_user_defaults {
  location: String!
  devcontainer_path: String
}

"""Permission check result for a given devcontainer config."""
type Codespaces_Permissions_Check {
  """
  Whether the user has accepted the permissions defined by the devcontainer config
  """
  accepted: Boolean!
}

type codespaces_list_repo_secrets_200_response {
  total_count: Int!
  secrets: [repo_codespaces_secret]!
}

"""Set repository secrets for GitHub Codespaces."""
type repo_codespaces_secret {
  """The name of the secret."""
  name: String!
  created_at: DateTime!
  updated_at: DateTime!
}

"""Collaborator"""
type collaborator {
  login: String!
  id: BigInt!
  email: String
  name: String
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  permissions: query_repos_list_collaborators_items_permissions
  role_name: String!
  user_view_type: String
}

type query_repos_list_collaborators_items_permissions {
  pull: Boolean!
  triage: Boolean
  push: Boolean!
  maintain: Boolean
  admin: Boolean!
}

"""
Filter collaborators returned by their affiliation. `outside` means all outside collaborators of an organization-owned repository. `direct` means all collaborators with permissions to an organization-owned repository, regardless of organization membership status. `all` means all collaborators the authenticated user can see.
"""
enum queryInput_repos_list_collaborators_affiliation {
  outside
  direct
  all
}

"""
Filter collaborators by the permissions they have on the repository. If not specified, all collaborators will be returned.
"""
enum queryInput_repos_list_collaborators_permission {
  pull
  triage
  push
  maintain
  admin
}

"""Repository Collaborator Permission"""
type Repository_Collaborator_Permission {
  permission: String!
  role_name: String!
  user: nullable_collaborator
}

"""Collaborator"""
type nullable_collaborator {
  login: String!
  id: BigInt!
  email: String
  name: String
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  permissions: query_repos_get_collaborator_permission_level_user_permissions
  role_name: String!
  user_view_type: String
}

type query_repos_get_collaborator_permission_level_user_permissions {
  pull: Boolean!
  triage: Boolean
  push: Boolean!
  maintain: Boolean
  admin: Boolean!
}

"""Commit Comment"""
type Commit_Comment {
  html_url: URL!
  url: URL!
  id: Int!
  node_id: String!
  body: String!
  path: String
  position: Int
  line: Int
  commit_id: String!
  user: nullable_simple_user
  created_at: DateTime!
  updated_at: DateTime!
  author_association: author_association!
  reactions: Reaction_Rollup
}

"""
Reactions to conversations provide a way to help people express their feelings more simply and effectively.
"""
type Reaction {
  id: Int!
  node_id: String!
  user: nullable_simple_user
  content: query_reactions_list_for_commit_comment_items_content!
  created_at: DateTime!
}

"""The reaction to use"""
enum query_reactions_list_for_commit_comment_items_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

"""
Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to a commit comment.
"""
enum queryInput_reactions_list_for_commit_comment_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

"""Branch Short"""
type Branch_Short {
  name: String!
  commit: query_repos_list_branches_for_head_commit_items_commit!
  protected: Boolean!
}

type query_repos_list_branches_for_head_commit_items_commit {
  sha: String!
  url: String!
}

"""Pull Request Simple"""
type Pull_Request_Simple {
  url: URL!
  id: BigInt!
  node_id: String!
  html_url: URL!
  diff_url: URL!
  patch_url: URL!
  issue_url: URL!
  commits_url: URL!
  review_comments_url: URL!
  review_comment_url: URL!
  comments_url: URL!
  statuses_url: URL!
  number: Int!
  state: String!
  locked: Boolean!
  title: String!
  user: nullable_simple_user
  body: String
  labels: [query_repos_list_pull_requests_associated_with_commit_items_labels_items]!
  milestone: nullable_milestone
  active_lock_reason: String
  created_at: DateTime!
  updated_at: DateTime!
  closed_at: DateTime
  merged_at: DateTime
  merge_commit_sha: String
  assignee: nullable_simple_user
  assignees: [simple_user]
  requested_reviewers: [simple_user]
  requested_teams: [team]
  head: query_repos_list_pull_requests_associated_with_commit_items_head!
  base: query_repos_list_pull_requests_associated_with_commit_items_base!
  _links: query_repos_list_pull_requests_associated_with_commit_items__links!
  author_association: author_association!
  auto_merge: Auto_merge

  """Indicates whether or not the pull request is a draft."""
  draft: Boolean
}

type query_repos_list_pull_requests_associated_with_commit_items_labels_items {
  id: BigInt!
  node_id: String!
  url: String!
  name: String!
  description: String!
  color: String!
  default: Boolean!
}

type query_repos_list_pull_requests_associated_with_commit_items_head {
  label: String!
  ref: String!
  repo: repository!
  sha: String!
  user: nullable_simple_user
}

type query_repos_list_pull_requests_associated_with_commit_items_base {
  label: String!
  ref: String!
  repo: repository!
  sha: String!
  user: nullable_simple_user
}

type query_repos_list_pull_requests_associated_with_commit_items__links {
  comments: link!
  commits: link!
  statuses: link!
  html: link!
  issue: link!
  review_comments: link!
  review_comment: link!
  self: link!
}

"""Hypermedia Link"""
type link {
  href: String!
}

"""The status of auto merging a pull request."""
type Auto_merge {
  enabled_by: simple_user!
  merge_method: query_repos_list_pull_requests_associated_with_commit_items_auto_merge_merge_method!

  """Title for the merge commit message."""
  commit_title: String!

  """Commit message for the merge commit."""
  commit_message: String!
}

"""The merge method to use."""
enum query_repos_list_pull_requests_associated_with_commit_items_auto_merge_merge_method {
  merge
  squash
  rebase
}

type checks_list_for_ref_200_response {
  total_count: Int!
  check_runs: [check_run]!
}

"""
Filters check runs by their `completed_at` timestamp. `latest` returns the most recent check runs.
"""
enum queryInput_checks_list_for_ref_filter {
  latest
  all
}

type checks_list_suites_for_ref_200_response {
  total_count: Int!
  check_suites: [CheckSuite]!
}

"""Combined Commit Status"""
type Combined_Commit_Status {
  state: String!
  statuses: [Simple_Commit_Status]!
  sha: String!
  total_count: Int!
  repository: minimal_repository!
  commit_url: URL!
  url: URL!
}

type Simple_Commit_Status {
  description: String
  id: Int!
  node_id: String!
  state: String!
  context: String!
  target_url: URL
  required: Boolean
  avatar_url: URL
  url: URL!
  created_at: DateTime!
  updated_at: DateTime!
}

"""The status of a commit."""
type Status {
  url: String!
  avatar_url: String
  id: Int!
  node_id: String!
  state: String!
  description: String
  target_url: String
  context: String!
  created_at: String!
  updated_at: String!
  creator: nullable_simple_user
}

"""Community Profile"""
type Community_Profile {
  health_percentage: Int!
  description: String
  documentation: String
  files: query_repos_get_community_profile_metrics_files!
  updated_at: DateTime
  content_reports_enabled: Boolean
}

type query_repos_get_community_profile_metrics_files {
  code_of_conduct: nullable_code_of_conduct_simple
  code_of_conduct_file: Community_Health_File
  license: nullable_license_simple
  contributing: Community_Health_File
  readme: Community_Health_File
  issue_template: Community_Health_File
  pull_request_template: Community_Health_File
}

"""Code of Conduct Simple"""
type nullable_code_of_conduct_simple {
  url: URL!
  key: String!
  name: String!
  html_url: URL
}

type Community_Health_File {
  url: URL!
  html_url: URL!
}

"""Commit Comparison"""
type Commit_Comparison {
  url: URL!
  html_url: URL!
  permalink_url: URL!
  diff_url: URL!
  patch_url: URL!
  base_commit: Commit!
  merge_base_commit: Commit!
  status: query_repos_compare_commits_status!
  ahead_by: Int!
  behind_by: Int!
  total_commits: Int!
  commits: [Commit]!
  files: [Diff_Entry]
}

enum query_repos_compare_commits_status {
  diverged
  ahead
  behind
  identical
}

union repos_get_content_200_response = query_repos_get_content_oneOf_0_items | Content_File | Symlink_Content | Submodule_Content

type query_repos_get_content_oneOf_0_items {
  type: query_repos_get_content_oneOf_0_items_type!
  size: Int!
  name: String!
  path: String!
  content: String
  sha: String!
  url: URL!
  git_url: URL
  html_url: URL
  download_url: URL
  _links: query_repos_get_content_oneOf_0_items__links!
}

enum query_repos_get_content_oneOf_0_items_type {
  dir
  file
  submodule
  symlink
}

type query_repos_get_content_oneOf_0_items__links {
  git: URL
  html: URL
  self: URL!
}

"""Content File"""
type Content_File {
  type: file_const!
  encoding: String!
  size: Int!
  name: String!
  path: String!
  content: String!
  sha: String!
  url: URL!
  git_url: URL
  html_url: URL
  download_url: URL
  _links: query_repos_get_content_oneOf_1__links!
  target: String
  submodule_git_url: String
}

enum file_const {
  file
}

type query_repos_get_content_oneOf_1__links {
  git: URL
  html: URL
  self: URL!
}

"""An object describing a symlink"""
type Symlink_Content {
  type: symlink_const!
  target: String!
  size: Int!
  name: String!
  path: String!
  sha: String!
  url: URL!
  git_url: URL
  html_url: URL
  download_url: URL
  _links: query_repos_get_content_oneOf_2__links!
}

enum symlink_const {
  symlink
}

type query_repos_get_content_oneOf_2__links {
  git: URL
  html: URL
  self: URL!
}

"""An object describing a submodule"""
type Submodule_Content {
  type: submodule_const!
  submodule_git_url: URL!
  size: Int!
  name: String!
  path: String!
  sha: String!
  url: URL!
  git_url: URL
  html_url: URL
  download_url: URL
  _links: query_repos_get_content_oneOf_3__links!
}

enum submodule_const {
  submodule
}

type query_repos_get_content_oneOf_3__links {
  git: URL
  html: URL
  self: URL!
}

input queryInput_repos_get_content_input_Input {
  summary: String
}

union repos_list_contributors_response = Contributor | Void_container

"""Contributor"""
type Contributor {
  login: String
  id: Int
  node_id: String
  avatar_url: URL
  gravatar_id: String
  url: URL
  html_url: URL
  followers_url: URL
  following_url: String
  gists_url: String
  starred_url: String
  subscriptions_url: URL
  organizations_url: URL
  repos_url: URL
  events_url: String
  received_events_url: URL
  type: String!
  site_admin: Boolean
  contributions: Int!
  email: String
  name: String
  user_view_type: String
}

"""A Dependabot alert."""
type dependabot_alert {
  """The security alert number."""
  number: Int!
  state: query_dependabot_list_alerts_for_repo_items_state!
  dependency: query_dependabot_list_alerts_for_repo_items_dependency!
  security_advisory: dependabot_alert_security_advisory!
  security_vulnerability: dependabot_alert_security_vulnerability!

  """The REST API URL of the alert resource."""
  url: URL!

  """The GitHub URL of the alert resource."""
  html_url: URL!

  """
  The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime!

  """
  The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime!

  """
  The time that the alert was dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  dismissed_at: DateTime
  dismissed_by: nullable_simple_user
  dismissed_reason: query_dependabot_list_alerts_for_repo_items_dismissed_reason

  """An optional comment associated with the alert's dismissal."""
  dismissed_comment: query_dependabot_list_alerts_for_repo_items_dismissed_comment

  """
  The time that the alert was no longer detected and was considered fixed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  fixed_at: DateTime

  """
  The time that the alert was auto-dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  auto_dismissed_at: DateTime
  dismissal_request: Dependabot_alert_dismissal_request

  """The users assigned to this alert."""
  assignees: [simple_user]
}

"""The state of the Dependabot alert."""
enum query_dependabot_list_alerts_for_repo_items_state {
  auto_dismissed
  dismissed
  fixed
  open
}

"""Details for the vulnerable dependency."""
type query_dependabot_list_alerts_for_repo_items_dependency {
  package: dependabot_alert_package

  """
  The full path to the dependency manifest file, relative to the root of the repository.
  """
  manifest_path: String
  scope: query_dependabot_list_alerts_for_repo_items_dependency_scope
  relationship: query_dependabot_list_alerts_for_repo_items_dependency_relationship
}

"""The execution scope of the vulnerable dependency."""
enum query_dependabot_list_alerts_for_repo_items_dependency_scope {
  development
  runtime
}

"""
The vulnerable dependency's relationship to your project.

> [!NOTE]
> We are rolling out support for dependency relationship across ecosystems. This value will be "unknown" for all dependencies in unsupported ecosystems.
"""
enum query_dependabot_list_alerts_for_repo_items_dependency_relationship {
  unknown
  direct
  transitive
}

"""The reason that the alert was dismissed."""
enum query_dependabot_list_alerts_for_repo_items_dismissed_reason {
  fix_started
  inaccurate
  no_bandwidth
  not_used
  tolerable_risk
}

"""An optional comment associated with the alert's dismissal."""
scalar query_dependabot_list_alerts_for_repo_items_dismissed_comment

type dependabot_list_repo_secrets_200_response {
  total_count: Int!
  secrets: [Dependabot_Secret]!
}

"""Set secrets for Dependabot."""
type Dependabot_Secret {
  """The name of the secret."""
  name: String!
  created_at: DateTime!
  updated_at: DateTime!
}

type query_dependency_graph_diff_range_items {
  change_type: query_dependency_graph_diff_range_items_change_type!
  manifest: String!
  ecosystem: String!
  name: String!
  version: String!
  package_url: URL
  license: String
  source_repository_url: URL
  vulnerabilities: [query_dependency_graph_diff_range_items_vulnerabilities_items]!
  scope: query_dependency_graph_diff_range_items_scope!
}

enum query_dependency_graph_diff_range_items_change_type {
  added
  removed
}

type query_dependency_graph_diff_range_items_vulnerabilities_items {
  severity: String!
  advisory_ghsa_id: String!
  advisory_summary: String!
  advisory_url: URL!
}

"""
Where the dependency is utilized. `development` means that the dependency is only utilized in the development environment. `runtime` means that the dependency is utilized at runtime and in the development environment.
"""
enum query_dependency_graph_diff_range_items_scope {
  unknown
  runtime
  development
}

"""A schema for the SPDX JSON format returned by the Dependency Graph."""
type Dependency_Graph_SPDX_SBOM {
  sbom: query_dependency_graph_export_sbom_sbom!
}

type query_dependency_graph_export_sbom_sbom {
  """The SPDX identifier for the SPDX document."""
  SPDXID: String!

  """The version of the SPDX specification that this document conforms to."""
  spdxVersion: String!

  """An optional comment about the SPDX document."""
  comment: String
  creationInfo: query_dependency_graph_export_sbom_sbom_creationInfo!

  """The name of the SPDX document."""
  name: String!

  """The license under which the SPDX document is licensed."""
  dataLicense: String!

  """The namespace for the SPDX document."""
  documentNamespace: URL!
  packages: [query_dependency_graph_export_sbom_sbom_packages_items]!
  relationships: [query_dependency_graph_export_sbom_sbom_relationships_items]
}

type query_dependency_graph_export_sbom_sbom_creationInfo {
  """The date and time the SPDX document was created."""
  created: DateTime!

  """The tools that were used to generate the SPDX document."""
  creators: [String]!
}

type query_dependency_graph_export_sbom_sbom_packages_items {
  """A unique SPDX identifier for the package."""
  SPDXID: String

  """The name of the package."""
  name: String

  """
  The version of the package. If the package does not have an exact version specified,
  a version range is given.
  """
  versionInfo: String

  """
  The location where the package can be downloaded,
  or NOASSERTION if this has not been determined.
  """
  downloadLocation: String

  """
  Whether the package's file content has been subjected to
  analysis during the creation of the SPDX document.
  """
  filesAnalyzed: Boolean

  """
  The license of the package as determined while creating the SPDX document.
  """
  licenseConcluded: String

  """
  The license of the package as declared by its author, or NOASSERTION if this information
  was not available when the SPDX document was created.
  """
  licenseDeclared: String

  """
  The distribution source of this package, or NOASSERTION if this was not determined.
  """
  supplier: String

  """
  The copyright holders of the package, and any dates present with those notices, if available.
  """
  copyrightText: String
  externalRefs: [query_dependency_graph_export_sbom_sbom_packages_items_externalRefs_items]
}

type query_dependency_graph_export_sbom_sbom_packages_items_externalRefs_items {
  """
  The category of reference to an external resource this reference refers to.
  """
  referenceCategory: String!

  """
  A locator for the particular external resource this reference refers to.
  """
  referenceLocator: URL!

  """
  The category of reference to an external resource this reference refers to.
  """
  referenceType: String!
}

type query_dependency_graph_export_sbom_sbom_relationships_items {
  """The type of relationship between the two SPDX elements."""
  relationshipType: String

  """
  The SPDX identifier of the package that is the source of the relationship.
  """
  spdxElementId: String

  """
  The SPDX identifier of the package that is the target of the relationship.
  """
  relatedSpdxElement: String
}

"""A request for a specific ref(branch,sha,tag) to be deployed"""
type deployment {
  url: URL!

  """Unique identifier of the deployment"""
  id: BigInt!
  node_id: String!
  sha: String!

  """The ref to deploy. This can be a branch, tag, or sha."""
  ref: String!

  """Parameter to specify a task to execute"""
  task: String!
  payload: query_repos_list_deployments_items_payload!
  original_environment: String

  """Name for the target deployment environment."""
  environment: String!
  description: String
  creator: nullable_simple_user
  created_at: DateTime!
  updated_at: DateTime!
  statuses_url: URL!
  repository_url: URL!

  """
  Specifies if the given environment is will no longer exist at some point in the future. Default: false.
  """
  transient_environment: Boolean

  """
  Specifies if the given environment is one that end-users directly interact with. Default: false.
  """
  production_environment: Boolean
  performed_via_github_app: nullable_integration
}

union query_repos_list_deployments_items_payload = JSON_container | String_container

"""The status of a deployment."""
type Deployment_Status {
  url: URL!
  id: BigInt!
  node_id: String!
  state: query_repos_list_deployment_statuses_items_state!
  creator: nullable_simple_user

  """A short description of the status."""
  description: query_repos_list_deployment_statuses_items_description!

  """The environment of the deployment that the status is for."""
  environment: String

  """Closing down notice: the URL to associate with this status."""
  target_url: URL!
  created_at: DateTime!
  updated_at: DateTime!
  deployment_url: URL!
  repository_url: URL!

  """The URL for accessing your environment."""
  environment_url: URL

  """The URL to associate with this status."""
  log_url: URL
  performed_via_github_app: nullable_integration
}

"""The state of the status."""
enum query_repos_list_deployment_statuses_items_state {
  error
  failure
  inactive
  pending
  success
  queued
  in_progress
}

"""A short description of the status."""
scalar query_repos_list_deployment_statuses_items_description

type repos_get_all_environments_200_response {
  """The number of environments in this repository"""
  total_count: Int
  environments: [Environment]
}

"""Details of a deployment environment"""
type Environment {
  """The id of the environment."""
  id: BigInt!
  node_id: String!

  """The name of the environment."""
  name: String!
  url: URL!
  html_url: URL!

  """The time that the environment was created, in ISO 8601 format."""
  created_at: DateTime!

  """The time that the environment was last updated, in ISO 8601 format."""
  updated_at: DateTime!

  """Built-in deployment protection rules for the environment."""
  protection_rules: [query_repos_get_all_environments_environments_items_protection_rules_items]
  deployment_branch_policy: deployment_branch_policy_settings
}

type query_repos_get_all_environments_environments_items_protection_rules_items {
  id: Int!
  node_id: String!
  type: String!

  """
  The amount of time to delay a job after the job is initially triggered. The time (in minutes) must be an integer between 0 and 43,200 (30 days).
  """
  wait_timer: Int

  """
  Whether deployments to this environment can be approved by the user who created the deployment.
  """
  prevent_self_review: Boolean

  """
  The people or teams that may approve jobs that reference the environment. You can list up to six users or teams as reviewers. The reviewers must have at least read access to the repository. Only one of the required reviewers needs to approve the job for it to proceed.
  """
  reviewers: [query_repos_get_all_environments_environments_items_protection_rules_items_anyOf_1_reviewers_items]
}

type query_repos_get_all_environments_environments_items_protection_rules_items_anyOf_1_reviewers_items {
  type: deployment_reviewer_type
  reviewer: query_repos_get_all_environments_environments_items_protection_rules_items_anyOf_1_reviewers_items_reviewer
}

type query_repos_get_all_environments_environments_items_protection_rules_items_anyOf_1_reviewers_items_reviewer {
  name: String!
  email: String
  login: String!
  id: JSON
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: JSON
  site_admin: Boolean!
  starred_at: String
  user_view_type: String
  additionalProperties: JSON
  slug: String!
  description: String
  privacy: String
  notification_setting: String
  permission: String!
  permissions: query_campaigns_list_org_campaigns_items_team_managers_items_permissions
  members_url: String!
  repositories_url: URL!

  """Unique identifier of the organization to which this team belongs"""
  organization_id: Int

  """Unique identifier of the enterprise to which this team belongs"""
  enterprise_id: Int
  parent: nullable_team_simple
}

"""
The type of deployment branch policy for this environment. To allow all branches to deploy, set to `null`.
"""
type deployment_branch_policy_settings {
  """
  Whether only branches with branch protection rules can deploy to this environment. If `protected_branches` is `true`, `custom_branch_policies` must be `false`; if `protected_branches` is `false`, `custom_branch_policies` must be `true`.
  """
  protected_branches: Boolean!

  """
  Whether only branches that match the specified name patterns can deploy to this environment.  If `custom_branch_policies` is `true`, `protected_branches` must be `false`; if `custom_branch_policies` is `false`, `protected_branches` must be `true`.
  """
  custom_branch_policies: Boolean!
}

type repos_list_deployment_branch_policies_200_response {
  """The number of deployment branch policies for the environment."""
  total_count: Int!
  branch_policies: [Deployment_branch_policy]!
}

"""Details of a deployment branch or tag policy."""
type Deployment_branch_policy {
  """The unique identifier of the branch or tag policy."""
  id: Int
  node_id: String

  """
  The name pattern that branches or tags must match in order to deploy to the environment.
  """
  name: String
  type: query_repos_list_deployment_branch_policies_branch_policies_items_type
}

"""Whether this rule targets a branch or tag."""
enum query_repos_list_deployment_branch_policies_branch_policies_items_type {
  branch
  tag
}

type repos_get_all_deployment_protection_rules_200_response {
  """
  The number of enabled custom deployment protection rules for this environment
  """
  total_count: Int
  custom_deployment_protection_rules: [Deployment_protection_rule]
}

"""Deployment protection rule"""
type Deployment_protection_rule {
  """The unique identifier for the deployment protection rule."""
  id: Int!

  """The node ID for the deployment protection rule."""
  node_id: String!

  """Whether the deployment protection rule is enabled for the environment."""
  enabled: Boolean!
  app: Custom_deployment_protection_rule_app!
}

"""A GitHub App that is providing a custom deployment protection rule."""
type Custom_deployment_protection_rule_app {
  """The unique identifier of the deployment protection rule integration."""
  id: Int!

  """The slugified name of the deployment protection rule integration."""
  slug: String!

  """The URL for the endpoint to get details about the app."""
  integration_url: URL!

  """The node ID for the deployment protection rule integration."""
  node_id: String!
}

type repos_list_custom_deployment_rule_integrations_200_response {
  """
  The total number of custom deployment protection rule integrations available for this environment.
  """
  total_count: Int
  available_custom_deployment_protection_rule_integrations: [Custom_deployment_protection_rule_app]
}

type actions_list_environment_secrets_200_response {
  total_count: Int!
  secrets: [Actions_Secret]!
}

type actions_list_environment_variables_200_response {
  total_count: Int!
  variables: [Actions_Variable]!
}

"""The sort order. `stargazers` will sort by star count."""
enum queryInput_repos_list_forks_sort {
  newest
  oldest
  stargazers
  watchers
}

"""Blob"""
type Blob {
  content: String!
  encoding: String!
  url: URL!
  sha: String!
  size: Int
  node_id: String!
  highlighted_content: String
}

"""Low-level Git commit operations within a repository"""
type Git_Commit {
  """SHA for the commit"""
  sha: String!
  node_id: String!
  url: URL!
  author: query_git_get_commit_author!
  committer: query_git_get_commit_committer!

  """Message describing the purpose of the commit"""
  message: String!
  tree: query_git_get_commit_tree!
  parents: [query_git_get_commit_parents_items]!
  verification: query_git_get_commit_verification!
  html_url: URL!
}

"""Identifying information for the git-user"""
type query_git_get_commit_author {
  """Timestamp of the commit"""
  date: DateTime!

  """Git email address of the user"""
  email: EmailAddress!

  """Name of the git user"""
  name: String!
}

"""Identifying information for the git-user"""
type query_git_get_commit_committer {
  """Timestamp of the commit"""
  date: DateTime!

  """Git email address of the user"""
  email: EmailAddress!

  """Name of the git user"""
  name: String!
}

type query_git_get_commit_tree {
  """SHA for the commit"""
  sha: String!
  url: URL!
}

type query_git_get_commit_parents_items {
  """SHA for the commit"""
  sha: String!
  url: URL!
  html_url: URL!
}

type query_git_get_commit_verification {
  verified: Boolean!
  reason: String!
  signature: String
  payload: String
  verified_at: String
}

"""Git references within a repository"""
type Git_Reference {
  ref: String!
  node_id: String!
  url: URL!
  object: query_git_list_matching_refs_items_object!
}

type query_git_list_matching_refs_items_object {
  type: String!

  """SHA for the reference"""
  sha: query_git_list_matching_refs_items_object_sha!
  url: URL!
}

"""SHA for the reference"""
scalar query_git_list_matching_refs_items_object_sha

"""Metadata for a Git tag"""
type Git_Tag {
  node_id: String!

  """Name of the tag"""
  tag: String!
  sha: String!

  """URL for the tag"""
  url: URL!

  """Message describing the purpose of the tag"""
  message: String!
  tagger: query_git_get_tag_tagger!
  object: query_git_get_tag_object!
  verification: Verification
}

type query_git_get_tag_tagger {
  date: String!
  email: String!
  name: String!
}

type query_git_get_tag_object {
  sha: String!
  type: String!
  url: URL!
}

"""The hierarchy between files in a Git repository."""
type Git_Tree {
  sha: String!
  url: URL
  truncated: Boolean!

  """Objects specifying a tree structure"""
  tree: [query_git_get_tree_tree_items]!
}

type query_git_get_tree_tree_items {
  path: String!
  mode: String!
  type: String!
  sha: String!
  size: Int
  url: URL
}

"""Webhooks for repositories."""
type Webhook {
  type: String!

  """Unique identifier of the webhook."""
  id: Int!

  """The name of a valid service, use 'web' for a webhook."""
  name: String!

  """Determines whether the hook is actually triggered on pushes."""
  active: Boolean!

  """Determines what events the hook is triggered for. Default: ['push']."""
  events: [String]!
  config: Webhook_Configuration!
  updated_at: DateTime!
  created_at: DateTime!
  url: URL!
  test_url: URL!
  ping_url: URL!
  deliveries_url: URL
  last_response: Hook_Response!
}

type Hook_Response {
  code: Int
  status: String
  message: String
}

"""Check immutable releases"""
type Check_immutable_releases {
  """Whether immutable releases are enabled for the repository."""
  enabled: Boolean!

  """Whether immutable releases are enforced by the repository owner."""
  enforced_by_owner: Boolean!
}

"""A repository import from an external source."""
type Import {
  vcs: String
  use_lfs: Boolean

  """The URL of the originating repository."""
  vcs_url: String!
  svc_root: String
  tfvc_project: String
  status: query_migrations_get_import_status_status!
  status_text: String
  failed_step: String
  error_message: String
  import_percent: Int
  commit_count: Int
  push_percent: Int
  has_large_files: Boolean
  large_files_size: Int
  large_files_count: Int
  project_choices: [query_migrations_get_import_status_project_choices_items]
  message: String
  authors_count: Int
  url: URL!
  html_url: URL!
  authors_url: URL!
  repository_url: URL!
  svn_root: String
}

enum query_migrations_get_import_status_status {
  auth
  error
  none
  detecting
  choose
  auth_failed
  importing
  mapping
  waiting_to_push
  pushing
  complete
  setup
  unknown
  detection_found_multiple
  detection_found_nothing
  detection_needs_auth
}

type query_migrations_get_import_status_project_choices_items {
  vcs: String
  tfvc_project: String
  human_name: String
}

"""Porter Author"""
type Porter_Author {
  id: Int!
  remote_id: String!
  remote_name: String!
  email: String!
  name: String!
  url: URL!
  import_url: URL!
}

"""Porter Large File"""
type Porter_Large_File {
  ref_name: String!
  path: String!
  oid: String!
  size: Int!
}

type interactions_get_restrictions_for_repo_200_response {
  limit: interaction_group!
  origin: String!
  expires_at: DateTime!
  JSON: JSON
}

"""Repository invitations let you manage who you collaborate with."""
type repository_invitation {
  """Unique identifier of the repository invitation."""
  id: BigInt!
  repository: minimal_repository!
  invitee: nullable_simple_user
  inviter: nullable_simple_user
  permissions: query_repos_list_invitations_items_permissions!
  created_at: DateTime!

  """Whether or not the invitation has expired"""
  expired: Boolean

  """URL for the repository invitation"""
  url: URL!
  html_url: URL!
  node_id: String!
}

"""The permission associated with the invitation."""
enum query_repos_list_invitations_items_permissions {
  read
  write
  admin
  triage
  maintain
}

"""Indicates the state of the issues to return."""
enum queryInput_issues_list_for_repo_state {
  open
  closed
  all
}

"""What to sort results by."""
enum queryInput_issues_list_for_repo_sort {
  created
  updated
  comments
}

"""Either `asc` or `desc`. Ignored without the `sort` parameter."""
enum queryInput_issues_list_comments_for_repo_direction {
  asc
  desc
}

"""
Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to an issue comment.
"""
enum queryInput_reactions_list_for_issue_comment_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

"""Issue Event"""
type Issue_Event {
  id: BigInt!
  node_id: String!
  url: URL!
  actor: nullable_simple_user
  event: String!
  commit_id: String
  commit_url: URL
  created_at: DateTime!
  issue: nullable_issue
  label: Issue_Event_Label
  assignee: nullable_simple_user
  assigner: nullable_simple_user
  review_requester: nullable_simple_user
  requested_reviewer: nullable_simple_user
  requested_team: team
  dismissed_review: Issue_Event_Dismissed_Review
  milestone: Issue_Event_Milestone
  project_card: Issue_Event_Project_Card
  rename: Issue_Event_Rename
  author_association: author_association
  lock_reason: String
  performed_via_github_app: nullable_integration
}

"""
Issues are a great way to keep track of tasks, enhancements, and bugs for your projects.
"""
type nullable_issue {
  id: BigInt!
  node_id: String!

  """URL for the issue"""
  url: URL!
  repository_url: URL!
  labels_url: String!
  comments_url: URL!
  events_url: URL!
  html_url: URL!

  """Number uniquely identifying the issue within its repository"""
  number: Int!

  """State of the issue; either 'open' or 'closed'"""
  state: String!
  state_reason: query_issues_list_events_for_repo_items_issue_state_reason

  """Title of the issue"""
  title: String!

  """Contents of the issue"""
  body: String
  user: nullable_simple_user

  """
  Labels to associate with this issue; pass one or more label names to replace the set of labels on this issue; send an empty array to clear all labels from the issue; note that the labels are silently dropped for users without push access to the repository
  """
  labels: [query_issues_list_events_for_repo_items_issue_labels_items]!
  assignee: nullable_simple_user
  assignees: [simple_user]
  milestone: nullable_milestone
  locked: Boolean!
  active_lock_reason: String
  comments: Int!
  pull_request: query_issues_list_events_for_repo_items_issue_pull_request
  closed_at: DateTime
  created_at: DateTime!
  updated_at: DateTime!
  draft: Boolean
  closed_by: nullable_simple_user
  body_html: String
  body_text: String
  timeline_url: URL
  type: Issue_Type
  repository: repository
  performed_via_github_app: nullable_integration
  author_association: author_association
  reactions: Reaction_Rollup
  sub_issues_summary: Sub_issues_Summary

  """URL to get the parent issue of this issue, if it is a sub-issue"""
  parent_issue_url: URL
  pinned_comment: nullable_issue_comment
  issue_dependencies_summary: Issue_Dependencies_Summary
  issue_field_values: [Issue_Field_Value]
}

"""The reason for the current state"""
enum query_issues_list_events_for_repo_items_issue_state_reason {
  completed
  reopened
  not_planned
  duplicate
}

union query_issues_list_events_for_repo_items_issue_labels_items = String_container | query_issues_list_events_for_repo_items_issue_labels_items_oneOf_1

type query_issues_list_events_for_repo_items_issue_labels_items_oneOf_1 {
  id: BigInt
  node_id: String
  url: URL
  name: String
  description: String
  color: String
  default: Boolean
}

type query_issues_list_events_for_repo_items_issue_pull_request {
  merged_at: DateTime
  diff_url: URL
  html_url: URL
  patch_url: URL
  url: URL
}

"""Issue Event Label"""
type Issue_Event_Label {
  name: String
  color: String
}

type Issue_Event_Dismissed_Review {
  state: String!
  review_id: Int!
  dismissal_message: String
  dismissal_commit_id: String
}

"""Issue Event Milestone"""
type Issue_Event_Milestone {
  title: String!
}

"""Issue Event Project Card"""
type Issue_Event_Project_Card {
  url: URL!
  id: Int!
  project_url: URL!
  project_id: Int!
  column_name: String!
  previous_column_name: String
}

"""Issue Event Rename"""
type Issue_Event_Rename {
  from: String!
  to: String!
}

"""Issue Event for Issue"""
type Issue_Event_for_Issue {
  id: Int!
  node_id: String!
  url: String!
  actor: simple_user!
  event: String!
  commit_id: String
  commit_url: String
  created_at: String!
  performed_via_github_app: JSON
  label: JSON
  assignee: simple_user!
  assigner: simple_user!
  milestone: JSON
  rename: query_issues_list_events_items_anyOf_6_rename!
  review_requester: simple_user!
  requested_team: team
  requested_reviewer: simple_user
  dismissed_review: query_issues_list_events_items_anyOf_9_dismissed_review!
  lock_reason: String
  project_card: JSON
}

type query_issues_list_events_items_anyOf_6_rename {
  from: String!
  to: String!
}

type query_issues_list_events_items_anyOf_9_dismissed_review {
  state: String!
  review_id: Int!
  dismissal_message: String
  dismissal_commit_id: String
}

"""
Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to an issue.
"""
enum queryInput_reactions_list_for_issue_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

"""Timeline Event"""
type Timeline_Event {
  id: Int!
  node_id: String!
  url: JSON
  actor: simple_user!
  event: String!
  commit_id: String
  commit_url: String
  created_at: JSON
  performed_via_github_app: JSON
  label: JSON
  milestone: JSON
  rename: query_issues_list_events_items_anyOf_6_rename!
  review_requester: simple_user!
  requested_team: team
  requested_reviewer: simple_user
  dismissed_review: query_issues_list_events_items_anyOf_9_dismissed_review!
  lock_reason: String
  project_card: JSON

  """The text of the review."""
  body: String
  body_text: String
  body_html: String
  html_url: URL!
  user: simple_user!
  updated_at: DateTime
  issue_url: URL!
  author_association: author_association!
  reactions: Reaction_Rollup
  pin: Pinned_Issue_Comment
  source: query_issues_list_events_for_timeline_items_anyOf_14_source!

  """SHA for the commit"""
  sha: String!
  author: query_issues_list_events_for_timeline_items_anyOf_15_author!
  committer: query_issues_list_events_for_timeline_items_anyOf_15_committer!

  """Message describing the purpose of the commit"""
  message: String!
  tree: query_issues_list_events_for_timeline_items_anyOf_15_tree!
  parents: [query_issues_list_events_for_timeline_items_anyOf_15_parents_items]!
  verification: query_issues_list_events_for_timeline_items_anyOf_15_verification!
  state: String!
  pull_request_url: URL!
  _links: query_issues_list_events_for_timeline_items_anyOf_16__links!
  submitted_at: DateTime
  comments: JSON
  assignee: simple_user!
  state_reason: String
}

type query_issues_list_events_for_timeline_items_anyOf_14_source {
  type: String
  issue: issue
}

"""Identifying information for the git-user"""
type query_issues_list_events_for_timeline_items_anyOf_15_author {
  """Timestamp of the commit"""
  date: DateTime!

  """Git email address of the user"""
  email: EmailAddress!

  """Name of the git user"""
  name: String!
}

"""Identifying information for the git-user"""
type query_issues_list_events_for_timeline_items_anyOf_15_committer {
  """Timestamp of the commit"""
  date: DateTime!

  """Git email address of the user"""
  email: EmailAddress!

  """Name of the git user"""
  name: String!
}

type query_issues_list_events_for_timeline_items_anyOf_15_tree {
  """SHA for the commit"""
  sha: String!
  url: URL!
}

type query_issues_list_events_for_timeline_items_anyOf_15_parents_items {
  """SHA for the commit"""
  sha: String!
  url: URL!
  html_url: URL!
}

type query_issues_list_events_for_timeline_items_anyOf_15_verification {
  verified: Boolean!
  reason: String!
  signature: String
  payload: String
  verified_at: String
}

type query_issues_list_events_for_timeline_items_anyOf_16__links {
  html: query_issues_list_events_for_timeline_items_anyOf_16__links_html!
  pull_request: query_issues_list_events_for_timeline_items_anyOf_16__links_pull_request!
}

type query_issues_list_events_for_timeline_items_anyOf_16__links_html {
  href: String!
}

type query_issues_list_events_for_timeline_items_anyOf_16__links_pull_request {
  href: String!
}

"""An SSH key granting access to a single repository."""
type Deploy_Key {
  id: Int!
  key: String!
  url: String!
  title: String!
  verified: Boolean!
  created_at: String!
  read_only: Boolean!
  added_by: String
  last_used: DateTime
  enabled: Boolean
}

"""License Content"""
type License_Content {
  name: String!
  path: String!
  sha: String!
  size: Int!
  url: URL!
  html_url: URL
  git_url: URL
  download_url: URL
  type: String!
  content: String!
  encoding: String!
  _links: query_licenses_get_for_repo__links!
  license: nullable_license_simple
}

type query_licenses_get_for_repo__links {
  git: URL
  html: URL
  self: URL!
}

"""A collection of related issues and pull requests."""
type milestone {
  url: URL!
  html_url: URL!
  labels_url: URL!
  id: Int!
  node_id: String!

  """The number of the milestone."""
  number: Int!
  state: query_issues_list_milestones_items_state!

  """The title of the milestone."""
  title: String!
  description: String
  creator: nullable_simple_user
  open_issues: Int!
  closed_issues: Int!
  created_at: DateTime!
  updated_at: DateTime!
  closed_at: DateTime
  due_on: DateTime
}

"""The state of the milestone."""
enum query_issues_list_milestones_items_state {
  open
  closed
}

"""The state of the milestone. Either `open`, `closed`, or `all`."""
enum queryInput_issues_list_milestones_state {
  open
  closed
  all
}

"""What to sort results by. Either `due_on` or `completeness`."""
enum queryInput_issues_list_milestones_sort {
  due_on
  completeness
}

"""The direction of the sort. Either `asc` or `desc`."""
enum queryInput_issues_list_milestones_direction {
  asc
  desc
}

"""The configuration for GitHub Pages for a repository."""
type page {
  """The API address for accessing this Page resource."""
  url: URL!
  status: query_repos_get_pages_status

  """The Pages site's custom domain"""
  cname: String
  protected_domain_state: query_repos_get_pages_protected_domain_state

  """The timestamp when a pending domain becomes unverified."""
  pending_domain_unverified_at: DateTime

  """Whether the Page has a custom 404 page."""
  custom_404: Boolean!

  """The web address the Page can be accessed from."""
  html_url: URL
  build_type: query_repos_get_pages_build_type
  source: Pages_Source_Hash

  """
  Whether the GitHub Pages site is publicly visible. If set to `true`, the site is accessible to anyone on the internet. If set to `false`, the site will only be accessible to users who have at least `read` access to the repository that published the site.
  """
  public: Boolean!
  https_certificate: Pages_Https_Certificate

  """Whether https is enabled on the domain"""
  https_enforced: Boolean
}

"""The status of the most recent build of the Page."""
enum query_repos_get_pages_status {
  built
  building
  errored
}

"""The state if the domain is verified"""
enum query_repos_get_pages_protected_domain_state {
  pending
  verified
  unverified
}

"""The process in which the Page will be built."""
enum query_repos_get_pages_build_type {
  legacy
  workflow
}

type Pages_Source_Hash {
  branch: String!
  path: String!
}

type Pages_Https_Certificate {
  state: query_repos_get_pages_https_certificate_state!
  description: String!

  """Array of the domain set and its alternate name (if it is configured)"""
  domains: [String]!
  expires_at: Date
}

enum query_repos_get_pages_https_certificate_state {
  new
  authorization_created
  authorization_pending
  authorized
  authorization_revoked
  issued
  uploaded
  approved
  errored
  bad_authz
  destroy_pending
  dns_changed
}

"""Page Build"""
type Page_Build {
  url: URL!
  status: String!
  error: query_repos_list_pages_builds_items_error!
  pusher: nullable_simple_user
  commit: String!
  duration: Int!
  created_at: DateTime!
  updated_at: DateTime!
}

type query_repos_list_pages_builds_items_error {
  message: String
}

type GitHub_Pages_deployment_status {
  status: query_repos_get_pages_deployment_status
}

"""The current status of the deployment."""
enum query_repos_get_pages_deployment_status {
  deployment_in_progress
  syncing_files
  finished_file_sync
  updating_pages
  purging_cdn
  deployment_cancelled
  deployment_failed
  deployment_content_failed
  deployment_attempt_error
  deployment_lost
  succeed
}

input pages_deployment_id_parameter_Input {
  Int: Int
  String: String
}

union repos_get_pages_health_check_response = Pages_Health_Check_Status | JSON_container

"""Pages Health Check Status"""
type Pages_Health_Check_Status {
  domain: query_repos_get_pages_health_check_oneOf_0_domain
  alt_domain: query_repos_get_pages_health_check_oneOf_0_alt_domain
}

type query_repos_get_pages_health_check_oneOf_0_domain {
  host: String
  uri: String
  nameservers: String
  dns_resolves: Boolean
  is_proxied: Boolean
  is_cloudflare_ip: Boolean
  is_fastly_ip: Boolean
  is_old_ip_address: Boolean
  is_a_record: Boolean
  has_cname_record: Boolean
  has_mx_records_present: Boolean
  is_valid_domain: Boolean
  is_apex_domain: Boolean
  should_be_a_record: Boolean
  is_cname_to_github_user_domain: Boolean
  is_cname_to_pages_dot_github_dot_com: Boolean
  is_cname_to_fastly: Boolean
  is_pointed_to_github_pages_ip: Boolean
  is_non_github_pages_ip_present: Boolean
  is_pages_domain: Boolean
  is_served_by_pages: Boolean
  is_valid: Boolean
  reason: String
  responds_to_https: Boolean
  enforces_https: Boolean
  https_error: String
  is_https_eligible: Boolean
  caa_error: String
}

type query_repos_get_pages_health_check_oneOf_0_alt_domain {
  host: String
  uri: String
  nameservers: String
  dns_resolves: Boolean
  is_proxied: Boolean
  is_cloudflare_ip: Boolean
  is_fastly_ip: Boolean
  is_old_ip_address: Boolean
  is_a_record: Boolean
  has_cname_record: Boolean
  has_mx_records_present: Boolean
  is_valid_domain: Boolean
  is_apex_domain: Boolean
  should_be_a_record: Boolean
  is_cname_to_github_user_domain: Boolean
  is_cname_to_pages_dot_github_dot_com: Boolean
  is_cname_to_fastly: Boolean
  is_pointed_to_github_pages_ip: Boolean
  is_non_github_pages_ip_present: Boolean
  is_pages_domain: Boolean
  is_served_by_pages: Boolean
  is_valid: Boolean
  reason: String
  responds_to_https: Boolean
  enforces_https: Boolean
  https_error: String
  is_https_eligible: Boolean
  caa_error: String
}

type repos_check_private_vulnerability_reporting_200_response {
  """
  Whether or not private vulnerability reporting is enabled for the repository.
  """
  enabled: Boolean!
}

"""Either `open`, `closed`, or `all` to filter by state."""
enum queryInput_pulls_list_state {
  open
  closed
  all
}

"""
What to sort results by. `popularity` will sort by the number of comments. `long-running` will sort by date created and will limit the results to pull requests that have been open for more than a month and have had activity within the past month.
"""
enum queryInput_pulls_list_sort {
  created
  updated
  popularity
  long_running
}

"""
The direction of the sort. Default: `desc` when sort is `created` or sort is not specified, otherwise `asc`.
"""
enum queryInput_pulls_list_direction {
  asc
  desc
}

"""
Pull Request Review Comments are comments on a portion of the Pull Request's diff.
"""
type pull_request_review_comment {
  """URL for the pull request review comment"""
  url: URL!

  """The ID of the pull request review to which the comment belongs."""
  pull_request_review_id: BigInt

  """The ID of the pull request review comment."""
  id: BigInt!

  """The node ID of the pull request review comment."""
  node_id: String!

  """The diff of the line that the comment refers to."""
  diff_hunk: String!

  """The relative path of the file to which the comment applies."""
  path: String!

  """
  The line index in the diff to which the comment applies. This field is closing down; use `line` instead.
  """
  position: Int

  """
  The index of the original line in the diff to which the comment applies. This field is closing down; use `original_line` instead.
  """
  original_position: Int

  """The SHA of the commit to which the comment applies."""
  commit_id: String!

  """The SHA of the original commit to which the comment applies."""
  original_commit_id: String!

  """The comment ID to reply to."""
  in_reply_to_id: Int
  user: nullable_simple_user

  """The text of the comment."""
  body: String!
  created_at: DateTime!
  updated_at: DateTime!

  """HTML URL for the pull request review comment."""
  html_url: URL!

  """URL for the pull request that the review comment belongs to."""
  pull_request_url: URL!
  author_association: author_association!
  _links: query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links!

  """The first line of the range for a multi-line comment."""
  start_line: Int

  """The first line of the range for a multi-line comment."""
  original_start_line: Int
  start_side: query_issues_list_events_for_timeline_items_anyOf_17_comments_items_start_side

  """
  The line of the blob to which the comment applies. The last line of the range for a multi-line comment
  """
  line: Int

  """
  The line of the blob to which the comment applies. The last line of the range for a multi-line comment
  """
  original_line: Int
  side: query_issues_list_events_for_timeline_items_anyOf_17_comments_items_side
  subject_type: query_issues_list_events_for_timeline_items_anyOf_17_comments_items_subject_type
  reactions: Reaction_Rollup
  body_html: String
  body_text: String
}

type query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links {
  self: query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links_self!
  html: query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links_html!
  pull_request: query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links_pull_request!
}

type query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links_self {
  href: URL!
}

type query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links_html {
  href: URL!
}

type query_issues_list_events_for_timeline_items_anyOf_17_comments_items__links_pull_request {
  href: URL!
}

"""The side of the first line of the range for a multi-line comment."""
enum query_issues_list_events_for_timeline_items_anyOf_17_comments_items_start_side {
  LEFT
  RIGHT
}

"""
The side of the diff to which the comment applies. The side of the last line of the range for a multi-line comment
"""
enum query_issues_list_events_for_timeline_items_anyOf_17_comments_items_side {
  LEFT
  RIGHT
}

"""
The level at which the comment is targeted, can be a diff line or a file.
"""
enum query_issues_list_events_for_timeline_items_anyOf_17_comments_items_subject_type {
  line
  file
}

enum queryInput_pulls_list_review_comments_for_repo_sort {
  created
  updated
  created_at
}

"""The direction to sort results. Ignored without `sort` parameter."""
enum queryInput_pulls_list_review_comments_for_repo_direction {
  asc
  desc
}

"""
Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to a pull request review comment.
"""
enum queryInput_reactions_list_for_pull_request_review_comment_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

"""
Pull requests let you tell others about changes you've pushed to a repository on GitHub. Once a pull request is sent, interested parties can review the set of changes, discuss potential modifications, and even push follow-up commits if necessary.
"""
type pull_request2 {
  url: URL!
  id: BigInt!
  node_id: String!
  html_url: URL!
  diff_url: URL!
  patch_url: URL!
  issue_url: URL!
  commits_url: URL!
  review_comments_url: URL!
  review_comment_url: URL!
  comments_url: URL!
  statuses_url: URL!

  """Number uniquely identifying the pull request within its repository."""
  number: Int!
  state: query_pulls_get_state!
  locked: Boolean!

  """The title of the pull request."""
  title: String!
  user: simple_user!
  body: String
  labels: [query_pulls_get_labels_items]!
  milestone: nullable_milestone
  active_lock_reason: String
  created_at: DateTime!
  updated_at: DateTime!
  closed_at: DateTime
  merged_at: DateTime
  merge_commit_sha: String
  assignee: nullable_simple_user
  assignees: [simple_user]
  requested_reviewers: [simple_user]
  requested_teams: [team_simple]
  head: query_pulls_get_head!
  base: query_pulls_get_base!
  _links: query_pulls_get__links!
  author_association: author_association!
  auto_merge: Auto_merge

  """Indicates whether or not the pull request is a draft."""
  draft: Boolean
  merged: Boolean!
  mergeable: Boolean
  rebaseable: Boolean
  mergeable_state: String!
  merged_by: nullable_simple_user
  comments: Int!
  review_comments: Int!

  """Indicates whether maintainers can modify the pull request."""
  maintainer_can_modify: Boolean!
  commits: Int!
  additions: Int!
  deletions: Int!
  changed_files: Int!
}

"""State of this Pull Request. Either `open` or `closed`."""
enum query_pulls_get_state {
  open
  closed
}

type query_pulls_get_labels_items {
  id: BigInt!
  node_id: String!
  url: String!
  name: String!
  description: String
  color: String!
  default: Boolean!
}

type query_pulls_get_head {
  label: String!
  ref: String!
  repo: repository!
  sha: String!
  user: simple_user!
}

type query_pulls_get_base {
  label: String!
  ref: String!
  repo: repository!
  sha: String!
  user: simple_user!
}

type query_pulls_get__links {
  comments: link!
  commits: link!
  statuses: link!
  html: link!
  issue: link!
  review_comments: link!
  review_comment: link!
  self: link!
}

"""The direction to sort results. Ignored without `sort` parameter."""
enum queryInput_pulls_list_review_comments_direction {
  asc
  desc
}

"""Pull Request Review Request"""
type Pull_Request_Review_Request {
  users: [simple_user]!
  teams: [team]!
}

"""Pull Request Reviews are reviews on pull requests."""
type Pull_Request_Review {
  """Unique identifier of the review"""
  id: BigInt!
  node_id: String!
  user: nullable_simple_user

  """The text of the review."""
  body: String!
  state: String!
  html_url: URL!
  pull_request_url: URL!
  _links: query_pulls_list_reviews_items__links!
  submitted_at: DateTime

  """
  A commit SHA for the review. If the commit object was garbage collected or forcibly deleted, then it no longer exists in Git and this value will be `null`.
  """
  commit_id: String
  body_html: String
  body_text: String
  author_association: author_association!
}

type query_pulls_list_reviews_items__links {
  html: query_pulls_list_reviews_items__links_html!
  pull_request: query_pulls_list_reviews_items__links_pull_request!
}

type query_pulls_list_reviews_items__links_html {
  href: String!
}

type query_pulls_list_reviews_items__links_pull_request {
  href: String!
}

"""Legacy Review Comment"""
type Legacy_Review_Comment {
  url: URL!
  pull_request_review_id: BigInt
  id: BigInt!
  node_id: String!
  diff_hunk: String!
  path: String!
  position: Int
  original_position: Int!
  commit_id: String!
  original_commit_id: String!
  in_reply_to_id: Int
  user: nullable_simple_user
  body: String!
  created_at: DateTime!
  updated_at: DateTime!
  html_url: URL!
  pull_request_url: URL!
  author_association: author_association!
  _links: query_pulls_list_comments_for_review_items__links!
  body_text: String
  body_html: String
  reactions: Reaction_Rollup
  side: query_pulls_list_comments_for_review_items_side
  start_side: query_pulls_list_comments_for_review_items_start_side

  """
  The line of the blob to which the comment applies. The last line of the range for a multi-line comment
  """
  line: Int

  """
  The original line of the blob to which the comment applies. The last line of the range for a multi-line comment
  """
  original_line: Int

  """The first line of the range for a multi-line comment."""
  start_line: Int

  """The original first line of the range for a multi-line comment."""
  original_start_line: Int
  subject_type: query_pulls_list_comments_for_review_items_subject_type
}

type query_pulls_list_comments_for_review_items__links {
  self: link!
  html: link!
  pull_request: link!
}

"""The side of the first line of the range for a multi-line comment."""
enum query_pulls_list_comments_for_review_items_side {
  LEFT
  RIGHT
}

"""The side of the first line of the range for a multi-line comment."""
enum query_pulls_list_comments_for_review_items_start_side {
  LEFT
  RIGHT
}

"""
The level at which the comment is targeted, can be a diff line or a file.
"""
enum query_pulls_list_comments_for_review_items_subject_type {
  line
  file
}

"""A release."""
type release {
  url: URL!
  html_url: URL!
  assets_url: URL!
  upload_url: String!
  tarball_url: URL
  zipball_url: URL
  id: Int!
  node_id: String!

  """The name of the tag."""
  tag_name: String!

  """
  Specifies the commitish value that determines where the Git tag is created from.
  """
  target_commitish: String!
  name: String
  body: String

  """
  true to create a draft (unpublished) release, false to create a published one.
  """
  draft: Boolean!

  """Whether to identify the release as a prerelease or a full release."""
  prerelease: Boolean!

  """Whether or not the release is immutable."""
  immutable: Boolean
  created_at: DateTime!
  published_at: DateTime
  updated_at: DateTime
  author: simple_user!
  assets: [Release_Asset]!
  body_html: String
  body_text: String
  mentions_count: Int

  """The URL of the release discussion."""
  discussion_url: URL
  reactions: Reaction_Rollup
}

"""
Returns a single [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions). Omit this parameter to list all reactions to a release.
"""
enum queryInput_reactions_list_for_release_content {
  _0PLUS_1
  laugh
  heart
  hooray
  rocket
  eyes
}

"""A repository rule with ruleset details."""
union repository_rule_detailed = query_repos_get_branch_rules_items_oneOf_0 | query_repos_get_branch_rules_items_oneOf_1 | query_repos_get_branch_rules_items_oneOf_2 | query_repos_get_branch_rules_items_oneOf_3 | query_repos_get_branch_rules_items_oneOf_4 | query_repos_get_branch_rules_items_oneOf_5 | query_repos_get_branch_rules_items_oneOf_6 | query_repos_get_branch_rules_items_oneOf_7 | query_repos_get_branch_rules_items_oneOf_8 | query_repos_get_branch_rules_items_oneOf_9 | query_repos_get_branch_rules_items_oneOf_10 | query_repos_get_branch_rules_items_oneOf_11 | query_repos_get_branch_rules_items_oneOf_12 | query_repos_get_branch_rules_items_oneOf_13 | query_repos_get_branch_rules_items_oneOf_14 | query_repos_get_branch_rules_items_oneOf_15 | query_repos_get_branch_rules_items_oneOf_16 | query_repos_get_branch_rules_items_oneOf_17 | query_repos_get_branch_rules_items_oneOf_18 | query_repos_get_branch_rules_items_oneOf_19 | query_repos_get_branch_rules_items_oneOf_20 | query_repos_get_branch_rules_items_oneOf_21

type query_repos_get_branch_rules_items_oneOf_0 {
  type: creation_const!
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

"""The type of source for the ruleset that includes this rule."""
enum query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type {
  Repository
  Organization
}

type query_repos_get_branch_rules_items_oneOf_1 {
  type: update_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_1_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_2 {
  type: deletion_const!
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_3 {
  type: required_linear_history_const!
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_4 {
  type: merge_queue_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_5 {
  type: required_deployments_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_5_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_6 {
  type: required_signatures_const!
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_7 {
  type: pull_request_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_8 {
  type: required_status_checks_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_8_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_9 {
  type: non_fast_forward_const!
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_10 {
  type: commit_message_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_11 {
  type: commit_author_email_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_12 {
  type: committer_email_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_13 {
  type: branch_name_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_14 {
  type: tag_name_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_15 {
  type: file_path_restriction_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_15_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_16 {
  type: max_file_path_length_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_16_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_17 {
  type: file_extension_restriction_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_17_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_18 {
  type: max_file_size_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_18_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_19 {
  type: workflows_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_19_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_20 {
  type: code_scanning_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type query_repos_get_branch_rules_items_oneOf_21 {
  type: copilot_code_review_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_21_parameters
  ruleset_source_type: query_repos_get_branch_rules_items_oneOf_0_allOf_1_ruleset_source_type

  """The name of the source of the ruleset that includes this rule."""
  ruleset_source: String

  """The ID of the ruleset that includes this rule."""
  ruleset_id: Int
}

type secret_scanning_alert {
  """The security alert number."""
  number: Int!

  """
  The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  created_at: DateTime

  """
  The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  updated_at: DateTime

  """The REST API URL of the alert resource."""
  url: URL

  """The GitHub URL of the alert resource."""
  html_url: URL

  """The REST API URL of the code locations for this alert."""
  locations_url: URL
  state: secret_scanning_alert_state
  resolution: secret_scanning_alert_resolution

  """
  The time that the alert was resolved in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  resolved_at: DateTime
  resolved_by: nullable_simple_user

  """An optional comment to resolve an alert."""
  resolution_comment: String

  """The type of secret that secret scanning detected."""
  secret_type: String

  """
  User-friendly name for the detected secret, matching the `secret_type`.
  For a list of built-in patterns, see "[Supported secret scanning patterns](https://docs.github.com/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)."
  """
  secret_type_display_name: String

  """The secret that was detected."""
  secret: String

  """Whether push protection was bypassed for the detected secret."""
  push_protection_bypassed: Boolean
  push_protection_bypassed_by: nullable_simple_user

  """
  The time that push protection was bypassed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  push_protection_bypassed_at: DateTime
  push_protection_bypass_request_reviewer: nullable_simple_user

  """An optional comment when reviewing a push protection bypass."""
  push_protection_bypass_request_reviewer_comment: String

  """An optional comment when requesting a push protection bypass."""
  push_protection_bypass_request_comment: String

  """The URL to a push protection bypass request."""
  push_protection_bypass_request_html_url: URL
  validity: query_secret_scanning_list_alerts_for_repo_items_validity

  """Whether the detected secret was publicly leaked."""
  publicly_leaked: Boolean

  """
  Whether the detected secret was found in multiple repositories under the same organization or enterprise.
  """
  multi_repo: Boolean

  """A boolean value representing whether or not alert is base64 encoded"""
  is_base64_encoded: Boolean
  first_location_detected: nullable_secret_scanning_first_detected_location

  """
  A boolean value representing whether or not the token in the alert was detected in more than one location.
  """
  has_more_locations: Boolean
  assigned_to: nullable_simple_user
}

"""The token status as of the latest validity check."""
enum query_secret_scanning_list_alerts_for_repo_items_validity {
  active
  inactive
  unknown
}

type secret_scanning_location {
  type: query_secret_scanning_list_locations_for_alert_items_type
  details: query_secret_scanning_list_locations_for_alert_items_details
}

"""
The location type. Because secrets may be found in different types of resources (ie. code, comments, issues, pull requests, discussions), this field identifies the type of resource where the secret was found.
"""
enum query_secret_scanning_list_locations_for_alert_items_type {
  commit
  wiki_commit
  issue_title
  issue_body
  issue_comment
  discussion_title
  discussion_body
  discussion_comment
  pull_request_title
  pull_request_body
  pull_request_comment
  pull_request_review
  pull_request_review_comment
}

union query_secret_scanning_list_locations_for_alert_items_details = secret_scanning_location_commit | secret_scanning_location_wiki_commit | secret_scanning_location_issue_title | secret_scanning_location_issue_body | secret_scanning_location_issue_comment | secret_scanning_location_discussion_title | secret_scanning_location_discussion_body | secret_scanning_location_discussion_comment | secret_scanning_location_pull_request_title | secret_scanning_location_pull_request_body | secret_scanning_location_pull_request_comment | secret_scanning_location_pull_request_review | secret_scanning_location_pull_request_review_comment

type secret_scanning_scan_history {
  incremental_scans: [secret_scanning_scan]
  pattern_update_scans: [secret_scanning_scan]
  backfill_scans: [secret_scanning_scan]
  custom_pattern_backfill_scans: [query_secret_scanning_get_scan_history_custom_pattern_backfill_scans_items]
}

"""
Information on a single scan performed by secret scanning on the repository
"""
type secret_scanning_scan {
  """The type of scan"""
  type: String

  """
  The state of the scan. Either "completed", "running", or "pending"
  """
  status: String

  """The time that the scan was completed. Empty if the scan is running"""
  completed_at: DateTime

  """The time that the scan was started. Empty if the scan is pending"""
  started_at: DateTime
}

type query_secret_scanning_get_scan_history_custom_pattern_backfill_scans_items {
  """The type of scan"""
  type: String

  """
  The state of the scan. Either "completed", "running", or "pending"
  """
  status: String

  """The time that the scan was completed. Empty if the scan is running"""
  completed_at: DateTime

  """The time that the scan was started. Empty if the scan is pending"""
  started_at: DateTime

  """Name of the custom pattern for custom pattern scans"""
  pattern_name: String

  """
  Level at which the custom pattern is defined, one of "repository", "organization", or "enterprise"
  """
  pattern_scope: String
}

"""The property to sort the results by."""
enum queryInput_security_advisories_list_repository_advisories_sort {
  created
  updated
  published
}

"""
Filter by state of the repository advisories. Only advisories of this state will be returned.
"""
enum queryInput_security_advisories_list_repository_advisories_state {
  triage
  draft
  published
  closed
}

type activity_list_stargazers_for_repo_200_response {
  name: String
  email: String
  login: String!
  id: BigInt!
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  starred_at: JSON
  user_view_type: String
  additionalProperties: JSON
  user: nullable_simple_user
}

union repos_get_code_frequency_stats_response = Int_container | JSON_container | Void_container

type Int_container {
  Int: Int
}

union repos_get_commit_activity_stats_response = Commit_Activity | JSON_container | Void_container

"""Commit Activity"""
type Commit_Activity {
  days: [Int]!
  total: Int!
  week: Int!
}

union repos_get_contributors_stats_response = Contributor_Activity | JSON_container | Void_container

"""Contributor Activity"""
type Contributor_Activity {
  author: nullable_simple_user
  total: Int!
  weeks: [query_repos_get_contributors_stats_oneOf_0_items_weeks_items]!
}

type query_repos_get_contributors_stats_oneOf_0_items_weeks_items {
  w: Int
  a: Int
  d: Int
  c: Int
}

type Participation_Stats {
  all: [Int]!
  owner: [Int]!
}

union repos_get_punch_card_stats_response = Int_container | Void_container

"""Repository invitations let you manage who you collaborate with."""
type repository_subscription {
  """Determines if notifications should be received from this repository."""
  subscribed: Boolean!

  """
  Determines if all notifications should be blocked from this repository.
  """
  ignored: Boolean!
  reason: String
  created_at: DateTime!
  url: URL!
  repository_url: URL!
}

"""Tag"""
type Tag {
  name: String!
  commit: query_repos_list_tags_items_commit!
  zipball_url: URL!
  tarball_url: URL!
  node_id: String!
}

type query_repos_list_tags_items_commit {
  sha: String!
  url: URL!
}

"""A topic aggregates entities that are related to a subject."""
type Topic {
  names: [String]!
}

"""Clone Traffic"""
type Clone_Traffic {
  count: Int!
  uniques: Int!
  clones: [Traffic]!
}

type Traffic {
  timestamp: DateTime!
  uniques: Int!
  count: Int!
}

"""The time frame to display results for."""
enum per_parameter {
  day
  week
}

"""Content Traffic"""
type Content_Traffic {
  path: String!
  title: String!
  count: Int!
  uniques: Int!
}

"""Referrer Traffic"""
type Referrer_Traffic {
  referrer: String!
  count: Int!
  uniques: Int!
}

"""View Traffic"""
type View_Traffic {
  count: Int!
  uniques: Int!
  views: [Traffic]!
}

type search_code_200_response {
  total_count: Int!
  incomplete_results: Boolean!
  items: [Code_Search_Result_Item]!
}

"""Code Search Result Item"""
type Code_Search_Result_Item {
  name: String!
  path: String!
  sha: String!
  url: URL!
  git_url: URL!
  html_url: URL!
  repository: minimal_repository!
  score: Float!
  file_size: Int
  language: String
  last_modified_at: DateTime
  line_numbers: [String]
  text_matches: [query_search_code_items_items_text_matches_items]
}

type query_search_code_items_items_text_matches_items {
  object_url: String
  object_type: String
  property: String
  fragment: String
  matches: [query_search_code_items_items_text_matches_items_matches_items]
}

type query_search_code_items_items_text_matches_items_matches_items {
  text: String
  indices: [Int]
}

enum indexed_const {
  indexed
}

"""
**This field is closing down.** Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`.
"""
enum queryInput_search_code_order {
  desc
  asc
}

type search_commits_200_response {
  total_count: Int!
  incomplete_results: Boolean!
  items: [Commit_Search_Result_Item]!
}

"""Commit Search Result Item"""
type Commit_Search_Result_Item {
  url: URL!
  sha: String!
  html_url: URL!
  comments_url: URL!
  commit: query_search_commits_items_items_commit!
  author: nullable_simple_user
  committer: Git_User
  parents: [query_search_commits_items_items_parents_items]!
  repository: minimal_repository!
  score: Float!
  node_id: String!
  text_matches: [query_search_code_items_items_text_matches_items]
}

type query_search_commits_items_items_commit {
  author: query_search_commits_items_items_commit_author!
  committer: Git_User
  comment_count: Int!
  message: String!
  tree: query_search_commits_items_items_commit_tree!
  url: URL!
  verification: Verification
}

type query_search_commits_items_items_commit_author {
  name: String!
  email: String!
  date: DateTime!
}

type query_search_commits_items_items_commit_tree {
  sha: String!
  url: URL!
}

type query_search_commits_items_items_parents_items {
  url: String
  html_url: String
  sha: String
}

"""
Sorts the results of your query by `author-date` or `committer-date`. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)
"""
enum queryInput_search_commits_sort {
  author_date
  committer_date
}

"""
Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`.
"""
enum order_parameter {
  desc
  asc
}

type search_issues_and_pull_requests_200_response {
  total_count: Int!
  incomplete_results: Boolean!
  items: [Issue_Search_Result_Item]!
}

"""Issue Search Result Item"""
type Issue_Search_Result_Item {
  url: URL!
  repository_url: URL!
  labels_url: String!
  comments_url: URL!
  events_url: URL!
  html_url: URL!
  id: BigInt!
  node_id: String!
  number: Int!
  title: String!
  locked: Boolean!
  active_lock_reason: String
  assignees: [simple_user]
  user: nullable_simple_user
  labels: [query_search_issues_and_pull_requests_items_items_labels_items]!
  sub_issues_summary: Sub_issues_Summary
  issue_dependencies_summary: Issue_Dependencies_Summary
  issue_field_values: [Issue_Field_Value]
  state: String!
  state_reason: String
  assignee: nullable_simple_user
  milestone: nullable_milestone
  comments: Int!
  created_at: DateTime!
  updated_at: DateTime!
  closed_at: DateTime
  text_matches: [query_search_code_items_items_text_matches_items]
  pull_request: query_search_issues_and_pull_requests_items_items_pull_request
  body: String
  score: Float!
  author_association: author_association!
  draft: Boolean
  repository: repository
  body_html: String
  body_text: String
  timeline_url: URL
  type: Issue_Type
  performed_via_github_app: nullable_integration
  pinned_comment: nullable_issue_comment
  reactions: Reaction_Rollup
}

type query_search_issues_and_pull_requests_items_items_labels_items {
  id: BigInt
  node_id: String
  url: String
  name: String
  color: String
  default: Boolean
  description: String
}

type query_search_issues_and_pull_requests_items_items_pull_request {
  merged_at: DateTime
  diff_url: URL
  html_url: URL
  patch_url: URL
  url: URL
}

"""
Sorts the results of your query by the number of `comments`, `reactions`, `reactions-+1`, `reactions--1`, `reactions-smile`, `reactions-thinking_face`, `reactions-heart`, `reactions-tada`, or `interactions`. You can also sort results by how recently the items were `created` or `updated`, Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)
"""
enum queryInput_search_issues_and_pull_requests_sort {
  comments
  reactions
  reactions__PLUS_1
  reactions__1
  reactions_smile
  reactions_thinking_face
  reactions_heart
  reactions_tada
  interactions
  created
  updated
}

type search_labels_200_response {
  total_count: Int!
  incomplete_results: Boolean!
  items: [Label_Search_Result_Item]!
}

"""Label Search Result Item"""
type Label_Search_Result_Item {
  id: Int!
  node_id: String!
  url: URL!
  name: String!
  color: String!
  default: Boolean!
  description: String
  score: Float!
  text_matches: [query_search_code_items_items_text_matches_items]
}

"""
Sorts the results of your query by when the label was `created` or `updated`. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)
"""
enum queryInput_search_labels_sort {
  created
  updated
}

type search_repos_200_response {
  total_count: Int!
  incomplete_results: Boolean!
  items: [Repo_Search_Result_Item]!
}

"""Repo Search Result Item"""
type Repo_Search_Result_Item {
  id: Int!
  node_id: String!
  name: String!
  full_name: String!
  owner: nullable_simple_user
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  created_at: DateTime!
  updated_at: DateTime!
  pushed_at: DateTime!
  homepage: URL
  size: Int!
  stargazers_count: Int!
  watchers_count: Int!
  language: String
  forks_count: Int!
  open_issues_count: Int!
  master_branch: String
  default_branch: String!
  score: Float!
  forks_url: URL!
  keys_url: String!
  collaborators_url: String!
  teams_url: URL!
  hooks_url: URL!
  issue_events_url: String!
  events_url: URL!
  assignees_url: String!
  branches_url: String!
  tags_url: URL!
  blobs_url: String!
  git_tags_url: String!
  git_refs_url: String!
  trees_url: String!
  statuses_url: String!
  languages_url: URL!
  stargazers_url: URL!
  contributors_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  commits_url: String!
  git_commits_url: String!
  comments_url: String!
  issue_comment_url: String!
  contents_url: String!
  compare_url: String!
  merges_url: URL!
  archive_url: String!
  downloads_url: URL!
  issues_url: String!
  pulls_url: String!
  milestones_url: String!
  notifications_url: String!
  labels_url: String!
  releases_url: String!
  deployments_url: URL!
  git_url: String!
  ssh_url: String!
  clone_url: String!
  svn_url: URL!
  forks: Int!
  open_issues: Int!
  watchers: Int!
  topics: [String]
  mirror_url: URL
  has_issues: Boolean!
  has_projects: Boolean!
  has_pages: Boolean!
  has_wiki: Boolean!
  has_downloads: Boolean!
  has_discussions: Boolean
  has_pull_requests: Boolean
  pull_request_creation_policy: query_search_repos_items_items_pull_request_creation_policy
  has_commit_comments: Boolean
  archived: Boolean!

  """Returns whether or not this repository disabled."""
  disabled: Boolean!

  """The repository visibility: public, private, or internal."""
  visibility: String
  license: nullable_license_simple
  permissions: query_search_repos_items_items_permissions
  text_matches: [query_search_code_items_items_text_matches_items]
  temp_clone_token: String
  allow_merge_commit: Boolean
  allow_squash_merge: Boolean
  allow_rebase_merge: Boolean
  allow_auto_merge: Boolean
  delete_branch_on_merge: Boolean
  allow_forking: Boolean
  is_template: Boolean
  web_commit_signoff_required: Boolean
}

"""
The policy controlling who can create pull requests: all or collaborators_only.
"""
enum query_search_repos_items_items_pull_request_creation_policy {
  all
  collaborators_only
}

type query_search_repos_items_items_permissions {
  admin: Boolean!
  maintain: Boolean
  push: Boolean!
  triage: Boolean
  pull: Boolean!
}

"""
Sorts the results of your query by number of `stars`, `forks`, or `help-wanted-issues` or how recently the items were `updated`. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)
"""
enum queryInput_search_repos_sort {
  stars
  forks
  help_wanted_issues
  updated
}

type search_topics_200_response {
  total_count: Int!
  incomplete_results: Boolean!
  items: [Topic_Search_Result_Item]!
}

"""Topic Search Result Item"""
type Topic_Search_Result_Item {
  name: String!
  display_name: String
  short_description: String
  description: String
  created_by: String
  released: String
  created_at: DateTime!
  updated_at: DateTime!
  featured: Boolean!
  curated: Boolean!
  score: Float!
  repository_count: Int
  logo_url: URL
  text_matches: [query_search_code_items_items_text_matches_items]
  related: [query_search_topics_items_items_related_items]
  aliases: [query_search_topics_items_items_aliases_items]
}

type query_search_topics_items_items_related_items {
  topic_relation: query_search_topics_items_items_related_items_topic_relation
}

type query_search_topics_items_items_related_items_topic_relation {
  id: Int
  name: String
  topic_id: Int
  relation_type: String
}

type query_search_topics_items_items_aliases_items {
  topic_relation: query_search_topics_items_items_aliases_items_topic_relation
}

type query_search_topics_items_items_aliases_items_topic_relation {
  id: Int
  name: String
  topic_id: Int
  relation_type: String
}

type search_users_200_response {
  total_count: Int!
  incomplete_results: Boolean!
  items: [User_Search_Result_Item]!
}

"""User Search Result Item"""
type User_Search_Result_Item {
  login: String!
  id: BigInt!
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  received_events_url: URL!
  type: String!
  score: Float!
  following_url: String!
  gists_url: String!
  starred_url: String!
  events_url: String!
  public_repos: Int
  public_gists: Int
  followers: Int
  following: Int
  created_at: DateTime
  updated_at: DateTime
  name: String
  bio: String
  email: EmailAddress
  location: String
  site_admin: Boolean!
  hireable: Boolean
  text_matches: [query_search_code_items_items_text_matches_items]
  blog: String
  company: String
  suspended_at: DateTime
  user_view_type: String
}

"""
Sorts the results of your query by number of `followers` or `repositories`, or when the person `joined` GitHub. Default: [best match](https://docs.github.com/rest/search/search#ranking-search-results)
"""
enum queryInput_search_users_sort {
  followers
  repositories
  joined
}

"""Filters members returned by their role in the team."""
enum queryInput_teams_list_members_legacy_role {
  member
  maintainer
  all
}

union teams_check_permissions_for_repo_legacy_response = Team_Repository | Void_container

union users_get_authenticated_200_response = Private_User | Public_User

"""Private User"""
type Private_User {
  login: String!
  id: BigInt!
  user_view_type: String
  node_id: String!
  avatar_url: URL!
  gravatar_id: String
  url: URL!
  html_url: URL!
  followers_url: URL!
  following_url: URL!
  gists_url: URL!
  starred_url: URL!
  subscriptions_url: URL!
  organizations_url: URL!
  repos_url: URL!
  events_url: URL!
  received_events_url: URL!
  type: String!
  site_admin: Boolean!
  name: String
  company: String
  blog: URL
  location: String
  email: EmailAddress
  notification_email: EmailAddress
  hireable: Boolean
  bio: String
  twitter_username: String
  public_repos: Int!
  public_gists: Int!
  followers: Int!
  following: Int!
  created_at: DateTime!
  updated_at: DateTime!
  private_gists: Int!
  total_private_repos: Int!
  owned_private_repos: Int!
  disk_usage: Int!
  collaborators: Int!
  two_factor_authentication: Boolean!
  plan: query_users_get_authenticated_oneOf_0_plan
  business_plus: Boolean
  ldap_dn: String
}

type query_users_get_authenticated_oneOf_0_plan {
  collaborators: Int!
  name: String!
  space: Int!
  private_repos: Int!
}

type codespaces_list_for_authenticated_user_200_response {
  total_count: Int!
  codespaces: [codespace]!
}

type codespaces_list_secrets_for_authenticated_user_200_response {
  total_count: Int!
  secrets: [codespaces_secret]!
}

"""Secrets for a GitHub Codespace."""
type codespaces_secret {
  """The name of the secret"""
  name: String!

  """
  The date and time at which the secret was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  created_at: DateTime!

  """
  The date and time at which the secret was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  updated_at: DateTime!
  visibility: query_codespaces_list_secrets_for_authenticated_user_secrets_items_visibility!

  """
  The API URL at which the list of repositories this secret is visible to can be retrieved
  """
  selected_repositories_url: URL!
}

"""
The type of repositories in the organization that the secret is visible to
"""
enum query_codespaces_list_secrets_for_authenticated_user_secrets_items_visibility {
  all
  private
  selected
}

"""The public key used for setting user Codespaces' Secrets."""
type CodespacesUserPublicKey {
  """The identifier for the key."""
  key_id: String!

  """The Base64 encoded public key."""
  key: String!
}

type codespaces_list_repositories_for_secret_for_authenticated_user_200_response {
  total_count: Int!
  repositories: [minimal_repository]!
}

"""
An export of a codespace. Also, latest export details for a codespace can be fetched with id = latest
"""
type Fetches_information_about_an_export_of_a_codespace_ {
  """State of the latest export"""
  state: String

  """Completion time of the last export operation"""
  completed_at: DateTime

  """Name of the exported branch"""
  branch: String

  """Git commit SHA of the exported branch"""
  sha: String

  """Id for the export details"""
  id: String

  """Url for fetching export details"""
  export_url: URL

  """Web url for the exported branch"""
  html_url: URL
}

type codespaces_codespace_machines_for_authenticated_user_200_response {
  total_count: Int!
  machines: [codespace_machine]!
}

"""Email"""
type Email {
  email: EmailAddress!
  primary: Boolean!
  verified: Boolean!
  visibility: String
}

"""A unique encryption key"""
type GPG_Key {
  id: BigInt!
  name: String
  primary_key_id: Int
  key_id: String!
  public_key: String!
  emails: [query_users_list_gpg_keys_for_authenticated_user_items_emails_items]!
  subkeys: [query_users_list_gpg_keys_for_authenticated_user_items_subkeys_items]!
  can_sign: Boolean!
  can_encrypt_comms: Boolean!
  can_encrypt_storage: Boolean!
  can_certify: Boolean!
  created_at: DateTime!
  expires_at: DateTime
  revoked: Boolean!
  raw_key: String
}

type query_users_list_gpg_keys_for_authenticated_user_items_emails_items {
  email: String
  verified: Boolean
}

type query_users_list_gpg_keys_for_authenticated_user_items_subkeys_items {
  id: BigInt
  primary_key_id: Int
  key_id: String
  public_key: String
  emails: [query_users_list_gpg_keys_for_authenticated_user_items_subkeys_items_emails_items]
  subkeys: [JSON]
  can_sign: Boolean
  can_encrypt_comms: Boolean
  can_encrypt_storage: Boolean
  can_certify: Boolean
  created_at: String
  expires_at: String
  raw_key: String
  revoked: Boolean
}

type query_users_list_gpg_keys_for_authenticated_user_items_subkeys_items_emails_items {
  email: String
  verified: Boolean
}

type apps_list_installations_for_authenticated_user_200_response {
  total_count: Int!
  installations: [Installation]!
}

type apps_list_installation_repos_for_authenticated_user_200_response {
  total_count: Int!
  repository_selection: String
  repositories: [repository]!
}

union interactions_get_restrictions_for_authenticated_user_response = interactions_get_restrictions_for_authenticated_user_200_response | Void_container

type interactions_get_restrictions_for_authenticated_user_200_response {
  limit: interaction_group!
  origin: String!
  expires_at: DateTime!
  JSON: JSON
}

"""
Indicates which sorts of issues to return. `assigned` means issues assigned to you. `created` means issues created by you. `mentioned` means issues mentioning you. `subscribed` means issues you're subscribed to updates for. `all` or `repos` means all issues you can see, regardless of participation or creation.
"""
enum queryInput_issues_list_for_authenticated_user_filter {
  assigned
  created
  mentioned
  subscribed
  repos
  all
}

"""Indicates the state of the issues to return."""
enum queryInput_issues_list_for_authenticated_user_state {
  open
  closed
  all
}

"""What to sort results by."""
enum queryInput_issues_list_for_authenticated_user_sort {
  created
  updated
  comments
}

"""Key"""
type Key {
  key: String!
  id: BigInt!
  url: String!
  title: String!
  created_at: DateTime!
  verified: Boolean!
  read_only: Boolean!
  last_used: DateTime
}

"""User Marketplace Purchase"""
type User_Marketplace_Purchase {
  billing_cycle: String!
  next_billing_date: DateTime
  unit_count: Int
  on_free_trial: Boolean!
  free_trial_ends_on: DateTime
  updated_at: DateTime
  account: Marketplace_Account!
  plan: Marketplace_Listing_Plan!
}

type Marketplace_Account {
  url: URL!
  id: Int!
  type: String!
  node_id: String
  login: String!
  email: EmailAddress
  organization_billing_email: EmailAddress
}

"""
Indicates the state of the memberships to return. If not specified, the API returns both active and pending memberships.
"""
enum queryInput_orgs_list_memberships_for_authenticated_user_state {
  active
  pending
}

"""
The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry.
"""
enum queryInput_packages_list_packages_for_authenticated_user_package_type {
  npm
  maven
  rubygems
  docker
  nuget
  container
}

"""The state of the package, either active or deleted."""
enum queryInput_packages_get_all_package_versions_for_package_owned_by_authenticated_user_state {
  active
  deleted
}

"""Limit results to repositories with the specified visibility."""
enum queryInput_repos_list_for_authenticated_user_visibility {
  all
  public
  private
}

"""
Limit results to repositories of the specified type. Will cause a `422` error if used in the same request as **visibility** or **affiliation**.
"""
enum queryInput_repos_list_for_authenticated_user_type {
  all
  owner
  public
  private
  member
}

"""The property to sort the results by."""
enum queryInput_repos_list_for_authenticated_user_sort {
  created
  updated
  pushed
  full_name
}

"""
The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`.
"""
enum queryInput_repos_list_for_authenticated_user_direction {
  asc
  desc
}

"""Social media account"""
type Social_account {
  provider: String!
  url: URL!
}

"""A public SSH key used to sign Git commits"""
type SSH_Signing_Key {
  key: String!
  id: Int!
  title: String!
  created_at: DateTime!
}

union activity_list_repos_starred_by_authenticated_user_200_response = repository | Starred_Repository

"""Starred Repository"""
type Starred_Repository {
  starred_at: DateTime!
  repo: repository!
}

"""
The property to sort the results by. `created` means when the repository was starred. `updated` means when the repository was last pushed to.
"""
enum sort_starred_parameter {
  created
  updated
}

union users_get_by_id_200_response = Private_User | Public_User

union users_get_by_username_200_response = Private_User | Public_User

union users_list_attestations_response = users_list_attestations_200_response | JSON_container | Void_container

type users_list_attestations_200_response {
  attestations: [query_users_list_attestations_oneOf_0_attestations_items]
}

type query_users_list_attestations_oneOf_0_attestations_items {
  bundle: query_users_list_attestations_oneOf_0_attestations_items_bundle
  repository_id: Int
  bundle_url: String
  initiator: String
}

"""
The attestation's Sigstore Bundle.
Refer to the [Sigstore Bundle Specification](https://github.com/sigstore/protobuf-specs/blob/main/protos/sigstore_bundle.proto) for more information.
"""
type query_users_list_attestations_oneOf_0_attestations_items_bundle {
  mediaType: String
  verificationMaterial: JSON
  dsseEnvelope: JSON
}

"""Hovercard"""
type Hovercard {
  contexts: [query_users_get_context_for_user_contexts_items]!
}

type query_users_get_context_for_user_contexts_items {
  message: String!
  octicon: String!
}

"""
Identifies which additional information you'd like to receive about the person's hovercard. Can be `organization`, `repository`, `issue`, `pull_request`. **Required** when using `subject_id`.
"""
enum queryInput_users_get_context_for_user_subject_type {
  organization
  repository
  issue
  pull_request
}

"""Key Simple"""
type Key_Simple {
  id: Int!
  key: String!
  created_at: DateTime
  last_used: DateTime
}

"""
The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry.
"""
enum queryInput_packages_list_packages_for_user_package_type {
  npm
  maven
  rubygems
  docker
  nuget
  container
}

"""Limit results to repositories of the specified type."""
enum queryInput_repos_list_for_user_type {
  all
  owner
  member
}

"""The property to sort the results by."""
enum queryInput_repos_list_for_user_sort {
  created
  updated
  pushed
  full_name
}

"""
The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`.
"""
enum queryInput_repos_list_for_user_direction {
  asc
  desc
}

"""Response when getting a billing premium request usage report"""
type billing_premium_request_usage_report_user {
  timePeriod: query_billing_get_github_billing_premium_request_usage_report_user_timePeriod!

  """The unique identifier of the user."""
  user: String!

  """The product for the usage report."""
  product: String

  """The model for the usage report."""
  model: String
  usageItems: [query_billing_get_github_billing_premium_request_usage_report_user_usageItems_items]!
}

type query_billing_get_github_billing_premium_request_usage_report_user_timePeriod {
  """The year for the usage report."""
  year: Int!

  """The month for the usage report."""
  month: Int

  """The day for the usage report."""
  day: Int
}

type query_billing_get_github_billing_premium_request_usage_report_user_usageItems_items {
  """Product name."""
  product: String!

  """SKU name."""
  sku: String!

  """Model name."""
  model: String!

  """Unit type of the usage line item."""
  unitType: String!

  """Price per unit of the usage line item."""
  pricePerUnit: Float!

  """Gross quantity of the usage line item."""
  grossQuantity: Float!

  """Gross amount of the usage line item."""
  grossAmount: Float!

  """Discount quantity of the usage line item."""
  discountQuantity: Float!

  """Discount amount of the usage line item."""
  discountAmount: Float!

  """Net quantity of the usage line item."""
  netQuantity: Float!

  """Net amount of the usage line item."""
  netAmount: Float!
}

"""Response when getting a billing usage report"""
type billing_usage_report_user {
  usageItems: [query_billing_get_github_billing_usage_report_user_usageItems_items]
}

type query_billing_get_github_billing_usage_report_user_usageItems_items {
  """Date of the usage line item."""
  date: String!

  """Product name."""
  product: String!

  """SKU name."""
  sku: String!

  """Quantity of the usage line item."""
  quantity: Int!

  """Unit type of the usage line item."""
  unitType: String!

  """Price per unit of the usage line item."""
  pricePerUnit: Float!

  """Gross amount of the usage line item."""
  grossAmount: Float!

  """Discount amount of the usage line item."""
  discountAmount: Float!

  """Net amount of the usage line item."""
  netAmount: Float!

  """Name of the repository."""
  repositoryName: String
}

"""Response when getting a billing usage summary"""
type billing_usage_summary_report_user {
  timePeriod: query_billing_get_github_billing_usage_summary_report_user_timePeriod!

  """The unique identifier of the user."""
  user: String!

  """The name of the repository for the usage report."""
  repository: String

  """The product for the usage report."""
  product: String

  """The SKU for the usage report."""
  sku: String
  usageItems: [query_billing_get_github_billing_usage_summary_report_user_usageItems_items]!
}

type query_billing_get_github_billing_usage_summary_report_user_timePeriod {
  """The year for the usage report."""
  year: Int!

  """The month for the usage report."""
  month: Int

  """The day for the usage report."""
  day: Int
}

type query_billing_get_github_billing_usage_summary_report_user_usageItems_items {
  """Product name."""
  product: String!

  """SKU name."""
  sku: String!

  """Unit type of the usage line item."""
  unitType: String!

  """Price per unit of the usage line item."""
  pricePerUnit: Float!

  """Gross quantity of the usage line item."""
  grossQuantity: Float!

  """Gross amount of the usage line item."""
  grossAmount: Float!

  """Discount quantity of the usage line item."""
  discountQuantity: Float!

  """Discount amount of the usage line item."""
  discountAmount: Float!

  """Net quantity of the usage line item."""
  netQuantity: Float!

  """Net amount of the usage line item."""
  netAmount: Float!
}

type activity_list_repos_starred_by_user_200_response {
  starred_at: JSON
  repo: repository!

  """Unique identifier of the repository"""
  id: BigInt!
  node_id: String!

  """The name of the repository."""
  name: String!
  full_name: String!
  license: nullable_license_simple
  forks: Int!
  permissions: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_permissions
  owner: simple_user!

  """Whether the repository is private or public."""
  private: Boolean!
  html_url: URL!
  description: String
  fork: Boolean!
  url: URL!
  archive_url: URL!
  assignees_url: URL!
  blobs_url: URL!
  branches_url: URL!
  collaborators_url: URL!
  comments_url: URL!
  commits_url: URL!
  compare_url: URL!
  contents_url: URL!
  contributors_url: URL!
  deployments_url: URL!
  downloads_url: URL!
  events_url: URL!
  forks_url: URL!
  git_commits_url: URL!
  git_refs_url: URL!
  git_tags_url: URL!
  git_url: URL!
  issue_comment_url: URL!
  issue_events_url: URL!
  issues_url: URL!
  keys_url: URL!
  labels_url: URL!
  languages_url: URL!
  merges_url: URL!
  milestones_url: URL!
  notifications_url: URL!
  pulls_url: URL!
  releases_url: URL!
  ssh_url: String!
  stargazers_url: URL!
  statuses_url: URL!
  subscribers_url: URL!
  subscription_url: URL!
  tags_url: URL!
  teams_url: URL!
  trees_url: URL!
  clone_url: URL!
  mirror_url: URL
  hooks_url: URL!
  svn_url: URL!
  homepage: URL
  language: String
  forks_count: Int!
  stargazers_count: Int!
  watchers_count: Int!

  """
  The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0.
  """
  size: Int!

  """The default branch of the repository."""
  default_branch: String!
  open_issues_count: Int!

  """
  Whether this repository acts as a template that can be used to generate new repositories.
  """
  is_template: Boolean
  topics: [String]

  """Whether issues are enabled."""
  has_issues: Boolean!

  """Whether projects are enabled."""
  has_projects: Boolean!

  """Whether the wiki is enabled."""
  has_wiki: Boolean!
  has_pages: Boolean!

  """Whether downloads are enabled."""
  has_downloads: Boolean! @deprecated(reason: "deprecated")

  """Whether discussions are enabled."""
  has_discussions: Boolean

  """Whether pull requests are enabled."""
  has_pull_requests: Boolean
  pull_request_creation_policy: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_pull_request_creation_policy

  """Whether commit comments are enabled."""
  has_commit_comments: Boolean

  """Whether the repository is archived."""
  archived: Boolean!

  """Returns whether or not this repository disabled."""
  disabled: Boolean!

  """The repository visibility: public, private, or internal."""
  visibility: String
  pushed_at: DateTime
  created_at: DateTime
  updated_at: DateTime

  """Whether to allow rebase merges for pull requests."""
  allow_rebase_merge: Boolean
  temp_clone_token: String

  """Whether to allow squash merges for pull requests."""
  allow_squash_merge: Boolean

  """Whether to allow Auto-merge to be used on pull requests."""
  allow_auto_merge: Boolean

  """Whether to delete head branches when pull requests are merged"""
  delete_branch_on_merge: Boolean

  """
  Whether or not a pull request head branch that is behind its base branch can always be updated even if it is not required to be up to date before merging.
  """
  allow_update_branch: Boolean

  """
  Whether a squash merge commit can use the pull request title as default. **This property is closing down. Please use `squash_merge_commit_title` instead.
  """
  use_squash_pr_title_as_default: Boolean @deprecated(reason: "deprecated")
  squash_merge_commit_title: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_squash_merge_commit_title
  squash_merge_commit_message: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_squash_merge_commit_message
  merge_commit_title: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_merge_commit_title
  merge_commit_message: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_merge_commit_message

  """Whether to allow merge commits for pull requests."""
  allow_merge_commit: Boolean

  """Whether to allow forking this repo"""
  allow_forking: Boolean

  """Whether to require contributors to sign off on web-based commits"""
  web_commit_signoff_required: Boolean
  open_issues: Int!
  watchers: Int!
  master_branch: String

  """Whether anonymous git access is enabled for this repository"""
  anonymous_access_enabled: Boolean
  code_search_index_status: query_activity_list_public_events_items_payload_oneOf_3_issue_repository_code_search_index_status
  additionalProperties: JSON
}

type Mutation {
  """
  Use this endpoint to complete the handshake necessary when implementing the [GitHub App Manifest flow](https://docs.github.com/apps/building-github-apps/creating-github-apps-from-a-manifest/). When you create a GitHub App with the manifest flow, you receive a temporary `code` used to retrieve the GitHub App's `id`, `pem` (private key), and `webhook_secret`.
  """
  apps_create_from_manifest(code: String!): apps_create_from_manifest_201_response

  """
  Updates the webhook configuration for a GitHub App. For more information about configuring a webhook for your app, see "[Creating a GitHub App](/developers/apps/creating-a-github-app)."
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_update_webhook_config_for_app(input: apps_update_webhook_config_for_app_request_Input): Webhook_Configuration

  """
  Redeliver a delivery for the webhook configured for a GitHub App.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_redeliver_webhook_delivery(delivery_id: Int!): JSON

  """
  Uninstalls a GitHub App on a user, organization, or enterprise account. If you prefer to temporarily suspend an app's access to your account's resources, then we recommend the "[Suspend an app installation](https://docs.github.com/rest/apps/apps#suspend-an-app-installation)" endpoint.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_delete_installation(
    """The unique identifier of the installation."""
    installation_id: Int!
  ): Void

  """
  Creates an installation access token that enables a GitHub App to make authenticated API requests for the app's installation on an organization or individual account. Installation tokens expire one hour from the time you create them. Using an expired token produces a status code of `401 - Unauthorized`, and requires creating a new installation token. By default the installation token has access to all repositories that the installation can access.
  
  Optionally, you can use the `repositories` or `repository_ids` body parameters to specify individual repositories that the installation access token can access. If you don't use `repositories` or `repository_ids` to grant access to specific repositories, the installation access token will have access to all repositories that the installation was granted access to. The installation access token cannot be granted access to repositories that the installation was not granted access to. Up to 500 repositories can be listed in this manner.
  
  Optionally, use the `permissions` body parameter to specify the permissions that the installation access token should have. If `permissions` is not specified, the installation access token will have all of the permissions that were granted to the app. The installation access token cannot be granted permissions that the app was not granted.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_create_installation_access_token(
    """The unique identifier of the installation."""
    installation_id: Int!
    input: apps_create_installation_access_token_request_Input
  ): Installation_Token

  """
  Suspends a GitHub App on a user, organization, or enterprise account, which blocks the app from accessing the account's resources. When a GitHub App is suspended, the app's access to the GitHub API or webhook events is blocked for that account.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_suspend_installation(
    """The unique identifier of the installation."""
    installation_id: Int!
  ): Void

  """
  Removes a GitHub App installation suspension.
  
  You must use a [JWT](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/#authenticating-as-a-github-app) to access this endpoint.
  """
  apps_unsuspend_installation(
    """The unique identifier of the installation."""
    installation_id: Int!
  ): Void

  """
  OAuth and GitHub application owners can revoke a grant for their application and a specific user. You must provide a valid OAuth `access_token` as an input parameter and the grant for the token's owner will be deleted.
  Deleting an application's grant will also delete all OAuth tokens associated with the application for the user. Once deleted, the application will have no access to the user's account and will no longer be listed on [the application authorizations settings screen within GitHub](https://github.com/settings/applications#authorized).
  """
  apps_delete_authorization(
    """The client ID of the GitHub app."""
    client_id: String!
    input: apps_delete_authorization_request_Input
  ): Void

  """
  OAuth applications and GitHub applications with OAuth authorizations can use this API method for checking OAuth token validity without exceeding the normal rate limits for failed login attempts. Authentication works differently with this particular endpoint. Invalid tokens will return `404 NOT FOUND`.
  """
  apps_check_token(
    """The client ID of the GitHub app."""
    client_id: String!
    input: apps_check_token_request_Input
  ): Authorization

  """
  OAuth applications and GitHub applications with OAuth authorizations can use this API method to reset a valid OAuth token without end-user involvement. Applications must save the "token" property in the response because changes take effect immediately. Invalid tokens will return `404 NOT FOUND`.
  """
  apps_reset_token(
    """The client ID of the GitHub app."""
    client_id: String!
    input: apps_reset_token_request_Input
  ): Authorization

  """
  OAuth  or GitHub application owners can revoke a single token for an OAuth application or a GitHub application with an OAuth authorization.
  """
  apps_delete_token(
    """The client ID of the GitHub app."""
    client_id: String!
    input: apps_delete_token_request_Input
  ): Void

  """
  Use a non-scoped user access token to create a repository-scoped and/or permission-scoped user access token. You can specify
  which repositories the token can access and which permissions are granted to the
  token.
  
  Invalid tokens will return `404 NOT FOUND`.
  """
  apps_scope_token(
    """The client ID of the GitHub app."""
    client_id: String!
    input: apps_scope_token_request_Input
  ): Authorization

  """
  Submit a list of credentials to be revoked. This endpoint is intended to revoke credentials the caller does not own and may have found exposed on GitHub.com or elsewhere. It can also be used for credentials associated with an old user account that you no longer have access to. Credential owners will be notified of the revocation.
  
  This endpoint currently accepts the following credential types:
  - Personal access tokens (classic)
  - Fine-grained personal access tokens
  
  Revoked credentials may impact users on GitHub Free, Pro, & Team and GitHub Enterprise Cloud, and GitHub Enterprise Cloud with Enterprise Managed Users.
  GitHub cannot reactivate any credentials that have been revoked; new credentials will need to be generated.
  
  To prevent abuse, this API is limited to only 60 unauthenticated requests per hour and a max of 1000 tokens per API request.
  
  > [!NOTE]
  > Any authenticated requests will return a 403.
  """
  credentials_revoke(input: credentials_revoke_request_Input): JSON

  """
  Sets GitHub Actions cache retention limit for an enterprise. All organizations and repositories under this
  enterprise may not set a higher cache retention limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  actions_set_actions_cache_retention_limit_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
    input: Actions_cache_retention_limit_for_an_enterprise_Input
  ): Void

  """
  Sets GitHub Actions cache storage limit for an enterprise. All organizations and repositories under this
  enterprise may not set a higher cache storage limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  actions_set_actions_cache_storage_limit_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
    input: Actions_cache_storage_limit_for_an_enterprise_Input
  ): Void

  """
  Adds a repository custom property to be included in the OIDC token for repository actions in an enterprise.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  oidc_create_oidc_custom_property_inclusion_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
    input: Actions_OIDC_Custom_Property_Inclusion_Input_Input
  ): Actions_OIDC_Custom_Property_Inclusion

  """
  Removes a repository custom property from being included in the OIDC token for repository actions in an enterprise.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  oidc_delete_oidc_custom_property_inclusion_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The name of the custom property to remove from OIDC token inclusion"""
    custom_property_name: String!
  ): Void

  """
  Creates a code security configuration in an enterprise.
  
  The authenticated user must be an administrator of the enterprise in order to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  code_security_create_configuration_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!
    input: code_security_create_configuration_for_enterprise_request_Input
  ): code_security_configuration

  """
  Updates a code security configuration in an enterprise.
  
  The authenticated user must be an administrator of the enterprise in order to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  code_security_update_enterprise_configuration(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
    input: code_security_update_enterprise_configuration_request_Input
  ): code_security_configuration

  """
  Deletes a code security configuration from an enterprise.
  Repositories attached to the configuration will retain their settings but will no longer be associated with
  the configuration.
  
  The authenticated user must be an administrator for the enterprise to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  code_security_delete_configuration_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
  ): Void

  """
  Attaches an enterprise code security configuration to repositories. If the repositories specified are already attached to a configuration, they will be re-attached to the provided configuration.
  
  If insufficient GHAS licenses are available to attach the configuration to a repository, only free features will be enabled.
  
  The authenticated user must be an administrator for the enterprise to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  code_security_attach_enterprise_configuration(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
    input: code_security_attach_enterprise_configuration_request_Input
  ): JSON

  """
  Sets a code security configuration as a default to be applied to new repositories in your enterprise.
  
  This configuration will be applied by default to the matching repository type when created, but only for organizations within the enterprise that do not already have a default code security configuration set.
  
  The authenticated user must be an administrator for the enterprise to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  """
  code_security_set_configuration_as_default_for_enterprise(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
    input: code_security_set_configuration_as_default_for_enterprise_request_Input
  ): code_security_set_configuration_as_default_for_enterprise_200_response

  """
  To create an enterprise team, the authenticated user must be an owner of the enterprise.
  """
  enterprise_teams_create(
    """The slug version of the enterprise name."""
    enterprise: String!
    input: enterprise_teams_create_request_Input
  ): Enterprise_Team

  """Add multiple team members to an enterprise team."""
  enterprise_team_memberships_bulk_add(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!
    input: enterprise_team_memberships_bulk_add_request_Input
  ): [simple_user]

  """Remove multiple team members from an enterprise team."""
  enterprise_team_memberships_bulk_remove(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!
    input: enterprise_team_memberships_bulk_remove_request_Input
  ): [simple_user]

  """Add a team member to an enterprise team."""
  enterprise_team_memberships_add(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """The handle for the GitHub user account."""
    username: String!
  ): simple_user

  """
  Remove membership of a specific user from a particular team in an enterprise.
  """
  enterprise_team_memberships_remove(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """Assign an enterprise team to multiple organizations."""
  enterprise_team_organizations_bulk_add(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!
    input: enterprise_team_organizations_bulk_add_request_Input
  ): [organization_simple]

  """Unassign an enterprise team from multiple organizations."""
  enterprise_team_organizations_bulk_remove(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!
    input: enterprise_team_organizations_bulk_remove_request_Input
  ): Void

  """Assign an enterprise team to an organization."""
  enterprise_team_organizations_add(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """The organization name. The name is not case sensitive."""
    org: String!
  ): organization_simple

  """Unassign an enterprise team from an organization."""
  enterprise_team_organizations_delete(
    """The slug version of the enterprise name."""
    enterprise: String!

    """
    The slug version of the enterprise team name. You can also substitute this value with the enterprise team id.
    """
    enterprise_team: String!

    """The organization name. The name is not case sensitive."""
    org: String!
  ): Void

  """To edit a team, the authenticated user must be an enterprise owner."""
  enterprise_teams_update(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The slug of the team name."""
    team_slug: String!
    input: enterprise_teams_update_request_Input
  ): Enterprise_Team

  """
  To delete an enterprise team, the authenticated user must be an enterprise owner.
  
  If you are an enterprise owner, deleting an enterprise team will delete all of its IdP mappings as well.
  """
  enterprise_teams_delete(
    """The slug version of the enterprise name."""
    enterprise: String!

    """The slug of the team name."""
    team_slug: String!
  ): Void

  """
  Allows you to add a new gist with one or more files.
  
  > [!NOTE]
  > Don't name your files "gistfile" with a numerical suffix. This is the format of the automatic naming scheme that Gist uses internally.
  """
  gists_create(input: gists_create_request_Input): Gist_Simple

  """
  Allows you to update a gist's description and to update, delete, or rename gist files. Files
  from the previous version of the gist that aren't explicitly changed during an edit
  are unchanged.
  
  At least one of `description` or `files` is required.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  """
  gists_update(
    """The unique identifier of the gist."""
    gist_id: String!
    input: gists_update_request_Input
  ): Gist_Simple

  """Delete a gist"""
  gists_delete(
    """The unique identifier of the gist."""
    gist_id: String!
  ): Void

  """
  Creates a comment on a gist.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  """
  gists_create_comment(
    """The unique identifier of the gist."""
    gist_id: String!
    input: gists_create_comment_request_Input
  ): Gist_Comment

  """
  Updates a comment on a gist.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  """
  gists_update_comment(
    """The unique identifier of the gist."""
    gist_id: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: gists_update_comment_request_Input
  ): Gist_Comment

  """Delete a gist comment"""
  gists_delete_comment(
    """The unique identifier of the gist."""
    gist_id: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): Void

  """Fork a gist"""
  gists_fork(
    """The unique identifier of the gist."""
    gist_id: String!
  ): Base_Gist

  """
  Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  """
  gists_star(
    """The unique identifier of the gist."""
    gist_id: String!
  ): Void

  """Unstar a gist"""
  gists_unstar(
    """The unique identifier of the gist."""
    gist_id: String!
  ): Void

  """
  Revokes the installation token you're using to authenticate as an installation and access this endpoint.
  
  Once an installation token is revoked, the token is invalidated and cannot be used. Other endpoints that require the revoked installation token must have a new installation token to work. You can create a new token using the "[Create an installation access token for an app](https://docs.github.com/rest/apps/apps#create-an-installation-access-token-for-an-app)" endpoint.
  """
  apps_revoke_installation_access_token: Void

  """
  Depending on what is rendered in the Markdown, you may need to provide additional token scopes for labels, such as `issues:read` or `pull_requests:read`.
  """
  markdown_render(input: markdown_render_request_Input): String

  """
  You must send Markdown as plain text (using a `Content-Type` header of `text/plain` or `text/x-markdown`) to this endpoint, rather than using JSON format. In raw mode, [GitHub Flavored Markdown](https://github.github.com/gfm/) is not supported and Markdown will be rendered in plain format like a README.md file. Markdown content must be 400 KB or less.
  """
  markdown_render_raw(input: String): String

  """
  Marks all notifications as "read" for the current user. If the number of notifications is too large to complete in one request, you will receive a `202 Accepted` status and GitHub will run an asynchronous process to mark notifications as "read." To check whether any "unread" notifications remain, you can use the [List notifications for the authenticated user](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user) endpoint and pass the query parameter `all=false`.
  """
  activity_mark_notifications_as_read(input: activity_mark_notifications_as_read_request_Input): activity_mark_notifications_as_read_202_response

  """
  Marks a thread as "read." Marking a thread as "read" is equivalent to clicking a notification in your notification inbox on GitHub: https://github.com/notifications.
  """
  activity_mark_thread_as_read(
    """
    The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)).
    """
    thread_id: Int!
  ): JSON

  """
  Marks a thread as "done." Marking a thread as "done" is equivalent to marking a notification in your notification inbox on GitHub as done: https://github.com/notifications.
  """
  activity_mark_thread_as_done(
    """
    The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)).
    """
    thread_id: Int!
  ): Void

  """
  If you are watching a repository, you receive notifications for all threads by default. Use this endpoint to ignore future notifications for threads until you comment on the thread or get an **@mention**.
  
  You can also use this endpoint to subscribe to threads that you are currently not receiving notifications for or to subscribed to threads that you have previously ignored.
  
  Unsubscribing from a conversation in a repository that you are not watching is functionally equivalent to the [Delete a thread subscription](https://docs.github.com/rest/activity/notifications#delete-a-thread-subscription) endpoint.
  """
  activity_set_thread_subscription(
    """
    The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)).
    """
    thread_id: Int!
    input: activity_set_thread_subscription_request_Input
  ): Thread_Subscription

  """
  Mutes all future notifications for a conversation until you comment on the thread or get an **@mention**. If you are watching the repository of the thread, you will still receive notifications. To ignore future notifications for a repository you are watching, use the [Set a thread subscription](https://docs.github.com/rest/activity/notifications#set-a-thread-subscription) endpoint and set `ignore` to `true`.
  """
  activity_delete_thread_subscription(
    """
    The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)).
    """
    thread_id: Int!
  ): Void

  """
  Sets GitHub Actions cache retention limit for an organization. All repositories under this
  organization may not set a higher cache retention limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:organization` scope to use this endpoint.
  """
  actions_set_actions_cache_retention_limit_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: Actions_cache_retention_limit_for_an_organization_Input
  ): Void

  """
  Sets GitHub Actions cache storage limit for an organization. All organizations and repositories under this
  organization may not set a higher cache storage limit.
  
  OAuth tokens and personal access tokens (classic) need the `admin:organization` scope to use this endpoint.
  """
  actions_set_actions_cache_storage_limit_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: Actions_cache_storage_limit_for_an_organization_Input
  ): Void

  """
  Updates repositories according to the list of repositories that organization admins have given Dependabot access to when they've updated dependencies.
  
  > [!NOTE]
  >    This operation supports both server-to-server and user-to-server access.
  Unauthorized users will not see the existence of this endpoint.
  
  **Example request body:**
  ```json
  {
    "repository_ids_to_add": [123, 456],
    "repository_ids_to_remove": [789]
  }
  ```
  """
  dependabot_update_repository_access_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: dependabot_update_repository_access_for_org_request_Input
  ): Void

  """
  Sets the default level of repository access Dependabot will have while performing an update.  Available values are:
  - 'public' - Dependabot will only have access to public repositories, unless access is explicitly granted to non-public repositories.
  - 'internal' - Dependabot will only have access to public and internal repositories, unless access is explicitly granted to private repositories.
  
  Unauthorized users will not see the existence of this endpoint.
  
  This operation supports both server-to-server and user-to-server access.
  """
  dependabot_set_repository_access_default_level(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: dependabot_set_repository_access_default_level_request_Input
  ): Void

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Updates an existing budget for an organization. The authenticated user must be an organization admin or billing manager.
  """
  billing_update_budget_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID corresponding to the budget."""
    budget_id: String!
    input: billing_update_budget_org_request_Input
  ): billing_update_budget_org_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Deletes a budget by ID for an organization. The authenticated user must be an organization admin or billing manager.
  """
  billing_delete_budget_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID corresponding to the budget."""
    budget_id: String!
  ): delete_budget

  """
  > [!WARNING]
  > **Closing down notice:** GitHub will replace and discontinue `members_allowed_repository_creation_type` in favor of more granular permissions. The new input parameters are `members_can_create_public_repositories`, `members_can_create_private_repositories` for all organizations and `members_can_create_internal_repositories` for organizations associated with an enterprise account using GitHub Enterprise Cloud or GitHub Enterprise Server 2.20+. For more information, see the [blog post](https://developer.github.com/changes/2019-12-03-internal-visibility-changes).
  
  > [!WARNING]
  > **Closing down notice:** Code security product enablement for new repositories through the organization API is closing down. Please use [code security configurations](https://docs.github.com/rest/code-security/configurations#set-a-code-security-configuration-as-a-default-for-an-organization) to set defaults instead. For more information on setting a default security configuration, see the [changelog](https://github.blog/changelog/2024-07-09-sunsetting-security-settings-defaults-parameters-in-the-organizations-rest-api/).
  
  Updates the organization's profile and member privileges.
  
  The authenticated user must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` or `repo` scope to use this endpoint.
  """
  orgs_update(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_update_request_Input
  ): Organization_Full

  """
  Deletes an organization and all its repositories.
  
  The organization login will be unavailable for 90 days after deletion.
  
  Please review the Terms of Service regarding account deletion before using this endpoint:
  
  https://docs.github.com/site-policy/github-terms/github-terms-of-service
  """
  orgs_delete(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): JSON

  """
  Creates a GitHub-hosted runner for an organization.
  OAuth tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_create_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_create_hosted_runner_for_org_request_Input
  ): GitHub_hosted_hosted_runner

  """
  Delete a custom image from the organization.
  
  OAuth tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_delete_custom_image_from_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Image definition ID of custom image"""
    image_definition_id: Int!
  ): Void

  """
  Delete an image version of custom image from the organization.
  
  OAuth tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_delete_custom_image_version_from_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Image definition ID of custom image"""
    image_definition_id: Int!
    version: actions_custom_image_version_parameter!
  ): Void

  """
  Updates a GitHub-hosted runner for an organization.
  OAuth app tokens and personal access tokens (classic) need the `manage_runners:org` scope to use this endpoint.
  """
  actions_update_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the GitHub-hosted runner."""
    hosted_runner_id: Int!
    input: actions_update_hosted_runner_for_org_request_Input
  ): GitHub_hosted_hosted_runner

  """Deletes a GitHub-hosted runner for an organization."""
  actions_delete_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the GitHub-hosted runner."""
    hosted_runner_id: Int!
  ): GitHub_hosted_hosted_runner

  """
  Adds a repository custom property to be included in the OIDC token for repository actions in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  oidc_create_oidc_custom_property_inclusion_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: Actions_OIDC_Custom_Property_Inclusion_Input_Input
  ): Actions_OIDC_Custom_Property_Inclusion

  """
  Removes a repository custom property from being included in the OIDC token for repository actions in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  oidc_delete_oidc_custom_property_inclusion_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the custom property to remove from OIDC token inclusion"""
    custom_property_name: String!
  ): Void

  """
  Creates or updates the customization template for an OpenID Connect (OIDC) subject claim.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  oidc_update_oidc_custom_sub_template_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: Actions_OIDC_Subject_customization_Input
  ): JSON

  """
  Sets the GitHub Actions permissions policy for repositories and allowed actions and reusable workflows in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_set_github_actions_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_set_github_actions_permissions_organization_request_Input
  ): Void

  """
  Sets artifact and log retention settings for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_set_artifact_and_log_retention_settings_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_artifact_and_log_retention_Input
  ): Void

  """
  Sets the fork PR contributor approval policy for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_set_fork_pr_contributor_approval_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_fork_pr_contributor_approval_Input
  ): Void

  """
  Sets the settings for whether workflows from fork pull requests can run on private repositories in an organization.
  """
  actions_set_private_repo_fork_pr_workflows_settings_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_fork_pr_workflows_private_repos_request_Input
  ): Void

  """
  Replaces the list of selected repositories that are enabled for GitHub Actions in an organization. To use this endpoint, the organization permission policy for `enabled_repositories` must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for an organization](#set-github-actions-permissions-for-an-organization)."
  
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_set_selected_repositories_enabled_github_actions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_set_selected_repositories_enabled_github_actions_organization_request_Input
  ): Void

  """
  Adds a repository to the list of selected repositories that are enabled for GitHub Actions in an organization. To use this endpoint, the organization permission policy for `enabled_repositories` must be must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for an organization](#set-github-actions-permissions-for-an-organization)."
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_enable_selected_repository_github_actions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Removes a repository from the list of selected repositories that are enabled for GitHub Actions in an organization. To use this endpoint, the organization permission policy for `enabled_repositories` must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for an organization](#set-github-actions-permissions-for-an-organization)."
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_disable_selected_repository_github_actions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Sets the actions and reusable workflows that are allowed in an organization. To use this endpoint, the organization permission policy for `allowed_actions` must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for an organization](#set-github-actions-permissions-for-an-organization)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_set_allowed_actions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: selected_actions_Input
  ): Void

  """
  Sets the settings for self-hosted runners for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_set_self_hosted_runners_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_set_self_hosted_runners_permissions_organization_request_Input
  ): Void

  """
  Sets repositories that are allowed to use self-hosted runners in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_set_selected_repositories_self_hosted_runners_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_set_selected_repositories_self_hosted_runners_organization_request_Input
  ): Void

  """
  Adds a repository to the list of repositories that are allowed to use self-hosted runners in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_enable_selected_repository_self_hosted_runners_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Removes a repository from the list of repositories that are allowed to use self-hosted runners in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the "Actions policies" fine-grained permission to use this endpoint.
  """
  actions_disable_selected_repository_self_hosted_runners_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Sets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in an organization, and sets if GitHub Actions
  can submit approving pull request reviews. For more information, see
  "[Setting the permissions of the GITHUB_TOKEN for your organization](https://docs.github.com/organizations/managing-organization-settings/disabling-or-limiting-github-actions-for-your-organization#setting-the-permissions-of-the-github_token-for-your-organization)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_set_github_actions_default_workflow_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_set_default_workflow_permissions_Input
  ): Void

  """
  Creates a new self-hosted runner group for an organization.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_create_self_hosted_runner_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_create_self_hosted_runner_group_for_org_request_Input
  ): runner_groups_org

  """
  Updates the `name` and `visibility` of a self-hosted runner group in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_update_self_hosted_runner_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!
    input: actions_update_self_hosted_runner_group_for_org_request_Input
  ): runner_groups_org

  """
  Deletes a self-hosted runner group for an organization.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_delete_self_hosted_runner_group_from_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!
  ): Void

  """
  Replaces the list of repositories that have access to a self-hosted runner group configured in an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_set_repo_access_to_self_hosted_runner_group_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!
    input: actions_set_repo_access_to_self_hosted_runner_group_in_org_request_Input
  ): Void

  """
  Adds a repository to the list of repositories that can access a self-hosted runner group. The runner group must have `visibility` set to `selected`. For more information, see "[Create a self-hosted runner group for an organization](#create-a-self-hosted-runner-group-for-an-organization)."
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_add_repo_access_to_self_hosted_runner_group_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Removes a repository from the list of selected repositories that can access a self-hosted runner group. The runner group must have `visibility` set to `selected`. For more information, see "[Create a self-hosted runner group for an organization](#create-a-self-hosted-runner-group-for-an-organization)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_remove_repo_access_to_self_hosted_runner_group_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Replaces the list of self-hosted runners that are part of an organization runner group.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_set_self_hosted_runners_in_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!
    input: actions_set_self_hosted_runners_in_group_for_org_request_Input
  ): Void

  """
  Adds a self-hosted runner to a runner group configured in an organization.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_add_self_hosted_runner_to_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): Void

  """
  Removes a self-hosted runner from a group configured in an organization. The runner is then returned to the default group.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_remove_self_hosted_runner_from_group_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner group."""
    runner_group_id: Int!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): Void

  """
  Generates a configuration that can be passed to the runner application at startup.
  
  The authenticated user must have admin access to the organization.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_generate_runner_jitconfig_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_generate_runner_jitconfig_for_org_request_Input
  ): actions_runner_jitconfig_response

  """
  Returns a token that you can pass to the `config` script. The token expires after one hour.
  
  For example, you can replace `TOKEN` in the following example with the registration token provided by this endpoint to configure your self-hosted runner:
  
  ```
  ./config.sh --url https://github.com/octo-org --token TOKEN
  ```
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_registration_token_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Authentication_Token

  """
  Returns a token that you can pass to the `config` script to remove a self-hosted runner from an organization. The token expires after one hour.
  
  For example, you can replace `TOKEN` in the following example with the registration token provided by this endpoint to remove your self-hosted runner from an organization:
  
  ```
  ./config.sh remove --token TOKEN
  ```
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_remove_token_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Authentication_Token

  """
  Forces the removal of a self-hosted runner from an organization. You can use this endpoint to completely remove the runner when the machine you were using no longer exists.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_self_hosted_runner_from_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): Void

  """
  Adds custom labels to a self-hosted runner configured in an organization.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  actions_add_custom_labels_to_self_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
    input: actions_add_custom_labels_to_self_hosted_runner_for_org_request_Input
  ): actions_runner_labels_response

  """
  Remove all previous custom labels and set the new custom labels for a specific
  self-hosted runner configured in an organization.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_set_custom_labels_for_self_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
    input: actions_set_custom_labels_for_self_hosted_runner_for_org_request_Input
  ): actions_runner_labels_response

  """
  Remove all custom labels from a self-hosted runner configured in an
  organization. Returns the remaining read-only labels from the runner.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_remove_all_custom_labels_from_self_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): actions_runner_labels_readonly_response

  """
  Remove a custom label from a self-hosted runner configured
  in an organization. Returns the remaining labels from the runner.
  
  This endpoint returns a `404 Not Found` status if the custom label is not
  present on the runner.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_remove_custom_label_from_self_hosted_runner_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!

    """The name of a self-hosted runner's custom label."""
    name: String!
  ): actions_runner_labels_response

  """
  Creates or updates an organization secret with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_or_update_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    input: actions_create_or_update_org_secret_request_Input
  ): actions_create_or_update_org_secret_response

  """
  Deletes a secret in an organization using the secret name.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Replaces all repositories for an organization secret when the `visibility`
  for repository access is set to `selected`. The visibility is set when you [Create
  or update an organization secret](https://docs.github.com/rest/actions/secrets#create-or-update-an-organization-secret).
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_set_selected_repos_for_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    input: actions_set_selected_repos_for_org_secret_request_Input
  ): Void

  """
  Adds a repository to an organization secret when the `visibility` for
  repository access is set to `selected`. For more information about setting the visibility, see [Create or
  update an organization secret](https://docs.github.com/rest/actions/secrets#create-or-update-an-organization-secret).
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_add_selected_repo_to_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  Removes a repository from an organization secret when the `visibility`
  for repository access is set to `selected`. The visibility is set when you [Create
  or update an organization secret](https://docs.github.com/rest/actions/secrets#create-or-update-an-organization-secret).
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_remove_selected_repo_from_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  Creates an organization variable that you can reference in a GitHub Actions workflow.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: actions_create_org_variable_request_Input
  ): JSON

  """
  Updates an organization variable that you can reference in a GitHub Actions workflow.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_update_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the variable."""
    name: String!
    input: actions_update_org_variable_request_Input
  ): Void

  """
  Deletes an organization variable using the variable name.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the variable."""
    name: String!
  ): Void

  """
  Replaces all repositories for an organization variable that is available
  to selected repositories. Organization variables that are available to selected
  repositories have their `visibility` field set to `selected`.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_set_selected_repos_for_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the variable."""
    name: String!
    input: actions_set_selected_repos_for_org_variable_request_Input
  ): Void

  """
  Adds a repository to an organization variable that is available to selected repositories.
  Organization variables that are available to selected repositories have their `visibility` field set to `selected`.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_add_selected_repo_to_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the variable."""
    name: String!
    repository_id: Int!
  ): Void

  """
  Removes a repository from an organization variable that is
  available to selected repositories. Organization variables that are available to
  selected repositories have their `visibility` field set to `selected`.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.
  """
  actions_remove_selected_repo_from_org_variable(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the variable."""
    name: String!
    repository_id: Int!
  ): Void

  """
  Create or update deployment records for an artifact associated
  with an organization.
  This endpoint allows you to record information about a specific
  artifact, such as its name, digest, environments, cluster, and
  deployment.
  The deployment name has to be uniqe within a cluster (i.e a
  combination of logical, physical environment and cluster) as it
  identifies unique deployment.
  Multiple requests for the same combination of logical, physical
  environment, cluster and deployment name will only create one
  record, successive request will update the existing record.
  This allows for a stable tracking of a deployment where the actual
  deployed artifact can change over time.
  """
  orgs_create_artifact_deployment_record(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_create_artifact_deployment_record_request_Input
  ): orgs_create_artifact_deployment_record_200_response

  """
  Set deployment records for a given cluster.
  If proposed records in the 'deployments' field have identical 'cluster', 'logical_environment',
  'physical_environment', and 'deployment_name' values as existing records, the existing records will be updated.
  If no existing records match, new records will be created.
  """
  orgs_set_cluster_deployment_records(
    """The organization name. The name is not case sensitive."""
    org: String!
    cluster: mutationInput_orgs_set_cluster_deployment_records_cluster!
    input: orgs_set_cluster_deployment_records_request_Input
  ): orgs_set_cluster_deployment_records_200_response

  """
  Create metadata storage records for artifacts associated with an organization.
  This endpoint will create a new artifact storage record on behalf of any artifact matching the provided digest and
  associated with a repository owned by the organization.
  """
  orgs_create_artifact_storage_record(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_create_artifact_storage_record_request_Input
  ): orgs_create_artifact_storage_record_200_response

  """
  List a collection of artifact attestations associated with any entry in a list of subject digests owned by an organization.
  
  The collection of attestations returned by this endpoint is filtered according to the authenticated user's permissions; if the authenticated user cannot read a repository, the attestations associated with that repository will not be included in the response. In addition, when using a fine-grained access token the `attestations:read` permission is required.
  
  **Please note:** in order to offer meaningful security benefits, an attestation's signature and timestamps **must** be cryptographically verified, and the identity of the attestation signer **must** be validated. Attestations can be verified using the [GitHub CLI `attestation verify` command](https://cli.github.com/manual/gh_attestation_verify). For more information, see [our guide on how to use artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
  """
  orgs_list_attestations_bulk(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
    input: orgs_list_attestations_bulk_request_Input
  ): orgs_list_attestations_bulk_200_response

  """
  Delete artifact attestations in bulk by either subject digests or unique ID.
  """
  orgs_delete_attestations_bulk(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_delete_attestations_bulk_request_Input
  ): JSON

  """Delete an artifact attestation by subject digest."""
  orgs_delete_attestations_by_subject_digest(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Subject Digest"""
    subject_digest: String!
  ): Void

  """
  Delete an artifact attestation by unique ID that is associated with a repository owned by an org.
  """
  orgs_delete_attestations_by_id(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Attestation ID"""
    attestation_id: Int!
  ): Void

  """
  Blocks the given user on behalf of the specified organization and returns a 204. If the organization cannot block the given user a 422 is returned.
  """
  orgs_block_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """Unblocks the given user on behalf of the specified organization."""
  orgs_unblock_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Create a campaign for an organization.
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
  
  Fine-grained tokens must have the "Code scanning alerts" repository permissions (read) on all repositories included
  in the campaign.
  """
  campaigns_create_campaign(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: campaigns_create_campaign_request_Input
  ): Campaign_summary

  """
  Updates a campaign in an organization.
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
  """
  campaigns_update_campaign(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The campaign number."""
    campaign_number: Int!
    input: campaigns_update_campaign_request_Input
  ): Campaign_summary

  """
  Deletes a campaign in an organization.
  
  The authenticated user must be an owner or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint.
  """
  campaigns_delete_campaign(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The campaign number."""
    campaign_number: Int!
  ): Void

  """
  Creates a code security configuration in an organization.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  code_security_create_configuration(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: code_security_create_configuration_request_Input
  ): code_security_configuration

  """
  Detach code security configuration(s) from a set of repositories.
  Repositories will retain their settings but will no longer be associated with the configuration.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  code_security_detach_configuration(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: code_security_detach_configuration_request_Input
  ): Void

  """
  Updates a code security configuration in an organization.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  code_security_update_configuration(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
    input: code_security_update_configuration_request_Input
  ): code_security_update_configuration_response

  """
  Deletes the desired code security configuration from an organization.
  Repositories attached to the configuration will retain their settings but will no longer be associated with
  the configuration.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  code_security_delete_configuration(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
  ): Void

  """
  Attach a code security configuration to a set of repositories. If the repositories specified are already attached to a configuration, they will be re-attached to the provided configuration.
  
  If insufficient GHAS licenses are available to attach the configuration to a repository, only free features will be enabled.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  code_security_attach_configuration(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
    input: code_security_attach_configuration_request_Input
  ): JSON

  """
  Sets a code security configuration as a default to be applied to new repositories in your organization.
  
  This configuration will be applied to the matching repository type (all, none, public, private and internal) by default when they are created.
  
  The authenticated user must be an administrator or security manager for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  code_security_set_configuration_as_default(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the code security configuration."""
    configuration_id: Int!
    input: code_security_set_configuration_as_default_request_Input
  ): code_security_set_configuration_as_default_200_response

  """
  Sets which users can access codespaces in an organization. This is synonymous with granting or revoking codespaces access permissions for users according to the visibility.
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_set_codespaces_access(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: codespaces_set_codespaces_access_request_Input
  ): Void @deprecated(reason: "deprecated")

  """
  Codespaces for the specified users will be billed to the organization.
  
  To use this endpoint, the access settings for the organization must be set to `selected_members`.
  For information on how to change this setting, see "[Manage access control for organization codespaces](https://docs.github.com/rest/codespaces/organizations#manage-access-control-for-organization-codespaces)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_set_codespaces_access_users(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: codespaces_set_codespaces_access_users_request_Input
  ): Void @deprecated(reason: "deprecated")

  """
  Codespaces for the specified users will no longer be billed to the organization.
  
  To use this endpoint, the access settings for the organization must be set to `selected_members`.
  For information on how to change this setting, see "[Manage access control for organization codespaces](https://docs.github.com/rest/codespaces/organizations#manage-access-control-for-organization-codespaces)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_delete_codespaces_access_users(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: codespaces_delete_codespaces_access_users_request_Input
  ): Void @deprecated(reason: "deprecated")

  """
  Creates or updates an organization development environment secret with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_create_or_update_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    input: codespaces_create_or_update_org_secret_request_Input
  ): codespaces_create_or_update_org_secret_response

  """
  Deletes an organization development environment secret using the secret name.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_delete_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Replaces all repositories for an organization development environment secret when the `visibility`
  for repository access is set to `selected`. The visibility is set when you [Create
  or update an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#create-or-update-an-organization-secret).
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_set_selected_repos_for_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    input: codespaces_set_selected_repos_for_org_secret_request_Input
  ): Void

  """
  Adds a repository to an organization development environment secret when the `visibility` for repository access is set to `selected`. The visibility is set when you [Create or update an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#create-or-update-an-organization-secret).
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_add_selected_repo_to_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  Removes a repository from an organization development environment secret when the `visibility`
  for repository access is set to `selected`. The visibility is set when you [Create
  or update an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#create-or-update-an-organization-secret).
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_remove_selected_repo_from_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Purchases a GitHub Copilot seat for all users within each specified team.
  The organization will be billed for each seat based on the organization's Copilot plan. For more information about Copilot pricing, see "[About billing for GitHub Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/about-billing-for-github-copilot-in-your-organization)."
  
  Only organization owners can purchase Copilot seats for their organization members. The organization must have a Copilot Business or Copilot Enterprise subscription and a configured suggestion matching policy.
  For more information about setting up a Copilot subscription, see "[Subscribing to Copilot for your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/subscribing-to-copilot-for-your-organization)."
  For more information about setting a suggestion matching policy, see "[Managing policies for Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/setting-policies-for-copilot-in-your-organization/managing-policies-for-copilot-in-your-organization#policies-for-suggestion-matching)."
  
  The response contains the total number of new seats that were created and existing seats that were refreshed.
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:org` scopes to use this endpoint.
  """
  copilot_add_copilot_seats_for_teams(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: copilot_add_copilot_seats_for_teams_request_Input
  ): copilot_add_copilot_seats_for_teams_201_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Sets seats for all members of each team specified to "pending cancellation".
  This will cause the members of the specified team(s) to lose access to GitHub Copilot at the end of the current billing cycle unless they retain access through another team.
  For more information about disabling access to Copilot, see "[Revoking access to Copilot for members of your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-access-to-github-copilot-in-your-organization/revoking-access-to-copilot-for-members-of-your-organization)."
  
  Only organization owners can cancel Copilot seats for their organization members.
  
  The response contains the total number of seats set to "pending cancellation".
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:org` scopes to use this endpoint.
  """
  copilot_cancel_copilot_seat_assignment_for_teams(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: copilot_cancel_copilot_seat_assignment_for_teams_request_Input
  ): copilot_cancel_copilot_seat_assignment_for_teams_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Purchases a GitHub Copilot seat for each user specified.
  The organization will be billed for each seat based on the organization's Copilot plan. For more information about Copilot pricing, see "[About billing for GitHub Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/about-billing-for-github-copilot-in-your-organization)."
  
  Only organization owners can purchase Copilot seats for their organization members. The organization must have a Copilot Business or Copilot Enterprise subscription and a configured suggestion matching policy.
  For more information about setting up a Copilot subscription, see "[Subscribing to Copilot for your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-the-copilot-subscription-for-your-organization/subscribing-to-copilot-for-your-organization)."
  For more information about setting a suggestion matching policy, see "[Managing policies for Copilot in your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/setting-policies-for-copilot-in-your-organization/managing-policies-for-copilot-in-your-organization#policies-for-suggestion-matching)."
  
  The response contains the total number of new seats that were created and existing seats that were refreshed.
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:org` scopes to use this endpoint.
  """
  copilot_add_copilot_seats_for_users(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: copilot_add_copilot_seats_for_users_request_Input
  ): copilot_add_copilot_seats_for_users_201_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Sets seats for all users specified to "pending cancellation".
  This will cause the specified users to lose access to GitHub Copilot at the end of the current billing cycle unless they retain access through team membership.
  For more information about disabling access to Copilot, see "[Revoking access to Copilot for members of your organization](https://docs.github.com/copilot/managing-copilot/managing-github-copilot-in-your-organization/managing-access-to-github-copilot-in-your-organization/revoking-access-to-copilot-for-members-of-your-organization)."
  
  Only organization owners can cancel Copilot seats for their organization members.
  
  The response contains the total number of seats set to "pending cancellation".
  
  OAuth app tokens and personal access tokens (classic) need either the `manage_billing:copilot` or `admin:org` scopes to use this endpoint.
  """
  copilot_cancel_copilot_seat_assignment_for_users(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: copilot_cancel_copilot_seat_assignment_for_users_request_Input
  ): copilot_cancel_copilot_seat_assignment_for_users_200_response

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Sets the policy for which repositories in an organization can use Copilot coding agent.
  
  Organization owners can configure whether Copilot coding agent is enabled for
  all repositories, selected repositories, or no repositories owned by the organization.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  copilot_set_copilot_coding_agent_permissions_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: copilot_set_copilot_coding_agent_permissions_organization_request_Input
  ): Void

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Replaces the list of selected repositories that are enabled for Copilot coding
  agent in an organization. This method can only be called when the coding agent
  repository policy is set to `selected`.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  copilot_set_copilot_coding_agent_selected_repositories_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: copilot_set_copilot_coding_agent_selected_repositories_for_organization_request_Input
  ): Void

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Adds a repository to the list of selected repositories enabled for Copilot
  coding agent in an organization. This method can only be called when the
  coding agent repository policy is set to `selected`.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  copilot_enable_copilot_coding_agent_for_repository_in_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Removes a repository from the list of selected repositories enabled for Copilot
  coding agent in an organization. This method can only be called when the
  coding agent repository policy is set to `selected`.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scopes to use this endpoint.
  """
  copilot_disable_copilot_coding_agent_for_repository_in_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  > [!NOTE]
  > This endpoint is in public preview and is subject to change.
  
  Sets Copilot content exclusion path rules for an organization.
  To configure these settings, go to the organization's settings on GitHub.
  For more information, see "[Excluding content from GitHub Copilot](https://docs.github.com/copilot/managing-copilot/configuring-and-auditing-content-exclusion/excluding-content-from-github-copilot#configuring-content-exclusions-for-your-organization)."
  
  Organization owners can set Copilot content exclusion rules for the organization.
  
  OAuth app tokens and personal access tokens (classic) need the `copilot` scope to use this endpoint.
  
  > [!CAUTION]
  > * At this time, the API does not support comments. When using this endpoint, any existing comments in your rules will be deleted.
  > * At this time, the API does not support duplicate keys. If you submit content exclusions through the API with duplicate keys, only the last occurrence will be saved. Earlier entries with the same key will be overwritten.
  """
  copilot_set_copilot_content_exclusion_for_organization(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: JSON
  ): copilot_set_copilot_content_exclusion_for_organization_200_response

  """
  Creates or updates an organization secret with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_create_or_update_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    input: dependabot_create_or_update_org_secret_request_Input
  ): dependabot_create_or_update_org_secret_response

  """
  Deletes a secret in an organization using the secret name.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_delete_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Replaces all repositories for an organization secret when the `visibility`
  for repository access is set to `selected`. The visibility is set when you [Create
  or update an organization secret](https://docs.github.com/rest/dependabot/secrets#create-or-update-an-organization-secret).
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_set_selected_repos_for_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    input: dependabot_set_selected_repos_for_org_secret_request_Input
  ): Void

  """
  Adds a repository to an organization secret when the `visibility` for
  repository access is set to `selected`. The visibility is set when you [Create or
  update an organization secret](https://docs.github.com/rest/dependabot/secrets#create-or-update-an-organization-secret).
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_add_selected_repo_to_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  Removes a repository from an organization secret when the `visibility`
  for repository access is set to `selected`. The visibility is set when you [Create
  or update an organization secret](https://docs.github.com/rest/dependabot/secrets#create-or-update-an-organization-secret).
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  dependabot_remove_selected_repo_from_org_secret(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  Create a hook that posts payloads in JSON format.
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or
  edit webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_create_webhook(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_create_webhook_request_Input
  ): Org_Hook

  """
  Updates a webhook configured in an organization. When you update a webhook,
  the `secret` will be overwritten. If you previously had a `secret` set, you must
  provide the same `secret` or set a new `secret` or the secret will be removed. If
  you are only updating individual webhook `config` properties, use "[Update a webhook
  configuration for an organization](/rest/orgs/webhooks#update-a-webhook-configuration-for-an-organization)".
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_update_webhook(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    input: orgs_update_webhook_request_Input
  ): Org_Hook

  """
  Delete a webhook for an organization.
  
  The authenticated user must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_delete_webhook(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Void

  """
  Updates the webhook configuration for an organization. To update more information about the webhook, including the `active` state and `events`, use "[Update an organization webhook ](/rest/orgs/webhooks#update-an-organization-webhook)."
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_update_webhook_config_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    input: orgs_update_webhook_config_for_org_request_Input
  ): Webhook_Configuration

  """
  Redeliver a delivery for a webhook configured in an organization.
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_redeliver_webhook_delivery(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    delivery_id: Int!
  ): JSON

  """
  This will trigger a [ping event](https://docs.github.com/webhooks/#ping-event)
  to be sent to the hook.
  
  You must be an organization owner to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need `admin:org_hook` scope. OAuth apps cannot list, view, or edit
  webhooks that they did not create and users cannot list, view, or edit webhooks that were created by OAuth apps.
  """
  orgs_ping_webhook(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Void

  """
  Temporarily restricts interactions to a certain type of GitHub user in any public repository in the given organization. You must be an organization owner to set these restrictions. Setting the interaction limit at the organization level will overwrite any interaction limits that are set for individual repositories owned by the organization.
  """
  interactions_set_restrictions_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: Interaction_Restrictions_Input
  ): Interaction_Limits

  """
  Removes all interaction restrictions from public repositories in the given organization. You must be an organization owner to remove restrictions.
  """
  interactions_remove_restrictions_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
  ): Void

  """
  Invite people to an organization by using their GitHub user ID or their email address. In order to create invitations in an organization, the authenticated user must be an organization owner.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  """
  orgs_create_invitation(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_create_invitation_request_Input
  ): Organization_Invitation

  """
  Cancel an organization invitation. In order to cancel an organization invitation, the authenticated user must be an organization owner.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications).
  """
  orgs_cancel_invitation(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the invitation."""
    invitation_id: Int!
  ): Void

  """
  Creates a new issue field for an organization.
  
  You can find out more about issue fields in [Managing issue fields in an organization](https://docs.github.com/issues/tracking-your-work-with-issues/using-issues/managing-issue-fields-in-an-organization).
  
  To use this endpoint, the authenticated user must be an administrator for the organization. OAuth app tokens and
  personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_create_issue_field(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: organization_create_issue_field_Input
  ): Issue_Field

  """
  Updates an issue field for an organization.
  
  You can find out more about issue fields in [Managing issue fields in an organization](https://docs.github.com/issues/tracking-your-work-with-issues/using-issues/managing-issue-fields-in-an-organization).
  
  To use this endpoint, the authenticated user must be an administrator for the organization. OAuth app tokens and
  personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_update_issue_field(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the issue field."""
    issue_field_id: Int!
    input: organization_update_issue_field_Input
  ): Issue_Field

  """
  Deletes an issue field for an organization.
  
  You can find out more about issue fields in [Managing issue fields in an organization](https://docs.github.com/issues/tracking-your-work-with-issues/using-issues/managing-issue-fields-in-an-organization).
  
  To use this endpoint, the authenticated user must be an administrator for the organization. OAuth app tokens and
  personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_delete_issue_field(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the issue field."""
    issue_field_id: Int!
  ): Void

  """
  Create a new issue type for an organization.
  
  You can find out more about issue types in [Managing issue types in an organization](https://docs.github.com/issues/tracking-your-work-with-issues/configuring-issues/managing-issue-types-in-an-organization).
  
  To use this endpoint, the authenticated user must be an administrator for the organization. OAuth app tokens and
  personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_create_issue_type(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: organization_create_issue_type_Input
  ): Issue_Type

  """
  Updates an issue type for an organization.
  
  You can find out more about issue types in [Managing issue types in an organization](https://docs.github.com/issues/tracking-your-work-with-issues/configuring-issues/managing-issue-types-in-an-organization).
  
  To use this endpoint, the authenticated user must be an administrator for the organization. OAuth app tokens and
  personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_update_issue_type(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the issue type."""
    issue_type_id: Int!
    input: organization_update_issue_type_Input
  ): Issue_Type

  """
  Deletes an issue type for an organization.
  
  You can find out more about issue types in [Managing issue types in an organization](https://docs.github.com/issues/tracking-your-work-with-issues/configuring-issues/managing-issue-types-in-an-organization).
  
  To use this endpoint, the authenticated user must be an administrator for the organization. OAuth app tokens and
  personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_delete_issue_type(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the issue type."""
    issue_type_id: Int!
  ): Void

  """
  Removing a user from this list will remove them from all teams and they will no longer have any access to the organization's repositories.
  
  > [!NOTE]
  > If a user has both direct membership in the organization as well as indirect membership via an enterprise team, only their direct membership will be removed. Their indirect membership via an enterprise team remains until the user is removed from the enterprise team.
  """
  orgs_remove_member(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Deletes a user's codespace.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_delete_from_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!

    """The name of the codespace."""
    codespace_name: String!
  ): JSON

  """
  Stops a user's codespace.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  codespaces_stop_in_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!

    """The name of the codespace."""
    codespace_name: String!
  ): codespace

  """
  Only authenticated organization owners can add a member to the organization or update the member's role.
  
  *   If the authenticated user is _adding_ a member to the organization, the invited user will receive an email inviting them to the organization. The user's [membership status](https://docs.github.com/rest/orgs/members#get-organization-membership-for-a-user) will be `pending` until they accept the invitation.
      
  *   Authenticated users can _update_ a user's membership by passing the `role` parameter. If the authenticated user changes a member's role to `admin`, the affected user will receive an email notifying them that they've been made an organization owner. If the authenticated user changes an owner's role to `member`, no email will be sent.
  
  **Rate limits**
  
  To prevent abuse, organization owners are limited to creating 50 organization invitations for an organization within a 24 hour period. If the organization is more than one month old or on a paid plan, the limit is 500 invitations per 24 hour period.
  """
  orgs_set_membership_for_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
    input: orgs_set_membership_for_user_request_Input
  ): Org_Membership

  """
  In order to remove a user's membership with an organization, the authenticated user must be an organization owner.
  
  If the specified user is an active member of the organization, this will remove them from the organization. If the specified user has been invited to the organization, this will cancel their invitation. The specified user will receive an email notification in both cases.
  
  > [!NOTE]
  > If a user has both direct membership in the organization as well as indirect membership via an enterprise team, only their direct membership will be removed. Their indirect membership via an enterprise team remains until the user is removed from the enterprise team.
  """
  orgs_remove_membership_for_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """Initiates the generation of a migration archive."""
  migrations_start_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: migrations_start_for_org_request_Input
  ): Migration

  """
  Deletes a previous migration archive. Migration archives are automatically deleted after seven days.
  """
  migrations_delete_archive_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the migration."""
    migration_id: Int!
  ): Void

  """
  Unlocks a repository that was locked for migration. You should unlock each migrated repository and [delete them](https://docs.github.com/rest/repos/repos#delete-a-repository) when the migration is complete and you no longer need the source data.
  """
  migrations_unlock_repo_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the migration."""
    migration_id: Int!

    """repo_name parameter"""
    repo_name: String!
  ): Void

  """
  Removes all assigned organization roles from a team. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  The authenticated user must be an administrator for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_revoke_all_org_roles_team(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!
  ): Void

  """
  Assigns an organization role to a team in an organization. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  The authenticated user must be an administrator for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_assign_team_to_org_role(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The unique identifier of the role."""
    role_id: Int!
  ): Void

  """
  Removes an organization role from a team. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  The authenticated user must be an administrator for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_revoke_org_role_team(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The unique identifier of the role."""
    role_id: Int!
  ): Void

  """
  Revokes all assigned organization roles from a user. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  The authenticated user must be an administrator for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_revoke_all_org_roles_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Assigns an organization role to a member of an organization. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  The authenticated user must be an administrator for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_assign_user_to_org_role(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!

    """The unique identifier of the role."""
    role_id: Int!
  ): Void

  """
  Remove an organization role from a user. For more information on organization roles, see "[Using organization roles](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/using-organization-roles)."
  
  The authenticated user must be an administrator for the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_revoke_org_role_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!

    """The unique identifier of the role."""
    role_id: Int!
  ): Void

  """
  When an organization member is converted to an outside collaborator, they'll only have access to the repositories that their current team membership allows. The user will no longer be a member of the organization. For more information, see "[Converting an organization member to an outside collaborator](https://docs.github.com/articles/converting-an-organization-member-to-an-outside-collaborator/)". Converting an organization member to an outside collaborator may be restricted by enterprise administrators. For more information, see "[Enforcing repository management policies in your enterprise](https://docs.github.com/admin/policies/enforcing-policies-for-your-enterprise/enforcing-repository-management-policies-in-your-enterprise#enforcing-a-policy-for-inviting-outside-collaborators-to-repositories)."
  """
  orgs_convert_member_to_outside_collaborator(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
    input: orgs_convert_member_to_outside_collaborator_request_Input
  ): orgs_convert_member_to_outside_collaborator_response

  """
  Removing a user from this list will remove them from all the organization's repositories.
  """
  orgs_remove_outside_collaborator(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Deletes an entire package in an organization. You cannot delete a public package if any version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for further assistance.
  
  The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_delete_package_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!
  ): Void

  """
  Restores an entire package in an organization.
  
  You can restore a deleted package under the following conditions:
    - The package was deleted within the last 30 days.
    - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
  
  The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_restore_package_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """package token"""
    token: String
  ): Void

  """
  Deletes a specific package version in an organization. If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance.
  
  The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_delete_package_version_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Void

  """
  Restores a specific package version in an organization.
  
  You can restore a deleted package under the following conditions:
    - The package was deleted within the last 30 days.
    - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
  
  The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_restore_package_version_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Void

  """
  Approves or denies multiple pending requests to access organization resources via a fine-grained personal access token.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_review_pat_grant_requests_in_bulk(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_review_pat_grant_requests_in_bulk_request_Input
  ): JSON

  """
  Approves or denies a pending request to access organization resources via a fine-grained personal access token.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_review_pat_grant_request(
    """The organization name. The name is not case sensitive."""
    org: String!

    """
    Unique identifier of the request for access via fine-grained personal access token.
    """
    pat_request_id: Int!
    input: orgs_review_pat_grant_request_request_Input
  ): Void

  """
  Updates the access organization members have to organization resources via fine-grained personal access tokens. Limited to revoking a token's existing access.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_update_pat_accesses(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_update_pat_accesses_request_Input
  ): JSON

  """
  Updates the access an organization member has to organization resources via a fine-grained personal access token. Limited to revoking the token's existing access. Limited to revoking a token's existing access.
  
  Only GitHub Apps can use this endpoint.
  """
  orgs_update_pat_access(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the fine-grained personal access token."""
    pat_id: Int!
    input: orgs_update_pat_access_request_Input
  ): Void

  "\nCreates a private registry configuration with an encrypted value for an organization. Encrypt your secret using [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see \"[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api).\"\n\nOAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint."
  private_registries_create_org_private_registry(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: private_registries_create_org_private_registry_request_Input
  ): org_private_registry_configuration_with_selected_repositories

  "\nUpdates a private registry configuration with an encrypted value for an organization. Encrypt your secret using [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see \"[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api).\"\n\nOAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint."
  private_registries_update_org_private_registry(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
    input: private_registries_update_org_private_registry_request_Input
  ): Void

  "\nDelete a private registry configuration at the organization-level.\n\nOAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint."
  private_registries_delete_org_private_registry(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """Create draft issue item for the specified organization owned project."""
  projects_create_draft_item_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!
    input: projects_create_draft_item_for_org_request_Input
  ): projects_v2_item_simple

  """Add a field to an organization-owned project."""
  projects_add_field_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!
    input: projects_add_field_for_org_request_Input
  ): Projects_v2_Field

  """
  Add an issue or pull request item to the specified organization owned project.
  """
  projects_add_item_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!
    input: projects_add_item_for_org_request_Input
  ): projects_v2_item_simple

  """Update a specific item in an organization-owned project."""
  projects_update_item_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the project item."""
    item_id: Int!
    input: projects_update_item_for_org_request_Input
  ): projects_v2_item_with_content

  """Delete a specific item from an organization-owned project."""
  projects_delete_item_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the project item."""
    item_id: Int!
  ): Void

  """
  Create a new view in an organization-owned project. Views allow you to customize how items in a project are displayed and filtered.
  """
  projects_create_view_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The project's number."""
    project_number: Int!
    input: projects_create_view_for_org_request_Input
  ): Projects_v2_View

  """
  Creates new or updates existing custom properties defined for an organization in a batch.
  
  If the property already exists, the existing property will be replaced with the new values.
  Missing optional values will fall back to default values, previous values will be overwritten.
  E.g. if a property exists with `values_editable_by: org_and_repo_actors` and it's updated without specifying `values_editable_by`, it will be updated to default value `org_actors`.
  
  To use this endpoint, the authenticated user must be one of:
    - An administrator for the organization.
    - A user, or a user on a team, with the fine-grained permission of `custom_properties_org_definitions_manager` in the organization.
  """
  orgs_custom_properties_for_repos_create_or_update_organization_definitions(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_custom_properties_for_repos_create_or_update_organization_definitions_request_Input
  ): [Organization_Custom_Property]

  """
  Creates a new or updates an existing custom property that is defined for an organization.
  
  To use this endpoint, the authenticated user must be one of:
  - An administrator for the organization.
  - A user, or a user on a team, with the fine-grained permission of `custom_properties_org_definitions_manager` in the organization.
  """
  orgs_custom_properties_for_repos_create_or_update_organization_definition(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The custom property name"""
    custom_property_name: String!
    input: Custom_Property_Set_Payload_Input
  ): Organization_Custom_Property

  """
  Removes a custom property that is defined for an organization.
  
  To use this endpoint, the authenticated user must be one of:
    - An administrator for the organization.
    - A user, or a user on a team, with the fine-grained permission of `custom_properties_org_definitions_manager` in the organization.
  """
  orgs_custom_properties_for_repos_delete_organization_definition(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The custom property name"""
    custom_property_name: String!
  ): Void

  """
  Create new or update existing custom property values for repositories in a batch that belong to an organization.
  Each target repository will have its custom property values updated to match the values provided in the request.
  
  A maximum of 30 repositories can be updated in a single request.
  
  Using a value of `null` for a custom property will remove or 'unset' the property value from the repository.
  
  To use this endpoint, the authenticated user must be one of:
    - An administrator for the organization.
    - A user, or a user on a team, with the fine-grained permission of `custom_properties_org_values_editor` in the organization.
  """
  orgs_custom_properties_for_repos_create_or_update_organization_values(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_custom_properties_for_repos_create_or_update_organization_values_request_Input
  ): Void

  """
  The user can publicize their own membership. (A user cannot publicize the membership for another user.)
  
  Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  """
  orgs_set_public_membership_for_authenticated_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Removes the public membership for the authenticated user from the specified organization, unless public visibility is enforced by default.
  """
  orgs_remove_public_membership_for_authenticated_user(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Creates a new repository in the specified organization. The authenticated user must be a member of the organization.
  
  OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to create a public repository, and `repo` scope to create a private repository.
  """
  repos_create_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: repos_create_in_org_request_Input
  ): Full_Repository

  """Create a repository ruleset for an organization."""
  repos_create_org_ruleset(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: repos_create_org_ruleset_request_Input
  ): Repository_ruleset

  """Update a ruleset for an organization."""
  repos_update_org_ruleset(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the ruleset."""
    ruleset_id: Int!
    input: repos_update_org_ruleset_request_Input
  ): Repository_ruleset

  """Delete a ruleset for an organization."""
  repos_delete_org_ruleset(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The ID of the ruleset."""
    ruleset_id: Int!
  ): Void

  """
  Updates the secret scanning pattern configurations for an organization.
  
  Personal access tokens (classic) need the `write:org` scope to use this endpoint.
  """
  secret_scanning_update_org_pattern_configs(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: secret_scanning_update_org_pattern_configs_request_Input
  ): secret_scanning_update_org_pattern_configs_200_response

  """
  > [!WARNING]
  > **Closing down notice:** This operation is closing down and will be removed starting January 1, 2026. Please use the "[Organization Roles](https://docs.github.com/rest/orgs/organization-roles)" endpoints instead.
  """
  orgs_add_security_manager_team(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!
  ): Void @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Closing down notice:** This operation is closing down and will be removed starting January 1, 2026. Please use the "[Organization Roles](https://docs.github.com/rest/orgs/organization-roles)" endpoints instead.
  """
  orgs_remove_security_manager_team(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!
  ): Void @deprecated(reason: "deprecated")

  """
  Sets the immutable releases policy for repositories in an organization.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_set_immutable_releases_settings(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_set_immutable_releases_settings_request_Input
  ): Void

  """
  Replaces all repositories that have been selected for immutable releases enforcement in an organization. To use this endpoint, the organization immutable releases policy for `enforced_repositories` must be configured to `selected`.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_set_immutable_releases_settings_repositories(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_set_immutable_releases_settings_repositories_request_Input
  ): Void

  """
  Adds a repository to the list of selected repositories that are enforced for immutable releases in an organization. To use this endpoint, the organization immutable releases policy for `enforced_repositories` must be configured to `selected`.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_enable_selected_repository_immutable_releases_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Removes a repository from the list of selected repositories that are enforced for immutable releases in an organization. To use this endpoint, the organization immutable releases policy for `enforced_repositories` must be configured to `selected`.
  
  OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.
  """
  orgs_disable_selected_repository_immutable_releases_organization(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Creates a hosted compute network configuration for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `write:network_configurations` scope to use this endpoint.
  """
  hosted_compute_create_network_configuration_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: hosted_compute_create_network_configuration_for_org_request_Input
  ): Hosted_compute_network_configuration

  """
  Updates a hosted compute network configuration for an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `write:network_configurations` scope to use this endpoint.
  """
  hosted_compute_update_network_configuration_for_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the hosted compute network configuration."""
    network_configuration_id: String!
    input: hosted_compute_update_network_configuration_for_org_request_Input
  ): Hosted_compute_network_configuration

  """
  Deletes a hosted compute network configuration from an organization.
  
  OAuth app tokens and personal access tokens (classic) need the `write:network_configurations` scope to use this endpoint.
  """
  hosted_compute_delete_network_configuration_from_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """Unique identifier of the hosted compute network configuration."""
    network_configuration_id: String!
  ): Void

  """
  To create a team, the authenticated user must be a member or owner of `{org}`. By default, organization members can create teams. Organization owners can limit team creation to organization owners. For more information, see "[Setting team creation permissions](https://docs.github.com/articles/setting-team-creation-permissions-in-your-organization)."
  
  When you create a new team, you automatically become a team maintainer without explicitly adding yourself to the optional array of `maintainers`. For more information, see "[About teams](https://docs.github.com/github/setting-up-and-managing-organizations-and-teams/about-teams)".
  """
  teams_create(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: teams_create_request_Input
  ): Full_Team @deprecated(reason: "deprecated")

  """
  To edit a team, the authenticated user must either be an organization owner or a team maintainer.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `PATCH /organizations/{org_id}/team/{team_id}`.
  """
  teams_update_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!
    input: teams_update_in_org_request_Input
  ): Full_Team @deprecated(reason: "deprecated")

  """
  To delete a team, the authenticated user must be an organization owner or team maintainer.
  
  If you are an organization owner, deleting a parent team will delete all of its child teams as well.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `DELETE /organizations/{org_id}/team/{team_id}`.
  """
  teams_delete_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!
  ): Void

  """
  Adds an organization member to a team. An authenticated organization owner or team maintainer can add organization members to a team.
  
  Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  > [!NOTE]
  > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
  
  An organization owner can add someone who is not part of the team's organization to a team. When an organization owner adds someone to a team who is not an organization member, this endpoint will send an invitation to the person via email. This newly-created membership will be in the "pending" state until the person accepts the invitation, at which point the membership will transition to the "active" state and the user will be added as a member of the team.
  
  If the user is already a member of the team, this endpoint will update the role of the team member's role. To update the membership of a team member, the authenticated user must be an organization owner or a team maintainer.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `PUT /organizations/{org_id}/team/{team_id}/memberships/{username}`.
  """
  teams_add_or_update_membership_for_user_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The handle for the GitHub user account."""
    username: String!
    input: teams_add_or_update_membership_for_user_in_org_request_Input
  ): Team_Membership @deprecated(reason: "deprecated")

  """
  To remove a membership between a user and a team, the authenticated user must have 'admin' permissions to the team or be an owner of the organization that the team is associated with. Removing team membership does not delete the user, it just removes their membership from the team.
  
  Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  > [!NOTE]
  > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `DELETE /organizations/{org_id}/team/{team_id}/memberships/{username}`.
  """
  teams_remove_membership_for_user_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  To add a repository to a team or update the team's permission on a repository, the authenticated user must have admin access to the repository, and must be able to see the team. The repository must be owned by the organization, or a direct fork of a repository owned by the organization. You will get a `422 Unprocessable Entity` status if you attempt to add a repository to a team that is not owned by the organization. Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `PUT /organizations/{org_id}/team/{team_id}/repos/{owner}/{repo}`.
  
  For more information about the permission levels, see "[Repository permission levels for an organization](https://docs.github.com/github/setting-up-and-managing-organizations-and-teams/repository-permission-levels-for-an-organization#permission-levels-for-repositories-owned-by-an-organization)".
  """
  teams_add_or_update_repo_permissions_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: teams_add_or_update_repo_permissions_in_org_request_Input
  ): Void

  """
  If the authenticated user is an organization owner or a team maintainer, they can remove any repositories from the team. To remove a repository from a team as an organization member, the authenticated user must have admin access to the repository and must be able to see the team. This does not delete the repository, it just removes it from the team.
  
  > [!NOTE]
  > You can also specify a team by `org_id` and `team_id` using the route `DELETE /organizations/{org_id}/team/{team_id}/repos/{owner}/{repo}`.
  """
  teams_remove_repo_in_org(
    """The organization name. The name is not case sensitive."""
    org: String!

    """The slug of the team name."""
    team_slug: String!

    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  > [!WARNING]
  > **Closing down notice:** The ability to enable or disable a security feature for all eligible repositories in an organization is closing down. Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead. For more information, see the [changelog](https://github.blog/changelog/2024-07-22-deprecation-of-api-endpoint-to-enable-or-disable-a-security-feature-for-an-organization/).
  
  Enables or disables the specified security feature for all eligible repositories in an organization. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  The authenticated user must be an organization owner or be member of a team with the security manager role to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:org`, `write:org`, or `repo` scopes to use this endpoint.
  """
  orgs_enable_or_disable_security_product_on_all_org_repos(
    """The organization name. The name is not case sensitive."""
    org: String!
    security_product: security_product_parameter!
    enablement: org_security_product_enablement_parameter!
    input: orgs_enable_or_disable_security_product_on_all_org_repos_request_Input
  ): Void @deprecated(reason: "deprecated")

  """
  **Note**: To edit a repository's topics, use the [Replace all repository topics](https://docs.github.com/rest/repos/repos#replace-all-repository-topics) endpoint.
  """
  repos_update(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_update_request_Input
  ): Full_Repository

  """
  Deleting a repository requires admin access.
  
  If an organization owner has configured the organization to prevent members from deleting organization-owned
  repositories, you will get a `403 Forbidden` response.
  
  OAuth app tokens and personal access tokens (classic) need the `delete_repo` scope to use this endpoint.
  """
  repos_delete(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Deletes an artifact for a workflow run.
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_artifact(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the artifact."""
    artifact_id: Int!
  ): Void

  """
  Sets GitHub Actions cache retention limit for a repository. This determines how long caches will be retained for, if
  not manually removed or evicted due to size constraints.
  
  OAuth tokens and personal access tokens (classic) need the `admin:repository` scope to use this endpoint.
  """
  actions_set_actions_cache_retention_limit_for_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: Actions_cache_retention_limit_for_a_repository_Input
  ): Void

  """
  Sets GitHub Actions cache storage limit for a repository. This determines the maximum size of caches that can be
  stored before eviction occurs.
  
  OAuth tokens and personal access tokens (classic) need the `admin:repository` scope to use this endpoint.
  """
  actions_set_actions_cache_storage_limit_for_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: Actions_cache_storage_limit_for_a_repository_Input
  ): Void

  """
  Deletes one or more GitHub Actions caches for a repository, using a complete cache key. By default, all caches that match the provided key are deleted, but you can optionally provide a Git ref to restrict deletions to caches that match both the provided key and the Git ref.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_actions_cache_by_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """A key for identifying the cache."""
    key: String!

    """
    The full Git reference for narrowing down the cache. The `ref` for a branch should be formatted as `refs/heads/<branch name>`. To reference a pull request use `refs/pull/<number>/merge`.
    """
    ref: String
  ): Repository_actions_caches

  """
  Deletes a GitHub Actions cache for a repository, using a cache ID.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_actions_cache_by_id(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the GitHub Actions cache."""
    cache_id: Int!
  ): Void

  """
  Re-run a job and its dependent jobs in a workflow run.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_re_run_job_for_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the job."""
    job_id: Int!
    input: actions_re_run_job_for_workflow_run_request_Input
  ): JSON

  """
  Sets the customization template and `opt-in` or `opt-out` flag for an OpenID Connect (OIDC) subject claim for a repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_custom_oidc_sub_claim_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: mutationInput_actions_set_custom_oidc_sub_claim_for_repo_input_Input
  ): JSON

  """
  Sets the GitHub Actions permissions policy for enabling GitHub Actions and allowed actions and reusable workflows in the repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_github_actions_permissions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_set_github_actions_permissions_repository_request_Input
  ): Void

  """
  Sets the level of access that workflows outside of the repository have to actions and reusable workflows in the repository.
  This endpoint only applies to private repositories.
  For more information, see "[Allowing access to components in a private repository](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository#allowing-access-to-components-in-a-private-repository)".
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_workflow_access_to_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_workflow_access_to_repository_Input
  ): Void

  """
  Sets artifact and log retention settings for a repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_artifact_and_log_retention_settings_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_artifact_and_log_retention_Input
  ): Void

  """
  Sets the fork PR contributor approval policy for a repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_fork_pr_contributor_approval_permissions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_fork_pr_contributor_approval_Input
  ): Void

  """
  Sets the settings for whether workflows from fork pull requests can run on a private repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_private_repo_fork_pr_workflows_settings_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_fork_pr_workflows_private_repos_request_Input
  ): Void

  """
  Sets the actions and reusable workflows that are allowed in a repository. To use this endpoint, the repository permission policy for `allowed_actions` must be configured to `selected`. For more information, see "[Set GitHub Actions permissions for a repository](#set-github-actions-permissions-for-a-repository)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_allowed_actions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: selected_actions_Input
  ): Void

  """
  Sets the default workflow permissions granted to the `GITHUB_TOKEN` when running workflows in a repository, and sets if GitHub Actions
  can submit approving pull request reviews.
  For more information, see "[Setting the permissions of the GITHUB_TOKEN for your repository](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository#setting-the-permissions-of-the-github_token-for-your-repository)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_github_actions_default_workflow_permissions_repository(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_set_default_workflow_permissions_Input
  ): Void

  """
  Generates a configuration that can be passed to the runner application at startup.
  
  The authenticated user must have admin access to the repository.
  
  OAuth tokens and personal access tokens (classic) need the`repo` scope to use this endpoint.
  """
  actions_generate_runner_jitconfig_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_generate_runner_jitconfig_for_repo_request_Input
  ): actions_runner_jitconfig_response

  """
  Returns a token that you can pass to the `config` script. The token expires after one hour.
  
  For example, you can replace `TOKEN` in the following example with the registration token provided by this endpoint to configure your self-hosted runner:
  
  ```
  ./config.sh --url https://github.com/octo-org --token TOKEN
  ```
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_registration_token_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Authentication_Token

  """
  Returns a token that you can pass to the `config` script to remove a self-hosted runner from an repository. The token expires after one hour.
  
  For example, you can replace `TOKEN` in the following example with the registration token provided by this endpoint to remove your self-hosted runner from an organization:
  
  ```
  ./config.sh remove --token TOKEN
  ```
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_remove_token_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Authentication_Token

  """
  Forces the removal of a self-hosted runner from a repository. You can use this endpoint to completely remove the runner when the machine you were using no longer exists.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_self_hosted_runner_from_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): Void

  """
  Adds custom labels to a self-hosted runner configured in a repository.
  
  Authenticated users must have admin access to the organization to use this endpoint.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_add_custom_labels_to_self_hosted_runner_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
    input: actions_add_custom_labels_to_self_hosted_runner_for_repo_request_Input
  ): actions_runner_labels_response

  """
  Remove all previous custom labels and set the new custom labels for a specific
  self-hosted runner configured in a repository.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_set_custom_labels_for_self_hosted_runner_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
    input: actions_set_custom_labels_for_self_hosted_runner_for_repo_request_Input
  ): actions_runner_labels_response

  """
  Remove all custom labels from a self-hosted runner configured in a
  repository. Returns the remaining read-only labels from the runner.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_remove_all_custom_labels_from_self_hosted_runner_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!
  ): actions_runner_labels_readonly_response

  """
  Remove a custom label from a self-hosted runner configured
  in a repository. Returns the remaining labels from the runner.
  
  This endpoint returns a `404 Not Found` status if the custom label is not
  present on the runner.
  
  Authenticated users must have admin access to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_remove_custom_label_from_self_hosted_runner_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """Unique identifier of the self-hosted runner."""
    runner_id: Int!

    """The name of a self-hosted runner's custom label."""
    name: String!
  ): actions_runner_labels_response

  """
  Deletes a specific workflow run.
  
  Anyone with write access to the repository can use this endpoint.
  
  If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): Void

  """
  Approves a workflow run for a pull request from a public fork of a first time contributor. For more information, see ["Approving workflow runs from public forks](https://docs.github.com/actions/managing-workflow-runs/approving-workflow-runs-from-public-forks)."
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_approve_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): JSON

  """
  Cancels a workflow run using its `id`.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_cancel_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): JSON

  """
  Approve or reject custom deployment protection rules provided by a GitHub App for a workflow run. For more information, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
  
  > [!NOTE]
  > GitHub Apps can only review their own custom deployment protection rules. To approve or reject pending deployments that are waiting for review from a specific person or team, see [`POST /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments`](/rest/actions/workflow-runs#review-pending-deployments-for-a-workflow-run).
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
  """
  actions_review_custom_gates_for_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
    input: actions_review_custom_gates_for_run_request_Input
  ): Void

  """
  Cancels a workflow run and bypasses conditions that would otherwise cause a workflow execution to continue, such as an `always()` condition on a job.
  You should only use this endpoint to cancel a workflow run when the workflow run is not responding to [`POST /repos/{owner}/{repo}/actions/runs/{run_id}/cancel`](/rest/actions/workflow-runs#cancel-a-workflow-run).
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_force_cancel_workflow_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): JSON

  """
  Deletes all logs for a workflow run.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_workflow_run_logs(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
  ): Void

  """
  Approve or reject pending deployments that are waiting on approval by a required reviewer.
  
  Required reviewers with read access to the repository contents and deployments can use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_review_pending_deployments_for_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
    input: actions_review_pending_deployments_for_run_request_Input
  ): [deployment]

  """
  Re-runs your workflow run using its `id`.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_re_run_workflow(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
    input: actions_re_run_workflow_request_Input
  ): JSON

  """
  Re-run all of the failed jobs and their dependent jobs in a workflow run using the `id` of the workflow run.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_re_run_workflow_failed_jobs(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the workflow run."""
    run_id: Int!
    input: actions_re_run_workflow_failed_jobs_request_Input
  ): JSON

  """
  Creates or updates a repository secret with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_or_update_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
    input: actions_create_or_update_repo_secret_request_Input
  ): actions_create_or_update_repo_secret_response

  """
  Deletes a secret in a repository using the secret name.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Creates a repository variable that you can reference in a GitHub Actions workflow.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_repo_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: actions_create_repo_variable_request_Input
  ): JSON

  """
  Updates a repository variable that you can reference in a GitHub Actions workflow.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_update_repo_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the variable."""
    name: String!
    input: actions_update_repo_variable_request_Input
  ): Void

  """
  Deletes a repository variable using the variable name.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_repo_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the variable."""
    name: String!
  ): Void

  """
  Disables a workflow and sets the `state` of the workflow to `disabled_manually`. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_disable_workflow(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    workflow_id: workflow_id_parameter_Input!
  ): Void

  """
  You can use this endpoint to manually trigger a GitHub Actions workflow run. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`.
  
  You must configure your GitHub Actions workflow to run when the [`workflow_dispatch` webhook](/developers/webhooks-and-events/webhook-events-and-payloads#workflow_dispatch) event occurs. The `inputs` are configured in the workflow file. For more information about how to configure the `workflow_dispatch` event in the workflow file, see "[Events that trigger workflows](/actions/reference/events-that-trigger-workflows#workflow_dispatch)."
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_workflow_dispatch(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    workflow_id: workflow_id_parameter_Input!
    input: actions_create_workflow_dispatch_request_Input
  ): actions_create_workflow_dispatch_response

  """
  Enables a workflow and sets the `state` of the workflow to `active`. You can replace `workflow_id` with the workflow file name. For example, you could use `main.yaml`.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_enable_workflow(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    workflow_id: workflow_id_parameter_Input!
  ): Void

  """
  Store an artifact attestation and associate it with a repository.
  
  The authenticated user must have write permission to the repository and, if using a fine-grained access token, the `attestations:write` permission is required.
  
  Artifact attestations are meant to be created using the [attest action](https://github.com/actions/attest). For more information, see our guide on [using artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
  """
  repos_create_attestation(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_attestation_request_Input
  ): repos_create_attestation_201_response

  """Users with admin access to the repository can create an autolink."""
  repos_create_autolink(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_autolink_request_Input
  ): Autolink_reference

  """
  This deletes a single autolink reference by ID that was configured for the given repository.
  
  Information about autolinks are only available to repository administrators.
  """
  repos_delete_autolink(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the autolink."""
    autolink_id: Int!
  ): Void

  """
  Enables Dependabot security updates for a repository. The authenticated user must have admin access to the repository. For more information, see "[Configuring Dependabot security updates](https://docs.github.com/articles/configuring-automated-security-fixes)".
  """
  repos_enable_automated_security_fixes(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Disables Dependabot security updates for a repository. The authenticated user must have admin access to the repository. For more information, see "[Configuring Dependabot security updates](https://docs.github.com/articles/configuring-automated-security-fixes)".
  """
  repos_disable_automated_security_fixes(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Protecting a branch requires admin or owner permissions to the repository.
  
  > [!NOTE]
  > Passing new arrays of `users` and `teams` replaces their previous values.
  
  > [!NOTE]
  > The list of users, apps, and teams in total is limited to 100 items.
  """
  repos_update_branch_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_update_branch_protection_request_Input
  ): Protected_Branch

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_delete_branch_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Void

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Adding admin enforcement requires admin or owner permissions to the repository and branch protection to be enabled.
  """
  repos_set_admin_branch_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Protected_Branch_Admin_Enforced

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Removing admin enforcement requires admin or owner permissions to the repository and branch protection to be enabled.
  """
  repos_delete_admin_branch_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Void

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Updating pull request review enforcement requires admin or owner permissions to the repository and branch protection to be enabled.
  
  > [!NOTE]
  > Passing new arrays of `users` and `teams` replaces their previous values.
  """
  repos_update_pull_request_review_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_update_pull_request_review_protection_request_Input
  ): Protected_Branch_Pull_Request_Review

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_delete_pull_request_review_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Void

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  When authenticated with admin or owner permissions to the repository, you can use this endpoint to require signed commits on a branch. You must enable branch protection to require signed commits.
  """
  repos_create_commit_signature_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Protected_Branch_Admin_Enforced

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  When authenticated with admin or owner permissions to the repository, you can use this endpoint to disable required signed commits on a branch. You must enable branch protection to require signed commits.
  """
  repos_delete_commit_signature_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Void

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Updating required status checks requires admin or owner permissions to the repository and branch protection to be enabled.
  """
  repos_update_status_check_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_update_status_check_protection_request_Input
  ): Status_Check_Policy

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_remove_status_check_protection(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Void

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_add_status_check_contexts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: [repos_add_status_check_contexts_request_Input]
  ): [String]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_set_status_check_contexts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: [repos_set_status_check_contexts_request_Input]
  ): [String]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  """
  repos_remove_status_check_contexts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: [repos_remove_status_check_contexts_request_Input]
  ): [String]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Disables the ability to restrict who can push to this branch.
  """
  repos_delete_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
  ): Void

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Grants the specified apps push access for this branch. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
  """
  repos_add_app_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_add_app_access_restrictions_request_Input
  ): [integration]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Replaces the list of apps that have push access to this branch. This removes all apps that previously had push access and grants push access to the new list of apps. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
  """
  repos_set_app_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_set_app_access_restrictions_request_Input
  ): [integration]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Removes the ability of an app to push to this branch. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
  """
  repos_remove_app_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_remove_app_access_restrictions_request_Input
  ): [integration]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Grants the specified teams push access for this branch. You can also give push access to child teams.
  """
  repos_add_team_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: [repos_add_team_access_restrictions_request_Input]
  ): [team]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Replaces the list of teams that have push access to this branch. This removes all teams that previously had push access and grants push access to the new list of teams. Team restrictions include child teams.
  """
  repos_set_team_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: [repos_set_team_access_restrictions_request_Input]
  ): [team]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Removes the ability of a team to push to this branch. You can also remove push access for child teams.
  """
  repos_remove_team_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: [repos_remove_team_access_restrictions_request_Input]
  ): [team]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Grants the specified people push access for this branch.
  
  | Type    | Description                                                                                                                   |
  | ------- | ----------------------------------------------------------------------------------------------------------------------------- |
  | `array` | Usernames for people who can have push access. **Note**: The list of users, apps, and teams in total is limited to 100 items. |
  """
  repos_add_user_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_add_user_access_restrictions_request_Input
  ): [simple_user]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Replaces the list of people that have push access to this branch. This removes all people that previously had push access and grants push access to the new list of people.
  
  | Type    | Description                                                                                                                   |
  | ------- | ----------------------------------------------------------------------------------------------------------------------------- |
  | `array` | Usernames for people who can have push access. **Note**: The list of users, apps, and teams in total is limited to 100 items. |
  """
  repos_set_user_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_set_user_access_restrictions_request_Input
  ): [simple_user]

  """
  Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  Removes the ability of a user to push to this branch.
  
  | Type    | Description                                                                                                                                   |
  | ------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
  | `array` | Usernames of the people who should no longer have push access. **Note**: The list of users, apps, and teams in total is limited to 100 items. |
  """
  repos_remove_user_access_restrictions(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_remove_user_access_restrictions_request_Input
  ): [simple_user]

  """
  Renames a branch in a repository.
  
  > [!NOTE]
  > Although the API responds immediately, the branch rename process might take some extra time to complete in the background. You won't be able to push to the old branch name while the rename process is in progress. For more information, see "[Renaming a branch](https://docs.github.com/github/administering-a-repository/renaming-a-branch)".
  
  The authenticated user must have push access to the branch. If the branch is the default branch, the authenticated user must also have admin or owner permissions.
  
  In order to rename the default branch, fine-grained access tokens also need the `administration:write` repository permission.
  """
  repos_rename_branch(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).
    """
    branch: String!
    input: repos_rename_branch_request_Input
  ): Branch_With_Protection

  """
  Creates a new check run for a specific commit in a repository.
  
  To create a check run, you must use a GitHub App. OAuth apps and authenticated users are not able to create a check suite.
  
  In a check suite, GitHub limits the number of check runs with the same name to 1000. Once these check runs exceed 1000, GitHub will start to automatically delete older check runs.
  
  > [!NOTE]
  > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array.
  """
  checks_create(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: checks_create_request_Input
  ): check_run

  """
  Updates a check run for a specific commit in a repository.
  
  > [!NOTE]
  > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array.
  
  OAuth apps and personal access tokens (classic) cannot use this endpoint.
  """
  checks_update(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the check run."""
    check_run_id: Int!
    input: checks_update_request_Input
  ): check_run

  """
  Triggers GitHub to rerequest an existing check run, without pushing new code to a repository. This endpoint will trigger the [`check_run` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) event with the action `rerequested`. When a check run is `rerequested`, the `status` of the check suite it belongs to is reset to `queued` and the `conclusion` is cleared. The check run itself is not updated. GitHub apps recieving the [`check_run` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) with the `rerequested` action should then decide if the check run should be reset or updated and call the [update `check_run` endpoint](https://docs.github.com/rest/checks/runs#update-a-check-run) to update the check_run if desired.
  
  For more information about how to re-run GitHub Actions jobs, see "[Re-run a job from a workflow run](https://docs.github.com/rest/actions/workflow-runs#re-run-a-job-from-a-workflow-run)".
  """
  checks_rerequest_run(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the check run."""
    check_run_id: Int!
  ): JSON

  """
  Creates a check suite manually. By default, check suites are automatically created when you create a [check run](https://docs.github.com/rest/checks/runs). You only need to use this endpoint for manually creating check suites when you've disabled automatic creation using "[Update repository preferences for check suites](https://docs.github.com/rest/checks/suites#update-repository-preferences-for-check-suites)".
  
  > [!NOTE]
  > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`.
  
  OAuth apps and personal access tokens (classic) cannot use this endpoint.
  """
  checks_create_suite(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: checks_create_suite_request_Input
  ): CheckSuite

  """
  Changes the default automatic flow when creating check suites. By default, a check suite is automatically created each time code is pushed to a repository. When you disable the automatic creation of check suites, you can manually [Create a check suite](https://docs.github.com/rest/checks/suites#create-a-check-suite).
  You must have admin permissions in the repository to set preferences for check suites.
  """
  checks_set_suites_preferences(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: checks_set_suites_preferences_request_Input
  ): Check_Suite_Preference

  """
  Triggers GitHub to rerequest an existing check suite, without pushing new code to a repository. This endpoint will trigger the [`check_suite` webhook](https://docs.github.com/webhooks/event-payloads/#check_suite) event with the action `rerequested`. When a check suite is `rerequested`, its `status` is reset to `queued` and the `conclusion` is cleared.
  """
  checks_rerequest_suite(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the check suite."""
    check_suite_id: Int!
  ): JSON

  """
  Updates the status of a single code scanning alert.
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_update_alert(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
    input: code_scanning_update_alert_request_Input
  ): code_scanning_alert

  """
  Creates an autofix for a code scanning alert.
  
  If a new autofix is to be created as a result of this request or is currently being generated, then this endpoint will return a 202 Accepted response.
  
  If an autofix already exists for a given alert, then this endpoint will return a 200 OK response.
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_create_autofix(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
  ): code_scanning_autofix

  """
  Commits an autofix for a code scanning alert.
  
  If an autofix is committed as a result of this request, then this endpoint will return a 201 Created response.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_commit_autofix(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
    input: code_scanning_autofix_commits_Input
  ): code_scanning_autofix_commits_response

  """
  Deletes a specified code scanning analysis from a repository.
  
  You can delete one analysis at a time.
  To delete a series of analyses, start with the most recent analysis and work backwards.
  Conceptually, the process is similar to the undo function in a text editor.
  
  When you list the analyses for a repository,
  one or more will be identified as deletable in the response:
  
  ```
  "deletable": true
  ```
  
  An analysis is deletable when it's the most recent in a set of analyses.
  Typically, a repository will have multiple sets of analyses
  for each enabled code scanning tool,
  where a set is determined by a unique combination of analysis values:
  
  * `ref`
  * `tool`
  * `category`
  
  If you attempt to delete an analysis that is not the most recent in a set,
  you'll get a 400 response with the message:
  
  ```
  Analysis specified is not deletable.
  ```
  
  The response from a successful `DELETE` operation provides you with
  two alternative URLs for deleting the next analysis in the set:
  `next_analysis_url` and `confirm_delete_url`.
  Use the `next_analysis_url` URL if you want to avoid accidentally deleting the final analysis
  in a set. This is a useful option if you want to preserve at least one analysis
  for the specified tool in your repository.
  Use the `confirm_delete_url` URL if you are content to remove all analyses for a tool.
  When you delete the last analysis in a set, the value of `next_analysis_url` and `confirm_delete_url`
  in the 200 response is `null`.
  
  As an example of the deletion process,
  let's imagine that you added a workflow that configured a particular code scanning tool
  to analyze the code in a repository. This tool has added 15 analyses:
  10 on the default branch, and another 5 on a topic branch.
  You therefore have two separate sets of analyses for this tool.
  You've now decided that you want to remove all of the analyses for the tool.
  To do this you must make 15 separate deletion requests.
  To start, you must find an analysis that's identified as deletable.
  Each set of analyses always has one that's identified as deletable.
  Having found the deletable analysis for one of the two sets,
  delete this analysis and then continue deleting the next analysis in the set until they're all deleted.
  Then repeat the process for the second set.
  The procedure therefore consists of a nested loop:
  
  **Outer loop**:
  * List the analyses for the repository, filtered by tool.
  * Parse this list to find a deletable analysis. If found:
  
    **Inner loop**:
    * Delete the identified analysis.
    * Parse the response for the value of `confirm_delete_url` and, if found, use this in the next iteration.
  
  The above process assumes that you want to remove all trace of the tool's analyses from the GitHub user interface, for the specified repository, and it therefore uses the `confirm_delete_url` value. Alternatively, you could use the `next_analysis_url` value, which would leave the last analysis in each set undeleted to avoid removing a tool's analysis entirely.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_delete_analysis(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The ID of the analysis, as returned from the `GET /repos/{owner}/{repo}/code-scanning/analyses` operation.
    """
    analysis_id: Int!

    """
    Allow deletion if the specified analysis is the last in a set. If you attempt to delete the final analysis in a set without setting this parameter to `true`, you'll get a 400 response with the message: `Analysis is last of its type and deletion may result in the loss of historical alert data. Please specify confirm_delete.`
    """
    confirm_delete: String
  ): Analysis_deletion

  """
  Deletes a CodeQL database for a language in a repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_delete_codeql_database(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The language of the CodeQL database."""
    language: String!
  ): Void

  """
  Creates a new CodeQL variant analysis, which will run a CodeQL query against one or more repositories.
  
  Get started by learning more about [running CodeQL queries at scale with Multi-Repository Variant Analysis](https://docs.github.com/code-security/codeql-for-vs-code/getting-started-with-codeql-for-vs-code/running-codeql-queries-at-scale-with-multi-repository-variant-analysis).
  
  Use the `owner` and `repo` parameters in the URL to specify the controller repository that
  will be used for running GitHub Actions workflows and storing the results of the CodeQL variant analysis.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  code_scanning_create_variant_analysis(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: code_scanning_create_variant_analysis_request_Input
  ): Variant_Analysis

  """
  Updates a code scanning default setup configuration.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  """
  code_scanning_update_default_setup(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: code_scanning_default_setup_update_Input
  ): code_scanning_update_default_setup_response

  """
  Uploads SARIF data containing the results of a code scanning analysis to make the results available in a repository. For troubleshooting information, see "[Troubleshooting SARIF uploads](https://docs.github.com/code-security/code-scanning/troubleshooting-sarif)."
  
  There are two places where you can upload code scanning results.
   - If you upload to a pull request, for example `--ref refs/pull/42/merge` or `--ref refs/pull/42/head`, then the results appear as alerts in a pull request check. For more information, see "[Triaging code scanning alerts in pull requests](/code-security/secure-coding/triaging-code-scanning-alerts-in-pull-requests)."
   - If you upload to a branch, for example `--ref refs/heads/my-branch`, then the results appear in the **Security** tab for your repository. For more information, see "[Managing code scanning alerts for your repository](/code-security/secure-coding/managing-code-scanning-alerts-for-your-repository#viewing-the-alerts-for-a-repository)."
  
  You must compress the SARIF-formatted analysis data that you want to upload, using `gzip`, and then encode it as a Base64 format string. For example:
  
  ```
  gzip -c analysis-data.sarif | base64 -w0
  ```
  
  SARIF upload supports a maximum number of entries per the following data objects, and an analysis will be rejected if any of these objects is above its maximum value. For some objects, there are additional values over which the entries will be ignored while keeping the most important entries whenever applicable.
  To get the most out of your analysis when it includes data above the supported limits, try to optimize the analysis configuration. For example, for the CodeQL tool, identify and remove the most noisy queries. For more information, see "[SARIF results exceed one or more limits](https://docs.github.com/code-security/code-scanning/troubleshooting-sarif/results-exceed-limit)."
  
  
  | **SARIF data**                   | **Maximum values** | **Additional limits**                                                            |
  |----------------------------------|:------------------:|----------------------------------------------------------------------------------|
  | Runs per file                    |         20         |                                                                                  |
  | Results per run                  |       25,000       | Only the top 5,000 results will be included, prioritized by severity.            |
  | Rules per run                    |       25,000       |                                                                                  |
  | Tool extensions per run          |        100         |                                                                                  |
  | Thread Flow Locations per result |       10,000       | Only the top 1,000 Thread Flow Locations will be included, using prioritization. |
  | Location per result	             |       1,000        | Only 100 locations will be included.                                             |
  | Tags per rule	                   |         20         | Only 10 tags will be included.                                                   |
  
  
  The `202 Accepted` response includes an `id` value.
  You can use this ID to check the status of the upload by using it in the `/sarifs/{sarif_id}` endpoint.
  For more information, see "[Get information about a SARIF upload](/rest/code-scanning/code-scanning#get-information-about-a-sarif-upload)."
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint with private or public repositories, or the `public_repo` scope to use this endpoint with only public repositories.
  
  This endpoint is limited to 1,000 requests per hour for each user or app installation calling it.
  """
  code_scanning_upload_sarif(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: code_scanning_upload_sarif_request_Input
  ): code_scanning_sarifs_receipt

  """
  Creates a codespace owned by the authenticated user in the specified repository.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_create_with_repo_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: codespaces_create_with_repo_for_authenticated_user_request_Input
  ): codespace

  """
  Creates or updates a repository development environment secret with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint. The associated user must be a repository admin.
  """
  codespaces_create_or_update_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
    input: codespaces_create_or_update_repo_secret_request_Input
  ): codespaces_create_or_update_repo_secret_response

  """
  Deletes a development environment secret in a repository using the secret name.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint. The associated user must be a repository admin.
  """
  codespaces_delete_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Add a user to a repository with a specified level of access. If the repository is owned by an organization, this API does not add the user to the organization - a user that has repository access without being an organization member is called an "outside collaborator" (if they are not an Enterprise Managed User) or a "repository collaborator" if they are an Enterprise Managed User. These users are exempt from some organization policies - see "[Adding outside collaborators to repositories](https://docs.github.com/organizations/managing-user-access-to-your-organizations-repositories/managing-outside-collaborators/adding-outside-collaborators-to-repositories-in-your-organization)" to learn more about these collaborator types.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications).
  
  Adding an outside collaborator may be restricted by enterprise and organization administrators. For more information, see "[Enforcing repository management policies in your enterprise](https://docs.github.com/admin/policies/enforcing-policies-for-your-enterprise/enforcing-repository-management-policies-in-your-enterprise#enforcing-a-policy-for-inviting-outside-collaborators-to-repositories)" and "[Setting permissions for adding outside collaborators](https://docs.github.com/organizations/managing-organization-settings/setting-permissions-for-adding-outside-collaborators)" for organization settings.
  
  For more information on permission levels, see "[Repository permission levels for an organization](https://docs.github.com/github/setting-up-and-managing-organizations-and-teams/repository-permission-levels-for-an-organization#permission-levels-for-repositories-owned-by-an-organization)". There are restrictions on which permissions can be granted to organization members when an organization base role is in place. In this case, the role being given must be equal to or higher than the org base permission. Otherwise, the request will fail with:
  
  ```
  Cannot assign {member} permission of {role name}
  ```
  
  Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  
  The invitee will receive a notification that they have been invited to the repository, which they must accept or decline. They may do this via the notifications page, the email they receive, or by using the [API](https://docs.github.com/rest/collaborators/invitations).
  
  For Enterprise Managed Users, this endpoint does not send invitations - these users are automatically added to organizations and repositories. Enterprise Managed Users can only be added to organizations and repositories within their enterprise.
  
  **Updating an existing collaborator's permission level**
  
  The endpoint can also be used to change the permissions of an existing collaborator without first removing and re-adding the collaborator. To change the permissions, use the same endpoint and pass a different `permission` parameter. The response will be a `204`, with no other indication that the permission level changed.
  
  **Rate limits**
  
  You are limited to sending 50 invitations to a repository per 24 hour period. Note there is no limit if you are inviting organization members to an organization repository.
  """
  repos_add_collaborator(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The handle for the GitHub user account."""
    username: String!
    input: repos_add_collaborator_request_Input
  ): repos_add_collaborator_response

  """
  Removes a collaborator from a repository.
  
  To use this endpoint, the authenticated user must either be an administrator of the repository or target themselves for removal.
  
  This endpoint also:
  - Cancels any outstanding invitations sent by the collaborator
  - Unassigns the user from any issues
  - Removes access to organization projects if the user is not an organization member and is not a collaborator on any other organization repositories.
  - Unstars the repository
  - Updates access permissions to packages
  
  Removing a user as a collaborator has the following effects on forks:
   - If the user had access to a fork through their membership to this repository, the user will also be removed from the fork.
   - If the user had their own fork of the repository, the fork will be deleted.
   - If the user still has read access to the repository, open pull requests by this user from a fork will be denied.
  
  > [!NOTE]
  > A user can still have access to the repository through organization permissions like base repository permissions.
  
  Although the API responds immediately, the additional permission updates might take some extra time to complete in the background.
  
  For more information on fork permissions, see "[About permissions and visibility of forks](https://docs.github.com/pull-requests/collaborating-with-pull-requests/working-with-forks/about-permissions-and-visibility-of-forks)".
  """
  repos_remove_collaborator(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Updates the contents of a specified commit comment.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  repos_update_commit_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: repos_update_commit_comment_request_Input
  ): Commit_Comment

  """Delete a commit comment"""
  repos_delete_commit_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): Void

  """
  Create a reaction to a [commit comment](https://docs.github.com/rest/commits/comments#get-a-commit-comment). A response with an HTTP `200` status means that you already added the reaction type to this commit comment.
  """
  reactions_create_for_commit_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: reactions_create_for_commit_comment_request_Input
  ): Reaction

  """
  > [!NOTE]
  > You can also specify a repository by `repository_id` using the route `DELETE /repositories/:repository_id/comments/:comment_id/reactions/:reaction_id`.
  
  Delete a reaction to a [commit comment](https://docs.github.com/rest/commits/comments#get-a-commit-comment).
  """
  reactions_delete_for_commit_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!

    """The unique identifier of the reaction."""
    reaction_id: Int!
  ): Void

  """
  Create a comment for a commit using its `:commit_sha`.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  repos_create_commit_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The SHA of the commit."""
    commit_sha: String!
    input: repos_create_commit_comment_request_Input
  ): Commit_Comment

  """
  Creates a new file or replaces an existing file in a repository.
  
  > [!NOTE]
  > If you use this endpoint and the "[Delete a file](https://docs.github.com/rest/repos/contents/#delete-a-file)" endpoint in parallel, the concurrent requests will conflict and you will receive errors. You must use these endpoints serially instead.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint. The `workflow` scope is also required in order to modify files in the `.github/workflows` directory.
  """
  repos_create_or_update_file_contents(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """path parameter"""
    path: String!
    input: repos_create_or_update_file_contents_request_Input
  ): File_Commit

  """
  Deletes a file in a repository.
  
  You can provide an additional `committer` parameter, which is an object containing information about the committer. Or, you can provide an `author` parameter, which is an object containing information about the author.
  
  The `author` section is optional and is filled in with the `committer` information if omitted. If the `committer` information is omitted, the authenticated user's information is used.
  
  You must provide values for both `name` and `email`, whether you choose to use `author` or `committer`. Otherwise, you'll receive a `422` status code.
  
  > [!NOTE]
  > If you use this endpoint and the "[Create or update file contents](https://docs.github.com/rest/repos/contents/#create-or-update-file-contents)" endpoint in parallel, the concurrent requests will conflict and you will receive errors. You must use these endpoints serially instead.
  """
  repos_delete_file(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """path parameter"""
    path: String!
    input: repos_delete_file_request_Input
  ): File_Commit

  """
  The authenticated user must have access to security alerts for the repository to use this endpoint. For more information, see "[Granting access to security alerts](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-security-and-analysis-settings-for-your-repository#granting-access-to-security-alerts)."
  
  OAuth app tokens and personal access tokens (classic) need the `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  dependabot_update_alert(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
    input: dependabot_update_alert_request_Input
  ): dependabot_alert

  """
  Creates or updates a repository secret with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  dependabot_create_or_update_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
    input: dependabot_create_or_update_repo_secret_request_Input
  ): dependabot_create_or_update_repo_secret_response

  """
  Deletes a secret in a repository using the secret name.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  dependabot_delete_repo_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Create a new snapshot of a repository's dependencies.
  
  The authenticated user must have access to the repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  dependency_graph_create_repository_snapshot(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: snapshot_Input
  ): dependency_graph_create_repository_snapshot_201_response

  """
  Deployments offer a few configurable parameters with certain defaults.
  
  The `ref` parameter can be any named branch, tag, or SHA. At GitHub we often deploy branches and verify them
  before we merge a pull request.
  
  The `environment` parameter allows deployments to be issued to different runtime environments. Teams often have
  multiple environments for verifying their applications, such as `production`, `staging`, and `qa`. This parameter
  makes it easier to track which environments have requested deployments. The default environment is `production`.
  
  The `auto_merge` parameter is used to ensure that the requested ref is not behind the repository's default branch. If
  the ref _is_ behind the default branch for the repository, we will attempt to merge it for you. If the merge succeeds,
  the API will return a successful merge commit. If merge conflicts prevent the merge from succeeding, the API will
  return a failure response.
  
  By default, [commit statuses](https://docs.github.com/rest/commits/statuses) for every submitted context must be in a `success`
  state. The `required_contexts` parameter allows you to specify a subset of contexts that must be `success`, or to
  specify contexts that have not yet been submitted. You are not required to use commit statuses to deploy. If you do
  not require any contexts or create any commit statuses, the deployment will always succeed.
  
  The `payload` parameter is available for any extra information that a deployment system might need. It is a JSON text
  field that will be passed on when a deployment event is dispatched.
  
  The `task` parameter is used by the deployment system to allow different execution paths. In the web world this might
  be `deploy:migrations` to run schema changes on the system. In the compiled world this could be a flag to compile an
  application with debugging enabled.
  
  Merged branch response:
  
  You will see this response when GitHub automatically merges the base branch into the topic branch instead of creating
  a deployment. This auto-merge happens when:
  *   Auto-merge option is enabled in the repository
  *   Topic branch does not include the latest changes on the base branch, which is `master` in the response example
  *   There are no merge conflicts
  
  If there are no new commits in the base branch, a new request to create a deployment should give a successful
  response.
  
  Merge conflict response:
  
  This error happens when the `auto_merge` option is enabled and when the default branch (in this case `master`), can't
  be merged into the branch that's being deployed (in this case `topic-branch`), due to merge conflicts.
  
  Failed commit status checks:
  
  This error happens when the `required_contexts` parameter indicates that one or more contexts need to have a `success`
  status for the commit to be deployed, but one or more of the required contexts do not have a state of `success`.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repo_deployment` scope to use this endpoint.
  """
  repos_create_deployment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_deployment_request_Input
  ): repos_create_deployment_response

  """
  If the repository only has one deployment, you can delete the deployment regardless of its status. If the repository has more than one deployment, you can only delete inactive deployments. This ensures that repositories with multiple deployments will always have an active deployment.
  
  To set a deployment as inactive, you must:
  
  *   Create a new deployment that is active so that the system has a record of the current state, then delete the previously active deployment.
  *   Mark the active deployment as inactive by adding any non-successful deployment status.
  
  For more information, see "[Create a deployment](https://docs.github.com/rest/deployments/deployments/#create-a-deployment)" and "[Create a deployment status](https://docs.github.com/rest/deployments/statuses#create-a-deployment-status)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repo_deployment` scope to use this endpoint.
  """
  repos_delete_deployment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """deployment_id parameter"""
    deployment_id: Int!
  ): Void

  """
  Users with `push` access can create deployment statuses for a given deployment.
  
  OAuth app tokens and personal access tokens (classic) need the `repo_deployment` scope to use this endpoint.
  """
  repos_create_deployment_status(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """deployment_id parameter"""
    deployment_id: Int!
    input: repos_create_deployment_status_request_Input
  ): Deployment_Status

  """
  You can use this endpoint to trigger a webhook event called `repository_dispatch` when you want activity that happens outside of GitHub to trigger a GitHub Actions workflow or GitHub App webhook. You must configure your GitHub Actions workflow or GitHub App to run when the `repository_dispatch` event occurs. For an example `repository_dispatch` webhook payload, see "[RepositoryDispatchEvent](https://docs.github.com/webhooks/event-payloads/#repository_dispatch)."
  
  The `client_payload` parameter is available for any extra information that your workflow might need. This parameter is a JSON payload that will be passed on when the webhook event is dispatched. For example, the `client_payload` can include a message that a user would like to send using a GitHub Actions workflow. Or the `client_payload` can be used as a test to debug your workflow.
  
  This input example shows how you can use the `client_payload` as a test to debug your workflow.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_create_dispatch_event(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_dispatch_event_request_Input
  ): Void

  """
  Create or update an environment with protection rules, such as required reviewers. For more information about environment protection rules, see "[Environments](/actions/reference/environments#environment-protection-rules)."
  
  > [!NOTE]
  > To create or update name patterns that branches must match in order to deploy to this environment, see "[Deployment branch policies](/rest/deployments/branch-policies)."
  
  > [!NOTE]
  > To create or update secrets for an environment, see "[GitHub Actions secrets](/rest/actions/secrets)."
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_create_or_update_environment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
    input: repos_create_or_update_environment_request_Input
  ): Environment

  """
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_delete_an_environment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
  ): Void

  """
  Creates a deployment branch or tag policy for an environment.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_create_deployment_branch_policy(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
    input: Deployment_branch_and_tag_policy_name_pattern_Input
  ): Deployment_branch_policy

  """
  Updates a deployment branch or tag policy for an environment.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_update_deployment_branch_policy(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The unique identifier of the branch policy."""
    branch_policy_id: Int!
    input: Deployment_branch_policy_name_pattern_Input
  ): Deployment_branch_policy

  """
  Deletes a deployment branch or tag policy for an environment.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_delete_deployment_branch_policy(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The unique identifier of the branch policy."""
    branch_policy_id: Int!
  ): Void

  """
  Enable a custom deployment protection rule for an environment.
  
  The authenticated user must have admin or owner permissions to the repository to use this endpoint.
  
  For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app), as well as the [guide to creating custom deployment protection rules](https://docs.github.com/actions/managing-workflow-runs-and-deployments/managing-deployments/creating-custom-deployment-protection-rules).
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_create_deployment_protection_rule(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
    input: repos_create_deployment_protection_rule_request_Input
  ): Deployment_protection_rule

  """
  Disables a custom deployment protection rule for an environment.
  
  The authenticated user must have admin or owner permissions to the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_disable_deployment_protection_rule(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The unique identifier of the protection rule."""
    protection_rule_id: Int!
  ): Void

  """
  Creates or updates an environment secret with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_or_update_environment_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The name of the secret."""
    secret_name: String!
    input: actions_create_or_update_environment_secret_request_Input
  ): actions_create_or_update_environment_secret_response

  """
  Deletes a secret in an environment using the secret name.
  
  Authenticated users must have collaborator access to a repository to create, update, or read secrets.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_environment_secret(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Create an environment variable that you can reference in a GitHub Actions workflow.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_create_environment_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!
    input: actions_create_environment_variable_request_Input
  ): JSON

  """
  Updates an environment variable that you can reference in a GitHub Actions workflow.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_update_environment_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The name of the variable."""
    name: String!
    input: actions_update_environment_variable_request_Input
  ): Void

  """
  Deletes an environment variable using the variable name.
  
  Authenticated users must have collaborator access to a repository to create, update, or read variables.
  
  OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  actions_delete_environment_variable(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.
    """
    environment_name: String!

    """The name of the variable."""
    name: String!
  ): Void

  """
  Create a fork for the authenticated user.
  
  > [!NOTE]
  > Forking a Repository happens asynchronously. You may have to wait a short period of time before you can access the git objects. If this takes longer than 5 minutes, be sure to contact [GitHub Support](https://support.github.com/contact?tags=dotcom-rest-api).
  
  > [!NOTE]
  > Although this endpoint works with GitHub Apps, the GitHub App must be installed on the destination account with access to all repositories and on the source account with access to the source repository.
  """
  repos_create_fork(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_fork_request_Input
  ): Full_Repository

  """Create a blob"""
  git_create_blob(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: git_create_blob_request_Input
  ): Short_Blob

  """
  Creates a new Git [commit object](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects).
  
  **Signature verification object**
  
  The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
  
  | Name | Type | Description |
  | ---- | ---- | ----------- |
  | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
  | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in the table below. |
  | `signature` | `string` | The signature that was extracted from the commit. |
  | `payload` | `string` | The value that was signed. |
  | `verified_at` | `string` | The date the signature was verified by GitHub. |
  
  These are the possible values for `reason` in the `verification` object:
  
  | Value | Description |
  | ----- | ----------- |
  | `expired_key` | The key that made the signature is expired. |
  | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
  | `gpgverify_error` | There was an error communicating with the signature verification service. |
  | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
  | `unsigned` | The object does not include a signature. |
  | `unknown_signature_type` | A non-PGP signature was found in the commit. |
  | `no_user` | No user was associated with the `committer` email address in the commit. |
  | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
  | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
  | `unknown_key` | The key that made the signature has not been registered with any user's account. |
  | `malformed_signature` | There was an error parsing the signature. |
  | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
  | `valid` | None of the above errors applied, so the signature is considered to be verified. |
  """
  git_create_commit(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: git_create_commit_request_Input
  ): Git_Commit

  """
  Creates a reference for your repository. You are unable to create new references for empty repositories, even if the commit SHA-1 hash used exists. Empty repositories are repositories without branches.
  """
  git_create_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: git_create_ref_request_Input
  ): Git_Reference

  """
  Updates the provided reference to point to a new SHA. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
  """
  git_update_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!
    input: git_update_ref_request_Input
  ): Git_Reference

  """Deletes the provided reference."""
  git_delete_ref(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
    """
    ref: String!
  ): Void

  """
  Note that creating a tag object does not create the reference that makes a tag in Git. If you want to create an annotated tag in Git, you have to do this call to create the tag object, and then [create](https://docs.github.com/rest/git/refs#create-a-reference) the `refs/tags/[tag]` reference. If you want to create a lightweight tag, you only have to [create](https://docs.github.com/rest/git/refs#create-a-reference) the tag reference - this call would be unnecessary.
  
  **Signature verification object**
  
  The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
  
  | Name | Type | Description |
  | ---- | ---- | ----------- |
  | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
  | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
  | `signature` | `string` | The signature that was extracted from the commit. |
  | `payload` | `string` | The value that was signed. |
  | `verified_at` | `string` | The date the signature was verified by GitHub. |
  
  These are the possible values for `reason` in the `verification` object:
  
  | Value | Description |
  | ----- | ----------- |
  | `expired_key` | The key that made the signature is expired. |
  | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
  | `gpgverify_error` | There was an error communicating with the signature verification service. |
  | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
  | `unsigned` | The object does not include a signature. |
  | `unknown_signature_type` | A non-PGP signature was found in the commit. |
  | `no_user` | No user was associated with the `committer` email address in the commit. |
  | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
  | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
  | `unknown_key` | The key that made the signature has not been registered with any user's account. |
  | `malformed_signature` | There was an error parsing the signature. |
  | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
  | `valid` | None of the above errors applied, so the signature is considered to be verified. |
  """
  git_create_tag(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: git_create_tag_request_Input
  ): Git_Tag

  """
  The tree creation API accepts nested entries. If you specify both a tree and a nested path modifying that tree, this endpoint will overwrite the contents of the tree with the new path contents, and create a new tree structure.
  
  If you use this endpoint to add, delete, or modify the file contents in a tree, you will need to commit the tree and then update a branch to point to the commit. For more information see "[Create a commit](https://docs.github.com/rest/git/commits#create-a-commit)" and "[Update a reference](https://docs.github.com/rest/git/refs#update-a-reference)."
  
  Returns an error if you try to delete a file that does not exist.
  """
  git_create_tree(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: git_create_tree_request_Input
  ): Git_Tree

  """
  Repositories can have multiple webhooks installed. Each webhook should have a unique `config`. Multiple webhooks can
  share the same `config` as long as those webhooks do not have any `events` that overlap.
  """
  repos_create_webhook(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_webhook_request_Input
  ): Webhook

  """
  Updates a webhook configured in a repository. If you previously had a `secret` set, you must provide the same `secret` or set a new `secret` or the secret will be removed. If you are only updating individual webhook `config` properties, use "[Update a webhook configuration for a repository](/rest/webhooks/repo-config#update-a-webhook-configuration-for-a-repository)."
  """
  repos_update_webhook(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    input: repos_update_webhook_request_Input
  ): Webhook

  """
  Delete a webhook for an organization.
  
  The authenticated user must be a repository owner, or have admin access in the repository, to delete the webhook.
  """
  repos_delete_webhook(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Void

  """
  Updates the webhook configuration for a repository. To update more information about the webhook, including the `active` state and `events`, use "[Update a repository webhook](/rest/webhooks/repos#update-a-repository-webhook)."
  
  OAuth app tokens and personal access tokens (classic) need the `write:repo_hook` or `repo` scope to use this endpoint.
  """
  repos_update_webhook_config_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    input: repos_update_webhook_config_for_repo_request_Input
  ): Webhook_Configuration

  """Redeliver a webhook delivery for a webhook configured in a repository."""
  repos_redeliver_webhook_delivery(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
    delivery_id: Int!
  ): JSON

  """
  This will trigger a [ping event](https://docs.github.com/webhooks/#ping-event) to be sent to the hook.
  """
  repos_ping_webhook(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Void

  """
  This will trigger the hook with the latest push to the current repository if the hook is subscribed to `push` events. If the hook is not subscribed to `push` events, the server will respond with 204 but no test POST will be generated.
  
  > [!NOTE]
  > Previously `/repos/:owner/:repo/hooks/:hook_id/test`
  """
  repos_test_push_webhook(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """
    The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.
    """
    hook_id: Int!
  ): Void

  """
  Enables immutable releases for a repository. The authenticated user must have admin access to the repository.
  """
  repos_enable_immutable_releases(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Disables immutable releases for a repository. The authenticated user must have admin access to the repository.
  """
  repos_disable_immutable_releases(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Start a source import to a GitHub repository using GitHub Importer.
  Importing into a GitHub repository with GitHub Actions enabled is not supported and will
  return a status `422 Unprocessable Entity` response.
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  """
  migrations_start_import(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: migrations_start_import_request_Input
  ): Import @deprecated(reason: "deprecated")

  """
  An import can be updated with credentials or a project choice by passing in the appropriate parameters in this API
  request. If no parameters are provided, the import will be restarted.
  
  Some servers (e.g. TFS servers) can have several projects at a single URL. In those cases the import progress will
  have the status `detection_found_multiple` and the Import Progress response will include a `project_choices` array.
  You can select the project to import by providing one of the objects in the `project_choices` array in the update request.
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  """
  migrations_update_import(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: migrations_update_import_request_Input
  ): Import @deprecated(reason: "deprecated")

  """
  Stop an import for a repository.
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  """
  migrations_cancel_import(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void @deprecated(reason: "deprecated")

  """
  Update an author's identity for the import. Your application can continue updating authors any time before you push
  new commits to the repository.
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  """
  migrations_map_commit_author(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    author_id: Int!
    input: migrations_map_commit_author_request_Input
  ): Porter_Author @deprecated(reason: "deprecated")

  """
  You can import repositories from Subversion, Mercurial, and TFS that include files larger than 100MB. This ability
  is powered by [Git LFS](https://git-lfs.com).
  
  You can learn more about our LFS feature and working with large files [on our help
  site](https://docs.github.com/repositories/working-with-files/managing-large-files).
  
  > [!WARNING]
  > **Endpoint closing down notice:** Due to very low levels of usage and available alternatives, this endpoint is closing down and will no longer be available from 00:00 UTC on April 12, 2024. For more details and alternatives, see the [changelog](https://gh.io/source-imports-api-deprecation).
  """
  migrations_set_lfs_preference(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: migrations_set_lfs_preference_request_Input
  ): Import @deprecated(reason: "deprecated")

  """
  Temporarily restricts interactions to a certain type of GitHub user within the given repository. You must have owner or admin access to set these restrictions. If an interaction limit is set for the user or organization that owns this repository, you will receive a `409 Conflict` response and will not be able to use this endpoint to change the interaction limit for a single repository.
  """
  interactions_set_restrictions_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: Interaction_Restrictions_Input
  ): Interaction_Limits

  """
  Removes all interaction restrictions from the given repository. You must have owner or admin access to remove restrictions. If the interaction limit is set for the user or organization that owns this repository, you will receive a `409 Conflict` response and will not be able to use this endpoint to change the interaction limit for a single repository.
  """
  interactions_remove_restrictions_for_repo(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """Update a repository invitation"""
  repos_update_invitation(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the invitation."""
    invitation_id: Int!
    input: repos_update_invitation_request_Input
  ): repository_invitation

  """Delete a repository invitation"""
  repos_delete_invitation(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the invitation."""
    invitation_id: Int!
  ): Void

  """
  Any user with pull access to a repository can create an issue. If [issues are disabled in the repository](https://docs.github.com/articles/disabling-issues/), the API returns a `410 Gone` status.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_create(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: issues_create_request_Input
  ): issue

  """
  You can use the REST API to update comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_update_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: issues_update_comment_request_Input
  ): issue_comment

  """
  You can use the REST API to delete comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
  """
  issues_delete_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): Void

  """
  You can use the REST API to pin comments on issues.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_pin_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): issue_comment

  """You can use the REST API to unpin comments on issues."""
  issues_unpin_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): Void

  """
  Create a reaction to an [issue comment](https://docs.github.com/rest/issues/comments#get-an-issue-comment). A response with an HTTP `200` status means that you already added the reaction type to this issue comment.
  """
  reactions_create_for_issue_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: reactions_create_for_issue_comment_request_Input
  ): Reaction

  """
  > [!NOTE]
  > You can also specify a repository by `repository_id` using the route `DELETE delete /repositories/:repository_id/issues/comments/:comment_id/reactions/:reaction_id`.
  
  Delete a reaction to an [issue comment](https://docs.github.com/rest/issues/comments#get-an-issue-comment).
  """
  reactions_delete_for_issue_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!

    """The unique identifier of the reaction."""
    reaction_id: Int!
  ): Void

  """
  Issue owners and users with push access or Triage role can edit an issue.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_update(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_update_request_Input
  ): issue

  """
  Adds up to 10 assignees to an issue. Users already assigned to an issue are not replaced.
  """
  issues_add_assignees(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_add_assignees_request_Input
  ): issue

  """Removes one or more assignees from an issue."""
  issues_remove_assignees(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_remove_assignees_request_Input
  ): issue

  """
  You can use the REST API to create comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications).
  Creating content too quickly using this endpoint may result in secondary rate limiting.
  For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_create_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_create_comment_request_Input
  ): issue_comment

  """
  You can use the REST API to add a 'blocked by' relationship to an issue.
  
  Creating content too quickly using this endpoint may result in secondary rate limiting.
  For more information, see [Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)
  and [Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api).
  
  This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
  
  - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_add_blocked_by_dependency(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_add_blocked_by_dependency_request_Input
  ): issue

  """
  You can use the REST API to remove a dependency that an issue is blocked by.
  
  Removing content too quickly using this endpoint may result in secondary rate limiting.
  For more information, see [Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)
  and [Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api).
  
  This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
  - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass a specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_remove_dependency_blocked_by(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """The id of the blocking issue to remove as a dependency"""
    issue_id: Int!
  ): issue

  """Adds labels to an issue."""
  issues_add_labels(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: [issues_add_labels_request_Input]
  ): [label]

  """Removes any previous labels and sets the new labels for an issue."""
  issues_set_labels(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: [issues_set_labels_request_Input]
  ): [label]

  """Removes all labels from an issue."""
  issues_remove_all_labels(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
  ): Void

  """
  Removes the specified label from the issue, and returns the remaining labels on the issue. This endpoint returns a `404 Not Found` status if the label does not exist.
  """
  issues_remove_label(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    name: String!
  ): [label]

  """
  Users with push access can lock an issue or pull request's conversation.
  
  Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  """
  issues_lock(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_lock_request_Input
  ): Void

  """Users with push access can unlock an issue's conversation."""
  issues_unlock(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
  ): Void

  """
  Create a reaction to an [issue](https://docs.github.com/rest/issues/issues#get-an-issue). A response with an HTTP `200` status means that you already added the reaction type to this issue.
  """
  reactions_create_for_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: reactions_create_for_issue_request_Input
  ): Reaction

  """
  > [!NOTE]
  > You can also specify a repository by `repository_id` using the route `DELETE /repositories/:repository_id/issues/:issue_number/reactions/:reaction_id`.
  
  Delete a reaction to an [issue](https://docs.github.com/rest/issues/issues#get-an-issue).
  """
  reactions_delete_for_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!

    """The unique identifier of the reaction."""
    reaction_id: Int!
  ): Void

  """
  You can use the REST API to remove a sub-issue from an issue.
  Removing content too quickly using this endpoint may result in secondary rate limiting.
  For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass a specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_remove_sub_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_remove_sub_issue_request_Input
  ): issue

  """
  You can use the REST API to add sub-issues to issues.
  
  Creating content too quickly using this endpoint may result in secondary rate limiting.
  For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  issues_add_sub_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_add_sub_issue_request_Input
  ): issue

  """
  You can use the REST API to reprioritize a sub-issue to a different position in the parent list.
  """
  issues_reprioritize_sub_issue(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_reprioritize_sub_issue_request_Input
  ): issue

  """You can create a read-only deploy key."""
  repos_create_deploy_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_deploy_key_request_Input
  ): Deploy_Key

  """
  Deploy keys are immutable. If you need to update a key, remove the key and create a new one instead.
  """
  repos_delete_deploy_key(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the key."""
    key_id: Int!
  ): Void

  """
  Creates a label for the specified repository with the given name and color. The name and color parameters are required. The color must be a valid [hexadecimal color code](http://www.color-hex.com/).
  """
  issues_create_label(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: issues_create_label_request_Input
  ): label

  """Updates a label using the given label name."""
  issues_update_label(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    name: String!
    input: issues_update_label_request_Input
  ): label

  """Deletes a label using the given label name."""
  issues_delete_label(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    name: String!
  ): Void

  """
  Sync a branch of a forked repository to keep it up-to-date with the upstream repository.
  """
  repos_merge_upstream(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_merge_upstream_request_Input
  ): Merged_upstream

  """Merge a branch"""
  repos_merge(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_merge_request_Input
  ): repos_merge_response

  """Creates a milestone."""
  issues_create_milestone(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: issues_create_milestone_request_Input
  ): milestone

  """Update a milestone"""
  issues_update_milestone(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the milestone."""
    milestone_number: Int!
    input: issues_update_milestone_request_Input
  ): milestone

  """Deletes a milestone using the given milestone number."""
  issues_delete_milestone(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the milestone."""
    milestone_number: Int!
  ): Void

  """
  Marks all notifications in a repository as "read" for the current user. If the number of notifications is too large to complete in one request, you will receive a `202 Accepted` status and GitHub will run an asynchronous process to mark notifications as "read." To check whether any "unread" notifications remain, you can use the [List repository notifications for the authenticated user](https://docs.github.com/rest/activity/notifications#list-repository-notifications-for-the-authenticated-user) endpoint and pass the query parameter `all=false`.
  """
  activity_mark_repo_notifications_as_read(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: activity_mark_repo_notifications_as_read_request_Input
  ): activity_mark_repo_notifications_as_read_202_response

  """
  Configures a GitHub Pages site. For more information, see "[About GitHub Pages](/github/working-with-github-pages/about-github-pages)."
  
  The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_create_pages_site(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_pages_site_request_Input
  ): page

  """
  Updates information for a GitHub Pages site. For more information, see "[About GitHub Pages](/github/working-with-github-pages/about-github-pages).
  
  The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_update_information_about_pages_site(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_update_information_about_pages_site_request_Input
  ): Void

  """
  Deletes a GitHub Pages site. For more information, see "[About GitHub Pages](/github/working-with-github-pages/about-github-pages).
  
  The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  repos_delete_pages_site(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  You can request that your site be built from the latest revision on the default branch. This has the same effect as pushing a commit to your default branch, but does not require an additional commit. Manually triggering page builds can be helpful when diagnosing build warnings and failures.
  
  Build requests are limited to one concurrent build per repository and one concurrent build per requester. If you request a build while another is still in progress, the second request will be queued until the first completes.
  """
  repos_request_pages_build(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Page_Build_Status

  """
  Create a GitHub Pages deployment for a repository.
  
  The authenticated user must have write permission to the repository.
  """
  repos_create_pages_deployment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_pages_deployment_request_Input
  ): page_deployment

  """
  Cancels a GitHub Pages deployment.
  
  The authenticated user must have write permissions for the GitHub Pages site.
  """
  repos_cancel_pages_deployment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    pages_deployment_id: pages_deployment_id_parameter_Input!
  ): Void

  """
  Enables private vulnerability reporting for a repository. The authenticated user must have admin access to the repository. For more information, see "[Privately reporting a security vulnerability](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)."
  """
  repos_enable_private_vulnerability_reporting(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Disables private vulnerability reporting for a repository. The authenticated user must have admin access to the repository. For more information, see "[Privately reporting a security vulnerability](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)".
  """
  repos_disable_private_vulnerability_reporting(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Create new or update existing custom property values for a repository.
  Using a value of `null` for a custom property will remove or 'unset' the property value from the repository.
  
  Repository admins and other users with the repository-level "edit custom property values" fine-grained permission can use this endpoint.
  """
  repos_custom_properties_for_repos_create_or_update_repository_values(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_custom_properties_for_repos_create_or_update_repository_values_request_Input
  ): Void

  """
  Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  To open or update a pull request in a public repository, you must have write access to the head or the source branch. For organization-owned repositories, you must be a member of the organization that owns the repository to open or update a pull request.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_create(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: pulls_create_request_Input
  ): pull_request2

  """
  Edits the content of a specified review comment.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_update_review_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: pulls_update_review_comment_request_Input
  ): pull_request_review_comment

  """Deletes a review comment."""
  pulls_delete_review_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
  ): Void

  """
  Create a reaction to a [pull request review comment](https://docs.github.com/rest/pulls/comments#get-a-review-comment-for-a-pull-request). A response with an HTTP `200` status means that you already added the reaction type to this pull request review comment.
  """
  reactions_create_for_pull_request_review_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: reactions_create_for_pull_request_review_comment_request_Input
  ): Reaction

  """
  > [!NOTE]
  > You can also specify a repository by `repository_id` using the route `DELETE /repositories/:repository_id/pulls/comments/:comment_id/reactions/:reaction_id.`
  
  Delete a reaction to a [pull request review comment](https://docs.github.com/rest/pulls/comments#get-a-review-comment-for-a-pull-request).
  """
  reactions_delete_for_pull_request_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the comment."""
    comment_id: BigInt!

    """The unique identifier of the reaction."""
    reaction_id: Int!
  ): Void

  """
  Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  To open or update a pull request in a public repository, you must have write access to the head or the source branch. For organization-owned repositories, you must be a member of the organization that owns the repository to open or update a pull request.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_update(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: pulls_update_request_Input
  ): pull_request2

  """
  Creates a codespace owned by the authenticated user for the specified pull request.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_create_with_pr_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: codespaces_create_with_pr_for_authenticated_user_request_Input
  ): codespace

  """
  Creates a review comment on the diff of a specified pull request. To add a regular comment to a pull request timeline, see "[Create an issue comment](https://docs.github.com/rest/issues/comments#create-an-issue-comment)."
  
  If your comment applies to more than one line in the pull request diff, you should use the parameters `line`, `side`, and optionally `start_line` and `start_side` in your request.
  
  The `position` parameter is closing down. If you use `position`, the `line`, `side`, `start_line`, and `start_side` parameters are not required.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_create_review_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: pulls_create_review_comment_request_Input
  ): pull_request_review_comment

  """
  Creates a reply to a review comment for a pull request. For the `comment_id`, provide the ID of the review comment you are replying to. This must be the ID of a _top-level review comment_, not a reply to that comment. Replies to replies are not supported.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_create_reply_for_review_comment(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """The unique identifier of the comment."""
    comment_id: BigInt!
    input: pulls_create_reply_for_review_comment_request_Input
  ): pull_request_review_comment

  """
  Merges a pull request into the base branch.
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  """
  pulls_merge(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: pulls_merge_request_Input
  ): Pull_Request_Merge_Result

  """
  Requests reviews for a pull request from a given set of users and/or teams.
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  """
  pulls_request_reviewers(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: pulls_request_reviewers_request_Input
  ): Pull_Request_Simple

  """
  Removes review requests from a pull request for a given set of users and/or teams.
  """
  pulls_remove_requested_reviewers(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: pulls_remove_requested_reviewers_request_Input
  ): Pull_Request_Simple

  """
  Creates a review on a specified pull request.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  
  Pull request reviews created in the `PENDING` state are not submitted and therefore do not include the `submitted_at` property in the response. To create a pending review for a pull request, leave the `event` parameter blank. For more information about submitting a `PENDING` review, see "[Submit a review for a pull request](https://docs.github.com/rest/pulls/reviews#submit-a-review-for-a-pull-request)."
  
  > [!NOTE]
  > To comment on a specific line in a file, you need to first determine the position of that line in the diff. To see a pull request diff, add the `application/vnd.github.v3.diff` media type to the `Accept` header of a call to the [Get a pull request](https://docs.github.com/rest/pulls/pulls#get-a-pull-request) endpoint.
  
  The `position` value equals the number of lines down from the first "@@" hunk header in the file you want to add a comment. The line just below the "@@" line is position 1, the next line is position 2, and so on. The position in the diff continues to increase through lines of whitespace and additional hunks until the beginning of a new file.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_create_review(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: pulls_create_review_request_Input
  ): Pull_Request_Review

  """
  Updates the contents of a specified review summary comment.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_update_review(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """The unique identifier of the review."""
    review_id: Int!
    input: pulls_update_review_request_Input
  ): Pull_Request_Review

  """
  Deletes a pull request review that has not been submitted. Submitted reviews cannot be deleted.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_delete_pending_review(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """The unique identifier of the review."""
    review_id: Int!
  ): Pull_Request_Review

  """
  Dismisses a specified review on a pull request.
  
  > [!NOTE]
  > To dismiss a pull request review on a [protected branch](https://docs.github.com/rest/branches/branch-protection), you must be a repository administrator or be included in the list of people or teams who can dismiss pull request reviews.
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_dismiss_review(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """The unique identifier of the review."""
    review_id: Int!
    input: pulls_dismiss_review_request_Input
  ): Pull_Request_Review

  """
  Submits a pending review for a pull request. For more information about creating a pending review for a pull request, see "[Create a review for a pull request](https://docs.github.com/rest/pulls/reviews#create-a-review-for-a-pull-request)."
  
  This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  
  - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
  - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
  - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
  - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
  """
  pulls_submit_review(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!

    """The unique identifier of the review."""
    review_id: Int!
    input: pulls_submit_review_request_Input
  ): Pull_Request_Review

  """
  Updates the pull request branch with the latest upstream changes by merging HEAD from the base branch into the pull request branch.
  Note: If making a request on behalf of a GitHub App you must also have permissions to write the contents of the head repository.
  """
  pulls_update_branch(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The number that identifies the pull request."""
    pull_number: Int!
    input: pulls_update_branch_request_Input
  ): pulls_update_branch_202_response

  """
  Users with push access to the repository can create a release.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  """
  repos_create_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_release_request_Input
  ): release

  """Users with push access to the repository can edit a release asset."""
  repos_update_release_asset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the asset."""
    asset_id: Int!
    input: repos_update_release_asset_request_Input
  ): Release_Asset

  """Delete a release asset"""
  repos_delete_release_asset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the asset."""
    asset_id: Int!
  ): Void

  """
  Generate a name and body describing a [release](https://docs.github.com/rest/releases/releases#get-a-release). The body content will be markdown formatted and contain information like the changes since last release and users who contributed. The generated release notes are not saved anywhere. They are intended to be generated and used when creating a new release.
  """
  repos_generate_release_notes(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_generate_release_notes_request_Input
  ): Generated_Release_Notes_Content

  """Users with push access to the repository can edit a release."""
  repos_update_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!
    input: repos_update_release_request_Input
  ): release

  """Users with push access to the repository can delete a release."""
  repos_delete_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!
  ): Void

  """
  This endpoint makes use of a [Hypermedia relation](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#hypermedia) to determine which URL to access. The endpoint you call to upload release assets is specific to your release. Use the `upload_url` returned in
  the response of the [Create a release endpoint](https://docs.github.com/rest/releases/releases#create-a-release) to upload a release asset.
  
  You need to use an HTTP client which supports [SNI](http://en.wikipedia.org/wiki/Server_Name_Indication) to make calls to this endpoint.
  
  Most libraries will set the required `Content-Length` header automatically. Use the required `Content-Type` header to provide the media type of the asset. For a list of media types, see [Media Types](https://www.iana.org/assignments/media-types/media-types.xhtml). For example: 
  
  `application/zip`
  
  GitHub expects the asset data in its raw binary form, rather than JSON. You will send the raw binary content of the asset as the request body. Everything else about the endpoint is the same as the rest of the API. For example,
  you'll still need to pass your authentication to be able to upload an asset.
  
  When an upstream failure occurs, you will receive a `502 Bad Gateway` status. This may leave an empty asset with a state of `starter`. It can be safely deleted.
  
  **Notes:**
  *   GitHub renames asset filenames that have special characters, non-alphanumeric characters, and leading or trailing periods. The "[List release assets](https://docs.github.com/rest/releases/assets#list-release-assets)"
  endpoint lists the renamed filenames. For more information and help, contact [GitHub Support](https://support.github.com/contact?tags=dotcom-rest-api).
  *   To find the `release_id` query the [`GET /repos/{owner}/{repo}/releases/latest` endpoint](https://docs.github.com/rest/releases/releases#get-the-latest-release). 
  *   If you upload an asset with the same filename as another uploaded asset, you'll receive an error and must delete the old file before you can re-upload the new asset.
  """
  repos_upload_release_asset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!
    name: String!
    label: String

    """The raw file data"""
    input: File
  ): Release_Asset

  """
  Create a reaction to a [release](https://docs.github.com/rest/releases/releases#get-a-release). A response with a `Status: 200 OK` means that you already added the reaction type to this release.
  """
  reactions_create_for_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!
    input: reactions_create_for_release_request_Input
  ): Reaction

  """
  > [!NOTE]
  > You can also specify a repository by `repository_id` using the route `DELETE delete /repositories/:repository_id/releases/:release_id/reactions/:reaction_id`.
  
  Delete a reaction to a [release](https://docs.github.com/rest/releases/releases#get-a-release).
  """
  reactions_delete_for_release(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The unique identifier of the release."""
    release_id: Int!

    """The unique identifier of the reaction."""
    reaction_id: Int!
  ): Void

  """Create a ruleset for a repository."""
  repos_create_repo_ruleset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_create_repo_ruleset_request_Input
  ): Repository_ruleset

  """Update a ruleset for a repository."""
  repos_update_repo_ruleset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The ID of the ruleset."""
    ruleset_id: Int!
    input: repos_update_repo_ruleset_request_Input
  ): Repository_ruleset

  """Delete a ruleset for a repository."""
  repos_delete_repo_ruleset(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The ID of the ruleset."""
    ruleset_id: Int!
  ): Void

  """
  Updates the status of a secret scanning alert in an eligible repository.
  
  You can also use this endpoint to assign or unassign an alert to a user who has write access to the repository.
  
  The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
  """
  secret_scanning_update_alert(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The security alert number."""
    alert_number: Int
    input: secret_scanning_update_alert_request_Input
  ): secret_scanning_alert

  """
  Creates a bypass for a previously push protected secret.
  
  The authenticated user must be the original author of the committed secret.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
  """
  secret_scanning_create_push_protection_bypass(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: secret_scanning_create_push_protection_bypass_request_Input
  ): secret_scanning_push_protection_bypass

  """
  Creates a new repository security advisory.
  
  In order to create a draft repository security advisory, the authenticated user must be a security manager or administrator of that repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
  """
  security_advisories_create_repository_advisory(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repository_advisory_create_Input
  ): repository_advisory

  """
  Report a security vulnerability to the maintainers of the repository.
  See "[Privately reporting a security vulnerability](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)" for more information about private vulnerability reporting.
  """
  security_advisories_create_private_vulnerability_report(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: private_vulnerability_report_create_Input
  ): repository_advisory

  """
  Update a repository security advisory using its GitHub Security Advisory (GHSA) identifier.
  
  In order to update any security advisory, the authenticated user must be a security manager or administrator of that repository,
  or a collaborator on the repository security advisory.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
  """
  security_advisories_update_repository_advisory(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The GHSA (GitHub Security Advisory) identifier of the advisory."""
    ghsa_id: String!
    input: repository_advisory_update_Input
  ): repository_advisory

  """
  If you want a CVE identification number for the security vulnerability in your project, and don't already have one, you can request a CVE identification number from GitHub. For more information see "[Requesting a CVE identification number](https://docs.github.com/code-security/security-advisories/repository-security-advisories/publishing-a-repository-security-advisory#requesting-a-cve-identification-number-optional)."
  
  You may request a CVE for public repositories, but cannot do so for private repositories.
  
  In order to request a CVE for a repository security advisory, the authenticated user must be a security manager or administrator of that repository.
  
  OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
  """
  security_advisories_create_repository_advisory_cve_request(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The GHSA (GitHub Security Advisory) identifier of the advisory."""
    ghsa_id: String!
  ): JSON

  """
  Create a temporary private fork to collaborate on fixing a security vulnerability in your repository.
  
  > [!NOTE]
  > Forking a repository happens asynchronously. You may have to wait up to 5 minutes before you can access the fork.
  """
  security_advisories_create_fork(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!

    """The GHSA (GitHub Security Advisory) identifier of the advisory."""
    ghsa_id: String!
  ): Full_Repository

  """
  Users with push access in a repository can create commit statuses for a given SHA.
  
  Note: there is a limit of 1000 statuses per `sha` and `context` within a repository. Attempts to create more than 1000 statuses will result in a validation error.
  """
  repos_create_commit_status(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    sha: String!
    input: repos_create_commit_status_request_Input
  ): Status

  """
  If you would like to watch a repository, set `subscribed` to `true`. If you would like to ignore notifications made within a repository, set `ignored` to `true`. If you would like to stop watching a repository, [delete the repository's subscription](https://docs.github.com/rest/activity/watching#delete-a-repository-subscription) completely.
  """
  activity_set_repo_subscription(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: activity_set_repo_subscription_request_Input
  ): repository_subscription

  """
  This endpoint should only be used to stop watching a repository. To control whether or not you wish to receive notifications from a repository, [set the repository's subscription manually](https://docs.github.com/rest/activity/watching#set-a-repository-subscription).
  """
  activity_delete_repo_subscription(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """Replace all repository topics"""
  repos_replace_all_topics(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_replace_all_topics_request_Input
  ): Topic

  """
  A transfer request will need to be accepted by the new owner when transferring a personal repository to another user. The response will contain the original `owner`, and the transfer will continue asynchronously. For more details on the requirements to transfer personal and organization-owned repositories, see [about repository transfers](https://docs.github.com/articles/about-repository-transfers/).
  """
  repos_transfer(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: repos_transfer_request_Input
  ): minimal_repository

  """
  Enables dependency alerts and the dependency graph for a repository. The authenticated user must have admin access to the repository. For more information, see "[About security alerts for vulnerable dependencies](https://docs.github.com/articles/about-security-alerts-for-vulnerable-dependencies)".
  """
  repos_enable_vulnerability_alerts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Disables dependency alerts and the dependency graph for a repository.
  The authenticated user must have admin access to the repository. For more information,
  see "[About security alerts for vulnerable dependencies](https://docs.github.com/articles/about-security-alerts-for-vulnerable-dependencies)".
  """
  repos_disable_vulnerability_alerts(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Creates a new repository using a repository template. Use the `template_owner` and `template_repo` route parameters to specify the repository to use as the template. If the repository is not public, the authenticated user must own or be a member of an organization that owns the repository. To check if a repository is available to use as a template, get the repository's information using the [Get a repository](https://docs.github.com/rest/repos/repos#get-a-repository) endpoint and check that the `is_template` key is `true`.
  
  OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to create a public repository, and `repo` scope to create a private repository.
  """
  repos_create_using_template(
    """
    The account owner of the template repository. The name is not case sensitive.
    """
    template_owner: String!

    """
    The name of the template repository without the `.git` extension. The name is not case sensitive.
    """
    template_repo: String!
    input: repos_create_using_template_request_Input
  ): Full_Repository

  """
  Add custom field values to an issue. You can set values for organization-level issue fields that have been defined for the repository's organization.
  Adding an empty array will clear all existing field values for the issue.
  
  This endpoint supports the following field data types:
  - **`text`**: String values for text fields
  - **`single_select`**: Option names for single-select fields (must match an existing option name)
  - **`number`**: Numeric values for number fields
  - **`date`**: ISO 8601 date strings for date fields
  
  Only users with push access to the repository can add issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  """
  issues_add_issue_field_values(
    """The unique identifier of the repository."""
    repository_id: Int!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_add_issue_field_values_request_Input
  ): [Issue_Field_Value]

  """
  Set custom field values for an issue, replacing any existing values. You can set values for organization-level issue fields that have been defined for the repository's organization.
  
  This endpoint supports the following field data types:
  - **`text`**: String values for text fields
  - **`single_select`**: Option names for single-select fields (must match an existing option name)
  - **`number`**: Numeric values for number fields
  - **`date`**: ISO 8601 date strings for date fields
  
  This operation will replace all existing field values with the provided ones. If you want to add field values without replacing existing ones, use the `POST` endpoint instead.
  
  Only users with push access to the repository can set issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  """
  issues_set_issue_field_values(
    """The unique identifier of the repository."""
    repository_id: Int!

    """The number that identifies the issue."""
    issue_number: Int!
    input: issues_set_issue_field_values_request_Input
  ): [Issue_Field_Value]

  """
  Remove a specific custom field value from an issue.
  
  Only users with push access to the repository can delete issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
  
  If the specified field does not have a value set on the issue, this operation will return a `404` error.
  
  This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
  and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
  """
  issues_delete_issue_field_value(
    """The unique identifier of the repository."""
    repository_id: Int!

    """The number that identifies the issue."""
    issue_number: Int!

    """The unique identifier of the issue field."""
    issue_field_id: Int!
  ): Void

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Update a team](https://docs.github.com/rest/teams/teams#update-a-team) endpoint.
  
  To edit a team, the authenticated user must either be an organization owner or a team maintainer.
  
  > [!NOTE]
  > With nested teams, the `privacy` for parent teams cannot be `secret`.
  """
  teams_update_legacy(
    """The unique identifier of the team."""
    team_id: Int!
    input: teams_update_legacy_request_Input
  ): Full_Team @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Delete a team](https://docs.github.com/rest/teams/teams#delete-a-team) endpoint.
  
  To delete a team, the authenticated user must be an organization owner or team maintainer.
  
  If you are an organization owner, deleting a parent team will delete all of its child teams as well.
  """
  teams_delete_legacy(
    """The unique identifier of the team."""
    team_id: Int!
  ): Void @deprecated(reason: "deprecated")

  """
  The "Add team member" endpoint (described below) is closing down.
  
  We recommend using the [Add or update team membership for a user](https://docs.github.com/rest/teams/members#add-or-update-team-membership-for-a-user) endpoint instead. It allows you to invite new organization members to your teams.
  
  Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  To add someone to a team, the authenticated user must be an organization owner or a team maintainer in the team they're changing. The person being added to the team must be a member of the team's organization.
  
  > [!NOTE]
  > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
  
  Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  """
  teams_add_member_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The handle for the GitHub user account."""
    username: String!
  ): Void @deprecated(reason: "deprecated")

  """
  The "Remove team member" endpoint (described below) is closing down.
  
  We recommend using the [Remove team membership for a user](https://docs.github.com/rest/teams/members#remove-team-membership-for-a-user) endpoint instead. It allows you to remove both active and pending memberships.
  
  Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  To remove a team member, the authenticated user must have 'admin' permissions to the team or be an owner of the org that the team is associated with. Removing a team member does not delete the user, it just removes them from the team.
  
  > [!NOTE]
  > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
  """
  teams_remove_member_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The handle for the GitHub user account."""
    username: String!
  ): Void @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Add or update team membership for a user](https://docs.github.com/rest/teams/members#add-or-update-team-membership-for-a-user) endpoint.
  
  Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  If the user is already a member of the team's organization, this endpoint will add the user to the team. To add a membership between an organization member and a team, the authenticated user must be an organization owner or a team maintainer.
  
  > [!NOTE]
  > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
  
  If the user is unaffiliated with the team's organization, this endpoint will send an invitation to the user via email. This newly-created membership will be in the "pending" state until the user accepts the invitation, at which point the membership will transition to the "active" state and the user will be added as a member of the team. To add a membership between an unaffiliated user and a team, the authenticated user must be an organization owner.
  
  If the user is already a member of the team, this endpoint will update the role of the team member's role. To update the membership of a team member, the authenticated user must be an organization owner or a team maintainer.
  """
  teams_add_or_update_membership_for_user_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The handle for the GitHub user account."""
    username: String!
    input: teams_add_or_update_membership_for_user_legacy_request_Input
  ): Team_Membership @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Remove team membership for a user](https://docs.github.com/rest/teams/members#remove-team-membership-for-a-user) endpoint.
  
  Team synchronization is available for organizations using GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
  
  To remove a membership between a user and a team, the authenticated user must have 'admin' permissions to the team or be an owner of the organization that the team is associated with. Removing team membership does not delete the user, it just removes their membership from the team.
  
  > [!NOTE]
  > When you have team synchronization set up for a team with your organization's identity provider (IdP), you will see an error if you attempt to use the API for making changes to the team's membership. If you have access to manage group membership in your IdP, you can manage GitHub team membership through your identity provider, which automatically adds and removes team members in an organization. For more information, see "[Synchronizing teams between your identity provider and GitHub](https://docs.github.com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
  """
  teams_remove_membership_for_user_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The handle for the GitHub user account."""
    username: String!
  ): Void @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new "[Add or update team repository permissions](https://docs.github.com/rest/teams/teams#add-or-update-team-repository-permissions)" endpoint.
  
  To add a repository to a team or update the team's permission on a repository, the authenticated user must have admin access to the repository, and must be able to see the team. The repository must be owned by the organization, or a direct fork of a repository owned by the organization. You will get a `422 Unprocessable Entity` status if you attempt to add a repository to a team that is not owned by the organization.
  
  Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  """
  teams_add_or_update_repo_permissions_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
    input: teams_add_or_update_repo_permissions_legacy_request_Input
  ): Void @deprecated(reason: "deprecated")

  """
  > [!WARNING]
  > **Endpoint closing down notice:** This endpoint route is closing down and will be removed from the Teams API. We recommend migrating your existing code to use the new [Remove a repository from a team](https://docs.github.com/rest/teams/teams#remove-a-repository-from-a-team) endpoint.
  
  If the authenticated user is an organization owner or a team maintainer, they can remove any repositories from the team. To remove a repository from a team as an organization member, the authenticated user must have admin access to the repository and must be able to see the team. NOTE: This does not delete the repository, it just removes it from the team.
  """
  teams_remove_repo_legacy(
    """The unique identifier of the team."""
    team_id: Int!

    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void @deprecated(reason: "deprecated")

  """
  **Note:** If your email is set to private and you send an `email` parameter as part of this request to update your profile, your privacy settings are still enforced: the email address will not be displayed on your public profile or via the API.
  """
  users_update_authenticated(input: users_update_authenticated_request_Input): Private_User

  """
  Blocks the given user and returns a 204. If the authenticated user cannot block the given user a 422 is returned.
  """
  users_block(
    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """Unblocks the given user and returns a 204."""
  users_unblock(
    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Creates a new codespace, owned by the authenticated user.
  
  This endpoint requires either a `repository_id` OR a `pull_request` but not both.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_create_for_authenticated_user(input: codespaces_create_for_authenticated_user_request_Input): codespace

  """
  Creates or updates a development environment secret for a user's codespace with an encrypted value. Encrypt your secret using
  [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)."
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_create_or_update_secret_for_authenticated_user(
    """The name of the secret."""
    secret_name: String!
    input: codespaces_create_or_update_secret_for_authenticated_user_request_Input
  ): codespaces_create_or_update_secret_for_authenticated_user_response

  """
  Deletes a development environment secret from a user's codespaces using the secret name. Deleting the secret will remove access from all codespaces that were allowed to access the secret.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_delete_secret_for_authenticated_user(
    """The name of the secret."""
    secret_name: String!
  ): Void

  """
  Select the repositories that will use a user's development environment secret.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_set_repositories_for_secret_for_authenticated_user(
    """The name of the secret."""
    secret_name: String!
    input: codespaces_set_repositories_for_secret_for_authenticated_user_request_Input
  ): Void

  """
  Adds a repository to the selected repositories for a user's development environment secret.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_add_repository_for_secret_for_authenticated_user(
    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  Removes a repository from the selected repositories for a user's development environment secret.
  
  The authenticated user must have Codespaces access to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.
  """
  codespaces_remove_repository_for_secret_for_authenticated_user(
    """The name of the secret."""
    secret_name: String!
    repository_id: Int!
  ): Void

  """
  Updates a codespace owned by the authenticated user. Currently only the codespace's machine type and recent folders can be modified using this endpoint.
  
  If you specify a new machine type it will be applied the next time your codespace is started.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_update_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
    input: codespaces_update_for_authenticated_user_request_Input
  ): codespace

  """
  Deletes a user's codespace.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_delete_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
  ): JSON

  """
  Triggers an export of the specified codespace and returns a URL and ID where the status of the export can be monitored.
  
  If changes cannot be pushed to the codespace's repository, they will be pushed to a new or previously-existing fork instead.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_export_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
  ): Fetches_information_about_an_export_of_a_codespace_

  """
  Publishes an unpublished codespace, creating a new repository and assigning it to the codespace.
  
  The codespace's token is granted write permissions to the repository, allowing the user to push their changes.
  
  This will fail for a codespace that is already published, meaning it has an associated repository.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_publish_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
    input: codespaces_publish_for_authenticated_user_request_Input
  ): codespace_with_full_repository

  """
  Starts a user's codespace.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_start_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
  ): codespace

  """
  Stops a user's codespace.
  
  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  """
  codespaces_stop_for_authenticated_user(
    """The name of the codespace."""
    codespace_name: String!
  ): codespace

  """Sets the visibility for your primary email addresses."""
  users_set_primary_email_visibility_for_authenticated_user(input: users_set_primary_email_visibility_for_authenticated_user_request_Input): [Email]

  """
  OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.
  """
  users_add_email_for_authenticated_user(input: [users_add_email_for_authenticated_user_request_Input]): [Email]

  """
  OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.
  """
  users_delete_email_for_authenticated_user(input: [users_delete_email_for_authenticated_user_request_Input]): Void

  """
  Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP verbs](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  
  OAuth app tokens and personal access tokens (classic) need the `user:follow` scope to use this endpoint.
  """
  users_follow(
    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  OAuth app tokens and personal access tokens (classic) need the `user:follow` scope to use this endpoint.
  """
  users_unfollow(
    """The handle for the GitHub user account."""
    username: String!
  ): Void

  """
  Adds a GPG key to the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `write:gpg_key` scope to use this endpoint.
  """
  users_create_gpg_key_for_authenticated_user(input: users_create_gpg_key_for_authenticated_user_request_Input): GPG_Key

  """
  Removes a GPG key from the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:gpg_key` scope to use this endpoint.
  """
  users_delete_gpg_key_for_authenticated_user(
    """The unique identifier of the GPG key."""
    gpg_key_id: Int!
  ): Void

  """
  Add a single repository to an installation. The authenticated user must have admin access to the repository.    
  
  This endpoint only works for PATs (classic) with the `repo` scope.
  """
  apps_add_repo_to_installation_for_authenticated_user(
    """The unique identifier of the installation."""
    installation_id: Int!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Remove a single repository from an installation. The authenticated user must have admin access to the repository. The installation must have the `repository_selection` of `selected`. 
  
  This endpoint only works for PATs (classic) with the `repo` scope.
  """
  apps_remove_repo_from_installation_for_authenticated_user(
    """The unique identifier of the installation."""
    installation_id: Int!

    """The unique identifier of the repository."""
    repository_id: Int!
  ): Void

  """
  Temporarily restricts which type of GitHub user can interact with your public repositories. Setting the interaction limit at the user level will overwrite any interaction limits that are set for individual repositories owned by the user.
  """
  interactions_set_restrictions_for_authenticated_user(input: Interaction_Restrictions_Input): Interaction_Limits

  """Removes any interaction restrictions from your public repositories."""
  interactions_remove_restrictions_for_authenticated_user: Void

  """
  Adds a public SSH key to the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `write:public_key` scope to use this endpoint.
  """
  users_create_public_ssh_key_for_authenticated_user(input: users_create_public_ssh_key_for_authenticated_user_request_Input): Key

  """
  Removes a public SSH key from the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:public_key` scope to use this endpoint.
  """
  users_delete_public_ssh_key_for_authenticated_user(
    """The unique identifier of the key."""
    key_id: Int!
  ): Void

  """
  Converts the authenticated user to an active member of the organization, if that user has a pending invitation from the organization.
  """
  orgs_update_membership_for_authenticated_user(
    """The organization name. The name is not case sensitive."""
    org: String!
    input: orgs_update_membership_for_authenticated_user_request_Input
  ): Org_Membership

  """Initiates the generation of a user migration archive."""
  migrations_start_for_authenticated_user(input: migrations_start_for_authenticated_user_request_Input): Migration

  """
  Deletes a previous migration archive. Downloadable migration archives are automatically deleted after seven days. Migration metadata, which is returned in the [List user migrations](https://docs.github.com/rest/migrations/users#list-user-migrations) and [Get a user migration status](https://docs.github.com/rest/migrations/users#get-a-user-migration-status) endpoints, will continue to be available even after an archive is deleted.
  """
  migrations_delete_archive_for_authenticated_user(
    """The unique identifier of the migration."""
    migration_id: Int!
  ): Void

  """
  Unlocks a repository. You can lock repositories when you [start a user migration](https://docs.github.com/rest/migrations/users#start-a-user-migration). Once the migration is complete you can unlock each repository to begin using it again or [delete the repository](https://docs.github.com/rest/repos/repos#delete-a-repository) if you no longer need the source data. Returns a status of `404 Not Found` if the repository is not locked.
  """
  migrations_unlock_repo_for_authenticated_user(
    """The unique identifier of the migration."""
    migration_id: Int!

    """repo_name parameter"""
    repo_name: String!
  ): Void

  """
  Deletes a package owned by the authenticated user. You cannot delete a public package if any version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for further assistance.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_delete_package_for_authenticated_user(
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!
  ): Void

  """
  Restores a package owned by the authenticated user.
  
  You can restore a deleted package under the following conditions:
    - The package was deleted within the last 30 days.
    - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_restore_package_for_authenticated_user(
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """package token"""
    token: String
  ): Void

  """
  Deletes a specific package version for a package owned by the authenticated user.  If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance.
  
  The authenticated user must have admin permissions in the organization to use this endpoint.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_delete_package_version_for_authenticated_user(
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Void

  """
  Restores a package version owned by the authenticated user.
  
  You can restore a deleted package version under the following conditions:
    - The package was deleted within the last 30 days.
    - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_restore_package_version_for_authenticated_user(
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Void

  """
  Creates a new repository for the authenticated user.
  
  OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to create a public repository, and `repo` scope to create a private repository.
  """
  repos_create_for_authenticated_user(input: repos_create_for_authenticated_user_request_Input): Full_Repository

  """Accept a repository invitation"""
  repos_accept_invitation_for_authenticated_user(
    """The unique identifier of the invitation."""
    invitation_id: Int!
  ): Void

  """Decline a repository invitation"""
  repos_decline_invitation_for_authenticated_user(
    """The unique identifier of the invitation."""
    invitation_id: Int!
  ): Void

  """
  Add one or more social accounts to the authenticated user's profile.
  
  OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.
  """
  users_add_social_account_for_authenticated_user(input: users_add_social_account_for_authenticated_user_request_Input): [Social_account]

  """
  Deletes one or more social accounts from the authenticated user's profile.
  
  OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint.
  """
  users_delete_social_account_for_authenticated_user(input: users_delete_social_account_for_authenticated_user_request_Input): Void

  """
  Creates an SSH signing key for the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `write:ssh_signing_key` scope to use this endpoint.
  """
  users_create_ssh_signing_key_for_authenticated_user(input: users_create_ssh_signing_key_for_authenticated_user_request_Input): SSH_Signing_Key

  """
  Deletes an SSH signing key from the authenticated user's GitHub account.
  
  OAuth app tokens and personal access tokens (classic) need the `admin:ssh_signing_key` scope to use this endpoint.
  """
  users_delete_ssh_signing_key_for_authenticated_user(
    """The unique identifier of the SSH signing key."""
    ssh_signing_key_id: Int!
  ): Void

  """
  Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  """
  activity_star_repo_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """
  Unstar a repository that the authenticated user has previously starred.
  """
  activity_unstar_repo_for_authenticated_user(
    """The account owner of the repository. The name is not case sensitive."""
    owner: String!

    """
    The name of the repository without the `.git` extension. The name is not case sensitive.
    """
    repo: String!
  ): Void

  """Create draft issue item for the specified user owned project."""
  projects_create_draft_item_for_authenticated_user(
    """The unique identifier of the user."""
    user_id: String!

    """The project's number."""
    project_number: Int!
    input: projects_create_draft_item_for_authenticated_user_request_Input
  ): projects_v2_item_simple

  """
  Create a new view in a user-owned project. Views allow you to customize how items in a project are displayed and filtered.
  """
  projects_create_view_for_user(
    """The unique identifier of the user."""
    user_id: String!

    """The project's number."""
    project_number: Int!
    input: projects_create_view_for_user_request_Input
  ): Projects_v2_View

  """
  List a collection of artifact attestations associated with any entry in a list of subject digests owned by a user.
  
  The collection of attestations returned by this endpoint is filtered according to the authenticated user's permissions; if the authenticated user cannot read a repository, the attestations associated with that repository will not be included in the response. In addition, when using a fine-grained access token the `attestations:read` permission is required.
  
  **Please note:** in order to offer meaningful security benefits, an attestation's signature and timestamps **must** be cryptographically verified, and the identity of the attestation signer **must** be validated. Attestations can be verified using the [GitHub CLI `attestation verify` command](https://cli.github.com/manual/gh_attestation_verify). For more information, see [our guide on how to use artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
  """
  users_list_attestations_bulk(
    """The handle for the GitHub user account."""
    username: String!

    """
    The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    per_page: Int = 30

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    before: String

    """
    A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)."
    """
    after: String
    input: users_list_attestations_bulk_request_Input
  ): users_list_attestations_bulk_200_response

  """
  Delete artifact attestations in bulk by either subject digests or unique ID.
  """
  users_delete_attestations_bulk(
    """The handle for the GitHub user account."""
    username: String!
    input: users_delete_attestations_bulk_request_Input
  ): JSON

  """Delete an artifact attestation by subject digest."""
  users_delete_attestations_by_subject_digest(
    """The handle for the GitHub user account."""
    username: String!

    """Subject Digest"""
    subject_digest: String!
  ): Void

  """
  Delete an artifact attestation by unique ID that is associated with a repository owned by a user.
  """
  users_delete_attestations_by_id(
    """The handle for the GitHub user account."""
    username: String!

    """Attestation ID"""
    attestation_id: Int!
  ): Void

  """
  Deletes an entire package for a user. You cannot delete a public package if any version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for further assistance.
  
  If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_delete_package_for_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!
  ): Void

  """
  Restores an entire package for a user.
  
  You can restore a deleted package under the following conditions:
    - The package was deleted within the last 30 days.
    - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
  
  If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_restore_package_for_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """package token"""
    token: String
  ): Void

  """
  Deletes a specific package version for a user. If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance.
  
  If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_delete_package_version_for_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Void

  """
  Restores a specific package version for a user.
  
  You can restore a deleted package under the following conditions:
    - The package was deleted within the last 30 days.
    - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
  
  If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
  
  OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
  """
  packages_restore_package_version_for_user(
    """The handle for the GitHub user account."""
    username: String!
    package_type: package_type_parameter!

    """The name of the package."""
    package_name: String!

    """Unique identifier of the package version."""
    package_version_id: Int!
  ): Void

  """Add a field to a specified user owned project."""
  projects_add_field_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!
    input: projects_add_field_for_user_request_Input
  ): Projects_v2_Field

  """Add an issue or pull request item to the specified user owned project."""
  projects_add_item_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!
    input: projects_add_item_for_user_request_Input
  ): projects_v2_item_simple

  """Update a specific item in a user-owned project."""
  projects_update_item_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the project item."""
    item_id: Int!
    input: projects_update_item_for_user_request_Input
  ): projects_v2_item_with_content

  """Delete a specific item from a user-owned project."""
  projects_delete_item_for_user(
    """The handle for the GitHub user account."""
    username: String!

    """The project's number."""
    project_number: Int!

    """The unique identifier of the project item."""
    item_id: Int!
  ): Void
}

type apps_create_from_manifest_201_response {
  """Unique identifier of the GitHub app"""
  id: Int!

  """The slug name of the GitHub app"""
  slug: String
  node_id: String!
  client_id: String!
  owner: query_apps_get_authenticated_owner!

  """The name of the GitHub app"""
  name: String!
  description: String
  external_url: URL!
  html_url: URL!
  created_at: DateTime!
  updated_at: DateTime!
  permissions: query_apps_get_authenticated_permissions!

  """
  The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation.
  """
  events: [String]!

  """
  The number of installations associated with the GitHub app. Only returned when the integration is requesting details about itself.
  """
  installations_count: Int
  client_secret: String!
  webhook_secret: String
  pem: String!
  additionalProperties: JSON
}

input apps_update_webhook_config_for_app_request_Input {
  """The URL to which the payloads will be delivered."""
  url: URL

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl_Input
}

input webhook_config_insecure_ssl_Input {
  String: String
  Float: Float
}

"""Authentication token for a GitHub App installed on a user or org."""
type Installation_Token {
  token: String!
  expires_at: String!
  permissions: App_Permissions
  repository_selection: mutation_apps_create_installation_access_token_repository_selection
  repositories: [repository]
  single_file: String
  has_multiple_single_files: Boolean
  single_file_paths: [String]
}

enum mutation_apps_create_installation_access_token_repository_selection {
  all
  selected
}

input apps_create_installation_access_token_request_Input {
  """List of repository names that the token should have access to"""
  repositories: [String]

  """List of repository IDs that the token should have access to"""
  repository_ids: [Int]
  permissions: App_Permissions_Input
}

"""The permissions granted to the user access token."""
input App_Permissions_Input {
  actions: query_apps_list_installations_items_permissions_actions
  administration: query_apps_list_installations_items_permissions_administration
  artifact_metadata: query_apps_list_installations_items_permissions_artifact_metadata
  attestations: query_apps_list_installations_items_permissions_attestations
  checks: query_apps_list_installations_items_permissions_checks
  codespaces: query_apps_list_installations_items_permissions_codespaces
  contents: query_apps_list_installations_items_permissions_contents
  dependabot_secrets: query_apps_list_installations_items_permissions_dependabot_secrets
  deployments: query_apps_list_installations_items_permissions_deployments
  discussions: query_apps_list_installations_items_permissions_discussions
  environments: query_apps_list_installations_items_permissions_environments
  issues: query_apps_list_installations_items_permissions_issues
  merge_queues: query_apps_list_installations_items_permissions_merge_queues
  metadata: query_apps_list_installations_items_permissions_metadata
  packages: query_apps_list_installations_items_permissions_packages
  pages: query_apps_list_installations_items_permissions_pages
  pull_requests: query_apps_list_installations_items_permissions_pull_requests
  repository_custom_properties: query_apps_list_installations_items_permissions_repository_custom_properties
  repository_hooks: query_apps_list_installations_items_permissions_repository_hooks
  repository_projects: query_apps_list_installations_items_permissions_repository_projects
  secret_scanning_alerts: query_apps_list_installations_items_permissions_secret_scanning_alerts
  secrets: query_apps_list_installations_items_permissions_secrets
  security_events: query_apps_list_installations_items_permissions_security_events
  single_file: query_apps_list_installations_items_permissions_single_file
  statuses: query_apps_list_installations_items_permissions_statuses
  vulnerability_alerts: query_apps_list_installations_items_permissions_vulnerability_alerts
  workflows: write_const
  custom_properties_for_organizations: query_apps_list_installations_items_permissions_custom_properties_for_organizations
  members: query_apps_list_installations_items_permissions_members
  organization_administration: query_apps_list_installations_items_permissions_organization_administration
  organization_custom_roles: query_apps_list_installations_items_permissions_organization_custom_roles
  organization_custom_org_roles: query_apps_list_installations_items_permissions_organization_custom_org_roles
  organization_custom_properties: query_apps_list_installations_items_permissions_organization_custom_properties
  organization_copilot_seat_management: write_const
  organization_copilot_agent_settings: query_apps_list_installations_items_permissions_organization_copilot_agent_settings
  organization_announcement_banners: query_apps_list_installations_items_permissions_organization_announcement_banners
  organization_events: read_const
  organization_hooks: query_apps_list_installations_items_permissions_organization_hooks
  organization_personal_access_tokens: query_apps_list_installations_items_permissions_organization_personal_access_tokens
  organization_personal_access_token_requests: query_apps_list_installations_items_permissions_organization_personal_access_token_requests
  organization_plan: read_const
  organization_projects: query_apps_list_installations_items_permissions_organization_projects
  organization_packages: query_apps_list_installations_items_permissions_organization_packages
  organization_secrets: query_apps_list_installations_items_permissions_organization_secrets
  organization_self_hosted_runners: query_apps_list_installations_items_permissions_organization_self_hosted_runners
  organization_user_blocking: query_apps_list_installations_items_permissions_organization_user_blocking
  email_addresses: query_apps_list_installations_items_permissions_email_addresses
  followers: query_apps_list_installations_items_permissions_followers
  git_ssh_keys: query_apps_list_installations_items_permissions_git_ssh_keys
  gpg_keys: query_apps_list_installations_items_permissions_gpg_keys
  interaction_limits: query_apps_list_installations_items_permissions_interaction_limits
  profile: write_const
  starring: query_apps_list_installations_items_permissions_starring
  enterprise_custom_properties_for_organizations: query_apps_list_installations_items_permissions_enterprise_custom_properties_for_organizations
}

input apps_delete_authorization_request_Input {
  """The OAuth access token used to authenticate to the GitHub API."""
  access_token: String!
}

"""
The authorization for an OAuth app, GitHub App, or a Personal Access Token.
"""
type Authorization {
  id: BigInt!
  url: URL!

  """A list of scopes that this authorization is in."""
  scopes: [String]
  token: String!
  token_last_eight: String
  hashed_token: String
  app: mutation_apps_check_token_app!
  note: String
  note_url: URL
  updated_at: DateTime!
  created_at: DateTime!
  fingerprint: String
  user: nullable_simple_user
  installation: Scoped_Installation
  expires_at: DateTime
}

type mutation_apps_check_token_app {
  client_id: String!
  name: String!
  url: URL!
}

type Scoped_Installation {
  permissions: App_Permissions!
  repository_selection: mutation_apps_check_token_installation_repository_selection!
  single_file_name: String
  has_multiple_single_files: Boolean
  single_file_paths: [String]
  repositories_url: URL!
  account: simple_user!
}

"""
Describe whether all repositories have been selected or there's a selection involved
"""
enum mutation_apps_check_token_installation_repository_selection {
  all
  selected
}

input apps_check_token_request_Input {
  """The access_token of the OAuth or GitHub application."""
  access_token: String!
}

input apps_reset_token_request_Input {
  """The access_token of the OAuth or GitHub application."""
  access_token: String!
}

input apps_delete_token_request_Input {
  """The OAuth access token used to authenticate to the GitHub API."""
  access_token: String!
}

input apps_scope_token_request_Input {
  """The access token used to authenticate to the GitHub API."""
  access_token: String!

  """
  The name of the user or organization to scope the user access token to. **Required** unless `target_id` is specified.
  """
  target: String

  """
  The ID of the user or organization to scope the user access token to. **Required** unless `target` is specified.
  """
  target_id: Int

  """
  The list of repository names to scope the user access token to. `repositories` may not be specified if `repository_ids` is specified.
  """
  repositories: [String]

  """
  The list of repository IDs to scope the user access token to. `repository_ids` may not be specified if `repositories` is specified.
  """
  repository_ids: [Int]
  permissions: App_Permissions_Input
}

input credentials_revoke_request_Input {
  """A list of credentials to be revoked, up to 1000 per request."""
  credentials: [String]!
}

"""GitHub Actions cache retention policy for an enterprise."""
input Actions_cache_retention_limit_for_an_enterprise_Input {
  """
  For repositories & organizations in an enterprise, the maximum duration, in days, for which caches in a repository may be retained.
  """
  max_cache_retention_days: Int
}

"""GitHub Actions cache storage policy for an enterprise."""
input Actions_cache_storage_limit_for_an_enterprise_Input {
  """
  For repositories & organizations in an enterprise, the maximum size limit for the sum of all caches in a repository, in gigabytes.
  """
  max_cache_size_gb: Int
}

"""Input for creating an OIDC custom property inclusion"""
input Actions_OIDC_Custom_Property_Inclusion_Input_Input {
  """The name of the custom property to include in the OIDC token"""
  custom_property_name: String!
}

input code_security_create_configuration_for_enterprise_request_Input {
  """
  The name of the code security configuration. Must be unique within the enterprise.
  """
  name: String!

  """A description of the code security configuration"""
  description: mutationInput_code_security_create_configuration_for_enterprise_input_description!
  advanced_security: mutationInput_code_security_create_configuration_for_enterprise_input_advanced_security = disabled
  code_security: mutationInput_code_security_create_configuration_for_enterprise_input_code_security
  dependency_graph: mutationInput_code_security_create_configuration_for_enterprise_input_dependency_graph = enabled
  dependency_graph_autosubmit_action: mutationInput_code_security_create_configuration_for_enterprise_input_dependency_graph_autosubmit_action = disabled
  dependency_graph_autosubmit_action_options: mutationInput_code_security_create_configuration_for_enterprise_input_dependency_graph_autosubmit_action_options_Input
  dependabot_alerts: mutationInput_code_security_create_configuration_for_enterprise_input_dependabot_alerts = disabled
  dependabot_security_updates: mutationInput_code_security_create_configuration_for_enterprise_input_dependabot_security_updates = disabled
  code_scanning_options: code_scanning_options_Input
  code_scanning_default_setup: mutationInput_code_security_create_configuration_for_enterprise_input_code_scanning_default_setup = disabled
  code_scanning_default_setup_options: code_scanning_default_setup_options_Input
  code_scanning_delegated_alert_dismissal: mutationInput_code_security_create_configuration_for_enterprise_input_code_scanning_delegated_alert_dismissal = disabled
  secret_protection: mutationInput_code_security_create_configuration_for_enterprise_input_secret_protection
  secret_scanning: mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning = disabled
  secret_scanning_push_protection: mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_push_protection = disabled
  secret_scanning_validity_checks: mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_validity_checks = disabled
  secret_scanning_non_provider_patterns: mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_non_provider_patterns = disabled
  secret_scanning_generic_secrets: mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_generic_secrets = disabled
  secret_scanning_delegated_alert_dismissal: mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_delegated_alert_dismissal = disabled
  secret_scanning_extended_metadata: mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_extended_metadata = disabled
  private_vulnerability_reporting: mutationInput_code_security_create_configuration_for_enterprise_input_private_vulnerability_reporting = disabled
  enforcement: mutationInput_code_security_create_configuration_for_enterprise_input_enforcement = enforced
}

"""A description of the code security configuration"""
scalar mutationInput_code_security_create_configuration_for_enterprise_input_description

"""
The enablement status of GitHub Advanced Security features. `enabled` will enable both Code Security and Secret Protection features.

> [!WARNING]
> `code_security` and `secret_protection` are deprecated values for this field. Prefer the individual `code_security` and `secret_protection` fields to set the status of these features.
"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_advanced_security {
  enabled
  disabled
  code_security
  secret_protection
}

"""The enablement status of GitHub Code Security features."""
enum mutationInput_code_security_create_configuration_for_enterprise_input_code_security {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependency Graph"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_dependency_graph {
  enabled
  disabled
  not_set
}

"""The enablement status of Automatic dependency submission"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_dependency_graph_autosubmit_action {
  enabled
  disabled
  not_set
}

"""Feature options for Automatic dependency submission"""
input mutationInput_code_security_create_configuration_for_enterprise_input_dependency_graph_autosubmit_action_options_Input {
  """
  Whether to use runners labeled with 'dependency-submission' or standard GitHub runners.
  """
  labeled_runners: Boolean
}

"""The enablement status of Dependabot alerts"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_dependabot_alerts {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependabot security updates"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_dependabot_security_updates {
  enabled
  disabled
  not_set
}

"""Security Configuration feature options for code scanning"""
input code_scanning_options_Input {
  """Whether to allow repos which use advanced setup"""
  allow_advanced: Boolean
}

"""The enablement status of code scanning default setup"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_code_scanning_default_setup {
  enabled
  disabled
  not_set
}

"""Feature options for code scanning default setup"""
input code_scanning_default_setup_options_Input {
  runner_type: mutationInput_code_security_create_configuration_for_enterprise_input_code_scanning_default_setup_options_runner_type

  """
  The label of the runner to use for code scanning default setup when runner_type is 'labeled'.
  """
  runner_label: String
}

"""Whether to use labeled runners or standard GitHub runners."""
enum mutationInput_code_security_create_configuration_for_enterprise_input_code_scanning_default_setup_options_runner_type {
  standard
  labeled
  not_set
}

"""The enablement status of code scanning delegated alert dismissal"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_code_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of GitHub Secret Protection features."""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning push protection"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_push_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning validity checks"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_validity_checks {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning non provider patterns"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_non_provider_patterns {
  enabled
  disabled
  not_set
}

"""The enablement status of Copilot secret scanning"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_generic_secrets {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated alert dismissal"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning extended metadata"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_secret_scanning_extended_metadata {
  enabled
  disabled
  not_set
}

"""The enablement status of private vulnerability reporting"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_private_vulnerability_reporting {
  enabled
  disabled
  not_set
}

"""The enforcement status for a security configuration"""
enum mutationInput_code_security_create_configuration_for_enterprise_input_enforcement {
  enforced
  unenforced
}

input code_security_update_enterprise_configuration_request_Input {
  """
  The name of the code security configuration. Must be unique across the enterprise.
  """
  name: String

  """A description of the code security configuration"""
  description: mutationInput_code_security_update_enterprise_configuration_input_description
  advanced_security: mutationInput_code_security_update_enterprise_configuration_input_advanced_security
  code_security: mutationInput_code_security_update_enterprise_configuration_input_code_security
  dependency_graph: mutationInput_code_security_update_enterprise_configuration_input_dependency_graph
  dependency_graph_autosubmit_action: mutationInput_code_security_update_enterprise_configuration_input_dependency_graph_autosubmit_action
  dependency_graph_autosubmit_action_options: mutationInput_code_security_update_enterprise_configuration_input_dependency_graph_autosubmit_action_options_Input
  dependabot_alerts: mutationInput_code_security_update_enterprise_configuration_input_dependabot_alerts
  dependabot_security_updates: mutationInput_code_security_update_enterprise_configuration_input_dependabot_security_updates
  code_scanning_default_setup: mutationInput_code_security_update_enterprise_configuration_input_code_scanning_default_setup
  code_scanning_default_setup_options: code_scanning_default_setup_options_Input
  code_scanning_options: code_scanning_options_Input
  code_scanning_delegated_alert_dismissal: mutationInput_code_security_update_enterprise_configuration_input_code_scanning_delegated_alert_dismissal = disabled
  secret_protection: mutationInput_code_security_update_enterprise_configuration_input_secret_protection
  secret_scanning: mutationInput_code_security_update_enterprise_configuration_input_secret_scanning
  secret_scanning_push_protection: mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_push_protection
  secret_scanning_validity_checks: mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_validity_checks
  secret_scanning_non_provider_patterns: mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_non_provider_patterns
  secret_scanning_generic_secrets: mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_generic_secrets = disabled
  secret_scanning_delegated_alert_dismissal: mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_delegated_alert_dismissal = disabled
  secret_scanning_extended_metadata: mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_extended_metadata = disabled
  private_vulnerability_reporting: mutationInput_code_security_update_enterprise_configuration_input_private_vulnerability_reporting
  enforcement: mutationInput_code_security_update_enterprise_configuration_input_enforcement
}

"""A description of the code security configuration"""
scalar mutationInput_code_security_update_enterprise_configuration_input_description

"""
The enablement status of GitHub Advanced Security features. `enabled` will enable both Code Security and Secret Protection features.

> [!WARNING]
> `code_security` and `secret_protection` are deprecated values for this field. Prefer the individual `code_security` and `secret_protection` fields to set the status of these features.
"""
enum mutationInput_code_security_update_enterprise_configuration_input_advanced_security {
  enabled
  disabled
  code_security
  secret_protection
}

"""The enablement status of GitHub Code Security features."""
enum mutationInput_code_security_update_enterprise_configuration_input_code_security {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependency Graph"""
enum mutationInput_code_security_update_enterprise_configuration_input_dependency_graph {
  enabled
  disabled
  not_set
}

"""The enablement status of Automatic dependency submission"""
enum mutationInput_code_security_update_enterprise_configuration_input_dependency_graph_autosubmit_action {
  enabled
  disabled
  not_set
}

"""Feature options for Automatic dependency submission"""
input mutationInput_code_security_update_enterprise_configuration_input_dependency_graph_autosubmit_action_options_Input {
  """
  Whether to use runners labeled with 'dependency-submission' or standard GitHub runners.
  """
  labeled_runners: Boolean
}

"""The enablement status of Dependabot alerts"""
enum mutationInput_code_security_update_enterprise_configuration_input_dependabot_alerts {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependabot security updates"""
enum mutationInput_code_security_update_enterprise_configuration_input_dependabot_security_updates {
  enabled
  disabled
  not_set
}

"""The enablement status of code scanning default setup"""
enum mutationInput_code_security_update_enterprise_configuration_input_code_scanning_default_setup {
  enabled
  disabled
  not_set
}

"""The enablement status of code scanning delegated alert dismissal"""
enum mutationInput_code_security_update_enterprise_configuration_input_code_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of GitHub Secret Protection features."""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning"""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_scanning {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning push protection"""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_push_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning validity checks"""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_validity_checks {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning non-provider patterns"""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_non_provider_patterns {
  enabled
  disabled
  not_set
}

"""The enablement status of Copilot secret scanning"""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_generic_secrets {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated alert dismissal"""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning extended metadata"""
enum mutationInput_code_security_update_enterprise_configuration_input_secret_scanning_extended_metadata {
  enabled
  disabled
  not_set
}

"""The enablement status of private vulnerability reporting"""
enum mutationInput_code_security_update_enterprise_configuration_input_private_vulnerability_reporting {
  enabled
  disabled
  not_set
}

"""The enforcement status for a security configuration"""
enum mutationInput_code_security_update_enterprise_configuration_input_enforcement {
  enforced
  unenforced
}

input code_security_attach_enterprise_configuration_request_Input {
  scope: mutationInput_code_security_attach_enterprise_configuration_input_scope!
}

"""The type of repositories to attach the configuration to."""
enum mutationInput_code_security_attach_enterprise_configuration_input_scope {
  all
  all_without_configurations
}

type code_security_set_configuration_as_default_for_enterprise_200_response {
  default_for_new_repos: mutation_code_security_set_configuration_as_default_for_enterprise_default_for_new_repos
  configuration: code_security_configuration
}

"""
Specifies which types of repository this security configuration is applied to by default.
"""
enum mutation_code_security_set_configuration_as_default_for_enterprise_default_for_new_repos {
  all
  none
  private_and_internal
  public
}

input code_security_set_configuration_as_default_for_enterprise_request_Input {
  default_for_new_repos: mutationInput_code_security_set_configuration_as_default_for_enterprise_input_default_for_new_repos
}

"""
Specify which types of repository this security configuration should be applied to by default.
"""
enum mutationInput_code_security_set_configuration_as_default_for_enterprise_input_default_for_new_repos {
  all
  none
  private_and_internal
  public
}

input enterprise_teams_create_request_Input {
  """The name of the team."""
  name: String!

  """A description of the team."""
  description: String
  sync_to_organizations: mutationInput_enterprise_teams_create_input_sync_to_organizations = disabled
  organization_selection_type: mutationInput_enterprise_teams_create_input_organization_selection_type = disabled

  """
  The ID of the IdP group to assign team membership with. You can get this value from the [REST API endpoints for SCIM](https://docs.github.com/rest/scim#list-provisioned-scim-groups-for-an-enterprise).
  """
  group_id: String
}

"""
Retired: this field is no longer supported.
Whether the enterprise team should be reflected in each organization.
This value cannot be set.
"""
enum mutationInput_enterprise_teams_create_input_sync_to_organizations {
  all
  disabled
}

"""
Specifies which organizations in the enterprise should have access to this team. Can be one of `disabled`, `selected`, or `all`.
`disabled`: The team is not assigned to any organizations. This is the default when you create a new team.
`selected`: The team is assigned to specific organizations. You can then use the [add organization assignments API](https://docs.github.com/rest/enterprise-teams/enterprise-team-organizations#add-organization-assignments) endpoint.
`all`: The team is assigned to all current and future organizations in the enterprise.
"""
enum mutationInput_enterprise_teams_create_input_organization_selection_type {
  disabled
  selected
  all
}

input enterprise_team_memberships_bulk_add_request_Input {
  """The GitHub user handles to add to the team."""
  usernames: [String]!
}

input enterprise_team_memberships_bulk_remove_request_Input {
  """The GitHub user handles to be removed from the team."""
  usernames: [String]!
}

input enterprise_team_organizations_bulk_add_request_Input {
  """Organization slug to assign the team to."""
  organization_slugs: [String]!
}

input enterprise_team_organizations_bulk_remove_request_Input {
  """Organization slug to unassign the team from."""
  organization_slugs: [String]!
}

input enterprise_teams_update_request_Input {
  """A new name for the team."""
  name: String

  """A new description for the team."""
  description: String
  sync_to_organizations: mutationInput_enterprise_teams_update_input_sync_to_organizations = disabled
  organization_selection_type: mutationInput_enterprise_teams_update_input_organization_selection_type = disabled

  """
  The ID of the IdP group to assign team membership with. The new IdP group will replace the existing one, or replace existing direct members if the team isn't currently linked to an IdP group.
  """
  group_id: String
}

"""
Retired: this field is no longer supported.
Whether the enterprise team should be reflected in each organization.
This value cannot be changed.
"""
enum mutationInput_enterprise_teams_update_input_sync_to_organizations {
  all
  disabled
}

"""
Specifies which organizations in the enterprise should have access to this team. Can be one of `disabled`, `selected`, or `all`.
`disabled`: The team is not assigned to any organizations. This is the default when you create a new team.
`selected`: The team is assigned to specific organizations. You can then use the [add organization assignments API](https://docs.github.com/rest/enterprise-teams/enterprise-team-organizations#add-organization-assignments).
`all`: The team is assigned to all current and future organizations in the enterprise.
"""
enum mutationInput_enterprise_teams_update_input_organization_selection_type {
  disabled
  selected
  all
}

input gists_create_request_Input {
  """Description of the gist"""
  description: String
  files: mutationInput_gists_create_input_files_Input!
  public: mutationInput_gists_create_input_public_Input
}

"""Names and content for the files that make up the gist"""
input mutationInput_gists_create_input_files_Input {
  hello_rb: mutationInput_gists_create_input_files_hello_rb_Input
}

input mutationInput_gists_create_input_files_hello_rb_Input {
  content: String
}

input mutationInput_gists_create_input_public_Input {
  Boolean: Boolean
  mutationInput_gists_create_input_public_oneOf_1: mutationInput_gists_create_input_public_oneOf_1
}

enum mutationInput_gists_create_input_public_oneOf_1 {
  TRUE
  FALSE
}

input gists_update_request_Input {
  """The description of the gist."""
  description: String
  files: mutationInput_gists_update_input_files_Input
}

"""
The gist files to be updated, renamed, or deleted. Each `key` must match the current filename
(including extension) of the targeted gist file. For example: `hello.py`.

To delete a file, set the whole file to null. For example: `hello.py : null`. The file will also be
deleted if the specified object does not contain at least one of `content` or `filename`.
"""
input mutationInput_gists_update_input_files_Input {
  hello_rb: mutationInput_gists_update_input_files_hello_rb_Input
}

input mutationInput_gists_update_input_files_hello_rb_Input {
  content: String
  filename: String
}

input gists_create_comment_request_Input {
  """The comment text."""
  body: mutationInput_gists_create_comment_input_body!
}

"""The comment text."""
scalar mutationInput_gists_create_comment_input_body

input gists_update_comment_request_Input {
  """The comment text."""
  body: mutationInput_gists_update_comment_input_body!
}

"""The comment text."""
scalar mutationInput_gists_update_comment_input_body

input markdown_render_request_Input {
  """The Markdown text to render in HTML."""
  text: String!
  mode: mutationInput_markdown_render_input_mode = markdown

  """
  The repository context to use when creating references in `gfm` mode.  For example, setting `context` to `octo-org/octo-repo` will change the text `#42` into an HTML link to issue 42 in the `octo-org/octo-repo` repository.
  """
  context: String
}

"""The rendering mode."""
enum mutationInput_markdown_render_input_mode {
  markdown
  gfm
}

type activity_mark_notifications_as_read_202_response {
  message: String
}

input activity_mark_notifications_as_read_request_Input {
  """
  Describes the last point that notifications were checked. Anything updated since this time will not be marked as read. If you omit this parameter, all notifications are marked as read. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Default: The current timestamp.
  """
  last_read_at: DateTime

  """Whether the notification has been read."""
  read: Boolean
}

input activity_set_thread_subscription_request_Input {
  """Whether to block all notifications from a thread."""
  ignored: Boolean
}

"""GitHub Actions cache retention policy for an organization."""
input Actions_cache_retention_limit_for_an_organization_Input {
  """
  For repositories in this organization, the maximum duration, in days, for which caches in a repository may be retained.
  """
  max_cache_retention_days: Int
}

"""GitHub Actions cache storage policy for an organization."""
input Actions_cache_storage_limit_for_an_organization_Input {
  """
  For repositories in the organization, the maximum size limit for the sum of all caches in a repository, in gigabytes.
  """
  max_cache_size_gb: Int
}

input dependabot_update_repository_access_for_org_request_Input {
  """List of repository IDs to add."""
  repository_ids_to_add: [Int]

  """List of repository IDs to remove."""
  repository_ids_to_remove: [Int]
}

input dependabot_set_repository_access_default_level_request_Input {
  default_level: mutationInput_dependabot_set_repository_access_default_level_input_default_level!
}

"""The default repository access level for Dependabot updates."""
enum mutationInput_dependabot_set_repository_access_default_level_input_default_level {
  public
  internal
}

type billing_update_budget_org_200_response {
  message: String
  budget: mutation_billing_update_budget_org_budget
}

type mutation_billing_update_budget_org_budget {
  """ID of the budget."""
  id: String

  """
  The budget amount in whole dollars. For license-based products, this represents the number of licenses.
  """
  budget_amount: Float

  """Whether to prevent additional spending once the budget is exceeded"""
  prevent_further_usage: Boolean
  budget_alerting: mutation_billing_update_budget_org_budget_budget_alerting
  budget_scope: mutation_billing_update_budget_org_budget_budget_scope

  """The name of the entity to apply the budget to"""
  budget_entity_name: String
  budget_type: mutation_billing_update_budget_org_budget_budget_type

  """A single product or SKU that will be covered in the budget"""
  budget_product_sku: String
}

type mutation_billing_update_budget_org_budget_budget_alerting {
  """Whether alerts are enabled for this budget"""
  will_alert: Boolean!

  """Array of user login names who will receive alerts"""
  alert_recipients: [String]!
}

"""The scope of the budget"""
enum mutation_billing_update_budget_org_budget_budget_scope {
  enterprise
  organization
  repository
  cost_center
}

"""The type of pricing for the budget"""
union mutation_billing_update_budget_org_budget_budget_type = ProductPricing_const_container | SkuPricing_const_container

type ProductPricing_const_container {
  ProductPricing_const: ProductPricing_const
}

enum ProductPricing_const {
  ProductPricing
}

type SkuPricing_const_container {
  SkuPricing_const: SkuPricing_const
}

enum SkuPricing_const {
  SkuPricing
}

input billing_update_budget_org_request_Input {
  """
  The budget amount in whole dollars. For license-based products, this represents the number of licenses.
  """
  budget_amount: Int

  """Whether to prevent additional spending once the budget is exceeded"""
  prevent_further_usage: Boolean
  budget_alerting: mutationInput_billing_update_budget_org_input_budget_alerting_Input
  budget_scope: mutationInput_billing_update_budget_org_input_budget_scope

  """The name of the entity to apply the budget to"""
  budget_entity_name: String
  budget_type: mutationInput_billing_update_budget_org_input_budget_type_Input

  """A single product or SKU that will be covered in the budget"""
  budget_product_sku: String
}

input mutationInput_billing_update_budget_org_input_budget_alerting_Input {
  """Whether alerts are enabled for this budget"""
  will_alert: Boolean

  """Array of user login names who will receive alerts"""
  alert_recipients: [String]
}

"""The scope of the budget"""
enum mutationInput_billing_update_budget_org_input_budget_scope {
  enterprise
  organization
  repository
  cost_center
}

input mutationInput_billing_update_budget_org_input_budget_type_Input {
  ProductPricing_const: ProductPricing_const
  SkuPricing_const: SkuPricing_const
}

"""Response when deleting a budget"""
type delete_budget {
  """A message indicating the result of the deletion operation"""
  message: String!

  """The ID of the deleted budget"""
  id: String!
}

input orgs_update_request_Input {
  """Billing email address. This address is not publicized."""
  billing_email: String

  """The company name."""
  company: String

  """The publicly visible email address."""
  email: String

  """The Twitter username of the company."""
  twitter_username: String

  """The location."""
  location: String

  """The shorthand name of the company."""
  name: String

  """The description of the company. The maximum size is 160 characters."""
  description: String

  """Whether an organization can use organization projects."""
  has_organization_projects: Boolean

  """
  Whether repositories that belong to the organization can use repository projects.
  """
  has_repository_projects: Boolean
  default_repository_permission: mutationInput_orgs_update_input_default_repository_permission = read

  """
  Whether of non-admin organization members can create repositories. **Note:** A parameter can override this parameter. See `members_allowed_repository_creation_type` in this table for details.
  """
  members_can_create_repositories: Boolean = true

  """
  Whether organization members can create internal repositories, which are visible to all enterprise members. You can only allow members to create internal repositories if your organization is associated with an enterprise account using GitHub Enterprise Cloud or GitHub Enterprise Server 2.20+. For more information, see "[Restricting repository creation in your organization](https://docs.github.com/github/setting-up-and-managing-organizations-and-teams/restricting-repository-creation-in-your-organization)" in the GitHub Help documentation.
  """
  members_can_create_internal_repositories: Boolean

  """
  Whether organization members can create private repositories, which are visible to organization members with permission. For more information, see "[Restricting repository creation in your organization](https://docs.github.com/github/setting-up-and-managing-organizations-and-teams/restricting-repository-creation-in-your-organization)" in the GitHub Help documentation.
  """
  members_can_create_private_repositories: Boolean

  """
  Whether organization members can create public repositories, which are visible to anyone. For more information, see "[Restricting repository creation in your organization](https://docs.github.com/github/setting-up-and-managing-organizations-and-teams/restricting-repository-creation-in-your-organization)" in the GitHub Help documentation.
  """
  members_can_create_public_repositories: Boolean
  members_allowed_repository_creation_type: mutationInput_orgs_update_input_members_allowed_repository_creation_type

  """
  Whether organization members can create GitHub Pages sites. Existing published sites will not be impacted.
  """
  members_can_create_pages: Boolean = true

  """
  Whether organization members can create public GitHub Pages sites. Existing published sites will not be impacted.
  """
  members_can_create_public_pages: Boolean = true

  """
  Whether organization members can create private GitHub Pages sites. Existing published sites will not be impacted.
  """
  members_can_create_private_pages: Boolean = true

  """
  Whether organization members can fork private organization repositories.
  """
  members_can_fork_private_repositories: Boolean

  """
  Whether contributors to organization repositories are required to sign off on commits they make through GitHub's web interface.
  """
  web_commit_signoff_required: Boolean
  blog: String

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether GitHub Advanced Security is automatically enabled for new repositories and repositories transferred to this organization.
  
  To use this parameter, you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  You can check which security and analysis features are currently enabled by using a `GET /orgs/{org}` request.
  """
  advanced_security_enabled_for_new_repositories: Boolean

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether Dependabot alerts are automatically enabled for new repositories and repositories transferred to this organization.
  
  To use this parameter, you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  You can check which security and analysis features are currently enabled by using a `GET /orgs/{org}` request.
  """
  dependabot_alerts_enabled_for_new_repositories: Boolean

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether Dependabot security updates are automatically enabled for new repositories and repositories transferred to this organization.
  
  To use this parameter, you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  You can check which security and analysis features are currently enabled by using a `GET /orgs/{org}` request.
  """
  dependabot_security_updates_enabled_for_new_repositories: Boolean

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether dependency graph is automatically enabled for new repositories and repositories transferred to this organization.
  
  To use this parameter, you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  You can check which security and analysis features are currently enabled by using a `GET /orgs/{org}` request.
  """
  dependency_graph_enabled_for_new_repositories: Boolean

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether secret scanning is automatically enabled for new repositories and repositories transferred to this organization.
  
  To use this parameter, you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  You can check which security and analysis features are currently enabled by using a `GET /orgs/{org}` request.
  """
  secret_scanning_enabled_for_new_repositories: Boolean

  """
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/rest/code-security/configurations) instead.
  
  Whether secret scanning push protection is automatically enabled for new repositories and repositories transferred to this organization.
  
  To use this parameter, you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
  
  You can check which security and analysis features are currently enabled by using a `GET /orgs/{org}` request.
  """
  secret_scanning_push_protection_enabled_for_new_repositories: Boolean

  """
  Whether a custom link is shown to contributors who are blocked from pushing a secret by push protection.
  """
  secret_scanning_push_protection_custom_link_enabled: Boolean

  """
  If `secret_scanning_push_protection_custom_link_enabled` is true, the URL that will be displayed to contributors who are blocked from pushing a secret.
  """
  secret_scanning_push_protection_custom_link: String

  """
  Controls whether or not deploy keys may be added and used for repositories in the organization.
  """
  deploy_keys_enabled_for_repositories: Boolean
}

"""Default permission level members have for organization repositories."""
enum mutationInput_orgs_update_input_default_repository_permission {
  read
  write
  admin
  none
}

"""
Specifies which types of repositories non-admin organization members can create. `private` is only available to repositories that are part of an organization on GitHub Enterprise Cloud. 
**Note:** This parameter is closing down and will be removed in the future. Its return value ignores internal repositories. Using this parameter overrides values set in `members_can_create_repositories`. See the parameter deprecation notice in the operation description for details.
"""
enum mutationInput_orgs_update_input_members_allowed_repository_creation_type {
  all
  private
  none
}

input actions_create_hosted_runner_for_org_request_Input {
  """
  Name of the runner. Must be between 1 and 64 characters and may only contain upper and lowercase letters a-z, numbers 0-9, '.', '-', and '_'.
  """
  name: String!
  image: mutationInput_actions_create_hosted_runner_for_org_input_image_Input!

  """
  The machine size of the runner. To list available sizes, use `GET actions/hosted-runners/machine-sizes`
  """
  size: String!

  """The existing runner group to add this runner to."""
  runner_group_id: Int!

  """
  The maximum amount of runners to scale up to. Runners will not auto-scale above this number. Use this setting to limit your cost.
  """
  maximum_runners: Int

  """
  Whether this runner should be created with a static public IP. Note limit on account. To list limits on account, use `GET actions/hosted-runners/limits`
  """
  enable_static_ip: Boolean

  """Whether this runner should be used to generate custom images."""
  image_gen: Boolean
}

"""
The image of runner. To list all available images, use `GET /actions/hosted-runners/images/github-owned` or `GET /actions/hosted-runners/images/partner`.
"""
input mutationInput_actions_create_hosted_runner_for_org_input_image_Input {
  """The unique identifier of the runner image."""
  id: String
  source: mutationInput_actions_create_hosted_runner_for_org_input_image_source

  """
  The version of the runner image to deploy. This is relevant only for runners using custom images.
  """
  version: String
}

"""The source of the runner image."""
enum mutationInput_actions_create_hosted_runner_for_org_input_image_source {
  github
  partner
  custom
}

input actions_update_hosted_runner_for_org_request_Input {
  """
  Name of the runner. Must be between 1 and 64 characters and may only contain upper and lowercase letters a-z, numbers 0-9, '.', '-', and '_'.
  """
  name: String

  """The existing runner group to add this runner to."""
  runner_group_id: Int

  """
  The maximum amount of runners to scale up to. Runners will not auto-scale above this number. Use this setting to limit your cost.
  """
  maximum_runners: Int

  """
  Whether this runner should be updated with a static public IP. Note limit on account. To list limits on account, use `GET actions/hosted-runners/limits`
  """
  enable_static_ip: Boolean

  """
  The machine size of the runner. To list available sizes, use `GET actions/hosted-runners/machine-sizes`
  """
  size: String

  """
  The unique identifier of the runner image. To list available images, use `GET /actions/hosted-runners/images/github-owned`, `GET /actions/hosted-runners/images/partner`, or `GET /actions/hosted-runners/images/custom`.
  """
  image_id: String

  """
  The version of the runner image to deploy. This is relevant only for runners using custom images.
  """
  image_version: String
}

"""Actions OIDC Subject customization"""
input Actions_OIDC_Subject_customization_Input {
  """
  Array of unique strings. Each claim key can only contain alphanumeric characters and underscores.
  """
  include_claim_keys: [String]!
}

input actions_set_github_actions_permissions_organization_request_Input {
  enabled_repositories: enabled_repositories!
  allowed_actions: allowed_actions

  """Whether actions must be pinned to a full-length commit SHA."""
  sha_pinning_required: Boolean
}

input actions_artifact_and_log_retention_Input {
  """The number of days to retain artifacts and logs"""
  days: Int!
}

input actions_fork_pr_contributor_approval_Input {
  approval_policy: query_actions_get_fork_pr_contributor_approval_permissions_organization_approval_policy!
}

input actions_fork_pr_workflows_private_repos_request_Input {
  """
  Whether workflows triggered by pull requests from forks are allowed to run on private repositories.
  """
  run_workflows_from_fork_pull_requests: Boolean!

  """
  Whether GitHub Actions can create pull requests or submit approving pull request reviews from a workflow triggered by a fork pull request.
  """
  send_write_tokens_to_workflows: Boolean

  """
  Whether to make secrets and variables available to workflows triggered by pull requests from forks.
  """
  send_secrets_and_variables: Boolean

  """
  Whether workflows triggered by pull requests from forks require approval from a repository administrator to run.
  """
  require_approval_for_fork_pr_workflows: Boolean
}

input actions_set_selected_repositories_enabled_github_actions_organization_request_Input {
  """List of repository IDs to enable for GitHub Actions."""
  selected_repository_ids: [Int]!
}

input selected_actions_Input {
  """
  Whether GitHub-owned actions are allowed. For example, this includes the actions in the `actions` organization.
  """
  github_owned_allowed: Boolean

  """
  Whether actions from GitHub Marketplace verified creators are allowed. Set to `true` to allow all actions by GitHub Marketplace verified creators.
  """
  verified_allowed: Boolean

  """
  Specifies a list of string-matching patterns to allow specific action(s) and reusable workflow(s). Wildcards, tags, and SHAs are allowed. For example, `monalisa/octocat@*`, `monalisa/octocat@v2`, `monalisa/*`.
  
  > [!NOTE]
  > The `patterns_allowed` setting only applies to public repositories.
  """
  patterns_allowed: [String]
}

input actions_set_self_hosted_runners_permissions_organization_request_Input {
  enabled_repositories: mutationInput_actions_set_self_hosted_runners_permissions_organization_input_enabled_repositories!
}

"""
The policy that controls whether self-hosted runners can be used in the organization
"""
enum mutationInput_actions_set_self_hosted_runners_permissions_organization_input_enabled_repositories {
  all
  selected
  none
}

input actions_set_selected_repositories_self_hosted_runners_organization_request_Input {
  """IDs of repositories that can use repository-level self-hosted runners"""
  selected_repository_ids: [Int]!
}

input actions_set_default_workflow_permissions_Input {
  default_workflow_permissions: actions_default_workflow_permissions

  """
  Whether GitHub Actions can approve pull requests. Enabling this can be a security risk.
  """
  can_approve_pull_request_reviews: Boolean
}

input actions_create_self_hosted_runner_group_for_org_request_Input {
  """Name of the runner group."""
  name: String!
  visibility: mutationInput_actions_create_self_hosted_runner_group_for_org_input_visibility = all

  """List of repository IDs that can access the runner group."""
  selected_repository_ids: [Int]

  """List of runner IDs to add to the runner group."""
  runners: [Int]

  """Whether the runner group can be used by `public` repositories."""
  allows_public_repositories: Boolean

  """
  If `true`, the runner group will be restricted to running only the workflows specified in the `selected_workflows` array.
  """
  restricted_to_workflows: Boolean

  """
  List of workflows the runner group should be allowed to run. This setting will be ignored unless `restricted_to_workflows` is set to `true`.
  """
  selected_workflows: [String]

  """The identifier of a hosted compute network configuration."""
  network_configuration_id: String
}

"""
Visibility of a runner group. You can select all repositories, select individual repositories, or limit access to private repositories.
"""
enum mutationInput_actions_create_self_hosted_runner_group_for_org_input_visibility {
  selected
  all
  private
}

input actions_update_self_hosted_runner_group_for_org_request_Input {
  """Name of the runner group."""
  name: String!
  visibility: mutationInput_actions_update_self_hosted_runner_group_for_org_input_visibility

  """Whether the runner group can be used by `public` repositories."""
  allows_public_repositories: Boolean

  """
  If `true`, the runner group will be restricted to running only the workflows specified in the `selected_workflows` array.
  """
  restricted_to_workflows: Boolean

  """
  List of workflows the runner group should be allowed to run. This setting will be ignored unless `restricted_to_workflows` is set to `true`.
  """
  selected_workflows: [String]

  """The identifier of a hosted compute network configuration."""
  network_configuration_id: String
}

"""
Visibility of a runner group. You can select all repositories, select individual repositories, or all private repositories.
"""
enum mutationInput_actions_update_self_hosted_runner_group_for_org_input_visibility {
  selected
  all
  private
}

input actions_set_repo_access_to_self_hosted_runner_group_in_org_request_Input {
  """List of repository IDs that can access the runner group."""
  selected_repository_ids: [Int]!
}

input actions_set_self_hosted_runners_in_group_for_org_request_Input {
  """List of runner IDs to add to the runner group."""
  runners: [Int]!
}

"""Response"""
type actions_runner_jitconfig_response {
  runner: Self_hosted_runners!

  """The base64 encoded runner configuration."""
  encoded_jit_config: String!
}

input actions_generate_runner_jitconfig_for_org_request_Input {
  """The name of the new runner."""
  name: String!

  """The ID of the runner group to register the runner to."""
  runner_group_id: Int!

  """
  The names of the custom labels to add to the runner. **Minimum items**: 1. **Maximum items**: 100.
  """
  labels: [String]!

  """
  The working directory to be used for job execution, relative to the runner install directory.
  """
  work_folder: String = "_work"
}

"""Authentication Token"""
type Authentication_Token {
  """The token used for authentication"""
  token: String!

  """The time this token expires"""
  expires_at: DateTime!
  permissions: mutation_actions_create_registration_token_for_org_permissions

  """The repositories this token has access to"""
  repositories: [repository]
  single_file: String
  repository_selection: mutation_actions_create_registration_token_for_org_repository_selection
}

type mutation_actions_create_registration_token_for_org_permissions {
  issues: String
  deployments: String
}

"""
Describe whether all repositories have been selected or there's a selection involved
"""
enum mutation_actions_create_registration_token_for_org_repository_selection {
  all
  selected
}

input actions_add_custom_labels_to_self_hosted_runner_for_org_request_Input {
  """The names of the custom labels to add to the runner."""
  labels: [String]!
}

input actions_set_custom_labels_for_self_hosted_runner_for_org_request_Input {
  """
  The names of the custom labels to set for the runner. You can pass an empty array to remove all custom labels.
  """
  labels: [String]!
}

"""Response"""
type actions_runner_labels_readonly_response {
  total_count: Int!
  labels: [Self_hosted_runner_label]!
}

union actions_create_or_update_org_secret_response = JSON_container | Void_container

input actions_create_or_update_org_secret_request_Input {
  encrypted_value: mutationInput_actions_create_or_update_org_secret_input_encrypted_value!

  """ID of the key you used to encrypt the secret."""
  key_id: String!
  visibility: mutationInput_actions_create_or_update_org_secret_input_visibility!

  """
  An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can manage the list of selected repositories using the [List selected repositories for an organization secret](https://docs.github.com/rest/actions/secrets#list-selected-repositories-for-an-organization-secret), [Set selected repositories for an organization secret](https://docs.github.com/rest/actions/secrets#set-selected-repositories-for-an-organization-secret), and [Remove selected repository from an organization secret](https://docs.github.com/rest/actions/secrets#remove-selected-repository-from-an-organization-secret) endpoints.
  """
  selected_repository_ids: [Int]
}

scalar mutationInput_actions_create_or_update_org_secret_input_encrypted_value

"""
Which type of organization repositories have access to the organization secret. `selected` means only the repositories specified by `selected_repository_ids` can access the secret.
"""
enum mutationInput_actions_create_or_update_org_secret_input_visibility {
  all
  private
  selected
}

input actions_set_selected_repos_for_org_secret_request_Input {
  """
  An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can add and remove individual repositories using the [Add selected repository to an organization secret](https://docs.github.com/rest/actions/secrets#add-selected-repository-to-an-organization-secret) and [Remove selected repository from an organization secret](https://docs.github.com/rest/actions/secrets#remove-selected-repository-from-an-organization-secret) endpoints.
  """
  selected_repository_ids: [Int]!
}

input actions_create_org_variable_request_Input {
  """The name of the variable."""
  name: String!

  """The value of the variable."""
  value: String!
  visibility: mutationInput_actions_create_org_variable_input_visibility!

  """
  An array of repository ids that can access the organization variable. You can only provide a list of repository ids when the `visibility` is set to `selected`.
  """
  selected_repository_ids: [Int]
}

"""
The type of repositories in the organization that can access the variable. `selected` means only the repositories specified by `selected_repository_ids` can access the variable.
"""
enum mutationInput_actions_create_org_variable_input_visibility {
  all
  private
  selected
}

input actions_update_org_variable_request_Input {
  """The name of the variable."""
  name: String

  """The value of the variable."""
  value: String
  visibility: mutationInput_actions_update_org_variable_input_visibility

  """
  An array of repository ids that can access the organization variable. You can only provide a list of repository ids when the `visibility` is set to `selected`.
  """
  selected_repository_ids: [Int]
}

"""
The type of repositories in the organization that can access the variable. `selected` means only the repositories specified by `selected_repository_ids` can access the variable.
"""
enum mutationInput_actions_update_org_variable_input_visibility {
  all
  private
  selected
}

input actions_set_selected_repos_for_org_variable_request_Input {
  """The IDs of the repositories that can access the organization variable."""
  selected_repository_ids: [Int]!
}

type orgs_create_artifact_deployment_record_200_response {
  """The number of deployment records created"""
  total_count: Int
  deployment_records: [Artifact_Deployment_Record]
}

input orgs_create_artifact_deployment_record_request_Input {
  """The name of the artifact."""
  name: mutationInput_orgs_create_artifact_deployment_record_input_name!
  digest: mutationInput_orgs_create_artifact_deployment_record_input_digest!

  """The artifact version."""
  version: mutationInput_orgs_create_artifact_deployment_record_input_version
  status: mutationInput_orgs_create_artifact_deployment_record_input_status!

  """The stage of the deployment."""
  logical_environment: mutationInput_orgs_create_artifact_deployment_record_input_logical_environment!

  """The physical region of the deployment."""
  physical_environment: mutationInput_orgs_create_artifact_deployment_record_input_physical_environment

  """The deployment cluster."""
  cluster: mutationInput_orgs_create_artifact_deployment_record_input_cluster

  """
  The unique identifier for the deployment represented by the new record. To accommodate differing
  containers and namespaces within a cluster, the following format is recommended:
  {namespaceName}-{deploymentName}-{containerName}.
  """
  deployment_name: mutationInput_orgs_create_artifact_deployment_record_input_deployment_name!

  """The tags associated with the deployment."""
  tags: JSON

  """A list of runtime risks associated with the deployment."""
  runtime_risks: [mutationInput_orgs_create_artifact_deployment_record_input_runtime_risks_items]
  github_repository: mutationInput_orgs_create_artifact_deployment_record_input_github_repository
}

"""The name of the artifact."""
scalar mutationInput_orgs_create_artifact_deployment_record_input_name

scalar mutationInput_orgs_create_artifact_deployment_record_input_digest

"""The artifact version."""
scalar mutationInput_orgs_create_artifact_deployment_record_input_version

"""The status of the artifact. Can be either deployed or decommissioned."""
enum mutationInput_orgs_create_artifact_deployment_record_input_status {
  deployed
  decommissioned
}

"""The stage of the deployment."""
scalar mutationInput_orgs_create_artifact_deployment_record_input_logical_environment

"""The physical region of the deployment."""
scalar mutationInput_orgs_create_artifact_deployment_record_input_physical_environment

"""The deployment cluster."""
scalar mutationInput_orgs_create_artifact_deployment_record_input_cluster

"""
The unique identifier for the deployment represented by the new record. To accommodate differing
containers and namespaces within a cluster, the following format is recommended:
{namespaceName}-{deploymentName}-{containerName}.
"""
scalar mutationInput_orgs_create_artifact_deployment_record_input_deployment_name

enum mutationInput_orgs_create_artifact_deployment_record_input_runtime_risks_items {
  critical_resource
  internet_exposed
  lateral_movement
  sensitive_data
}

scalar mutationInput_orgs_create_artifact_deployment_record_input_github_repository

type orgs_set_cluster_deployment_records_200_response {
  """The number of deployment records created"""
  total_count: Int
  deployment_records: [Artifact_Deployment_Record]
}

scalar mutationInput_orgs_set_cluster_deployment_records_cluster

input orgs_set_cluster_deployment_records_request_Input {
  """The stage of the deployment."""
  logical_environment: mutationInput_orgs_set_cluster_deployment_records_input_logical_environment!

  """The physical region of the deployment."""
  physical_environment: mutationInput_orgs_set_cluster_deployment_records_input_physical_environment

  """The list of deployments to record."""
  deployments: [mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_Input]!
}

"""The stage of the deployment."""
scalar mutationInput_orgs_set_cluster_deployment_records_input_logical_environment

"""The physical region of the deployment."""
scalar mutationInput_orgs_set_cluster_deployment_records_input_physical_environment

input mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_Input {
  """
  The name of the artifact. Note that if multiple deployments have identical 'digest' parameter values,
  the name parameter must also be identical across all entries.
  """
  name: mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_name!
  digest: mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_digest!

  """
  The artifact version. Note that if multiple deployments have identical 'digest' parameter values,
  the version parameter must also be identical across all entries.
  """
  version: mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_version
  status: mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_status = deployed

  """
  The unique identifier for the deployment represented by the new record. To accommodate differing
  containers and namespaces within a record set, the following format is recommended:
  {namespaceName}-{deploymentName}-{containerName}.
  The deployment_name must be unique across all entries in the deployments array.
  """
  deployment_name: mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_deployment_name!
  github_repository: mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_github_repository

  """Key-value pairs to tag the deployment record."""
  tags: JSON

  """A list of runtime risks associated with the deployment."""
  runtime_risks: [mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_runtime_risks_items]
}

"""
The name of the artifact. Note that if multiple deployments have identical 'digest' parameter values,
the name parameter must also be identical across all entries.
"""
scalar mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_name

scalar mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_digest

"""
The artifact version. Note that if multiple deployments have identical 'digest' parameter values,
the version parameter must also be identical across all entries.
"""
scalar mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_version

"""The deployment status of the artifact."""
enum mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_status {
  deployed
  decommissioned
}

"""
The unique identifier for the deployment represented by the new record. To accommodate differing
containers and namespaces within a record set, the following format is recommended:
{namespaceName}-{deploymentName}-{containerName}.
The deployment_name must be unique across all entries in the deployments array.
"""
scalar mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_deployment_name

scalar mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_github_repository

enum mutationInput_orgs_set_cluster_deployment_records_input_deployments_items_runtime_risks_items {
  critical_resource
  internet_exposed
  lateral_movement
  sensitive_data
}

type orgs_create_artifact_storage_record_200_response {
  total_count: Int
  storage_records: [mutation_orgs_create_artifact_storage_record_storage_records_items]
}

type mutation_orgs_create_artifact_storage_record_storage_records_items {
  id: Int
  name: String
  digest: String
  artifact_url: String
  registry_url: String
  repository: String
  status: String
  created_at: String
  updated_at: String
}

input orgs_create_artifact_storage_record_request_Input {
  """The name of the artifact."""
  name: mutationInput_orgs_create_artifact_storage_record_input_name!
  digest: mutationInput_orgs_create_artifact_storage_record_input_digest!

  """The artifact version."""
  version: mutationInput_orgs_create_artifact_storage_record_input_version
  artifact_url: mutationInput_orgs_create_artifact_storage_record_input_artifact_url

  """The path of the artifact."""
  path: URL
  registry_url: mutationInput_orgs_create_artifact_storage_record_input_registry_url!

  """The repository name within the registry."""
  repository: String
  status: mutationInput_orgs_create_artifact_storage_record_input_status = active
  github_repository: mutationInput_orgs_create_artifact_storage_record_input_github_repository
}

"""The name of the artifact."""
scalar mutationInput_orgs_create_artifact_storage_record_input_name

scalar mutationInput_orgs_create_artifact_storage_record_input_digest

"""The artifact version."""
scalar mutationInput_orgs_create_artifact_storage_record_input_version

scalar mutationInput_orgs_create_artifact_storage_record_input_artifact_url

scalar mutationInput_orgs_create_artifact_storage_record_input_registry_url

"""The status of the artifact (e.g., active, inactive)."""
enum mutationInput_orgs_create_artifact_storage_record_input_status {
  active
  eol
  deleted
}

scalar mutationInput_orgs_create_artifact_storage_record_input_github_repository

type orgs_list_attestations_bulk_200_response {
  """Mapping of subject digest to bundles."""
  attestations_subject_digests: JSON
  page_info: mutation_orgs_list_attestations_bulk_page_info
}

"""Information about the current page."""
type mutation_orgs_list_attestations_bulk_page_info {
  """Indicates whether there is a next page."""
  has_next: Boolean

  """Indicates whether there is a previous page."""
  has_previous: Boolean

  """The cursor to the next page."""
  next: String

  """The cursor to the previous page."""
  previous: String
}

input orgs_list_attestations_bulk_request_Input {
  """List of subject digests to fetch attestations for."""
  subject_digests: [String]!

  """
  Optional filter for fetching attestations with a given predicate type.
  This option accepts `provenance`, `sbom`, `release`, or freeform text
  for custom predicate types.
  """
  predicate_type: String
}

input orgs_delete_attestations_bulk_request_Input {
  mutationInput_orgs_delete_attestations_bulk_input_oneOf_0_Input: mutationInput_orgs_delete_attestations_bulk_input_oneOf_0_Input
  mutationInput_orgs_delete_attestations_bulk_input_oneOf_1_Input: mutationInput_orgs_delete_attestations_bulk_input_oneOf_1_Input
}

input mutationInput_orgs_delete_attestations_bulk_input_oneOf_0_Input {
  """
  List of subject digests associated with the artifact attestations to delete.
  """
  subject_digests: [String]!
}

input mutationInput_orgs_delete_attestations_bulk_input_oneOf_1_Input {
  """
  List of unique IDs associated with the artifact attestations to delete.
  """
  attestation_ids: [Int]!
}

input campaigns_create_campaign_request_Input {
  """The name of the campaign"""
  name: mutationInput_campaigns_create_campaign_input_name!

  """A description for the campaign"""
  description: mutationInput_campaigns_create_campaign_input_description!

  """
  The logins of the users to set as the campaign managers. At this time, only a single manager can be supplied.
  """
  managers: [String]

  """The slugs of the teams to set as the campaign managers."""
  team_managers: [String]

  """The end date and time of the campaign. The date must be in the future."""
  ends_at: DateTime!

  """The contact link of the campaign. Must be a URI."""
  contact_link: URL

  """The code scanning alerts to include in this campaign"""
  code_scanning_alerts: [mutationInput_campaigns_create_campaign_input_code_scanning_alerts_items_Input]

  """
  If true, will automatically generate issues for the campaign. The default is false.
  """
  generate_issues: Boolean
}

"""The name of the campaign"""
scalar mutationInput_campaigns_create_campaign_input_name

"""A description for the campaign"""
scalar mutationInput_campaigns_create_campaign_input_description

input mutationInput_campaigns_create_campaign_input_code_scanning_alerts_items_Input {
  """The repository id"""
  repository_id: Int!

  """The alert numbers"""
  alert_numbers: [Int]!
}

input campaigns_update_campaign_request_Input {
  """The name of the campaign"""
  name: mutationInput_campaigns_update_campaign_input_name

  """A description for the campaign"""
  description: mutationInput_campaigns_update_campaign_input_description

  """
  The logins of the users to set as the campaign managers. At this time, only a single manager can be supplied.
  """
  managers: [String]

  """The slugs of the teams to set as the campaign managers."""
  team_managers: [String]

  """
  The end date and time of the campaign, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  """
  ends_at: DateTime

  """The contact link of the campaign. Must be a URI."""
  contact_link: URL
  state: Campaign_state
}

"""The name of the campaign"""
scalar mutationInput_campaigns_update_campaign_input_name

"""A description for the campaign"""
scalar mutationInput_campaigns_update_campaign_input_description

input code_security_create_configuration_request_Input {
  """
  The name of the code security configuration. Must be unique within the organization.
  """
  name: String!

  """A description of the code security configuration"""
  description: mutationInput_code_security_create_configuration_input_description!
  advanced_security: mutationInput_code_security_create_configuration_input_advanced_security = disabled
  code_security: mutationInput_code_security_create_configuration_input_code_security
  dependency_graph: mutationInput_code_security_create_configuration_input_dependency_graph = enabled
  dependency_graph_autosubmit_action: mutationInput_code_security_create_configuration_input_dependency_graph_autosubmit_action = disabled
  dependency_graph_autosubmit_action_options: mutationInput_code_security_create_configuration_input_dependency_graph_autosubmit_action_options_Input
  dependabot_alerts: mutationInput_code_security_create_configuration_input_dependabot_alerts = disabled
  dependabot_security_updates: mutationInput_code_security_create_configuration_input_dependabot_security_updates = disabled
  dependabot_delegated_alert_dismissal: mutationInput_code_security_create_configuration_input_dependabot_delegated_alert_dismissal = disabled
  code_scanning_options: code_scanning_options_Input
  code_scanning_default_setup: mutationInput_code_security_create_configuration_input_code_scanning_default_setup = disabled
  code_scanning_default_setup_options: code_scanning_default_setup_options_Input
  code_scanning_delegated_alert_dismissal: mutationInput_code_security_create_configuration_input_code_scanning_delegated_alert_dismissal = not_set
  secret_protection: mutationInput_code_security_create_configuration_input_secret_protection
  secret_scanning: mutationInput_code_security_create_configuration_input_secret_scanning = disabled
  secret_scanning_push_protection: mutationInput_code_security_create_configuration_input_secret_scanning_push_protection = disabled
  secret_scanning_delegated_bypass: mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass = disabled
  secret_scanning_delegated_bypass_options: mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass_options_Input
  secret_scanning_validity_checks: mutationInput_code_security_create_configuration_input_secret_scanning_validity_checks = disabled
  secret_scanning_non_provider_patterns: mutationInput_code_security_create_configuration_input_secret_scanning_non_provider_patterns = disabled
  secret_scanning_generic_secrets: mutationInput_code_security_create_configuration_input_secret_scanning_generic_secrets = disabled
  secret_scanning_delegated_alert_dismissal: mutationInput_code_security_create_configuration_input_secret_scanning_delegated_alert_dismissal
  secret_scanning_extended_metadata: mutationInput_code_security_create_configuration_input_secret_scanning_extended_metadata
  private_vulnerability_reporting: mutationInput_code_security_create_configuration_input_private_vulnerability_reporting = disabled
  enforcement: mutationInput_code_security_create_configuration_input_enforcement = enforced
}

"""A description of the code security configuration"""
scalar mutationInput_code_security_create_configuration_input_description

"""
The enablement status of GitHub Advanced Security features. `enabled` will enable both Code Security and Secret Protection features.

> [!WARNING]
> `code_security` and `secret_protection` are deprecated values for this field. Prefer the individual `code_security` and `secret_protection` fields to set the status of these features.
"""
enum mutationInput_code_security_create_configuration_input_advanced_security {
  enabled
  disabled
  code_security
  secret_protection
}

"""The enablement status of GitHub Code Security features."""
enum mutationInput_code_security_create_configuration_input_code_security {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependency Graph"""
enum mutationInput_code_security_create_configuration_input_dependency_graph {
  enabled
  disabled
  not_set
}

"""The enablement status of Automatic dependency submission"""
enum mutationInput_code_security_create_configuration_input_dependency_graph_autosubmit_action {
  enabled
  disabled
  not_set
}

"""Feature options for Automatic dependency submission"""
input mutationInput_code_security_create_configuration_input_dependency_graph_autosubmit_action_options_Input {
  """
  Whether to use runners labeled with 'dependency-submission' or standard GitHub runners.
  """
  labeled_runners: Boolean
}

"""The enablement status of Dependabot alerts"""
enum mutationInput_code_security_create_configuration_input_dependabot_alerts {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependabot security updates"""
enum mutationInput_code_security_create_configuration_input_dependabot_security_updates {
  enabled
  disabled
  not_set
}

"""
The enablement status of Dependabot delegated alert dismissal. Requires Dependabot alerts to be enabled.
"""
enum mutationInput_code_security_create_configuration_input_dependabot_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of code scanning default setup"""
enum mutationInput_code_security_create_configuration_input_code_scanning_default_setup {
  enabled
  disabled
  not_set
}

"""The enablement status of code scanning delegated alert dismissal"""
enum mutationInput_code_security_create_configuration_input_code_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of GitHub Secret Protection features."""
enum mutationInput_code_security_create_configuration_input_secret_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning"""
enum mutationInput_code_security_create_configuration_input_secret_scanning {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning push protection"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_push_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated bypass"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass {
  enabled
  disabled
  not_set
}

"""Feature options for secret scanning delegated bypass"""
input mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass_options_Input {
  """The bypass reviewers for secret scanning delegated bypass"""
  reviewers: [mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_Input]
}

input mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_Input {
  """The ID of the team or role selected as a bypass reviewer"""
  reviewer_id: Int!
  reviewer_type: mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type!
}

"""The type of the bypass reviewer"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type {
  TEAM
  ROLE
}

"""The enablement status of secret scanning validity checks"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_validity_checks {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning non provider patterns"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_non_provider_patterns {
  enabled
  disabled
  not_set
}

"""The enablement status of Copilot secret scanning"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_generic_secrets {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated alert dismissal"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning extended metadata"""
enum mutationInput_code_security_create_configuration_input_secret_scanning_extended_metadata {
  enabled
  disabled
  not_set
}

"""The enablement status of private vulnerability reporting"""
enum mutationInput_code_security_create_configuration_input_private_vulnerability_reporting {
  enabled
  disabled
  not_set
}

"""The enforcement status for a security configuration"""
enum mutationInput_code_security_create_configuration_input_enforcement {
  enforced
  unenforced
}

input code_security_detach_configuration_request_Input {
  """
  An array of repository IDs to detach from configurations. Up to 250 IDs can be provided.
  """
  selected_repository_ids: [Int]
}

union code_security_update_configuration_response = code_security_configuration | Void_container

input code_security_update_configuration_request_Input {
  """
  The name of the code security configuration. Must be unique within the organization.
  """
  name: String

  """A description of the code security configuration"""
  description: mutationInput_code_security_update_configuration_input_description
  advanced_security: mutationInput_code_security_update_configuration_input_advanced_security
  code_security: mutationInput_code_security_update_configuration_input_code_security
  dependency_graph: mutationInput_code_security_update_configuration_input_dependency_graph
  dependency_graph_autosubmit_action: mutationInput_code_security_update_configuration_input_dependency_graph_autosubmit_action
  dependency_graph_autosubmit_action_options: mutationInput_code_security_update_configuration_input_dependency_graph_autosubmit_action_options_Input
  dependabot_alerts: mutationInput_code_security_update_configuration_input_dependabot_alerts
  dependabot_security_updates: mutationInput_code_security_update_configuration_input_dependabot_security_updates
  dependabot_delegated_alert_dismissal: mutationInput_code_security_update_configuration_input_dependabot_delegated_alert_dismissal
  code_scanning_default_setup: mutationInput_code_security_update_configuration_input_code_scanning_default_setup
  code_scanning_default_setup_options: code_scanning_default_setup_options_Input
  code_scanning_options: code_scanning_options_Input
  code_scanning_delegated_alert_dismissal: mutationInput_code_security_update_configuration_input_code_scanning_delegated_alert_dismissal = disabled
  secret_protection: mutationInput_code_security_update_configuration_input_secret_protection
  secret_scanning: mutationInput_code_security_update_configuration_input_secret_scanning
  secret_scanning_push_protection: mutationInput_code_security_update_configuration_input_secret_scanning_push_protection
  secret_scanning_delegated_bypass: mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass
  secret_scanning_delegated_bypass_options: mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass_options_Input
  secret_scanning_validity_checks: mutationInput_code_security_update_configuration_input_secret_scanning_validity_checks
  secret_scanning_non_provider_patterns: mutationInput_code_security_update_configuration_input_secret_scanning_non_provider_patterns
  secret_scanning_generic_secrets: mutationInput_code_security_update_configuration_input_secret_scanning_generic_secrets
  secret_scanning_delegated_alert_dismissal: mutationInput_code_security_update_configuration_input_secret_scanning_delegated_alert_dismissal
  secret_scanning_extended_metadata: mutationInput_code_security_update_configuration_input_secret_scanning_extended_metadata
  private_vulnerability_reporting: mutationInput_code_security_update_configuration_input_private_vulnerability_reporting
  enforcement: mutationInput_code_security_update_configuration_input_enforcement
}

"""A description of the code security configuration"""
scalar mutationInput_code_security_update_configuration_input_description

"""
The enablement status of GitHub Advanced Security features. `enabled` will enable both Code Security and Secret Protection features.

> [!WARNING]
> `code_security` and `secret_protection` are deprecated values for this field. Prefer the individual `code_security` and `secret_protection` fields to set the status of these features.
"""
enum mutationInput_code_security_update_configuration_input_advanced_security {
  enabled
  disabled
  code_security
  secret_protection
}

"""The enablement status of GitHub Code Security features."""
enum mutationInput_code_security_update_configuration_input_code_security {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependency Graph"""
enum mutationInput_code_security_update_configuration_input_dependency_graph {
  enabled
  disabled
  not_set
}

"""The enablement status of Automatic dependency submission"""
enum mutationInput_code_security_update_configuration_input_dependency_graph_autosubmit_action {
  enabled
  disabled
  not_set
}

"""Feature options for Automatic dependency submission"""
input mutationInput_code_security_update_configuration_input_dependency_graph_autosubmit_action_options_Input {
  """
  Whether to use runners labeled with 'dependency-submission' or standard GitHub runners.
  """
  labeled_runners: Boolean
}

"""The enablement status of Dependabot alerts"""
enum mutationInput_code_security_update_configuration_input_dependabot_alerts {
  enabled
  disabled
  not_set
}

"""The enablement status of Dependabot security updates"""
enum mutationInput_code_security_update_configuration_input_dependabot_security_updates {
  enabled
  disabled
  not_set
}

"""
The enablement status of Dependabot delegated alert dismissal. Requires Dependabot alerts to be enabled.
"""
enum mutationInput_code_security_update_configuration_input_dependabot_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of code scanning default setup"""
enum mutationInput_code_security_update_configuration_input_code_scanning_default_setup {
  enabled
  disabled
  not_set
}

"""The enablement status of code scanning delegated alert dismissal"""
enum mutationInput_code_security_update_configuration_input_code_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of GitHub Secret Protection features."""
enum mutationInput_code_security_update_configuration_input_secret_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning"""
enum mutationInput_code_security_update_configuration_input_secret_scanning {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning push protection"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_push_protection {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated bypass"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass {
  enabled
  disabled
  not_set
}

"""Feature options for secret scanning delegated bypass"""
input mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass_options_Input {
  """The bypass reviewers for secret scanning delegated bypass"""
  reviewers: [mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_Input]
}

input mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_Input {
  """The ID of the team or role selected as a bypass reviewer"""
  reviewer_id: Int!
  reviewer_type: mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type!
}

"""The type of the bypass reviewer"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type {
  TEAM
  ROLE
}

"""The enablement status of secret scanning validity checks"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_validity_checks {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning non-provider patterns"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_non_provider_patterns {
  enabled
  disabled
  not_set
}

"""The enablement status of Copilot secret scanning"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_generic_secrets {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning delegated alert dismissal"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_delegated_alert_dismissal {
  enabled
  disabled
  not_set
}

"""The enablement status of secret scanning extended metadata"""
enum mutationInput_code_security_update_configuration_input_secret_scanning_extended_metadata {
  enabled
  disabled
  not_set
}

"""The enablement status of private vulnerability reporting"""
enum mutationInput_code_security_update_configuration_input_private_vulnerability_reporting {
  enabled
  disabled
  not_set
}

"""The enforcement status for a security configuration"""
enum mutationInput_code_security_update_configuration_input_enforcement {
  enforced
  unenforced
}

input code_security_attach_configuration_request_Input {
  scope: mutationInput_code_security_attach_configuration_input_scope!

  """
  An array of repository IDs to attach the configuration to. You can only provide a list of repository ids when the `scope` is set to `selected`.
  """
  selected_repository_ids: [Int]
}

"""
The type of repositories to attach the configuration to. `selected` means the configuration will be attached to only the repositories specified by `selected_repository_ids`
"""
enum mutationInput_code_security_attach_configuration_input_scope {
  all
  all_without_configurations
  public
  private_or_internal
  selected
}

type code_security_set_configuration_as_default_200_response {
  default_for_new_repos: mutation_code_security_set_configuration_as_default_default_for_new_repos
  configuration: code_security_configuration
}

"""
Specifies which types of repository this security configuration is applied to by default.
"""
enum mutation_code_security_set_configuration_as_default_default_for_new_repos {
  all
  none
  private_and_internal
  public
}

input code_security_set_configuration_as_default_request_Input {
  default_for_new_repos: mutationInput_code_security_set_configuration_as_default_input_default_for_new_repos
}

"""
Specify which types of repository this security configuration should be applied to by default.
"""
enum mutationInput_code_security_set_configuration_as_default_input_default_for_new_repos {
  all
  none
  private_and_internal
  public
}

input codespaces_set_codespaces_access_request_Input {
  visibility: mutationInput_codespaces_set_codespaces_access_input_visibility!

  """
  The usernames of the organization members who should have access to codespaces in the organization. Required when `visibility` is `selected_members`. The provided list of usernames will replace any existing value.
  """
  selected_usernames: [String]
}

"""
Which users can access codespaces in the organization. `disabled` means that no users can access codespaces in the organization.
"""
enum mutationInput_codespaces_set_codespaces_access_input_visibility {
  disabled
  selected_members
  all_members
  all_members_and_outside_collaborators
}

input codespaces_set_codespaces_access_users_request_Input {
  """
  The usernames of the organization members and outside collaborators whose codespaces should be billed to the organization.
  """
  selected_usernames: [String]!
}

input codespaces_delete_codespaces_access_users_request_Input {
  """
  The usernames of the organization members and outside collaborators whose codespaces should not be billed to the organization.
  """
  selected_usernames: [String]!
}

union codespaces_create_or_update_org_secret_response = JSON_container | Void_container

input codespaces_create_or_update_org_secret_request_Input {
  encrypted_value: mutationInput_codespaces_create_or_update_org_secret_input_encrypted_value

  """The ID of the key you used to encrypt the secret."""
  key_id: String
  visibility: mutationInput_codespaces_create_or_update_org_secret_input_visibility!

  """
  An array of repository IDs that can access the organization secret. You can only provide a list of repository IDs when the `visibility` is set to `selected`. You can manage the list of selected repositories using the [List selected repositories for an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#list-selected-repositories-for-an-organization-secret), [Set selected repositories for an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#set-selected-repositories-for-an-organization-secret), and [Remove selected repository from an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#remove-selected-repository-from-an-organization-secret) endpoints.
  """
  selected_repository_ids: [Int]
}

scalar mutationInput_codespaces_create_or_update_org_secret_input_encrypted_value

"""
Which type of organization repositories have access to the organization secret. `selected` means only the repositories specified by `selected_repository_ids` can access the secret.
"""
enum mutationInput_codespaces_create_or_update_org_secret_input_visibility {
  all
  private
  selected
}

input codespaces_set_selected_repos_for_org_secret_request_Input {
  """
  An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can add and remove individual repositories using the [Set selected repositories for an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#set-selected-repositories-for-an-organization-secret) and [Remove selected repository from an organization secret](https://docs.github.com/rest/codespaces/organization-secrets#remove-selected-repository-from-an-organization-secret) endpoints.
  """
  selected_repository_ids: [Int]!
}

"""
The total number of seats created for members of the specified team(s).
"""
type copilot_add_copilot_seats_for_teams_201_response {
  seats_created: Int!
}

input copilot_add_copilot_seats_for_teams_request_Input {
  """
  List of team names within the organization to which to grant access to GitHub Copilot.
  """
  selected_teams: [String]!
}

"""
The total number of seats set to "pending cancellation" for members of the specified team(s).
"""
type copilot_cancel_copilot_seat_assignment_for_teams_200_response {
  seats_cancelled: Int!
}

input copilot_cancel_copilot_seat_assignment_for_teams_request_Input {
  """The names of teams from which to revoke access to GitHub Copilot."""
  selected_teams: [String]!
}

"""The total number of seats created for the specified user(s)."""
type copilot_add_copilot_seats_for_users_201_response {
  seats_created: Int!
}

input copilot_add_copilot_seats_for_users_request_Input {
  """
  The usernames of the organization members to be granted access to GitHub Copilot.
  """
  selected_usernames: [String]!
}

"""
The total number of seats set to "pending cancellation" for the specified users.
"""
type copilot_cancel_copilot_seat_assignment_for_users_200_response {
  seats_cancelled: Int!
}

input copilot_cancel_copilot_seat_assignment_for_users_request_Input {
  """
  The usernames of the organization members for which to revoke access to GitHub Copilot.
  """
  selected_usernames: [String]!
}

input copilot_set_copilot_coding_agent_permissions_organization_request_Input {
  enabled_repositories: mutationInput_copilot_set_copilot_coding_agent_permissions_organization_input_enabled_repositories!
}

"""
The policy for which repositories can use Copilot coding agent. Can be one of `all`, `selected`, or `none`.
"""
enum mutationInput_copilot_set_copilot_coding_agent_permissions_organization_input_enabled_repositories {
  all
  selected
  none
}

input copilot_set_copilot_coding_agent_selected_repositories_for_organization_request_Input {
  """List of repository IDs to enable for Copilot coding agent."""
  selected_repository_ids: [Int]!
}

type copilot_set_copilot_content_exclusion_for_organization_200_response {
  message: String
}

union dependabot_create_or_update_org_secret_response = JSON_container | Void_container

input dependabot_create_or_update_org_secret_request_Input {
  encrypted_value: mutationInput_dependabot_create_or_update_org_secret_input_encrypted_value

  """ID of the key you used to encrypt the secret."""
  key_id: String
  visibility: mutationInput_dependabot_create_or_update_org_secret_input_visibility!

  """
  An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can manage the list of selected repositories using the [List selected repositories for an organization secret](https://docs.github.com/rest/dependabot/secrets#list-selected-repositories-for-an-organization-secret), [Set selected repositories for an organization secret](https://docs.github.com/rest/dependabot/secrets#set-selected-repositories-for-an-organization-secret), and [Remove selected repository from an organization secret](https://docs.github.com/rest/dependabot/secrets#remove-selected-repository-from-an-organization-secret) endpoints.
  """
  selected_repository_ids: [JSON]
}

scalar mutationInput_dependabot_create_or_update_org_secret_input_encrypted_value

"""
Which type of organization repositories have access to the organization secret. `selected` means only the repositories specified by `selected_repository_ids` can access the secret.
"""
enum mutationInput_dependabot_create_or_update_org_secret_input_visibility {
  all
  private
  selected
}

input dependabot_set_selected_repos_for_org_secret_request_Input {
  """
  An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can add and remove individual repositories using the [Set selected repositories for an organization secret](https://docs.github.com/rest/dependabot/secrets#set-selected-repositories-for-an-organization-secret) and [Remove selected repository from an organization secret](https://docs.github.com/rest/dependabot/secrets#remove-selected-repository-from-an-organization-secret) endpoints.
  """
  selected_repository_ids: [Int]!
}

input orgs_create_webhook_request_Input {
  """Must be passed as "web"."""
  name: String!
  config: mutationInput_orgs_create_webhook_input_config_Input!

  """
  Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for. Set to `["*"]` to receive all possible events.
  """
  events: [String] = ["push"]

  """
  Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.
  """
  active: Boolean = true
}

"""Key/value pairs to provide settings for this webhook."""
input mutationInput_orgs_create_webhook_input_config_Input {
  """The URL to which the payloads will be delivered."""
  url: URL!

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl_Input
  username: String
  password: String
}

input orgs_update_webhook_request_Input {
  config: mutationInput_orgs_update_webhook_input_config_Input

  """
  Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for.
  """
  events: [String] = ["push"]

  """
  Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.
  """
  active: Boolean = true
  name: String
}

"""Key/value pairs to provide settings for this webhook."""
input mutationInput_orgs_update_webhook_input_config_Input {
  """The URL to which the payloads will be delivered."""
  url: URL!

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl_Input
}

input orgs_update_webhook_config_for_org_request_Input {
  """The URL to which the payloads will be delivered."""
  url: URL

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl_Input
}

"""Interaction limit settings."""
type Interaction_Limits {
  limit: interaction_group!
  origin: String!
  expires_at: DateTime!
}

"""Limit interactions to a specific type of user for a specified duration"""
input Interaction_Restrictions_Input {
  limit: interaction_group!
  expiry: interaction_expiry
}

"""The duration of the interaction restriction. Default: `one_day`."""
enum interaction_expiry {
  one_day
  three_days
  one_week
  one_month
  six_months
}

input orgs_create_invitation_request_Input {
  """
  **Required unless you provide `email`**. GitHub user ID for the person you are inviting.
  """
  invitee_id: Int

  """
  **Required unless you provide `invitee_id`**. Email address of the person you are inviting, which can be an existing GitHub user.
  """
  email: String
  role: mutationInput_orgs_create_invitation_input_role = direct_member

  """Specify IDs for the teams you want to invite new members to."""
  team_ids: [Int]
}

"""
The role for the new member. 
 * `admin` - Organization owners with full administrative rights to the organization and complete access to all repositories and teams.  
 * `direct_member` - Non-owner organization members with ability to see other members and join teams by invitation.  
 * `billing_manager` - Non-owner organization members with ability to manage the billing settings of your organization. 
 * `reinstate` - The previous role assigned to the invitee before they were removed from your organization. Can be one of the roles listed above. Only works if the invitee was previously part of your organization.
"""
enum mutationInput_orgs_create_invitation_input_role {
  admin
  direct_member
  billing_manager
  reinstate
}

input organization_create_issue_field_Input {
  """Name of the issue field."""
  name: String!

  """Description of the issue field."""
  description: String
  data_type: mutationInput_orgs_create_issue_field_input_data_type!
  visibility: mutationInput_orgs_create_issue_field_input_visibility

  """
  Options for single select fields. Required when data_type is 'single_select'.
  """
  options: [mutationInput_orgs_create_issue_field_input_options_items_Input]
}

"""The data type of the issue field."""
enum mutationInput_orgs_create_issue_field_input_data_type {
  text
  date
  single_select
  number
}

"""
The visibility of the issue field. Can be `organization_members_only` (visible only within the organization) or `all` (visible to all users who can see issues). Only used when the visibility settings feature is enabled. Defaults to `organization_members_only`.
"""
enum mutationInput_orgs_create_issue_field_input_visibility {
  organization_members_only
  all
}

input mutationInput_orgs_create_issue_field_input_options_items_Input {
  """Name of the option."""
  name: String!

  """Description of the option."""
  description: String
  color: mutationInput_orgs_create_issue_field_input_options_items_color!

  """Priority of the option for ordering."""
  priority: Int!
}

"""Color for the option."""
enum mutationInput_orgs_create_issue_field_input_options_items_color {
  gray
  blue
  green
  yellow
  orange
  red
  pink
  purple
}

input organization_update_issue_field_Input {
  """Name of the issue field."""
  name: String

  """Description of the issue field."""
  description: String
  visibility: mutationInput_orgs_update_issue_field_input_visibility

  """
  Options for single select fields. Only applicable when updating single_select fields.
  """
  options: [mutationInput_orgs_update_issue_field_input_options_items_Input]
}

"""
The visibility of the issue field. Can be `organization_members_only` (visible only within the organization) or `all` (visible to all users who can see issues). Only used when the visibility settings feature is enabled.
"""
enum mutationInput_orgs_update_issue_field_input_visibility {
  organization_members_only
  all
}

input mutationInput_orgs_update_issue_field_input_options_items_Input {
  """Name of the option."""
  name: String!

  """Description of the option."""
  description: String
  color: mutationInput_orgs_update_issue_field_input_options_items_color!

  """Priority of the option for ordering."""
  priority: Int!
}

"""Color for the option."""
enum mutationInput_orgs_update_issue_field_input_options_items_color {
  gray
  blue
  green
  yellow
  orange
  red
  pink
  purple
}

input organization_create_issue_type_Input {
  """Name of the issue type."""
  name: String!

  """Whether or not the issue type is enabled at the organization level."""
  is_enabled: Boolean!

  """Description of the issue type."""
  description: String
  color: mutationInput_orgs_create_issue_type_input_color
}

"""Color for the issue type."""
enum mutationInput_orgs_create_issue_type_input_color {
  gray
  blue
  green
  yellow
  orange
  red
  pink
  purple
}

input organization_update_issue_type_Input {
  """Name of the issue type."""
  name: String!

  """Whether or not the issue type is enabled at the organization level."""
  is_enabled: Boolean!

  """Description of the issue type."""
  description: String
  color: mutationInput_orgs_update_issue_type_input_color
}

"""Color for the issue type."""
enum mutationInput_orgs_update_issue_type_input_color {
  gray
  blue
  green
  yellow
  orange
  red
  pink
  purple
}

input orgs_set_membership_for_user_request_Input {
  role: mutationInput_orgs_set_membership_for_user_input_role = member
}

"""
The role to give the user in the organization. Can be one of:  
 * `admin` - The user will become an owner of the organization.  
 * `member` - The user will become a non-owner member of the organization.
"""
enum mutationInput_orgs_set_membership_for_user_input_role {
  admin
  member
}

input migrations_start_for_org_request_Input {
  """A list of arrays indicating which repositories should be migrated."""
  repositories: [String]!

  """
  Indicates whether repositories should be locked (to prevent manipulation) while migrating data.
  """
  lock_repositories: Boolean

  """
  Indicates whether metadata should be excluded and only git source should be included for the migration.
  """
  exclude_metadata: Boolean

  """
  Indicates whether the repository git data should be excluded from the migration.
  """
  exclude_git_data: Boolean

  """
  Indicates whether attachments should be excluded from the migration (to reduce migration archive file size).
  """
  exclude_attachments: Boolean

  """
  Indicates whether releases should be excluded from the migration (to reduce migration archive file size).
  """
  exclude_releases: Boolean

  """
  Indicates whether projects owned by the organization or users should be excluded. from the migration.
  """
  exclude_owner_projects: Boolean

  """
  Indicates whether this should only include organization metadata (repositories array should be empty and will ignore other flags).
  """
  org_metadata_only: Boolean

  """
  Exclude related items from being returned in the response in order to improve performance of the request.
  """
  exclude: [repositories_const]
}

union orgs_convert_member_to_outside_collaborator_response = JSON_container | Void_container

input orgs_convert_member_to_outside_collaborator_request_Input {
  """
  When set to `true`, the request will be performed asynchronously. Returns a 202 status code when the job is successfully queued.
  """
  async: Boolean
}

input orgs_review_pat_grant_requests_in_bulk_request_Input {
  """
  Unique identifiers of the requests for access via fine-grained personal access token. Must be formed of between 1 and 100 `pat_request_id` values.
  """
  pat_request_ids: [Int]
  action: mutationInput_orgs_review_pat_grant_requests_in_bulk_input_action!

  """Reason for approving or denying the requests. Max 1024 characters."""
  reason: mutationInput_orgs_review_pat_grant_requests_in_bulk_input_reason
}

"""Action to apply to the requests."""
enum mutationInput_orgs_review_pat_grant_requests_in_bulk_input_action {
  approve
  deny
}

"""Reason for approving or denying the requests. Max 1024 characters."""
scalar mutationInput_orgs_review_pat_grant_requests_in_bulk_input_reason

input orgs_review_pat_grant_request_request_Input {
  action: mutationInput_orgs_review_pat_grant_request_input_action!

  """Reason for approving or denying the request. Max 1024 characters."""
  reason: mutationInput_orgs_review_pat_grant_request_input_reason
}

"""Action to apply to the request."""
enum mutationInput_orgs_review_pat_grant_request_input_action {
  approve
  deny
}

"""Reason for approving or denying the request. Max 1024 characters."""
scalar mutationInput_orgs_review_pat_grant_request_input_reason

input orgs_update_pat_accesses_request_Input {
  action: revoke_const!

  """The IDs of the fine-grained personal access tokens."""
  pat_ids: [Int]!
}

enum revoke_const {
  revoke
}

input orgs_update_pat_access_request_Input {
  action: revoke_const!
}

"""Private registry configuration for an organization"""
type org_private_registry_configuration_with_selected_repositories {
  """The name of the private registry configuration."""
  name: String!
  registry_type: mutation_private_registries_create_org_private_registry_registry_type!

  """The URL of the private registry."""
  url: URL

  """The username to use when authenticating with the private registry."""
  username: String

  """
  Whether this private registry replaces the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When `true`, Dependabot will only use this registry and will not fall back to the public registry. When `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages.
  """
  replaces_base: Boolean
  visibility: mutation_private_registries_create_org_private_registry_visibility!

  """
  An array of repository IDs that can access the organization private registry when `visibility` is set to `selected`.
  """
  selected_repository_ids: [Int]
  created_at: DateTime!
  updated_at: DateTime!
}

"""The registry type."""
enum mutation_private_registries_create_org_private_registry_registry_type {
  maven_repository
  nuget_feed
  goproxy_server
  npm_registry
  rubygems_server
  cargo_registry
  composer_repository
  docker_registry
  git_source
  helm_registry
  hex_organization
  hex_repository
  pub_repository
  python_index
  terraform_registry
}

"""
Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry.
"""
enum mutation_private_registries_create_org_private_registry_visibility {
  all
  private
  selected
}

input private_registries_create_org_private_registry_request_Input {
  registry_type: mutationInput_private_registries_create_org_private_registry_input_registry_type!

  """The URL of the private registry."""
  url: URL!

  """
  The username to use when authenticating with the private registry. This field should be omitted if the private registry does not require a username for authentication.
  """
  username: String

  """
  Whether this private registry should replace the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When set to `true`, Dependabot will only use this registry and will not fall back to the public registry. When set to `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages.
  """
  replaces_base: Boolean
  encrypted_value: mutationInput_private_registries_create_org_private_registry_input_encrypted_value!

  """The ID of the key you used to encrypt the secret."""
  key_id: String!
  visibility: mutationInput_private_registries_create_org_private_registry_input_visibility!

  """
  An array of repository IDs that can access the organization private registry. You can only provide a list of repository IDs when `visibility` is set to `selected`. You can manage the list of selected repositories using the [Update a private registry for an organization](https://docs.github.com/rest/private-registries/organization-configurations#update-a-private-registry-for-an-organization) endpoint. This field should be omitted if `visibility` is set to `all` or `private`.
  """
  selected_repository_ids: [Int]
}

"""The registry type."""
enum mutationInput_private_registries_create_org_private_registry_input_registry_type {
  maven_repository
  nuget_feed
  goproxy_server
  npm_registry
  rubygems_server
  cargo_registry
  composer_repository
  docker_registry
  git_source
  helm_registry
  hex_organization
  hex_repository
  pub_repository
  python_index
  terraform_registry
}

scalar mutationInput_private_registries_create_org_private_registry_input_encrypted_value

"""
Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry.
"""
enum mutationInput_private_registries_create_org_private_registry_input_visibility {
  all
  private
  selected
}

input private_registries_update_org_private_registry_request_Input {
  registry_type: mutationInput_private_registries_update_org_private_registry_input_registry_type

  """The URL of the private registry."""
  url: URL

  """
  The username to use when authenticating with the private registry. This field should be omitted if the private registry does not require a username for authentication.
  """
  username: String

  """
  Whether this private registry should replace the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When set to `true`, Dependabot will only use this registry and will not fall back to the public registry. When set to `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages.
  """
  replaces_base: Boolean
  encrypted_value: mutationInput_private_registries_update_org_private_registry_input_encrypted_value

  """The ID of the key you used to encrypt the secret."""
  key_id: String
  visibility: mutationInput_private_registries_update_org_private_registry_input_visibility

  """
  An array of repository IDs that can access the organization private registry. You can only provide a list of repository IDs when `visibility` is set to `selected`. This field should be omitted if `visibility` is set to `all` or `private`.
  """
  selected_repository_ids: [Int]
}

"""The registry type."""
enum mutationInput_private_registries_update_org_private_registry_input_registry_type {
  maven_repository
  nuget_feed
  goproxy_server
  npm_registry
  rubygems_server
  cargo_registry
  composer_repository
  docker_registry
  git_source
  helm_registry
  hex_organization
  hex_repository
  pub_repository
  python_index
  terraform_registry
}

scalar mutationInput_private_registries_update_org_private_registry_input_encrypted_value

"""
Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry.
"""
enum mutationInput_private_registries_update_org_private_registry_input_visibility {
  all
  private
  selected
}

"""An item belonging to a project"""
type projects_v2_item_simple {
  """The unique identifier of the project item."""
  id: Float!

  """The node ID of the project item."""
  node_id: String
  content: mutation_projects_create_draft_item_for_org_content
  content_type: Projects_v2_Item_Content_Type!
  creator: simple_user

  """The time when the item was created."""
  created_at: DateTime!

  """The time when the item was last updated."""
  updated_at: DateTime!

  """The time when the item was archived."""
  archived_at: DateTime

  """The URL of the project this item belongs to."""
  project_url: URL

  """The URL of the item in the project."""
  item_url: URL
}

"""The content represented by the item."""
union mutation_projects_create_draft_item_for_org_content = issue | Pull_Request_Simple | Draft_Issue

"""A draft issue in a project"""
type Draft_Issue {
  """The ID of the draft issue"""
  id: Float!

  """The node ID of the draft issue"""
  node_id: String!

  """The title of the draft issue"""
  title: String!

  """The body content of the draft issue"""
  body: String
  user: nullable_simple_user

  """The time the draft issue was created"""
  created_at: DateTime!

  """The time the draft issue was last updated"""
  updated_at: DateTime!
}

input projects_create_draft_item_for_org_request_Input {
  """The title of the draft issue item to create in the project."""
  title: String!

  """The body content of the draft issue item to create in the project."""
  body: String
}

input projects_add_field_for_org_request_Input {
  mutationInput_projects_add_field_for_org_input_oneOf_0_Input: mutationInput_projects_add_field_for_org_input_oneOf_0_Input
  mutationInput_projects_add_field_for_org_input_oneOf_1_Input: mutationInput_projects_add_field_for_org_input_oneOf_1_Input
  mutationInput_projects_add_field_for_org_input_oneOf_2_Input: mutationInput_projects_add_field_for_org_input_oneOf_2_Input
  mutationInput_projects_add_field_for_org_input_oneOf_3_Input: mutationInput_projects_add_field_for_org_input_oneOf_3_Input
}

input mutationInput_projects_add_field_for_org_input_oneOf_0_Input {
  """The ID of the IssueField to create the field for."""
  issue_field_id: Int!
}

input mutationInput_projects_add_field_for_org_input_oneOf_1_Input {
  """The name of the field."""
  name: String!
  data_type: mutationInput_projects_add_field_for_org_input_oneOf_1_data_type!
}

"""The field's data type."""
enum mutationInput_projects_add_field_for_org_input_oneOf_1_data_type {
  text
  number
  date
}

input mutationInput_projects_add_field_for_org_input_oneOf_2_Input {
  """The name of the field."""
  name: String!
  data_type: single_select_const!

  """
  The options available for single select fields. At least one option must be provided when creating a single select field.
  """
  single_select_options: [projects_v2_field_single_select_option_Input]!
}

enum single_select_const {
  single_select
}

input projects_v2_field_single_select_option_Input {
  """The display name of the option."""
  name: String
  color: mutationInput_projects_add_field_for_org_input_oneOf_2_single_select_options_items_color

  """The description of the option."""
  description: String
}

"""The color associated with the option."""
enum mutationInput_projects_add_field_for_org_input_oneOf_2_single_select_options_items_color {
  BLUE
  GRAY
  GREEN
  ORANGE
  PINK
  PURPLE
  RED
  YELLOW
}

input mutationInput_projects_add_field_for_org_input_oneOf_3_Input {
  """The name of the field."""
  name: String!
  data_type: iteration_const!
  iteration_configuration: projects_v2_field_iteration_configuration_Input!
}

enum iteration_const {
  iteration
}

"""The configuration for iteration fields."""
input projects_v2_field_iteration_configuration_Input {
  """The start date of the first iteration."""
  start_date: Date

  """
  The default duration for iterations in days. Individual iterations can override this value.
  """
  duration: Int

  """Zero or more iterations for the field."""
  iterations: [mutationInput_projects_add_field_for_org_input_oneOf_3_iteration_configuration_iterations_items_Input]
}

input mutationInput_projects_add_field_for_org_input_oneOf_3_iteration_configuration_iterations_items_Input {
  """The title of the iteration."""
  title: String

  """The start date of the iteration."""
  start_date: Date

  """The duration of the iteration in days."""
  duration: Int
}

input projects_add_item_for_org_request_Input {
  type: mutationInput_projects_add_item_for_org_input_type!

  """
  The unique identifier of the issue or pull request to add to the project.
  """
  id: Int

  """The repository owner login."""
  owner: String

  """The repository name."""
  repo: String

  """The issue or pull request number."""
  number: Int
}

"""
The type of item to add to the project. Must be either Issue or PullRequest.
"""
enum mutationInput_projects_add_item_for_org_input_type {
  Issue
  PullRequest
}

input projects_update_item_for_org_request_Input {
  """A list of field updates to apply."""
  fields: [mutationInput_projects_update_item_for_org_input_fields_items_Input]!
}

input mutationInput_projects_update_item_for_org_input_fields_items_Input {
  """The ID of the project field to update."""
  id: Int!
  value: mutationInput_projects_update_item_for_org_input_fields_items_value_Input
}

input mutationInput_projects_update_item_for_org_input_fields_items_value_Input {
  String: String
  Float: Float
}

"""A view inside a projects v2 project"""
type Projects_v2_View {
  """The unique identifier of the view."""
  id: Int!

  """The number of the view within the project."""
  number: Int!

  """The name of the view."""
  name: String!
  layout: mutation_projects_create_view_for_org_layout!

  """The node ID of the view."""
  node_id: String!

  """The API URL of the project that contains the view."""
  project_url: URL!

  """The web URL of the view."""
  html_url: URL!
  creator: simple_user!

  """The time when the view was created."""
  created_at: DateTime!

  """The time when the view was last updated."""
  updated_at: DateTime!

  """The filter query for the view."""
  filter: String

  """The list of field IDs that are visible in the view."""
  visible_fields: [Int]!

  """
  The sorting configuration for the view. Each element is a tuple of [field_id, direction] where direction is "asc" or "desc".
  """
  sort_by: [[mutation_projects_create_view_for_org_sort_by_items_items]]!

  """The list of field IDs used for horizontal grouping."""
  group_by: [Int]!

  """The list of field IDs used for vertical grouping (board layout)."""
  vertical_group_by: [Int]!
}

"""The layout of the view."""
enum mutation_projects_create_view_for_org_layout {
  table
  board
  roadmap
}

union mutation_projects_create_view_for_org_sort_by_items_items = Int_container | String_container

input projects_create_view_for_org_request_Input {
  """The name of the view."""
  name: String!
  layout: mutationInput_projects_create_view_for_org_input_layout!

  """
  The filter query for the view. See [Filtering projects](https://docs.github.com/issues/planning-and-tracking-with-projects/customizing-views-in-your-project/filtering-projects) for more information.
  """
  filter: String

  """
  `visible_fields` is not applicable to `roadmap` layout views.
  For `table` and `board` layouts, this represents the field IDs that should be visible in the view. If not provided, the default visible fields will be used.
  """
  visible_fields: [Int]
}

"""The layout of the view."""
enum mutationInput_projects_create_view_for_org_input_layout {
  table
  board
  roadmap
}

input orgs_custom_properties_for_repos_create_or_update_organization_definitions_request_Input {
  """The array of custom properties to create or update."""
  properties: [Organization_Custom_Property_Input]!
}

"""Custom property defined on an organization"""
input Organization_Custom_Property_Input {
  """The name of the property"""
  property_name: String!

  """
  The URL that can be used to fetch, update, or delete info about this property via the API.
  """
  url: URL
  source_type: query_orgs_custom_properties_for_repos_get_organization_definitions_items_source_type
  value_type: query_orgs_custom_properties_for_repos_get_organization_definitions_items_value_type!

  """Whether the property is required."""
  required: Boolean
  default_value: [String]

  """Short description of the property"""
  description: String

  """
  An ordered list of the allowed values of the property.
  The property can have up to 200 allowed values.
  """
  allowed_values: [String]
  values_editable_by: query_orgs_custom_properties_for_repos_get_organization_definitions_items_values_editable_by

  """Whether setting properties values is mandatory"""
  require_explicit_values: Boolean
}

"""Custom property set payload"""
input Custom_Property_Set_Payload_Input {
  value_type: mutationInput_orgs_custom_properties_for_repos_create_or_update_organization_definition_input_value_type!

  """Whether the property is required."""
  required: Boolean
  default_value: [String]

  """Short description of the property"""
  description: String

  """
  An ordered list of the allowed values of the property.
  The property can have up to 200 allowed values.
  """
  allowed_values: [String]
  values_editable_by: mutationInput_orgs_custom_properties_for_repos_create_or_update_organization_definition_input_values_editable_by

  """Whether setting properties values is mandatory"""
  require_explicit_values: Boolean
}

"""The type of the value for the property"""
enum mutationInput_orgs_custom_properties_for_repos_create_or_update_organization_definition_input_value_type {
  string
  single_select
  multi_select
  true_false
  url
}

"""Who can edit the values of the property"""
enum mutationInput_orgs_custom_properties_for_repos_create_or_update_organization_definition_input_values_editable_by {
  org_actors
  org_and_repo_actors
}

input orgs_custom_properties_for_repos_create_or_update_organization_values_request_Input {
  """
  The names of repositories that the custom property values will be applied to.
  """
  repository_names: [String]!

  """
  List of custom property names and associated values to apply to the repositories.
  """
  properties: [Custom_Property_Value_Input]!
}

"""Custom property name and associated value"""
input Custom_Property_Value_Input {
  """The name of the property"""
  property_name: String!
  value: [String]
}

input repos_create_in_org_request_Input {
  """The name of the repository."""
  name: String!

  """A short description of the repository."""
  description: String

  """A URL with more information about the repository."""
  homepage: String

  """Whether the repository is private."""
  private: Boolean
  visibility: mutationInput_repos_create_in_org_input_visibility

  """
  Either `true` to enable issues for this repository or `false` to disable them.
  """
  has_issues: Boolean = true

  """
  Either `true` to enable projects for this repository or `false` to disable them. **Note:** If you're creating a repository in an organization that has disabled repository projects, the default is `false`, and if you pass `true`, the API returns an error.
  """
  has_projects: Boolean = true

  """
  Either `true` to enable the wiki for this repository or `false` to disable it.
  """
  has_wiki: Boolean = true

  """Whether downloads are enabled."""
  has_downloads: Boolean = true

  """
  Either `true` to make this repo available as a template repository or `false` to prevent it.
  """
  is_template: Boolean

  """
  The id of the team that will be granted access to this repository. This is only valid when creating a repository in an organization.
  """
  team_id: Int

  """Pass `true` to create an initial commit with empty README."""
  auto_init: Boolean

  """
  Desired language or platform [.gitignore template](https://github.com/github/gitignore) to apply. Use the name of the template without the extension. For example, "Haskell".
  """
  gitignore_template: String

  """
  Choose an [open source license template](https://choosealicense.com/) that best suits your needs, and then use the [license keyword](https://docs.github.com/articles/licensing-a-repository/#searching-github-by-license-type) as the `license_template` string. For example, "mit" or "mpl-2.0".
  """
  license_template: String

  """
  Either `true` to allow squash-merging pull requests, or `false` to prevent squash-merging.
  """
  allow_squash_merge: Boolean = true

  """
  Either `true` to allow merging pull requests with a merge commit, or `false` to prevent merging pull requests with merge commits.
  """
  allow_merge_commit: Boolean = true

  """
  Either `true` to allow rebase-merging pull requests, or `false` to prevent rebase-merging.
  """
  allow_rebase_merge: Boolean = true

  """
  Either `true` to allow auto-merge on pull requests, or `false` to disallow auto-merge.
  """
  allow_auto_merge: Boolean

  """
  Either `true` to allow automatically deleting head branches when pull requests are merged, or `false` to prevent automatic deletion. **The authenticated user must be an organization owner to set this property to `true`.**
  """
  delete_branch_on_merge: Boolean

  """
  Either `true` to allow squash-merge commits to use pull request title, or `false` to use commit message. **This property is closing down. Please use `squash_merge_commit_title` instead.
  """
  use_squash_pr_title_as_default: Boolean
  squash_merge_commit_title: mutationInput_repos_create_in_org_input_squash_merge_commit_title
  squash_merge_commit_message: mutationInput_repos_create_in_org_input_squash_merge_commit_message
  merge_commit_title: mutationInput_repos_create_in_org_input_merge_commit_title
  merge_commit_message: mutationInput_repos_create_in_org_input_merge_commit_message

  """
  The custom properties for the new repository. The keys are the custom property names, and the values are the corresponding custom property values.
  """
  custom_properties: JSON
}

"""The visibility of the repository."""
enum mutationInput_repos_create_in_org_input_visibility {
  public
  private
}

"""
Required when using `squash_merge_commit_message`.

The default value for a squash merge commit title:

- `PR_TITLE` - default to the pull request's title.
- `COMMIT_OR_PR_TITLE` - default to the commit's title (if only one commit) or the pull request's title (when more than one commit).
"""
enum mutationInput_repos_create_in_org_input_squash_merge_commit_title {
  PR_TITLE
  COMMIT_OR_PR_TITLE
}

"""
The default value for a squash merge commit message:

- `PR_BODY` - default to the pull request's body.
- `COMMIT_MESSAGES` - default to the branch's commit messages.
- `BLANK` - default to a blank commit message.
"""
enum mutationInput_repos_create_in_org_input_squash_merge_commit_message {
  PR_BODY
  COMMIT_MESSAGES
  BLANK
}

"""
Required when using `merge_commit_message`.

The default value for a merge commit title.

- `PR_TITLE` - default to the pull request's title.
- `MERGE_MESSAGE` - default to the classic title for a merge message (e.g., Merge pull request #123 from branch-name).
"""
enum mutationInput_repos_create_in_org_input_merge_commit_title {
  PR_TITLE
  MERGE_MESSAGE
}

"""
The default value for a merge commit message.

- `PR_TITLE` - default to the pull request's title.
- `PR_BODY` - default to the pull request's body.
- `BLANK` - default to a blank commit message.
"""
enum mutationInput_repos_create_in_org_input_merge_commit_message {
  PR_BODY
  PR_TITLE
  BLANK
}

input repos_create_org_ruleset_request_Input {
  """The name of the ruleset."""
  name: String!
  target: mutationInput_repos_create_org_ruleset_input_target = branch
  enforcement: repository_rule_enforcement!

  """The actors that can bypass the rules in this ruleset"""
  bypass_actors: [Repository_Ruleset_Bypass_Actor_Input]
  conditions: Organization_ruleset_conditions_Input

  """An array of rules within the ruleset."""
  rules: [org_rules_Input]
}

"""The target of the ruleset"""
enum mutationInput_repos_create_org_ruleset_input_target {
  branch
  tag
  push
  repository
}

"""An actor that can bypass rules in a ruleset"""
input Repository_Ruleset_Bypass_Actor_Input {
  """
  The ID of the actor that can bypass a ruleset. Required for `Integration`, `RepositoryRole`, and `Team` actor types. If `actor_type` is `OrganizationAdmin`, `actor_id` is ignored. If `actor_type` is `DeployKey`, this should be null. `OrganizationAdmin` is not applicable for personal repositories.
  """
  actor_id: Int
  actor_type: query_repos_get_org_rulesets_items_bypass_actors_items_actor_type!
  bypass_mode: query_repos_get_org_rulesets_items_bypass_actors_items_bypass_mode = always
}

input Organization_ruleset_conditions_Input {
  repository_name_and_ref_name_Input: repository_name_and_ref_name_Input
  repository_id_and_ref_name_Input: repository_id_and_ref_name_Input
  repository_property_and_ref_name_Input: repository_property_and_ref_name_Input
}

"""Conditions to target repositories by name and refs by name"""
input repository_name_and_ref_name_Input {
  ref_name: query_repos_get_org_rulesets_items_conditions_anyOf_0_ref_name_Input
  repository_name: query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_0_allOf_1_repository_name_Input!
}

input query_repos_get_org_rulesets_items_conditions_anyOf_0_ref_name_Input {
  """
  Array of ref names or patterns to include. One of these patterns must match for the condition to pass. Also accepts `~DEFAULT_BRANCH` to include the default branch or `~ALL` to include all branches.
  """
  include: [String]

  """
  Array of ref names or patterns to exclude. The condition will not pass if any of these patterns match.
  """
  exclude: [String]
}

input query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_0_allOf_1_repository_name_Input {
  """
  Array of repository names or patterns to include. One of these patterns must match for the condition to pass. Also accepts `~ALL` to include all repositories.
  """
  include: [String]

  """
  Array of repository names or patterns to exclude. The condition will not pass if any of these patterns match.
  """
  exclude: [String]

  """Whether renaming of target repositories is prevented."""
  protected: Boolean
}

"""Conditions to target repositories by id and refs by name"""
input repository_id_and_ref_name_Input {
  ref_name: query_repos_get_org_rulesets_items_conditions_anyOf_0_ref_name_Input
  repository_id: query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_1_allOf_1_repository_id_Input!
}

input query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_1_allOf_1_repository_id_Input {
  """
  The repository IDs that the ruleset applies to. One of these IDs must match for the condition to pass.
  """
  repository_ids: [Int]
}

"""Conditions to target repositories by property and refs by name"""
input repository_property_and_ref_name_Input {
  ref_name: query_repos_get_org_rulesets_items_conditions_anyOf_0_ref_name_Input
  repository_property: query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_2_allOf_1_repository_property_Input!
}

input query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_2_allOf_1_repository_property_Input {
  """
  The repository properties and values to include. All of these properties must match for the condition to pass.
  """
  include: [Repository_ruleset_property_targeting_definition_Input]

  """
  The repository properties and values to exclude. The condition will not pass if any of these properties match.
  """
  exclude: [Repository_ruleset_property_targeting_definition_Input]
}

"""Parameters for a targeting a repository property"""
input Repository_ruleset_property_targeting_definition_Input {
  """The name of the repository property to target"""
  name: String!

  """The values to match for the repository property"""
  property_values: [String]!
  source: query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_2_allOf_1_repository_property_include_items_source
}

"""
The source of the repository property. Defaults to 'custom' if not specified.
"""
enum query_repos_get_org_rulesets_items_conditions_anyOf_1_oneOf_2_allOf_1_repository_property_include_items_source {
  custom
  system
}

input org_rules_Input {
  creation_Input: creation_Input
  update_Input: update_Input
  deletion_Input: deletion_Input
  required_linear_history_Input: required_linear_history_Input
  required_deployments_Input: required_deployments_Input
  required_signatures_Input: required_signatures_Input
  pull_request_Input: pull_request_Input
  required_status_checks_Input: required_status_checks_Input
  non_fast_forward_Input: non_fast_forward_Input
  commit_message_pattern_Input: commit_message_pattern_Input
  commit_author_email_pattern_Input: commit_author_email_pattern_Input
  committer_email_pattern_Input: committer_email_pattern_Input
  branch_name_pattern_Input: branch_name_pattern_Input
  tag_name_pattern_Input: tag_name_pattern_Input
  file_path_restriction_Input: file_path_restriction_Input
  max_file_path_length_Input: max_file_path_length_Input
  file_extension_restriction_Input: file_extension_restriction_Input
  max_file_size_Input: max_file_size_Input
  workflows_Input: workflows_Input
  code_scanning_Input: code_scanning_Input
  copilot_code_review_Input: copilot_code_review_Input
}

"""Only allow users with bypass permission to create matching refs."""
input creation_Input {
  type: creation_const!
}

"""Only allow users with bypass permission to update matching refs."""
input update_Input {
  type: update_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_1_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_1_parameters_Input {
  """Branch can pull changes from its upstream repository"""
  update_allows_fetch_and_merge: Boolean!
}

"""Only allow users with bypass permissions to delete matching refs."""
input deletion_Input {
  type: deletion_const!
}

"""Prevent merge commits from being pushed to matching refs."""
input required_linear_history_Input {
  type: required_linear_history_const!
}

"""
Choose which environments must be successfully deployed to before refs can be pushed into a ref that matches this rule.
"""
input required_deployments_Input {
  type: required_deployments_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_5_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_5_parameters_Input {
  """
  The environments that must be successfully deployed to before branches can be merged.
  """
  required_deployment_environments: [String]!
}

"""Commits pushed to matching refs must have verified signatures."""
input required_signatures_Input {
  type: required_signatures_const!
}

"""
Require all commits be made to a non-target branch and submitted via a pull request before they can be merged.
"""
input pull_request_Input {
  type: pull_request_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters_Input {
  """
  Array of allowed merge methods. Allowed values include `merge`, `squash`, and `rebase`. At least one option must be enabled.
  """
  allowed_merge_methods: [query_repos_get_org_rulesets_items_rules_items_oneOf_7_parameters_allowed_merge_methods_items]

  """
  New, reviewable commits pushed will dismiss previous pull request review approvals.
  """
  dismiss_stale_reviews_on_push: Boolean!

  """
  Require an approving review in pull requests that modify files that have a designated code owner.
  """
  require_code_owner_review: Boolean!

  """
  Whether the most recent reviewable push must be approved by someone other than the person who pushed it.
  """
  require_last_push_approval: Boolean!

  """
  The number of approving reviews that are required before a pull request can be merged.
  """
  required_approving_review_count: NonNegativeInt!

  """
  All conversations on code must be resolved before a pull request can be merged.
  """
  required_review_thread_resolution: Boolean!

  """
  > [!NOTE]
  > `required_reviewers` is in beta and subject to change.
  
  A collection of reviewers and associated file patterns. Each reviewer has a list of file patterns which determine the files that reviewer is required to review.
  """
  required_reviewers: [RequiredReviewerConfiguration_Input]
}

"""
A reviewing team, and file patterns describing which files they must approve changes to.
"""
input RequiredReviewerConfiguration_Input {
  """
  Array of file patterns. Pull requests which change matching files must be approved by the specified team. File patterns use fnmatch syntax.
  """
  file_patterns: [String]!

  """
  Minimum number of approvals required from the specified team. If set to zero, the team will be added to the pull request but approval is optional.
  """
  minimum_approvals: Int!
  reviewer: Reviewer_Input!
}

"""A required reviewing team"""
input Reviewer_Input {
  """ID of the reviewer which must review changes to matching files."""
  id: Int!
  type: Team_const!
}

"""
Choose which status checks must pass before the ref is updated. When enabled, commits must first be pushed to another ref where the checks pass.
"""
input required_status_checks_Input {
  type: required_status_checks_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_8_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_8_parameters_Input {
  """
  Allow repositories and branches to be created if a check would otherwise prohibit it.
  """
  do_not_enforce_on_create: Boolean

  """Status checks that are required."""
  required_status_checks: [StatusCheckConfiguration_Input]!

  """
  Whether pull requests targeting a matching branch must be tested with the latest code. This setting will not take effect unless at least one status check is enabled.
  """
  strict_required_status_checks_policy: Boolean!
}

"""Required status check"""
input StatusCheckConfiguration_Input {
  """The status check context name that must be present on the commit."""
  context: String!

  """
  The optional integration ID that this status check must originate from.
  """
  integration_id: Int
}

"""Prevent users with push access from force pushing to refs."""
input non_fast_forward_Input {
  type: non_fast_forward_const!
}

"""Parameters to be used for the commit_message_pattern rule"""
input commit_message_pattern_Input {
  type: commit_message_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters_Input {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_10_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""Parameters to be used for the commit_author_email_pattern rule"""
input commit_author_email_pattern_Input {
  type: commit_author_email_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters_Input {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_11_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""Parameters to be used for the committer_email_pattern rule"""
input committer_email_pattern_Input {
  type: committer_email_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters_Input {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_12_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""Parameters to be used for the branch_name_pattern rule"""
input branch_name_pattern_Input {
  type: branch_name_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters_Input {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_13_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""Parameters to be used for the tag_name_pattern rule"""
input tag_name_pattern_Input {
  type: tag_name_pattern_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters_Input {
  """How this rule appears when configuring it."""
  name: String

  """If true, the rule will fail if the pattern matches."""
  negate: Boolean
  operator: query_repos_get_org_rulesets_items_rules_items_oneOf_14_parameters_operator!

  """The pattern to match with."""
  pattern: String!
}

"""
Prevent commits that include changes in specified file and folder paths from being pushed to the commit graph. This includes absolute paths that contain file names.
"""
input file_path_restriction_Input {
  type: file_path_restriction_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_15_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_15_parameters_Input {
  """
  The file paths that are restricted from being pushed to the commit graph.
  """
  restricted_file_paths: [String]!
}

"""
Prevent commits that include file paths that exceed the specified character limit from being pushed to the commit graph.
"""
input max_file_path_length_Input {
  type: max_file_path_length_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_16_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_16_parameters_Input {
  """The maximum amount of characters allowed in file paths."""
  max_file_path_length: PositiveInt!
}

"""
Prevent commits that include files with specified file extensions from being pushed to the commit graph.
"""
input file_extension_restriction_Input {
  type: file_extension_restriction_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_17_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_17_parameters_Input {
  """
  The file extensions that are restricted from being pushed to the commit graph.
  """
  restricted_file_extensions: [String]!
}

"""
Prevent commits with individual files that exceed the specified limit from being pushed to the commit graph.
"""
input max_file_size_Input {
  type: max_file_size_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_18_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_18_parameters_Input {
  """
  The maximum file size allowed in megabytes. This limit does not apply to Git Large File Storage (Git LFS).
  """
  max_file_size: PositiveInt!
}

"""
Require all changes made to a targeted branch to pass the specified workflows before they can be merged.
"""
input workflows_Input {
  type: workflows_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_19_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_19_parameters_Input {
  """
  Allow repositories and branches to be created if a check would otherwise prohibit it.
  """
  do_not_enforce_on_create: Boolean

  """Workflows that must pass for this rule to pass."""
  workflows: [WorkflowFileReference_Input]!
}

"""A workflow that must run for this rule to pass"""
input WorkflowFileReference_Input {
  """The path to the workflow file"""
  path: String!

  """The ref (branch or tag) of the workflow file to use"""
  ref: String

  """The ID of the repository where the workflow is defined"""
  repository_id: Int!

  """The commit SHA of the workflow file to use"""
  sha: String
}

"""
Choose which tools must provide code scanning results before the reference is updated. When configured, code scanning must be enabled and have results for both the commit and the reference being updated.
"""
input code_scanning_Input {
  type: code_scanning_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_Input {
  """Tools that must provide code scanning results for this rule to pass."""
  code_scanning_tools: [CodeScanningTool_Input]!
}

"""A tool that must provide code scanning results for this rule to pass."""
input CodeScanningTool_Input {
  alerts_threshold: query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_code_scanning_tools_items_alerts_threshold!
  security_alerts_threshold: query_repos_get_org_rulesets_items_rules_items_oneOf_20_parameters_code_scanning_tools_items_security_alerts_threshold!

  """The name of a code scanning tool"""
  tool: String!
}

"""
Request Copilot code review for new pull requests automatically if the author has access to Copilot code review and their premium requests quota has not reached the limit.
"""
input copilot_code_review_Input {
  type: copilot_code_review_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_21_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_21_parameters_Input {
  """
  Copilot automatically reviews draft pull requests before they are marked as ready for review.
  """
  review_draft_pull_requests: Boolean

  """Copilot automatically reviews each new push to the pull request."""
  review_on_push: Boolean
}

input repos_update_org_ruleset_request_Input {
  """The name of the ruleset."""
  name: String
  target: mutationInput_repos_update_org_ruleset_input_target
  enforcement: repository_rule_enforcement

  """The actors that can bypass the rules in this ruleset"""
  bypass_actors: [Repository_Ruleset_Bypass_Actor_Input]
  conditions: Organization_ruleset_conditions_Input

  """An array of rules within the ruleset."""
  rules: [org_rules_Input]
}

"""The target of the ruleset"""
enum mutationInput_repos_update_org_ruleset_input_target {
  branch
  tag
  push
  repository
}

type secret_scanning_update_org_pattern_configs_200_response {
  """The updated pattern configuration version."""
  pattern_config_version: String
}

input secret_scanning_update_org_pattern_configs_request_Input {
  """
  The version of the entity. This is used to confirm you're updating the current version of the entity and mitigate unintentionally overriding someone else's update.
  """
  pattern_config_version: String

  """Pattern settings for provider patterns."""
  provider_pattern_settings: [mutationInput_secret_scanning_update_org_pattern_configs_input_provider_pattern_settings_items_Input]

  """Pattern settings for custom patterns."""
  custom_pattern_settings: [mutationInput_secret_scanning_update_org_pattern_configs_input_custom_pattern_settings_items_Input]
}

input mutationInput_secret_scanning_update_org_pattern_configs_input_provider_pattern_settings_items_Input {
  """The ID of the pattern to configure."""
  token_type: String
  push_protection_setting: mutationInput_secret_scanning_update_org_pattern_configs_input_provider_pattern_settings_items_push_protection_setting
}

"""Push protection setting to set for the pattern."""
enum mutationInput_secret_scanning_update_org_pattern_configs_input_provider_pattern_settings_items_push_protection_setting {
  not_set
  disabled
  enabled
}

input mutationInput_secret_scanning_update_org_pattern_configs_input_custom_pattern_settings_items_Input {
  """The ID of the pattern to configure."""
  token_type: String

  """
  The version of the entity. This is used to confirm you're updating the current version of the entity and mitigate unintentionally overriding someone else's update.
  """
  custom_pattern_version: String
  push_protection_setting: mutationInput_secret_scanning_update_org_pattern_configs_input_custom_pattern_settings_items_push_protection_setting
}

"""Push protection setting to set for the pattern."""
enum mutationInput_secret_scanning_update_org_pattern_configs_input_custom_pattern_settings_items_push_protection_setting {
  disabled
  enabled
}

input orgs_set_immutable_releases_settings_request_Input {
  enforced_repositories: mutationInput_orgs_set_immutable_releases_settings_input_enforced_repositories!

  """
  An array of repository ids for which immutable releases enforcement should be applied. You can only provide a list of repository ids when the `enforced_repositories` is set to `selected`. You can add and remove individual repositories using the [Enable a selected repository for immutable releases in an organization](https://docs.github.com/rest/orgs/orgs#enable-a-selected-repository-for-immutable-releases-in-an-organization) and [Disable a selected repository for immutable releases in an organization](https://docs.github.com/rest/orgs/orgs#disable-a-selected-repository-for-immutable-releases-in-an-organization) endpoints.
  """
  selected_repository_ids: [Int]
}

"""
The policy that controls how immutable releases are enforced in the organization.
"""
enum mutationInput_orgs_set_immutable_releases_settings_input_enforced_repositories {
  all
  none
  selected
}

input orgs_set_immutable_releases_settings_repositories_request_Input {
  """
  An array of repository ids for which immutable releases enforcement should be applied. You can only provide a list of repository ids when the `enforced_repositories` is set to `selected`. You can add and remove individual repositories using the [Enable a selected repository for immutable releases in an organization](https://docs.github.com/rest/orgs/orgs#enable-a-selected-repository-for-immutable-releases-in-an-organization) and [Disable a selected repository for immutable releases in an organization](https://docs.github.com/rest/orgs/orgs#disable-a-selected-repository-for-immutable-releases-in-an-organization) endpoints.
  """
  selected_repository_ids: [Int]!
}

input hosted_compute_create_network_configuration_for_org_request_Input {
  """
  Name of the network configuration. Must be between 1 and 100 characters and may only contain upper and lowercase letters a-z, numbers 0-9, '.', '-', and '_'.
  """
  name: String!
  compute_service: mutationInput_hosted_compute_create_network_configuration_for_org_input_compute_service

  """
  A list of identifiers of the network settings resources to use for the network configuration. Exactly one resource identifier must be specified in the list.
  """
  network_settings_ids: [String]!
}

"""The hosted compute service to use for the network configuration."""
enum mutationInput_hosted_compute_create_network_configuration_for_org_input_compute_service {
  none
  actions
}

input hosted_compute_update_network_configuration_for_org_request_Input {
  """
  Name of the network configuration. Must be between 1 and 100 characters and may only contain upper and lowercase letters a-z, numbers 0-9, '.', '-', and '_'.
  """
  name: String
  compute_service: mutationInput_hosted_compute_update_network_configuration_for_org_input_compute_service

  """
  A list of identifiers of the network settings resources to use for the network configuration. Exactly one resource identifier must be specified in the list.
  """
  network_settings_ids: [String]
}

"""The hosted compute service to use for the network configuration."""
enum mutationInput_hosted_compute_update_network_configuration_for_org_input_compute_service {
  none
  actions
}

input teams_create_request_Input {
  """The name of the team."""
  name: String!

  """The description of the team."""
  description: String

  """
  List GitHub usernames for organization members who will become team maintainers.
  """
  maintainers: [String]

  """
  The full name (e.g., "organization-name/repository-name") of repositories to add the team to.
  """
  repo_names: [String]
  privacy: mutationInput_teams_create_input_privacy
  notification_setting: mutationInput_teams_create_input_notification_setting
  permission: mutationInput_teams_create_input_permission = pull

  """The ID of a team to set as the parent team."""
  parent_team_id: Int
}

"""
The level of privacy this team should have. The options are:  
**For a non-nested team:**  
 * `secret` - only visible to organization owners and members of this team.  
 * `closed` - visible to all members of this organization.  
Default: `secret`  
**For a parent or child team:**  
 * `closed` - visible to all members of this organization.  
Default for child team: `closed`
"""
enum mutationInput_teams_create_input_privacy {
  secret
  closed
}

"""
The notification setting the team has chosen. The options are:  
 * `notifications_enabled` - team members receive notifications when the team is @mentioned.  
 * `notifications_disabled` - no one receives notifications.  
Default: `notifications_enabled`
"""
enum mutationInput_teams_create_input_notification_setting {
  notifications_enabled
  notifications_disabled
}

"""
**Closing down notice**. The permission that new repositories will be added to the team with when none is specified.
"""
enum mutationInput_teams_create_input_permission {
  pull
  push
}

input teams_update_in_org_request_Input {
  """The name of the team."""
  name: String

  """The description of the team."""
  description: String
  privacy: mutationInput_teams_update_in_org_input_privacy
  notification_setting: mutationInput_teams_update_in_org_input_notification_setting
  permission: mutationInput_teams_update_in_org_input_permission = pull

  """The ID of a team to set as the parent team."""
  parent_team_id: Int
}

"""
The level of privacy this team should have. Editing teams without specifying this parameter leaves `privacy` intact. When a team is nested, the `privacy` for parent teams cannot be `secret`. The options are:  
**For a non-nested team:**  
 * `secret` - only visible to organization owners and members of this team.  
 * `closed` - visible to all members of this organization.  
**For a parent or child team:**  
 * `closed` - visible to all members of this organization.
"""
enum mutationInput_teams_update_in_org_input_privacy {
  secret
  closed
}

"""
The notification setting the team has chosen. Editing teams without specifying this parameter leaves `notification_setting` intact. The options are: 
 * `notifications_enabled` - team members receive notifications when the team is @mentioned.  
 * `notifications_disabled` - no one receives notifications.
"""
enum mutationInput_teams_update_in_org_input_notification_setting {
  notifications_enabled
  notifications_disabled
}

"""
**Closing down notice**. The permission that new repositories will be added to the team with when none is specified.
"""
enum mutationInput_teams_update_in_org_input_permission {
  pull
  push
  admin
}

input teams_add_or_update_membership_for_user_in_org_request_Input {
  role: mutationInput_teams_add_or_update_membership_for_user_in_org_input_role = member
}

"""The role that this user should have in the team."""
enum mutationInput_teams_add_or_update_membership_for_user_in_org_input_role {
  member
  maintainer
}

input teams_add_or_update_repo_permissions_in_org_request_Input {
  """
  The permission to grant the team on this repository. We accept the following permissions to be set: `pull`, `triage`, `push`, `maintain`, `admin` and you can also specify a custom repository role name, if the owning organization has defined any. If no permission is specified, the team's `permission` attribute will be used to determine what permission to grant the team on this repository.
  """
  permission: String
}

"""The security feature to enable or disable."""
enum security_product_parameter {
  dependency_graph
  dependabot_alerts
  dependabot_security_updates
  advanced_security
  code_scanning_default_setup
  secret_scanning
  secret_scanning_push_protection
}

"""
The action to take.

`enable_all` means to enable the specified security feature for all repositories in the organization.
`disable_all` means to disable the specified security feature for all repositories in the organization.
"""
enum org_security_product_enablement_parameter {
  enable_all
  disable_all
}

input orgs_enable_or_disable_security_product_on_all_org_repos_request_Input {
  query_suite: mutationInput_orgs_enable_or_disable_security_product_on_all_org_repos_input_query_suite
}

"""
CodeQL query suite to be used. If you specify the `query_suite` parameter, the default setup will be configured with this query suite only on all repositories that didn't have default setup already configured. It will not change the query suite on repositories that already have default setup configured.
If you don't specify any `query_suite` in your request, the preferred query suite of the organization will be applied.
"""
enum mutationInput_orgs_enable_or_disable_security_product_on_all_org_repos_input_query_suite {
  default
  extended
}

input repos_update_request_Input {
  """The name of the repository."""
  name: String

  """A short description of the repository."""
  description: String

  """A URL with more information about the repository."""
  homepage: String

  """
  Either `true` to make the repository private or `false` to make it public. Default: `false`.  
  **Note**: You will get a `422` error if the organization restricts [changing repository visibility](https://docs.github.com/articles/repository-permission-levels-for-an-organization#changing-the-visibility-of-repositories) to organization owners and a non-owner tries to change the value of private.
  """
  private: Boolean
  visibility: mutationInput_repos_update_input_visibility
  security_and_analysis: mutationInput_repos_update_input_security_and_analysis_Input

  """
  Either `true` to enable issues for this repository or `false` to disable them.
  """
  has_issues: Boolean = true

  """
  Either `true` to enable projects for this repository or `false` to disable them. **Note:** If you're creating a repository in an organization that has disabled repository projects, the default is `false`, and if you pass `true`, the API returns an error.
  """
  has_projects: Boolean = true

  """
  Either `true` to enable the wiki for this repository or `false` to disable it.
  """
  has_wiki: Boolean = true

  """
  Either `true` to make this repo available as a template repository or `false` to prevent it.
  """
  is_template: Boolean

  """Updates the default branch for this repository."""
  default_branch: String

  """
  Either `true` to allow squash-merging pull requests, or `false` to prevent squash-merging.
  """
  allow_squash_merge: Boolean = true

  """
  Either `true` to allow merging pull requests with a merge commit, or `false` to prevent merging pull requests with merge commits.
  """
  allow_merge_commit: Boolean = true

  """
  Either `true` to allow rebase-merging pull requests, or `false` to prevent rebase-merging.
  """
  allow_rebase_merge: Boolean = true

  """
  Either `true` to allow auto-merge on pull requests, or `false` to disallow auto-merge.
  """
  allow_auto_merge: Boolean

  """
  Either `true` to allow automatically deleting head branches when pull requests are merged, or `false` to prevent automatic deletion.
  """
  delete_branch_on_merge: Boolean

  """
  Either `true` to always allow a pull request head branch that is behind its base branch to be updated even if it is not required to be up to date before merging, or false otherwise.
  """
  allow_update_branch: Boolean

  """
  Either `true` to allow squash-merge commits to use pull request title, or `false` to use commit message. **This property is closing down. Please use `squash_merge_commit_title` instead.
  """
  use_squash_pr_title_as_default: Boolean
  squash_merge_commit_title: mutationInput_repos_update_input_squash_merge_commit_title
  squash_merge_commit_message: mutationInput_repos_update_input_squash_merge_commit_message
  merge_commit_title: mutationInput_repos_update_input_merge_commit_title
  merge_commit_message: mutationInput_repos_update_input_merge_commit_message

  """
  Whether to archive this repository. `false` will unarchive a previously archived repository.
  """
  archived: Boolean

  """
  Either `true` to allow private forks, or `false` to prevent private forks.
  """
  allow_forking: Boolean

  """
  Either `true` to require contributors to sign off on web-based commits, or `false` to not require contributors to sign off on web-based commits.
  """
  web_commit_signoff_required: Boolean
}

"""The visibility of the repository."""
enum mutationInput_repos_update_input_visibility {
  public
  private
}

"""
Specify which security and analysis features to enable or disable for the repository.

To use this parameter, you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."

For example, to enable GitHub Advanced Security, use this data in the body of the `PATCH` request:
`{ "security_and_analysis": {"advanced_security": { "status": "enabled" } } }`.

You can check which security and analysis features are currently enabled by using a `GET /repos/{owner}/{repo}` request.
"""
input mutationInput_repos_update_input_security_and_analysis_Input {
  advanced_security: mutationInput_repos_update_input_security_and_analysis_advanced_security_Input
  code_security: mutationInput_repos_update_input_security_and_analysis_code_security_Input
  secret_scanning: mutationInput_repos_update_input_security_and_analysis_secret_scanning_Input
  secret_scanning_push_protection: mutationInput_repos_update_input_security_and_analysis_secret_scanning_push_protection_Input
  secret_scanning_ai_detection: mutationInput_repos_update_input_security_and_analysis_secret_scanning_ai_detection_Input
  secret_scanning_non_provider_patterns: mutationInput_repos_update_input_security_and_analysis_secret_scanning_non_provider_patterns_Input
  secret_scanning_delegated_alert_dismissal: mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_alert_dismissal_Input
  secret_scanning_delegated_bypass: mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_Input
  secret_scanning_delegated_bypass_options: mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_options_Input
}

"""
Use the `status` property to enable or disable GitHub Advanced Security for this repository.
For more information, see "[About GitHub Advanced
Security](/github/getting-started-with-github/learning-about-github/about-github-advanced-security)."

For standalone Code Scanning or Secret Protection products, this parameter cannot be used.
"""
input mutationInput_repos_update_input_security_and_analysis_advanced_security_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Use the `status` property to enable or disable GitHub Code Security for this repository.
"""
input mutationInput_repos_update_input_security_and_analysis_code_security_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Use the `status` property to enable or disable secret scanning for this repository. For more information, see "[About secret scanning](/code-security/secret-security/about-secret-scanning)."
"""
input mutationInput_repos_update_input_security_and_analysis_secret_scanning_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Use the `status` property to enable or disable secret scanning push protection for this repository. For more information, see "[Protecting pushes with secret scanning](/code-security/secret-scanning/protecting-pushes-with-secret-scanning)."
"""
input mutationInput_repos_update_input_security_and_analysis_secret_scanning_push_protection_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Use the `status` property to enable or disable secret scanning AI detection for this repository. For more information, see "[Responsible detection of generic secrets with AI](https://docs.github.com/code-security/secret-scanning/using-advanced-secret-scanning-and-push-protection-features/generic-secret-detection/responsible-ai-generic-secrets)."
"""
input mutationInput_repos_update_input_security_and_analysis_secret_scanning_ai_detection_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Use the `status` property to enable or disable secret scanning non-provider patterns for this repository. For more information, see "[Supported secret scanning patterns](/code-security/secret-scanning/introduction/supported-secret-scanning-patterns#supported-secrets)."
"""
input mutationInput_repos_update_input_security_and_analysis_secret_scanning_non_provider_patterns_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Use the `status` property to enable or disable secret scanning delegated alert dismissal for this repository.
"""
input mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_alert_dismissal_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Use the `status` property to enable or disable secret scanning delegated bypass for this repository.
"""
input mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_Input {
  """Can be `enabled` or `disabled`."""
  status: String
}

"""
Feature options for secret scanning delegated bypass.
This object is only honored when `security_and_analysis.secret_scanning_delegated_bypass.status` is set to `enabled`.
You can send this object in the same request as `secret_scanning_delegated_bypass`, or update just the options in a separate request.
"""
input mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_options_Input {
  """
  The bypass reviewers for secret scanning delegated bypass.
  If you omit this field, the existing set of reviewers is unchanged.
  """
  reviewers: [mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items_Input]
}

input mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items_Input {
  """The ID of the team or role selected as a bypass reviewer"""
  reviewer_id: Int!
  reviewer_type: mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type!
}

"""The type of the bypass reviewer"""
enum mutationInput_repos_update_input_security_and_analysis_secret_scanning_delegated_bypass_options_reviewers_items_reviewer_type {
  TEAM
  ROLE
}

"""
Required when using `squash_merge_commit_message`.

The default value for a squash merge commit title:

- `PR_TITLE` - default to the pull request's title.
- `COMMIT_OR_PR_TITLE` - default to the commit's title (if only one commit) or the pull request's title (when more than one commit).
"""
enum mutationInput_repos_update_input_squash_merge_commit_title {
  PR_TITLE
  COMMIT_OR_PR_TITLE
}

"""
The default value for a squash merge commit message:

- `PR_BODY` - default to the pull request's body.
- `COMMIT_MESSAGES` - default to the branch's commit messages.
- `BLANK` - default to a blank commit message.
"""
enum mutationInput_repos_update_input_squash_merge_commit_message {
  PR_BODY
  COMMIT_MESSAGES
  BLANK
}

"""
Required when using `merge_commit_message`.

The default value for a merge commit title.

- `PR_TITLE` - default to the pull request's title.
- `MERGE_MESSAGE` - default to the classic title for a merge message (e.g., Merge pull request #123 from branch-name).
"""
enum mutationInput_repos_update_input_merge_commit_title {
  PR_TITLE
  MERGE_MESSAGE
}

"""
The default value for a merge commit message.

- `PR_TITLE` - default to the pull request's title.
- `PR_BODY` - default to the pull request's body.
- `BLANK` - default to a blank commit message.
"""
enum mutationInput_repos_update_input_merge_commit_message {
  PR_BODY
  PR_TITLE
  BLANK
}

"""GitHub Actions cache retention policy for a repository."""
input Actions_cache_retention_limit_for_a_repository_Input {
  """The maximum number of days to keep caches in this repository."""
  max_cache_retention_days: Int
}

"""GitHub Actions cache storage policy for a repository."""
input Actions_cache_storage_limit_for_a_repository_Input {
  """The maximum total cache size for this repository, in gigabytes."""
  max_cache_size_gb: Int
}

input actions_re_run_job_for_workflow_run_request_Input {
  """Whether to enable debug logging for the re-run."""
  enable_debug_logging: Boolean
}

"""Actions OIDC subject customization for a repository"""
input mutationInput_actions_set_custom_oidc_sub_claim_for_repo_input_Input {
  """
  Whether to use the default template or not. If `true`, the `include_claim_keys` field is ignored.
  """
  use_default: Boolean!

  """
  Array of unique strings. Each claim key can only contain alphanumeric characters and underscores.
  """
  include_claim_keys: [String]
}

input actions_set_github_actions_permissions_repository_request_Input {
  """Whether GitHub Actions is enabled on the repository."""
  enabled: Boolean!
  allowed_actions: allowed_actions

  """Whether actions must be pinned to a full-length commit SHA."""
  sha_pinning_required: Boolean
}

input actions_workflow_access_to_repository_Input {
  access_level: query_actions_get_workflow_access_to_repository_access_level!
}

input actions_generate_runner_jitconfig_for_repo_request_Input {
  """The name of the new runner."""
  name: String!

  """The ID of the runner group to register the runner to."""
  runner_group_id: Int!

  """
  The names of the custom labels to add to the runner. **Minimum items**: 1. **Maximum items**: 100.
  """
  labels: [String]!

  """
  The working directory to be used for job execution, relative to the runner install directory.
  """
  work_folder: String = "_work"
}

input actions_add_custom_labels_to_self_hosted_runner_for_repo_request_Input {
  """The names of the custom labels to add to the runner."""
  labels: [String]!
}

input actions_set_custom_labels_for_self_hosted_runner_for_repo_request_Input {
  """
  The names of the custom labels to set for the runner. You can pass an empty array to remove all custom labels.
  """
  labels: [String]!
}

input actions_review_custom_gates_for_run_request_Input {
  """The name of the environment to approve or reject."""
  environment_name: String!

  """
  Comment associated with the pending deployment protection rule. **Required when state is not provided.**
  """
  comment: String!
  state: mutationInput_actions_review_custom_gates_for_run_input_anyOf_1_state!
}

"""Whether to approve or reject deployment to the specified environments."""
enum mutationInput_actions_review_custom_gates_for_run_input_anyOf_1_state {
  approved
  rejected
}

input actions_review_pending_deployments_for_run_request_Input {
  """The list of environment ids to approve or reject"""
  environment_ids: [Int]!
  state: mutationInput_actions_review_pending_deployments_for_run_input_state!

  """A comment to accompany the deployment review"""
  comment: String!
}

"""Whether to approve or reject deployment to the specified environments."""
enum mutationInput_actions_review_pending_deployments_for_run_input_state {
  approved
  rejected
}

input actions_re_run_workflow_request_Input {
  """Whether to enable debug logging for the re-run."""
  enable_debug_logging: Boolean
}

input actions_re_run_workflow_failed_jobs_request_Input {
  """Whether to enable debug logging for the re-run."""
  enable_debug_logging: Boolean
}

union actions_create_or_update_repo_secret_response = JSON_container | Void_container

input actions_create_or_update_repo_secret_request_Input {
  encrypted_value: mutationInput_actions_create_or_update_repo_secret_input_encrypted_value!

  """ID of the key you used to encrypt the secret."""
  key_id: String!
}

scalar mutationInput_actions_create_or_update_repo_secret_input_encrypted_value

input actions_create_repo_variable_request_Input {
  """The name of the variable."""
  name: String!

  """The value of the variable."""
  value: String!
}

input actions_update_repo_variable_request_Input {
  """The name of the variable."""
  name: String

  """The value of the variable."""
  value: String
}

union actions_create_workflow_dispatch_response = Workflow_Dispatch_Response | Void_container

"""Response containing the workflow run ID and URLs."""
type Workflow_Dispatch_Response {
  """The ID of the workflow run."""
  workflow_run_id: BigInt!

  """The URL to the workflow run."""
  run_url: URL!
  html_url: URL!
}

input actions_create_workflow_dispatch_request_Input {
  """
  The git reference for the workflow. The reference can be a branch or tag name.
  """
  ref: String!

  """
  Input keys and values configured in the workflow file. The maximum number of properties is 25. Any default properties configured in the workflow file will be used when `inputs` are omitted.
  """
  inputs: JSON

  """Whether the response should include the workflow run ID and URLs."""
  return_run_details: Boolean
}

type repos_create_attestation_201_response {
  """The ID of the attestation."""
  id: Int
}

input repos_create_attestation_request_Input {
  bundle: mutationInput_repos_create_attestation_input_bundle_Input!
}

"""
The attestation's Sigstore Bundle.
Refer to the [Sigstore Bundle Specification](https://github.com/sigstore/protobuf-specs/blob/main/protos/sigstore_bundle.proto) for more information.
"""
input mutationInput_repos_create_attestation_input_bundle_Input {
  mediaType: String
  verificationMaterial: JSON
  dsseEnvelope: JSON
}

input repos_create_autolink_request_Input {
  """
  This prefix appended by certain characters will generate a link any time it is found in an issue, pull request, or commit.
  """
  key_prefix: String!

  """
  The URL must contain `<num>` for the reference number. `<num>` matches different characters depending on the value of `is_alphanumeric`.
  """
  url_template: String!

  """
  Whether this autolink reference matches alphanumeric characters. If true, the `<num>` parameter of the `url_template` matches alphanumeric characters `A-Z` (case insensitive), `0-9`, and `-`. If false, this autolink reference only matches numeric characters.
  """
  is_alphanumeric: Boolean = true
}

"""Branch protections protect branches"""
type Protected_Branch {
  url: URL!
  required_status_checks: Status_Check_Policy
  required_pull_request_reviews: mutation_repos_update_branch_protection_required_pull_request_reviews
  required_signatures: mutation_repos_update_branch_protection_required_signatures
  enforce_admins: mutation_repos_update_branch_protection_enforce_admins
  required_linear_history: mutation_repos_update_branch_protection_required_linear_history
  allow_force_pushes: mutation_repos_update_branch_protection_allow_force_pushes
  allow_deletions: mutation_repos_update_branch_protection_allow_deletions
  restrictions: Branch_Restriction_Policy
  required_conversation_resolution: mutation_repos_update_branch_protection_required_conversation_resolution
  block_creations: mutation_repos_update_branch_protection_block_creations
  lock_branch: mutation_repos_update_branch_protection_lock_branch
  allow_fork_syncing: mutation_repos_update_branch_protection_allow_fork_syncing
}

type mutation_repos_update_branch_protection_required_pull_request_reviews {
  url: URL!
  dismiss_stale_reviews: Boolean
  require_code_owner_reviews: Boolean
  required_approving_review_count: Int

  """
  Whether the most recent push must be approved by someone other than the person who pushed it.
  """
  require_last_push_approval: Boolean
  dismissal_restrictions: mutation_repos_update_branch_protection_required_pull_request_reviews_dismissal_restrictions
  bypass_pull_request_allowances: mutation_repos_update_branch_protection_required_pull_request_reviews_bypass_pull_request_allowances
}

type mutation_repos_update_branch_protection_required_pull_request_reviews_dismissal_restrictions {
  url: URL!
  users_url: URL!
  teams_url: URL!
  users: [simple_user]!
  teams: [team]!
  apps: [integration]
}

type mutation_repos_update_branch_protection_required_pull_request_reviews_bypass_pull_request_allowances {
  users: [simple_user]!
  teams: [team]!
  apps: [integration]
}

type mutation_repos_update_branch_protection_required_signatures {
  url: URL!
  enabled: Boolean!
}

type mutation_repos_update_branch_protection_enforce_admins {
  url: URL!
  enabled: Boolean!
}

type mutation_repos_update_branch_protection_required_linear_history {
  enabled: Boolean!
}

type mutation_repos_update_branch_protection_allow_force_pushes {
  enabled: Boolean!
}

type mutation_repos_update_branch_protection_allow_deletions {
  enabled: Boolean!
}

type mutation_repos_update_branch_protection_required_conversation_resolution {
  enabled: Boolean
}

type mutation_repos_update_branch_protection_block_creations {
  enabled: Boolean!
}

"""
Whether to set the branch as read-only. If this is true, users will not be able to push to the branch.
"""
type mutation_repos_update_branch_protection_lock_branch {
  enabled: Boolean
}

"""
Whether users can pull changes from upstream when the branch is locked. Set to `true` to allow fork syncing. Set to `false` to prevent fork syncing.
"""
type mutation_repos_update_branch_protection_allow_fork_syncing {
  enabled: Boolean
}

input repos_update_branch_protection_request_Input {
  required_status_checks: mutationInput_repos_update_branch_protection_input_required_status_checks_Input

  """
  Enforce all configured restrictions for administrators. Set to `true` to enforce required status checks for repository administrators. Set to `null` to disable.
  """
  enforce_admins: Boolean
  required_pull_request_reviews: mutationInput_repos_update_branch_protection_input_required_pull_request_reviews_Input
  restrictions: mutationInput_repos_update_branch_protection_input_restrictions_Input

  """
  Enforces a linear commit Git history, which prevents anyone from pushing merge commits to a branch. Set to `true` to enforce a linear commit history. Set to `false` to disable a linear commit Git history. Your repository must allow squash merging or rebase merging before you can enable a linear commit history. Default: `false`. For more information, see "[Requiring a linear commit history](https://docs.github.com/github/administering-a-repository/requiring-a-linear-commit-history)" in the GitHub Help documentation.
  """
  required_linear_history: Boolean

  """
  Permits force pushes to the protected branch by anyone with write access to the repository. Set to `true` to allow force pushes. Set to `false` or `null` to block force pushes. Default: `false`. For more information, see "[Enabling force pushes to a protected branch](https://docs.github.com/github/administering-a-repository/enabling-force-pushes-to-a-protected-branch)" in the GitHub Help documentation."
  """
  allow_force_pushes: Boolean

  """
  Allows deletion of the protected branch by anyone with write access to the repository. Set to `false` to prevent deletion of the protected branch. Default: `false`. For more information, see "[Enabling force pushes to a protected branch](https://docs.github.com/github/administering-a-repository/enabling-force-pushes-to-a-protected-branch)" in the GitHub Help documentation.
  """
  allow_deletions: Boolean

  """
  If set to `true`, the `restrictions` branch protection settings which limits who can push will also block pushes which create new branches, unless the push is initiated by a user, team, or app which has the ability to push. Set to `true` to restrict new branch creation. Default: `false`.
  """
  block_creations: Boolean

  """
  Requires all conversations on code to be resolved before a pull request can be merged into a branch that matches this rule. Set to `false` to disable. Default: `false`.
  """
  required_conversation_resolution: Boolean

  """
  Whether to set the branch as read-only. If this is true, users will not be able to push to the branch. Default: `false`.
  """
  lock_branch: Boolean

  """
  Whether users can pull changes from upstream when the branch is locked. Set to `true` to allow fork syncing. Set to `false` to prevent fork syncing. Default: `false`.
  """
  allow_fork_syncing: Boolean
}

"""
Require status checks to pass before merging. Set to `null` to disable.
"""
input mutationInput_repos_update_branch_protection_input_required_status_checks_Input {
  """Require branches to be up to date before merging."""
  strict: Boolean!

  """
  **Closing down notice**: The list of status checks to require in order to merge into this branch. If any of these checks have recently been set by a particular GitHub App, they will be required to come from that app in future for the branch to merge. Use `checks` instead of `contexts` for more fine-grained control.
  """
  contexts: [String]!

  """
  The list of status checks to require in order to merge into this branch.
  """
  checks: [mutationInput_repos_update_branch_protection_input_required_status_checks_checks_items_Input]
}

input mutationInput_repos_update_branch_protection_input_required_status_checks_checks_items_Input {
  """The name of the required check"""
  context: String!

  """
  The ID of the GitHub App that must provide this check. Omit this field to automatically select the GitHub App that has recently provided this check, or any app if it was not set by a GitHub App. Pass -1 to explicitly allow any app to set the status.
  """
  app_id: Int
}

"""
Require at least one approving review on a pull request, before merging. Set to `null` to disable.
"""
input mutationInput_repos_update_branch_protection_input_required_pull_request_reviews_Input {
  dismissal_restrictions: mutationInput_repos_update_branch_protection_input_required_pull_request_reviews_dismissal_restrictions_Input

  """
  Set to `true` if you want to automatically dismiss approving reviews when someone pushes a new commit.
  """
  dismiss_stale_reviews: Boolean

  """
  Blocks merging pull requests until [code owners](https://docs.github.com/articles/about-code-owners/) review them.
  """
  require_code_owner_reviews: Boolean

  """
  Specify the number of reviewers required to approve pull requests. Use a number between 1 and 6 or 0 to not require reviewers.
  """
  required_approving_review_count: Int

  """
  Whether the most recent push must be approved by someone other than the person who pushed it. Default: `false`.
  """
  require_last_push_approval: Boolean
  bypass_pull_request_allowances: mutationInput_repos_update_branch_protection_input_required_pull_request_reviews_bypass_pull_request_allowances_Input
}

"""
Specify which users, teams, and apps can dismiss pull request reviews. Pass an empty `dismissal_restrictions` object to disable. User and team `dismissal_restrictions` are only available for organization-owned repositories. Omit this parameter for personal repositories.
"""
input mutationInput_repos_update_branch_protection_input_required_pull_request_reviews_dismissal_restrictions_Input {
  """The list of user `login`s with dismissal access"""
  users: [String]

  """The list of team `slug`s with dismissal access"""
  teams: [String]

  """The list of app `slug`s with dismissal access"""
  apps: [String]
}

"""
Allow specific users, teams, or apps to bypass pull request requirements.
"""
input mutationInput_repos_update_branch_protection_input_required_pull_request_reviews_bypass_pull_request_allowances_Input {
  """The list of user `login`s allowed to bypass pull request requirements."""
  users: [String]

  """The list of team `slug`s allowed to bypass pull request requirements."""
  teams: [String]

  """The list of app `slug`s allowed to bypass pull request requirements."""
  apps: [String]
}

"""
Restrict who can push to the protected branch. User, app, and team `restrictions` are only available for organization-owned repositories. Set to `null` to disable.
"""
input mutationInput_repos_update_branch_protection_input_restrictions_Input {
  """The list of user `login`s with push access"""
  users: [String]!

  """The list of team `slug`s with push access"""
  teams: [String]!

  """The list of app `slug`s with push access"""
  apps: [String]
}

input repos_update_pull_request_review_protection_request_Input {
  dismissal_restrictions: mutationInput_repos_update_pull_request_review_protection_input_dismissal_restrictions_Input

  """
  Set to `true` if you want to automatically dismiss approving reviews when someone pushes a new commit.
  """
  dismiss_stale_reviews: Boolean

  """
  Blocks merging pull requests until [code owners](https://docs.github.com/articles/about-code-owners/) have reviewed.
  """
  require_code_owner_reviews: Boolean

  """
  Specifies the number of reviewers required to approve pull requests. Use a number between 1 and 6 or 0 to not require reviewers.
  """
  required_approving_review_count: Int

  """
  Whether the most recent push must be approved by someone other than the person who pushed it. Default: `false`
  """
  require_last_push_approval: Boolean
  bypass_pull_request_allowances: mutationInput_repos_update_pull_request_review_protection_input_bypass_pull_request_allowances_Input
}

"""
Specify which users, teams, and apps can dismiss pull request reviews. Pass an empty `dismissal_restrictions` object to disable. User and team `dismissal_restrictions` are only available for organization-owned repositories. Omit this parameter for personal repositories.
"""
input mutationInput_repos_update_pull_request_review_protection_input_dismissal_restrictions_Input {
  """The list of user `login`s with dismissal access"""
  users: [String]

  """The list of team `slug`s with dismissal access"""
  teams: [String]

  """The list of app `slug`s with dismissal access"""
  apps: [String]
}

"""
Allow specific users, teams, or apps to bypass pull request requirements.
"""
input mutationInput_repos_update_pull_request_review_protection_input_bypass_pull_request_allowances_Input {
  """The list of user `login`s allowed to bypass pull request requirements."""
  users: [String]

  """The list of team `slug`s allowed to bypass pull request requirements."""
  teams: [String]

  """The list of app `slug`s allowed to bypass pull request requirements."""
  apps: [String]
}

input repos_update_status_check_protection_request_Input {
  """Require branches to be up to date before merging."""
  strict: Boolean

  """
  **Closing down notice**: The list of status checks to require in order to merge into this branch. If any of these checks have recently been set by a particular GitHub App, they will be required to come from that app in future for the branch to merge. Use `checks` instead of `contexts` for more fine-grained control.
  """
  contexts: [String]

  """
  The list of status checks to require in order to merge into this branch.
  """
  checks: [mutationInput_repos_update_status_check_protection_input_checks_items_Input]
}

input mutationInput_repos_update_status_check_protection_input_checks_items_Input {
  """The name of the required check"""
  context: String!

  """
  The ID of the GitHub App that must provide this check. Omit this field to automatically select the GitHub App that has recently provided this check, or any app if it was not set by a GitHub App. Pass -1 to explicitly allow any app to set the status.
  """
  app_id: Int
}

input repos_add_status_check_contexts_request_Input {
  mutationInput_repos_add_status_check_contexts_input_oneOf_0_Input: mutationInput_repos_add_status_check_contexts_input_oneOf_0_Input
  String: String
}

input mutationInput_repos_add_status_check_contexts_input_oneOf_0_Input {
  """The name of the status checks"""
  contexts: [String]!
}

input repos_set_status_check_contexts_request_Input {
  mutationInput_repos_set_status_check_contexts_input_oneOf_0_Input: mutationInput_repos_set_status_check_contexts_input_oneOf_0_Input
  String: String
}

input mutationInput_repos_set_status_check_contexts_input_oneOf_0_Input {
  """The name of the status checks"""
  contexts: [String]!
}

input repos_remove_status_check_contexts_request_Input {
  mutationInput_repos_remove_status_check_contexts_input_oneOf_0_Input: mutationInput_repos_remove_status_check_contexts_input_oneOf_0_Input
  String: String
}

input mutationInput_repos_remove_status_check_contexts_input_oneOf_0_Input {
  """The name of the status checks"""
  contexts: [String]!
}

input repos_add_app_access_restrictions_request_Input {
  """
  The GitHub Apps that have push access to this branch. Use the slugified version of the app name. **Note**: The list of users, apps, and teams in total is limited to 100 items.
  """
  apps: [String]!
}

input repos_set_app_access_restrictions_request_Input {
  """
  The GitHub Apps that have push access to this branch. Use the slugified version of the app name. **Note**: The list of users, apps, and teams in total is limited to 100 items.
  """
  apps: [String]!
}

input repos_remove_app_access_restrictions_request_Input {
  """
  The GitHub Apps that have push access to this branch. Use the slugified version of the app name. **Note**: The list of users, apps, and teams in total is limited to 100 items.
  """
  apps: [String]!
}

input repos_add_team_access_restrictions_request_Input {
  mutationInput_repos_add_team_access_restrictions_input_oneOf_0_Input: mutationInput_repos_add_team_access_restrictions_input_oneOf_0_Input
  String: String
}

input mutationInput_repos_add_team_access_restrictions_input_oneOf_0_Input {
  """The slug values for teams"""
  teams: [String]!
}

input repos_set_team_access_restrictions_request_Input {
  mutationInput_repos_set_team_access_restrictions_input_oneOf_0_Input: mutationInput_repos_set_team_access_restrictions_input_oneOf_0_Input
  String: String
}

input mutationInput_repos_set_team_access_restrictions_input_oneOf_0_Input {
  """The slug values for teams"""
  teams: [String]!
}

input repos_remove_team_access_restrictions_request_Input {
  mutationInput_repos_remove_team_access_restrictions_input_oneOf_0_Input: mutationInput_repos_remove_team_access_restrictions_input_oneOf_0_Input
  String: String
}

input mutationInput_repos_remove_team_access_restrictions_input_oneOf_0_Input {
  """The slug values for teams"""
  teams: [String]!
}

input repos_add_user_access_restrictions_request_Input {
  """The username for users"""
  users: [String]!
}

input repos_set_user_access_restrictions_request_Input {
  """The username for users"""
  users: [String]!
}

input repos_remove_user_access_restrictions_request_Input {
  """The username for users"""
  users: [String]!
}

input repos_rename_branch_request_Input {
  """The new name of the branch."""
  new_name: String!
}

input checks_create_request_Input {
  """The name of the check. For example, "code-coverage"."""
  name: String!

  """The SHA of the commit."""
  head_sha: String!

  """
  The URL of the integrator's site that has the full details of the check. If the integrator does not provide this, then the homepage of the GitHub app is used.
  """
  details_url: String

  """A reference for the run on the integrator's system."""
  external_id: String
  status: mutationInput_checks_create_input_status = queued

  """
  The time that the check run began. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  started_at: DateTime
  conclusion: mutationInput_checks_create_input_conclusion

  """
  The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  completed_at: DateTime
  output: mutationInput_checks_create_input_output_Input

  """
  Displays a button on GitHub that can be clicked to alert your app to do additional tasks. For example, a code linting app can display a button that automatically fixes detected errors. The button created in this object is displayed after the check run completes. When a user clicks the button, GitHub sends the [`check_run.requested_action` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) to your app. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions)."
  """
  actions: [mutationInput_checks_create_input_actions_items_Input]
}

"""
The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`.
"""
enum mutationInput_checks_create_input_status {
  queued
  in_progress
  completed
  waiting
  requested
  pending
}

"""
**Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. 
**Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this.
"""
enum mutationInput_checks_create_input_conclusion {
  action_required
  cancelled
  failure
  neutral
  success
  skipped
  stale
  timed_out
}

"""
Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run.
"""
input mutationInput_checks_create_input_output_Input {
  """The title of the check run."""
  title: String!

  """
  The summary of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters.
  """
  summary: mutationInput_checks_create_input_output_summary!

  """
  The details of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters.
  """
  text: mutationInput_checks_create_input_output_text

  """
  Adds information from your analysis to specific lines of code. Annotations are visible on GitHub in the **Checks** and **Files changed** tab of the pull request. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about how you can view annotations on GitHub, see "[About status checks](https://docs.github.com/articles/about-status-checks#checks)".
  """
  annotations: [mutationInput_checks_create_input_output_annotations_items_Input]

  """Adds images to the output displayed in the GitHub pull request UI."""
  images: [mutationInput_checks_create_input_output_images_items_Input]
}

"""
The summary of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters.
"""
scalar mutationInput_checks_create_input_output_summary

"""
The details of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters.
"""
scalar mutationInput_checks_create_input_output_text

input mutationInput_checks_create_input_output_annotations_items_Input {
  """
  The path of the file to add an annotation to. For example, `assets/css/main.css`.
  """
  path: String!

  """The start line of the annotation. Line numbers start at 1."""
  start_line: Int!

  """The end line of the annotation."""
  end_line: Int!

  """
  The start column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values. Column numbers start at 1.
  """
  start_column: Int

  """
  The end column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values.
  """
  end_column: Int
  annotation_level: mutationInput_checks_create_input_output_annotations_items_annotation_level!

  """
  A short description of the feedback for these lines of code. The maximum size is 64 KB.
  """
  message: String!

  """
  The title that represents the annotation. The maximum size is 255 characters.
  """
  title: String

  """Details about this annotation. The maximum size is 64 KB."""
  raw_details: String
}

"""The level of the annotation."""
enum mutationInput_checks_create_input_output_annotations_items_annotation_level {
  notice
  warning
  failure
}

input mutationInput_checks_create_input_output_images_items_Input {
  """The alternative text for the image."""
  alt: String!

  """The full URL of the image."""
  image_url: String!

  """A short image description."""
  caption: String
}

input mutationInput_checks_create_input_actions_items_Input {
  """
  The text to be displayed on a button in the web UI. The maximum size is 20 characters.
  """
  label: mutationInput_checks_create_input_actions_items_label!

  """
  A short explanation of what this action would do. The maximum size is 40 characters.
  """
  description: mutationInput_checks_create_input_actions_items_description!

  """
  A reference for the action on the integrator's system. The maximum size is 20 characters.
  """
  identifier: mutationInput_checks_create_input_actions_items_identifier!
}

"""
The text to be displayed on a button in the web UI. The maximum size is 20 characters.
"""
scalar mutationInput_checks_create_input_actions_items_label

"""
A short explanation of what this action would do. The maximum size is 40 characters.
"""
scalar mutationInput_checks_create_input_actions_items_description

"""
A reference for the action on the integrator's system. The maximum size is 20 characters.
"""
scalar mutationInput_checks_create_input_actions_items_identifier

input checks_update_request_Input {
  status: mutationInput_checks_update_input_status
  conclusion: mutationInput_checks_update_input_conclusion

  """The name of the check. For example, "code-coverage"."""
  name: String

  """
  The URL of the integrator's site that has the full details of the check.
  """
  details_url: String

  """A reference for the run on the integrator's system."""
  external_id: String

  """
  This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  started_at: DateTime

  """
  The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  completed_at: DateTime
  output: mutationInput_checks_update_input_output_Input

  """
  Possible further actions the integrator can perform, which a user may trigger. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions)."
  """
  actions: [mutationInput_checks_update_input_actions_items_Input]
}

"""
The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`.
"""
enum mutationInput_checks_update_input_status {
  queued
  in_progress
  completed
  waiting
  requested
  pending
}

"""
**Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. 
**Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this.
"""
enum mutationInput_checks_update_input_conclusion {
  action_required
  cancelled
  failure
  neutral
  success
  skipped
  stale
  timed_out
}

"""
Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run.
"""
input mutationInput_checks_update_input_output_Input {
  """**Required**."""
  title: String

  """Can contain Markdown."""
  summary: mutationInput_checks_update_input_output_summary!

  """Can contain Markdown."""
  text: mutationInput_checks_update_input_output_text

  """
  Adds information from your analysis to specific lines of code. Annotations are visible in GitHub's pull request UI. Annotations are visible in GitHub's pull request UI. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about annotations in the UI, see "[About status checks](https://docs.github.com/articles/about-status-checks#checks)".
  """
  annotations: [mutationInput_checks_update_input_output_annotations_items_Input]

  """Adds images to the output displayed in the GitHub pull request UI."""
  images: [mutationInput_checks_update_input_output_images_items_Input]
}

"""Can contain Markdown."""
scalar mutationInput_checks_update_input_output_summary

"""Can contain Markdown."""
scalar mutationInput_checks_update_input_output_text

input mutationInput_checks_update_input_output_annotations_items_Input {
  """
  The path of the file to add an annotation to. For example, `assets/css/main.css`.
  """
  path: String!

  """The start line of the annotation. Line numbers start at 1."""
  start_line: Int!

  """The end line of the annotation."""
  end_line: Int!

  """
  The start column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values. Column numbers start at 1.
  """
  start_column: Int

  """
  The end column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values.
  """
  end_column: Int
  annotation_level: mutationInput_checks_update_input_output_annotations_items_annotation_level!

  """
  A short description of the feedback for these lines of code. The maximum size is 64 KB.
  """
  message: String!

  """
  The title that represents the annotation. The maximum size is 255 characters.
  """
  title: String

  """Details about this annotation. The maximum size is 64 KB."""
  raw_details: String
}

"""The level of the annotation."""
enum mutationInput_checks_update_input_output_annotations_items_annotation_level {
  notice
  warning
  failure
}

input mutationInput_checks_update_input_output_images_items_Input {
  """The alternative text for the image."""
  alt: String!

  """The full URL of the image."""
  image_url: String!

  """A short image description."""
  caption: String
}

input mutationInput_checks_update_input_actions_items_Input {
  """
  The text to be displayed on a button in the web UI. The maximum size is 20 characters.
  """
  label: mutationInput_checks_update_input_actions_items_label!

  """
  A short explanation of what this action would do. The maximum size is 40 characters.
  """
  description: mutationInput_checks_update_input_actions_items_description!

  """
  A reference for the action on the integrator's system. The maximum size is 20 characters.
  """
  identifier: mutationInput_checks_update_input_actions_items_identifier!
}

"""
The text to be displayed on a button in the web UI. The maximum size is 20 characters.
"""
scalar mutationInput_checks_update_input_actions_items_label

"""
A short explanation of what this action would do. The maximum size is 40 characters.
"""
scalar mutationInput_checks_update_input_actions_items_description

"""
A reference for the action on the integrator's system. The maximum size is 20 characters.
"""
scalar mutationInput_checks_update_input_actions_items_identifier

input checks_create_suite_request_Input {
  """The sha of the head commit."""
  head_sha: String!
}

"""Check suite configuration preferences for a repository."""
type Check_Suite_Preference {
  preferences: mutation_checks_set_suites_preferences_preferences!
  repository: minimal_repository!
}

type mutation_checks_set_suites_preferences_preferences {
  auto_trigger_checks: [mutation_checks_set_suites_preferences_preferences_auto_trigger_checks_items]
}

type mutation_checks_set_suites_preferences_preferences_auto_trigger_checks_items {
  app_id: Int!
  setting: Boolean!
}

input checks_set_suites_preferences_request_Input {
  """
  Enables or disables automatic creation of CheckSuite events upon pushes to the repository. Enabled by default.
  """
  auto_trigger_checks: [mutationInput_checks_set_suites_preferences_input_auto_trigger_checks_items_Input]
}

input mutationInput_checks_set_suites_preferences_input_auto_trigger_checks_items_Input {
  """The `id` of the GitHub App."""
  app_id: Int!

  """
  Set to `true` to enable automatic creation of CheckSuite events upon pushes to the repository, or `false` to disable them.
  """
  setting: Boolean! = true
}

input code_scanning_update_alert_request_Input {
  state: code_scanning_alert_set_state

  """
  The list of users to assign to the code scanning alert. An empty array unassigns all previous assignees from the alert.
  """
  assignees: [String]
  dismissed_reason: code_scanning_alert_dismissed_reason

  """The dismissal comment associated with the dismissal of the alert."""
  dismissed_comment: code_scanning_alert_dismissed_comment

  """If `true`, attempt to create an alert dismissal request."""
  create_request: Boolean
}

"""
Sets the state of the code scanning alert. You must provide `dismissed_reason` when you set the state to `dismissed`.
"""
enum code_scanning_alert_set_state {
  open
  dismissed
}

type code_scanning_autofix_commits_response {
  """
  The Git reference of target branch for the commit. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
  """
  target_ref: String

  """SHA of commit with autofix."""
  sha: String
}

"""Commit an autofix for a code scanning alert"""
input code_scanning_autofix_commits_Input {
  """
  The Git reference of target branch for the commit. Branch needs to already exist.  For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation.
  """
  target_ref: String

  """Commit message to be used."""
  message: String
}

"""Successful deletion of a code scanning analysis"""
type Analysis_deletion {
  """
  Next deletable analysis in chain, without last analysis deletion confirmation
  """
  next_analysis_url: URL

  """
  Next deletable analysis in chain, with last analysis deletion confirmation
  """
  confirm_delete_url: URL
}

input code_scanning_create_variant_analysis_request_Input {
  language: code_scanning_variant_analysis_language!

  """
  A Base64-encoded tarball containing a CodeQL query and all its dependencies
  """
  query_pack: String!

  """
  List of repository names (in the form `owner/repo-name`) to run the query against. Precisely one property from `repositories`, `repository_lists` and `repository_owners` is required.
  """
  repositories: [String]

  """
  List of repository lists to run the query against. Precisely one property from `repositories`, `repository_lists` and `repository_owners` is required.
  """
  repository_lists: [String]

  """
  List of organization or user names whose repositories the query should be run against. Precisely one property from `repositories`, `repository_lists` and `repository_owners` is required.
  """
  repository_owners: [String]
}

union code_scanning_update_default_setup_response = JSON_container | code_scanning_default_setup_update_response

"""
You can use `run_url` to track the status of the run. This includes a property status and conclusion.
You should not rely on this always being an actions workflow run object.
"""
type code_scanning_default_setup_update_response {
  """ID of the corresponding run."""
  run_id: Int

  """URL of the corresponding run."""
  run_url: String
}

"""Configuration for code scanning default setup."""
input code_scanning_default_setup_update_Input {
  state: mutationInput_code_scanning_update_default_setup_input_state
  runner_type: mutationInput_code_scanning_update_default_setup_input_runner_type

  """Runner label to be used if the runner type is labeled."""
  runner_label: String
  query_suite: mutationInput_code_scanning_update_default_setup_input_query_suite
  threat_model: mutationInput_code_scanning_update_default_setup_input_threat_model

  """CodeQL languages to be analyzed."""
  languages: [mutationInput_code_scanning_update_default_setup_input_languages_items]
}

"""The desired state of code scanning default setup."""
enum mutationInput_code_scanning_update_default_setup_input_state {
  configured
  not_configured
}

"""Runner type to be used."""
enum mutationInput_code_scanning_update_default_setup_input_runner_type {
  standard
  labeled
}

"""CodeQL query suite to be used."""
enum mutationInput_code_scanning_update_default_setup_input_query_suite {
  default
  extended
}

"""
Threat model to be used for code scanning analysis. Use `remote` to analyze only network sources and `remote_and_local` to include local sources like filesystem access, command-line arguments, database reads, environment variable and standard input.
"""
enum mutationInput_code_scanning_update_default_setup_input_threat_model {
  remote
  remote_and_local
}

enum mutationInput_code_scanning_update_default_setup_input_languages_items {
  actions
  c_cpp
  csharp
  go
  java_kotlin
  javascript_typescript
  python
  ruby
  swift
}

type code_scanning_sarifs_receipt {
  """An identifier for the upload."""
  id: String

  """The REST API URL for checking the status of the upload."""
  url: URL
}

input code_scanning_upload_sarif_request_Input {
  commit_sha: code_scanning_analysis_commit_sha!
  ref: code_scanning_ref_full!

  """
  A Base64 string representing the SARIF file to upload. You must first compress your SARIF file using [`gzip`](http://www.gnu.org/software/gzip/manual/gzip.html) and then translate the contents of the file into a Base64 encoding string. For more information, see "[SARIF support for code scanning](https://docs.github.com/code-security/secure-coding/sarif-support-for-code-scanning)."
  """
  sarif: String!

  """
  The base directory used in the analysis, as it appears in the SARIF file.
  This property is used to convert file paths from absolute to relative, so that alerts can be mapped to their correct location in the repository.
  """
  checkout_uri: URL

  """
  The time that the analysis run began. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  started_at: DateTime

  """
  The name of the tool used to generate the code scanning analysis. If this parameter is not used, the tool name defaults to "API". If the uploaded SARIF contains a tool GUID, this will be available for filtering using the `tool_guid` parameter of operations such as `GET /repos/{owner}/{repo}/code-scanning/alerts`.
  """
  tool_name: String

  """
  Whether the SARIF file will be validated according to the code scanning specifications.
  This parameter is intended to help integrators ensure that the uploaded SARIF files are correctly rendered by code scanning.
  """
  validate: Boolean
}

scalar code_scanning_ref_full

input codespaces_create_with_repo_for_authenticated_user_request_Input {
  """Git ref (typically a branch name) for this codespace"""
  ref: String

  """
  The requested location for a new codespace. Best efforts are made to respect this upon creation. Assigned by IP if not provided.
  """
  location: String
  geo: mutationInput_codespaces_create_with_repo_for_authenticated_user_input_geo

  """IP for location auto-detection when proxying a request"""
  client_ip: String

  """Machine type to use for this codespace"""
  machine: String

  """Path to devcontainer.json config to use for this codespace"""
  devcontainer_path: String

  """Whether to authorize requested permissions from devcontainer.json"""
  multi_repo_permissions_opt_out: Boolean

  """Working directory for this codespace"""
  working_directory: String

  """Time in minutes before codespace stops from inactivity"""
  idle_timeout_minutes: Int

  """Display name for this codespace"""
  display_name: String

  """
  Duration in minutes after codespace has gone idle in which it will be deleted. Must be integer minutes between 0 and 43200 (30 days).
  """
  retention_period_minutes: Int
}

"""
The geographic area for this codespace. If not specified, the value is assigned by IP. This property replaces `location`, which is closing down.
"""
enum mutationInput_codespaces_create_with_repo_for_authenticated_user_input_geo {
  EuropeWest
  SoutheastAsia
  UsEast
  UsWest
}

union codespaces_create_or_update_repo_secret_response = JSON_container | Void_container

input codespaces_create_or_update_repo_secret_request_Input {
  encrypted_value: mutationInput_codespaces_create_or_update_repo_secret_input_encrypted_value

  """ID of the key you used to encrypt the secret."""
  key_id: String
}

scalar mutationInput_codespaces_create_or_update_repo_secret_input_encrypted_value

union repos_add_collaborator_response = repository_invitation | Void_container

input repos_add_collaborator_request_Input {
  """
  The permission to grant the collaborator. **Only valid on organization-owned repositories.** We accept the following permissions to be set: `pull`, `triage`, `push`, `maintain`, `admin` and you can also specify a custom repository role name, if the owning organization has defined any.
  """
  permission: String = "push"
}

input repos_update_commit_comment_request_Input {
  """The contents of the comment"""
  body: String!
}

input reactions_create_for_commit_comment_request_Input {
  content: mutationInput_reactions_create_for_commit_comment_input_content!
}

"""
The [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions) to add to the commit comment.
"""
enum mutationInput_reactions_create_for_commit_comment_input_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

input repos_create_commit_comment_request_Input {
  """The contents of the comment."""
  body: String!

  """Relative path of the file to comment on."""
  path: String

  """Line index in the diff to comment on."""
  position: Int

  """
  **Closing down notice**. Use **position** parameter instead. Line number in the file to comment on.
  """
  line: Int
}

"""File Commit"""
type File_Commit {
  content: mutation_repos_create_or_update_file_contents_oneOf_0_content
  commit: mutation_repos_create_or_update_file_contents_oneOf_0_commit!
}

type mutation_repos_create_or_update_file_contents_oneOf_0_content {
  name: String
  path: String
  sha: String
  size: Int
  url: String
  html_url: String
  git_url: String
  download_url: String
  type: String
  _links: mutation_repos_create_or_update_file_contents_oneOf_0_content__links
}

type mutation_repos_create_or_update_file_contents_oneOf_0_content__links {
  self: String
  git: String
  html: String
}

type mutation_repos_create_or_update_file_contents_oneOf_0_commit {
  sha: String
  node_id: String
  url: String
  html_url: String
  author: mutation_repos_create_or_update_file_contents_oneOf_0_commit_author
  committer: mutation_repos_create_or_update_file_contents_oneOf_0_commit_committer
  message: String
  tree: mutation_repos_create_or_update_file_contents_oneOf_0_commit_tree
  parents: [mutation_repos_create_or_update_file_contents_oneOf_0_commit_parents_items]
  verification: mutation_repos_create_or_update_file_contents_oneOf_0_commit_verification
}

type mutation_repos_create_or_update_file_contents_oneOf_0_commit_author {
  date: String
  name: String
  email: String
}

type mutation_repos_create_or_update_file_contents_oneOf_0_commit_committer {
  date: String
  name: String
  email: String
}

type mutation_repos_create_or_update_file_contents_oneOf_0_commit_tree {
  url: String
  sha: String
}

type mutation_repos_create_or_update_file_contents_oneOf_0_commit_parents_items {
  url: String
  html_url: String
  sha: String
}

type mutation_repos_create_or_update_file_contents_oneOf_0_commit_verification {
  verified: Boolean
  reason: String
  signature: String
  payload: String
  verified_at: String
}

input repos_create_or_update_file_contents_request_Input {
  """The commit message."""
  message: String!

  """The new file content, using Base64 encoding."""
  content: String!

  """
  **Required if you are updating a file**. The blob SHA of the file being replaced.
  """
  sha: String

  """The branch name. Default: the repository’s default branch."""
  branch: String
  committer: mutationInput_repos_create_or_update_file_contents_input_committer_Input
  author: mutationInput_repos_create_or_update_file_contents_input_author_Input
}

"""The person that committed the file. Default: the authenticated user."""
input mutationInput_repos_create_or_update_file_contents_input_committer_Input {
  """
  The name of the author or committer of the commit. You'll receive a `422` status code if `name` is omitted.
  """
  name: String!

  """
  The email of the author or committer of the commit. You'll receive a `422` status code if `email` is omitted.
  """
  email: String!
  date: String
}

"""
The author of the file. Default: The `committer` or the authenticated user if you omit `committer`.
"""
input mutationInput_repos_create_or_update_file_contents_input_author_Input {
  """
  The name of the author or committer of the commit. You'll receive a `422` status code if `name` is omitted.
  """
  name: String!

  """
  The email of the author or committer of the commit. You'll receive a `422` status code if `email` is omitted.
  """
  email: String!
  date: String
}

input repos_delete_file_request_Input {
  """The commit message."""
  message: String!

  """The blob SHA of the file being deleted."""
  sha: String!

  """The branch name. Default: the repository’s default branch"""
  branch: String
  committer: mutationInput_repos_delete_file_input_committer_Input
  author: mutationInput_repos_delete_file_input_author_Input
}

"""object containing information about the committer."""
input mutationInput_repos_delete_file_input_committer_Input {
  """The name of the author (or committer) of the commit"""
  name: String

  """The email of the author (or committer) of the commit"""
  email: String
}

"""object containing information about the author."""
input mutationInput_repos_delete_file_input_author_Input {
  """The name of the author (or committer) of the commit"""
  name: String

  """The email of the author (or committer) of the commit"""
  email: String
}

input dependabot_update_alert_request_Input {
  state: mutationInput_dependabot_update_alert_input_state

  """
  Usernames to assign to this Dependabot Alert.
  Pass one or more user logins to _replace_ the set of assignees on this alert.
  Send an empty array (`[]`) to clear all assignees from the alert.
  """
  assignees: [String]
  dismissed_reason: mutationInput_dependabot_update_alert_input_dismissed_reason

  """An optional comment associated with dismissing the alert."""
  dismissed_comment: mutationInput_dependabot_update_alert_input_dismissed_comment
}

"""
The state of the Dependabot alert.
A `dismissed_reason` must be provided when setting the state to `dismissed`.
"""
enum mutationInput_dependabot_update_alert_input_state {
  dismissed
  open
}

"""
**Required when `state` is `dismissed`.** A reason for dismissing the alert.
"""
enum mutationInput_dependabot_update_alert_input_dismissed_reason {
  fix_started
  inaccurate
  no_bandwidth
  not_used
  tolerable_risk
}

"""An optional comment associated with dismissing the alert."""
scalar mutationInput_dependabot_update_alert_input_dismissed_comment

union dependabot_create_or_update_repo_secret_response = JSON_container | Void_container

input dependabot_create_or_update_repo_secret_request_Input {
  encrypted_value: mutationInput_dependabot_create_or_update_repo_secret_input_encrypted_value

  """ID of the key you used to encrypt the secret."""
  key_id: String
}

scalar mutationInput_dependabot_create_or_update_repo_secret_input_encrypted_value

type dependency_graph_create_repository_snapshot_201_response {
  """ID of the created snapshot."""
  id: Int!

  """The time at which the snapshot was created."""
  created_at: String!

  """
  Either "SUCCESS", "ACCEPTED", or "INVALID". "SUCCESS" indicates that the snapshot was successfully created and the repository's dependencies were updated. "ACCEPTED" indicates that the snapshot was successfully created, but the repository's dependencies were not updated. "INVALID" indicates that the snapshot was malformed.
  """
  result: String!

  """
  A message providing further details about the result, such as why the dependencies were not updated.
  """
  message: String!
}

"""Create a new snapshot of a repository's dependencies."""
input snapshot_Input {
  """The version of the repository snapshot submission."""
  version: Int!
  job: mutationInput_dependency_graph_create_repository_snapshot_input_job_Input!

  """
  The commit SHA associated with this dependency snapshot. Maximum length: 40 characters.
  """
  sha: mutationInput_dependency_graph_create_repository_snapshot_input_sha!
  ref: mutationInput_dependency_graph_create_repository_snapshot_input_ref!
  detector: mutationInput_dependency_graph_create_repository_snapshot_input_detector_Input!
  metadata: JSON
  manifests: JSON

  """The time at which the snapshot was scanned."""
  scanned: DateTime!
}

input mutationInput_dependency_graph_create_repository_snapshot_input_job_Input {
  """The external ID of the job."""
  id: String!

  """
  Correlator provides a key that is used to group snapshots submitted over time. Only the "latest" submitted snapshot for a given combination of `job.correlator` and `detector.name` will be considered when calculating a repository's current dependencies. Correlator should be as unique as it takes to distinguish all detection runs for a given "wave" of CI workflow you run. If you're using GitHub Actions, a good default value for this could be the environment variables GITHUB_WORKFLOW and GITHUB_JOB concatenated together. If you're using a build matrix, then you'll also need to add additional key(s) to distinguish between each submission inside a matrix variation.
  """
  correlator: String!

  """The url for the job."""
  html_url: URL
}

"""
The commit SHA associated with this dependency snapshot. Maximum length: 40 characters.
"""
scalar mutationInput_dependency_graph_create_repository_snapshot_input_sha

scalar mutationInput_dependency_graph_create_repository_snapshot_input_ref

"""A description of the detector used."""
input mutationInput_dependency_graph_create_repository_snapshot_input_detector_Input {
  """The name of the detector used."""
  name: String!

  """The version of the detector used."""
  version: String!

  """The url of the detector used."""
  url: URL!
}

union repos_create_deployment_response = deployment | repos_create_deployment_202_response

type repos_create_deployment_202_response {
  message: String
}

input repos_create_deployment_request_Input {
  """The ref to deploy. This can be a branch, tag, or SHA."""
  ref: String!

  """Specifies a task to execute (e.g., `deploy` or `deploy:migrations`)."""
  task: String = "deploy"

  """
  Attempts to automatically merge the default branch into the requested ref, if it's behind the default branch.
  """
  auto_merge: Boolean = true

  """
  The [status](https://docs.github.com/rest/commits/statuses) contexts to verify against commit status checks. If you omit this parameter, GitHub verifies all unique contexts before creating a deployment. To bypass checking entirely, pass an empty array. Defaults to all unique contexts.
  """
  required_contexts: [String]
  payload: mutationInput_repos_create_deployment_input_payload_Input

  """
  Name for the target deployment environment (e.g., `production`, `staging`, `qa`).
  """
  environment: String = "production"

  """Short description of the deployment."""
  description: String

  """
  Specifies if the given environment is specific to the deployment and will no longer exist at some point in the future. Default: `false`
  """
  transient_environment: Boolean

  """
  Specifies if the given environment is one that end-users directly interact with. Default: `true` when `environment` is `production` and `false` otherwise.
  """
  production_environment: Boolean
}

input mutationInput_repos_create_deployment_input_payload_Input {
  JSON: JSON
  String: String
}

input repos_create_deployment_status_request_Input {
  state: mutationInput_repos_create_deployment_status_input_state!

  """
  The target URL to associate with this status. This URL should contain output to keep the user updated while the task is running or serve as historical information for what happened in the deployment.
  
  > [!NOTE]
  > It's recommended to use the `log_url` parameter, which replaces `target_url`.
  """
  target_url: String

  """
  The full URL of the deployment's output. This parameter replaces `target_url`. We will continue to accept `target_url` to support legacy uses, but we recommend replacing `target_url` with `log_url`. Setting `log_url` will automatically set `target_url` to the same value. Default: `""`
  """
  log_url: String

  """
  A short description of the status. The maximum description length is 140 characters.
  """
  description: String

  """
  Name for the target deployment environment, which can be changed when setting a deploy status. For example, `production`, `staging`, or `qa`. If not defined, the environment of the previous status on the deployment will be used, if it exists. Otherwise, the environment of the deployment will be used.
  """
  environment: String

  """Sets the URL for accessing your environment. Default: `""`"""
  environment_url: String

  """
  Adds a new `inactive` status to all prior non-transient, non-production environment deployments with the same repository and `environment` name as the created status's deployment. An `inactive` status is only added to deployments that had a `success` state. Default: `true`
  """
  auto_inactive: Boolean
}

"""
The state of the status. When you set a transient deployment to `inactive`, the deployment will be shown as `destroyed` in GitHub.
"""
enum mutationInput_repos_create_deployment_status_input_state {
  error
  failure
  inactive
  in_progress
  queued
  pending
  success
}

input repos_create_dispatch_event_request_Input {
  """A custom webhook event name. Must be 100 characters or fewer."""
  event_type: mutationInput_repos_create_dispatch_event_input_event_type!

  """
  JSON payload with extra information about the webhook event that your action or workflow may use. The maximum number of top-level properties is 10. The total size of the JSON payload must be less than 64KB.
  """
  client_payload: JSON
}

"""A custom webhook event name. Must be 100 characters or fewer."""
scalar mutationInput_repos_create_dispatch_event_input_event_type

input repos_create_or_update_environment_request_Input {
  """
  The amount of time to delay a job after the job is initially triggered. The time (in minutes) must be an integer between 0 and 43,200 (30 days).
  """
  wait_timer: Int

  """
  Whether or not a user who created the job is prevented from approving their own job.
  """
  prevent_self_review: Boolean

  """
  The people or teams that may review jobs that reference the environment. You can list up to six users or teams as reviewers. The reviewers must have at least read access to the repository. Only one of the required reviewers needs to approve the job for it to proceed.
  """
  reviewers: [mutationInput_repos_create_or_update_environment_input_reviewers_items_Input]
  deployment_branch_policy: deployment_branch_policy_settings_Input
}

input mutationInput_repos_create_or_update_environment_input_reviewers_items_Input {
  type: deployment_reviewer_type

  """The id of the user or team who can review the deployment"""
  id: Int
}

"""
The type of deployment branch policy for this environment. To allow all branches to deploy, set to `null`.
"""
input deployment_branch_policy_settings_Input {
  """
  Whether only branches with branch protection rules can deploy to this environment. If `protected_branches` is `true`, `custom_branch_policies` must be `false`; if `protected_branches` is `false`, `custom_branch_policies` must be `true`.
  """
  protected_branches: Boolean!

  """
  Whether only branches that match the specified name patterns can deploy to this environment.  If `custom_branch_policies` is `true`, `protected_branches` must be `false`; if `custom_branch_policies` is `false`, `protected_branches` must be `true`.
  """
  custom_branch_policies: Boolean!
}

input Deployment_branch_and_tag_policy_name_pattern_Input {
  """
  The name pattern that branches or tags must match in order to deploy to the environment.
  
  Wildcard characters will not match `/`. For example, to match branches that begin with `release/` and contain an additional single slash, use `release/*/*`.
  For more information about pattern matching syntax, see the [Ruby File.fnmatch documentation](https://ruby-doc.org/core-2.5.1/File.html#method-c-fnmatch).
  """
  name: String!
  type: mutationInput_repos_create_deployment_branch_policy_input_type
}

"""Whether this rule targets a branch or tag"""
enum mutationInput_repos_create_deployment_branch_policy_input_type {
  branch
  tag
}

input Deployment_branch_policy_name_pattern_Input {
  """
  The name pattern that branches must match in order to deploy to the environment.
  
  Wildcard characters will not match `/`. For example, to match branches that begin with `release/` and contain an additional single slash, use `release/*/*`.
  For more information about pattern matching syntax, see the [Ruby File.fnmatch documentation](https://ruby-doc.org/core-2.5.1/File.html#method-c-fnmatch).
  """
  name: String!
}

input repos_create_deployment_protection_rule_request_Input {
  """The ID of the custom app that will be enabled on the environment."""
  integration_id: Int
}

union actions_create_or_update_environment_secret_response = JSON_container | Void_container

input actions_create_or_update_environment_secret_request_Input {
  encrypted_value: mutationInput_actions_create_or_update_environment_secret_input_encrypted_value!

  """ID of the key you used to encrypt the secret."""
  key_id: String!
}

scalar mutationInput_actions_create_or_update_environment_secret_input_encrypted_value

input actions_create_environment_variable_request_Input {
  """The name of the variable."""
  name: String!

  """The value of the variable."""
  value: String!
}

input actions_update_environment_variable_request_Input {
  """The name of the variable."""
  name: String

  """The value of the variable."""
  value: String
}

input repos_create_fork_request_Input {
  """
  Optional parameter to specify the organization name if forking into an organization.
  """
  organization: String

  """When forking from an existing repository, a new name for the fork."""
  name: String

  """
  When forking from an existing repository, fork with only the default branch.
  """
  default_branch_only: Boolean
}

"""Short Blob"""
type Short_Blob {
  url: String!
  sha: String!
}

input git_create_blob_request_Input {
  """The new blob's content."""
  content: String!

  """
  The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported.
  """
  encoding: String = "utf-8"
}

input git_create_commit_request_Input {
  """The commit message"""
  message: String!

  """The SHA of the tree object this commit points to"""
  tree: String!

  """
  The full SHAs of the commits that were the parents of this commit. If omitted or empty, the commit will be written as a root commit. For a single parent, an array of one SHA should be provided; for a merge commit, an array of more than one should be provided.
  """
  parents: [String]
  author: mutationInput_git_create_commit_input_author_Input
  committer: mutationInput_git_create_commit_input_committer_Input

  """
  The [PGP signature](https://en.wikipedia.org/wiki/Pretty_Good_Privacy) of the commit. GitHub adds the signature to the `gpgsig` header of the created commit. For a commit signature to be verifiable by Git or GitHub, it must be an ASCII-armored detached PGP signature over the string commit as it would be written to the object database. To pass a `signature` parameter, you need to first manually create a valid PGP signature, which can be complicated. You may find it easier to [use the command line](https://git-scm.com/book/id/v2/Git-Tools-Signing-Your-Work) to create signed commits.
  """
  signature: String
}

"""
Information about the author of the commit. By default, the `author` will be the authenticated user and the current date. See the `author` and `committer` object below for details.
"""
input mutationInput_git_create_commit_input_author_Input {
  """The name of the author (or committer) of the commit"""
  name: String!

  """The email of the author (or committer) of the commit"""
  email: String!

  """
  Indicates when this commit was authored (or committed). This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  date: DateTime
}

"""
Information about the person who is making the commit. By default, `committer` will use the information set in `author`. See the `author` and `committer` object below for details.
"""
input mutationInput_git_create_commit_input_committer_Input {
  """The name of the author (or committer) of the commit"""
  name: String

  """The email of the author (or committer) of the commit"""
  email: String

  """
  Indicates when this commit was authored (or committed). This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  date: DateTime
}

input git_create_ref_request_Input {
  """
  The name of the fully qualified reference (ie: `refs/heads/master`). If it doesn't start with 'refs' and have at least two slashes, it will be rejected.
  """
  ref: String!

  """The SHA1 value for this reference."""
  sha: String!
}

input git_update_ref_request_Input {
  """The SHA1 value to set this reference to"""
  sha: String!

  """
  Indicates whether to force the update or to make sure the update is a fast-forward update. Leaving this out or setting it to `false` will make sure you're not overwriting work.
  """
  force: Boolean
}

input git_create_tag_request_Input {
  """The tag's name. This is typically a version (e.g., "v0.0.1")."""
  tag: String!

  """The tag message."""
  message: String!

  """The SHA of the git object this is tagging."""
  object: String!
  type: mutationInput_git_create_tag_input_type!
  tagger: mutationInput_git_create_tag_input_tagger_Input
}

"""
The type of the object we're tagging. Normally this is a `commit` but it can also be a `tree` or a `blob`.
"""
enum mutationInput_git_create_tag_input_type {
  commit
  tree
  blob
}

"""An object with information about the individual creating the tag."""
input mutationInput_git_create_tag_input_tagger_Input {
  """The name of the author of the tag"""
  name: String!

  """The email of the author of the tag"""
  email: String!

  """
  When this object was tagged. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  date: DateTime
}

input git_create_tree_request_Input {
  """
  Objects (of `path`, `mode`, `type`, and `sha`) specifying a tree structure.
  """
  tree: [mutationInput_git_create_tree_input_tree_items_Input]!

  """
  The SHA1 of an existing Git tree object which will be used as the base for the new tree. If provided, a new Git tree object will be created from entries in the Git tree object pointed to by `base_tree` and entries defined in the `tree` parameter. Entries defined in the `tree` parameter will overwrite items from `base_tree` with the same `path`. If you're creating new changes on a branch, then normally you'd set `base_tree` to the SHA1 of the Git tree object of the current latest commit on the branch you're working on.
  If not provided, GitHub will create a new Git tree object from only the entries defined in the `tree` parameter. If you create a new commit pointing to such a tree, then all files which were a part of the parent commit's tree and were not defined in the `tree` parameter will be listed as deleted by the new commit.
  """
  base_tree: String
}

input mutationInput_git_create_tree_input_tree_items_Input {
  """The file referenced in the tree."""
  path: String
  mode: mutationInput_git_create_tree_input_tree_items_mode
  type: mutationInput_git_create_tree_input_tree_items_type

  """
  The SHA1 checksum ID of the object in the tree. Also called `tree.sha`. If the value is `null` then the file will be deleted.  
    
  **Note:** Use either `tree.sha` or `content` to specify the contents of the entry. Using both `tree.sha` and `content` will return an error.
  """
  sha: String

  """
  The content you want this file to have. GitHub will write this blob out and use that SHA for this entry. Use either this, or `tree.sha`.  
    
  **Note:** Use either `tree.sha` or `content` to specify the contents of the entry. Using both `tree.sha` and `content` will return an error.
  """
  content: String
}

"""
The file mode; one of `100644` for file (blob), `100755` for executable (blob), `040000` for subdirectory (tree), `160000` for submodule (commit), or `120000` for a blob that specifies the path of a symlink.
"""
enum mutationInput_git_create_tree_input_tree_items_mode {
  _100644
  _100755
  _040000
  _160000
  _120000
}

"""Either `blob`, `tree`, or `commit`."""
enum mutationInput_git_create_tree_input_tree_items_type {
  blob
  tree
  commit
}

input repos_create_webhook_request_Input {
  """
  Use `web` to create a webhook. Default: `web`. This parameter only accepts the value `web`.
  """
  name: String
  config: mutationInput_repos_create_webhook_input_config_Input

  """
  Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for.
  """
  events: [String] = ["push"]

  """
  Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.
  """
  active: Boolean = true
}

"""Key/value pairs to provide settings for this webhook."""
input mutationInput_repos_create_webhook_input_config_Input {
  """The URL to which the payloads will be delivered."""
  url: URL

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl_Input
}

input repos_update_webhook_request_Input {
  config: Webhook_Configuration_Input

  """
  Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for. This replaces the entire array of events.
  """
  events: [String] = ["push"]

  """
  Determines a list of events to be added to the list of events that the Hook triggers for.
  """
  add_events: [String]

  """
  Determines a list of events to be removed from the list of events that the Hook triggers for.
  """
  remove_events: [String]

  """
  Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.
  """
  active: Boolean = true
}

"""Configuration object of the webhook"""
input Webhook_Configuration_Input {
  """The URL to which the payloads will be delivered."""
  url: URL

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl_Input
}

input repos_update_webhook_config_for_repo_request_Input {
  """The URL to which the payloads will be delivered."""
  url: URL

  """
  The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.
  """
  content_type: String

  """
  If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).
  """
  secret: String
  insecure_ssl: webhook_config_insecure_ssl_Input
}

input migrations_start_import_request_Input {
  """The URL of the originating repository."""
  vcs_url: String!
  vcs: mutationInput_migrations_start_import_input_vcs

  """If authentication is required, the username to provide to `vcs_url`."""
  vcs_username: String

  """If authentication is required, the password to provide to `vcs_url`."""
  vcs_password: String

  """For a tfvc import, the name of the project that is being imported."""
  tfvc_project: String
}

"""
The originating VCS type. Without this parameter, the import job will take additional time to detect the VCS type before beginning the import. This detection step will be reflected in the response.
"""
enum mutationInput_migrations_start_import_input_vcs {
  subversion
  git
  mercurial
  tfvc
}

input migrations_update_import_request_Input {
  """The username to provide to the originating repository."""
  vcs_username: String

  """The password to provide to the originating repository."""
  vcs_password: String
  vcs: mutationInput_migrations_update_import_input_vcs

  """For a tfvc import, the name of the project that is being imported."""
  tfvc_project: String
}

"""The type of version control system you are migrating from."""
enum mutationInput_migrations_update_import_input_vcs {
  subversion
  tfvc
  git
  mercurial
}

input migrations_map_commit_author_request_Input {
  """The new Git author email."""
  email: String

  """The new Git author name."""
  name: String
}

input migrations_set_lfs_preference_request_Input {
  use_lfs: mutationInput_migrations_set_lfs_preference_input_use_lfs!
}

"""
Whether to store large files during the import. `opt_in` means large files will be stored using Git LFS. `opt_out` means large files will be removed during the import.
"""
enum mutationInput_migrations_set_lfs_preference_input_use_lfs {
  opt_in
  opt_out
}

input repos_update_invitation_request_Input {
  permissions: mutationInput_repos_update_invitation_input_permissions
}

"""
The permissions that the associated user will have on the repository. Valid values are `read`, `write`, `maintain`, `triage`, and `admin`.
"""
enum mutationInput_repos_update_invitation_input_permissions {
  read
  write
  maintain
  triage
  admin
}

input issues_create_request_Input {
  title: mutationInput_issues_create_input_title_Input!

  """The contents of the issue."""
  body: String

  """
  Login for the user that this issue should be assigned to. _NOTE: Only users with push access can set the assignee for new issues. The assignee is silently dropped otherwise. **This field is closing down.**_
  """
  assignee: String
  milestone: mutationInput_issues_create_input_milestone_Input

  """
  Labels to associate with this issue. _NOTE: Only users with push access can set labels for new issues. Labels are silently dropped otherwise._
  """
  labels: [mutationInput_issues_create_input_labels_items_Input]

  """
  Logins for Users to assign to this issue. _NOTE: Only users with push access can set assignees for new issues. Assignees are silently dropped otherwise._
  """
  assignees: [String]

  """
  The name of the issue type to associate with this issue. _NOTE: Only users with push access can set the type for new issues. The type is silently dropped otherwise._
  """
  type: String
}

input mutationInput_issues_create_input_title_Input {
  String: String
  Int: Int
}

input mutationInput_issues_create_input_milestone_Input {
  String: String
  Int: Int
}

input mutationInput_issues_create_input_labels_items_Input {
  String: String
  mutationInput_issues_create_input_labels_items_oneOf_1_Input: mutationInput_issues_create_input_labels_items_oneOf_1_Input
}

input mutationInput_issues_create_input_labels_items_oneOf_1_Input {
  id: Int
  name: String
  description: String
  color: String
}

input issues_update_comment_request_Input {
  """The contents of the comment."""
  body: String!
}

input reactions_create_for_issue_comment_request_Input {
  content: mutationInput_reactions_create_for_issue_comment_input_content!
}

"""
The [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions) to add to the issue comment.
"""
enum mutationInput_reactions_create_for_issue_comment_input_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

input issues_update_request_Input {
  title: mutationInput_issues_update_input_title_Input

  """The contents of the issue."""
  body: String

  """Username to assign to this issue. **This field is closing down.**"""
  assignee: String
  state: mutationInput_issues_update_input_state
  state_reason: mutationInput_issues_update_input_state_reason
  milestone: mutationInput_issues_update_input_milestone_Input

  """
  Labels to associate with this issue. Pass one or more labels to _replace_ the set of labels on this issue. Send an empty array (`[]`) to clear all labels from the issue. Only users with push access can set labels for issues. Without push access to the repository, label changes are silently dropped.
  """
  labels: [mutationInput_issues_update_input_labels_items_Input]

  """
  Usernames to assign to this issue. Pass one or more user logins to _replace_ the set of assignees on this issue. Send an empty array (`[]`) to clear all assignees from the issue. Only users with push access can set assignees for new issues. Without push access to the repository, assignee changes are silently dropped.
  """
  assignees: [String]

  """
  An array of issue field values to set on this issue. Each field value must include the field ID and the value to set. Only users with push access can set field values for issues
  """
  issue_field_values: [mutationInput_issues_update_input_issue_field_values_items_Input]

  """
  The name of the issue type to associate with this issue or use `null` to remove the current issue type. Only users with push access can set the type for issues. Without push access to the repository, type changes are silently dropped.
  """
  type: String
}

input mutationInput_issues_update_input_title_Input {
  String: String
  Int: Int
}

"""The open or closed state of the issue."""
enum mutationInput_issues_update_input_state {
  open
  closed
}

"""The reason for the state change. Ignored unless `state` is changed."""
enum mutationInput_issues_update_input_state_reason {
  completed
  not_planned
  duplicate
  reopened
}

input mutationInput_issues_update_input_milestone_Input {
  String: String
  Int: Int
}

input mutationInput_issues_update_input_labels_items_Input {
  String: String
  mutationInput_issues_update_input_labels_items_oneOf_1_Input: mutationInput_issues_update_input_labels_items_oneOf_1_Input
}

input mutationInput_issues_update_input_labels_items_oneOf_1_Input {
  id: Int
  name: String
  description: String
  color: String
}

input mutationInput_issues_update_input_issue_field_values_items_Input {
  """The ID of the issue field to set"""
  field_id: Int!
  value: mutationInput_issues_update_input_issue_field_values_items_value_Input!
}

input mutationInput_issues_update_input_issue_field_values_items_value_Input {
  String: String
  Float: Float
}

input issues_add_assignees_request_Input {
  """
  Usernames of people to assign this issue to. _NOTE: Only users with push access can add assignees to an issue. Assignees are silently ignored otherwise._
  """
  assignees: [String]
}

input issues_remove_assignees_request_Input {
  """
  Usernames of assignees to remove from an issue. _NOTE: Only users with push access can remove assignees from an issue. Assignees are silently ignored otherwise._
  """
  assignees: [String]
}

input issues_create_comment_request_Input {
  """The contents of the comment."""
  body: String!
}

input issues_add_blocked_by_dependency_request_Input {
  """The id of the issue that blocks the current issue"""
  issue_id: Int!
}

input issues_add_labels_request_Input {
  mutationInput_issues_add_labels_input_oneOf_0_Input: mutationInput_issues_add_labels_input_oneOf_0_Input
  String: String
  mutationInput_issues_add_labels_input_oneOf_2_items_Input: mutationInput_issues_add_labels_input_oneOf_2_items_Input
}

input mutationInput_issues_add_labels_input_oneOf_0_Input {
  """
  The names of the labels to add to the issue's existing labels. You can also pass an `array` of labels directly, but GitHub recommends passing an object with the `labels` key. To replace all of the labels for an issue, use "[Set labels for an issue](https://docs.github.com/rest/issues/labels#set-labels-for-an-issue)."
  """
  labels: [String]
}

input mutationInput_issues_add_labels_input_oneOf_2_items_Input {
  name: String!
}

input issues_set_labels_request_Input {
  mutationInput_issues_set_labels_input_oneOf_0_Input: mutationInput_issues_set_labels_input_oneOf_0_Input
  String: String
  mutationInput_issues_set_labels_input_oneOf_2_Input: mutationInput_issues_set_labels_input_oneOf_2_Input
  mutationInput_issues_set_labels_input_oneOf_3_items_Input: mutationInput_issues_set_labels_input_oneOf_3_items_Input
}

input mutationInput_issues_set_labels_input_oneOf_0_Input {
  """
  The names of the labels to set for the issue. The labels you set replace any existing labels. You can pass an empty array to remove all labels. Alternatively, you can pass a single label as a `string` or an `array` of labels directly, but GitHub recommends passing an object with the `labels` key. You can also add labels to the existing labels for an issue. For more information, see "[Add labels to an issue](https://docs.github.com/rest/issues/labels#add-labels-to-an-issue)."
  """
  labels: [String]
}

input mutationInput_issues_set_labels_input_oneOf_2_Input {
  labels: [mutationInput_issues_set_labels_input_oneOf_2_labels_items_Input]
}

input mutationInput_issues_set_labels_input_oneOf_2_labels_items_Input {
  name: String!
}

input mutationInput_issues_set_labels_input_oneOf_3_items_Input {
  name: String!
}

input issues_lock_request_Input {
  lock_reason: mutationInput_issues_lock_input_lock_reason
}

"""
The reason for locking the issue or pull request conversation. Lock will fail if you don't use one of these reasons:  
 * `off-topic`  
 * `too heated`  
 * `resolved`  
 * `spam`
"""
enum mutationInput_issues_lock_input_lock_reason {
  off_topic
  too_heated
  resolved
  spam
}

input reactions_create_for_issue_request_Input {
  content: mutationInput_reactions_create_for_issue_input_content!
}

"""
The [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions) to add to the issue.
"""
enum mutationInput_reactions_create_for_issue_input_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

input issues_remove_sub_issue_request_Input {
  """The id of the sub-issue to remove"""
  sub_issue_id: Int!
}

input issues_add_sub_issue_request_Input {
  """
  The id of the sub-issue to add. The sub-issue must belong to the same repository owner as the parent issue
  """
  sub_issue_id: Int!

  """
  Option that, when true, instructs the operation to replace the sub-issues current parent issue
  """
  replace_parent: Boolean
}

input issues_reprioritize_sub_issue_request_Input {
  """The id of the sub-issue to reprioritize"""
  sub_issue_id: Int!

  """
  The id of the sub-issue to be prioritized after (either positional argument after OR before should be specified).
  """
  after_id: Int

  """
  The id of the sub-issue to be prioritized before (either positional argument after OR before should be specified).
  """
  before_id: Int
}

input repos_create_deploy_key_request_Input {
  """A name for the key."""
  title: String

  """The contents of the key."""
  key: String!

  """
  If `true`, the key will only be able to read repository contents. Otherwise, the key will be able to read and write.  
    
  Deploy keys with write access can perform the same actions as an organization member with admin access, or a collaborator on a personal repository. For more information, see "[Repository permission levels for an organization](https://docs.github.com/articles/repository-permission-levels-for-an-organization/)" and "[Permission levels for a user account repository](https://docs.github.com/articles/permission-levels-for-a-user-account-repository/)."
  """
  read_only: Boolean
}

input issues_create_label_request_Input {
  """
  The name of the label. Emoji can be added to label names, using either native emoji or colon-style markup. For example, typing `:strawberry:` will render the emoji ![:strawberry:](https://github.githubassets.com/images/icons/emoji/unicode/1f353.png ":strawberry:"). For a full list of available emoji and codes, see "[Emoji cheat sheet](https://github.com/ikatyang/emoji-cheat-sheet)."
  """
  name: String!

  """
  The [hexadecimal color code](http://www.color-hex.com/) for the label, without the leading `#`.
  """
  color: String

  """A short description of the label. Must be 100 characters or fewer."""
  description: String
}

input issues_update_label_request_Input {
  """
  The new name of the label. Emoji can be added to label names, using either native emoji or colon-style markup. For example, typing `:strawberry:` will render the emoji ![:strawberry:](https://github.githubassets.com/images/icons/emoji/unicode/1f353.png ":strawberry:"). For a full list of available emoji and codes, see "[Emoji cheat sheet](https://github.com/ikatyang/emoji-cheat-sheet)."
  """
  new_name: String

  """
  The [hexadecimal color code](http://www.color-hex.com/) for the label, without the leading `#`.
  """
  color: String

  """A short description of the label. Must be 100 characters or fewer."""
  description: String
}

"""Results of a successful merge upstream request"""
type Merged_upstream {
  message: String
  merge_type: mutation_repos_merge_upstream_merge_type
  base_branch: String
}

enum mutation_repos_merge_upstream_merge_type {
  merge
  fast_forward
  none
}

input repos_merge_upstream_request_Input {
  """The name of the branch which should be updated to match upstream."""
  branch: String!
}

union repos_merge_response = Commit | Void_container

input repos_merge_request_Input {
  """The name of the base branch that the head will be merged into."""
  base: String!

  """The head to merge. This can be a branch name or a commit SHA1."""
  head: String!

  """
  Commit message to use for the merge commit. If omitted, a default message will be used.
  """
  commit_message: String
}

input issues_create_milestone_request_Input {
  """The title of the milestone."""
  title: String!
  state: mutationInput_issues_create_milestone_input_state = open

  """A description of the milestone."""
  description: String

  """
  The milestone due date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  due_on: DateTime
}

"""The state of the milestone. Either `open` or `closed`."""
enum mutationInput_issues_create_milestone_input_state {
  open
  closed
}

input issues_update_milestone_request_Input {
  """The title of the milestone."""
  title: String
  state: mutationInput_issues_update_milestone_input_state = open

  """A description of the milestone."""
  description: String

  """
  The milestone due date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  due_on: DateTime
}

"""The state of the milestone. Either `open` or `closed`."""
enum mutationInput_issues_update_milestone_input_state {
  open
  closed
}

type activity_mark_repo_notifications_as_read_202_response {
  message: String
  url: String
}

input activity_mark_repo_notifications_as_read_request_Input {
  """
  Describes the last point that notifications were checked. Anything updated since this time will not be marked as read. If you omit this parameter, all notifications are marked as read. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Default: The current timestamp.
  """
  last_read_at: DateTime
}

"""The source branch and directory used to publish your Pages site."""
input repos_create_pages_site_request_Input {
  source: mutationInput_repos_create_pages_site_input_source_Input
  build_type: mutationInput_repos_create_pages_site_input_build_type
}

"""The source branch and directory used to publish your Pages site."""
input mutationInput_repos_create_pages_site_input_source_Input {
  """The repository branch used to publish your site's source files."""
  branch: String!
  path: mutationInput_repos_create_pages_site_input_source_path = _
}

"""
The repository directory that includes the source files for the Pages site. Allowed paths are `/` or `/docs`. Default: `/`
"""
enum mutationInput_repos_create_pages_site_input_source_path {
  _
  _docs
}

"""
The process in which the Page will be built. Possible values are `"legacy"` and `"workflow"`.
"""
enum mutationInput_repos_create_pages_site_input_build_type {
  legacy
  workflow
}

input repos_update_information_about_pages_site_request_Input {
  build_type: mutationInput_repos_update_information_about_pages_site_input_build_type
  source: JSON

  """
  Specify a custom domain for the repository. Sending a `null` value will remove the custom domain. For more about custom domains, see "[Using a custom domain with GitHub Pages](https://docs.github.com/pages/configuring-a-custom-domain-for-your-github-pages-site)."
  """
  cname: String
  public: JSON!

  """Specify whether HTTPS should be enforced for the repository."""
  https_enforced: Boolean
}

"""
The process by which the GitHub Pages site will be built. `workflow` means that the site is built by a custom GitHub Actions workflow. `legacy` means that the site is built by GitHub when changes are pushed to a specific branch.
"""
enum mutationInput_repos_update_information_about_pages_site_input_build_type {
  legacy
  workflow
}

"""Page Build Status"""
type Page_Build_Status {
  url: URL!
  status: String!
}

"""The GitHub Pages deployment status."""
type page_deployment {
  id: mutation_repos_create_pages_deployment_id!

  """The URI to monitor GitHub Pages deployment status."""
  status_url: URL!

  """The URI to the deployed GitHub Pages."""
  page_url: URL!

  """The URI to the deployed GitHub Pages preview."""
  preview_url: URL
}

"""
The ID of the GitHub Pages deployment. This is the Git SHA of the deployed commit.
"""
union mutation_repos_create_pages_deployment_id = Int_container | String_container

"""The object used to create GitHub Pages deployment"""
input repos_create_pages_deployment_request_Input {
  """
  The ID of an artifact that contains the .zip or .tar of static assets to deploy. The artifact belongs to the repository. Either `artifact_id` or `artifact_url` are required.
  """
  artifact_id: Float

  """
  The URL of an artifact that contains the .zip or .tar of static assets to deploy. The artifact belongs to the repository. Either `artifact_id` or `artifact_url` are required.
  """
  artifact_url: String

  """The target environment for this GitHub Pages deployment."""
  environment: String = "github-pages"

  """
  A unique string that represents the version of the build for this deployment.
  """
  pages_build_version: String! = "GITHUB_SHA"

  """
  The OIDC token issued by GitHub Actions certifying the origin of the deployment.
  """
  oidc_token: String!
}

input repos_custom_properties_for_repos_create_or_update_repository_values_request_Input {
  """
  A list of custom property names and associated values to apply to the repositories.
  """
  properties: [Custom_Property_Value_Input]!
}

input pulls_create_request_Input {
  """
  The title of the new pull request. Required unless `issue` is specified.
  """
  title: String

  """
  The name of the branch where your changes are implemented. For cross-repository pull requests in the same network, namespace `head` with a user like this: `username:branch`.
  """
  head: String!

  """
  The name of the repository where the changes in the pull request were made. This field is required for cross-repository pull requests if both repositories are owned by the same organization.
  """
  head_repo: String

  """
  The name of the branch you want the changes pulled into. This should be an existing branch on the current repository. You cannot submit a pull request to one repository that requests a merge to a base of another repository.
  """
  base: String!

  """The contents of the pull request."""
  body: String

  """
  Indicates whether [maintainers can modify](https://docs.github.com/articles/allowing-changes-to-a-pull-request-branch-created-from-a-fork/) the pull request.
  """
  maintainer_can_modify: Boolean

  """
  Indicates whether the pull request is a draft. See "[Draft Pull Requests](https://docs.github.com/articles/about-pull-requests#draft-pull-requests)" in the GitHub Help documentation to learn more.
  """
  draft: Boolean

  """
  An issue in the repository to convert to a pull request. The issue title, body, and comments will become the title, body, and comments on the new pull request. Required unless `title` is specified.
  """
  issue: BigInt
}

input pulls_update_review_comment_request_Input {
  """The text of the reply to the review comment."""
  body: String!
}

input reactions_create_for_pull_request_review_comment_request_Input {
  content: mutationInput_reactions_create_for_pull_request_review_comment_input_content!
}

"""
The [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions) to add to the pull request review comment.
"""
enum mutationInput_reactions_create_for_pull_request_review_comment_input_content {
  _0PLUS_1
  NEGATIVE_1
  laugh
  confused
  heart
  hooray
  rocket
  eyes
}

input pulls_update_request_Input {
  """The title of the pull request."""
  title: String

  """The contents of the pull request."""
  body: String
  state: mutationInput_pulls_update_input_state

  """
  The name of the branch you want your changes pulled into. This should be an existing branch on the current repository. You cannot update the base branch on a pull request to point to another repository.
  """
  base: String

  """
  Indicates whether [maintainers can modify](https://docs.github.com/articles/allowing-changes-to-a-pull-request-branch-created-from-a-fork/) the pull request.
  """
  maintainer_can_modify: Boolean
}

"""State of this Pull Request. Either `open` or `closed`."""
enum mutationInput_pulls_update_input_state {
  open
  closed
}

input codespaces_create_with_pr_for_authenticated_user_request_Input {
  """
  The requested location for a new codespace. Best efforts are made to respect this upon creation. Assigned by IP if not provided.
  """
  location: String
  geo: mutationInput_codespaces_create_with_pr_for_authenticated_user_input_geo

  """IP for location auto-detection when proxying a request"""
  client_ip: String

  """Machine type to use for this codespace"""
  machine: String

  """Path to devcontainer.json config to use for this codespace"""
  devcontainer_path: String

  """Whether to authorize requested permissions from devcontainer.json"""
  multi_repo_permissions_opt_out: Boolean

  """Working directory for this codespace"""
  working_directory: String

  """Time in minutes before codespace stops from inactivity"""
  idle_timeout_minutes: Int

  """Display name for this codespace"""
  display_name: String

  """
  Duration in minutes after codespace has gone idle in which it will be deleted. Must be integer minutes between 0 and 43200 (30 days).
  """
  retention_period_minutes: Int
}

"""
The geographic area for this codespace. If not specified, the value is assigned by IP. This property replaces `location`, which is closing down.
"""
enum mutationInput_codespaces_create_with_pr_for_authenticated_user_input_geo {
  EuropeWest
  SoutheastAsia
  UsEast
  UsWest
}

input pulls_create_review_comment_request_Input {
  """The text of the review comment."""
  body: String!

  """
  The SHA of the commit needing a comment. Not using the latest commit SHA may render your comment outdated if a subsequent commit modifies the line you specify as the `position`.
  """
  commit_id: String!

  """The relative path to the file that necessitates a comment."""
  path: String!

  """
  **This parameter is closing down. Use `line` instead**. The position in the diff where you want to add a review comment. Note this value is not the same as the line number in the file. The position value equals the number of lines down from the first "@@" hunk header in the file you want to add a comment. The line just below the "@@" line is position 1, the next line is position 2, and so on. The position in the diff continues to increase through lines of whitespace and additional hunks until the beginning of a new file.
  """
  position: Int
  side: mutationInput_pulls_create_review_comment_input_side

  """
  **Required unless using `subject_type:file`**. The line of the blob in the pull request diff that the comment applies to. For a multi-line comment, the last line of the range that your comment applies to.
  """
  line: Int

  """
  **Required when using multi-line comments unless using `in_reply_to`**. The `start_line` is the first line in the pull request diff that your multi-line comment applies to. To learn more about multi-line comments, see "[Commenting on a pull request](https://docs.github.com/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation.
  """
  start_line: Int
  start_side: mutationInput_pulls_create_review_comment_input_start_side

  """
  The ID of the review comment to reply to. To find the ID of a review comment with ["List review comments on a pull request"](#list-review-comments-on-a-pull-request). When specified, all parameters other than `body` in the request body are ignored.
  """
  in_reply_to: Int
  subject_type: mutationInput_pulls_create_review_comment_input_subject_type
}

"""
In a split diff view, the side of the diff that the pull request's changes appear on. Can be `LEFT` or `RIGHT`. Use `LEFT` for deletions that appear in red. Use `RIGHT` for additions that appear in green or unchanged lines that appear in white and are shown for context. For a multi-line comment, side represents whether the last line of the comment range is a deletion or addition. For more information, see "[Diff view options](https://docs.github.com/articles/about-comparing-branches-in-pull-requests#diff-view-options)" in the GitHub Help documentation.
"""
enum mutationInput_pulls_create_review_comment_input_side {
  LEFT
  RIGHT
}

"""
**Required when using multi-line comments unless using `in_reply_to`**. The `start_side` is the starting side of the diff that the comment applies to. Can be `LEFT` or `RIGHT`. To learn more about multi-line comments, see "[Commenting on a pull request](https://docs.github.com/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation. See `side` in this table for additional context.
"""
enum mutationInput_pulls_create_review_comment_input_start_side {
  LEFT
  RIGHT
  side
}

"""The level at which the comment is targeted."""
enum mutationInput_pulls_create_review_comment_input_subject_type {
  line
  file
}

input pulls_create_reply_for_review_comment_request_Input {
  """The text of the review comment."""
  body: String!
}

"""Pull Request Merge Result"""
type Pull_Request_Merge_Result {
  sha: String!
  merged: Boolean!
  message: String!
}

input pulls_merge_request_Input {
  """Title for the automatic commit message."""
  commit_title: String

  """Extra detail to append to automatic commit message."""
  commit_message: String

  """SHA that pull request head must match to allow merge."""
  sha: String
  merge_method: mutationInput_pulls_merge_input_merge_method
}

"""The merge method to use."""
enum mutationInput_pulls_merge_input_merge_method {
  merge
  squash
  rebase
}

input pulls_request_reviewers_request_Input {
  """An array of user `login`s that will be requested."""
  reviewers: [String]

  """An array of team `slug`s that will be requested."""
  team_reviewers: [String]
}

input pulls_remove_requested_reviewers_request_Input {
  """An array of user `login`s that will be removed."""
  reviewers: [String]!

  """An array of team `slug`s that will be removed."""
  team_reviewers: [String]
}

input pulls_create_review_request_Input {
  """
  The SHA of the commit that needs a review. Not using the latest commit SHA may render your review comment outdated if a subsequent commit modifies the line you specify as the `position`. Defaults to the most recent commit in the pull request when you do not specify a value.
  """
  commit_id: String

  """
  **Required** when using `REQUEST_CHANGES` or `COMMENT` for the `event` parameter. The body text of the pull request review.
  """
  body: String
  event: mutationInput_pulls_create_review_input_event

  """
  Use the following table to specify the location, destination, and contents of the draft review comment.
  """
  comments: [mutationInput_pulls_create_review_input_comments_items_Input]
}

"""
The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. By leaving this blank, you set the review action state to `PENDING`, which means you will need to [submit the pull request review](https://docs.github.com/rest/pulls/reviews#submit-a-review-for-a-pull-request) when you are ready.
"""
enum mutationInput_pulls_create_review_input_event {
  APPROVE
  REQUEST_CHANGES
  COMMENT
}

input mutationInput_pulls_create_review_input_comments_items_Input {
  """The relative path to the file that necessitates a review comment."""
  path: String!

  """
  The position in the diff where you want to add a review comment. Note this value is not the same as the line number in the file. The `position` value equals the number of lines down from the first "@@" hunk header in the file you want to add a comment. The line just below the "@@" line is position 1, the next line is position 2, and so on. The position in the diff continues to increase through lines of whitespace and additional hunks until the beginning of a new file.
  """
  position: Int

  """Text of the review comment."""
  body: String!
  line: Int
  side: String
  start_line: Int
  start_side: String
}

input pulls_update_review_request_Input {
  """The body text of the pull request review."""
  body: String!
}

input pulls_dismiss_review_request_Input {
  """The message for the pull request review dismissal"""
  message: String!
  event: DISMISS_const
}

enum DISMISS_const {
  DISMISS
}

input pulls_submit_review_request_Input {
  """The body text of the pull request review"""
  body: String
  event: mutationInput_pulls_submit_review_input_event!
}

"""
The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. When you leave this blank, the API returns _HTTP 422 (Unrecognizable entity)_ and sets the review action state to `PENDING`, which means you will need to re-submit the pull request review using a review action.
"""
enum mutationInput_pulls_submit_review_input_event {
  APPROVE
  REQUEST_CHANGES
  COMMENT
}

type pulls_update_branch_202_response {
  message: String
  url: String
}

input pulls_update_branch_request_Input {
  """
  The expected SHA of the pull request's HEAD ref. This is the most recent commit on the pull request's branch. If the expected SHA does not match the pull request's HEAD, you will receive a `422 Unprocessable Entity` status. You can use the "[List commits](https://docs.github.com/rest/commits/commits#list-commits)" endpoint to find the most recent commit SHA. Default: SHA of the pull request's current HEAD ref.
  """
  expected_head_sha: String
}

input repos_create_release_request_Input {
  """The name of the tag."""
  tag_name: String!

  """
  Specifies the commitish value that determines where the Git tag is created from. Can be any branch or commit SHA. Unused if the Git tag already exists. Default: the repository's default branch.
  """
  target_commitish: String

  """The name of the release."""
  name: String

  """Text describing the contents of the tag."""
  body: String

  """
  `true` to create a draft (unpublished) release, `false` to create a published one.
  """
  draft: Boolean

  """
  `true` to identify the release as a prerelease. `false` to identify the release as a full release.
  """
  prerelease: Boolean

  """
  If specified, a discussion of the specified category is created and linked to the release. The value must be a category that already exists in the repository. For more information, see "[Managing categories for discussions in your repository](https://docs.github.com/discussions/managing-discussions-for-your-community/managing-categories-for-discussions-in-your-repository)."
  """
  discussion_category_name: String

  """
  Whether to automatically generate the name and body for this release. If `name` is specified, the specified name will be used; otherwise, a name will be automatically generated. If `body` is specified, the body will be pre-pended to the automatically generated notes.
  """
  generate_release_notes: Boolean
  make_latest: mutationInput_repos_create_release_input_make_latest = TRUE
}

"""
Specifies whether this release should be set as the latest release for the repository. Drafts and prereleases cannot be set as latest. Defaults to `true` for newly published releases. `legacy` specifies that the latest release should be determined based on the release creation date and higher semantic version.
"""
enum mutationInput_repos_create_release_input_make_latest {
  TRUE
  FALSE
  legacy
}

input repos_update_release_asset_request_Input {
  """The file name of the asset."""
  name: String

  """
  An alternate short description of the asset. Used in place of the filename.
  """
  label: String
  state: String
}

"""Generated name and body describing a release"""
type Generated_Release_Notes_Content {
  """The generated name of the release"""
  name: String!

  """
  The generated body describing the contents of the release supporting markdown formatting
  """
  body: String!
}

input repos_generate_release_notes_request_Input {
  """
  The tag name for the release. This can be an existing tag or a new one.
  """
  tag_name: String!

  """
  Specifies the commitish value that will be the target for the release's tag. Required if the supplied tag_name does not reference an existing tag. Ignored if the tag_name already exists.
  """
  target_commitish: String

  """
  The name of the previous tag to use as the starting point for the release notes. Use to manually specify the range for the set of changes considered as part this release.
  """
  previous_tag_name: String

  """
  Specifies a path to a file in the repository containing configuration settings used for generating the release notes. If unspecified, the configuration file located in the repository at '.github/release.yml' or '.github/release.yaml' will be used. If that is not present, the default configuration will be used.
  """
  configuration_file_path: String
}

input repos_update_release_request_Input {
  """The name of the tag."""
  tag_name: String

  """
  Specifies the commitish value that determines where the Git tag is created from. Can be any branch or commit SHA. Unused if the Git tag already exists. Default: the repository's default branch.
  """
  target_commitish: String

  """The name of the release."""
  name: String

  """Text describing the contents of the tag."""
  body: String

  """`true` makes the release a draft, and `false` publishes the release."""
  draft: Boolean

  """
  `true` to identify the release as a prerelease, `false` to identify the release as a full release.
  """
  prerelease: Boolean
  make_latest: mutationInput_repos_update_release_input_make_latest

  """
  If specified, a discussion of the specified category is created and linked to the release. The value must be a category that already exists in the repository. If there is already a discussion linked to the release, this parameter is ignored. For more information, see "[Managing categories for discussions in your repository](https://docs.github.com/discussions/managing-discussions-for-your-community/managing-categories-for-discussions-in-your-repository)."
  """
  discussion_category_name: String
}

"""
Specifies whether this release should be set as the latest release for the repository. Drafts and prereleases cannot be set as latest. Defaults to `true` for newly published releases. `legacy` specifies that the latest release should be determined based on the release creation date and higher semantic version.
"""
enum mutationInput_repos_update_release_input_make_latest {
  TRUE
  FALSE
  legacy
}

"""The `File` scalar type represents a file upload."""
scalar File

input reactions_create_for_release_request_Input {
  content: mutationInput_reactions_create_for_release_input_content!
}

"""
The [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions) to add to the release.
"""
enum mutationInput_reactions_create_for_release_input_content {
  _0PLUS_1
  laugh
  heart
  hooray
  rocket
  eyes
}

input repos_create_repo_ruleset_request_Input {
  """The name of the ruleset."""
  name: String!
  target: mutationInput_repos_create_repo_ruleset_input_target = branch
  enforcement: repository_rule_enforcement!

  """The actors that can bypass the rules in this ruleset"""
  bypass_actors: [Repository_Ruleset_Bypass_Actor_Input]
  conditions: Repository_ruleset_conditions_for_ref_names_Input

  """An array of rules within the ruleset."""
  rules: [repository_rule_Input]
}

"""The target of the ruleset"""
enum mutationInput_repos_create_repo_ruleset_input_target {
  branch
  tag
  push
}

"""Parameters for a repository ruleset ref name condition"""
input Repository_ruleset_conditions_for_ref_names_Input {
  ref_name: query_repos_get_org_rulesets_items_conditions_anyOf_0_ref_name_Input
}

input repository_rule_Input {
  creation_Input: creation_Input
  update_Input: update_Input
  deletion_Input: deletion_Input
  required_linear_history_Input: required_linear_history_Input
  merge_queue_Input: merge_queue_Input
  required_deployments_Input: required_deployments_Input
  required_signatures_Input: required_signatures_Input
  pull_request_Input: pull_request_Input
  required_status_checks_Input: required_status_checks_Input
  non_fast_forward_Input: non_fast_forward_Input
  commit_message_pattern_Input: commit_message_pattern_Input
  commit_author_email_pattern_Input: commit_author_email_pattern_Input
  committer_email_pattern_Input: committer_email_pattern_Input
  branch_name_pattern_Input: branch_name_pattern_Input
  tag_name_pattern_Input: tag_name_pattern_Input
  file_path_restriction_Input: file_path_restriction_Input
  max_file_path_length_Input: max_file_path_length_Input
  file_extension_restriction_Input: file_extension_restriction_Input
  max_file_size_Input: max_file_size_Input
  workflows_Input: workflows_Input
  code_scanning_Input: code_scanning_Input
  copilot_code_review_Input: copilot_code_review_Input
}

"""Merges must be performed via a merge queue."""
input merge_queue_Input {
  type: merge_queue_const!
  parameters: query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_Input
}

input query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_Input {
  """
  Maximum time for a required status check to report a conclusion. After this much time has elapsed, checks that have not reported a conclusion will be assumed to have failed
  """
  check_response_timeout_minutes: PositiveInt!
  grouping_strategy: query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_grouping_strategy!

  """
  Limit the number of queued pull requests requesting checks and workflow runs at the same time.
  """
  max_entries_to_build: NonNegativeInt!

  """The maximum number of PRs that will be merged together in a group."""
  max_entries_to_merge: NonNegativeInt!
  merge_method: query_repos_get_org_rulesets_items_rules_items_oneOf_4_parameters_merge_method!

  """The minimum number of PRs that will be merged together in a group."""
  min_entries_to_merge: NonNegativeInt!

  """
  The time merge queue should wait after the first PR is added to the queue for the minimum group size to be met. After this time has elapsed, the minimum group size will be ignored and a smaller group will be merged.
  """
  min_entries_to_merge_wait_minutes: NonNegativeInt!
}

input repos_update_repo_ruleset_request_Input {
  """The name of the ruleset."""
  name: String
  target: mutationInput_repos_update_repo_ruleset_input_target
  enforcement: repository_rule_enforcement

  """The actors that can bypass the rules in this ruleset"""
  bypass_actors: [Repository_Ruleset_Bypass_Actor_Input]
  conditions: Repository_ruleset_conditions_for_ref_names_Input

  """An array of rules within the ruleset."""
  rules: [repository_rule_Input]
}

"""The target of the ruleset"""
enum mutationInput_repos_update_repo_ruleset_input_target {
  branch
  tag
  push
}

input secret_scanning_update_alert_request_Input {
  state: secret_scanning_alert_state

  """
  The username of the user to assign to the alert. Set to `null` to unassign the alert.
  """
  assignee: String
  resolution: secret_scanning_alert_resolution

  """
  An optional comment when closing or reopening an alert. Cannot be updated or deleted.
  """
  resolution_comment: String
}

type secret_scanning_push_protection_bypass {
  reason: secret_scanning_push_protection_bypass_reason

  """
  The time that the bypass will expire in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.
  """
  expire_at: DateTime

  """The token type this bypass is for."""
  token_type: String
}

"""The reason for bypassing push protection."""
enum secret_scanning_push_protection_bypass_reason {
  false_positive
  used_in_tests
  will_fix_later
}

input secret_scanning_create_push_protection_bypass_request_Input {
  reason: secret_scanning_push_protection_bypass_reason!

  """
  The ID of the push protection bypass placeholder. This value is returned on any push protected routes.
  """
  placeholder_id: String!
}

input repository_advisory_create_Input {
  """A short summary of the advisory."""
  summary: mutationInput_security_advisories_create_repository_advisory_input_summary!

  """A detailed description of what the advisory impacts."""
  description: mutationInput_security_advisories_create_repository_advisory_input_description!

  """The Common Vulnerabilities and Exposures (CVE) ID."""
  cve_id: String

  """
  A product affected by the vulnerability detailed in a repository security advisory.
  """
  vulnerabilities: [mutationInput_security_advisories_create_repository_advisory_input_vulnerabilities_items_Input]!

  """A list of Common Weakness Enumeration (CWE) IDs."""
  cwe_ids: [String]

  """
  A list of users receiving credit for their participation in the security advisory.
  """
  credits: [mutationInput_security_advisories_create_repository_advisory_input_credits_items_Input]
  severity: mutationInput_security_advisories_create_repository_advisory_input_severity

  """
  The CVSS vector that calculates the severity of the advisory. You must choose between setting this field or `severity`.
  """
  cvss_vector_string: String

  """
  Whether to create a temporary private fork of the repository to collaborate on a fix.
  """
  start_private_fork: Boolean
}

"""A short summary of the advisory."""
scalar mutationInput_security_advisories_create_repository_advisory_input_summary

"""A detailed description of what the advisory impacts."""
scalar mutationInput_security_advisories_create_repository_advisory_input_description

input mutationInput_security_advisories_create_repository_advisory_input_vulnerabilities_items_Input {
  package: mutationInput_security_advisories_create_repository_advisory_input_vulnerabilities_items_package_Input!

  """The range of the package versions affected by the vulnerability."""
  vulnerable_version_range: String

  """The package version(s) that resolve the vulnerability."""
  patched_versions: String

  """The functions in the package that are affected."""
  vulnerable_functions: [String]
}

"""The name of the package affected by the vulnerability."""
input mutationInput_security_advisories_create_repository_advisory_input_vulnerabilities_items_package_Input {
  ecosystem: security_advisory_ecosystems!

  """The unique package name within its ecosystem."""
  name: String
}

input mutationInput_security_advisories_create_repository_advisory_input_credits_items_Input {
  """The username of the user credited."""
  login: String!
  type: security_advisory_credit_types!
}

"""
The severity of the advisory. You must choose between setting this field or `cvss_vector_string`.
"""
enum mutationInput_security_advisories_create_repository_advisory_input_severity {
  critical
  high
  medium
  low
}

input private_vulnerability_report_create_Input {
  """A short summary of the advisory."""
  summary: mutationInput_security_advisories_create_private_vulnerability_report_input_summary!

  """A detailed description of what the advisory impacts."""
  description: mutationInput_security_advisories_create_private_vulnerability_report_input_description!

  """
  An array of products affected by the vulnerability detailed in a repository security advisory.
  """
  vulnerabilities: [mutationInput_security_advisories_create_private_vulnerability_report_input_vulnerabilities_items_Input]

  """A list of Common Weakness Enumeration (CWE) IDs."""
  cwe_ids: [String]
  severity: mutationInput_security_advisories_create_private_vulnerability_report_input_severity

  """
  The CVSS vector that calculates the severity of the advisory. You must choose between setting this field or `severity`.
  """
  cvss_vector_string: String

  """
  Whether to create a temporary private fork of the repository to collaborate on a fix.
  """
  start_private_fork: Boolean
}

"""A short summary of the advisory."""
scalar mutationInput_security_advisories_create_private_vulnerability_report_input_summary

"""A detailed description of what the advisory impacts."""
scalar mutationInput_security_advisories_create_private_vulnerability_report_input_description

input mutationInput_security_advisories_create_private_vulnerability_report_input_vulnerabilities_items_Input {
  package: mutationInput_security_advisories_create_private_vulnerability_report_input_vulnerabilities_items_package_Input!

  """The range of the package versions affected by the vulnerability."""
  vulnerable_version_range: String

  """The package version(s) that resolve the vulnerability."""
  patched_versions: String

  """The functions in the package that are affected."""
  vulnerable_functions: [String]
}

"""The name of the package affected by the vulnerability."""
input mutationInput_security_advisories_create_private_vulnerability_report_input_vulnerabilities_items_package_Input {
  ecosystem: security_advisory_ecosystems!

  """The unique package name within its ecosystem."""
  name: String
}

"""
The severity of the advisory. You must choose between setting this field or `cvss_vector_string`.
"""
enum mutationInput_security_advisories_create_private_vulnerability_report_input_severity {
  critical
  high
  medium
  low
}

input repository_advisory_update_Input {
  """A short summary of the advisory."""
  summary: mutationInput_security_advisories_update_repository_advisory_input_summary

  """A detailed description of what the advisory impacts."""
  description: mutationInput_security_advisories_update_repository_advisory_input_description

  """The Common Vulnerabilities and Exposures (CVE) ID."""
  cve_id: String

  """
  A product affected by the vulnerability detailed in a repository security advisory.
  """
  vulnerabilities: [mutationInput_security_advisories_update_repository_advisory_input_vulnerabilities_items_Input]

  """A list of Common Weakness Enumeration (CWE) IDs."""
  cwe_ids: [String]

  """
  A list of users receiving credit for their participation in the security advisory.
  """
  credits: [mutationInput_security_advisories_update_repository_advisory_input_credits_items_Input]
  severity: mutationInput_security_advisories_update_repository_advisory_input_severity

  """
  The CVSS vector that calculates the severity of the advisory. You must choose between setting this field or `severity`.
  """
  cvss_vector_string: String
  state: mutationInput_security_advisories_update_repository_advisory_input_state

  """
  A list of usernames who have been granted write access to the advisory.
  """
  collaborating_users: [String]

  """
  A list of team slugs which have been granted write access to the advisory.
  """
  collaborating_teams: [String]
}

"""A short summary of the advisory."""
scalar mutationInput_security_advisories_update_repository_advisory_input_summary

"""A detailed description of what the advisory impacts."""
scalar mutationInput_security_advisories_update_repository_advisory_input_description

input mutationInput_security_advisories_update_repository_advisory_input_vulnerabilities_items_Input {
  package: mutationInput_security_advisories_update_repository_advisory_input_vulnerabilities_items_package_Input!

  """The range of the package versions affected by the vulnerability."""
  vulnerable_version_range: String

  """The package version(s) that resolve the vulnerability."""
  patched_versions: String

  """The functions in the package that are affected."""
  vulnerable_functions: [String]
}

"""The name of the package affected by the vulnerability."""
input mutationInput_security_advisories_update_repository_advisory_input_vulnerabilities_items_package_Input {
  ecosystem: security_advisory_ecosystems!

  """The unique package name within its ecosystem."""
  name: String
}

input mutationInput_security_advisories_update_repository_advisory_input_credits_items_Input {
  """The username of the user credited."""
  login: String!
  type: security_advisory_credit_types!
}

"""
The severity of the advisory. You must choose between setting this field or `cvss_vector_string`.
"""
enum mutationInput_security_advisories_update_repository_advisory_input_severity {
  critical
  high
  medium
  low
}

"""The state of the advisory."""
enum mutationInput_security_advisories_update_repository_advisory_input_state {
  published
  closed
  draft
}

input repos_create_commit_status_request_Input {
  state: mutationInput_repos_create_commit_status_input_state!

  """
  The target URL to associate with this status. This URL will be linked from the GitHub UI to allow users to easily see the source of the status.  
  For example, if your continuous integration system is posting build status, you would want to provide the deep link for the build output for this specific SHA:  
  `http://ci.example.com/user/repo/build/sha`
  """
  target_url: String

  """A short description of the status."""
  description: String

  """
  A string label to differentiate this status from the status of other systems. This field is case-insensitive.
  """
  context: String = "default"
}

"""The state of the status."""
enum mutationInput_repos_create_commit_status_input_state {
  error
  failure
  pending
  success
}

input activity_set_repo_subscription_request_Input {
  """Determines if notifications should be received from this repository."""
  subscribed: Boolean

  """
  Determines if all notifications should be blocked from this repository.
  """
  ignored: Boolean
}

input repos_replace_all_topics_request_Input {
  """
  An array of topics to add to the repository. Pass one or more topics to _replace_ the set of existing topics. Send an empty array (`[]`) to clear all topics from the repository. **Note:** Topic `names` will be saved as lowercase.
  """
  names: [String]!
}

input repos_transfer_request_Input {
  """
  The username or organization name the repository will be transferred to.
  """
  new_owner: String!

  """The new name to be given to the repository."""
  new_name: String

  """
  ID of the team or teams to add to the repository. Teams can only be added to organization-owned repositories.
  """
  team_ids: [Int]
}

input repos_create_using_template_request_Input {
  """
  The organization or person who will own the new repository. To create a new repository in an organization, the authenticated user must be a member of the specified organization.
  """
  owner: String

  """The name of the new repository."""
  name: String!

  """A short description of the new repository."""
  description: String

  """
  Set to `true` to include the directory structure and files from all branches in the template repository, and not just the default branch. Default: `false`.
  """
  include_all_branches: Boolean

  """
  Either `true` to create a new private repository or `false` to create a new public one.
  """
  private: Boolean
}

input issues_add_issue_field_values_request_Input {
  """
  An array of issue field values to add to this issue. Each field value must include the field ID and the value to set.
  """
  issue_field_values: [mutationInput_issues_add_issue_field_values_input_issue_field_values_items_Input]
}

input mutationInput_issues_add_issue_field_values_input_issue_field_values_items_Input {
  """The ID of the issue field to set"""
  field_id: Int!

  """
  The value to set for the field. The type depends on the field's data type:
  - For text fields: provide a string value
  - For single_select fields: provide the option name as a string (must match an existing option)
  - For number fields: provide a numeric value
  - For date fields: provide an ISO 8601 date string
  """
  value: String!
}

input issues_set_issue_field_values_request_Input {
  """
  An array of issue field values to set for this issue. Each field value must include the field ID and the value to set. All existing field values will be replaced.
  """
  issue_field_values: [mutationInput_issues_set_issue_field_values_input_issue_field_values_items_Input]
}

input mutationInput_issues_set_issue_field_values_input_issue_field_values_items_Input {
  """The ID of the issue field to set"""
  field_id: Int!

  """
  The value to set for the field. The type depends on the field's data type:
  - For text fields: provide a string value
  - For single_select fields: provide the option name as a string (must match an existing option)
  - For number fields: provide a numeric value
  - For date fields: provide an ISO 8601 date string
  """
  value: String!
}

input teams_update_legacy_request_Input {
  """The name of the team."""
  name: String!

  """The description of the team."""
  description: String
  privacy: mutationInput_teams_update_legacy_input_privacy
  notification_setting: mutationInput_teams_update_legacy_input_notification_setting
  permission: mutationInput_teams_update_legacy_input_permission = pull

  """The ID of a team to set as the parent team."""
  parent_team_id: Int
}

"""
The level of privacy this team should have. Editing teams without specifying this parameter leaves `privacy` intact. The options are:  
**For a non-nested team:**  
 * `secret` - only visible to organization owners and members of this team.  
 * `closed` - visible to all members of this organization.  
**For a parent or child team:**  
 * `closed` - visible to all members of this organization.
"""
enum mutationInput_teams_update_legacy_input_privacy {
  secret
  closed
}

"""
The notification setting the team has chosen. Editing teams without specifying this parameter leaves `notification_setting` intact. The options are: 
 * `notifications_enabled` - team members receive notifications when the team is @mentioned.  
 * `notifications_disabled` - no one receives notifications.
"""
enum mutationInput_teams_update_legacy_input_notification_setting {
  notifications_enabled
  notifications_disabled
}

"""
**Closing down notice**. The permission that new repositories will be added to the team with when none is specified.
"""
enum mutationInput_teams_update_legacy_input_permission {
  pull
  push
  admin
}

input teams_add_or_update_membership_for_user_legacy_request_Input {
  role: mutationInput_teams_add_or_update_membership_for_user_legacy_input_role = member
}

"""The role that this user should have in the team."""
enum mutationInput_teams_add_or_update_membership_for_user_legacy_input_role {
  member
  maintainer
}

input teams_add_or_update_repo_permissions_legacy_request_Input {
  permission: mutationInput_teams_add_or_update_repo_permissions_legacy_input_permission
}

"""
The permission to grant the team on this repository. If no permission is specified, the team's `permission` attribute will be used to determine what permission to grant the team on this repository.
"""
enum mutationInput_teams_add_or_update_repo_permissions_legacy_input_permission {
  pull
  push
  admin
}

input users_update_authenticated_request_Input {
  """The new name of the user."""
  name: String

  """The publicly visible email address of the user."""
  email: EmailAddress

  """The new blog URL of the user."""
  blog: String

  """The new Twitter username of the user."""
  twitter_username: String

  """The new company of the user."""
  company: String

  """The new location of the user."""
  location: String

  """The new hiring availability of the user."""
  hireable: Boolean

  """The new short biography of the user."""
  bio: String
}

input codespaces_create_for_authenticated_user_request_Input {
  mutationInput_codespaces_create_for_authenticated_user_input_oneOf_0_Input: mutationInput_codespaces_create_for_authenticated_user_input_oneOf_0_Input
  mutationInput_codespaces_create_for_authenticated_user_input_oneOf_1_Input: mutationInput_codespaces_create_for_authenticated_user_input_oneOf_1_Input
}

input mutationInput_codespaces_create_for_authenticated_user_input_oneOf_0_Input {
  """Repository id for this codespace"""
  repository_id: Int!

  """Git ref (typically a branch name) for this codespace"""
  ref: String

  """
  The requested location for a new codespace. Best efforts are made to respect this upon creation. Assigned by IP if not provided.
  """
  location: String
  geo: mutationInput_codespaces_create_for_authenticated_user_input_oneOf_0_geo

  """IP for location auto-detection when proxying a request"""
  client_ip: String

  """Machine type to use for this codespace"""
  machine: String

  """Path to devcontainer.json config to use for this codespace"""
  devcontainer_path: String

  """Whether to authorize requested permissions from devcontainer.json"""
  multi_repo_permissions_opt_out: Boolean

  """Working directory for this codespace"""
  working_directory: String

  """Time in minutes before codespace stops from inactivity"""
  idle_timeout_minutes: Int

  """Display name for this codespace"""
  display_name: String

  """
  Duration in minutes after codespace has gone idle in which it will be deleted. Must be integer minutes between 0 and 43200 (30 days).
  """
  retention_period_minutes: Int
}

"""
The geographic area for this codespace. If not specified, the value is assigned by IP. This property replaces `location`, which is closing down.
"""
enum mutationInput_codespaces_create_for_authenticated_user_input_oneOf_0_geo {
  EuropeWest
  SoutheastAsia
  UsEast
  UsWest
}

input mutationInput_codespaces_create_for_authenticated_user_input_oneOf_1_Input {
  pull_request: mutationInput_codespaces_create_for_authenticated_user_input_oneOf_1_pull_request_Input!

  """
  The requested location for a new codespace. Best efforts are made to respect this upon creation. Assigned by IP if not provided.
  """
  location: String
  geo: mutationInput_codespaces_create_for_authenticated_user_input_oneOf_1_geo

  """Machine type to use for this codespace"""
  machine: String

  """Path to devcontainer.json config to use for this codespace"""
  devcontainer_path: String

  """Working directory for this codespace"""
  working_directory: String

  """Time in minutes before codespace stops from inactivity"""
  idle_timeout_minutes: Int
}

"""Pull request number for this codespace"""
input mutationInput_codespaces_create_for_authenticated_user_input_oneOf_1_pull_request_Input {
  """Pull request number"""
  pull_request_number: Int!

  """Repository id for this codespace"""
  repository_id: Int!
}

"""
The geographic area for this codespace. If not specified, the value is assigned by IP. This property replaces `location`, which is closing down.
"""
enum mutationInput_codespaces_create_for_authenticated_user_input_oneOf_1_geo {
  EuropeWest
  SoutheastAsia
  UsEast
  UsWest
}

union codespaces_create_or_update_secret_for_authenticated_user_response = JSON_container | Void_container

input codespaces_create_or_update_secret_for_authenticated_user_request_Input {
  encrypted_value: mutationInput_codespaces_create_or_update_secret_for_authenticated_user_input_encrypted_value

  """ID of the key you used to encrypt the secret."""
  key_id: String!

  """
  An array of repository ids that can access the user secret. You can manage the list of selected repositories using the [List selected repositories for a user secret](https://docs.github.com/rest/codespaces/secrets#list-selected-repositories-for-a-user-secret), [Set selected repositories for a user secret](https://docs.github.com/rest/codespaces/secrets#set-selected-repositories-for-a-user-secret), and [Remove a selected repository from a user secret](https://docs.github.com/rest/codespaces/secrets#remove-a-selected-repository-from-a-user-secret) endpoints.
  """
  selected_repository_ids: [JSON]
}

scalar mutationInput_codespaces_create_or_update_secret_for_authenticated_user_input_encrypted_value

input codespaces_set_repositories_for_secret_for_authenticated_user_request_Input {
  """
  An array of repository ids for which a codespace can access the secret. You can manage the list of selected repositories using the [List selected repositories for a user secret](https://docs.github.com/rest/codespaces/secrets#list-selected-repositories-for-a-user-secret), [Add a selected repository to a user secret](https://docs.github.com/rest/codespaces/secrets#add-a-selected-repository-to-a-user-secret), and [Remove a selected repository from a user secret](https://docs.github.com/rest/codespaces/secrets#remove-a-selected-repository-from-a-user-secret) endpoints.
  """
  selected_repository_ids: [Int]!
}

input codespaces_update_for_authenticated_user_request_Input {
  """A valid machine to transition this codespace to."""
  machine: String

  """Display name for this codespace"""
  display_name: String

  """
  Recently opened folders inside the codespace. It is currently used by the clients to determine the folder path to load the codespace in.
  """
  recent_folders: [String]
}

"""A codespace."""
type codespace_with_full_repository {
  id: BigInt!

  """Automatically generated name of this codespace."""
  name: String!

  """Display name for this codespace."""
  display_name: String

  """UUID identifying this codespace's environment."""
  environment_id: String
  owner: simple_user!
  billable_owner: simple_user!
  repository: Full_Repository!
  machine: nullable_codespace_machine

  """Path to devcontainer.json from repo root used to create Codespace."""
  devcontainer_path: String

  """Whether the codespace was created from a prebuild."""
  prebuild: Boolean
  created_at: DateTime!
  updated_at: DateTime!

  """Last known time this codespace was started."""
  last_used_at: DateTime!
  state: mutation_codespaces_publish_for_authenticated_user_state!

  """API URL for this codespace."""
  url: URL!
  git_status: mutation_codespaces_publish_for_authenticated_user_git_status!
  location: mutation_codespaces_publish_for_authenticated_user_location!

  """
  The number of minutes of inactivity after which this codespace will be automatically stopped.
  """
  idle_timeout_minutes: Int

  """URL to access this codespace on the web."""
  web_url: URL!

  """
  API URL to access available alternate machine types for this codespace.
  """
  machines_url: URL!

  """API URL to start this codespace."""
  start_url: URL!

  """API URL to stop this codespace."""
  stop_url: URL!

  """API URL to publish this codespace to a new repository."""
  publish_url: URL

  """API URL for the Pull Request associated with this codespace, if any."""
  pulls_url: URL
  recent_folders: [String]!
  runtime_constraints: mutation_codespaces_publish_for_authenticated_user_runtime_constraints

  """
  Whether or not a codespace has a pending async operation. This would mean that the codespace is temporarily unavailable. The only thing that you can do with a codespace in this state is delete it.
  """
  pending_operation: Boolean

  """Text to show user when codespace is disabled by a pending operation"""
  pending_operation_disabled_reason: String

  """
  Text to show user when codespace idle timeout minutes has been overriden by an organization policy
  """
  idle_timeout_notice: String

  """
  Duration in minutes after codespace has gone idle in which it will be deleted. Must be integer minutes between 0 and 43200 (30 days).
  """
  retention_period_minutes: Int

  """
  When a codespace will be auto-deleted based on the "retention_period_minutes" and "last_used_at"
  """
  retention_expires_at: DateTime
}

"""State of this codespace."""
enum mutation_codespaces_publish_for_authenticated_user_state {
  Unknown
  Created
  Queued
  Provisioning
  Available
  Awaiting
  Unavailable
  Deleted
  Moved
  Shutdown
  Archived
  Starting
  ShuttingDown
  Failed
  Exporting
  Updating
  Rebuilding
}

"""Details about the codespace's git repository."""
type mutation_codespaces_publish_for_authenticated_user_git_status {
  """The number of commits the local repository is ahead of the remote."""
  ahead: Int

  """The number of commits the local repository is behind the remote."""
  behind: Int

  """Whether the local repository has unpushed changes."""
  has_unpushed_changes: Boolean

  """Whether the local repository has uncommitted changes."""
  has_uncommitted_changes: Boolean

  """
  The current branch (or SHA if in detached HEAD state) of the local repository.
  """
  ref: String
}

"""The initally assigned location of a new codespace."""
enum mutation_codespaces_publish_for_authenticated_user_location {
  EastUs
  SouthEastAsia
  WestEurope
  WestUs2
}

type mutation_codespaces_publish_for_authenticated_user_runtime_constraints {
  """The privacy settings a user can select from when forwarding a port."""
  allowed_port_privacy_settings: [String]
}

input codespaces_publish_for_authenticated_user_request_Input {
  """A name for the new repository."""
  name: String

  """Whether the new repository should be private."""
  private: Boolean
}

input users_set_primary_email_visibility_for_authenticated_user_request_Input {
  visibility: mutationInput_users_set_primary_email_visibility_for_authenticated_user_input_visibility!
}

"""Denotes whether an email is publicly visible."""
enum mutationInput_users_set_primary_email_visibility_for_authenticated_user_input_visibility {
  public
  private
}

input users_add_email_for_authenticated_user_request_Input {
  mutationInput_users_add_email_for_authenticated_user_input_oneOf_0_Input: mutationInput_users_add_email_for_authenticated_user_input_oneOf_0_Input
  EmailAddress: EmailAddress
  String: String
}

input mutationInput_users_add_email_for_authenticated_user_input_oneOf_0_Input {
  """
  Adds one or more email addresses to your GitHub account. Must contain at least one email address. **Note:** Alternatively, you can pass a single email address or an `array` of emails addresses directly, but we recommend that you pass an object using the `emails` key.
  """
  emails: [EmailAddress]!
}

input users_delete_email_for_authenticated_user_request_Input {
  mutationInput_users_delete_email_for_authenticated_user_input_oneOf_0_Input: mutationInput_users_delete_email_for_authenticated_user_input_oneOf_0_Input
  EmailAddress: EmailAddress
  String: String
}

"""
Deletes one or more email addresses from your GitHub account. Must contain at least one email address. **Note:** Alternatively, you can pass a single email address or an `array` of emails addresses directly, but we recommend that you pass an object using the `emails` key.
"""
input mutationInput_users_delete_email_for_authenticated_user_input_oneOf_0_Input {
  """Email addresses associated with the GitHub user account."""
  emails: [EmailAddress]!
}

input users_create_gpg_key_for_authenticated_user_request_Input {
  """A descriptive name for the new key."""
  name: String

  """A GPG key in ASCII-armored format."""
  armored_public_key: String!
}

input users_create_public_ssh_key_for_authenticated_user_request_Input {
  """A descriptive name for the new key."""
  title: String
  key: mutationInput_users_create_public_ssh_key_for_authenticated_user_input_key!
}

scalar mutationInput_users_create_public_ssh_key_for_authenticated_user_input_key

input orgs_update_membership_for_authenticated_user_request_Input {
  state: active_const!
}

enum active_const {
  active
}

input migrations_start_for_authenticated_user_request_Input {
  """Lock the repositories being migrated at the start of the migration"""
  lock_repositories: Boolean

  """
  Indicates whether metadata should be excluded and only git source should be included for the migration.
  """
  exclude_metadata: Boolean

  """
  Indicates whether the repository git data should be excluded from the migration.
  """
  exclude_git_data: Boolean

  """Do not include attachments in the migration"""
  exclude_attachments: Boolean

  """Do not include releases in the migration"""
  exclude_releases: Boolean

  """
  Indicates whether projects owned by the organization or users should be excluded.
  """
  exclude_owner_projects: Boolean

  """
  Indicates whether this should only include organization metadata (repositories array should be empty and will ignore other flags).
  """
  org_metadata_only: Boolean

  """Exclude attributes from the API response to improve performance"""
  exclude: [repositories_const]
  repositories: [String]!
}

input repos_create_for_authenticated_user_request_Input {
  """The name of the repository."""
  name: String!

  """A short description of the repository."""
  description: String

  """A URL with more information about the repository."""
  homepage: String

  """Whether the repository is private."""
  private: Boolean

  """Whether issues are enabled."""
  has_issues: Boolean = true

  """Whether projects are enabled."""
  has_projects: Boolean = true

  """Whether the wiki is enabled."""
  has_wiki: Boolean = true

  """Whether discussions are enabled."""
  has_discussions: Boolean

  """
  The id of the team that will be granted access to this repository. This is only valid when creating a repository in an organization.
  """
  team_id: Int

  """Whether the repository is initialized with a minimal README."""
  auto_init: Boolean

  """The desired language or platform to apply to the .gitignore."""
  gitignore_template: String

  """The license keyword of the open source license for this repository."""
  license_template: String

  """Whether to allow squash merges for pull requests."""
  allow_squash_merge: Boolean = true

  """Whether to allow merge commits for pull requests."""
  allow_merge_commit: Boolean = true

  """Whether to allow rebase merges for pull requests."""
  allow_rebase_merge: Boolean = true

  """Whether to allow Auto-merge to be used on pull requests."""
  allow_auto_merge: Boolean

  """Whether to delete head branches when pull requests are merged"""
  delete_branch_on_merge: Boolean
  squash_merge_commit_title: mutationInput_repos_create_for_authenticated_user_input_squash_merge_commit_title
  squash_merge_commit_message: mutationInput_repos_create_for_authenticated_user_input_squash_merge_commit_message
  merge_commit_title: mutationInput_repos_create_for_authenticated_user_input_merge_commit_title
  merge_commit_message: mutationInput_repos_create_for_authenticated_user_input_merge_commit_message

  """Whether downloads are enabled."""
  has_downloads: Boolean = true

  """
  Whether this repository acts as a template that can be used to generate new repositories.
  """
  is_template: Boolean
}

"""
Required when using `squash_merge_commit_message`.

The default value for a squash merge commit title:

- `PR_TITLE` - default to the pull request's title.
- `COMMIT_OR_PR_TITLE` - default to the commit's title (if only one commit) or the pull request's title (when more than one commit).
"""
enum mutationInput_repos_create_for_authenticated_user_input_squash_merge_commit_title {
  PR_TITLE
  COMMIT_OR_PR_TITLE
}

"""
The default value for a squash merge commit message:

- `PR_BODY` - default to the pull request's body.
- `COMMIT_MESSAGES` - default to the branch's commit messages.
- `BLANK` - default to a blank commit message.
"""
enum mutationInput_repos_create_for_authenticated_user_input_squash_merge_commit_message {
  PR_BODY
  COMMIT_MESSAGES
  BLANK
}

"""
Required when using `merge_commit_message`.

The default value for a merge commit title.

- `PR_TITLE` - default to the pull request's title.
- `MERGE_MESSAGE` - default to the classic title for a merge message (e.g., Merge pull request #123 from branch-name).
"""
enum mutationInput_repos_create_for_authenticated_user_input_merge_commit_title {
  PR_TITLE
  MERGE_MESSAGE
}

"""
The default value for a merge commit message.

- `PR_TITLE` - default to the pull request's title.
- `PR_BODY` - default to the pull request's body.
- `BLANK` - default to a blank commit message.
"""
enum mutationInput_repos_create_for_authenticated_user_input_merge_commit_message {
  PR_BODY
  PR_TITLE
  BLANK
}

input users_add_social_account_for_authenticated_user_request_Input {
  """Full URLs for the social media profiles to add."""
  account_urls: [URL]!
}

input users_delete_social_account_for_authenticated_user_request_Input {
  """Full URLs for the social media profiles to delete."""
  account_urls: [URL]!
}

input users_create_ssh_signing_key_for_authenticated_user_request_Input {
  """A descriptive name for the new key."""
  title: String
  key: mutationInput_users_create_ssh_signing_key_for_authenticated_user_input_key!
}

scalar mutationInput_users_create_ssh_signing_key_for_authenticated_user_input_key

input projects_create_draft_item_for_authenticated_user_request_Input {
  """The title of the draft issue item to create in the project."""
  title: String!

  """The body content of the draft issue item to create in the project."""
  body: String
}

input projects_create_view_for_user_request_Input {
  """The name of the view."""
  name: String!
  layout: mutationInput_projects_create_view_for_user_input_layout!

  """
  The filter query for the view. See [Filtering projects](https://docs.github.com/issues/planning-and-tracking-with-projects/customizing-views-in-your-project/filtering-projects) for more information.
  """
  filter: String

  """
  `visible_fields` is not applicable to `roadmap` layout views.
  For `table` and `board` layouts, this represents the field IDs that should be visible in the view. If not provided, the default visible fields will be used.
  """
  visible_fields: [Int]
}

"""The layout of the view."""
enum mutationInput_projects_create_view_for_user_input_layout {
  table
  board
  roadmap
}

type users_list_attestations_bulk_200_response {
  """Mapping of subject digest to bundles."""
  attestations_subject_digests: JSON
  page_info: mutation_users_list_attestations_bulk_page_info
}

"""Information about the current page."""
type mutation_users_list_attestations_bulk_page_info {
  """Indicates whether there is a next page."""
  has_next: Boolean

  """Indicates whether there is a previous page."""
  has_previous: Boolean

  """The cursor to the next page."""
  next: String

  """The cursor to the previous page."""
  previous: String
}

input users_list_attestations_bulk_request_Input {
  """List of subject digests to fetch attestations for."""
  subject_digests: [String]!

  """
  Optional filter for fetching attestations with a given predicate type.
  This option accepts `provenance`, `sbom`, `release`, or freeform text
  for custom predicate types.
  """
  predicate_type: String
}

input users_delete_attestations_bulk_request_Input {
  mutationInput_users_delete_attestations_bulk_input_oneOf_0_Input: mutationInput_users_delete_attestations_bulk_input_oneOf_0_Input
  mutationInput_users_delete_attestations_bulk_input_oneOf_1_Input: mutationInput_users_delete_attestations_bulk_input_oneOf_1_Input
}

input mutationInput_users_delete_attestations_bulk_input_oneOf_0_Input {
  """
  List of subject digests associated with the artifact attestations to delete.
  """
  subject_digests: [String]!
}

input mutationInput_users_delete_attestations_bulk_input_oneOf_1_Input {
  """
  List of unique IDs associated with the artifact attestations to delete.
  """
  attestation_ids: [Int]!
}

input projects_add_field_for_user_request_Input {
  mutationInput_projects_add_field_for_user_input_oneOf_0_Input: mutationInput_projects_add_field_for_user_input_oneOf_0_Input
  mutationInput_projects_add_field_for_user_input_oneOf_1_Input: mutationInput_projects_add_field_for_user_input_oneOf_1_Input
  mutationInput_projects_add_field_for_user_input_oneOf_2_Input: mutationInput_projects_add_field_for_user_input_oneOf_2_Input
}

input mutationInput_projects_add_field_for_user_input_oneOf_0_Input {
  """The name of the field."""
  name: String!
  data_type: mutationInput_projects_add_field_for_user_input_oneOf_0_data_type!
}

"""The field's data type."""
enum mutationInput_projects_add_field_for_user_input_oneOf_0_data_type {
  text
  number
  date
}

input mutationInput_projects_add_field_for_user_input_oneOf_1_Input {
  """The name of the field."""
  name: String!
  data_type: single_select_const!

  """
  The options available for single select fields. At least one option must be provided when creating a single select field.
  """
  single_select_options: [projects_v2_field_single_select_option_Input]!
}

input mutationInput_projects_add_field_for_user_input_oneOf_2_Input {
  """The name of the field."""
  name: String!
  data_type: iteration_const!
  iteration_configuration: projects_v2_field_iteration_configuration_Input!
}

input projects_add_item_for_user_request_Input {
  type: mutationInput_projects_add_item_for_user_input_type!

  """
  The unique identifier of the issue or pull request to add to the project.
  """
  id: Int

  """The repository owner login."""
  owner: String

  """The repository name."""
  repo: String

  """The issue or pull request number."""
  number: Int
}

"""
The type of item to add to the project. Must be either Issue or PullRequest.
"""
enum mutationInput_projects_add_item_for_user_input_type {
  Issue
  PullRequest
}

input projects_update_item_for_user_request_Input {
  """A list of field updates to apply."""
  fields: [mutationInput_projects_update_item_for_user_input_fields_items_Input]!
}

input mutationInput_projects_update_item_for_user_input_fields_items_Input {
  """The ID of the project field to update."""
  id: Int!
  value: mutationInput_projects_update_item_for_user_input_fields_items_value_Input
}

input mutationInput_projects_update_item_for_user_input_fields_items_value_Input {
  String: String
  Float: Float
}

scalar ObjMap

enum HTTPMethod {
  GET
  HEAD
  POST
  PUT
  DELETE
  CONNECT
  OPTIONS
  TRACE
  PATCH
}