PluginAvailableclawql-harnessPlugins · Registry · Plugin modelOn this pageMCP toolsRegistration and lineageLearn moreOuroboros

Plugin ID: clawql-harness (Ouroboros harness)

Packages: packages/clawql-harness — createOuroborosHarnessPlugin / makeHarnessLayer; library packages/clawql-ouroboros

Specification-first evolutionary loops with optional Postgres lineage storage. In clawql-mcp, tools always register through the harness — CLAWQL_ENABLE_OUROBOROS is deprecated as a registration gate.

MCP tools

ToolPurposeclawql_thinkStructured planning / reflection helperouroboros_create_seed_from_documentCreate a Seed from an input documentouroboros_run_evolutionary_loopRun Wonder/Reflect → Executor/Evaluator cyclesouroboros_get_lineage_statusInspect lineage and convergence stateouroboros_measure_drift3-component drift vs root Seed (#557)

Registration and lineage

Env / HelmDefaultEffect(always on)—makeHarnessLayer(createOuroborosHarnessPlugin) registers the tools aboveCLAWQL*ENABLE_OUROBOROS(deprecated)No longer gates MCP registration; ignore for tool loadCLAWQL_OUROBOROS_DATABASE_URL or CLAWQL_OUROBOROS_DB*\*unset = in-memoryDurable Postgres events (clawql_ouroboros_events)CLAWQL_ENABLE_LANGFUSE_EVAL=1offLangfuse eval → ouroboros_propose_seed_revision_from_eval

Do not teach clawql-ouroboros/mcp-hooks as the clawql-mcp registration path (OK for embedding the library in your own MCP host).

Learn more

Ouroboros library

Ouroboros tools walkthrough

clawql-ouroboros.md
