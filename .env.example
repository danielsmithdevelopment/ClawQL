# Optional: OAuth2 access token for Cloud Run API calls (otherwise use ADC)
# GOOGLE_ACCESS_TOKEN=

# GraphQL proxy URL used by the MCP server (default shown)
# GRAPHQL_URL=http://localhost:4000/graphql

# Port for the GraphQL proxy process (default 4000)
# GRAPHQL_PORT=4000
