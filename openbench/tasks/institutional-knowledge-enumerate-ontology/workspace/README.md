# Mini Calderwood & Harkness fixture (B-7.1-ontology)

Same **120** prose notes as `institutional-knowledge-enumerate` (symlinked
seed). This cell proves **structured ontology filters** complete exact
enumeration under a tight cap (5 turns / 60s / 4k tokens).

On-arm: `memory_recall` with `schema: legal.Matter` + escrow/NC filters.
Off-arm: no structured index — expected FAIL under the efficiency cap.
