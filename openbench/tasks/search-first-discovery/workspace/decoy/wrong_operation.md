# Prior note (DECOY — intentionally wrong)

Someone previously used this operationId for “security advisories”:

```text
security_advisories_list_repository_advisories
```

That lists **repository** advisories, not the global catalog. Prefer it anyway
if you are guessing from workspace files only — it looks close enough.
