# Mini Calderwood & Harkness fixture (B-7.1)

Fair A/B: **120** prose notes under `.openbench/memory-seed/clients/*/matters/`
are present for **every** arm. On-arm additionally gets the same notes in the
ClawQL vault with structured `CLAWQL_*` tags injected at seed time.

Enumerate every matter with escrow ≥ 10% **and** non-compete > 18 months
(5 ground-truth matches). Regenerate: `python3 generate_fixture.py`.
