# B-7.2 — Client preference reconstruction

Fair same-files cell: identical prose notes under `.openbench/memory-seed/` for
all arms. On-arm additionally receives vault `CLAWQL_*` client↔matter links.

Write `preference.json` with Meridian Capital's top-1 term-sheet choice.
