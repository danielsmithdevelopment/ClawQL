/** @deprecated Search index is built by scripts/generate-search-index.mjs — this export is a no-op for next.config compatibility. */
export default function Search(nextConfig = {}) {
  return nextConfig
}
