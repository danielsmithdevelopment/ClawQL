import { Button } from '@/components/Button'
import { Note } from '@/components/mdx'
import { OptionalToolsHubGrid } from '@/components/DocsHubSections'

# Optional MCP tools

**ClawQL Core** always registers **`search`**, **`execute`**, **`audit`**, and **`cache`**. The tools below are optional — enable via **`CLAWQL_ENABLE_*`** env vars. Start with the **Learn walkthrough** for when to use each tool and composed recipes; use the cards below for deep dives. {{ className: 'lead' }}

<div className="not-prose my-8 flex flex-wrap gap-3">
  <Button href="/learn/optional-mcp-tools" arrow="right">
    <>Optional tools walkthrough</>
  </Button>
  <Button href="/tools" variant="outline">
    <>Full tool reference</>
  </Button>
</div>

<Note>
  Prefer the unified [MCP tool reference](/tools) for parameters and behavior.
  The [Learn walkthrough](/learn/optional-mcp-tools) teaches workflows; these
  cards link to per-tool deep dives.
</Note>

<OptionalToolsHubGrid />
