import { Guides } from '@/components/Guides'
import { Resources } from '@/components/Resources'
import { HeroPattern } from '@/components/HeroPattern'
import { homePageSections } from '@/lib/home-page-sections'
import { docsPageMetadata } from '@/lib/seo'

export const metadata = docsPageMetadata({
  title: 'ClawQL documentation',
  description:
    'ClawQL documentation: MCP server for OpenAPI 3, Swagger 2, and Google Discovery—search and execute, optional sandbox, vault memory, schedule synthetics, notify, Onyx knowledge_search_onyx, Ouroboros tools, internal GraphQL layer, stdio or HTTP or gRPC, Docker and Kubernetes deployment guides.',
  path: '/',
})

export const sections = homePageSections

<HeroPattern />

# ClawQL documentation

**ClawQL** registers **core** tools **`search`** and **`execute`**, **`audit`** and **`cache`** (always on), plus **`sandbox_exec`** (remote code in a Cloudflare Sandbox via bridge), **`ingest_external_knowledge`** (bulk Markdown + optional URL fetch), and vault **`memory_ingest`** / **`memory_recall`** by default (set **`CLAWQL_ENABLE_MEMORY=0`** to opt out of vault memory tools). An internal **GraphQL** projection layer keeps API responses lean for **OpenAPI 3**, **Swagger 2**, and **Google Discovery**. The same tool set is available over **stdio**, **Streamable HTTP**, and (when enabled) **gRPC** via the reusable **[`mcp-grpc-transport`](https://www.npmjs.com/package/mcp-grpc-transport)** package—see [Deployment](/deployment) and [Kubernetes](/kubernetes). [Full tool reference (repo)](https://github.com/danielsmithdevelopment/ClawQL/blob/main/docs/mcp-tools.md). {{ className: 'lead' }}

**`audit`** and **`cache`** (**ClawQL Core**, no opt-out) are **in-process** — Core audit ring buffer vs LRU scratch — not the vault — **[enterprise-mcp-tools.md](https://github.com/danielsmithdevelopment/ClawQL/blob/main/docs/enterprise-mcp-tools.md)** ([#89](https://github.com/danielsmithdevelopment/ClawQL/issues/89)), [Session cache](/cache), [cache-tool.md](https://github.com/danielsmithdevelopment/ClawQL/blob/main/docs/cache-tool.md). Optional **`schedule`** (**`CLAWQL_ENABLE_SCHEDULE`**) adds persisted jobs and synthetic checks — [Schedule synthetic checks](/schedule), [schedule-synthetic-checks.md](https://github.com/danielsmithdevelopment/ClawQL/blob/main/docs/schedule-synthetic-checks.md) ([#76](https://github.com/danielsmithdevelopment/ClawQL/issues/76)). Optional **`notify`** (**`CLAWQL_ENABLE_NOTIFY`**) posts to Slack — [Slack notify](/notify), [notify-tool.md](https://github.com/danielsmithdevelopment/ClawQL/blob/main/docs/notify-tool.md) ([#77](https://github.com/danielsmithdevelopment/ClawQL/issues/77)). Optional **`knowledge_search_onyx`** (**`CLAWQL_ENABLE_ONYX`**) queries **[Onyx](https://www.onyx.app/)** document search — [Onyx knowledge search](/onyx-knowledge), [onyx-knowledge-tool.md](https://github.com/danielsmithdevelopment/ClawQL/blob/main/docs/onyx-knowledge-tool.md) ([#118](https://github.com/danielsmithdevelopment/ClawQL/issues/118)). Optional **`ouroboros_*`** tools (**`CLAWQL_ENABLE_OUROBOROS`**) embed the **[Ouroboros library](/ouroboros)** evolutionary loop, with durable Postgres lineage via **`CLAWQL_OUROBOROS_DATABASE_URL`** (or split **`CLAWQL_OUROBOROS_DB_*`**) — [clawql-ouroboros.md](https://github.com/danielsmithdevelopment/ClawQL/blob/main/docs/clawql-ouroboros.md) ([#141](https://github.com/danielsmithdevelopment/ClawQL/issues/141), [#142](https://github.com/danielsmithdevelopment/ClawQL/issues/142)).

<div className="not-prose mt-6 mb-16 flex gap-3">
  <Button href="/quickstart" arrow="right">
    <>Quickstart</>
  </Button>
  <Button href="/install" variant="outline">
    <>Install</>
  </Button>
</div>

## Getting started {{ anchor: false }}

Install **`clawql-mcp`**, run it on stdio, then point your MCP client (e.g. Cursor or Claude Desktop) at the server. Optionally set **`CLAWQL_SPEC_PATH`**, **`CLAWQL_SPEC_URL`**, or **`CLAWQL_DISCOVERY_URL`**; if you set nothing, ClawQL loads a bundled **multi-provider** merge (see [spec configuration](/spec-configuration) — default **`all-providers`** includes Google Cloud, Cloudflare, GitHub, Slack, document APIs, Onyx, and more). {{ className: 'lead' }}

<div className="not-prose">
  <Button href="/mcp-clients" variant="text" arrow="right">
    <>Configure your MCP client</>
  </Button>
</div>

<Guides />

<Resources />
