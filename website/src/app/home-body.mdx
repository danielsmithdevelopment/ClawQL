import { HeroPattern } from '@/components/HeroPattern'
import { HomeMarketingSections } from '@/components/HomeMarketingSections'
import { Button } from '@/components/Button'

<HeroPattern />

# ClawQL documentation

**ClawQL** is the secure, persistent MCP gateway for production agents — **`search`** and **`execute`** across OpenAPI, Discovery, and optional GraphQL/gRPC sources, with always-on **`audit`** and **`cache`**, optional vault memory, and defense-in-depth controls. {{ className: 'lead' }}

<div className="not-prose mt-6 mb-10 flex flex-wrap gap-3">
  <Button href="/agent-setup" arrow="right">
    <>Agent setup (recommended)</>
  </Button>
  <Button href="/getting-started" variant="outline">
    <>Getting started</>
  </Button>
  <Button href="/quickstart" variant="outline">
    <>Quickstart</>
  </Button>
  <Button href="/resources/migration" variant="outline">
    <>Migration (7.0)</>
  </Button>
  <Button href="/architecture" variant="outline">
    <>Architecture</>
  </Button>
  <Button href="/deployment" variant="outline">
    <>Deployment</>
  </Button>
</div>

## Agent-first operating system

Everything flows through one intelligent gateway. Disabled components cost nothing. Official artifacts verify against **Layer 0 immutable releases**. Start with the [Vision & Roadmap](/vision/roadmap), then the [Master enablement guide](/vision/technical-enablement) for the full 6-layer architecture.

<div className="not-prose mt-4">
  <Button href="/vision/roadmap" variant="text" arrow="right">
    <>Vision & Roadmap</>
  </Button>
</div>

<HomeMarketingSections />
