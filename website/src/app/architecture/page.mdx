import { Button } from '@/components/Button'
import { Note } from '@/components/mdx'
import { ArchitectureHubGrid } from '@/components/DocsHubSections'

# Architecture & Vision

ClawQL is an **agent-first operating system** — not another prompt library. Use this hub for platform shape; use [Vision & status](/vision/roadmap) for what actually ships. {{ className: 'lead' }}

<Note>
  **Status:** [Vision & roadmap](/vision/roadmap). **Run it:**
  [Quickstart](/quickstart) · [Agent setup](/agent-setup) ·
  [Operations](/deployment/operations-guide).
</Note>

<div className="not-prose my-8 flex flex-wrap gap-3">
  <Button href="/vision/roadmap" arrow="right">
    <>Vision & status</>
  </Button>
  <Button href="/concepts" variant="outline">
    <>Concepts</>
  </Button>
  <Button href="/plugins" variant="outline">
    <>Plugins</>
  </Button>
</div>

## Platform topics

Per-plugin docs live on the [Plugins hub](/plugins). Document pipeline detail is under [Learn — document pipeline](/learn/document-pipeline) with product context on [IDP platform](/vision/idp-platform).

<ArchitectureHubGrid />
