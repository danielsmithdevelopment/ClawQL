import type { Section } from '@/components/SectionProvider'

/** Sidebar / in-page nav sections for the home doc route (`/`). */
export const homePageSections: Array<Section> = [
  { title: 'Guides', id: 'guides' },
  { title: 'Reference', id: 'reference' },
]
