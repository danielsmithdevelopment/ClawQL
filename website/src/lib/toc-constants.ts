/** Shared TOC thresholds (client + build scripts). */

/** Minimum h2 count before we surface an in-page / sidebar TOC. */
export const TOC_MIN_SECTIONS = 2

/** Above this, OnThisPage starts collapsed. */
export const TOC_COMPACT_THRESHOLD = 20

/** Sidebar in-page links truncate after this many entries. */
export const TOC_SIDEBAR_MAX = 24
